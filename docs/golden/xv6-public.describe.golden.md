# Golden 描述报告：xv6-public

- 审核日期：2026-06-13
- 输入仓库：`data/samples/xv6-public`
- 自动报告：`data/reports/describe/xv6-public.md`
- 审核方式：Codex 按人工审核口径逐项核对源码证据
- 人工审核结论：通过

## 自动报告基线

自动描述报告给出 `xv6-public` A 级成熟度，七个 OS 机制维度全部确认。报告 self-check 结果为关键结论 16 条、含证据关键结论 16 条、无效证据引用 0 条、未确认结论 0 条。

该结果可以作为 golden 描述样例，但审核时不直接信任 self-check 分数，而是重新检查每个维度是否有强源码证据支撑。

## 人工校准证据表

| 维度 | 自动结论 | 人工确认的强证据 | 审核结论 | 校准口径 |
| --- | --- | --- | --- | --- |
| 调度与任务管理 | 已确认，high | `proc.c:L315-L352` 定义 `scheduler()`，遍历 `RUNNABLE` 进程，调用 `switchuvm()` 和 `swtch()` 完成上下文切换 | 通过 | 调度维度必须能落到调度循环、进程状态或上下文切换，不能只依赖 `proc.h` 或 include 片段 |
| 内存管理 | 已确认，high | `vm.c:L36-L74` 实现 `walkpgdir()` 和 `mappages()`；`kalloc.c:L83-L95` 实现物理页分配 | 通过 | 页表遍历、映射建立和物理页分配共同支撑内存管理结论 |
| 系统调用 | 已确认，high | `syscall.c:L107-L145` 定义系统调用分发表和 `syscall()` 分发逻辑；`syscall.h:L1-L22` 定义系统调用编号 | 通过 | 需要同时看到编号或分发表，以及从 trap frame 取号并调用内核函数的入口 |
| 文件系统 | 已确认，high | `fs.c:L190-L227` 实现 inode 分配和写回；`fs.c:L453-L494` 实现 `readi()`/`writei()` 文件读写路径 | 通过 | inode、block、read/write 任一强证据可确认文件系统，多个证据组合更稳 |
| 同步机制 | 已确认，high | `spinlock.c:L13-L55` 实现 `initlock()`、`acquire()`、`release()`，并使用原子 `xchg()` | 通过 | 需看到锁结构或 acquire/release 实现，不能只由变量名 `lock` 推断 |
| 中断与异常 | 已确认，high | `trap.c:L37-L70` 处理 syscall、timer、IDE、keyboard、COM1 等 trap/IRQ 分支 | 通过 | 需要真实 trap/IRQ 分发逻辑，硬件常量表只作为辅助 |
| 设备驱动 | 已确认，high | `ide.c:L104-L125` 实现 IDE 中断处理；`kbd.c:L47-L51` 实现键盘中断入口 | 通过 | 设备驱动需要设备 I/O 或中断处理证据，不能只由文件名 `driver` 推断 |

## 人工审核备注

- 自动报告的 self-check 覆盖率为 100%，说明引用路径和行号有效，但不等于每条首选证据都是最强证据。
- 调度维度中，自动报告早期片段包含 `proc.c:L5-L9` 这类 include 附近命中，单独看证据强度偏弱；golden 以 `proc.c:L315-L352` 的 `scheduler()` 函数体作为校准证据。
- 内存、系统调用、文件系统、同步、中断和驱动维度均能找到函数体或核心数据结构，结论可保留 high。
- `xv6-public` 是教学和架构参考样本，不是获奖比赛样本，不能在答辩中包装成获奖案例。

## Golden 期望输出

一份合格的 `xv6-public` 描述报告应满足：

- 说明它是小型教学内核，架构和机制完整；
- 确认调度、内存、系统调用、文件系统、同步、中断、驱动七个维度；
- 每个维度至少给出一个源码路径和行号，且优先引用函数体或结构定义；
- 在报告末尾保留 self-check 摘要；
- 不声称该样本代表比赛获奖水平，也不把证据覆盖率解释为官方评分。

## 不合格输出示例

以下表述不应出现在 golden 级描述报告中：

- “仅根据 `proc.h` 出现 scheduler 字样即可确认完整调度实现。”
- “xv6-public 是优秀获奖作品。”
- “self-check 100% 说明报告完全无误。”
- “检测到文件名 `fs.c`，因此文件系统实现无需进一步证据。”

## 结论

`xv6-public` 描述报告可以作为 KernelSage 的描述 golden 样例。它的价值不是复杂，而是稳定、可解释、证据强，适合校准自动报告是否真正做到“结论能回到源码”。
