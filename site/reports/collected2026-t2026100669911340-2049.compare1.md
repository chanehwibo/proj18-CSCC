# 2026 T2026100669911340 比较报告

- 对比历史仓库：2025 reXvapor
- 生成时间：2026-07-02T09:00:49.323916+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 reXvapor（来源：赛事历史作品）：画像相似度 score=9.72；同属 ucore-variant 风格; 语言构成相似度 0.89; OS 维度重合度 1.00; 代码规模接近度 0.94

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 reXvapor 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/kernel/uart.c:L1-L3`：关键词命中
    代码片段：`/* ns16550 UART 驱动实现 */ #include "uart.h"`
  - `src/kernel/uart.h:L1-L3`：关键词命中
    代码片段：`/* ns16550 UART 驱动 */ #ifndef UUOS_UART_H`
  - `kernel/arch/qemu/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/fs/virtio_disk.c:L1-L4`：关键词命中
    代码片段：`// // driver for qemu's virtio disk device. // uses qemu's mmio interface to virtio. //`
- 与 2025 reXvapor 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/kernel/ext4.c:L158-L162`：关键词命中
    代码片段：`} ext4_bg_desc_64_t; /* Inode 结构 (标准 ext4, 256 字节) */ typedef struct __attribute__((packed)) { uint16_t i_mode;`
  - `src/kernel/ext4.h:L18-L22`：关键词命中
    代码片段：`/* 目录项迭代器 */ typedef struct { uint32_t inode; uint16_t rec_len; uint8_t  name_len;`
  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `kernel/fs/file.c:L154-L158`：关键词命中
    代码片段：`size_t fileread(struct file *f, int user_dst, uint64 addr, size_t n, int64_t off) { size_t r = 0;`
- 与 2025 reXvapor 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/kernel/proc.h:L72-L76`：关键词命中
    代码片段：`uintptr_t ra;                /* 内核返回地址 */ uintptr_t satp;              /* 内核页表 (satp 值) */ uintptr_t trap_frame_addr;   /* trap 帧地址 */ } kernel_context_t;`
  - `src/kernel/syscall.c:L1-L3`：关键词命中
    代码片段：`/* 系统调用处理和 trap 分发 */ #include "syscall.h"`
  - `kernel/sched/trap.c:L115-L119`：关键词命中
    代码片段：`// trampoline已经换栈和页表 // // handle an interrupt, exception, or system call from user space. // called from trampoline.S //`
  - `kernel/arch/qemu/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
- 与 2025 reXvapor 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/kernel/alloc.c:L15-L19`：关键词命中
    代码片段：`#define BLOCK_ALIGN     16  /* 对齐边界 */ static uint8_t  heap_area[8 * 1024 * 1024];  /* 8MB 静态堆 */ static block_t *free_list = NULL; static size_t   heap_size = 0;`
  - `src/kernel/alloc.h:L11-L15`：关键词命中
    代码片段：`/* 分配/释放内存 */ void *kmalloc(size_t size); void  kfree(void *ptr); void *kcalloc(size_t nmemb, size_t size); void *krealloc(void *ptr, size_t new_size);`
  - `kernel/mm/vm.c:L8-L12`：关键词命中
    代码片段：`#include "xv6fs.h" #include "proc.h" #include "kalloc.h" /*`
  - `kernel/mm/mmap.c:L7-L11`：关键词命中
    代码片段：`static uint64 allocvma() { struct vma_struct *vma = (struct vma_struct *)kalloc(); if (vma == 0) { return 0;`
- 与 2025 reXvapor 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/kernel/proc.c:L1-L5`：关键词命中
    代码片段：`/* 进程管理和页表实现 */ #include "proc.h" #include "alloc.h" #include "string.h"`
  - `src/kernel/main.c:L19-L23`：关键词命中
    代码片段：`#include "virtio_blk.h" #include "ext4.h" #include "proc.h" #include "syscall.h" #include "elf_loader.h"`
  - `kernel/sched/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "sched.h"`
  - `kernel/sched/trap.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "thread.h"`
- 与 2025 reXvapor 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `Libary/stm32f10x_tim.h:L147-L151`：关键词命中
    代码片段：`This parameter can be a value of @ref OSSI_Off_State_Selection_for_Idle_mode_state */ uint16_t TIM_LOCKLevel;        /*!< Specifies the LOCK level parameters. This parameter can...`
  - `kernel/atomic/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "cond.h" #include "semaphore.h" /// @brief init a semaphore`
  - `kernel/include/semaphore.h:L3-L7`：关键词命中
    代码片段：`#include "types.h" #include "spinlock.h" #include "cond.h"`
- 与 2025 reXvapor 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/kernel/syscall.c:L1-L5`：关键词命中
    代码片段：`/* 系统调用处理和 trap 分发 */ #include "syscall.h" #include "proc.h" #include "printf.h"`
  - `src/kernel/syscall.h:L7-L11`：关键词命中
    代码片段：`#include "proc.h" /* RISC-V Linux syscall 编号 */ #define SYS_exit          93 #define SYS_write         64`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "thread.h"`
  - `kernel/sched/trap.c:L155-L159`：关键词命中
    代码片段：`thread_exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. // p->trapframe->epc += 4;`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 文件路径重合：与 2025 reXvapor 在“设备驱动”维度出现同名文件源码路径 `src/kernel/uart.c` / `kernel/arch/qemu/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/kernel/uart.c:L1-L3`：关键词命中
    代码片段：`/* ns16550 UART 驱动实现 */ #include "uart.h"`
  - `kernel/arch/qemu/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
- 文件路径重合：与 2025 reXvapor 在“文件系统”维度出现同名文件源码路径 `src/kernel/ext4.c` / `kernel/fs/ext4.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/kernel/ext4.c:L158-L162`：关键词命中
    代码片段：`} ext4_bg_desc_64_t; /* Inode 结构 (标准 ext4, 256 字节) */ typedef struct __attribute__((packed)) { uint16_t i_mode;`
  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
- 文件路径重合：与 2025 reXvapor 在“调度与任务管理”维度出现同名文件源码路径 `src/kernel/proc.c` / `kernel/sched/proc.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/kernel/proc.c:L1-L5`：关键词命中
    代码片段：`/* 进程管理和页表实现 */ #include "proc.h" #include "alloc.h" #include "string.h"`
  - `kernel/sched/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "sched.h"`
- 文件路径重合：与 2025 reXvapor 在“系统调用”维度出现同名文件源码路径 `src/kernel/syscall.c` / `kernel/syscall.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/kernel/syscall.c:L1-L5`：关键词命中
    代码片段：`/* 系统调用处理和 trap 分发 */ #include "syscall.h" #include "proc.h" #include "printf.h"`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "thread.h"`
- 文件路径重合：与 2025 reXvapor 在“系统调用”维度出现同名文件源码路径 `src/kernel/syscall.h` / `kernel/include/syscall.h`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/kernel/syscall.h:L7-L11`：关键词命中
    代码片段：`#include "proc.h" /* RISC-V Linux syscall 编号 */ #define SYS_exit          93 #define SYS_write         64`
  - `kernel/include/syscall.h:L1-L4`：macro __SYSCALL_H
    代码片段：`#ifndef __SYSCALL_H #define __SYSCALL_H // // System call numbers // #define SYS_fork    1`

## 相似点

- 与 2025 reXvapor 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 reXvapor 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/kernel/uart.c:L1-L3`：关键词命中
    代码片段：`/* ns16550 UART 驱动实现 */ #include "uart.h"`
  - `src/kernel/uart.h:L1-L3`：关键词命中
    代码片段：`/* ns16550 UART 驱动 */ #ifndef UUOS_UART_H`
- 与 2025 reXvapor 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/kernel/ext4.c:L158-L162`：关键词命中
    代码片段：`} ext4_bg_desc_64_t; /* Inode 结构 (标准 ext4, 256 字节) */ typedef struct __attribute__((packed)) { uint16_t i_mode;`
  - `src/kernel/ext4.h:L18-L22`：关键词命中
    代码片段：`/* 目录项迭代器 */ typedef struct { uint32_t inode; uint16_t rec_len; uint8_t  name_len;`
- 与 2025 reXvapor 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/kernel/proc.h:L72-L76`：关键词命中
    代码片段：`uintptr_t ra;                /* 内核返回地址 */ uintptr_t satp;              /* 内核页表 (satp 值) */ uintptr_t trap_frame_addr;   /* trap 帧地址 */ } kernel_context_t;`
  - `src/kernel/syscall.c:L1-L3`：关键词命中
    代码片段：`/* 系统调用处理和 trap 分发 */ #include "syscall.h"`
- 与 2025 reXvapor 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/kernel/alloc.c:L15-L19`：关键词命中
    代码片段：`#define BLOCK_ALIGN     16  /* 对齐边界 */ static uint8_t  heap_area[8 * 1024 * 1024];  /* 8MB 静态堆 */ static block_t *free_list = NULL; static size_t   heap_size = 0;`
  - `src/kernel/alloc.h:L11-L15`：关键词命中
    代码片段：`/* 分配/释放内存 */ void *kmalloc(size_t size); void  kfree(void *ptr); void *kcalloc(size_t nmemb, size_t size); void *krealloc(void *ptr, size_t new_size);`
- 与 2025 reXvapor 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/kernel/proc.c:L1-L5`：关键词命中
    代码片段：`/* 进程管理和页表实现 */ #include "proc.h" #include "alloc.h" #include "string.h"`
  - `src/kernel/main.c:L19-L23`：关键词命中
    代码片段：`#include "virtio_blk.h" #include "ext4.h" #include "proc.h" #include "syscall.h" #include "elf_loader.h"`
- 与 2025 reXvapor 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `Libary/stm32f10x_tim.h:L147-L151`：关键词命中
    代码片段：`This parameter can be a value of @ref OSSI_Off_State_Selection_for_Idle_mode_state */ uint16_t TIM_LOCKLevel;        /*!< Specifies the LOCK level parameters. This parameter can...`
  - `kernel/atomic/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "cond.h" #include "semaphore.h" /// @brief init a semaphore`
- 与 2025 reXvapor 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/kernel/syscall.c:L1-L5`：关键词命中
    代码片段：`/* 系统调用处理和 trap 分发 */ #include "syscall.h" #include "proc.h" #include "printf.h"`
  - `src/kernel/syscall.h:L7-L11`：关键词命中
    代码片段：`#include "proc.h" /* RISC-V Linux syscall 编号 */ #define SYS_exit          93 #define SYS_write         64`
- 与 2025 reXvapor 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 reXvapor 的语言构成不同：待测作品为 {'json': 20, 'build': 202, 'markdown': 711, 'c': 52465, 'asm': 3393}，历史样本为 {'json': 20, 'build': 470, 'markdown': 7279, 'make': 1, 'c': 52130, 'asm': 700}。（置信度：medium）

## 可能创新点

- 双方均实现七维核心 OS 机制，当前维度级比较未发现"你有我无"的差异。
- 创新性差异（如算法选择、调度策略、内存分配方案、独特驱动设计等）超出静态维度比较的能力边界，需结合源码语义和设计文档人工评估。

## 附录：核验摘要

- 关键结论数：19
- 含证据关键结论数：19（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
