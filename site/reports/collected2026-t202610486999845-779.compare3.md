# 2026 T202610486999845 比较报告

- 对比历史仓库：2024 NPUcore-IMPACT!!!
- 生成时间：2026-07-02T05:02:02.130484+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 NPUcore-IMPACT!!!（来源：赛事历史作品）：画像相似度 score=9.61；同属 ucore-variant 风格; 语言构成相似度 0.99; OS 维度重合度 1.00; 代码规模接近度 0.62

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use crate::sbi::console_putchar;`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device-driver entry points used by the current contest kernel. //! //! Drivers expose small, capability-oriented APIs to the rest of the kernel so`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/ext4.rs:L23-L27`：关键词命中
    代码片段：`const MAX_BLOCK_SIZE: usize = 4096; /// EXT4 root inode number. pub(crate) const ROOT_INODE: u32 = 2;`
  - `os-la/src/fs/ext4.rs:L23-L27`：关键词命中
    代码片段：`const MAX_BLOCK_SIZE: usize = 4096; /// EXT4 root inode number. pub(crate) const ROOT_INODE: u32 = 2;`
  - `os/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod file_trait; mod filesystem; // mod inode; mod layout; pub mod poll;`
  - `os/src/fs/cache.rs:L193-L197`：关键词命中
    代码片段：`/// Every time kernel tried to alloc this pagecache this number will increase 1(at most 1) /// Every time out of memory occurred this number will decrease 1(at least 0) /// When...`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V trap handling for user-mode contest programs. mod context;`
  - `os/src/trap/trap.S:L41-L45`：关键词命中
    代码片段：`__restore: # case1: start running app by __restore # case2: back to U after handling trap mv sp, a0 # now sp->kernel stack(after allocated), sscratch->user stack`
  - `os/src/timer.rs:L18-L22`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/main.rs:L36-L40`：关键词命中
    代码片段：`mod syscall; mod task; mod timer; // use crate::arch::config::DISK_IMAGE_BASE;`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/arch.rs:L62-L66`：关键词命中
    代码片段：`} /// Write 'satp'. #[inline] pub unsafe fn write_satp(bits: usize) {`
  - `os/src/task/user.rs:L15-L19`：关键词命中
    代码片段：`const SATP_MODE_SV39: usize = 8usize << 60; const PTE_V: u64 = 1 << 0; const PTE_R: u64 = 1 << 1; const PTE_W: u64 = 1 << 2;`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `os/src/mm/map_area.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::Debug; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User-task runtime services. //! //! This layer groups address-space setup, user-stack construction, and the`
  - `os/src/task/user.rs:L1-L5`：关键词命中
    代码片段：`//! Minimal user address space and enter/return path for contest ELFs. //! //! The 'task' layer owns user-memory setup and the supervisor-to-user handoff, //! keeping execution...`
  - `os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `os/src/task/task.rs:L380-L384`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/user.rs:L9-L13`：关键词命中
    代码片段：`use core::arch::{asm, global_asm}; use core::mem::size_of; use core::sync::atomic::{Ordering, compiler_fence}; const PAGE_SIZE: usize = 4096;`
  - `os/src/syscall/fs.rs:L1387-L1391`：关键词命中
    代码片段：`used: bool, counter: u64, semaphore: bool, }`
  - `os/src/fs/mod.rs:L35-L39`：关键词命中
    代码片段：`use lazy_static::*; use log::warn; use spin::Mutex; lazy_static! {`
  - `os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::{arch::BLOCK_SZ, config::PAGE_SIZE, drivers::BLOCK_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File, directory, and fd-table syscall subset. //! //! The basic tests run one user program at a time, so a compact global runtime`
  - `os/src/syscall/mod.rs:L369-L373`：关键词命中
    代码片段：`}; /// Result of handling one syscall. pub enum SyscallResult { /// Return this value to user register 'a0'.`
  - `os/src/syscall/fs.rs:L548-L552`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `os/src/syscall/mod.rs:L115-L119`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现片段级代码相似度 0.93 （token=0.89, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：args, arguments, fmt, fn, print, pub, unwrap, write_fmt。（置信度：high）
  证据：
  - `os/src/console.rs:L15-L19`：fn print
    代码片段：`} pub fn print(args: fmt::Arguments) { Stdout.write_fmt(args).unwrap(); }`
  - `os/src/console.rs:L27-L31`：fn print
    代码片段：`} pub fn print(args: fmt::Arguments) { KernelOutput.write_fmt(args).unwrap(); }`
- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现片段级代码相似度 0.77 （token=0.73, structure=0.85）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：c, chars, fmt, fn, for, impl, in, mut。（置信度：medium）
  证据：
  - `os/src/console.rs:L7-L11`：fn write_str
    代码片段：`impl Write for Stdout { fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.chars() { console_putchar(c as usize);`
  - `os/src/console.rs:L10-L14`：fn write_str
    代码片段：`impl Write for KernelOutput { fn write_str(&mut self, s: &str) -> fmt::Result { let mut i = 0; for c in s.chars() {`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现片段级代码相似度 0.71 （token=0.55, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, derive, pub, struct。（置信度：medium）
  证据：
  - `os/src/fs/ext4.rs:L39-L43`：struct LookupResult
    代码片段：`/// One resolved directory entry. #[derive(Clone, Copy)] pub(crate) struct LookupResult { /// Target inode number. pub(crate) inode: u32,`
  - `os/src/fs/mod.rs:L48-L52`：struct FileDescriptor
    代码片段：`#[derive(Clone)] pub struct FileDescriptor { cloexec: bool, nonblock: bool,`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os-la/src/console.rs:L9-L9`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `user/src/console.rs:L10-L10`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现 1 个同名定义：read_block。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os-la/src/fs/ext4.rs:L310-L310`：fn read_block
    代码片段：`fn read_block(&mut self, block_num: u64, buf: &mut [u8]) -> Result<(), &'static str> {`
  - `os/src/fs/cache.rs:L90-L90`：fn read_block
    代码片段：`pub fn read_block(&mut self, block_id: usize, block_device: &Arc<dyn BlockDevice>) {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“同步机制”维度发现 2 个同名定义：read_block, new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os-la/src/fs/ext4.rs:L310-L310`：fn read_block
    代码片段：`fn read_block(&mut self, block_num: u64, buf: &mut [u8]) -> Result<(), &'static str> {`
  - `os/src/fs/cache.rs:L90-L90`：fn read_block
    代码片段：`pub fn read_block(&mut self, block_id: usize, block_device: &Arc<dyn BlockDevice>) {`
  - `os-la/src/sync/up.rs:L25-L25`：fn new
    代码片段：`pub unsafe fn new(value: T) -> Self {`
  - `os/src/fs/cache.rs:L81-L81`：fn new
    代码片段：`pub fn new(buffer_ptr: *mut [u8; BUFFER_SIZE]) -> Self {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“系统调用”维度发现 2 个同名定义：sys_read, sys_write。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os-la/src/syscall/fs.rs:L3085-L3085`：fn sys_read
    代码片段：`pub fn sys_read(fd: usize, buf: usize, len: usize) -> isize {`
  - `user/src/syscall.rs:L96-L96`：fn sys_read
    代码片段：`pub fn sys_read(fd: usize, buffer: &mut [u8]) -> isize {`
  - `os-la/src/syscall/fs.rs:L3648-L3648`：fn sys_write
    代码片段：`pub fn sys_write(fd: usize, buf: usize, len: usize) -> isize {`
  - `user/src/syscall.rs:L103-L103`：fn sys_write
    代码片段：`pub fn sys_write(fd: usize, buffer: &[u8]) -> isize {`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度出现同路径源码路径 `os/src/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use crate::sbi::console_putchar;`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现 1 个同名定义：Stdout。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os-la/src/console.rs:L6-L6`：struct Stdout
    代码片段：`struct Stdout;`
  - `user/src/console.rs:L7-L7`：struct Stdout
    代码片段：`struct Stdout;`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“系统调用”维度出现同路径源码路径 `os/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File, directory, and fd-table syscall subset. //! //! The basic tests run one user program at a time, so a compact global runtime`
  - `os/src/syscall/fs.rs:L548-L552`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“系统调用”维度出现同名文件源码路径 `os-la/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os-la/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File, directory, and fd-table syscall subset. //! //! The basic tests run one user program at a time, so a compact global runtime`
  - `os/src/syscall/fs.rs:L548-L552`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`

## 相似点

- 与 2024 NPUcore-IMPACT!!! 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use crate::sbi::console_putchar;`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device-driver entry points used by the current contest kernel. //! //! Drivers expose small, capability-oriented APIs to the rest of the kernel so`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/ext4.rs:L23-L27`：关键词命中
    代码片段：`const MAX_BLOCK_SIZE: usize = 4096; /// EXT4 root inode number. pub(crate) const ROOT_INODE: u32 = 2;`
  - `os-la/src/fs/ext4.rs:L23-L27`：关键词命中
    代码片段：`const MAX_BLOCK_SIZE: usize = 4096; /// EXT4 root inode number. pub(crate) const ROOT_INODE: u32 = 2;`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V trap handling for user-mode contest programs. mod context;`
  - `os/src/trap/trap.S:L41-L45`：关键词命中
    代码片段：`__restore: # case1: start running app by __restore # case2: back to U after handling trap mv sp, a0 # now sp->kernel stack(after allocated), sscratch->user stack`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/arch.rs:L62-L66`：关键词命中
    代码片段：`} /// Write 'satp'. #[inline] pub unsafe fn write_satp(bits: usize) {`
  - `os/src/task/user.rs:L15-L19`：关键词命中
    代码片段：`const SATP_MODE_SV39: usize = 8usize << 60; const PTE_V: u64 = 1 << 0; const PTE_R: u64 = 1 << 1; const PTE_W: u64 = 1 << 2;`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User-task runtime services. //! //! This layer groups address-space setup, user-stack construction, and the`
  - `os/src/task/user.rs:L1-L5`：关键词命中
    代码片段：`//! Minimal user address space and enter/return path for contest ELFs. //! //! The 'task' layer owns user-memory setup and the supervisor-to-user handoff, //! keeping execution...`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/user.rs:L9-L13`：关键词命中
    代码片段：`use core::arch::{asm, global_asm}; use core::mem::size_of; use core::sync::atomic::{Ordering, compiler_fence}; const PAGE_SIZE: usize = 4096;`
  - `os/src/syscall/fs.rs:L1387-L1391`：关键词命中
    代码片段：`used: bool, counter: u64, semaphore: bool, }`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File, directory, and fd-table syscall subset. //! //! The basic tests run one user program at a time, so a compact global runtime`
  - `os/src/syscall/mod.rs:L369-L373`：关键词命中
    代码片段：`}; /// Result of handling one syscall. pub enum SyscallResult { /// Return this value to user register 'a0'.`
- 与 2024 NPUcore-IMPACT!!! 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 NPUcore-IMPACT!!! 的语言构成不同：待测作品为 {'json': 21, 'markdown': 205, 'build': 593, 'toml': 23, 'rust': 96864, 'asm': 221}，历史样本为 {'json': 20, 'build': 338, 'markdown': 743, 'rust': 177629, 'asm': 10}。（置信度：medium）

## 可能创新点

- 待测作品包含 439 个历史样本 2024 NPUcore-IMPACT!!! 中未见的独特函数/类型定义（如 `load_vaddr_range`, `emit_netperf_case`, `iperf_case_profile`, `ltp_aiodio_storage_compat_bridge_case`, `lua_case_exists`, `open_fifo_mem_file`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `os-la/src/loader/elf.rs:L776-L776`：fn load_vaddr_range
    代码片段：`fn load_vaddr_range(program_headers: &[u8], program_header_count: usize) -> Option<(usize, usize)> {`
  - `os-la/src/platform/contest/mod.rs:L11134-L11134`：fn emit_netperf_case
    代码片段：`fn emit_netperf_case(name: &str, mode: NetperfMode, msg_size: &str, reply_size: &str, rate: &str) {`

## 附录：核验摘要

- 关键结论数：26
- 含证据关键结论数：26（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
