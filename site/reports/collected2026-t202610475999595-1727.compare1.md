# 2026 T202610475999595 比较报告

- 对比历史仓库：2025 别panic
- 生成时间：2026-07-02T05:01:05.402537+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 别panic（来源：赛事历史作品）：画像相似度 score=9.95；同属 ucore-variant 风格; 语言构成相似度 0.99; OS 维度重合度 1.00; 代码规模接近度 0.97

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 别panic 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `kernel/dev/uart.c:L1-L4`：关键词命中
    代码片段：`/** * @file uart.c * @brief 16550A UART驱动程序 *`
  - `kernel/dev/virtio.c:L1-L5`：关键词命中
    代码片段：`/* QEMU提供的虚拟磁盘的驱动 QEMUOPTS = -device virtio-blk-device,drive=x0,bus=virtio-mmio-bus.0 这个文件最终提供三个重要函数: virtio_init() // 初始化函数`
- 与 2025 别panic 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs.h:L7-L11`：关键词命中
    代码片段：`// Disk layout: // [ boot block | super block | log | inode blocks | //                                          free bit map | data blocks] //`
  - `kernel/fs/file.c:L122-L126`：关键词命中
    代码片段：`if (ff.type == FD_INODE) { // 关闭FAT32文件 fat32_close(&ff.fat); } else if (ff.type == FD_PIPE) { // 关闭管道`
  - `kernel/fs/fat32.c:L141-L145`：关键词命中
    代码片段：`current_path[1] = '\0'; printf("--- FAT32 SuperBlock ---\n"); printf("Bytes per Sector: %d\n", sb.bytes_per_sector); printf("Sector per Cluster: %d\n", sb.sector_per_cluster);`
- 与 2025 别panic 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L31-L35`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/dev/plic.c:L1-L3`：关键词命中
    代码片段：`// Platform-level interrupt controller #include "memlayout.h"`
  - `kernel/dev/timer.c:L1-L4`：关键词命中
    代码片段：`/** * @file timer.c * @brief 定时器驱动（基于RISC-V CLINT） *`
- 与 2025 别panic 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/vm.c:L10-L14`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/kalloc.c:L36-L40`：关键词命中
    代码片段：`char *p; p = (char*)PGROUNDUP((uint64)pa_start); for(; p + PGSIZE <= (char*)pa_end; p += PGSIZE) kfree(p); }`
  - `kernel/fs/elf.c:L84-L88`：关键词命中
    代码片段：`// 加载 ELF 文件到进程地址空间 int elf_load(struct fat32_file* file, pgtbl_t pgdir, uint64* entry_point, uint64* end_addr) { elf_hdr_t elf_hdr;`
  - `kernel/mem/uvm.c:L14-L18`：关键词命中
    代码片段：`// 遍历当前页表的所有项 for(int i = 0; i < 512; i++) { pte_t pte = pgtbl[i]; if((pte & PTE_V) && (pte & (PTE_R | PTE_W | PTE_X)) == 0) { // 这是一个指向下级页表的有效页表项`
- 与 2025 别panic 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "riscv.h" #include "spinlock.h" #include "proc.h" #include "defs.h"`
  - `kernel/proc.h:L22-L26`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `kernel/proc/cpu.c:L1-L3`：关键词命中
    代码片段：`#include "proc/cpu.h" #include "riscv.h"`
  - `kernel/proc/proc.c:L2-L6`：关键词命中
    代码片段：`#include "mem/pmem.h" #include "mem/vmem.h" #include "proc/cpu.h" #include "proc/initcode.h" #include "memlayout.h"`
- 与 2025 别panic 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs.c:L15-L19`：关键词命中
    代码片段：`#include "param.h" #include "stat.h" #include "spinlock.h" #include "proc.h" #include "sleeplock.h"`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "riscv.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "fs.h"`
  - `kernel/lib/spinlock.c:L1-L4`：关键词命中
    代码片段：`/** * @file spinlock.c * @brief 自旋锁实现（支持中断嵌套和锁层次管理） *`
  - `include/lib/lock.h:L1-L4`：关键词命中
    代码片段：`/** * @file lock.h * @brief 自旋锁接口定义 *`
- 与 2025 别panic 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap.c:L58-L62`：关键词命中
    代码片段：`kexit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h"`
  - `kernel/trap/trap_user.c:L4-L8`：关键词命中
    代码片段：`* * 处理从用户态陷入内核的事件： * - 系统调用（ecall指令） * - 页面错误 * - 时钟中断（调度）`
  - `kernel/syscall/syscall.c:L3-L7`：关键词命中
    代码片段：`//#include "mem/mmap.h" #include "mem/vmem.h" #include "syscall/syscall.h" #include "syscall/sysnum.h" #include "syscall/sysfunc.h"`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 宏名重合：与 2025 别panic 在“设备驱动”维度发现 4 个同名定义：CONSOLE, Reg, RHR, THR。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/file.h:L41-L41`：macro CONSOLE
    代码片段：`#define CONSOLE 1`
  - `include/fs/file.h:L47-L47`：macro CONSOLE
    代码片段：`#define CONSOLE 1`
  - `kernel/uart.c:L16-L16`：macro Reg
    代码片段：`#define Reg(reg) ((volatile unsigned char *)(UART0 + (reg)))`
  - `kernel/dev/uart.c:L46-L46`：macro Reg
    代码片段：`#define Reg(reg)         ((volatile unsigned char *)(UART_BASE + reg))`
- 宏名重合：与 2025 别panic 在“文件系统”维度发现 4 个同名定义：CONSOLE, NOFILE, NFILE, PGSHIFT。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/file.h:L41-L41`：macro CONSOLE
    代码片段：`#define CONSOLE 1`
  - `include/fs/file.h:L47-L47`：macro CONSOLE
    代码片段：`#define CONSOLE 1`
  - `kernel/param.h:L3-L3`：macro NOFILE
    代码片段：`#define NOFILE       16  // open files per process`
  - `include/proc/proc.h:L18-L18`：macro NOFILE
    代码片段：`#define NOFILE 128`
- 宏名重合：与 2025 别panic 在“中断与异常”维度发现 4 个同名定义：PLIC_PRIORITY, PLIC_PENDING, PLIC_SENABLE, PLIC_SPRIORITY。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/memlayout.h:L30-L30`：macro PLIC_PRIORITY
    代码片段：`#define PLIC_PRIORITY (PLIC + 0x0)`
  - `include/memlayout.h:L15-L15`：macro PLIC_PRIORITY
    代码片段：`#define PLIC_PRIORITY(id) (PLIC_BASE + (id) * 4)`
  - `kernel/memlayout.h:L31-L31`：macro PLIC_PENDING
    代码片段：`#define PLIC_PENDING (PLIC + 0x1000)`
  - `include/memlayout.h:L16-L16`：macro PLIC_PENDING
    代码片段：`#define PLIC_PENDING (PLIC_BASE + 0x1000)`
- 宏名重合：与 2025 别panic 在“内存管理”维度发现 4 个同名定义：TRAMPOLINE, KSTACK, TRAPFRAME, MSTATUS_MPP_MASK。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/memlayout.h:L44-L44`：macro TRAMPOLINE
    代码片段：`#define TRAMPOLINE (MAXVA - PGSIZE)`
  - `kernel/proc/proc.c:L16-L16`：macro TRAMPOLINE
    代码片段：`#define TRAMPOLINE (VA_MAX - PGSIZE)     // trampoline页的虚拟地址`
  - `kernel/memlayout.h:L48-L48`：macro KSTACK
    代码片段：`#define KSTACK(p) (TRAMPOLINE - ((p)+1)* 2*PGSIZE)`
  - `include/memlayout.h:L31-L31`：macro KSTACK
    代码片段：`#define KSTACK(hartid) (0x3f80000000L + ((hartid) + 1) * 2 * PGSIZE)`
- 宏名重合：与 2025 别panic 在“调度与任务管理”维度发现 3 个同名定义：NPROC, NOFILE, SYS_sched_yield。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/param.h:L1-L1`：macro NPROC
    代码片段：`#define NPROC        64  // maximum number of processes`
  - `include/common.h:L37-L37`：macro NPROC
    代码片段：`#define NPROC 64`
  - `kernel/param.h:L3-L3`：macro NOFILE
    代码片段：`#define NOFILE       16  // open files per process`
  - `include/proc/proc.h:L18-L18`：macro NOFILE
    代码片段：`#define NOFILE 128`
- 宏名重合：与 2025 别panic 在“系统调用”维度发现 4 个同名定义：TRAPFRAME, SYS_fork, SYS_exit, SYS_wait。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/memlayout.h:L59-L59`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME (TRAMPOLINE - PGSIZE)`
  - `kernel/proc/proc.c:L17-L17`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME (TRAMPOLINE - PGSIZE)  // trapframe页的虚拟地址`
  - `kernel/syscall.h:L2-L2`：macro SYS_fork
    代码片段：`#define SYS_fork    1`
  - `user/syscall_num.h:L8-L8`：macro SYS_fork
    代码片段：`#define SYS_fork         4`
- 结构体/类型重合：与 2025 别panic 在“文件系统”维度发现 3 个同名定义：file, devsw, dirent。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/file.h:L2-L2`：struct file
    代码片段：`struct file {`
  - `include/fs/file.h:L31-L31`：struct file
    代码片段：`struct file {`
  - `kernel/file.h:L34-L34`：struct devsw
    代码片段：`struct devsw {`
  - `include/fs/file.h:L25-L25`：struct devsw
    代码片段：`struct devsw {`
- 函数/符号名重合：与 2025 别panic 在“调度与任务管理”维度发现 1 个同名定义：swtch。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/swtch.S:L9-L9`：fn swtch
    代码片段：`swtch:`
  - `kernel/proc/swtch.S:L7-L7`：fn swtch
    代码片段：`swtch:`
- 结构体/类型重合：与 2025 别panic 在“调度与任务管理”维度发现 4 个同名定义：context, cpu, trapframe, proc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/proc.h:L3-L3`：struct context
    代码片段：`struct context {`
  - `include/proc/proc.h:L36-L36`：struct context
    代码片段：`typedef struct context {`
  - `kernel/proc.h:L23-L23`：struct cpu
    代码片段：`struct cpu {`
  - `include/proc/cpu.h:L23-L23`：struct cpu
    代码片段：`typedef struct cpu {`
- 结构体/类型重合：与 2025 别panic 在“中断与异常”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/proc.h:L44-L44`：struct trapframe
    代码片段：`struct trapframe {`
  - `include/proc/proc.h:L67-L67`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 结构体/类型重合：与 2025 别panic 在“内存管理”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/proc.h:L44-L44`：struct trapframe
    代码片段：`struct trapframe {`
  - `include/proc/proc.h:L67-L67`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 结构体/类型重合：与 2025 别panic 在“同步机制”维度发现 1 个同名定义：spinlock。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/spinlock.h:L2-L2`：struct spinlock
    代码片段：`struct spinlock {`
  - `include/lib/lock.h:L24-L24`：struct spinlock
    代码片段：`typedef struct spinlock {`
- 结构体/类型重合：与 2025 别panic 在“系统调用”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/proc.h:L44-L44`：struct trapframe
    代码片段：`struct trapframe {`
  - `include/proc/proc.h:L67-L67`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 文件路径重合：与 2025 别panic 在“设备驱动”维度出现同名文件源码路径 `kernel/uart.c` / `kernel/dev/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/dev/uart.c:L1-L4`：关键词命中
    代码片段：`/** * @file uart.c * @brief 16550A UART驱动程序 *`
- 文件路径重合：与 2025 别panic 在“设备驱动”维度出现同名文件源码路径 `kernel/virtio.h` / `include/dev/virtio.h`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `include/dev/virtio.h:L14-L18`：macro VIRTIO_MMIO_MAGIC_VALUE
    代码片段：`// virtio mmio control registers, mapped starting at 0x10001000. // from qemu virtio_mmio.h #define VIRTIO_MMIO_MAGIC_VALUE 0x000 // 0x74726976 #define VIRTIO_MMIO_VERSION 0x004...`
- 文件路径重合：与 2025 别panic 在“文件系统”维度出现同名文件源码路径 `kernel/file.c` / `kernel/fs/file.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/file.c:L105-L109`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `kernel/fs/file.c:L122-L126`：关键词命中
    代码片段：`if (ff.type == FD_INODE) { // 关闭FAT32文件 fat32_close(&ff.fat); } else if (ff.type == FD_PIPE) { // 关闭管道`
- 文件路径重合：与 2025 别panic 在“中断与异常”维度出现同名文件源码路径 `kernel/plic.c` / `kernel/dev/plic.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/dev/plic.c:L1-L3`：关键词命中
    代码片段：`// Platform-level interrupt controller #include "memlayout.h"`
- 文件路径重合：与 2025 别panic 在“内存管理”维度出现同名文件源码路径 `kernel/riscv.h` / `include/riscv.h`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/riscv.h:L350-L354`：macro PGSIZE
    代码片段：`#endif // __ASSEMBLER__ #define PGSIZE 4096 // bytes per page #define PGSHIFT 12  // bits of offset within a page`
  - `include/riscv.h:L312-L316`：macro PGSIZE
    代码片段：`// 内存管理相关 #define PGSIZE 4096 // bytes per page #define PGSHIFT 12  // bits of offset within a page`
- 文件路径重合：与 2025 别panic 在“调度与任务管理”维度出现同名文件源码路径 `kernel/proc.c` / `kernel/proc/proc.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "riscv.h" #include "spinlock.h" #include "proc.h" #include "defs.h"`
  - `kernel/proc/proc.c:L2-L6`：关键词命中
    代码片段：`#include "mem/pmem.h" #include "mem/vmem.h" #include "proc/cpu.h" #include "proc/initcode.h" #include "memlayout.h"`

## 相似点

- 与 2025 别panic 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 别panic 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
- 与 2025 别panic 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs.h:L7-L11`：关键词命中
    代码片段：`// Disk layout: // [ boot block | super block | log | inode blocks | //                                          free bit map | data blocks] //`
- 与 2025 别panic 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L31-L35`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
- 与 2025 别panic 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/vm.c:L10-L14`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/kalloc.c:L36-L40`：关键词命中
    代码片段：`char *p; p = (char*)PGROUNDUP((uint64)pa_start); for(; p + PGSIZE <= (char*)pa_end; p += PGSIZE) kfree(p); }`
- 与 2025 别panic 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "riscv.h" #include "spinlock.h" #include "proc.h" #include "defs.h"`
  - `kernel/proc.h:L22-L26`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2025 别panic 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs.c:L15-L19`：关键词命中
    代码片段：`#include "param.h" #include "stat.h" #include "spinlock.h" #include "proc.h" #include "sleeplock.h"`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "riscv.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "fs.h"`
- 与 2025 别panic 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap.c:L58-L62`：关键词命中
    代码片段：`kexit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h"`
- 与 2025 别panic 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 别panic 的语言构成不同：待测作品为 {'markdown': 207, 'build': 195, 'c': 11886, 'asm': 276}，历史样本为 {'json': 20, 'make': 26, 'build': 250, 'markdown': 73, 'c': 11493, 'asm': 305}。（置信度：medium）

## 可能创新点

- 待测作品包含 30 个历史样本 2025 别panic 中未见的独特函数/类型定义（如 `procstate`, `busy_loop`, `sched_yield_test`, `gettimeofday_test`, `getppid_test`, `virtq_used_elem`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/proc.h:L83-L83`：enum procstate
    代码片段：`enum procstate { UNUSED, USED, SLEEPING, RUNNABLE, RUNNING, ZOMBIE };`
  - `user/myCall.c:L46-L46`：fn busy_loop
    代码片段：`void busy_loop(int seconds) {`

## 附录：核验摘要

- 关键结论数：34
- 含证据关键结论数：34（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
