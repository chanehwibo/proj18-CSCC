# 2026 T2026181239910972 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026181239910972-3268`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：970
- 代码/文本行数：163731
- 主要语言：rust 97500 LOC, markdown 31718 LOC, c 27779 LOC, text 4147 LOC, build 1381 LOC

## 总览

2026 T2026181239910972 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 97500 LOC, markdown: 31718 LOC, c: 27779 LOC, text: 4147 LOC。仓库包含 970 个已扫描文件、约 163731 行可分析文本，当前抽取到 7395 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, anemone-abi/Cargo.toml, anemone-abi/build.rs, anemone-apps/args/Cargo.toml, anemone-apps/float-test/Cargo.toml。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`#! Kernel workspace. anemone-apps & anemone-rs & anemone-rs excluded. [workspace]`
  - `anemone-abi/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "anemone-abi" version = "0.1.0"`
  - `anemone-abi/build.rs:L1-L3`：构建入口
    代码片段：`use std::env; fn main() {`
  - `anemone-apps/args/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "args" version = "0.1.0"`
  - `anemone-apps/float-test/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "float-test" version = "0.1.0"`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：enum FsState, fn new_hanging, fn new, fn new_root, fn root。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `anemone-kernel/src/task/fs.rs` | L1-L3 | 关键词命中 |
| `anemone-kernel/src/task/mod.rs` | L1-L3 | 关键词命中 |
| `anemone-kernel/src/task/tid.rs` | L1-L3 | 关键词命中 |
| `anemone-kernel/src/task/fs.rs` | L7-L11 | enum FsState |
| `anemone-kernel/src/task/fs.rs` | L18-L22 | fn new_hanging |
| `anemone-kernel/src/task/fs.rs` | L22-L26 | fn new |

### 关键代码片段

  - `anemone-kernel/src/task/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File System related structures and functions for a task. //! //! Reference:`
  - `anemone-kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! we expose all fields of Task, ThreadGroup, etc. to the whole task module. //! this is a bit too much, but i think that's inevitable for kernel code. too //! much encapsulati...`
  - `anemone-kernel/src/task/tid.rs:L1-L3`：关键词命中
    代码片段：`//! Task ID management. use core::{`
  - `anemone-kernel/src/task/fs.rs:L7-L11`：enum FsState
    代码片段：`#[derive(Debug, Clone)] pub enum FsState { Hanging, Ready { root: PathRef, cwd: PathRef },`

### 相关符号

`enum FsState` at `anemone-kernel/src/task/fs.rs:L7`、`fn new_hanging` at `anemone-kernel/src/task/fs.rs:L18`、`fn new` at `anemone-kernel/src/task/fs.rs:L22`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：struct DmaRegion, fn ppn, fn as_ptr, fn sync_for_device, fn sync_for_cpu。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `anemone-kernel/src/mm/stack.rs` | L26-L30 | 关键词命中 |
| `anemone-kernel/src/mm/layout.rs` | L7-L11 | 关键词命中 |
| `anemone-kernel/src/mm/kptable.rs` | L13-L17 | 关键词命中 |
| `anemone-kernel/src/mm/dma.rs` | L14-L18 | struct DmaRegion |
| `anemone-kernel/src/mm/dma.rs` | L20-L24 | fn ppn |
| `anemone-kernel/src/mm/dma.rs` | L33-L37 | fn as_ptr |

### 关键代码片段

  - `anemone-kernel/src/mm/stack.rs:L26-L30`：关键词命中
    代码片段：`let vpn_range = unsafe { mm::remap::alloc_virt_range(NPAGES + 1) } .expect("failed to allocate virtual range for boot stack guard page"); // The first page is the guard – we sim...`
  - `anemone-kernel/src/mm/layout.rs:L7-L11`：关键词命中
    代码片段：`/// Common kernel layout and key constants: /// /// ## Page Table Managed Spaces: /// ''' ///  -0x0   +-----------+---------------+ -0x0`
  - `anemone-kernel/src/mm/kptable.rs:L13-L17`：关键词命中
    代码片段：`pub static KERNEL_PTABLE: Lazy<KPTable> = Lazy::new(|| KPTable::new()); /// Nothing more than [crate::PageTable]. Just provides some specialized methods /// for mapping kernel m...`
  - `anemone-kernel/src/mm/dma.rs:L14-L18`：struct DmaRegion
    代码片段：`/// be accessed from a Rust reference. Only raw pointers should be used. #[derive(Debug)] pub struct DmaRegion { folio: OwnedFolio, }`

### 相关符号

`struct DmaRegion` at `anemone-kernel/src/mm/dma.rs:L14`、`fn ppn` at `anemone-kernel/src/mm/dma.rs:L20`、`fn as_ptr` at `anemone-kernel/src/mm/dma.rs:L33`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn syscall_raw, fn syscall, struct SyscallTable, fn register_syscall_handlers, fn handle_syscall。（置信度：medium）
  - 静态识别到 349 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `anemone-abi/src/syscall/mod.rs` | L12-L16 | 关键词命中 |
| `anemone-abi/src/syscall/riscv.rs` | L14-L18 | 关键词命中 |
| `anemone-abi/src/syscall/native.rs` | L1-L3 | 关键词命中 |
| `anemone-abi/src/syscall/riscv.rs` | L2-L6 | fn syscall_raw |
| `anemone-abi/src/syscall/riscv.rs` | L28-L32 | fn syscall |
| `anemone-kernel/src/syscall/mod.rs` | L20-L24 | struct SyscallTable |

### 关键代码片段

  - `anemone-abi/src/syscall/mod.rs:L12-L16`：关键词命中
    代码片段：`pub use native::*; /// The Linux kernel actually does not define a maximum syscall number, /// but it's obvious that syscall numbers won't exceed this value minus one, on /// an...`
  - `anemone-abi/src/syscall/riscv.rs:L14-L18`：关键词命中
    代码片段：`unsafe { core::arch::asm!( "ecall", in("a7") sysno, in("a0") arg0,`
  - `anemone-abi/src/syscall/native.rs:L1-L3`：关键词命中
    代码片段：`use crate::syscall::SYS_ANEMONE_START; pub const SYS_DBG_PRINT: u64 = SYS_ANEMONE_START + 0;`
  - `anemone-abi/src/syscall/riscv.rs:L2-L6`：fn syscall_raw
    代码片段：`//! Architecture-specific. unsafe fn syscall_raw( sysno: u64, arg0: u64,`

### 相关符号

`fn syscall_raw` at `anemone-abi/src/syscall/riscv.rs:L2`、`fn syscall` at `anemone-abi/src/syscall/riscv.rs:L28`、`struct SyscallTable` at `anemone-kernel/src/syscall/mod.rs:L20`、`fn syscall_raw` at `anemone-abi/src/syscall/riscv.rs:L2`、`fn syscall` at `anemone-abi/src/syscall/riscv.rs:L28`、`struct SyscallTable` at `anemone-kernel/src/syscall/mod.rs:L20`、`fn register_syscall_handlers` at `anemone-kernel/src/syscall/mod.rs:L30`、`fn handle_syscall` at `anemone-kernel/src/syscall/mod.rs:L76`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct OpenOptions, fn new, fn read, fn write, fn create。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `anemone-kernel/src/fs/mod.rs` | L1-L5 | 关键词命中 |
| `anemone-kernel/src/fs/file.rs` | L7-L11 | 关键词命中 |
| `anemone-kernel/src/fs/path.rs` | L10-L14 | 关键词命中 |
| `anemone-rs/src/fs.rs` | L9-L13 | struct OpenOptions |
| `anemone-rs/src/fs.rs` | L18-L22 | fn new |
| `anemone-rs/src/fs.rs` | L27-L31 | fn read |

### 关键代码片段

  - `anemone-kernel/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Virtual file system and filesystem drivers. // vfs infrastructure mod anonymous; mod cache_stats;`
  - `anemone-kernel/src/fs/file.rs:L7-L11`：关键词命中
    代码片段：`bitflags! { #[derive(Debug, Clone, Copy, PartialEq, Eq)] /// Open-time VFS behavior flags for one file object. /// /// This is not a Linux UAPI bitset or a mirror of Linux 'fmod...`
  - `anemone-kernel/src/fs/path.rs:L10-L14`：关键词命中
    代码片段：`pub struct PathRef { mount: Arc<Mount>, dentry: Arc<Dentry>, }`
  - `anemone-rs/src/fs.rs:L9-L13`：struct OpenOptions
    代码片段：`#[derive(Debug)] pub struct OpenOptions { read: bool, write: bool,`

### 相关符号

`struct OpenOptions` at `anemone-rs/src/fs.rs:L9`、`fn new` at `anemone-rs/src/fs.rs:L18`、`fn read` at `anemone-rs/src/fs.rs:L27`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct MonoFlow, fn with, fn with_mut, struct MonoOnce, fn init。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `anemone-kernel/src/sync/mod.rs` | L1-L3 | 关键词命中 |
| `anemone-kernel/src/sync/mono.rs` | L1-L4 | 关键词命中 |
| `anemone-kernel/src/sync/mutex.rs` | L10-L14 | 关键词命中 |
| `anemone-kernel/src/sync/mono.rs` | L22-L26 | struct MonoFlow |
| `anemone-kernel/src/sync/mono.rs` | L56-L60 | fn with |
| `anemone-kernel/src/sync/mono.rs` | L80-L84 | fn with_mut |

### 关键代码片段

  - `anemone-kernel/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`//! TODO: Typed no-irq lock. //! //! TODO: Interrupt-safe lazy.`
  - `anemone-kernel/src/sync/mono.rs:L1-L4`：关键词命中
    代码片段：`#[cfg(debug_assertions)] use core::sync::atomic::{AtomicBool, Ordering}; use core::{cell::UnsafeCell, mem::MaybeUninit};`
  - `anemone-kernel/src/sync/mutex.rs:L10-L14`：关键词命中
    代码片段：`#[derive(Debug)] pub struct Mutex<T: ?Sized> { locked: AtomicBool, lock_released: Event,`
  - `anemone-kernel/src/sync/mono.rs:L22-L26`：struct MonoFlow
    代码片段：`///    enforced in dev builds. #[derive(Debug)] pub struct MonoFlow<T> { data: UnsafeCell<T>, #[cfg(debug_assertions)]`

### 相关符号

`struct MonoFlow` at `anemone-kernel/src/sync/mono.rs:L22`、`fn with` at `anemone-kernel/src/sync/mono.rs:L56`、`fn with_mut` at `anemone-kernel/src/sync/mono.rs:L80`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn schedule_local_irq_timer_event, enum TimerLane, fn fmt, struct TimerEvent, fn new_irq。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `anemone-kernel/src/time/timer/irq.rs` | L3-L7 | 关键词命中 |
| `anemone-kernel/src/time/timer/mod.rs` | L1-L3 | 关键词命中 |
| `anemone-kernel/src/exception/timer.rs` | L1-L3 | 关键词命中 |
| `anemone-kernel/src/time/timer/irq.rs` | L10-L14 | fn schedule_local_irq_timer_event |
| `anemone-kernel/src/time/timer/mod.rs` | L16-L20 | enum TimerLane |
| `anemone-kernel/src/time/timer/mod.rs` | L22-L26 | fn fmt |

### 关键代码片段

  - `anemone-kernel/src/time/timer/irq.rs:L3-L7`：关键词命中
    代码片段：`use super::{TimerEvent, expire_ticks_after, push_timer_event}; /// Schedule a timer event to run in interrupt context on the local CPU after /// the given duration. ///`
  - `anemone-kernel/src/time/timer/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Soft timer. //! //! Timer events are one-shot completions. Callers explicitly choose whether a`
  - `anemone-kernel/src/exception/timer.rs:L1-L3`：关键词命中
    代码片段：`//! Timer interrupt handling. use crate::prelude::*;`
  - `anemone-kernel/src/time/timer/irq.rs:L10-L14`：fn schedule_local_irq_timer_event
    代码片段：`/// The callback runs in timer IRQ context. It must not sleep, block, take /// ordinary mutexes, or perform work that relies on process context. pub unsafe fn schedule_local_irq...`

### 相关符号

`fn schedule_local_irq_timer_event` at `anemone-kernel/src/time/timer/irq.rs:L10`、`enum TimerLane` at `anemone-kernel/src/time/timer/mod.rs:L16`、`fn fmt` at `anemone-kernel/src/time/timer/mod.rs:L22`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：trait CpuArchTrait, fn set_percpu_base, fn percpu_base, struct CpuId, fn new。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `anemone-kernel/src/driver/mod.rs` | L1-L3 | 关键词命中 |
| `anemone-kernel/src/device/console.rs` | L1-L3 | 关键词命中 |
| `anemone-kernel/src/driver/rtc/mod.rs` | L1-L3 | 关键词命中 |
| `anemone-kernel/src/device/cpu.rs` | L1-L5 | trait CpuArchTrait |
| `anemone-kernel/src/device/cpu.rs` | L6-L10 | fn set_percpu_base |
| `anemone-kernel/src/device/cpu.rs` | L12-L16 | fn percpu_base |

### 关键代码片段

  - `anemone-kernel/src/driver/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Here lies the driver framework, and all driver implementations. //! //! Most drivers implement the [Driver] trait, but some drivers are for system`
  - `anemone-kernel/src/device/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console subsystem. //! //! Here lies /dev/console.`
  - `anemone-kernel/src/driver/rtc/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Real time clock (RTC) driver. //! //! RTCs provide a persistent time source that can be used to maintain the`
  - `anemone-kernel/src/device/cpu.rs:L1-L5`：trait CpuArchTrait
    代码片段：`use core::fmt::{Debug, Display}; pub trait CpuArchTrait { /// Sets the base address of the per-CPU area for the current CPU. ///`

### 相关符号

`trait CpuArchTrait` at `anemone-kernel/src/device/cpu.rs:L1`、`fn set_percpu_base` at `anemone-kernel/src/device/cpu.rs:L6`、`fn percpu_base` at `anemone-kernel/src/device/cpu.rs:L12`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
