# 2026 T2026114499910840 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026114499910840-1796`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：795
- 代码/文本行数：302276
- 主要语言：c 299469 LOC, asm 2076 LOC, markdown 670 LOC, text 61 LOC

## 总览

2026 T2026114499910840 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 c: 299469 LOC, asm: 2076 LOC, markdown: 670 LOC, text: 61 LOC。仓库包含 795 个已扫描文件、约 302276 行可分析文本，当前抽取到 19130 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（90/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：未确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 0/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 未确认构建系统。（置信度：unconfirmed）

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn task_ns_alloc_inum, fn task_ns_common_init, fn task_ns_common_get, fn task_ns_common_put, fn task_fs_get。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/fs/proc.h` | L1-L3 | 关键词命中 |
| `kernel/src/task/ns.c` | L1-L4 | 关键词命中 |
| `kernel/src/task/ns.h` | L5-L9 | 关键词命中 |
| `kernel/src/task/ns.c` | L6-L10 | fn task_ns_alloc_inum |
| `kernel/src/task/ns.c` | L10-L14 | fn task_ns_common_init |
| `kernel/src/task/ns.c` | L17-L21 | fn task_ns_common_get |

### 关键代码片段

  - `kernel/src/fs/proc.h:L1-L3`：关键词命中
    代码片段：`#pragma once #include <fs/proc/proc.h>`
  - `kernel/src/task/ns.c:L1-L4`：关键词命中
    代码片段：`#include <fs/vfs/vfs.h> #include <task/ns.h> #include <task/task.h> #include <arch/task_abi.h>`
  - `kernel/src/task/ns.h:L5-L9`：关键词命中
    代码片段：`#include <libs/llist.h> struct task; typedef struct task task_t;`
  - `kernel/src/task/ns.c:L6-L10`：fn task_ns_alloc_inum
    代码片段：`static uint64_t next_namespace_inum = 1; static uint64_t task_ns_alloc_inum(void) { return __atomic_fetch_add(&next_namespace_inum, 1, __ATOMIC_RELAXED); }`

### 相关符号

`fn task_ns_alloc_inum` at `kernel/src/task/ns.c:L6`、`fn task_ns_common_init` at `kernel/src/task/ns.c:L10`、`fn task_ns_common_get` at `kernel/src/task/ns.c:L17`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn arch_user_va_limit, fn user_va_limit, fn mm_default_mmap_top, fn mmap_aslr_next, fn task_mm_init_aslr。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/mm/mm.c` | L427-L431 | 关键词命中 |
| `kernel/src/mm/mm.h` | L4-L8 | 关键词命中 |
| `kernel/src/mm/shm.c` | L246-L250 | 关键词命中 |
| `kernel/src/mm/mm.c` | L24-L28 | fn arch_user_va_limit |
| `kernel/src/mm/mm.c` | L26-L30 | fn user_va_limit |
| `kernel/src/mm/mm.c` | L37-L41 | fn mm_default_mmap_top |

### 关键代码片段

  - `kernel/src/mm/mm.c:L427-L431`：关键词命中
    代码片段：`} uint64_t map_change_attribute_range(uint64_t *pgdir, uint64_t vaddr, uint64_t len, uint64_t flags) { for (uint64_t va = vaddr; va < vaddr + len; va += PAGE_SIZE) {`
  - `kernel/src/mm/mm.h:L4-L8`：关键词命中
    代码片段：`#include <mm/hhdm.h> #include <mm/buddy.h> #include <mm/page_table.h> #include <arch/arch.h> #include <mm/vma.h>`
  - `kernel/src/mm/shm.c:L246-L250`：关键词命中
    代码片段：`uint64_t flags; uint64_t start; uint64_t *pgdir; vma_t *vma;`
  - `kernel/src/mm/mm.c:L24-L28`：fn arch_user_va_limit
    代码片段：`uint64_t arch_user_va_limit(void) __attribute__((weak)); uint64_t arch_user_va_limit(void) { return 0; } uint64_t user_va_limit(void) {`

### 相关符号

`fn arch_user_va_limit` at `kernel/src/mm/mm.c:L24`、`fn user_va_limit` at `kernel/src/mm/mm.c:L26`、`fn mm_default_mmap_top` at `kernel/src/mm/mm.c:L37`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：struct iovec, struct dqblk, struct statx_timestamp, struct statx, struct pollfd。（置信度：medium）
  - 静态识别到 314 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/fs/fs_syscall.h` | L568-L572 | 关键词命中 |
| `kernel/src/mm/mm_syscall.h` | L144-L148 | 关键词命中 |
| `kernel/src/net/net_syscall.h` | L65-L69 | 关键词命中 |
| `kernel/src/fs/fs_syscall.h` | L12-L16 | struct iovec |
| `kernel/src/fs/fs_syscall.h` | L90-L94 | struct dqblk |
| `kernel/src/fs/fs_syscall.h` | L141-L145 | struct statx_timestamp |

### 关键代码片段

  - `kernel/src/fs/fs_syscall.h:L568-L572`：关键词命中
    代码片段：`* delegates to vfs_read_file(). * Notes: a zero-length request still goes through the normal file path instead * of being short-circuited in the syscall wrapper. */ uint64_t sys...`
  - `kernel/src/mm/mm_syscall.h:L144-L148`：关键词命中
    代码片段：`* Notes: mincore() is often used as a hinting or observability tool rather * than a correctness primitive. Callers should not assume that residency * answers stay true after the...`
  - `kernel/src/net/net_syscall.h:L65-L69`：关键词命中
    代码片段：`* port, so backends need to reject duplicate ownership rather than silently * rebinding the same endpoint. * Gaps: if the backend has no bind hook the syscall currently returns...`
  - `kernel/src/fs/fs_syscall.h:L12-L16`：struct iovec
    代码片段：`int fsfd_mount_get_path(struct vfs_file *file, struct vfs_path *path); struct iovec { uint8_t *iov_base; uint64_t len;`

### 相关符号

`struct iovec` at `kernel/src/fs/fs_syscall.h:L12`、`struct dqblk` at `kernel/src/fs/fs_syscall.h:L90`、`struct statx_timestamp` at `kernel/src/fs/fs_syscall.h:L141`、`fn sys_brk` at `kernel/src/mm/mm_syscall.c:L601`、`fn sys_munmap` at `kernel/src/mm/mm_syscall.c:L911`、`fn sys_mprotect` at `kernel/src/mm/mm_syscall.c:L922`、`fn sys_msync` at `kernel/src/mm/mm_syscall.c:L1694`、`fn sys_mincore` at `kernel/src/mm/mm_syscall.c:L1758`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct devtmpfs_dirent, struct devtmpfs_inode_info, struct devtmpfs_fs_info, fn devtmpfs_dtype, fn devtmpfs_resize_inode。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/fs/dev.c` | L1-L4 | 关键词命中 |
| `kernel/src/fs/dev.h` | L1-L5 | 关键词命中 |
| `kernel/src/fs/sys.c` | L41-L45 | 关键词命中 |
| `kernel/src/fs/dev.c` | L19-L23 | struct devtmpfs_dirent |
| `kernel/src/fs/dev.c` | L25-L29 | struct devtmpfs_inode_info |
| `kernel/src/fs/dev.c` | L33-L37 | struct devtmpfs_fs_info |

### 关键代码片段

  - `kernel/src/fs/dev.c:L1-L4`：关键词命中
    代码片段：`#include <libs/keys.h> #include <fs/vfs/vfs.h> #include <fs/sys.h> #include <block/partition.h>`
  - `kernel/src/fs/dev.h:L1-L5`：关键词命中
    代码片段：`#pragma once #include <fs/vfs/vfs.h> #include <dev/device.h> #include <libs/mutex.h>`
  - `kernel/src/fs/sys.c:L41-L45`：关键词命中
    代码片段：`static bool sysfs_internal_mounting = false; static inline sysfs_inode_info_t *sysfs_i(vfs_node_t *inode) { return inode ? container_of(inode, sysfs_inode_info_t, vfs_inode) : N...`
  - `kernel/src/fs/dev.c:L19-L23`：struct devtmpfs_dirent
    代码片段：`#define MAX_DEVTMPFS_FILE_SIZE (128 * 1024 * 1024) typedef struct devtmpfs_dirent { struct llist_header node; char *name;`

### 相关符号

`struct devtmpfs_dirent` at `kernel/src/fs/dev.c:L19`、`struct devtmpfs_inode_info` at `kernel/src/fs/dev.c:L25`、`struct devtmpfs_fs_info` at `kernel/src/fs/dev.c:L33`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn try_acquire_global_lock_from_firmware, fn do_release_global_lock_to_firmware, fn uacpi_acquire_global_lock_from_firmware, fn uacpi_release_global_lock_to_firmware, fn uacpi_acquire_global_lock。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/acpi/mutex.c` | L1-L3 | 关键词命中 |
| `kernel/src/libs/mutex.c` | L1-L3 | 关键词命中 |
| `kernel/src/libs/mutex.h` | L13-L17 | 关键词命中 |
| `kernel/src/acpi/mutex.c` | L18-L22 | fn try_acquire_global_lock_from_firmware |
| `kernel/src/acpi/mutex.c` | L40-L44 | fn do_release_global_lock_to_firmware |
| `kernel/src/acpi/mutex.c` | L51-L55 | fn uacpi_acquire_global_lock_from_firmware |

### 关键代码片段

  - `kernel/src/acpi/mutex.c:L1-L3`：关键词命中
    代码片段：`#include <uacpi/platform/atomic.h> #include <uacpi/internal/mutex.h> #include <uacpi/internal/log.h>`
  - `kernel/src/libs/mutex.c:L1-L3`：关键词命中
    代码片段：`#include <libs/mutex.h> #include <task/task.h>`
  - `kernel/src/libs/mutex.h:L13-L17`：关键词命中
    代码片段：`typedef struct { spinlock_t guard;       // 保护 mutex 元数据（多核安全） volatile bool locked;   // 是否已锁定 task_t *owner;          // 持有锁的任务`
  - `kernel/src/acpi/mutex.c:L18-L22`：fn try_acquire_global_lock_from_firmware
    代码片段：`#define GLOBAL_LOCK_MASK 3u static uacpi_bool try_acquire_global_lock_from_firmware(uacpi_u32 *lock) { uacpi_u32 value, new_value; uacpi_bool was_owned;`

### 相关符号

`fn try_acquire_global_lock_from_firmware` at `kernel/src/acpi/mutex.c:L18`、`fn do_release_global_lock_to_firmware` at `kernel/src/acpi/mutex.c:L40`、`fn uacpi_acquire_global_lock_from_firmware` at `kernel/src/acpi/mutex.c:L51`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn softirq_init, fn softirq_register, fn softirq_raise, fn softirq_has_pending, fn softirq_handle_pending。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/irq/softirq.c` | L1-L3 | 关键词命中 |
| `kernel/src/irq/irq_manager.c` | L1-L3 | 关键词命中 |
| `kernel/src/irq/irq_manager.h` | L6-L10 | 关键词命中 |
| `kernel/src/irq/softirq.c` | L4-L8 | fn softirq_init |
| `kernel/src/irq/softirq.c` | L9-L13 | fn softirq_register |
| `kernel/src/irq/softirq.c` | L17-L21 | fn softirq_raise |

### 关键代码片段

  - `kernel/src/irq/softirq.c:L1-L3`：关键词命中
    代码片段：`#include <irq/softirq.h> static softirq_handler_t softirq_handlers[SOFTIRQ_MAX] = {0};`
  - `kernel/src/irq/irq_manager.c:L1-L3`：关键词命中
    代码片段：`#include <irq/irq_manager.h> #include <drivers/logger.h> #include <arch/arch.h>`
  - `kernel/src/irq/irq_manager.h:L6-L10`：关键词命中
    代码片段：`typedef struct irq_controller { int64_t (*unmask)(uint64_t irq, uint64_t flags); int64_t (*mask)(uint64_t irq, uint64_t flags); int64_t (*install)(uint64_t irq, uint64_t arg, ui...`
  - `kernel/src/irq/softirq.c:L4-L8`：fn softirq_init
    代码片段：`static uint64_t softirq_pending = 0; void softirq_init(void) { memset(softirq_handlers, 0, sizeof(softirq_handlers)); __atomic_store_n(&softirq_pending, 0, __ATOMIC_RELEASE);`

### 相关符号

`fn softirq_init` at `kernel/src/irq/softirq.c:L4`、`fn softirq_register` at `kernel/src/irq/softirq.c:L9`、`fn softirq_raise` at `kernel/src/irq/softirq.c:L17`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：fn device_minor_in_use, fn device_reset_entry, fn device_open, fn device_close, fn device_ioctl。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/drivers/fb.c` | L137-L141 | 关键词命中 |
| `kernel/src/drivers/fb.h` | L35-L39 | 关键词命中 |
| `kernel/src/drivers/tty.c` | L1-L4 | 关键词命中 |
| `kernel/src/dev/device.c` | L10-L14 | fn device_minor_in_use |
| `kernel/src/dev/device.c` | L94-L98 | fn device_reset_entry |
| `kernel/src/dev/device.c` | L106-L110 | fn device_open |

### 关键代码片段

  - `kernel/src/drivers/fb.c:L137-L141`：关键词命中
    代码片段：`sysfs_write_node(modes, content, strlen(content), 0); vfs_node_t *device = sysfs_child_append(node, "device", true); vfs_node_t *subsystem = sysfs_child_append_symlink(device, "...`
  - `kernel/src/drivers/fb.h:L35-L39`：关键词命中
    代码片段：`/* (physical address) */ uint32_t mmio_len;     /* length of Memory Mapped I/O  */ uint32_t accel;        /* Indicate to driver which	*/ /*  specific chip/card we have	*/ uint16...`
  - `kernel/src/drivers/tty.c:L1-L4`：关键词命中
    代码片段：`#include <drivers/tty.h> #include <dev/device.h> #include <mm/mm.h> #include <boot/boot.h>`
  - `kernel/src/dev/device.c:L10-L14`：fn device_minor_in_use
    代码片段：`static void device_reset_entry(device_t *device); static bool device_minor_in_use(int subtype, uint64_t minor) { device_t *ptr, *tmp; llist_for_each(ptr, tmp, &device_list, node) {`

### 相关符号

`fn device_minor_in_use` at `kernel/src/dev/device.c:L10`、`fn device_reset_entry` at `kernel/src/dev/device.c:L94`、`fn device_open` at `kernel/src/dev/device.c:L106`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：15
- 含证据关键结论数：15（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：1
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
