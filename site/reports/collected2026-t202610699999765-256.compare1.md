# 2026 T202610699999765 比较报告

- 对比历史仓库：2024 刘奶奶找牛奶奶买牛奶队
- 生成时间：2026-07-02T09:03:54.156077+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 刘奶奶找牛奶奶买牛奶队（来源：赛事历史作品）：画像相似度 score=9.59；同属 rcore-variant 风格; 语言构成相似度 0.92; OS 维度重合度 1.00; 代码规模接近度 0.75

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 刘奶奶找牛奶奶买牛奶队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console.rs:L5-L9`：关键词命中
    代码片段：`use spin::Mutex; /// Kernel output writer for console struct KernelOutput;`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device drivers //! //! This module provides device driver implementations:`
  - `os/src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/block/mod.rs:L1-L3`：关键词命中
    代码片段：`mod virtio_blk; mod block_device;`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! This module provides: //! - Virtual filesystem (VFS) abstraction //! - FAT32 and EXT4 filesystem support //! - File descriptor management`
  - `os/src/fs/vfs.rs:L12-L16`：关键词命中
    代码片段：`use super::filesystem::{pre_mount, FS_Type}; // VFS trait, 实现了该trait的文件系统都应该可以直接 // 被 NPUcore 支持 pub trait VFS: DowncastSync {`
  - `os/src/fs/mod.rs:L1-L4`：关键词命中
    代码片段：`#![allow(unused)] mod inode; mod pipe; mod stdio;`
  - `os/src/fatfs/fs.rs:L76-L80`：关键词命中
    代码片段：`let cluster = { let mut fat = self.fat_slice(); match table_alloc_cluster(&mut fat, prev_cluster, hint, self.total_clusters) { Ok(x) => x,`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/timer.rs:L52-L56`：关键词命中
    代码片段：`} let i = get_time() / freq; //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/utils/interrupt_guard.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt state RAII guard //! //! Provides automatic interrupt state management using RAII pattern`
  - `os/src/timer.rs:L81-L85`：关键词命中
    代码片段：`let current_ms = get_time_ms(); let mut timers = TIMERS.exclusive_access(); while let Some(timer) = timers.peek() { if timer.expire_ms <= current_ms { wakeup_task(Arc::clone(&ti...`
  - `os/src/trap/mod.rs:L7-L11`：关键词命中
    代码片段：`current_user_token, exit_current_and_run_next, suspend_current_and_run_next, SignalFlags, }; use crate::timer::{check_timer, set_next_trigger}; use core::arch::{asm, global_asm}...`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L4-L8`：关键词命中
    代码片段：`//! - Physical frame allocation (stack-based and bitmap-based strategies) //! - Virtual address space management //! - Page table operations //! - Heap allocation //!`
  - `os/src/mm/map_area.rs:L9-L13`：关键词命中
    代码片段：`use core::{fmt::Debug, mem}; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{frame_alloc, FrameTracker}; use super::{PTEFlags, PageTable, PageTableEntry}; use super::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum};`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L8-L12`：关键词命中
    代码片段：`pub mod signal; pub mod state_machine; pub mod task; pub mod threads;`
  - `os/src/task/pid.rs:L3-L7`：关键词命中
    代码片段：`//! This module provides: //! - PID allocation with recycling //! - Thread ID allocation //! - RAII-based PID handle for automatic deallocation`
  - `os/src/task/mod.rs:L7-L11`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
  - `os/src/task/switch.S:L13-L17`：关键词命中
    代码片段：`#     next_task_cx_ptr: *const TaskContext # ) # save kernel stack of current task sd sp, 8(a0) # save ra & s0~s11 of current execution`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/main.rs:L107-L111`：关键词命中
    代码片段：`use core::hint::spin_loop; use core::sync::atomic::{AtomicBool, Ordering}; #[link_section = ".data"]`
  - `os/src/timer.rs:L2-L6`：关键词命中
    代码片段：`use core::cmp::Ordering; use core::ops::{Add, AddAssign, Sub}; use core::sync::atomic::{AtomicI64, Ordering as AtomicOrdering}; pub use crate::hal::{get_clock_freq, get_time};`
  - `os/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`mod condvar; mod mutex; mod semaphore;`
  - `os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub trait Mutex: Sync + Send { fn lock(&self); fn unlock(&self);`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L5160-L5164`：关键词命中
    代码片段：`fn is_loongarch_musl_zero_readlink_buffer(buf: *mut u8, bufsiz: usize) -> bool { // The bundled LoongArch musl turns readlink*(..., size=0) into a dummy // one-byte syscall and...`
  - `os/src/syscall/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! This module implements the kernel's system call interface, providing: //! - Unified syscall dispatch mechanism via function pointer table //! - Context abstraction for s...`
  - `os/src/syscall/mod.rs:L48-L52`：关键词命中
    代码片段：`use sync::*; use thread::*; use crate::syscall::sys_info::sys_uname; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> isize {`
  - `os/vendor/sbi-rt/src/binary.rs:L8-L12`：关键词命中
    代码片段：`unsafe { core::arch::asm!( "ecall", in("a7") eid, in("a6") fid,`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 刘奶奶找牛奶奶买牛奶队 在“内存管理”维度发现片段级代码相似度 0.90 （token=0.92, structure=0.86）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, frame_allocator, heap_allocator, init, init_frame_allocator, init_heap, pub。（置信度：high）
  证据：
  - `os/src/mm/mod.rs:L54-L58`：fn init
    代码片段：`/// Initialize the memory management subsystem pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`
  - `os/src/mm/mod.rs:L16-L20`：fn init
    代码片段：`}; pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“设备驱动”维度发现片段级代码相似度 0.65 （token=0.58, structure=0.77）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fmt, fn, for, impl, mut, result, s, self。（置信度：medium）
  证据：
  - `os/src/console.rs:L9-L13`：fn write_str
    代码片段：`impl Write for KernelOutput { fn write_str(&mut self, s: &str) -> fmt::Result { const FLUSH_THRESHOLD: usize = 4; let mut count = 0;`
  - `os/src/console.rs:L5-L9`：fn write_str
    代码片段：`impl Write for Stdout { fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.chars() { console_putchar(c as usize);`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“设备驱动”维度发现片段级代码相似度 0.62 （token=0.57, structure=0.70）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：args, arguments, fmt, fn, print, pub。（置信度：medium）
  证据：
  - `os/src/console.rs:L31-L35`：fn print
    代码片段：`/// Print formatted output to console with interrupt protection pub fn print(args: fmt::Arguments) { // Disable interrupts before acquiring lock to prevent deadlock from timer i...`
  - `os/src/console.rs:L13-L17`：fn print
    代码片段：`} pub fn print(args: fmt::Arguments) { Stdout.write_fmt(args).unwrap(); }`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“内存管理”维度发现片段级代码相似度 0.64 （token=0.52, structure=0.86）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：frametracker, pagetable, physaddr, physpagenum, super, use, virtaddr, virtpagenum。（置信度：medium）
  证据：
  - `os/src/mm/memory_set.rs:L1-L5`：关键词命中
    代码片段：`use super::address::VPNRange; use super::map_area::*; use super::page_table::{copy_to_user, copy_to_user_array, PageTable}; use super::{FrameTracker, PhysAddr, PhysPageNum, Virt...`
  - `os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{frame_alloc, FrameTracker}; use super::{PTEFlags, PageTable, PageTableEntry}; use super::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum};`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“调度与任务管理”维度发现片段级代码相似度 0.68 （token=0.58, structure=0.88）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, impl, new, pub, self。（置信度：medium）
  证据：
  - `os/src/task/elf.rs:L117-L121`：fn new
    代码片段：`impl AuxvEntry { pub fn new(auxv_type: AuxvType, auxv_val: usize) -> Self { Self { auxv_type,`
  - `os/src/task/id.rs:L15-L19`：fn new
    代码片段：`impl RecycleAllocator { pub fn new() -> Self { RecycleAllocator { current: 0,`
- 宏名重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“文件系统”维度发现 4 个同名定义：REG_READ, REG_WRITE, REG_SET, REG_CLEAR。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `dependency/riscv/asm.h:L4-L4`：macro REG_READ
    代码片段：`#define REG_READ(name, offset) \`
  - `os/vendor/riscv/asm.h:L4-L4`：macro REG_READ
    代码片段：`#define REG_READ(name, offset) \`
  - `dependency/riscv/asm.h:L11-L11`：macro REG_WRITE
    代码片段：`#define REG_WRITE(name, offset) \`
  - `os/vendor/riscv/asm.h:L11-L11`：macro REG_WRITE
    代码片段：`#define REG_WRITE(name, offset) \`
- 函数/符号名重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“内存管理”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `dependency/riscv/src/addr/sv39.rs:L13-L13`：fn new
    代码片段：`fn new(addr: usize) -> Self {`
  - `user/src/bin/user_shell.rs:L30-L30`：fn new
    代码片段：`pub fn new(command: &str) -> Self {`
- 函数/符号名重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“同步机制”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `dependency/dep_iso/src/block/ahci.rs:L308-L308`：fn new
    代码片段：`pub fn new(header: usize, size: usize) -> Option<Self> {`
  - `user/src/bin/barrier_condvar.rs:L25-L25`：fn new
    代码片段：`pub fn new() -> Self {`
- 文件路径重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“设备驱动”维度出现同路径源码路径 `os/src/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/console.rs:L5-L9`：关键词命中
    代码片段：`use spin::Mutex; /// Kernel output writer for console struct KernelOutput;`
  - `os/src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
- 文件路径重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“中断与异常”维度出现同路径源码路径 `os/src/timer.rs` / `os/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/timer.rs:L52-L56`：关键词命中
    代码片段：`} let i = get_time() / freq; //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/timer.rs:L81-L85`：关键词命中
    代码片段：`let current_ms = get_time_ms(); let mut timers = TIMERS.exclusive_access(); while let Some(timer) = timers.peek() { if timer.expire_ms <= current_ms { wakeup_task(Arc::clone(&ti...`
- 文件路径重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“内存管理”维度出现同路径源码路径 `os/src/mm/memory_set.rs` / `os/src/mm/memory_set.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/mm/memory_set.rs:L1-L5`：关键词命中
    代码片段：`use super::address::VPNRange; use super::map_area::*; use super::page_table::{copy_to_user, copy_to_user_array, PageTable}; use super::{FrameTracker, PhysAddr, PhysPageNum, Virt...`
  - `os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{frame_alloc, FrameTracker}; use super::{PTEFlags, PageTable, PageTableEntry}; use super::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum};`
- 文件路径重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“系统调用”维度出现同路径源码路径 `os/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/syscall/fs.rs:L5160-L5164`：关键词命中
    代码片段：`fn is_loongarch_musl_zero_readlink_buffer(buf: *mut u8, bufsiz: usize) -> bool { // The bundled LoongArch musl turns readlink*(..., size=0) into a dummy // one-byte syscall and...`
  - `os/src/syscall/fs.rs:L6-L10`：fn sys_write
    代码片段：`use core::mem::size_of; pub fn sys_write(fd: usize, buf: *const u8, len: usize) -> isize { let token = current_user_token(); let process = current_process();`

## 相似点

- 与 2024 刘奶奶找牛奶奶买牛奶队 同属 rcore-variant 风格。（置信度：medium）
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console.rs:L5-L9`：关键词命中
    代码片段：`use spin::Mutex; /// Kernel output writer for console struct KernelOutput;`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device drivers //! //! This module provides device driver implementations:`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! This module provides: //! - Virtual filesystem (VFS) abstraction //! - FAT32 and EXT4 filesystem support //! - File descriptor management`
  - `os/src/fs/vfs.rs:L12-L16`：关键词命中
    代码片段：`use super::filesystem::{pre_mount, FS_Type}; // VFS trait, 实现了该trait的文件系统都应该可以直接 // 被 NPUcore 支持 pub trait VFS: DowncastSync {`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/timer.rs:L52-L56`：关键词命中
    代码片段：`} let i = get_time() / freq; //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/utils/interrupt_guard.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt state RAII guard //! //! Provides automatic interrupt state management using RAII pattern`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L4-L8`：关键词命中
    代码片段：`//! - Physical frame allocation (stack-based and bitmap-based strategies) //! - Virtual address space management //! - Page table operations //! - Heap allocation //!`
  - `os/src/mm/map_area.rs:L9-L13`：关键词命中
    代码片段：`use core::{fmt::Debug, mem}; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L8-L12`：关键词命中
    代码片段：`pub mod signal; pub mod state_machine; pub mod task; pub mod threads;`
  - `os/src/task/pid.rs:L3-L7`：关键词命中
    代码片段：`//! This module provides: //! - PID allocation with recycling //! - Thread ID allocation //! - RAII-based PID handle for automatic deallocation`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/main.rs:L107-L111`：关键词命中
    代码片段：`use core::hint::spin_loop; use core::sync::atomic::{AtomicBool, Ordering}; #[link_section = ".data"]`
  - `os/src/timer.rs:L2-L6`：关键词命中
    代码片段：`use core::cmp::Ordering; use core::ops::{Add, AddAssign, Sub}; use core::sync::atomic::{AtomicI64, Ordering as AtomicOrdering}; pub use crate::hal::{get_clock_freq, get_time};`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L5160-L5164`：关键词命中
    代码片段：`fn is_loongarch_musl_zero_readlink_buffer(buf: *mut u8, bufsiz: usize) -> bool { // The bundled LoongArch musl turns readlink*(..., size=0) into a dummy // one-byte syscall and...`
  - `os/src/syscall/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! This module implements the kernel's system call interface, providing: //! - Unified syscall dispatch mechanism via function pointer table //! - Context abstraction for s...`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 刘奶奶找牛奶奶买牛奶队 的语言构成不同：待测作品为 {'build': 6639, 'markdown': 21272, 'c': 1673, 'cpp': 311, 'yaml': 116, 'rust': 592255, 'asm': 1647, 'text': 644, 'toml': 9867, 'make': 529, 'json': 90}，历史样本为 {'json': 80, 'build': 4070, 'markdown': 14544, 'text': 6192, 'rust': 390243, 'asm': 845, 'toml': 9587, 'c': 27863, 'yaml': 115, 'make': 664}。（置信度：medium）

## 可能创新点

- 待测作品包含 211 个历史样本 2024 刘奶奶找牛奶奶买牛奶队 中未见的独特函数/类型定义（如 `write32`, `Address`, `write16`, `Rv32PageTableWith`, `Rv39PageTableWith`, `write8`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `dependency/dep_pci/pci/src/lib.rs:L113-L113`：fn write32
    代码片段：`pub unsafe fn write32<T: PortOps>(self, _ops: &T, loc: Location, offset: u16, val: u32) {`
  - `dependency/riscv/src/addr/mod.rs:L1-L1`：trait Address
    代码片段：`pub trait Address: core::fmt::Debug + Copy + Clone + PartialEq + Eq + PartialOrd + Ord {`

## 附录：核验摘要

- 关键结论数：27
- 含证据关键结论数：27（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
