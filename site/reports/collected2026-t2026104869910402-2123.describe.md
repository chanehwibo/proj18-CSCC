# 2026 T2026104869910402 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026104869910402-2123`
- 风格：rcore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：2888
- 代码/文本行数：963342
- 主要语言：rust 644628 LOC, json 281998 LOC, markdown 25849 LOC, build 9736 LOC, asm 436 LOC

## 总览

2026 T2026104869910402 是一个 rcore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 644628 LOC, json: 281998 LOC, markdown: 25849 LOC, build: 9736 LOC。仓库包含 2888 个已扫描文件、约 963342 行可分析文本，当前抽取到 44714 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, filesystem/Cargo.toml, filesystem/ext4_rs/Cargo.toml, os/Cargo.toml, os/build.rs。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`# StellaOS Top-Level Makefile # # Contest requirement: 'make all' produces:`
  - `filesystem/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "filesystem" version = "0.1.0"`
  - `filesystem/ext4_rs/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "ext4_rs" version = "1.3.3"`
  - `os/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "os" version = "0.1.0"`
  - `os/build.rs:L1-L3`：构建入口
    代码片段：`static TARGET_PATH: &str = "../user/target/riscv64gc-unknown-none-elf/release/"; fn main() {`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct RecycleAllocator, fn new, fn with_start, fn alloc, fn dealloc。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/task/id.rs` | L5-L9 | 关键词命中 |
| `os/src/task/mod.rs` | L13-L17 | 关键词命中 |
| `os/src/task/futex.rs` | L4-L8 | 关键词命中 |
| `os/src/task/id.rs` | L14-L18 | struct RecycleAllocator |
| `os/src/task/id.rs` | L20-L24 | fn new |
| `os/src/task/id.rs` | L23-L27 | fn with_start |

### 关键代码片段

  - `os/src/task/id.rs:L5-L9`：关键词命中
    代码片段：`}; use crate::mm::{KERNEL_SPACE, PhysPageNum, VirtAddr}; use crate::task::boot_page_table; use polyhal::{MappingFlags, MappingSize::Page4KB}; use crate::sync::UPIntrFreeCell;`
  - `os/src/task/mod.rs:L13-L17`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
  - `os/src/task/futex.rs:L4-L8`：关键词命中
    代码片段：`/// /// ## Race-free design /// The key invariant: checking '*uaddr == val' and enqueuing the current task /// happen under the same 'FUTEX_QUEUES' lock. This prevents the lost-...`
  - `os/src/task/id.rs:L14-L18`：struct RecycleAllocator
    代码片段：`use lazy_static::*; pub struct RecycleAllocator { current: usize, recycled: Vec<usize>,`

### 相关符号

`struct RecycleAllocator` at `os/src/task/id.rs:L14`、`fn new` at `os/src/task/id.rs:L20`、`fn with_start` at `os/src/task/id.rs:L23`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：enum ElfError, struct Align16, fn with_aligned_elf, struct ElfSegment, struct ElfInfo。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/mm/elf.rs` | L74-L78 | 关键词命中 |
| `os/src/mm/mod.rs` | L1-L5 | 关键词命中 |
| `os/src/mm/shm.rs` | L72-L76 | 关键词命中 |
| `os/src/mm/elf.rs` | L4-L8 | enum ElfError |
| `os/src/mm/elf.rs` | L10-L14 | struct Align16 |
| `os/src/mm/elf.rs` | L12-L16 | fn with_aligned_elf |

### 关键代码片段

  - `os/src/mm/elf.rs:L74-L78`：关键词命中
    代码片段：`/// '"/lib/ld-linux-riscv64-lp64d.so.1"'。 pub interp_path: Option<String>, /// 所有 PT_LOAD 段，按文件顺序排列。供 'MemorySet::from_elf()' 消费。 pub segments: Vec<ElfSegment>, }`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`mod address; mod elf; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/shm.rs:L72-L76`：关键词命中
    代码片段：`let mut frames = Vec::with_capacity(pages); for _ in 0..pages { let frame = super::frame_alloc().ok_or(SyscallErrNo::ENOMEM)?; frames.push(frame); }`
  - `os/src/mm/elf.rs:L4-L8`：enum ElfError
    代码片段：`#[derive(Debug)] pub enum ElfError { InvalidMagic, InvalidFormat,`

### 相关符号

`enum ElfError` at `os/src/mm/elf.rs:L4`、`struct Align16` at `os/src/mm/elf.rs:L10`、`fn with_aligned_elf` at `os/src/mm/elf.rs:L12`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn reset_tick_count, fn user_loop, fn trap_pc, fn panic_kernel_page_fault, fn handle_user_trap。（置信度：medium）
  - 静态识别到 276 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/syscall/fs.rs` | L19-L23 | 关键词命中 |
| `os/src/syscall/mod.rs` | L194-L198 | 关键词命中 |
| `os/src/syscall/net.rs` | L73-L77 | 关键词命中 |
| `os/src/trap/mod.rs` | L28-L32 | fn reset_tick_count |
| `os/src/trap/mod.rs` | L36-L40 | fn user_loop |
| `os/src/trap/mod.rs` | L47-L51 | fn trap_pc |

### 关键代码片段

  - `os/src/syscall/fs.rs:L19-L23`：关键词命中
    代码片段：`/// unlinkat flags: 允许删除目录项指向的是目录（否则返回 EPERM）。 const AT_REMOVEDIR: u32 = 0x200; /// 路径型 syscall flags: 不跟随最终分量的符号链接。 const AT_SYMLINK_NOFOLLOW: usize = 0x100; /// 路径型 syscall fl...`
  - `os/src/syscall/mod.rs:L194-L198`：关键词命中
    代码片段：`use polyhal_trap::trapframe::TrapFrameArgs; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> SyscallRet { let tid = crate::task::current_task() .and_then(|t| {`
  - `os/src/syscall/net.rs:L73-L77`：关键词命中
    代码片段：`// ── syscall 实现 ── const AF_INET: usize = 2;`
  - `os/src/trap/mod.rs:L28-L32`：fn reset_tick_count
    代码片段：`/// 在任务切换时重置 tick 计数（由 run_tasks 调用）。 pub fn reset_tick_count() { TICK_COUNT.store(0, Ordering::Relaxed); }`

### 相关符号

`fn reset_tick_count` at `os/src/trap/mod.rs:L28`、`fn user_loop` at `os/src/trap/mod.rs:L36`、`fn trap_pc` at `os/src/trap/mod.rs:L47`、`fn trace_path_syscall` at `os/src/syscall/fs.rs:L191`、`fn sys_umask` at `os/src/syscall/fs.rs:L255`、`fn sys_debug_dump_dentry` at `os/src/syscall/fs.rs:L263`、`fn sys_write` at `os/src/syscall/fs.rs:L276`、`fn sys_read` at `os/src/syscall/fs.rs:L317`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：fn from, fn make_pipe, fn make_eventfd, fn init, fn list_test_scripts。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/fs/mod.rs` | L13-L17 | 关键词命中 |
| `os/src/fs/fdtable.rs` | L2-L6 | 关键词命中 |
| `os/src/syscall/fs.rs` | L6-L10 | 关键词命中 |
| `os/src/fs/mod.rs` | L19-L23 | fn from |
| `os/src/fs/mod.rs` | L44-L48 | fn make_pipe |
| `os/src/fs/mod.rs` | L54-L58 | fn make_eventfd |

### 关键代码片段

  - `os/src/fs/mod.rs:L13-L17`：关键词命中
    代码片段：`pub use fdtable::FdTable; pub use filesystem::vfs::{chdir, dcache_children, dentry_cache_cap, dentry_cache_hits, dentry_cache_len, dentry_cache_misses, get_cwd, get_root_dentry,...`
  - `os/src/fs/fdtable.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use filesystem::vfs::File as VfsFile; use filesystem::vfs_defs::{OpenFlags, VfsError, VfsRet};`
  - `os/src/syscall/fs.rs:L6-L10`：关键词命中
    代码片段：`use super::stub::{sys_getegid, sys_geteuid, sys_getgid, sys_getuid}; use crate::fs::{self, InodeMode, Kstat, OpenFlags, SeekFlags, Statfs, VfsError, VfsFile}; use crate::fs::Den...`
  - `os/src/fs/mod.rs:L19-L23`：fn from
    代码片段：`impl From<VfsError> for SyscallErrNo { fn from(e: VfsError) -> Self { match e { VfsError::NotSupported     => SyscallErrNo::ENOTSUP,`

### 相关符号

`fn from` at `os/src/fs/mod.rs:L19`、`fn make_pipe` at `os/src/fs/mod.rs:L44`、`fn make_eventfd` at `os/src/fs/mod.rs:L54`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn current_task_raw, fn clone_task_raw, fn drop_task_raw, fn wakeup_task_raw, fn block_current_task_raw。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/sync/mod.rs` | L8-L12 | 关键词命中 |
| `os/src/sync/mutex.rs` | L5-L9 | 关键词命中 |
| `filesystem/src/sync.rs` | L1-L3 | 关键词命中 |
| `os/src/sync/mod.rs` | L12-L16 | fn current_task_raw |
| `os/src/sync/mod.rs` | L16-L20 | fn clone_task_raw |
| `os/src/sync/mod.rs` | L23-L27 | fn drop_task_raw |

### 关键代码片段

  - `os/src/sync/mod.rs:L8-L12`：关键词命中
    代码片段：`pub use ksync::{ Condvar, Mutex, MutexBlocking, MutexSpin, Semaphore, UPIntrFreeCell, UPIntrRefMut, UPSafeCellRaw, };`
  - `os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`// use alloc::{collections::VecDeque, sync::Arc}; // pub trait Mutex: Sync + Send { //     fn lock(&self); //     fn unlock(&self);`
  - `filesystem/src/sync.rs:L1-L3`：关键词命中
    代码片段：`pub use ksync::{ Mutex, MutexBlocking, MutexSpin, UPIntrFreeCell, UPIntrRefMut, UPSafeCellRaw, };`
  - `os/src/sync/mod.rs:L12-L16`：fn current_task_raw
    代码片段：`}; fn current_task_raw() -> usize { Arc::into_raw(current_task().expect("current_task not available")) as usize }`

### 相关符号

`fn current_task_raw` at `os/src/sync/mod.rs:L12`、`fn clone_task_raw` at `os/src/sync/mod.rs:L16`、`fn drop_task_raw` at `os/src/sync/mod.rs:L23`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：struct Timespec, struct Timeval, fn get_time_ms, fn get_time_spec, fn get_time_val。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/timer.rs` | L8-L12 | 关键词命中 |
| `os/src/trap/mod.rs` | L11-L15 | 关键词命中 |
| `os/src/drivers/plic.rs` | L1-L4 | 关键词命中 |
| `os/src/timer.rs` | L19-L23 | struct Timespec |
| `os/src/timer.rs` | L26-L30 | struct Timeval |
| `os/src/timer.rs` | L31-L35 | fn get_time_ms |

### 关键代码片段

  - `os/src/timer.rs:L8-L12`：关键词命中
    代码片段：`use alloc::sync::Arc; use lazy_static::*; use polyhal::timer::{current_time, set_next_timer}; const TICKS_PER_SEC: usize = 100;`
  - `os/src/trap/mod.rs:L11-L15`：关键词命中
    代码片段：`SigInfo, SIGBUS, SIGSEGV, SIGILL, SIGTRAP, BUS_ADRERR, SEGV_MAPERR, ILL_ILLOPC, TRAP_BRKPT, }; use crate::timer::{check_timer, set_next_trigger}; use crate::config::PAGE_SIZE; u...`
  - `os/src/drivers/plic.rs:L1-L4`：关键词命中
    代码片段：`#[allow(clippy::upper_case_acronyms)] pub struct PLIC { base_addr: usize, }`
  - `os/src/timer.rs:L19-L23`：struct Timespec
    代码片段：`#[repr(C)] #[derive(Debug, Clone, Copy, Default)] pub struct Timespec { pub tv_sec: i64, pub tv_nsec: i64,`

### 相关符号

`struct Timespec` at `os/src/timer.rs:L19`、`struct Timeval` at `os/src/timer.rs:L26`、`fn get_time_ms` at `os/src/timer.rs:L31`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct Stdout, fn write_str, fn print, struct PLIC, enum IntrTargetPriority。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/console.rs` | L1-L4 | 关键词命中 |
| `os/src/drivers/bus/mod.rs` | L1-L1 | 关键词命中 |
| `os/src/drivers/gpu/mod.rs` | L2-L6 | 关键词命中 |
| `os/src/console.rs` | L3-L7 | struct Stdout |
| `os/src/console.rs` | L6-L10 | fn write_str |
| `os/src/console.rs` | L14-L18 | fn print |

### 关键代码片段

  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
  - `os/src/drivers/bus/mod.rs:L1-L1`：关键词命中
    代码片段：`pub mod virtio;`
  - `os/src/drivers/gpu/mod.rs:L2-L6`：关键词命中
    代码片段：`use core::ptr::NonNull; use crate::drivers::bus::virtio::VirtioHal; use crate::sync::UPIntrFreeCell; use alloc::{sync::Arc, vec::Vec};`
  - `os/src/console.rs:L3-L7`：struct Stdout
    代码片段：`use core::fmt::{self, Write}; struct Stdout; impl Write for Stdout {`

### 相关符号

`struct Stdout` at `os/src/console.rs:L3`、`fn write_str` at `os/src/console.rs:L6`、`fn print` at `os/src/console.rs:L14`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
