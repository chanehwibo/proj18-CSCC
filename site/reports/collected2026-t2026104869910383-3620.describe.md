# 2026 T2026104869910383 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026104869910383-3620`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：9110
- 代码/文本行数：3035162
- 主要语言：rust 2589340 LOC, json 285078 LOC, markdown 77862 LOC, build 36266 LOC, c 28944 LOC

## 总览

2026 T2026104869910383 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 2589340 LOC, json: 285078 LOC, markdown: 77862 LOC, build: 36266 LOC。仓库包含 9110 个已扫描文件、约 3035162 行可分析文本，当前抽取到 181279 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, Makefile, kernel/Cargo.toml, make/Makefile, page_table_entry/Cargo.toml。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] resolver = "2" members = ["kernel"]`
  - `Makefile:L1-L3`：构建入口
    代码片段：`# ============================================================================ # XJC-OS 构建系统 —— 双架构内核编译与测试编排 # ==================================================================...`
  - `kernel/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "xjc-kernel" version.workspace = true`
  - `make/Makefile:L1-L3`：构建入口
    代码片段：`# Available arguments: # * General options: #     - 'ARCH': Target architecture: x86_64, riscv64, aarch64, loongarch64`
  - `page_table_entry/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "page_table_entry" description = "Page table entry definition for various hardware architectures"`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct Credentials, struct AssumeSync, fn deref, struct Thread, struct VforkState。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/task/mod.rs` | L1-L3 | 关键词命中 |
| `kernel/src/task/ops.rs` | L31-L35 | 关键词命中 |
| `vendor/x86/src/task.rs` | L1-L3 | 关键词命中 |
| `kernel/src/task/mod.rs` | L33-L37 | struct Credentials |
| `kernel/src/task/mod.rs` | L57-L61 | struct AssumeSync |
| `kernel/src/task/mod.rs` | L64-L68 | fn deref |

### 关键代码片段

  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. mod futex;`
  - `kernel/src/task/ops.rs:L31-L35`：关键词命中
    代码片段：`static SESSION_TABLE: RwLock<WeakMap<Pid, Weak<Session>>> = RwLock::new(WeakMap::new()); /// Cleanup expired entries in the task tables. /// /// This function is intended to be...`
  - `vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`
  - `kernel/src/task/mod.rs:L33-L37`：struct Credentials
    代码片段：`/// Minimal Linux process credentials used by permission checks. #[derive(Clone, Copy, Debug, Eq, PartialEq)] pub struct Credentials { pub ruid: u32, pub euid: u32,`

### 相关符号

`struct Credentials` at `kernel/src/task/mod.rs:L33`、`struct AssumeSync` at `kernel/src/task/mod.rs:L57`、`fn deref` at `kernel/src/task/mod.rs:L64`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：struct IoVec, struct IoVectorBuf, fn new, fn read_with, fn fill_with。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/mm/access.rs` | L86-L90 | 关键词命中 |
| `kernel/src/mm/loader.rs` | L33-L37 | 关键词命中 |
| `kernel/src/mm/aspace/mod.rs` | L5-L9 | 关键词命中 |
| `kernel/src/mm/io.rs` | L14-L18 | struct IoVec |
| `kernel/src/mm/io.rs` | L20-L24 | struct IoVectorBuf |
| `kernel/src/mm/io.rs` | L27-L31 | fn new |

### 关键代码片段

  - `kernel/src/mm/access.rs:L86-L90`：关键词命中
    代码片段：`// TODO: this is inefficient, but we have to do this instead of // querying the page table since the page might has not been // allocated yet. let curr = current();`
  - `kernel/src/mm/loader.rs:L33-L37`：关键词命中
    代码片段：`#[cfg(not(any(target_arch = "aarch64", target_arch = "loongarch64")))] { // ARMv8 (aarch64) and LoongArch64 use separate page tables for user space // (aarch64: TTBR0_EL1, Loong...`
  - `kernel/src/mm/aspace/mod.rs:L5-L9`：关键词命中
    代码片段：`use axhal::{ mem::phys_to_virt, paging::{MappingFlags, PageTable}, trap::PageFaultFlags, };`
  - `kernel/src/mm/io.rs:L14-L18`：struct IoVec
    代码片段：`#[repr(C)] #[derive(Debug, Copy, Clone, AnyBitPattern)] pub struct IoVec { pub iov_base: *mut u8, pub iov_len: isize,`

### 相关符号

`struct IoVec` at `kernel/src/mm/io.rs:L14`、`struct IoVectorBuf` at `kernel/src/mm/io.rs:L20`、`fn new` at `kernel/src/mm/io.rs:L27`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn handle_syscall, fn current_credentials, fn set_current_credentials, fn is_privileged, fn id_is_current。（置信度：medium）
  - 静态识别到 595 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/syscall/mod.rs` | L4-L8 | 关键词命中 |
| `kernel/src/syscall/timer.rs` | L1-L4 | 关键词命中 |
| `kernel/src/syscall/fs/ctl.rs` | L79-L83 | 关键词命中 |
| `kernel/src/syscall/mod.rs` | L77-L81 | fn handle_syscall |
| `kernel/src/syscall/sys.rs` | L19-L23 | fn current_credentials |
| `kernel/src/syscall/sys.rs` | L23-L27 | fn set_current_credentials |

### 关键代码片段

  - `kernel/src/syscall/mod.rs:L4-L8`：关键词命中
    代码片段：`//! //! 本模块是 XJC-OS 内核中所有 Linux 系统调用的统一入口点。 //! 'handle_syscall()' 从用户上下文 'UserContext' 中提取系统调用号（sysno）， //! 通过一个巨大的 'match' 语句将其路由到对应的处理函数，最后将返回值 //! 写回 'UserContext' 的 retval...`
  - `kernel/src/syscall/timer.rs:L1-L4`：关键词命中
    代码片段：`//! POSIX timer ('timer_create' / 'timer_settime' / 'timer_gettime' / //! 'timer_delete' / 'timer_getoverrun') and 'timerfd_*' syscall //! implementations.`
  - `kernel/src/syscall/fs/ctl.rs:L79-L83`：关键词命中
    代码片段：`pub fn sys_chroot(path: *const c_char) -> AxResult<isize> { // Linux requires CAP_SYS_CHROOT. if crate::syscall::sys::sys_getuid()? != 0 { return Err(AxError::PermissionDenied); }`
  - `kernel/src/syscall/mod.rs:L77-L81`：fn handle_syscall
    代码片段：`/// - 调试模式下输出 'Syscall {name}' 跟踪日志 /// - 执行结束后输出 'Syscall {name} return {result}' 调试日志 pub fn handle_syscall(uctx: &mut UserContext) { let Some(sysno) = Sysno::new(uctx.sysno()...`

### 相关符号

`fn handle_syscall` at `kernel/src/syscall/mod.rs:L77`、`fn current_credentials` at `kernel/src/syscall/sys.rs:L19`、`fn set_current_credentials` at `kernel/src/syscall/sys.rs:L23`、`fn handle_syscall` at `kernel/src/syscall/mod.rs:L77`、`fn sys_getuid` at `kernel/src/syscall/sys.rs:L35`、`fn sys_geteuid` at `kernel/src/syscall/sys.rs:L39`、`fn sys_getgid` at `kernel/src/syscall/sys.rs:L43`、`fn sys_getegid` at `kernel/src/syscall/sys.rs:L47`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct FsCache, fn fs_stat_cache_epoch, fn fs_location_cache_epoch, fn ensure_stat_cache_epoch, fn ensure_location_cache_epoch。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/file/fs.rs` | L1-L3 | 关键词命中 |
| `vendor/axfatfs/src/fs.rs` | L18-L22 | 关键词命中 |
| `kernel/src/pseudofs/fs.rs` | L98-L102 | 关键词命中 |
| `kernel/src/file/fs.rs` | L64-L68 | struct FsCache |
| `kernel/src/file/fs.rs` | L84-L88 | fn fs_stat_cache_epoch |
| `kernel/src/file/fs.rs` | L88-L92 | fn fs_location_cache_epoch |

### 关键代码片段

  - `kernel/src/file/fs.rs:L1-L3`：关键词命中
    代码片段：`//! 文件系统句柄（File）实现 —— 将 VFS 节点包装为 FileLike trait。 //! //! # File 对象`
  - `vendor/axfatfs/src/fs.rs:L18-L22`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `kernel/src/pseudofs/fs.rs:L98-L102`：关键词命中
    代码片段：`let metadata = Metadata { device: 0, inode: ino, nlink: 1, mode,`
  - `kernel/src/file/fs.rs:L64-L68`：struct FsCache
    代码片段：`static FS_LOCATION_CACHE_EPOCH: AtomicU64 = AtomicU64::new(0); struct FsCache { stat_epoch: u64, location_epoch: u64,`

### 相关符号

`struct FsCache` at `kernel/src/file/fs.rs:L64`、`fn fs_stat_cache_epoch` at `kernel/src/file/fs.rs:L84`、`fn fs_location_cache_epoch` at `kernel/src/file/fs.rs:L88`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct LockRange, struct RecordLock, fn current_pid, fn record_lock_key, fn checked_offset。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/file/record_lock.rs` | L23-L27 | 关键词命中 |
| `kernel/src/syscall/sync/futex.rs` | L1-L5 | 关键词命中 |
| `vendor/concurrent-queue/src/sync.rs` | L1-L5 | 关键词命中 |
| `kernel/src/file/record_lock.rs` | L34-L38 | struct LockRange |
| `kernel/src/file/record_lock.rs` | L40-L44 | struct RecordLock |
| `kernel/src/file/record_lock.rs` | L51-L55 | fn current_pid |

### 关键代码片段

  - `kernel/src/file/record_lock.rs:L23-L27`：关键词命中
    代码片段：`use axerrno::{AxError, AxResult}; use axio::{Seek, SeekFrom}; use axsync::Mutex; use axtask::current; use lazy_static::lazy_static;`
  - `kernel/src/syscall/sync/futex.rs:L1-L5`：关键词命中
    代码片段：`//! futex 系统调用实现。 //! //! futex（Fast Userspace muTEX）是 Linux 中实现用户态锁的基础原语。 //! pthread_mutex、semaphore、barrier 等最终都通过 futex 实现。 //!`
  - `vendor/concurrent-queue/src/sync.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization facade to choose between 'core' primitives and 'loom' primitives. #[cfg(all(feature = "portable-atomic", not(loom)))] mod sync_impl { pub(crate) use core::cell;`
  - `kernel/src/file/record_lock.rs:L34-L38`：struct LockRange
    代码片段：`#[derive(Clone, Copy)] struct LockRange { start: u64, end: Option<u64>,`

### 相关符号

`struct LockRange` at `kernel/src/file/record_lock.rs:L34`、`struct RecordLock` at `kernel/src/file/record_lock.rs:L40`、`fn current_pid` at `kernel/src/file/record_lock.rs:L51`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn time_value_from_nanos, fn time_value_from_ticks, struct Entry, fn eq, fn partial_cmp。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/task/timer.rs` | L103-L107 | 关键词命中 |
| `vendor/axcpu/src/trap.rs` | L1-L3 | 关键词命中 |
| `kernel/src/syscall/timer.rs` | L1-L3 | 关键词命中 |
| `kernel/src/task/timer.rs` | L27-L31 | fn time_value_from_nanos |
| `kernel/src/task/timer.rs` | L33-L37 | fn time_value_from_ticks |
| `kernel/src/task/timer.rs` | L37-L41 | struct Entry |

### 关键代码片段

  - `kernel/src/task/timer.rs:L103-L107`：关键词命中
    代码片段：`} /// The type of interval timer. #[repr(i32)] #[allow(non_camel_case_types)]`
  - `vendor/axcpu/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use memory_addr::VirtAddr;`
  - `kernel/src/syscall/timer.rs:L1-L3`：关键词命中
    代码片段：`//! POSIX timer ('timer_create' / 'timer_settime' / 'timer_gettime' / //! 'timer_delete' / 'timer_getoverrun') and 'timerfd_*' syscall //! implementations.`
  - `kernel/src/task/timer.rs:L27-L31`：fn time_value_from_nanos
    代码片段：`use crate::task::poll_timer; fn time_value_from_nanos(nanos: usize) -> TimeValue { let secs = nanos as u64 / NANOS_PER_SEC; let nsecs = nanos as u64 - secs * NANOS_PER_SEC;`

### 相关符号

`fn time_value_from_nanos` at `kernel/src/task/timer.rs:L27`、`fn time_value_from_ticks` at `kernel/src/task/timer.rs:L33`、`struct Entry` at `kernel/src/task/timer.rs:L37`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：enum DeviceMmap, trait DeviceOps, fn read_at, fn write_at, fn ioctl。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `vendor/uart_16550/src/mmio.rs` | L6-L10 | 关键词命中 |
| `vendor/axdriver/src/drivers.rs` | L9-L13 | 关键词命中 |
| `vendor/virtio-drivers/src/hal.rs` | L5-L9 | 关键词命中 |
| `kernel/src/pseudofs/device.rs` | L14-L18 | enum DeviceMmap |
| `kernel/src/pseudofs/device.rs` | L26-L30 | trait DeviceOps |
| `kernel/src/pseudofs/device.rs` | L28-L32 | fn read_at |

### 关键代码片段

  - `vendor/uart_16550/src/mmio.rs:L6-L10`：关键词命中
    代码片段：`use crate::{LineStsFlags, WouldBlockError}; /// A memory-mapped UART. #[derive(Debug)] pub struct MmioSerialPort {`
  - `vendor/axdriver/src/drivers.rs:L9-L13`：关键词命中
    代码片段：`pub use super::dummy::*; use crate::AxDeviceEnum; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `kernel/src/pseudofs/device.rs:L14-L18`：enum DeviceMmap
    代码片段：`/// Mmap behavior for devices. pub enum DeviceMmap { /// The device is not mappable. None,`

### 相关符号

`enum DeviceMmap` at `kernel/src/pseudofs/device.rs:L14`、`trait DeviceOps` at `kernel/src/pseudofs/device.rs:L26`、`fn read_at` at `kernel/src/pseudofs/device.rs:L28`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
