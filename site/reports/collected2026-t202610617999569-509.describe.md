# 2026 T202610617999569 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t202610617999569-509`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：9732
- 代码/文本行数：3598093
- 主要语言：rust 2881273 LOC, json 282616 LOC, c 210872 LOC, text 103607 LOC, markdown 74937 LOC

## 总览

2026 T202610617999569 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 2881273 LOC, json: 282616 LOC, c: 210872 LOC, text: 103607 LOC。仓库包含 9732 个已扫描文件、约 3598093 行可分析文本，当前抽取到 199436 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, Makefile, kernel/Cargo.toml, make/Makefile, syscalls_code/Makefile。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] resolver = "2" members = ["kernel"]`
  - `Makefile:L1-L3`：构建入口
    代码片段：`# Build Options export ARCH := riscv64 export LOG := warn`
  - `kernel/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "starry-kernel" version.workspace = true`
  - `make/Makefile:L1-L3`：构建入口
    代码片段：`# Available arguments: # * General options: #     - 'ARCH': Target architecture: x86_64, riscv64, aarch64, loongarch64`
  - `syscalls_code/Makefile:L1-L3`：构建入口
    代码片段：`# SPDX-License-Identifier: GPL-2.0-or-later #    testcases/kernel/syscalls Makefile. #`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：enum ProcessWaitStatus, struct NamespaceState, fn child_of, struct ProcessNamespaces, struct AssumeSync。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/task/mod.rs` | L1-L3 | 关键词命中 |
| `kernel/src/task/ops.rs` | L28-L32 | 关键词命中 |
| `vendor/x86/src/task.rs` | L1-L3 | 关键词命中 |
| `kernel/src/task/mod.rs` | L32-L36 | enum ProcessWaitStatus |
| `kernel/src/task/mod.rs` | L43-L47 | struct NamespaceState |
| `kernel/src/task/mod.rs` | L58-L62 | fn child_of |

### 关键代码片段

  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. mod futex;`
  - `kernel/src/task/ops.rs:L28-L32`：关键词命中
    代码片段：`static SESSION_TABLE: RwLock<WeakMap<Pid, Weak<Session>>> = RwLock::new(WeakMap::new()); /// Cleanup expired entries in the task tables. /// /// This function is intended to be...`
  - `vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`
  - `kernel/src/task/mod.rs:L32-L36`：enum ProcessWaitStatus
    代码片段：`#[derive(Debug, Clone, Copy)] pub enum ProcessWaitStatus { Stopped(Signo), Continued(Signo),`

### 相关符号

`enum ProcessWaitStatus` at `kernel/src/task/mod.rs:L32`、`struct NamespaceState` at `kernel/src/task/mod.rs:L43`、`fn child_of` at `kernel/src/task/mod.rs:L58`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：struct IoVec, struct IoVectorBuf, fn new, fn read_with, fn fill_with。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/mm/access.rs` | L88-L92 | 关键词命中 |
| `kernel/src/mm/loader.rs` | L69-L73 | 关键词命中 |
| `kernel/src/mm/aspace/mod.rs` | L5-L9 | 关键词命中 |
| `kernel/src/mm/io.rs` | L8-L12 | struct IoVec |
| `kernel/src/mm/io.rs` | L14-L18 | struct IoVectorBuf |
| `kernel/src/mm/io.rs` | L21-L25 | fn new |

### 关键代码片段

  - `kernel/src/mm/access.rs:L88-L92`：关键词命中
    代码片段：`// TODO: this is inefficient, but we have to do this instead of // querying the page table since the page might has not been // allocated yet. let curr = current();`
  - `kernel/src/mm/loader.rs:L69-L73`：关键词命中
    代码片段：`#[cfg(not(any(target_arch = "aarch64", target_arch = "loongarch64")))] { // ARMv8 (aarch64) and LoongArch64 use separate page tables for user space // (aarch64: TTBR0_EL1, Loong...`
  - `kernel/src/mm/aspace/mod.rs:L5-L9`：关键词命中
    代码片段：`use axhal::{ mem::phys_to_virt, paging::{MappingFlags, PageTable, PagingError}, trap::PageFaultFlags, };`
  - `kernel/src/mm/io.rs:L8-L12`：struct IoVec
    代码片段：`#[repr(C)] #[derive(Debug, Copy, Clone, AnyBitPattern)] pub struct IoVec { pub iov_base: *mut u8, pub iov_len: isize,`

### 相关符号

`struct IoVec` at `kernel/src/mm/io.rs:L8`、`struct IoVectorBuf` at `kernel/src/mm/io.rs:L14`、`fn new` at `kernel/src/mm/io.rs:L21`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：struct BpfInsn, fn src_reg, struct Attr, fn new, fn u32。（置信度：medium）
  - 静态识别到 745 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/syscall/mod.rs` | L27-L31 | 关键词命中 |
| `kernel/src/syscall/fs/ctl.rs` | L504-L508 | 关键词命中 |
| `kernel/src/syscall/net/io.rs` | L14-L18 | 关键词命中 |
| `kernel/src/syscall/bpf.rs` | L24-L28 | struct BpfInsn |
| `kernel/src/syscall/bpf.rs` | L32-L36 | fn src_reg |
| `kernel/src/syscall/bpf.rs` | L37-L41 | struct Attr |

### 关键代码片段

  - `kernel/src/syscall/mod.rs:L27-L31`：关键词命中
    代码片段：`} pub fn handle_syscall(uctx: &mut UserContext) { let Some(sysno) = Sysno::new(uctx.sysno()) else { warn!("Invalid syscall number: {}", uctx.sysno());`
  - `kernel/src/syscall/fs/ctl.rs:L504-L508`：关键词命中
    代码片段：`} // Directory buffer for getdents64 syscall struct DirBuffer { buf: Vec<u8>,`
  - `kernel/src/syscall/net/io.rs:L14-L18`：关键词命中
    代码片段：`file::{FileLike, Socket, add_file_like}, mm::{IoVec, IoVectorBuf, UserConstPtr, UserPtr, VmBytes, VmBytesMut}, syscall::net::{CMsg, CMsgBuilder}, };`
  - `kernel/src/syscall/bpf.rs:L24-L28`：struct BpfInsn
    代码片段：`#[repr(C)] #[derive(Clone, Copy)] struct BpfInsn { code: u8, regs: u8,`

### 相关符号

`struct BpfInsn` at `kernel/src/syscall/bpf.rs:L24`、`fn src_reg` at `kernel/src/syscall/bpf.rs:L32`、`struct Attr` at `kernel/src/syscall/bpf.rs:L37`、`fn sys_bpf` at `kernel/src/syscall/bpf.rs:L113`、`fn handle_syscall` at `kernel/src/syscall/mod.rs:L27`、`fn sys_getresgid` at `kernel/src/syscall/sys.rs:L55`、`fn sys_getresuid` at `kernel/src/syscall/sys.rs:L64`、`fn sys_getuid` at `kernel/src/syscall/sys.rs:L73`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct SpecialNodeMetadata, fn register_special_node, fn inode_flags, fn set_inode_flags, fn should_update_atime_for_access。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/file/fs.rs` | L202-L206 | 关键词命中 |
| `kernel/src/file/pipe.rs` | L87-L91 | 关键词命中 |
| `kernel/src/pseudofs/fs.rs` | L98-L102 | 关键词命中 |
| `kernel/src/file/fs.rs` | L31-L35 | struct SpecialNodeMetadata |
| `kernel/src/file/fs.rs` | L40-L44 | fn register_special_node |
| `kernel/src/file/fs.rs` | L46-L50 | fn inode_flags |

### 关键代码片段

  - `kernel/src/file/fs.rs:L202-L206`：关键词命中
    代码片段：`Kstat { dev: metadata.device, ino: metadata.inode, mode, nlink: metadata.nlink as _,`
  - `kernel/src/file/pipe.rs:L87-L91`：关键词命中
    代码片段：`} pub fn new_fifo(device: u64, inode: u64, read_side: bool) -> Self { let key = (device, inode); let shared = {`
  - `kernel/src/pseudofs/fs.rs:L98-L102`：关键词命中
    代码片段：`let metadata = Metadata { device: 0, inode: ino, nlink: 1, mode,`
  - `kernel/src/file/fs.rs:L31-L35`：struct SpecialNodeMetadata
    代码片段：`#[derive(Clone, Copy)] struct SpecialNodeMetadata { mode: u32, rdev: axfs_ng_vfs::DeviceId,`

### 相关符号

`struct SpecialNodeMetadata` at `kernel/src/file/fs.rs:L31`、`fn register_special_node` at `kernel/src/file/fs.rs:L40`、`fn inode_flags` at `kernel/src/file/fs.rs:L46`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn assert_unsigned, fn sys_futex, fn sys_get_robust_list, fn resolve_robust_list_tid, fn check_get_robust_list_permission。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/syscall/sync/futex.rs` | L1-L3 | 关键词命中 |
| `vendor/concurrent-queue/src/sync.rs` | L1-L5 | 关键词命中 |
| `kernel/src/syscall/sync/membarrier.rs` | L1-L3 | 关键词命中 |
| `kernel/src/syscall/sync/futex.rs` | L15-L19 | fn assert_unsigned |
| `kernel/src/syscall/sync/futex.rs` | L23-L27 | fn sys_futex |
| `kernel/src/syscall/sync/futex.rs` | L125-L129 | fn sys_get_robust_list |

### 关键代码片段

  - `kernel/src/syscall/sync/futex.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::Ordering; use axerrno::{AxError, AxResult, LinuxError};`
  - `vendor/concurrent-queue/src/sync.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization facade to choose between 'core' primitives and 'loom' primitives. #[cfg(all(feature = "portable-atomic", not(loom)))] mod sync_impl { pub(crate) use core::cell;`
  - `kernel/src/syscall/sync/membarrier.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{Ordering, compiler_fence}; use axerrno::{AxError, AxResult, LinuxError};`
  - `kernel/src/syscall/sync/futex.rs:L15-L19`：fn assert_unsigned
    代码片段：`}; fn assert_unsigned(value: u32) -> AxResult<u32> { if (value as i32) < 0 { Err(AxError::InvalidInput)`

### 相关符号

`fn assert_unsigned` at `kernel/src/syscall/sync/futex.rs:L15`、`fn sys_futex` at `kernel/src/syscall/sync/futex.rs:L23`、`fn sys_get_robust_list` at `kernel/src/syscall/sync/futex.rs:L125`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn time_value_from_nanos, struct TimerKey, struct Entry, struct AlarmRuntime, fn insert。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/task/timer.rs` | L68-L72 | 关键词命中 |
| `vendor/axcpu/src/trap.rs` | L1-L3 | 关键词命中 |
| `kernel/src/task/mod.rs` | L6-L10 | 关键词命中 |
| `kernel/src/task/timer.rs` | L17-L21 | fn time_value_from_nanos |
| `kernel/src/task/timer.rs` | L24-L28 | struct TimerKey |
| `kernel/src/task/timer.rs` | L30-L34 | struct Entry |

### 关键代码片段

  - `kernel/src/task/timer.rs:L68-L72`：关键词命中
    代码片段：`} /// The type of interval timer. #[repr(i32)] #[allow(non_camel_case_types)]`
  - `vendor/axcpu/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use memory_addr::VirtAddr;`
  - `kernel/src/task/mod.rs:L6-L10`：关键词命中
    代码片段：`mod signal; mod stat; mod timer; mod user;`
  - `kernel/src/task/timer.rs:L17-L21`：fn time_value_from_nanos
    代码片段：`use crate::task::{AsThread, poll_timer}; fn time_value_from_nanos(nanos: usize) -> TimeValue { let secs = nanos as u64 / NANOS_PER_SEC; let nsecs = nanos as u64 - secs * NANOS_P...`

### 相关符号

`fn time_value_from_nanos` at `kernel/src/task/timer.rs:L17`、`struct TimerKey` at `kernel/src/task/timer.rs:L24`、`struct Entry` at `kernel/src/task/timer.rs:L30`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：enum DeviceMmap, trait DeviceOps, fn read_at, fn write_at, fn ioctl。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `vendor/uart_16550/src/mmio.rs` | L6-L10 | 关键词命中 |
| `vendor/axdriver/src/drivers.rs` | L6-L10 | 关键词命中 |
| `vendor/virtio-drivers/src/hal.rs` | L5-L9 | 关键词命中 |
| `kernel/src/pseudofs/device.rs` | L14-L18 | enum DeviceMmap |
| `kernel/src/pseudofs/device.rs` | L26-L30 | trait DeviceOps |
| `kernel/src/pseudofs/device.rs` | L28-L32 | fn read_at |

### 关键代码片段

  - `vendor/uart_16550/src/mmio.rs:L6-L10`：关键词命中
    代码片段：`use crate::{LineStsFlags, WouldBlockError}; /// A memory-mapped UART. #[derive(Debug)] pub struct MmioSerialPort {`
  - `vendor/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `kernel/src/pseudofs/device.rs:L14-L18`：enum DeviceMmap
    代码片段：`/// Mmap behavior for devices. pub enum DeviceMmap { /// The device is not mappable. None,`

### 相关符号

`enum DeviceMmap` at `kernel/src/pseudofs/device.rs:L14`、`trait DeviceOps` at `kernel/src/pseudofs/device.rs:L26`、`fn read_at` at `kernel/src/pseudofs/device.rs:L28`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
