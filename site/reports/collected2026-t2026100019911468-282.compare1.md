# 2026 T2026100019911468 比较报告

- 对比历史仓库：2023 git_push
- 生成时间：2026-07-02T04:57:36.986237+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2023 git_push（来源：赛事历史作品）：画像相似度 score=9.64；同属 ucore-variant 风格; 语言构成相似度 0.90; OS 维度重合度 1.00; 代码规模接近度 0.84

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2023 git_push 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `xv6-k210/kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 与 2023 git_push 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/file.c:L157-L161`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `xv6-k210/kernel/include/file.h:L33-L37`：关键词命中
    代码片段：`struct file*    filedup(struct file*); void            fileinit(void); int             fileread(struct file*, uint64, int n); int             filestat(struct file*, uint64 addr)...`
  - `kernel/file.c:L117-L121`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `kernel/include/file.h:L31-L35`：关键词命中
    代码片段：`struct file*    filedup(struct file*); void            fileinit(void); int             fileread(struct file*, uint64, int n); int             filestat(struct file*, uint64 addr)...`
- 与 2023 git_push 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
  - `xv6-k210/kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
  - `kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
  - `kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
- 与 2023 git_push 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`
  - `xv6-k210/kernel/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/kalloc.h" #include "include/string.h" #include "include/printf.h"`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`
  - `kernel/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/kalloc.h" #include "include/string.h" #include "include/printf.h"`
- 与 2023 git_push 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/proc.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/proc.h" #include "include/intr.h" #include "include/kalloc.h"`
  - `xv6-k210/kernel/include/proc.h:L32-L36`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/proc.h" #include "include/intr.h" #include "include/kalloc.h"`
  - `kernel/include/proc.h:L35-L39`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2023 git_push 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/bio.c:L18-L22`：关键词命中
    代码片段：`#include "include/types.h" #include "include/param.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/riscv.h"`
  - `xv6-k210/kernel/exec.c:L4-L8`：关键词命中
    代码片段：`#include "include/memlayout.h" #include "include/riscv.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/proc.h"`
  - `kernel/bio.c:L18-L22`：关键词命中
    代码片段：`#include "include/types.h" #include "include/param.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/riscv.h"`
  - `kernel/exec.c:L3-L7`：关键词命中
    代码片段：`#include "include/memlayout.h" #include "include/riscv.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/proc.h"`
- 与 2023 git_push 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/syscall.c:L6-L10`：关键词命中
    代码片段：`#include "include/spinlock.h" #include "include/proc.h" #include "include/syscall.h" #include "include/sysinfo.h" #include "include/kalloc.h"`
  - `xv6-k210/kernel/include/syscall.h:L10-L14`：关键词命中
    代码片段：`int argaddr(int n, uint64 *ip); int argstr(int n, char *buf, int max); void syscall(void); #endif`
  - `kernel/syscall.c:L6-L10`：关键词命中
    代码片段：`#include "include/spinlock.h" #include "include/proc.h" #include "include/syscall.h" #include "include/sysinfo.h" #include "include/kalloc.h"`
  - `kernel/include/syscall.h:L10-L14`：关键词命中
    代码片段：`int argaddr(int n, uint64 *ip); int argstr(int n, char *buf, int max); void syscall(void); #endif`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2023 git_push 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：reserved, struct, type, uint32, virtio_blk_outhdr。（置信度：high）
  证据：
  - `xv6-k210/kernel/virtio_disk.c:L196-L200`：struct virtio_blk_outhdr
    代码片段：`// qemu's virtio-blk.c reads them. struct virtio_blk_outhdr { uint32 type; uint32 reserved;`
  - `kernel/virtio_disk.c:L196-L200`：struct virtio_blk_outhdr
    代码片段：`// qemu's virtio-blk.c reads them. struct virtio_blk_outhdr { uint32 type; uint32 reserved;`
- 与 2023 git_push 在“设备驱动”维度发现片段级代码相似度 0.88 （token=0.91, structure=0.83）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：backspace, c, consputc, if, int, void。（置信度：high）
  证据：
  - `xv6-k210/kernel/console.c:L35-L39`：fn consputc
    代码片段：`} void consputc(int c) { if(c == BACKSPACE){ // if the user typed backspace, overwrite with a space.`
  - `kernel/console.c:L26-L30`：fn consputc
    代码片段：`#define C(x)  ((x)-'@')  // Control-x void consputc(int c) { if(c == BACKSPACE){ // if the user typed backspace, overwrite with a space.`
- 与 2023 git_push 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：addr, f, file, fileread, int, n, r, struct。（置信度：high）
  证据：
  - `xv6-k210/kernel/file.c:L157-L161`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `kernel/file.c:L117-L121`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
- 与 2023 git_push 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：enum, fd_device, fd_entry, fd_none, fd_pipe, file, int, ref。（置信度：high）
  证据：
  - `xv6-k210/kernel/include/file.h:L2-L6`：struct file
    代码片段：`#define __FILE_H struct file { enum { FD_NONE, FD_PIPE, FD_ENTRY, FD_DEVICE } type; int ref; // reference count`
  - `kernel/include/file.h:L2-L6`：struct file
    代码片段：`#define __FILE_H struct file { enum { FD_NONE, FD_PIPE, FD_ENTRY, FD_DEVICE } type; int ref; // reference count`
- 与 2023 git_push 在“中断与异常”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：initlock, ticks, tickslock, timerinit, uint, void。（置信度：high）
  证据：
  - `xv6-k210/kernel/timer.c:L14-L18`：fn timerinit
    代码片段：`uint ticks; void timerinit() { initlock(&tickslock, "time"); #ifdef DEBUG`
  - `kernel/timer.c:L14-L18`：fn timerinit
    代码片段：`uint ticks; void timerinit() { initlock(&tickslock, "time"); #ifdef DEBUG`
- 与 2023 git_push 在“中断与异常”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：devintr, int, r_scause, scause, uint64, void。（置信度：high）
  证据：
  - `xv6-k210/kernel/trap.c:L211-L215`：fn devintr
    代码片段：`//          1 if other device, //          0 if not recognized. int devintr(void) { uint64 scause = r_scause();`
  - `kernel/trap.c:L189-L193`：fn devintr
    代码片段：`//          1 if other device, //          0 if not recognized. int devintr(void) { uint64 scause = r_scause();`
- 函数/符号名重合：与 2023 git_push 在“中断与异常”维度发现 4 个同名定义：plicinit, timerinit, timer_tick, devintr。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/kernel/plic.c:L28-L28`：fn plicinit
    代码片段：`void plicinit(void) {`
  - `kernel/plic.c:L15-L15`：fn plicinit
    代码片段：`void plicinit(void) {`
  - `xv6-k210/kernel/timer.c:L16-L16`：fn timerinit
    代码片段：`void timerinit() {`
  - `kernel/timer.c:L16-L16`：fn timerinit
    代码片段：`void timerinit() {`
- 函数/符号名重合：与 2023 git_push 在“调度与任务管理”维度发现 3 个同名定义：reg_info, switch_to_SPI_mode, swtch。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/kernel/proc.c:L255-L255`：fn reg_info
    代码片段：`void reg_info(void) {`
  - `kernel/proc.c:L35-L35`：fn reg_info
    代码片段：`void reg_info(void) {`
  - `xv6-k210/kernel/sdcard.c:L125-L125`：fn switch_to_SPI_mode
    代码片段：`static int switch_to_SPI_mode(void) {`
  - `kernel/sdcard.c:L125-L125`：fn switch_to_SPI_mode
    代码片段：`static int switch_to_SPI_mode(void) {`
- 宏名重合：与 2023 git_push 在“设备驱动”维度发现 4 个同名定义：BACKSPACE, INPUT_BUF, Reg, ReadReg。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/kernel/console.c:L24-L24`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100`
  - `kernel/console.c:L25-L25`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100`
  - `xv6-k210/kernel/console.c:L51-L51`：macro INPUT_BUF
    代码片段：`#define INPUT_BUF 128`
  - `kernel/console.c:L42-L42`：macro INPUT_BUF
    代码片段：`#define INPUT_BUF 128`
- 宏名重合：与 2023 git_push 在“文件系统”维度发现 4 个同名定义：MAXFILE, BUFSZ, NELEM, __FAT32_H。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/xv6-user/usertests.c:L20-L20`：macro MAXFILE
    代码片段：`#define MAXFILE 512`
  - `xv6-user/usertests.c:L20-L20`：macro MAXFILE
    代码片段：`#define MAXFILE 512`
  - `xv6-k210/xv6-user/usertests.c:L21-L21`：macro BUFSZ
    代码片段：`#define BUFSZ  ((MAXOPBLOCKS+2)*BSIZE)`
  - `xv6-user/usertests.c:L21-L21`：macro BUFSZ
    代码片段：`#define BUFSZ  ((MAXOPBLOCKS+2)*BSIZE)`
- 宏名重合：与 2023 git_push 在“中断与异常”维度发现 4 个同名定义：IER, ISR, CLINT, CLINT_V。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/kernel/uart.c:L33-L33`：macro IER
    代码片段：`#define IER 1                 // interrupt enable register`
  - `kernel/uart.c:L26-L26`：macro IER
    代码片段：`#define IER 1                 // interrupt enable register`
  - `xv6-k210/kernel/uart.c:L39-L39`：macro ISR
    代码片段：`#define ISR 2                 // interrupt status register`
  - `kernel/uart.c:L32-L32`：macro ISR
    代码片段：`#define ISR 2                 // interrupt status register`
- 宏名重合：与 2023 git_push 在“内存管理”维度发现 4 个同名定义：PGSIZE, __KALLOC_H, TRAMPOLINE, TRAPFRAME。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/xv6-user/test_mem_cow.c:L4-L4`：macro PGSIZE
    代码片段：`#define PGSIZE (1 << 12)`
  - `kernel/include/riscv.h:L337-L337`：macro PGSIZE
    代码片段：`#define PGSIZE 4096 // bytes per page`
  - `xv6-k210/kernel/include/kalloc.h:L2-L2`：macro __KALLOC_H
    代码片段：`#define __KALLOC_H`
  - `kernel/include/kalloc.h:L2-L2`：macro __KALLOC_H
    代码片段：`#define __KALLOC_H`
- 宏名重合：与 2023 git_push 在“同步机制”维度发现 4 个同名定义：SD_CMD16, SD_CMD17, SD_CMD24, SYSCTRL_CLOCK_FREQ_IN0。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/kernel/sdcard.c:L87-L87`：macro SD_CMD16
    代码片段：`#define SD_CMD16 	16 		// SET_BLOCK_SIZE`
  - `kernel/sdcard.c:L87-L87`：macro SD_CMD16
    代码片段：`#define SD_CMD16 	16 		// SET_BLOCK_SIZE`
  - `xv6-k210/kernel/sdcard.c:L88-L88`：macro SD_CMD17
    代码片段：`#define SD_CMD17 	17 		// READ_SINGLE_BLOCK`
  - `kernel/sdcard.c:L88-L88`：macro SD_CMD17
    代码片段：`#define SD_CMD17 	17 		// READ_SINGLE_BLOCK`
- 宏名重合：与 2023 git_push 在“系统调用”维度发现 4 个同名定义：SYSCTRL_CLOCK_FREQ_IN0, ATTR_SYSTEM, SYSCTL, SYSCTL_V。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/kernel/sysctl.c:L20-L20`：macro SYSCTRL_CLOCK_FREQ_IN0
    代码片段：`#define SYSCTRL_CLOCK_FREQ_IN0 (26000000UL)`
  - `kernel/sysctl.c:L20-L20`：macro SYSCTRL_CLOCK_FREQ_IN0
    代码片段：`#define SYSCTRL_CLOCK_FREQ_IN0 (26000000UL)`
  - `xv6-k210/kernel/include/fat32.h:L9-L9`：macro ATTR_SYSTEM
    代码片段：`#define ATTR_SYSTEM         0x04`
  - `kernel/include/fat32.h:L9-L9`：macro ATTR_SYSTEM
    代码片段：`#define ATTR_SYSTEM         0x04`
- 函数/符号名重合：与 2023 git_push 在“设备驱动”维度发现 1 个同名定义：consputc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `xv6-k210/kernel/console.c:L37-L37`：fn consputc
    代码片段：`void consputc(int c) {`
  - `kernel/console.c:L28-L28`：fn consputc
    代码片段：`void consputc(int c) {`
- 结构体/类型重合：与 2023 git_push 在“文件系统”维度发现 4 个同名定义：short_name_entry, long_name_entry, dirent, file。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/kernel/fat32.c:L16-L16`：struct short_name_entry
    代码片段：`typedef struct short_name_entry {`
  - `kernel/fat32.c:L16-L16`：struct short_name_entry
    代码片段：`typedef struct short_name_entry {`
  - `xv6-k210/kernel/fat32.c:L31-L31`：struct long_name_entry
    代码片段：`typedef struct long_name_entry {`
  - `kernel/fat32.c:L31-L31`：struct long_name_entry
    代码片段：`typedef struct long_name_entry {`
- 函数/符号名重合：与 2023 git_push 在“内存管理”维度发现 2 个同名定义：sd_get_response_R3_rest, sd_get_response_R7_rest。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `xv6-k210/kernel/sdcard.c:L113-L113`：fn sd_get_response_R3_rest
    代码片段：`static void sd_get_response_R3_rest(uint8 *frame) {`
  - `kernel/sdcard.c:L113-L113`：fn sd_get_response_R3_rest
    代码片段：`static void sd_get_response_R3_rest(uint8 *frame) {`
  - `xv6-k210/kernel/sdcard.c:L121-L121`：fn sd_get_response_R7_rest
    代码片段：`static void sd_get_response_R7_rest(uint8 *frame) {`
  - `kernel/sdcard.c:L121-L121`：fn sd_get_response_R7_rest
    代码片段：`static void sd_get_response_R7_rest(uint8 *frame) {`
- 函数/符号名重合：与 2023 git_push 在“同步机制”维度发现 1 个同名定义：check_block_size。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `xv6-k210/kernel/sdcard.c:L232-L232`：fn check_block_size
    代码片段：`static int check_block_size(void) {`
  - `kernel/sdcard.c:L232-L232`：fn check_block_size
    代码片段：`static int check_block_size(void) {`
- 函数/符号名重合：与 2023 git_push 在“系统调用”维度发现 2 个同名定义：reg_info, devintr。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `xv6-k210/kernel/proc.c:L255-L255`：fn reg_info
    代码片段：`void reg_info(void) {`
  - `kernel/proc.c:L35-L35`：fn reg_info
    代码片段：`void reg_info(void) {`
  - `xv6-k210/kernel/trap.c:L213-L213`：fn devintr
    代码片段：`int devintr(void) {`
  - `kernel/trap.c:L191-L191`：fn devintr
    代码片段：`int devintr(void) {`
- 结构体/类型重合：与 2023 git_push 在“设备驱动”维度发现 1 个同名定义：virtio_blk_outhdr。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `xv6-k210/kernel/virtio_disk.c:L198-L198`：struct virtio_blk_outhdr
    代码片段：`struct virtio_blk_outhdr {`
  - `kernel/virtio_disk.c:L198-L198`：struct virtio_blk_outhdr
    代码片段：`struct virtio_blk_outhdr {`
- 结构体/类型重合：与 2023 git_push 在“内存管理”维度发现 1 个同名定义：run。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `xv6-k210/kernel/kalloc.c:L19-L19`：struct run
    代码片段：`struct run {`
  - `kernel/kalloc.c:L19-L19`：struct run
    代码片段：`struct run {`
- 文件路径重合：与 2023 git_push 在“设备驱动”维度出现同名文件源码路径 `xv6-k210/kernel/uart.c` / `kernel/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `xv6-k210/kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
- 文件路径重合：与 2023 git_push 在“设备驱动”维度出现同名文件源码路径 `xv6-k210/kernel/console.c` / `kernel/console.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `xv6-k210/kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 文件路径重合：与 2023 git_push 在“文件系统”维度出现同名文件源码路径 `xv6-k210/kernel/file.c` / `kernel/file.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `xv6-k210/kernel/file.c:L157-L161`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `kernel/file.c:L117-L121`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
- 文件路径重合：与 2023 git_push 在“文件系统”维度出现同名文件源码路径 `xv6-k210/kernel/include/file.h` / `kernel/include/file.h`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `xv6-k210/kernel/include/file.h:L33-L37`：关键词命中
    代码片段：`struct file*    filedup(struct file*); void            fileinit(void); int             fileread(struct file*, uint64, int n); int             filestat(struct file*, uint64 addr)...`
  - `kernel/include/file.h:L31-L35`：关键词命中
    代码片段：`struct file*    filedup(struct file*); void            fileinit(void); int             fileread(struct file*, uint64, int n); int             filestat(struct file*, uint64 addr)...`
- 文件路径重合：与 2023 git_push 在“中断与异常”维度出现同名文件源码路径 `xv6-k210/kernel/plic.c` / `kernel/plic.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `xv6-k210/kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
  - `kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
- 文件路径重合：与 2023 git_push 在“中断与异常”维度出现同名文件源码路径 `xv6-k210/kernel/trap.c` / `kernel/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `xv6-k210/kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
  - `kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`

## 相似点

- 与 2023 git_push 同属 ucore-variant 风格。（置信度：medium）
- 与 2023 git_push 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `xv6-k210/kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 与 2023 git_push 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/file.c:L157-L161`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `xv6-k210/kernel/include/file.h:L33-L37`：关键词命中
    代码片段：`struct file*    filedup(struct file*); void            fileinit(void); int             fileread(struct file*, uint64, int n); int             filestat(struct file*, uint64 addr)...`
- 与 2023 git_push 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
  - `xv6-k210/kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
- 与 2023 git_push 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`
  - `xv6-k210/kernel/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/kalloc.h" #include "include/string.h" #include "include/printf.h"`
- 与 2023 git_push 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/proc.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/proc.h" #include "include/intr.h" #include "include/kalloc.h"`
  - `xv6-k210/kernel/include/proc.h:L32-L36`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2023 git_push 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/bio.c:L18-L22`：关键词命中
    代码片段：`#include "include/types.h" #include "include/param.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/riscv.h"`
  - `xv6-k210/kernel/exec.c:L4-L8`：关键词命中
    代码片段：`#include "include/memlayout.h" #include "include/riscv.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/proc.h"`
- 与 2023 git_push 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/syscall.c:L6-L10`：关键词命中
    代码片段：`#include "include/spinlock.h" #include "include/proc.h" #include "include/syscall.h" #include "include/sysinfo.h" #include "include/kalloc.h"`
  - `xv6-k210/kernel/include/syscall.h:L10-L14`：关键词命中
    代码片段：`int argaddr(int n, uint64 *ip); int argstr(int n, char *buf, int max); void syscall(void); #endif`
- 与 2023 git_push 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2023 git_push 的语言构成不同：待测作品为 {'json': 35, 'build': 617, 'markdown': 5505, 'asm': 1191, 'text': 3372, 'c': 29141, 'toml': 14, 'rust': 1926}，历史样本为 {'json': 35, 'build': 430, 'markdown': 4959, 'asm': 367, 'c': 26800, 'toml': 14, 'rust': 1926}。（置信度：medium）

## 可能创新点

- 待测作品包含 92 个历史样本 2023 git_push 中未见的独特函数/类型定义（如 `rv_cyclictest_glibc_output_end`, `rv_cyclictest_musl_output_end`, `rv_libcbench_glibc_output_end`, `rv_libcbench_musl_output_end`, `rv_libctest_glibc_output_end`, `rv_libctest_musl_output_end`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `xv6-k210/kernel/rv_public_output_data.S:L94-L94`：fn rv_cyclictest_glibc_output_end
    代码片段：`rv_cyclictest_glibc_output_end:`
  - `xv6-k210/kernel/rv_public_output_data.S:L98-L98`：fn rv_cyclictest_musl_output_end
    代码片段：`rv_cyclictest_musl_output_end:`

## 附录：核验摘要

- 关键结论数：42
- 含证据关键结论数：42（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
