# 2026 T202610486999642 比较报告

- 对比历史仓库：2024 chaos
- 生成时间：2026-07-01T21:07:32.950137+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 chaos（来源：赛事历史作品）：画像相似度 score=9.55；同属 ucore-variant 风格; 语言构成相似度 0.96; OS 维度重合度 1.00; 代码规模接近度 0.62

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 chaos 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
  - `os/src/drivers/mod.rs:L5-L9`：关键词命中
    代码片段：`#[cfg(target_arch = "riscv64")] pub mod plic; pub mod virtio; pub use input::*;`
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use core::{ arch::asm,`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! block device driver pub mod block;`
- 与 2024 chaos 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/fat.rs:L1-L5`：关键词命中
    代码片段：`use super::dirent::{DT_DIR, DT_REG, RawDirEntry, write_dir_entries}; use super::mount::BlockPartition; use super::vfs::{FileSystemBackend, FileSystemStat, FsError, FsNodeKind, F...`
  - `os/src/fs/mod.rs:L7-L11`：关键词命中
    代码片段：`mod eventfd; mod ext4; mod fat; mod inode; mod memfd;`
  - `os/src/fs/fs.rs:L1-L5`：关键词命中
    代码片段：`use alloc::{collections::BTreeMap, sync::Arc}; use super::{inode::Inode, path::Path}; pub trait FileSystem: Send + Sync {`
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`use defs::OpenFlags; use dentry::Dentry; use ext4::fs::Ext4FS; use fs::FileSystemManager;`
- 与 2024 chaos 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/drivers/plic.rs:L1-L4`：关键词命中
    代码片段：`#[allow(clippy::upper_case_acronyms)] pub struct PLIC { base_addr: usize, }`
  - `os/src/arch/riscv64/timer.rs:L16-L20`：关键词命中
    代码片段：`pub const TICKS_PER_SEC: usize = 100; // CONTEXT: Keep Linux-visible clock ticks at 100 Hz, but drive scheduler // timer interrupts at 1 kHz so 1 ms clock_nanosleep workloads ca...`
  - `os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V timer-related functionality use alloc::{collections::BinaryHeap, sync::Arc};`
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality //! //! For rCore, we have a single trap entry point, namely '__alltraps'. At`
- 与 2024 chaos 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`mod area; mod elf_loader; mod frame_allocator; mod heap_allocator; mod kernel_space;`
  - `os/src/mm/shm.rs:L1-L3`：关键词命中
    代码片段：`use super::{FrameTracker, MapPermission, PhysPageNum, frame_alloc}; use crate::config::PAGE_SIZE; use crate::sync::UPIntrFreeCell;`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `os/src/mm/address.rs:L112-L116`：关键词命中
    代码片段：`// Self(v & ((1 << VA_WIDTH_SV39) - 1)) let tmp = (v as isize >> VA_WIDTH_SV39) as isize; // 检查传入地址是否合法（SV39标准 assert!(tmp == 0 || tmp == -1, "invalid va: {:#x}", v); Self(v)`
- 与 2024 chaos 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L139-L143`：关键词命中
    代码片段：`} // Per-thread stack windows keep one unmapped guard page between neighboring // user stacks by spacing bases with PAGE_SIZE + USER_STACK_SIZE. fn ustack_bottom_from_tid(ustack...`
  - `os/src/task/mod.rs:L13-L17`：关键词命中
    代码片段：`mod signal; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Implementation of process ['ProcessControlBlock'] and task(thread) ['TaskControlBlock'] management mechanism //! //! Here is the entry for task scheduling required by other...`
  - `os/src/task/res.rs:L1-L3`：关键词命中
    代码片段：`//! Allocator for pid, task user resource, kernel stack using a simple recycle strategy. use alloc::{`
- 与 2024 chaos 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`mod condvar; mod sleep_mutex; mod up;`
  - `os/src/sync/condvar.rs:L3-L7`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub struct Condvar { pub inner: UPIntrFreeCell<CondvarInner>, }`
  - `os/src/sync/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization and interior mutability primitives mod condvar; pub mod mutex; mod semaphore;`
  - `os/src/sync/condvar.rs:L5-L9`：关键词命中
    代码片段：`// use alloc::{collections::VecDeque, sync::Arc}; // use super::mutex::MutexSupport; // /// Condition variable structure`
- 与 2024 chaos 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`// Linux generic syscall numbers used by both contest RISC-V and LoongArch // ABIs. Keep this table aligned with the userspace libc headers, not with // local implementation order.`
  - `os/src/syscall/time.rs:L1-L5`：关键词命中
    代码片段：`use crate::perf; use crate::sync::UPIntrFreeCell; use crate::syscall::SyscallContext; use crate::task::{ CAP_SYS_TIME, ProcessCpuTimesSnapshot, block_current_task_no_schedule,`
  - `os/src/syscall/fs.rs:L20-L24`：关键词命中
    代码片段：`}, mm::{translated_byte_buffer, translated_refmut, translated_str}, syscall::{ errno::{EACCES, EBADF, EBUSY, ENOENT, ENOTDIR, ENOTTY}, Dirent,`
  - `os/src/syscall/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Implementation of syscalls //! //! The single entry point to all system calls, ['syscall()'], is called //! whenever userspace wishes to perform a system call using the 'eca...`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 chaos 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：args, arguments, fmt, fn, print, pub, stdout, unwrap。（置信度：high）
  证据：
  - `os/src/console.rs:L12-L16`：fn print
    代码片段：`} pub fn print(args: fmt::Arguments) { Stdout.write_fmt(args).unwrap(); }`
  - `os/src/console.rs:L19-L23`：fn print
    代码片段：`} /// print to the host console using the format string and arguments. pub fn print(args: fmt::Arguments) { Stdout.write_fmt(args).unwrap(); }`
- 与 2024 chaos 在“设备驱动”维度发现片段级代码相似度 0.80 （token=0.69, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fmt, fn, for, impl, mut, result, s, self。（置信度：high）
  证据：
  - `os/src/console.rs:L6-L10`：fn write_str
    代码片段：`impl Write for Stdout { fn write_str(&mut self, s: &str) -> fmt::Result { UART.write_bytes(s.as_bytes()); Ok(())`
  - `os/src/console.rs:L11-L15`：fn write_str
    代码片段：`impl Write for Stdout { /// write str to console fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.chars() { console_putchar(c as usize);`
- 与 2024 chaos 在“同步机制”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：inner, pub, refcell, struct, t, upsafecell。（置信度：high）
  证据：
  - `os/src/sync/up.rs:L12-L16`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>,`
  - `os/src/sync/up.rs:L14-L18`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>,`
- 与 2024 chaos 在“同步机制”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, inner, new, pub, refcell, self, t, unsafe。（置信度：high）
  证据：
  - `os/src/sync/up.rs:L22-L26`：fn new
    代码片段：`/// User is responsible to guarantee that inner struct is only used in /// uniprocessor. pub unsafe fn new(value: T) -> Self { Self { inner: RefCell::new(value),`
  - `os/src/sync/up.rs:L24-L28`：fn new
    代码片段：`/// User is responsible to guarantee that inner struct is only used in /// uniprocessor. pub unsafe fn new(value: T) -> Self { Self { inner: RefCell::new(value),`
- 函数/符号名重合：与 2024 chaos 在“中断与异常”维度发现 4 个同名定义：set_timer, new, get_time, get_time_ms。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `os/src/arch/loongarch64/sbi.rs:L5-L5`：fn set_timer
    代码片段：`pub fn set_timer(timer: usize) {`
  - `os/src/sbi.rs:L33-L33`：fn set_timer
    代码片段：`pub fn set_timer(timer: usize) {`
  - `os/src/arch/loongarch64/signal.rs:L89-L89`：fn new
    代码片段：`fn new(interrupted_pc: usize, saved_context: TrapContext) -> Self {`
  - `os/src/timer.rs:L102-L102`：fn new
    代码片段：`pub fn new() -> Self {`
- 函数/符号名重合：与 2024 chaos 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/console.rs:L8-L8`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `user/src/console.rs:L11-L11`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 函数/符号名重合：与 2024 chaos 在“内存管理”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/arch/loongarch64/signal.rs:L89-L89`：fn new
    代码片段：`fn new(interrupted_pc: usize, saved_context: TrapContext) -> Self {`
  - `user/src/bin/user_shell.rs:L30-L30`：fn new
    代码片段：`pub fn new(command: &str) -> Self {`
- 函数/符号名重合：与 2024 chaos 在“调度与任务管理”维度发现 1 个同名定义：add_timer。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/arch/loongarch64/timer.rs:L314-L314`：fn add_timer
    代码片段：`pub fn add_timer(expire_ms: usize, task: Arc<TaskControlBlock>) {`
  - `os/src/timer.rs:L227-L227`：fn add_timer
    代码片段：`pub fn add_timer(expire_ms: usize, task: Arc<TaskControlBlock>) {`
- 函数/符号名重合：与 2024 chaos 在“同步机制”维度发现 1 个同名定义：add_timer。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/arch/loongarch64/timer.rs:L314-L314`：fn add_timer
    代码片段：`pub fn add_timer(expire_ms: usize, task: Arc<TaskControlBlock>) {`
  - `os/src/timer.rs:L227-L227`：fn add_timer
    代码片段：`pub fn add_timer(expire_ms: usize, task: Arc<TaskControlBlock>) {`
- 文件路径重合：与 2024 chaos 在“设备驱动”维度出现同路径源码路径 `os/src/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use core::{ arch::asm,`
- 结构体/类型重合：与 2024 chaos 在“设备驱动”维度发现 1 个同名定义：Stdout。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/console.rs:L5-L5`：struct Stdout
    代码片段：`struct Stdout;`
  - `user/src/console.rs:L8-L8`：struct Stdout
    代码片段：`struct Stdout;`
- 结构体/类型重合：与 2024 chaos 在“中断与异常”维度发现 1 个同名定义：TimerCondVar。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/arch/loongarch64/timer.rs:L218-L218`：struct TimerCondVar
    代码片段：`pub struct TimerCondVar {`
  - `os/src/timer.rs:L193-L193`：struct TimerCondVar
    代码片段：`pub struct TimerCondVar {`
- 文件路径重合：与 2024 chaos 在“同步机制”维度出现同路径源码路径 `os/src/sync/condvar.rs` / `os/src/sync/condvar.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/sync/condvar.rs:L3-L7`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub struct Condvar { pub inner: UPIntrFreeCell<CondvarInner>, }`
  - `os/src/sync/condvar.rs:L5-L9`：关键词命中
    代码片段：`// use alloc::{collections::VecDeque, sync::Arc}; // use super::mutex::MutexSupport; // /// Condition variable structure`
- 文件路径重合：与 2024 chaos 在“同步机制”维度出现同路径源码路径 `os/src/sync/up.rs` / `os/src/sync/up.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/sync/up.rs:L12-L16`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>,`
  - `os/src/sync/up.rs:L14-L18`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>,`
- 结构体/类型重合：与 2024 chaos 在“同步机制”维度发现 1 个同名定义：TimerCondVar。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/arch/loongarch64/timer.rs:L218-L218`：struct TimerCondVar
    代码片段：`pub struct TimerCondVar {`
  - `os/src/timer.rs:L193-L193`：struct TimerCondVar
    代码片段：`pub struct TimerCondVar {`
- 文件路径重合：与 2024 chaos 在“中断与异常”维度出现同名文件源码路径 `os/src/arch/riscv64/timer.rs` / `os/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/arch/riscv64/timer.rs:L16-L20`：关键词命中
    代码片段：`pub const TICKS_PER_SEC: usize = 100; // CONTEXT: Keep Linux-visible clock ticks at 100 Hz, but drive scheduler // timer interrupts at 1 kHz so 1 ms clock_nanosleep workloads ca...`
  - `os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V timer-related functionality use alloc::{collections::BinaryHeap, sync::Arc};`

## 相似点

- 与 2024 chaos 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 chaos 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
  - `os/src/drivers/mod.rs:L5-L9`：关键词命中
    代码片段：`#[cfg(target_arch = "riscv64")] pub mod plic; pub mod virtio; pub use input::*;`
- 与 2024 chaos 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/fat.rs:L1-L5`：关键词命中
    代码片段：`use super::dirent::{DT_DIR, DT_REG, RawDirEntry, write_dir_entries}; use super::mount::BlockPartition; use super::vfs::{FileSystemBackend, FileSystemStat, FsError, FsNodeKind, F...`
  - `os/src/fs/mod.rs:L7-L11`：关键词命中
    代码片段：`mod eventfd; mod ext4; mod fat; mod inode; mod memfd;`
- 与 2024 chaos 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/drivers/plic.rs:L1-L4`：关键词命中
    代码片段：`#[allow(clippy::upper_case_acronyms)] pub struct PLIC { base_addr: usize, }`
  - `os/src/arch/riscv64/timer.rs:L16-L20`：关键词命中
    代码片段：`pub const TICKS_PER_SEC: usize = 100; // CONTEXT: Keep Linux-visible clock ticks at 100 Hz, but drive scheduler // timer interrupts at 1 kHz so 1 ms clock_nanosleep workloads ca...`
- 与 2024 chaos 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`mod area; mod elf_loader; mod frame_allocator; mod heap_allocator; mod kernel_space;`
  - `os/src/mm/shm.rs:L1-L3`：关键词命中
    代码片段：`use super::{FrameTracker, MapPermission, PhysPageNum, frame_alloc}; use crate::config::PAGE_SIZE; use crate::sync::UPIntrFreeCell;`
- 与 2024 chaos 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L139-L143`：关键词命中
    代码片段：`} // Per-thread stack windows keep one unmapped guard page between neighboring // user stacks by spacing bases with PAGE_SIZE + USER_STACK_SIZE. fn ustack_bottom_from_tid(ustack...`
  - `os/src/task/mod.rs:L13-L17`：关键词命中
    代码片段：`mod signal; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
- 与 2024 chaos 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`mod condvar; mod sleep_mutex; mod up;`
  - `os/src/sync/condvar.rs:L3-L7`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub struct Condvar { pub inner: UPIntrFreeCell<CondvarInner>, }`
- 与 2024 chaos 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`// Linux generic syscall numbers used by both contest RISC-V and LoongArch // ABIs. Keep this table aligned with the userspace libc headers, not with // local implementation order.`
  - `os/src/syscall/time.rs:L1-L5`：关键词命中
    代码片段：`use crate::perf; use crate::sync::UPIntrFreeCell; use crate::syscall::SyscallContext; use crate::task::{ CAP_SYS_TIME, ProcessCpuTimesSnapshot, block_current_task_no_schedule,`
- 与 2024 chaos 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 chaos 的语言构成不同：待测作品为 {'build': 7331, 'markdown': 18876, 'c': 27967, 'toml': 6865, 'rust': 695305, 'asm': 939, 'text': 1213, 'json': 104, 'yaml': 87, 'make': 664}，历史样本为 {'yaml': 63, 'json': 76, 'markdown': 12073, 'build': 3169, 'toml': 9675, 'c': 1011, 'asm': 791, 'text': 268, 'rust': 388593}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：30
- 含证据关键结论数：30（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
