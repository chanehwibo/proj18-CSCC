# 2026 T2026181239910972 比较报告

- 对比历史仓库：2024 NPUcore-IMPACT!!!
- 生成时间：2026-07-01T21:22:07.317346+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 NPUcore-IMPACT!!!（来源：赛事历史作品）：画像相似度 score=9.12；同属 ucore-variant 风格; 语言构成相似度 0.60; OS 维度重合度 1.00; 代码规模接近度 0.92

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-kernel/src/driver/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Here lies the driver framework, and all driver implementations. //! //! Most drivers implement the [Driver] trait, but some drivers are for system`
  - `anemone-kernel/src/device/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console subsystem. //! //! Here lies /dev/console.`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-kernel/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Virtual file system and filesystem drivers. // vfs infrastructure mod anonymous; mod cache_stats;`
  - `anemone-kernel/src/fs/file.rs:L7-L11`：关键词命中
    代码片段：`bitflags! { #[derive(Debug, Clone, Copy, PartialEq, Eq)] /// Open-time VFS behavior flags for one file object. /// /// This is not a Linux UAPI bitset or a mirror of Linux 'fmod...`
  - `os/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod file_trait; mod filesystem; // mod inode; mod layout; pub mod poll;`
  - `os/src/fs/cache.rs:L193-L197`：关键词命中
    代码片段：`/// Every time kernel tried to alloc this pagecache this number will increase 1(at most 1) /// Every time out of memory occurred this number will decrease 1(at least 0) /// When...`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-kernel/src/time/timer/irq.rs:L3-L7`：关键词命中
    代码片段：`use super::{TimerEvent, expire_ticks_after, push_timer_event}; /// Schedule a timer event to run in interrupt context on the local CPU after /// the given duration. ///`
  - `anemone-kernel/src/time/timer/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Soft timer. //! //! Timer events are one-shot completions. Callers explicitly choose whether a`
  - `os/src/timer.rs:L18-L22`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/main.rs:L36-L40`：关键词命中
    代码片段：`mod syscall; mod task; mod timer; // use crate::arch::config::DISK_IMAGE_BASE;`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-kernel/src/mm/stack.rs:L26-L30`：关键词命中
    代码片段：`let vpn_range = unsafe { mm::remap::alloc_virt_range(NPAGES + 1) } .expect("failed to allocate virtual range for boot stack guard page"); // The first page is the guard – we sim...`
  - `anemone-kernel/src/mm/layout.rs:L7-L11`：关键词命中
    代码片段：`/// Common kernel layout and key constants: /// /// ## Page Table Managed Spaces: /// ''' ///  -0x0   +-----------+---------------+ -0x0`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `os/src/mm/map_area.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::Debug; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-kernel/src/task/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File System related structures and functions for a task. //! //! Reference:`
  - `anemone-kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! we expose all fields of Task, ThreadGroup, etc. to the whole task module. //! this is a bit too much, but i think that's inevitable for kernel code. too //! much encapsulati...`
  - `os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `os/src/task/task.rs:L380-L384`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-kernel/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`//! TODO: Typed no-irq lock. //! //! TODO: Interrupt-safe lazy.`
  - `anemone-kernel/src/sync/mono.rs:L1-L4`：关键词命中
    代码片段：`#[cfg(debug_assertions)] use core::sync::atomic::{AtomicBool, Ordering}; use core::{cell::UnsafeCell, mem::MaybeUninit};`
  - `os/src/fs/mod.rs:L35-L39`：关键词命中
    代码片段：`use lazy_static::*; use log::warn; use spin::Mutex; lazy_static! {`
  - `os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::{arch::BLOCK_SZ, config::PAGE_SIZE, drivers::BLOCK_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-abi/src/syscall/mod.rs:L12-L16`：关键词命中
    代码片段：`pub use native::*; /// The Linux kernel actually does not define a maximum syscall number, /// but it's obvious that syscall numbers won't exceed this value minus one, on /// an...`
  - `anemone-abi/src/syscall/riscv.rs:L14-L18`：关键词命中
    代码片段：`unsafe { core::arch::asm!( "ecall", in("a7") sysno, in("a0") arg0,`
  - `os/src/syscall/fs.rs:L548-L552`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `os/src/syscall/mod.rs:L115-L119`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现片段级代码相似度 0.73 （token=0.58, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：bool, derive, pub, struct。（置信度：medium）
  证据：
  - `anemone-rs/src/fs.rs:L9-L13`：struct OpenOptions
    代码片段：`#[derive(Debug)] pub struct OpenOptions { read: bool, write: bool,`
  - `os/src/fs/mod.rs:L48-L52`：struct FileDescriptor
    代码片段：`#[derive(Clone)] pub struct FileDescriptor { cloexec: bool, nonblock: bool,`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现 4 个同名定义：StatXTimestamp, StatX, Stat, PollFd。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `anemone-abi/src/fs.rs:L123-L123`：struct StatXTimestamp
    代码片段：`pub struct StatXTimestamp {`
  - `os/src/fs/layout.rs:L185-L185`：struct StatxTimestamp
    代码片段：`struct StatxTimestamp {`
  - `anemone-abi/src/fs.rs:L131-L131`：struct StatX
    代码片段：`pub struct StatX {`
  - `os/src/fs/layout.rs:L201-L201`：struct Statx
    代码片段：`pub struct Statx {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“内存管理”维度发现 2 个同名定义：alloc, dealloc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `anemone-kernel/crates/buddy-system/src/system.rs:L88-L88`：fn alloc
    代码片段：`pub fn alloc(&mut self, order: usize) -> Result<NonNull<u8>, BuddyError> {`
  - `os/src/mm/frame_allocator.rs:L46-L46`：fn alloc
    代码片段：`fn alloc(&mut self) -> Option<FrameTracker>;`
  - `anemone-kernel/crates/buddy-system/src/system.rs:L107-L107`：fn dealloc
    代码片段：`pub unsafe fn dealloc(&mut self, addr: NonNull<u8>, order: usize) -> Result<(), BuddyError> {`
  - `os/src/mm/frame_allocator.rs:L48-L48`：fn dealloc
    代码片段：`fn dealloc(&mut self, ppn: PhysPageNum);`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“系统调用”维度发现 1 个同名定义：syscall。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `anemone-abi/src/syscall/loongarch.rs:L30-L30`：fn syscall
    代码片段：`pub unsafe fn syscall(`
  - `user/src/syscall.rs:L85-L85`：fn syscall
    代码片段：`fn syscall(id: usize, args: [usize; 3]) -> isize {`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度出现同名文件源码路径 `anemone-kernel/src/device/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `anemone-kernel/src/device/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console subsystem. //! //! Here lies /dev/console.`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度出现同名文件源码路径 `anemone-kernel/src/exception/timer.rs` / `os/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `anemone-kernel/src/exception/timer.rs:L1-L3`：关键词命中
    代码片段：`//! Timer interrupt handling. use crate::prelude::*;`
  - `os/src/timer.rs:L18-L22`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`

## 相似点

- 与 2024 NPUcore-IMPACT!!! 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-kernel/src/driver/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Here lies the driver framework, and all driver implementations. //! //! Most drivers implement the [Driver] trait, but some drivers are for system`
  - `anemone-kernel/src/device/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console subsystem. //! //! Here lies /dev/console.`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-kernel/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Virtual file system and filesystem drivers. // vfs infrastructure mod anonymous; mod cache_stats;`
  - `anemone-kernel/src/fs/file.rs:L7-L11`：关键词命中
    代码片段：`bitflags! { #[derive(Debug, Clone, Copy, PartialEq, Eq)] /// Open-time VFS behavior flags for one file object. /// /// This is not a Linux UAPI bitset or a mirror of Linux 'fmod...`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-kernel/src/time/timer/irq.rs:L3-L7`：关键词命中
    代码片段：`use super::{TimerEvent, expire_ticks_after, push_timer_event}; /// Schedule a timer event to run in interrupt context on the local CPU after /// the given duration. ///`
  - `anemone-kernel/src/time/timer/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Soft timer. //! //! Timer events are one-shot completions. Callers explicitly choose whether a`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-kernel/src/mm/stack.rs:L26-L30`：关键词命中
    代码片段：`let vpn_range = unsafe { mm::remap::alloc_virt_range(NPAGES + 1) } .expect("failed to allocate virtual range for boot stack guard page"); // The first page is the guard – we sim...`
  - `anemone-kernel/src/mm/layout.rs:L7-L11`：关键词命中
    代码片段：`/// Common kernel layout and key constants: /// /// ## Page Table Managed Spaces: /// ''' ///  -0x0   +-----------+---------------+ -0x0`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-kernel/src/task/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File System related structures and functions for a task. //! //! Reference:`
  - `anemone-kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! we expose all fields of Task, ThreadGroup, etc. to the whole task module. //! this is a bit too much, but i think that's inevitable for kernel code. too //! much encapsulati...`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-kernel/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`//! TODO: Typed no-irq lock. //! //! TODO: Interrupt-safe lazy.`
  - `anemone-kernel/src/sync/mono.rs:L1-L4`：关键词命中
    代码片段：`#[cfg(debug_assertions)] use core::sync::atomic::{AtomicBool, Ordering}; use core::{cell::UnsafeCell, mem::MaybeUninit};`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-abi/src/syscall/mod.rs:L12-L16`：关键词命中
    代码片段：`pub use native::*; /// The Linux kernel actually does not define a maximum syscall number, /// but it's obvious that syscall numbers won't exceed this value minus one, on /// an...`
  - `anemone-abi/src/syscall/riscv.rs:L14-L18`：关键词命中
    代码片段：`unsafe { core::arch::asm!( "ecall", in("a7") sysno, in("a0") arg0,`
- 与 2024 NPUcore-IMPACT!!! 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 NPUcore-IMPACT!!! 的语言构成不同：待测作品为 {'markdown': 31718, 'yaml': 40, 'text': 4147, 'json': 104, 'build': 1381, 'toml': 398, 'rust': 97500, 'c': 27779, 'make': 664}，历史样本为 {'json': 20, 'build': 338, 'markdown': 743, 'rust': 177629, 'asm': 10}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：20
- 含证据关键结论数：20（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
