# 2026 T2026104869910625 比较报告

- 对比历史仓库：2024 蕾姆赛高队
- 生成时间：2026-07-02T09:02:34.755628+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 蕾姆赛高队（来源：赛事历史作品）：画像相似度 score=9.82；同属 ucore-variant 风格; 语言构成相似度 0.95; OS 维度重合度 1.00; 代码规模接近度 0.91

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 蕾姆赛高队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/driver/riscv/virt.c:L1-L3`：关键词命中
    代码片段：`// virt.c - 最小化RISC-V VirtIO磁盘驱动 #include "virt.h" #include "print.h"`
  - `include/virtio.h:L6-L10`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `include/console.h:L4-L8`：关键词命中
    代码片段：`void consoleinit(void); void consputc(int c); void consoleintr(int c); #endif`
- 与 2024 蕾姆赛高队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/fs.c:L201-L205`：关键词命中
    代码片段：`void dir_init(void) { struct inode *ip; if ((ip=namei("/dev/null")) == NULL) vfs_ext4_mknod("/dev/null", T_CHR, DEVNULL);`
  - `kernel/fs/ext4.c:L146-L150`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `src/fs/file.c:L139-L143`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0, rcnt = 0;`
  - `src/fs/ext4/ext4.c:L177-L181`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
- 与 2024 蕾姆赛高队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/timer.c:L1-L5`：关键词命中
    代码片段：`#include "types.h" #include "spinlock.h" #include "timer.h" #include "print.h" #include "process.h"`
  - `kernel/test.c:L500-L504`：关键词命中
    代码片段：`void test_interrupt_nesting(void) { printf("Testing interrupt nesting...\n"); int old_intr = intr_get();`
  - `src/kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
  - `src/kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
- 与 2024 蕾姆赛高队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/vma.c:L22-L26`：关键词命中
    代码片段：`return NULL; } memset(vma, 0, PGSIZE); vma->type = NONE; vma->prev = vma->next = vma;`
  - `kernel/exec.c:L31-L35`：关键词命中
    代码片段：`void alloc_aux(uint64 *aux, uint64 atid, uint64 value); int loadaux(pgtbl_t pt, uint64 sp, uint64 stackbase, uint64 *aux); void debug_print_stack(pgtbl_t pagetable, uint64 sp, u...`
  - `src/mm/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`
  - `src/mm/mmap.c:L9-L13`：关键词命中
    代码片段：`#include "include/vm.h" #include "include/string.h" #include "include/kalloc.h" #include "include/ext4/ext4_init.h"`
- 与 2024 蕾姆赛高队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/thread.c:L1-L3`：关键词命中
    代码片段：`#include "thread.h" #include "pmem.h" #include "vmem.h"`
  - `include/kernel/thread.h:L29-L33`：关键词命中
    代码片段：`}; typedef struct thread { struct spinlock lock;`
  - `src/mm/mm.c:L10-L14`：关键词命中
    代码片段：`#include "include/defs.h" #include "include/mm.h" #include "include/proc.h" // Create segment node`
  - `src/mm/vm.c:L6-L10`：关键词命中
    代码片段：`#include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h" #include "include/string.h"`
- 与 2024 蕾姆赛高队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/cpu.c:L1-L4`：关键词命中
    代码片段：`#include "cpu.h" #include "spinlock.h" #ifdef RISCV #include "riscv.h"`
  - `kernel/test.c:L5-L9`：关键词命中
    代码片段：`#include "vmem.h" #include "string.h" #include "spinlock.h" #include "cpu.h" #if defined RISCV`
  - `src/fs/bio.c:L18-L22`：关键词命中
    代码片段：`#include "include/types.h" #include "include/param.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/riscv.h"`
  - `src/fs/file.c:L7-L11`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/param.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/ext4/ext4_init.h"`
- 与 2024 蕾姆赛高队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/syscall.c:L1-L5`：关键词命中
    代码片段：`#include "types.h" #include "print.h" #include "trap.h" //这个必须要再hsai_trap.h前。syscall(struct trapframe *trapframe)参数表中的trapframe对外部不可见，所以先声明trapframe #include "hsai_trap.h" #incl...`
  - `hal/riscv/sbi.c:L18-L22`：关键词命中
    代码片段：`register uint64 a2 asm("a2") = arg2; register uint64 a7 asm("a7") = which; asm volatile("ecall" : "=r"(a0) : "r"(a0), "r"(a1), "r"(a2), "r"(a7)`
  - `src/kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "include/spinlock.h" #include "include/proc.h" #include "include/syscall.h" #include "include/sysinfo.h" #include "include/kalloc.h"`
  - `include/syscall.h:L61-L65`：关键词命中
    代码片段：`int argaddr(int n, uint64 *ip); int argstr(int n, char *buf, int max); void syscall(void); #endif`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 蕾姆赛高队 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：enode, eok, ext4_inode_is_type, ext4_inode_mode_directory, false, has_children, if, inode。（置信度：high）
  证据：
  - `kernel/fs/ext4.c:L146-L150`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `src/fs/ext4/ext4.c:L177-L181`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
- 与 2024 蕾姆赛高队 在“文件系统”维度发现片段级代码相似度 0.90 （token=0.90, structure=0.90）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：addr, f, file, fileread, int, n, r, struct。（置信度：high）
  证据：
  - `kernel/fs/file.c:L694-L698`：关键词命中
    代码片段：`*/ int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `src/fs/file.c:L139-L143`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0, rcnt = 0;`
- 与 2024 蕾姆赛高队 在“系统调用”维度发现片段级代码相似度 0.84 （token=0.75, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：a0, a1, a2, a7, asm, register, volatile, which。（置信度：high）
  证据：
  - `hal/riscv/sbi.c:L18-L22`：关键词命中
    代码片段：`register uint64 a2 asm("a2") = arg2; register uint64 a7 asm("a7") = which; asm volatile("ecall" : "=r"(a0) : "r"(a0), "r"(a1), "r"(a2), "r"(a7)`
  - `include/sbi.h:L28-L32`：关键词命中
    代码片段：`register uintptr a3 asm ("a3") = (uintptr)(arg3);	\ register uintptr a7 asm ("a7") = (uintptr)(which);	\ asm volatile ("ecall"					\ : "+r" (a0)				\ : "r" (a1), "r" (a2), "r" (...`
- 宏名重合：与 2024 蕾姆赛高队 在“中断与异常”维度发现 4 个同名定义：MSTATUS_MIE, SIE_STIE, TRAPFRAME, SSTATUS_SPIE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `hal/riscv/start.c:L11-L11`：macro MSTATUS_MIE
    代码片段：`#define MSTATUS_MIE (1L << 3)    // machine-mode interrupt enable.`
  - `include/riscv.h:L19-L19`：macro MSTATUS_MIE
    代码片段：`#define MSTATUS_MIE (1L << 3)    // machine-mode interrupt enable.`
  - `hal/riscv/start.c:L15-L15`：macro SIE_STIE
    代码片段：`#define SIE_STIE (1L << 5) // timer`
  - `include/riscv.h:L83-L83`：macro SIE_STIE
    代码片段：`#define SIE_STIE (1L << 5) // timer`
- 宏名重合：与 2024 蕾姆赛高队 在“内存管理”维度发现 4 个同名定义：TRAMPOLINE, TRAPFRAME, PGSIZE, PGSHIFT。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/hal/loongarch/loongarch.h:L377-L377`：macro TRAMPOLINE
    代码片段：`#define TRAMPOLINE (MAXVA - PGSIZE)`
  - `include/memlayout.h:L109-L109`：macro TRAMPOLINE
    代码片段：`#define TRAMPOLINE              (MAXVA - PGSIZE)`
  - `include/hal/loongarch/loongarch.h:L378-L378`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME  (TRAMPOLINE - PGSIZE)`
  - `include/memlayout.h:L127-L127`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME               (TRAMPOLINE - PGSIZE)`
- 宏名重合：与 2024 蕾姆赛高队 在“系统调用”维度发现 3 个同名定义：TRAPFRAME, AT_SYSINFO, AT_SYSINFO_EHDR。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/hal/loongarch/loongarch.h:L378-L378`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME  (TRAMPOLINE - PGSIZE)`
  - `include/memlayout.h:L127-L127`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME               (TRAMPOLINE - PGSIZE)`
  - `include/kernel/elf.h:L79-L79`：macro AT_SYSINFO
    代码片段：`#define AT_SYSINFO              32`
  - `include/elf.h:L90-L90`：macro AT_SYSINFO
    代码片段：`#define AT_SYSINFO	32`
- 宏名重合：与 2024 蕾姆赛高队 在“文件系统”维度发现 2 个同名定义：PGSHIFT, NELEM。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/hal/loongarch/loongarch.h:L388-L388`：macro PGSHIFT
    代码片段：`#define PGSHIFT 12  // bits of offset within a page`
  - `include/riscv.h:L338-L338`：macro PGSHIFT
    代码片段：`#define PGSHIFT 12  // bits of offset within a page`
  - `include/kernel/defs.h:L10-L10`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x) / sizeof((x)[0]))`
  - `include/defs.h:L254-L254`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x)/sizeof((x)[0]))`
- 结构体/类型重合：与 2024 蕾姆赛高队 在“中断与异常”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/hsai/trap.h:L33-L33`：struct trapframe
    代码片段：`struct trapframe {//RISCV`
  - `include/trap.h:L17-L17`：struct trapframe
    代码片段：`struct trapframe {`
- 结构体/类型重合：与 2024 蕾姆赛高队 在“内存管理”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/hsai/trap.h:L33-L33`：struct trapframe
    代码片段：`struct trapframe {//RISCV`
  - `include/trap.h:L17-L17`：struct trapframe
    代码片段：`struct trapframe {`
- 结构体/类型重合：与 2024 蕾姆赛高队 在“系统调用”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/hsai/trap.h:L33-L33`：struct trapframe
    代码片段：`struct trapframe {//RISCV`
  - `include/trap.h:L17-L17`：struct trapframe
    代码片段：`struct trapframe {`
- 文件路径重合：与 2024 蕾姆赛高队 在“文件系统”维度出现同名文件源码路径 `kernel/fs/ext4.c` / `src/fs/ext4/ext4.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/fs/ext4.c:L146-L150`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `src/fs/ext4/ext4.c:L177-L181`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
- 文件路径重合：与 2024 蕾姆赛高队 在“文件系统”维度出现同名文件源码路径 `kernel/fs/file.c` / `src/fs/file.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/fs/file.c:L694-L698`：关键词命中
    代码片段：`*/ int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `src/fs/file.c:L139-L143`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0, rcnt = 0;`
- 文件路径重合：与 2024 蕾姆赛高队 在“中断与异常”维度出现同名文件源码路径 `kernel/timer.c` / `src/kernel/timer.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/timer.c:L1-L5`：关键词命中
    代码片段：`#include "types.h" #include "spinlock.h" #include "timer.h" #include "print.h" #include "process.h"`
  - `src/kernel/timer.c:L1-L3`：关键词命中
    代码片段：`// Timer Interrupt handler`
- 文件路径重合：与 2024 蕾姆赛高队 在“系统调用”维度出现同名文件源码路径 `kernel/syscall.c` / `src/kernel/syscall.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/syscall.c:L1-L5`：关键词命中
    代码片段：`#include "types.h" #include "print.h" #include "trap.h" //这个必须要再hsai_trap.h前。syscall(struct trapframe *trapframe)参数表中的trapframe对外部不可见，所以先声明trapframe #include "hsai_trap.h" #incl...`
  - `src/kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "include/spinlock.h" #include "include/proc.h" #include "include/syscall.h" #include "include/sysinfo.h" #include "include/kalloc.h"`

## 相似点

- 与 2024 蕾姆赛高队 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 蕾姆赛高队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/driver/riscv/virt.c:L1-L3`：关键词命中
    代码片段：`// virt.c - 最小化RISC-V VirtIO磁盘驱动 #include "virt.h" #include "print.h"`
- 与 2024 蕾姆赛高队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/fs.c:L201-L205`：关键词命中
    代码片段：`void dir_init(void) { struct inode *ip; if ((ip=namei("/dev/null")) == NULL) vfs_ext4_mknod("/dev/null", T_CHR, DEVNULL);`
  - `kernel/fs/ext4.c:L146-L150`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
- 与 2024 蕾姆赛高队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/timer.c:L1-L5`：关键词命中
    代码片段：`#include "types.h" #include "spinlock.h" #include "timer.h" #include "print.h" #include "process.h"`
  - `kernel/test.c:L500-L504`：关键词命中
    代码片段：`void test_interrupt_nesting(void) { printf("Testing interrupt nesting...\n"); int old_intr = intr_get();`
- 与 2024 蕾姆赛高队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/vma.c:L22-L26`：关键词命中
    代码片段：`return NULL; } memset(vma, 0, PGSIZE); vma->type = NONE; vma->prev = vma->next = vma;`
  - `kernel/exec.c:L31-L35`：关键词命中
    代码片段：`void alloc_aux(uint64 *aux, uint64 atid, uint64 value); int loadaux(pgtbl_t pt, uint64 sp, uint64 stackbase, uint64 *aux); void debug_print_stack(pgtbl_t pagetable, uint64 sp, u...`
- 与 2024 蕾姆赛高队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/thread.c:L1-L3`：关键词命中
    代码片段：`#include "thread.h" #include "pmem.h" #include "vmem.h"`
  - `include/kernel/thread.h:L29-L33`：关键词命中
    代码片段：`}; typedef struct thread { struct spinlock lock;`
- 与 2024 蕾姆赛高队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/cpu.c:L1-L4`：关键词命中
    代码片段：`#include "cpu.h" #include "spinlock.h" #ifdef RISCV #include "riscv.h"`
  - `kernel/test.c:L5-L9`：关键词命中
    代码片段：`#include "vmem.h" #include "string.h" #include "spinlock.h" #include "cpu.h" #if defined RISCV`
- 与 2024 蕾姆赛高队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/syscall.c:L1-L5`：关键词命中
    代码片段：`#include "types.h" #include "print.h" #include "trap.h" //这个必须要再hsai_trap.h前。syscall(struct trapframe *trapframe)参数表中的trapframe对外部不可见，所以先声明trapframe #include "hsai_trap.h" #incl...`
  - `hal/riscv/sbi.c:L18-L22`：关键词命中
    代码片段：`register uint64 a2 asm("a2") = arg2; register uint64 a7 asm("a7") = which; asm volatile("ecall" : "=r"(a0) : "r"(a0), "r"(a1), "r"(a2), "r"(a7)`
- 与 2024 蕾姆赛高队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 蕾姆赛高队 的语言构成不同：待测作品为 {'build': 869, 'markdown': 2143, 'asm': 797, 'c': 51320}，历史样本为 {'json': 20, 'build': 226, 'markdown': 574, 'c': 48956, 'asm': 332}。（置信度：medium）

## 可能创新点

- 待测作品包含 64 个历史样本 2024 蕾姆赛高队 中未见的独特函数/类型定义（如 `_write_reg`, `_read_reg`, `ext4_bg_set_inode_table_first_block`, `ext4_bmap_is_bit_clr`, `ext4_bmap_bit_clr`, `ext4_dir_en_set_entry_len`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `hal/loongarch/uart.c:L100-L100`：fn _write_reg
    代码片段：`void _write_reg( RegOffset reg, uint8 data ) { ( ( volatile uint8* ) ( (uint64) uart0 | (uint64) win_1 ) )[ ( int ) reg ] = data; }`
  - `hal/loongarch/uart.c:L101-L101`：fn _read_reg
    代码片段：`uint8 _read_reg( RegOffset reg ) { return ( ( volatile uint8* ) ( (uint64) uart0 | (uint64) win_1 ) )[ ( int ) reg ]; }`

## 附录：核验摘要

- 关键结论数：29
- 含证据关键结论数：29（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
