# 2026 T2026101839910132 比较报告

- 对比历史仓库：2024 chaos
- 生成时间：2026-07-02T09:01:04.791268+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 chaos（来源：赛事历史作品）：画像相似度 score=9.72；同属 ucore-variant 风格; 语言构成相似度 0.99; OS 维度重合度 1.00; 代码规模接近度 0.73

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 chaos 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `GCore/os/src/console.rs:L32-L36`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `GCore/os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use core::{ arch::asm,`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! block device driver pub mod block;`
- 与 2024 chaos 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `GCore/os/src/fs/mod.rs:L13-L17`：关键词命中
    代码片段：`pub mod dirent; pub mod file_descriptor; mod inode; mod timestamp; mod vfs;`
  - `GCore/os/src/fs/vfs.rs:L11-L15`：关键词命中
    代码片段：`use super::filesystem::{pre_mount, FS_Type}; pub trait VFS: DowncastSync { fn close(&self) { unreachable!()`
  - `os/src/fs/fs.rs:L1-L5`：关键词命中
    代码片段：`use alloc::{collections::BTreeMap, sync::Arc}; use super::{inode::Inode, path::Path}; pub trait FileSystem: Send + Sync {`
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`use defs::OpenFlags; use dentry::Dentry; use ext4::fs::Ext4FS; use fs::FileSystemManager;`
- 与 2024 chaos 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `GCore/os/src/timer.rs:L19-L23`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `GCore/os/src/hal/arch/riscv/trap/mod.rs:L17-L21`：关键词命中
    代码片段：`use riscv::register::{ mtvec::TrapMode, scause::{self, Exception, Interrupt, Trap}, sepc, sie, stval, stvec, };`
  - `os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V timer-related functionality use alloc::{collections::BinaryHeap, sync::Arc};`
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality //! //! For rCore, we have a single trap entry point, namely '__alltraps'. At`
- 与 2024 chaos 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `GCore/os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `GCore/os/src/mm/map_area.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::Debug; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `os/src/mm/address.rs:L112-L116`：关键词命中
    代码片段：`// Self(v & ((1 << VA_WIDTH_SV39) - 1)) let tmp = (v as isize >> VA_WIDTH_SV39) as isize; // 检查传入地址是否合法（SV39标准 assert!(tmp == 0 || tmp == -1, "invalid va: {:#x}", v); Self(v)`
- 与 2024 chaos 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `GCore/os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `GCore/os/src/task/task.rs:L517-L521`：关键词命中
    代码片段：`// 销毁所有其他同一线程组的任务 manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Implementation of process ['ProcessControlBlock'] and task(thread) ['TaskControlBlock'] management mechanism //! //! Here is the entry for task scheduling required by other...`
  - `os/src/task/res.rs:L1-L3`：关键词命中
    代码片段：`//! Allocator for pid, task user resource, kernel stack using a simple recycle strategy. use alloc::{`
- 与 2024 chaos 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `GCore/os/src/timer.rs:L309-L313`：关键词命中
    代码片段：`} use core::sync::atomic::{AtomicU64}; /// 启动以来的时间源（秒），例如通过读取 mtime / TSC`
  - `GCore/os/src/fs/vfs.rs:L39-L43`：关键词命中
    代码片段：`pub fn open_fs( block_device: Arc<dyn BlockDevice>, index_cache_mgr: Arc<spin::Mutex<BlockCacheManager>>, ) -> Arc<dyn VFS> { let fs_type = pre_mount();`
  - `os/src/sync/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization and interior mutability primitives mod condvar; pub mod mutex; mod semaphore;`
  - `os/src/sync/condvar.rs:L5-L9`：关键词命中
    代码片段：`// use alloc::{collections::VecDeque, sync::Arc}; // use super::mutex::MutexSupport; // /// Condition variable structure`
- 与 2024 chaos 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `GCore/os/src/syscall/fs.rs:L683-L687`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `GCore/os/src/syscall/mod.rs:L168-L172`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`
  - `os/src/syscall/fs.rs:L20-L24`：关键词命中
    代码片段：`}, mm::{translated_byte_buffer, translated_refmut, translated_str}, syscall::{ errno::{EACCES, EBADF, EBUSY, ENOENT, ENOTDIR, ENOTTY}, Dirent,`
  - `os/src/syscall/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Implementation of syscalls //! //! The single entry point to all system calls, ['syscall()'], is called //! whenever userspace wishes to perform a system call using the 'eca...`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 chaos 在“设备驱动”维度发现片段级代码相似度 0.93 （token=0.89, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：args, arguments, fmt, fn, print, pub, unwrap, write_fmt。（置信度：high）
  证据：
  - `GCore/os/src/console.rs:L25-L29`：fn print
    代码片段：`} pub fn print(args: fmt::Arguments) { KernelOutput.write_fmt(args).unwrap(); }`
  - `os/src/console.rs:L19-L23`：fn print
    代码片段：`} /// print to the host console using the format string and arguments. pub fn print(args: fmt::Arguments) { Stdout.write_fmt(args).unwrap(); }`
- 与 2024 chaos 在“设备驱动”维度发现片段级代码相似度 0.77 （token=0.73, structure=0.85）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：c, chars, fmt, fn, for, impl, in, mut。（置信度：medium）
  证据：
  - `GCore/os/src/console.rs:L8-L12`：fn write_str
    代码片段：`impl Write for KernelOutput { fn write_str(&mut self, s: &str) -> fmt::Result { let mut i = 0; for c in s.chars() {`
  - `os/src/console.rs:L11-L15`：fn write_str
    代码片段：`impl Write for Stdout { /// write str to console fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.chars() { console_putchar(c as usize);`
- 与 2024 chaos 在“内存管理”维度发现片段级代码相似度 0.62 （token=0.56, structure=0.75）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, heap_allocator, init, init_heap, pub。（置信度：medium）
  证据：
  - `GCore/os/src/mm/mod.rs:L36-L40`：fn init
    代码片段：`}; pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`
  - `os/src/mm/mod.rs:L32-L36`：fn init
    代码片段：`/// initiate heap allocator, frame allocator and kernel space pub fn init(memory_end: usize) { debug!("heap allocator initialize"); heap_allocator::init_heap();`
- 函数/符号名重合：与 2024 chaos 在“系统调用”维度发现 4 个同名定义：syscall, sys_dup, sys_open, sys_close。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `GCore/user/src/syscall.rs:L84-L84`：fn syscall
    代码片段：`fn syscall(id: usize, args: [usize; 3]) -> isize {`
  - `user/src/syscall.rs:L31-L31`：fn syscall
    代码片段：`fn syscall(id: usize, args: [usize; 3]) -> isize {`
  - `GCore/user/src/syscall.rs:L105-L105`：fn sys_dup
    代码片段：`pub fn sys_dup(fd: usize) -> isize {`
  - `user/src/syscall.rs:L45-L45`：fn sys_dup
    代码片段：`pub fn sys_dup(fd: usize) -> isize {`
- 函数/符号名重合：与 2024 chaos 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `GCore/user/src/console.rs:L10-L10`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `user/src/console.rs:L11-L11`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 函数/符号名重合：与 2024 chaos 在“内存管理”维度发现 2 个同名定义：handle_alloc_error, new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `GCore/user/src/lib.rs:L33-L33`：fn handle_alloc_error
    代码片段：`pub fn handle_alloc_error(layout: core::alloc::Layout) -> ! {`
  - `user/src/lib.rs:L27-L27`：fn handle_alloc_error
    代码片段：`pub fn handle_alloc_error(layout: core::alloc::Layout) -> ! {`
  - `GCore/user/vendor/buddy_system_allocator/src/frame.rs:L39-L39`：fn new
    代码片段：`pub fn new() -> Self {`
  - `user/src/bin/user_shell.rs:L30-L30`：fn new
    代码片段：`pub fn new(command: &str) -> Self {`
- 函数/符号名重合：与 2024 chaos 在“调度与任务管理”维度发现 2 个同名定义：sys_yield, yield_。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `GCore/user/src/syscall.rs:L137-L137`：fn sys_yield
    代码片段：`pub fn sys_yield() -> isize {`
  - `user/src/syscall.rs:L81-L81`：fn sys_yield
    代码片段：`pub fn sys_yield() -> isize {`
  - `GCore/user/src/usr_call.rs:L28-L28`：fn yield_
    代码片段：`pub fn yield_() -> isize {`
  - `user/src/lib.rs:L92-L92`：fn yield_
    代码片段：`pub fn yield_() -> isize {`
- 函数/符号名重合：与 2024 chaos 在“同步机制”维度发现 1 个同名定义：from。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `GCore/user/vendor/spin/src/mutex.rs:L243-L243`：fn from
    代码片段：`fn from(data: T) -> Self {`
  - `os/src/timer.rs:L316-L316`：fn from
    代码片段：`pub fn from(clock_id: usize) -> Self {`
- 结构体/类型重合：与 2024 chaos 在“设备驱动”维度发现 1 个同名定义：Stdout。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `GCore/user/src/console.rs:L7-L7`：struct Stdout
    代码片段：`struct Stdout;`
  - `user/src/console.rs:L8-L8`：struct Stdout
    代码片段：`struct Stdout;`
- 文件路径重合：与 2024 chaos 在“设备驱动”维度出现同名文件源码路径 `GCore/os/src/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `GCore/os/src/console.rs:L32-L36`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use core::{ arch::asm,`
- 文件路径重合：与 2024 chaos 在“中断与异常”维度出现同名文件源码路径 `GCore/os/src/timer.rs` / `os/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `GCore/os/src/timer.rs:L19-L23`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V timer-related functionality use alloc::{collections::BinaryHeap, sync::Arc};`
- 文件路径重合：与 2024 chaos 在“内存管理”维度出现同名文件源码路径 `GCore/os/src/mm/memory_set.rs` / `os/src/mm/memory_set.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `GCore/os/src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use super::map_area::*; use super::page_table::PageTable; use super::{PhysAddr, PhysPageNum, StepByOne, VirtAddr, VirtPageNum}; use crate::config::*;`
  - `os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`//! Address Space ['MemorySet'] management of Process use alloc::{`
- 文件路径重合：与 2024 chaos 在“调度与任务管理”维度出现同名文件源码路径 `GCore/os/src/task/task.rs` / `os/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `GCore/os/src/task/task.rs:L517-L521`：关键词命中
    代码片段：`// 销毁所有其他同一线程组的任务 manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
  - `os/src/task/task.rs:L1-L3`：关键词命中
    代码片段：`//! Types related to task management & Functions for completely changing TCB use alloc::{`
- 文件路径重合：与 2024 chaos 在“系统调用”维度出现同名文件源码路径 `GCore/os/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `GCore/os/src/syscall/fs.rs:L683-L687`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `os/src/syscall/fs.rs:L20-L24`：关键词命中
    代码片段：`}, mm::{translated_byte_buffer, translated_refmut, translated_str}, syscall::{ errno::{EACCES, EBADF, EBUSY, ENOENT, ENOTDIR, ENOTTY}, Dirent,`

## 相似点

- 与 2024 chaos 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 chaos 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `GCore/os/src/console.rs:L32-L36`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `GCore/os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2024 chaos 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `GCore/os/src/fs/mod.rs:L13-L17`：关键词命中
    代码片段：`pub mod dirent; pub mod file_descriptor; mod inode; mod timestamp; mod vfs;`
  - `GCore/os/src/fs/vfs.rs:L11-L15`：关键词命中
    代码片段：`use super::filesystem::{pre_mount, FS_Type}; pub trait VFS: DowncastSync { fn close(&self) { unreachable!()`
- 与 2024 chaos 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `GCore/os/src/timer.rs:L19-L23`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `GCore/os/src/hal/arch/riscv/trap/mod.rs:L17-L21`：关键词命中
    代码片段：`use riscv::register::{ mtvec::TrapMode, scause::{self, Exception, Interrupt, Trap}, sepc, sie, stval, stvec, };`
- 与 2024 chaos 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `GCore/os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `GCore/os/src/mm/map_area.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::Debug; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2024 chaos 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `GCore/os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `GCore/os/src/task/task.rs:L517-L521`：关键词命中
    代码片段：`// 销毁所有其他同一线程组的任务 manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 与 2024 chaos 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `GCore/os/src/timer.rs:L309-L313`：关键词命中
    代码片段：`} use core::sync::atomic::{AtomicU64}; /// 启动以来的时间源（秒），例如通过读取 mtime / TSC`
  - `GCore/os/src/fs/vfs.rs:L39-L43`：关键词命中
    代码片段：`pub fn open_fs( block_device: Arc<dyn BlockDevice>, index_cache_mgr: Arc<spin::Mutex<BlockCacheManager>>, ) -> Arc<dyn VFS> { let fs_type = pre_mount();`
- 与 2024 chaos 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `GCore/os/src/syscall/fs.rs:L683-L687`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `GCore/os/src/syscall/mod.rs:L168-L172`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`
- 与 2024 chaos 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 chaos 的语言构成不同：待测作品为 {'json': 112, 'build': 7097, 'markdown': 17507, 'yaml': 81, 'text': 1710, 'toml': 9892, 'rust': 558755, 'asm': 1134, 'make': 531, 'c': 48}，历史样本为 {'yaml': 63, 'json': 76, 'markdown': 12073, 'build': 3169, 'toml': 9675, 'c': 1011, 'asm': 791, 'text': 268, 'rust': 388593}。（置信度：medium）

## 可能创新点

- 待测作品包含 202 个历史样本 2024 chaos 中未见的独特函数/类型定义（如 `try_upgrade_internal`, `extend`, `from_iter`, `downgrade_to_upgradeable`, `sys_wait4`, `try_write_internal`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `GCore/user/vendor/spin/src/rw_lock.rs:L439-L439`：fn try_upgrade_internal
    代码片段：`fn try_upgrade_internal(self, strong: bool) -> Result<RwLockWriteGuard<'rwlock, T>, Self> {`
  - `GCore/user/vendor/bitflags/src/lib.rs:L809-L809`：fn extend
    代码片段：`fn extend<T: $crate::_core::iter::IntoIterator<Item=Self>>(&mut self, iterator: T) {`

## 附录：核验摘要

- 关键结论数：29
- 含证据关键结论数：29（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
