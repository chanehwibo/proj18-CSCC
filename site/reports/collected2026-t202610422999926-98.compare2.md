# 2026 T202610422999926 比较报告

- 对比历史仓库：2024 NPUcore-IMPACT!!!
- 生成时间：2026-07-02T05:00:53.479874+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 NPUcore-IMPACT!!!（来源：赛事历史作品）：画像相似度 score=9.42；同属 ucore-variant 风格; 语言构成相似度 0.95; OS 维度重合度 1.00; 代码规模接近度 0.52

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = u64;`
  - `os/vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/user_bringup_posix_fs.rs:L6-L10`：关键词命中
    代码片段：`pub fn run_stage_posix_fs_meta() { info!("[bringup][posix-fs-meta] BEGIN"); #[cfg(not(feature = "vfs-bridge"))] { warn!("[posix-fs-meta] vfs-bridge off: skip");`
  - `os/components/wateros-base/base-config/src/fs.rs:L1-L5`：关键词命中
    代码片段：`//! 根卷文件 I/O 与页缓存、块设备 LBA 缓存的 bring-up 尺度常量。 //! //! VFS 大文件分流、全局页缓存与 'impl-block-cache' 装饰器均从此处读取默认值。 /// 文件页缓存行大小（字节）；read/write 按该粒度加载与淘汰。`
  - `os/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod file_trait; mod filesystem; // mod inode; mod layout; pub mod poll;`
  - `os/src/fs/cache.rs:L193-L197`：关键词命中
    代码片段：`/// Every time kernel tried to alloc this pagecache this number will increase 1(at most 1) /// Every time out of memory occurred this number will decrease 1(at least 0) /// When...`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/trap_handler.rs:L1-L3`：关键词命中
    代码片段：`//! **组合层内核 trap 路由**：实现各架构 'trap_entry_rust' 交出的 'TrapCause' //! 分支与返回路径， //! 经 ['arch_api_v0::kernel_trap::register_kernel_trap_handler'] 挂接到`
  - `os/components/wateros-platform/platform-arch/arch-api/api-v0/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 帧、异常与中断原因，以及与用户态系统调用 ABI 的读写桥接（**纯架构语义**）。 //! //! ## 'TrapCause' 与架构敏感解码`
  - `os/src/timer.rs:L18-L22`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/main.rs:L36-L40`：关键词命中
    代码片段：`mod syscall; mod task; mod timer; // use crate::arch::config::DISK_IMAGE_BASE;`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/components/wateros-mm/mm-api/api-v0/src/mmap.rs:L9-L13`：关键词命中
    代码片段：`use crate::error::MmResult; use crate::flags::MapFlags; use crate::frame_allocator::PhysicalFrameAllocator; use crate::perm::PagePerm;`
  - `os/src/main.rs:L10-L14`：关键词命中
    代码片段：`//!    'platform::time::set_frequency_hz'；再初始化控制台与日志、堆分配器，再做 //!    'platform::arch' 与 MM //!   （frame 范围、Sv39、内核页表等；含 'mm' 自检日志）。 //! 3. 初始化任务与组合层 trap 路由（'task::init'、'trap_ha...`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `os/src/mm/map_area.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::Debug; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/components/wateros-task/task-api/api-v0/src/task.rs:L32-L36`：关键词命中
    代码片段：`pub enum TaskBlockReason { /// 主动让出 CPU。 Yield, /// 因定时睡眠而阻塞。 Sleep,`
  - `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs:L1-L3`：关键词命中
    代码片段：`//! 任务相关系统调用：'yield'、'exit'、'waitpid'、'getpid'/'getppid'/'gettid'、 //! 'times'、'uname'、'prctl'、'getrlimit'/'setrlimit'。`
  - `os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `os/src/task/task.rs:L380-L384`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/vendor/ext4plus/src/sync.rs:L4-L8`：关键词命中
    代码片段：`mod async_inner { /// A mutual exclusion primitive useful for protecting shared data. pub struct Mutex<T> { inner: async_lock::Mutex<T>, }`
  - `os/vendor/concurrent-queue/src/sync.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization facade to choose between 'core' primitives and 'loom' primitives. #[cfg(all(feature = "portable-atomic", not(loom)))] mod sync_impl { pub(crate) use core::cell;`
  - `os/src/fs/mod.rs:L35-L39`：关键词命中
    代码片段：`use lazy_static::*; use log::warn; use spin::Mutex; lazy_static! {`
  - `os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::{arch::BLOCK_SZ, config::PAGE_SIZE, drivers::BLOCK_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/components/wateros-klog/src/syscall.rs:L1-L3`：关键词命中
    代码片段：`//! 'sys_syslog' 语义（内核缓冲侧）；用户指针由 'wateros-syscall' 负责拷贝。 //! 本模块代码由AI完成`
  - `os/components/wateros-abi/abi-api/api-v0/src/syscall_args.rs:L1-L5`：关键词命中
    代码片段：`//! 系统调用参数在寄存器/陷阱帧与内核分发层之间的 C 布局承载形式。 //! //! 槽位数量由 'wateros-base-config' 的 'config::syscall::MAX_SYSCALL_ARGS' 决定， //! 与 ABI 约定的参数个数上限一致。`
  - `os/src/syscall/fs.rs:L548-L552`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `os/src/syscall/mod.rs:L115-L119`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度发现片段级代码相似度 0.69 （token=0.53, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：debug, derive, enum, pub。（置信度：medium）
  证据：
  - `os/components/wateros-mm/mm-api/api-v0/src/mmap.rs:L14-L18`：enum MmapKind
    代码片段：`/// mmap 映射类型（先做最小集）。 #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub enum MmapKind { /// 匿名映射：内容来自零填充（当前阶段可先按需延后实现）。 Anonymous,`
  - `os/src/mm/zram.rs:L5-L9`：enum ZramError
    代码片段：`#[derive(Debug)] pub enum ZramError { InvalidIndex, NoSpace,`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现 1 个同名定义：flush。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/components/wateros-driver/driver-block/block-impl/impl-block-cache/src/lib.rs:L81-L81`：fn flush
    代码片段：`pub fn flush(&mut self) -> DriverResult<()> {`
  - `os/src/console.rs:L79-L79`：fn flush
    代码片段：`fn flush(&self) {}`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现 2 个同名定义：read, write。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/components/wateros-driver/driver-network/src/socket_handles.rs:L79-L79`：fn read
    代码片段：`fn read(&mut self, buf: &mut [u8]) -> VfsResult<usize> {`
  - `os/src/fs/cache.rs:L15-L15`：fn read
    代码片段：`fn read<T, V>(&self, offset: usize, f: impl FnOnce(&T) -> V) -> V;`
  - `os/components/wateros-driver/driver-network/src/socket_handles.rs:L83-L83`：fn write
    代码片段：`fn write(&mut self, buf: &[u8]) -> VfsResult<usize> {`
  - `os/src/fs/ext4_file.rs:L20-L20`：fn write
    代码片段：`fn write(&self, offset: Option<&mut usize>, buf: &[u8]) -> usize {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“内存管理”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/components/wateros-driver/driver-network/network-impl/impl-dummy/src/lib.rs:L22-L22`：fn new
    代码片段：`pub fn new(mac: [u8; 6]) -> Self {`
  - `os/src/mm/address.rs:L232-L232`：fn new
    代码片段：`pub fn new(start: T, end: T) -> Self {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“同步机制”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/components/wateros-base/src/sync/uniprocessor.rs:L21-L21`：fn new
    代码片段：`pub unsafe fn new(value: T) -> Self {`
  - `os/src/fs/cache.rs:L81-L81`：fn new
    代码片段：`pub fn new(buffer_ptr: *mut [u8; BUFFER_SIZE]) -> Self {`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现 1 个同名定义：BlockDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/components/wateros-driver/driver-block/block-api/api-v0/src/lib.rs:L37-L37`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send {`
  - `os/src/fs/mod.rs:L20-L20`：trait BlockDevice
    代码片段：`pub trait BlockDevice: crate::drivers::block::BlockDevice {}`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“同步机制”维度发现 2 个同名定义：BlockDevice, BlockCacheManager。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/components/wateros-driver/driver-block/block-api/api-v0/src/lib.rs:L37-L37`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send {`
  - `os/src/fs/mod.rs:L20-L20`：trait BlockDevice
    代码片段：`pub trait BlockDevice: crate::drivers::block::BlockDevice {}`
  - `os/components/wateros-driver/driver-block/block-impl/impl-block-cache/src/manager.rs:L16-L16`：struct BlockCacheManager
    代码片段：`pub struct BlockCacheManager;`
  - `os/src/fs/cache.rs:L97-L97`：struct BlockCacheManager
    代码片段：`pub struct BlockCacheManager {`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度出现同名文件源码路径 `os/components/wateros-task/task-api/api-v0/src/task.rs` / `os/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/components/wateros-task/task-api/api-v0/src/task.rs:L32-L36`：关键词命中
    代码片段：`pub enum TaskBlockReason { /// 主动让出 CPU。 Yield, /// 因定时睡眠而阻塞。 Sleep,`
  - `os/src/task/task.rs:L380-L384`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度出现同名文件源码路径 `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs` / `os/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs:L1-L3`：关键词命中
    代码片段：`//! 任务相关系统调用：'yield'、'exit'、'waitpid'、'getpid'/'getppid'/'gettid'、 //! 'times'、'uname'、'prctl'、'getrlimit'/'setrlimit'。`
  - `os/src/task/task.rs:L380-L384`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`

## 相似点

- 与 2024 NPUcore-IMPACT!!! 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = u64;`
  - `os/vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/user_bringup_posix_fs.rs:L6-L10`：关键词命中
    代码片段：`pub fn run_stage_posix_fs_meta() { info!("[bringup][posix-fs-meta] BEGIN"); #[cfg(not(feature = "vfs-bridge"))] { warn!("[posix-fs-meta] vfs-bridge off: skip");`
  - `os/components/wateros-base/base-config/src/fs.rs:L1-L5`：关键词命中
    代码片段：`//! 根卷文件 I/O 与页缓存、块设备 LBA 缓存的 bring-up 尺度常量。 //! //! VFS 大文件分流、全局页缓存与 'impl-block-cache' 装饰器均从此处读取默认值。 /// 文件页缓存行大小（字节）；read/write 按该粒度加载与淘汰。`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/trap_handler.rs:L1-L3`：关键词命中
    代码片段：`//! **组合层内核 trap 路由**：实现各架构 'trap_entry_rust' 交出的 'TrapCause' //! 分支与返回路径， //! 经 ['arch_api_v0::kernel_trap::register_kernel_trap_handler'] 挂接到`
  - `os/components/wateros-platform/platform-arch/arch-api/api-v0/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 帧、异常与中断原因，以及与用户态系统调用 ABI 的读写桥接（**纯架构语义**）。 //! //! ## 'TrapCause' 与架构敏感解码`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/components/wateros-mm/mm-api/api-v0/src/mmap.rs:L9-L13`：关键词命中
    代码片段：`use crate::error::MmResult; use crate::flags::MapFlags; use crate::frame_allocator::PhysicalFrameAllocator; use crate::perm::PagePerm;`
  - `os/src/main.rs:L10-L14`：关键词命中
    代码片段：`//!    'platform::time::set_frequency_hz'；再初始化控制台与日志、堆分配器，再做 //!    'platform::arch' 与 MM //!   （frame 范围、Sv39、内核页表等；含 'mm' 自检日志）。 //! 3. 初始化任务与组合层 trap 路由（'task::init'、'trap_ha...`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/components/wateros-task/task-api/api-v0/src/task.rs:L32-L36`：关键词命中
    代码片段：`pub enum TaskBlockReason { /// 主动让出 CPU。 Yield, /// 因定时睡眠而阻塞。 Sleep,`
  - `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs:L1-L3`：关键词命中
    代码片段：`//! 任务相关系统调用：'yield'、'exit'、'waitpid'、'getpid'/'getppid'/'gettid'、 //! 'times'、'uname'、'prctl'、'getrlimit'/'setrlimit'。`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/vendor/ext4plus/src/sync.rs:L4-L8`：关键词命中
    代码片段：`mod async_inner { /// A mutual exclusion primitive useful for protecting shared data. pub struct Mutex<T> { inner: async_lock::Mutex<T>, }`
  - `os/vendor/concurrent-queue/src/sync.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization facade to choose between 'core' primitives and 'loom' primitives. #[cfg(all(feature = "portable-atomic", not(loom)))] mod sync_impl { pub(crate) use core::cell;`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/components/wateros-klog/src/syscall.rs:L1-L3`：关键词命中
    代码片段：`//! 'sys_syslog' 语义（内核缓冲侧）；用户指针由 'wateros-syscall' 负责拷贝。 //! 本模块代码由AI完成`
  - `os/components/wateros-abi/abi-api/api-v0/src/syscall_args.rs:L1-L5`：关键词命中
    代码片段：`//! 系统调用参数在寄存器/陷阱帧与内核分发层之间的 C 布局承载形式。 //! //! 槽位数量由 'wateros-base-config' 的 'config::syscall::MAX_SYSCALL_ARGS' 决定， //! 与 ABI 约定的参数个数上限一致。`
- 与 2024 NPUcore-IMPACT!!! 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 NPUcore-IMPACT!!! 的语言构成不同：待测作品为 {'build': 8075, 'markdown': 14040, 'toml': 307, 'rust': 419201, 'asm': 728, 'text': 2102, 'json': 387, 'yaml': 128}，历史样本为 {'json': 20, 'build': 338, 'markdown': 743, 'rust': 177629, 'asm': 10}。（置信度：medium）

## 可能创新点

- 待测作品包含 272 个历史样本 2024 NPUcore-IMPACT!!! 中未见的独特函数/类型定义（如 `socket_setsockopt`, `socket_sendto`, `socket_getsockopt`, `socket_recvfrom`, `socket_bind`, `socket_accept`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `os/components/wateros-driver/driver-network/src/lib.rs:L947-L947`：fn socket_setsockopt
    代码片段：`pub fn socket_setsockopt(handle: SocketHandle, level: usize, optname: usize, optval: &[u8]) -> Result<(), &'static str> {`
  - `os/components/wateros-driver/driver-network/src/lib.rs:L745-L745`：fn socket_sendto
    代码片段：`pub fn socket_sendto(handle: SocketHandle, data: &[u8], ip: [u8; 4], port: u16) -> Result<usize, &'static str> {`

## 附录：核验摘要

- 关键结论数：24
- 含证据关键结论数：24（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
