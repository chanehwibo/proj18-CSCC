# 2026 T202610346999652 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t202610346999652-1129`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：718
- 代码/文本行数：209373
- 主要语言：rust 167003 LOC, c 31276 LOC, build 4608 LOC, markdown 2579 LOC, make 1158 LOC

## 总览

2026 T202610346999652 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 167003 LOC, c: 31276 LOC, build: 4608 LOC, markdown: 2579 LOC。仓库包含 718 个已扫描文件、约 209373 行可分析文本，当前抽取到 12166 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, Makefile, crates/axnet-ng/Cargo.toml, kernel/Cargo.toml, make/Makefile。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] resolver = "2" members = ["kernel"]`
  - `Makefile:L1-L3`：构建入口
    代码片段：`# Build Options ARCH ?= riscv64 export ARCH`
  - `crates/axnet-ng/Cargo.toml:L1-L3`：构建入口
    代码片段：`# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO # # When uploading crates to the registry Cargo will automatically`
  - `kernel/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "starry-kernel" version.workspace = true`
  - `make/Makefile:L1-L3`：构建入口
    代码片段：`# Available arguments: # * General options: #     - 'ARCH': Target architecture: x86_64, riscv64, aarch64, loongarch64`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn parent_sigchld_autoreap, fn notify_reaper_of_inherited_zombie, fn install_current_user_page_table_root, fn cleanup_task_tables, fn add_task_to_table。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/task/mod.rs` | L1-L3 | 关键词命中 |
| `kernel/src/task/ops.rs` | L86-L90 | 关键词命中 |
| `kernel/src/task/stat.rs` | L6-L10 | 关键词命中 |
| `kernel/src/task/ops.rs` | L40-L44 | fn parent_sigchld_autoreap |
| `kernel/src/task/ops.rs` | L48-L52 | fn notify_reaper_of_inherited_zombie |
| `kernel/src/task/ops.rs` | L77-L81 | fn install_current_user_page_table_root |

### 关键代码片段

  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. mod accounting;`
  - `kernel/src/task/ops.rs:L86-L90`：关键词命中
    代码片段：`} /// Cleanup expired entries in the task tables. /// /// This function is intended to be used during memory leak analysis to remove`
  - `kernel/src/task/stat.rs:L6-L10`：关键词命中
    代码片段：`use starry_signal::Signo; use crate::task::{AsThread, ProcStateHint, TaskUsage}; fn task_state(task: &AxTaskRef) -> char {`
  - `kernel/src/task/ops.rs:L40-L44`：fn parent_sigchld_autoreap
    代码片段：`static PROCESS_GROUP_TABLE: RwLock<WeakMap<Pid, Weak<ProcessGroup>>> = RwLock::new(WeakMap::new()); fn parent_sigchld_autoreap(parent: &ProcessData) -> (bool, bool) { let action...`

### 相关符号

`fn parent_sigchld_autoreap` at `kernel/src/task/ops.rs:L40`、`fn notify_reaper_of_inherited_zombie` at `kernel/src/task/ops.rs:L48`、`fn install_current_user_page_table_root` at `kernel/src/task/ops.rs:L77`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：struct IoVec, struct IoVectorBuf, fn new, fn len, fn is_aligned。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/mm/stats.rs` | L48-L52 | 关键词命中 |
| `kernel/src/mm/access.rs` | L105-L109 | 关键词命中 |
| `kernel/src/mm/loader.rs` | L37-L41 | 关键词命中 |
| `kernel/src/mm/io.rs` | L12-L16 | struct IoVec |
| `kernel/src/mm/io.rs` | L18-L22 | struct IoVectorBuf |
| `kernel/src/mm/io.rs` | L25-L29 | fn new |

### 关键代码片段

  - `kernel/src/mm/stats.rs:L48-L52`：关键词命中
    代码片段：`cached_bytes: usages.get(UsageKind::PageCache), mapped_bytes: usages.get(UsageKind::VirtMem), page_table_bytes: usages.get(UsageKind::PageTable), } }`
  - `kernel/src/mm/access.rs:L105-L109`：关键词命中
    代码片段：`// TODO: this is inefficient, but we have to do this instead of // querying the page table since the page might has not been // allocated yet. let curr = current();`
  - `kernel/src/mm/loader.rs:L37-L41`：关键词命中
    代码片段：`#[cfg(not(any(target_arch = "aarch64", target_arch = "loongarch64")))] { // ARMv8 (aarch64) and LoongArch64 use separate page tables for user space // (aarch64: TTBR0_EL1, Loong...`
  - `kernel/src/mm/io.rs:L12-L16`：struct IoVec
    代码片段：`#[repr(C)] #[derive(Debug, Copy, Clone, AnyBitPattern)] pub struct IoVec { pub iov_base: *mut u8, pub iov_len: isize,`

### 相关符号

`struct IoVec` at `kernel/src/mm/io.rs:L12`、`struct IoVectorBuf` at `kernel/src/mm/io.rs:L18`、`fn new` at `kernel/src/mm/io.rs:L25`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：enum SocketIoDirection, fn socket_timeout_configured, fn restart_class_for_fd_io, fn restart_class_for_futex, fn restart_class_for_syscall。（置信度：medium）
  - 静态识别到 343 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/syscall/mod.rs` | L114-L118 | 关键词命中 |
| `kernel/src/syscall/fs/io.rs` | L197-L201 | 关键词命中 |
| `kernel/src/syscall/fs/ctl.rs` | L578-L582 | 关键词命中 |
| `kernel/src/syscall/mod.rs` | L35-L39 | enum SocketIoDirection |
| `kernel/src/syscall/mod.rs` | L40-L44 | fn socket_timeout_configured |
| `kernel/src/syscall/mod.rs` | L57-L61 | fn restart_class_for_fd_io |

### 关键代码片段

  - `kernel/src/syscall/mod.rs:L114-L118`：关键词命中
    代码片段：`/// Fast path for trivial getter syscalls that only read from the current /// thread and return a scalar: no user copy, no restart, no blocking, no /// timer accounting. Handlin...`
  - `kernel/src/syscall/fs/io.rs:L197-L201`：关键词命中
    代码片段：`pub fn sys_dummy_fd(sysno: Sysno) -> AxResult<isize> { warn!("Unimplemented fd syscall: {sysno}"); Err(AxError::Unsupported) }`
  - `kernel/src/syscall/fs/ctl.rs:L578-L582`：关键词命中
    代码片段：`} // Directory buffer for getdents64 syscall struct DirBuffer { buf: Vec<u8>,`
  - `kernel/src/syscall/mod.rs:L35-L39`：enum SocketIoDirection
    代码片段：`#[derive(Clone, Copy)] enum SocketIoDirection { Read, Write,`

### 相关符号

`enum SocketIoDirection` at `kernel/src/syscall/mod.rs:L35`、`fn socket_timeout_configured` at `kernel/src/syscall/mod.rs:L40`、`fn restart_class_for_fd_io` at `kernel/src/syscall/mod.rs:L57`、`fn restart_class_for_syscall` at `kernel/src/syscall/mod.rs:L70`、`fn maybe_request_syscall_restart` at `kernel/src/syscall/mod.rs:L105`、`fn handle_syscall` at `kernel/src/syscall/mod.rs:L131`、`fn sys_getuid` at `kernel/src/syscall/sys.rs:L23`、`fn sys_geteuid` at `kernel/src/syscall/sys.rs:L27`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：fn fsize_limit, fn raise_sigxfsz, fn allowed_write_len, fn check_resize_limit, struct TmpfileState。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/file/fs.rs` | L171-L175 | 关键词命中 |
| `kernel/src/file/flock.rs` | L12-L16 | 关键词命中 |
| `kernel/src/file/lease.rs` | L58-L62 | 关键词命中 |
| `kernel/src/file/fs.rs` | L30-L34 | fn fsize_limit |
| `kernel/src/file/fs.rs` | L35-L39 | fn raise_sigxfsz |
| `kernel/src/file/fs.rs` | L41-L45 | fn allowed_write_len |

### 关键代码片段

  - `kernel/src/file/fs.rs:L171-L175`：关键词命中
    代码片段：`Kstat { dev: metadata.device, ino: metadata.inode, mode, nlink: metadata.nlink as _,`
  - `kernel/src/file/flock.rs:L12-L16`：关键词命中
    代码片段：`use starry_process::Pid; /// Inode identity: (device, inode number). pub(crate) type InodeId = (u64, u64); type FlockOwner = u64;`
  - `kernel/src/file/lease.rs:L58-L62`：关键词命中
    代码片段：`fn lease_id(loc: &Location) -> InodeId { (loc.mountpoint().device(), loc.inode()) }`
  - `kernel/src/file/fs.rs:L30-L34`：fn fsize_limit
    代码片段：`const O_PATH_STATUS_FLAG: u32 = linux_raw_sys::general::O_PATH; fn fsize_limit() -> Option<u64> { let limit = current().as_thread().proc_data.rlim.read()[RLIMIT_FSIZE].current;...`

### 相关符号

`fn fsize_limit` at `kernel/src/file/fs.rs:L30`、`fn raise_sigxfsz` at `kernel/src/file/fs.rs:L35`、`fn allowed_write_len` at `kernel/src/file/fs.rs:L41`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct FutexWaitDeadline, fn assert_unsigned, fn futex_key_from, fn futex_wait_clock, fn futex_wait_deadline。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/syscall/sync/membarrier.rs` | L1-L3 | 关键词命中 |
| `kernel/src/time.rs` | L1-L3 | 关键词命中 |
| `kernel/src/entry.rs` | L6-L10 | 关键词命中 |
| `kernel/src/syscall/sync/futex.rs` | L21-L25 | struct FutexWaitDeadline |
| `kernel/src/syscall/sync/futex.rs` | L26-L30 | fn assert_unsigned |
| `kernel/src/syscall/sync/futex.rs` | L34-L38 | fn futex_key_from |

### 关键代码片段

  - `kernel/src/syscall/sync/membarrier.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{Ordering, compiler_fence}; use axerrno::{AxError, AxResult};`
  - `kernel/src/time.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicI64, Ordering}; use axerrno::{AxError, AxResult};`
  - `kernel/src/entry.rs:L6-L10`：关键词命中
    代码片段：`use axfs::FS_CONTEXT; use axhal::{power::system_off, uspace::UserContext}; use axsync::Mutex; use axtask::{AxTaskExt, SchedState, spawn_task_with_sched}; use starry_process::{Pi...`
  - `kernel/src/syscall/sync/futex.rs:L21-L25`：struct FutexWaitDeadline
    代码片段：`#[derive(Clone, Copy)] struct FutexWaitDeadline { clock: AlarmClock, deadline: Duration,`

### 相关符号

`struct FutexWaitDeadline` at `kernel/src/syscall/sync/futex.rs:L21`、`fn assert_unsigned` at `kernel/src/syscall/sync/futex.rs:L26`、`fn futex_key_from` at `kernel/src/syscall/sync/futex.rs:L34`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn time_value_from_nanos, enum AlarmClock, fn now, enum PosixTimerClock, fn alarm_clock。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/task/timer.rs` | L110-L114 | 关键词命中 |
| `third_party/rust-patches/axcpu/src/trap.rs` | L1-L3 | 关键词命中 |
| `kernel/src/task/mod.rs` | L13-L17 | 关键词命中 |
| `kernel/src/task/timer.rs` | L34-L38 | fn time_value_from_nanos |
| `kernel/src/task/timer.rs` | L42-L46 | enum AlarmClock |
| `kernel/src/task/timer.rs` | L48-L52 | fn now |

### 关键代码片段

  - `kernel/src/task/timer.rs:L110-L114`：关键词命中
    代码片段：`/// The action to take when an alarm fires. enum AlarmAction { /// Interrupt a task and poll an itimer if the queued generation is current. PollITimer { task: WeakAxTaskRef,`
  - `third_party/rust-patches/axcpu/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use memory_addr::VirtAddr;`
  - `kernel/src/task/mod.rs:L13-L17`：关键词命中
    代码片段：`mod stat; mod thread; mod timer; mod user;`
  - `kernel/src/task/timer.rs:L34-L38`：fn time_value_from_nanos
    代码片段：`use crate::time::wall_time; fn time_value_from_nanos(nanos: usize) -> TimeValue { let secs = nanos as u64 / NANOS_PER_SEC; let nsecs = nanos as u64 - secs * NANOS_PER_SEC;`

### 相关符号

`fn time_value_from_nanos` at `kernel/src/task/timer.rs:L34`、`enum AlarmClock` at `kernel/src/task/timer.rs:L42`、`fn now` at `kernel/src/task/timer.rs:L48`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：enum DeviceMmap, trait DeviceOps, fn read_at, fn write_at, fn ioctl。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `third_party/rust-patches/axdriver/src/drivers.rs` | L9-L13 | 关键词命中 |
| `third_party/rust-patches/virtio-drivers/src/hal.rs` | L5-L9 | 关键词命中 |
| `third_party/rust-patches/virtio-drivers/src/lib.rs` | L1-L3 | 关键词命中 |
| `kernel/src/pseudofs/device.rs` | L14-L18 | enum DeviceMmap |
| `kernel/src/pseudofs/device.rs` | L28-L32 | trait DeviceOps |
| `kernel/src/pseudofs/device.rs` | L30-L34 | fn read_at |

### 关键代码片段

  - `third_party/rust-patches/axdriver/src/drivers.rs:L9-L13`：关键词命中
    代码片段：`pub use super::dummy::*; use crate::AxDeviceEnum; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `third_party/rust-patches/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `third_party/rust-patches/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
  - `kernel/src/pseudofs/device.rs:L14-L18`：enum DeviceMmap
    代码片段：`/// Mmap behavior for devices. pub enum DeviceMmap { /// The device is not mappable. None,`

### 相关符号

`enum DeviceMmap` at `kernel/src/pseudofs/device.rs:L14`、`trait DeviceOps` at `kernel/src/pseudofs/device.rs:L28`、`fn read_at` at `kernel/src/pseudofs/device.rs:L30`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
