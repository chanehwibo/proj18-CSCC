# 2026 T2026106149910833 比较报告

- 对比历史仓库：2025 BlueStar
- 生成时间：2026-07-02T09:03:44.903873+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 BlueStar（来源：赛事历史作品）：画像相似度 score=9.31；同属 ucore-variant 风格; 语言构成相似度 0.93; OS 维度重合度 1.00; 代码规模接近度 0.44

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 BlueStar 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `source/arch/loongarch64/console.rs:L4-L8`：关键词命中
    代码片段：`// 写入 fatal，同时集中处理短写、地址错误或输出失败。 pub fn write_fatal(bytes: &[u8]) -> usize { let console = super::mmu::uncached_mmio_address(CONSOLE_PHYS_BASE) as *mut u8; for byte in bytes { un...`
  - `kernel/src/console.rs:L8-L12`：关键词命中
    代码片段：`#[macro_export] /// Kernel print function,it can output to serial macro_rules! kprint { ($fmt: literal $(, $($arg: tt)+)?) => {`
  - `kernel/src/driver/mod.rs:L1-L2`：关键词命中
    代码片段：`mod virtio_blk; pub use self::virtio_blk::*;`
- 与 2025 BlueStar 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `source/kernel/syscall_runtime/vfs_backend.rs:L7-L11`：关键词命中
    代码片段：`use notify::{notify_inotify_path, wake_socket_readable, wake_socket_writable}; // ActiveSyscallVfs 是 系统调用 VFS 后端 的状态载体，字段组合表达需要一起维护的不变量。 #[derive(Clone, Copy)] pub(super) struct...`
  - `source/kernel/syscall_runtime/vfs_backend/notify.rs:L1-L5`：关键词命中
    代码片段：`use super::*; // notify_inotify_description 是 系统调用 VFS 后端 的辅助函数，负责 notify inotify description 相关的状态更新或结果计算。 pub(in crate::kernel::syscall_runtime) fn notify_inotify_description(...`
  - `kernel/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`pub mod vfs; pub mod fs_backend; pub mod component;`
  - `kernel/src/fs/vfs/api.rs:L5-L9`：关键词命中
    代码片段：`use log::error; use crate::task::{TASK_MANAER, TASK_MANAGER_INIT}; use crate::fs::vfs::{File, KStat, MountFs, OpenFlags, ROOTFS,  VfsFsError, VfsStat}; use alloc::format; use al...`
- 与 2025 BlueStar 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `source/kernel/boot.rs:L115-L119`：关键词命中
    代码片段：`}; let trap = bsp.install_trap_vector(BoundaryMode::ApplyUnsafe); let timer = bsp.start_timer(BoundaryMode::ApplyUnsafe); BOOT_STAGE.store(2, Ordering::Relaxed);`
  - `source/kernel/scheduler.rs:L163-L167`：关键词命中
    代码片段：`} // BlockedContextSwitchPlan 保存跨 trap、调度或用户态返回必须恢复的执行上下文。 #[derive(Clone, Copy, Debug, Eq, PartialEq)] pub struct BlockedContextSwitchPlan {`
  - `kernel/src/trap/mod.rs:L4-L8`：关键词命中
    代码片段：`use crate::{config::*, task::TASK_MANAER, time::set_next_time_interupt}; use log::{debug, error, }; use riscv::register::{scause::{self, Exception, Trap},  sstatus::{self, SPP,...`
  - `kernel/src/time/timer.rs:L27-L31`：关键词命中
    代码片段：`///设置下一次时钟中断(不带中断检查，太耗时间，所有耗时操作其实都不应该出现在这里)，mtimecmp使用原始tick计数 pub fn set_next_time_interupt(){ //需要考虑调用误差，即使错过也没事，只是提前触发中断(mtime < mtimecmp)`
- 与 2025 BlueStar 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `source/kernel/exec.rs:L200-L204`：关键词命中
    代码片段：`single_set_rseq_registration(None); if !single_set_user_memory_state_from_address_space( process.heap_base(), process.program_break(), USER_MMAP_BASE,`
  - `source/kernel/syscall_runtime/exec_runtime.rs:L354-L358`：关键词命中
    代码片段：`single_set_rseq_registration(None); if !single_set_user_memory_state_from_address_space( process.heap_base(), process.program_break(), USER_MMAP_BASE,`
  - `kernel/src/memory/mod.rs:L2-L6`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod memset;`
  - `kernel/src/memory/memset.rs:L6-L10`：关键词命中
    代码片段：`use log::{debug, error, trace}; use core::arch::asm; use riscv::register::satp; use crate::fs::vfs::File; use crate::task::{TASK_MANAER};`
- 与 2025 BlueStar 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `source/kernel/scheduler/user_task.rs:L16-L20`：关键词命中
    代码片段：`} // prepare_pending_user_task_context 是 用户任务调度桥接 的辅助函数，负责 prepare pending user task context 相关的状态更新或结果计算。 pub fn prepare_pending_user_task_context( bsp: BspServices,`
  - `source/kernel/exec.rs:L17-L21`：关键词命中
    代码片段：`}; use crate::core::mm::{MemoryFoundation, PAGE_SIZE}; use crate::core::scheduler as core_scheduler; use crate::core::task::{ credentials_for_process, single_enter_pid, single_p...`
  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`mod task; mod process; use crate::fs::vfs::{OpenFlags, vfs_open};`
  - `kernel/src/task/task.rs:L19-L23`：关键词命中
    代码片段：`use crate::memory::*; use crate::sbi::shutdown; use crate::task::Signal; use crate::task::file_loader; use log::debug;`
- 与 2025 BlueStar 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `source/kernel/boot.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicUsize, Ordering}; use crate::arch::contract::{`
  - `source/kernel/exec.rs:L1-L5`：关键词命中
    代码片段：`//! Kernel orchestration for file-backed exec. use core::sync::atomic::{AtomicUsize, Ordering}; use crate::arch::contract::boot::BOOT_INIT_ARG_MAX;`
  - `kernel/src/sync/up.rs:L19-L23`：关键词命中
    代码片段：`#[track_caller] pub fn lock(&self)->core::cell::RefMut<'_,T>{ match self.inner.try_borrow_mut() { Ok(g) => g,`
  - `kernel/vendor/spin/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! Locks that have the same behaviour as a mutex. //! //! The ['Mutex'] in the root of the crate, can be configured using the 'ticket_mutex' feature.`
- 与 2025 BlueStar 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `source/kernel/syscall_runtime.rs:L41-L45`：关键词命中
    代码片段：`}; use crate::core::scheduler::{self, SchedulerError, EDF_TASK_CAPACITY}; use crate::core::syscall::{ dispatch_single_with_runtime, dispatch_with_runtime, ExecRequest, SyscallEr...`
  - `source/kernel/syscall_runtime/exec_runtime.rs:L1-L5`：关键词命中
    代码片段：`use super::*; // exec_from_syscall 是 系统调用运行时 的辅助函数，负责 exec from syscall 相关的状态更新或结果计算。 pub fn exec_from_syscall(request: ExecRequest) -> Result<PendingUserEntry, isize> { let bsp...`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`mod syscall; use log::{error}; use crate::memory::VirAddr;`
  - `kernel/src/syscall/syscall.rs:L1272-L1276`：关键词命中
    代码片段：`/// 返回：成功返回映射起始地址；失败返回 -1 /// /// 参数说明（Linux riscv64 ABI，用户态用 ecall 传参）： /// 'addr'  : 映射起始虚拟地址（用户 hint）。若带 'MAP_FIXED' 则必须使用该地址。 /// 'len'   : 映射长度（字节）。内核按页对齐到覆盖该区间。`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 BlueStar 在“内存管理”维度发现片段级代码相似度 0.63 （token=0.50, structure=0.88）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, debug, derive, pub。（置信度：medium）
  证据：
  - `source/core/mm/fault.rs:L3-L7`：enum UserFaultAccess
    代码片段：`// UserFaultAccess 列出内存管理的互斥结果分支，match 时可以显式覆盖每种情况。 #[derive(Clone, Copy, Debug, Eq, PartialEq)] pub enum UserFaultAccess { Execute, Read,`
  - `kernel/src/memory/memset.rs:L36-L40`：struct VirNumRange
    代码片段：`///开始和结束，一个范围,自动[start,end] start地址自动向下取整，end也向下取整，因为virnumrange用于代码映射，防止代码缺失, startva/PAGE =num+offset ,从num开始，endva/pagesize=endva+offset由于闭区间所以向下取整,防止多映射 #[derive(Debug,Clone...`
- 文件路径重合：与 2025 BlueStar 在“设备驱动”维度出现同名文件源码路径 `source/arch/loongarch64/console.rs` / `kernel/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `source/arch/loongarch64/console.rs:L4-L8`：关键词命中
    代码片段：`// 写入 fatal，同时集中处理短写、地址错误或输出失败。 pub fn write_fatal(bytes: &[u8]) -> usize { let console = super::mmu::uncached_mmio_address(CONSOLE_PHYS_BASE) as *mut u8; for byte in bytes { un...`
  - `kernel/src/console.rs:L8-L12`：关键词命中
    代码片段：`#[macro_export] /// Kernel print function,it can output to serial macro_rules! kprint { ($fmt: literal $(, $($arg: tt)+)?) => {`
- 文件路径重合：与 2025 BlueStar 在“设备驱动”维度出现同名文件源码路径 `source/arch/riscv64/console.rs` / `kernel/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `source/arch/riscv64/console.rs:L11-L15`：fn write_fatal
    代码片段：`/// 直接向控制台 MMIO 数据寄存器写入字节。 // 写入 fatal，同时集中处理短写、地址错误或输出失败。 pub fn write_fatal(bytes: &[u8]) -> usize { for byte in bytes { unsafe {`
  - `kernel/src/console.rs:L8-L12`：关键词命中
    代码片段：`#[macro_export] /// Kernel print function,it can output to serial macro_rules! kprint { ($fmt: literal $(, $($arg: tt)+)?) => {`
- 文件路径重合：与 2025 BlueStar 在“系统调用”维度出现同名文件源码路径 `source/core/syscall.rs` / `kernel/src/syscall/syscall.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `source/core/syscall.rs:L790-L794`：关键词命中
    代码片段：`// 处理 fd wait or errno 的文件资源，统一 fd 所有权、权限检查和错误返回。 pub(in crate::core::syscall) fn fd_wait_or_errno<V: SyscallVfs>( current_pid: Pid, frame: SyscallFrame,`
  - `kernel/src/syscall/syscall.rs:L1272-L1276`：关键词命中
    代码片段：`/// 返回：成功返回映射起始地址；失败返回 -1 /// /// 参数说明（Linux riscv64 ABI，用户态用 ecall 传参）： /// 'addr'  : 映射起始虚拟地址（用户 hint）。若带 'MAP_FIXED' 则必须使用该地址。 /// 'len'   : 映射长度（字节）。内核按页对齐到覆盖该区间。`

## 相似点

- 与 2025 BlueStar 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 BlueStar 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `source/arch/loongarch64/console.rs:L4-L8`：关键词命中
    代码片段：`// 写入 fatal，同时集中处理短写、地址错误或输出失败。 pub fn write_fatal(bytes: &[u8]) -> usize { let console = super::mmu::uncached_mmio_address(CONSOLE_PHYS_BASE) as *mut u8; for byte in bytes { un...`
  - `kernel/src/console.rs:L8-L12`：关键词命中
    代码片段：`#[macro_export] /// Kernel print function,it can output to serial macro_rules! kprint { ($fmt: literal $(, $($arg: tt)+)?) => {`
- 与 2025 BlueStar 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `source/kernel/syscall_runtime/vfs_backend.rs:L7-L11`：关键词命中
    代码片段：`use notify::{notify_inotify_path, wake_socket_readable, wake_socket_writable}; // ActiveSyscallVfs 是 系统调用 VFS 后端 的状态载体，字段组合表达需要一起维护的不变量。 #[derive(Clone, Copy)] pub(super) struct...`
  - `source/kernel/syscall_runtime/vfs_backend/notify.rs:L1-L5`：关键词命中
    代码片段：`use super::*; // notify_inotify_description 是 系统调用 VFS 后端 的辅助函数，负责 notify inotify description 相关的状态更新或结果计算。 pub(in crate::kernel::syscall_runtime) fn notify_inotify_description(...`
- 与 2025 BlueStar 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `source/kernel/boot.rs:L115-L119`：关键词命中
    代码片段：`}; let trap = bsp.install_trap_vector(BoundaryMode::ApplyUnsafe); let timer = bsp.start_timer(BoundaryMode::ApplyUnsafe); BOOT_STAGE.store(2, Ordering::Relaxed);`
  - `source/kernel/scheduler.rs:L163-L167`：关键词命中
    代码片段：`} // BlockedContextSwitchPlan 保存跨 trap、调度或用户态返回必须恢复的执行上下文。 #[derive(Clone, Copy, Debug, Eq, PartialEq)] pub struct BlockedContextSwitchPlan {`
- 与 2025 BlueStar 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `source/kernel/exec.rs:L200-L204`：关键词命中
    代码片段：`single_set_rseq_registration(None); if !single_set_user_memory_state_from_address_space( process.heap_base(), process.program_break(), USER_MMAP_BASE,`
  - `source/kernel/syscall_runtime/exec_runtime.rs:L354-L358`：关键词命中
    代码片段：`single_set_rseq_registration(None); if !single_set_user_memory_state_from_address_space( process.heap_base(), process.program_break(), USER_MMAP_BASE,`
- 与 2025 BlueStar 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `source/kernel/scheduler/user_task.rs:L16-L20`：关键词命中
    代码片段：`} // prepare_pending_user_task_context 是 用户任务调度桥接 的辅助函数，负责 prepare pending user task context 相关的状态更新或结果计算。 pub fn prepare_pending_user_task_context( bsp: BspServices,`
  - `source/kernel/exec.rs:L17-L21`：关键词命中
    代码片段：`}; use crate::core::mm::{MemoryFoundation, PAGE_SIZE}; use crate::core::scheduler as core_scheduler; use crate::core::task::{ credentials_for_process, single_enter_pid, single_p...`
- 与 2025 BlueStar 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `source/kernel/boot.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicUsize, Ordering}; use crate::arch::contract::{`
  - `source/kernel/exec.rs:L1-L5`：关键词命中
    代码片段：`//! Kernel orchestration for file-backed exec. use core::sync::atomic::{AtomicUsize, Ordering}; use crate::arch::contract::boot::BOOT_INIT_ARG_MAX;`
- 与 2025 BlueStar 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `source/kernel/syscall_runtime.rs:L41-L45`：关键词命中
    代码片段：`}; use crate::core::scheduler::{self, SchedulerError, EDF_TASK_CAPACITY}; use crate::core::syscall::{ dispatch_single_with_runtime, dispatch_with_runtime, ExecRequest, SyscallEr...`
  - `source/kernel/syscall_runtime/exec_runtime.rs:L1-L5`：关键词命中
    代码片段：`use super::*; // exec_from_syscall 是 系统调用运行时 的辅助函数，负责 exec from syscall 相关的状态更新或结果计算。 pub fn exec_from_syscall(request: ExecRequest) -> Result<PendingUserEntry, isize> { let bsp...`
- 与 2025 BlueStar 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 BlueStar 的语言构成不同：待测作品为 {'json': 789, 'build': 169, 'markdown': 1760, 'yaml': 313, 'rust': 84350, 'text': 551, 'asm': 295}，历史样本为 {'json': 53, 'build': 2684, 'markdown': 10844, 'toml': 9593, 'c': 336, 'rust': 279800, 'asm': 641, 'text': 5383, 'yaml': 51}。（置信度：medium）

## 可能创新点

- 待测作品包含 449 个历史样本 2025 BlueStar 中未见的独特函数/类型定义（如 `read_user_usize`, `release_active_record_locks_for_fd`, `reserve_kernel_task_stack`, `commit_wake_reschedule_continue`, `plan_yield_switch`, `proc_fdinfo_stat_for_path`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `source/core/syscall.rs:L1135-L1135`：fn read_user_usize
    代码片段：`fn read_user_usize<R: UserMemoryReader>(memory: &R, address: usize) -> Result<usize, SyscallError> {`
  - `source/kernel/syscall_runtime.rs:L511-L511`：fn release_active_record_locks_for_fd
    代码片段：`fn release_active_record_locks_for_fd(fd: usize) -> Result<Option<FileRecordLockWaitKey>, FdError> {`

## 附录：核验摘要

- 关键结论数：19
- 含证据关键结论数：19（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
