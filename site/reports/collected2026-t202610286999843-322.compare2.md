# 2026 T202610286999843 比较报告

- 对比历史仓库：2025 dwg
- 生成时间：2026-07-02T05:00:08.279179+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 dwg（来源：赛事历史作品）：画像相似度 score=8.51；同属 ucore-variant 风格; 语言构成相似度 0.57; OS 维度重合度 1.00; 代码规模接近度 0.37

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 dwg 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `kernel/dev/uart.c:L1-L3`：关键词命中
    代码片段：`// low-level driver routines for 16550a UART. #include "memlayout.h"`
  - `kernel/dev/virtio.c:L1-L5`：关键词命中
    代码片段：`/* QEMU提供的虚拟磁盘的驱动 QEMUOPTS = -device virtio-blk-device,drive=x0,bus=virtio-mmio-bus.0 这个文件最终提供三个重要函数: virtio_init() // 初始化函数`
- 与 2025 dwg 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs.h:L8-L12`：关键词命中
    代码片段：`// Disk layout: // [ boot block | super block | log | inode blocks | //                                          free bit map | data blocks] //`
  - `kernel/fs/file.c:L43-L47`：关键词命中
    代码片段：`file_t* file_create_dev(char* path, uint16 major, uint16 minor) { // FAT32 Phase 1: Skip disk inode creation for devices // Just allocate a file structure`
  - `kernel/fs/fat32.c:L60-L64`：关键词命中
    代码片段：`} // 4. Fill FAT Info fat_info.sectors_per_cluster = bpb->sectors_per_cluster; fat_info.bytes_per_cluster = bpb->sectors_per_cluster * FAT32_SECTOR_SIZE;`
- 与 2025 dwg 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L32-L36`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/dev/plic.c:L1-L3`：关键词命中
    代码片段：`// Platform-level interrupt controller #include "memlayout.h"`
  - `kernel/dev/timer.c:L1-L5`：关键词命中
    代码片段：`#include "lib/lock.h" #include "lib/print.h" #include "dev/timer.h" #include "memlayout.h" #include "proc/cpu.h"`
- 与 2025 dwg 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/vm.c:L10-L14`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/vm-la.c:L27-L31`：关键词命中
    代码片段：`pagetable_t kpgtbl; kpgtbl = (pagetable_t) kalloc(); memset(kpgtbl, 0, PGSIZE); proc_mapstacks(kpgtbl);`
  - `kernel/mem/kvm.c:L13-L17`：关键词命中
    代码片段：`// 根据pagetable,找到va对应的pte // 若设置alloc=true 则在PTE无效时尝试申请一个物理页 // 成功返回PTE, 失败返回NULL`
  - `kernel/mem/uvm.c:L14-L18`：关键词命中
    代码片段：`uint64 va, pa, page; int flags; pte_t* pte; for(va = begin; va < end; va += PGSIZE)`
- 与 2025 dwg 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc.c:L8-L12`：关键词命中
    代码片段：`#endif #include "spinlock.h" #include "proc.h" #include "defs.h"`
  - `kernel/proc.h:L46-L50`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `kernel/proc/cpu.c:L1-L3`：关键词命中
    代码片段：`#include "proc/cpu.h" #include "lib/lock.h" #include "riscv.h"`
  - `kernel/proc/exec.c:L1-L3`：关键词命中
    代码片段：`#include "proc/cpu.h" #include "proc/elf.h" #include "mem/vmem.h"`
- 与 2025 dwg 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs.c:L15-L19`：关键词命中
    代码片段：`#include "param.h" #include "stat.h" #include "spinlock.h" #include "proc.h" #include "sleeplock.h"`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "fs.h"`
  - `kernel/lib/spinlock.c:L1-L3`：关键词命中
    代码片段：`#include "lib/lock.h" #include "lib/print.h" #include "proc/cpu.h"`
  - `kernel/lib/sleeplock.c:L1-L3`：关键词命中
    代码片段：`#include "lib/lock.h" #include "lib/print.h" #include "proc/cpu.h"`
- 与 2025 dwg 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap.c:L59-L63`：关键词命中
    代码片段：`kexit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h"`
  - `kernel/syscall/syscall.c:L3-L7`：关键词命中
    代码片段：`#include "mem/mmap.h" #include "mem/vmem.h" #include "syscall/syscall.h" #include "syscall/sysnum.h" #include "syscall/sysfunc.h"`
  - `kernel/syscall/sysfile.c:L5-L9`：关键词命中
    代码片段：`#include "lib/str.h" #include "lib/print.h" #include "syscall/syscall.h" #include "syscall/sysfunc.h" #include "fs/fat32.h"`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 dwg 在“调度与任务管理”维度发现片段级代码相似度 0.92 （token=0.88, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：context, ra, sp, struct, uint64。（置信度：high）
  证据：
  - `kernel/proc.h:L7-L11`：struct context
    代码片段：`// Saved registers for kernel context switches. #ifdef __loongarch__ struct context { uint64 ra; uint64 sp;`
  - `include/proc/proc.h:L18-L22`：struct context
    代码片段：`// context 定义 typedef struct context { uint64 ra; // 返回地址 uint64 sp; // 栈指针`
- 与 2025 dwg 在“调度与任务管理”维度发现片段级代码相似度 0.68 （token=0.50, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：ra, struct, uint64。（置信度：medium）
  证据：
  - `kernel/proc.h:L67-L71`：struct trapframe
    代码片段：`// the entire kernel call stack. #ifdef __loongarch__ struct trapframe { /*   0 */ uint64 ra; /*   8 */ uint64 tp;`
  - `include/proc/proc.h:L18-L22`：struct context
    代码片段：`// context 定义 typedef struct context { uint64 ra; // 返回地址 uint64 sp; // 栈指针`
- 宏名重合：与 2025 dwg 在“中断与异常”维度发现 4 个同名定义：PLIC_PRIORITY, PLIC_PENDING, PLIC_SENABLE, PLIC_SPRIORITY。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/memlayout.h:L79-L79`：macro PLIC_PRIORITY
    代码片段：`#define PLIC_PRIORITY (PLIC + 0x0)`
  - `include/memlayout.h:L45-L45`：macro PLIC_PRIORITY
    代码片段：`#define PLIC_PRIORITY(id) (PLIC_BASE + (id) * 4)`
  - `kernel/memlayout.h:L80-L80`：macro PLIC_PENDING
    代码片段：`#define PLIC_PENDING (PLIC + 0x1000)`
  - `include/memlayout.h:L46-L46`：macro PLIC_PENDING
    代码片段：`#define PLIC_PENDING (PLIC_BASE + 0x1000)`
- 宏名重合：与 2025 dwg 在“内存管理”维度发现 4 个同名定义：PGSIZE, PTE_V, PTE_D, PTE_W。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/loongarch.h:L360-L360`：macro PGSIZE
    代码片段：`#define PGSIZE 4096`
  - `include/common.h:L25-L25`：macro PGSIZE
    代码片段：`#define PGSIZE 4096    // 物理页大小`
  - `kernel/loongarch.h:L366-L366`：macro PTE_V
    代码片段：`#define PTE_V (1L << 0)`
  - `include/mem/vmem.h:L54-L54`：macro PTE_V
    代码片段：`#define PTE_V (1 << 0) // valid`
- 宏名重合：与 2025 dwg 在“系统调用”维度发现 4 个同名定义：__SYSCALL_H__, SYS_fstat, __scc, __syscall1。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `codex-basic-syscall-bundle/references/lib/syscall.h:L2-L2`：macro __SYSCALL_H__
    代码片段：`#define __SYSCALL_H__`
  - `user/sys.h:L2-L2`：macro __SYSCALL_H__
    代码片段：`#define __SYSCALL_H__`
  - `codex-basic-syscall-bundle/references/lib/syscall.h:L9-L9`：macro SYS_fstat
    代码片段：`#define SYS_fstat SYS_fstat64`
  - `user/syscall_num.h:L23-L23`：macro SYS_fstat
    代码片段：`#define SYS_fstat        80`
- 函数/符号名重合：与 2025 dwg 在“调度与任务管理”维度发现 1 个同名定义：swtch。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/swtch-la.S:L14-L14`：fn swtch
    代码片段：`swtch:`
  - `kernel/proc/swtch.S:L7-L7`：fn swtch
    代码片段：`swtch:`
- 结构体/类型重合：与 2025 dwg 在“调度与任务管理”维度发现 4 个同名定义：context, cpu, trapframe, proc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/proc.h:L9-L9`：struct context
    代码片段：`struct context {`
  - `include/proc/proc.h:L20-L20`：struct context
    代码片段：`typedef struct context {`
  - `kernel/proc.h:L47-L47`：struct cpu
    代码片段：`struct cpu {`
  - `include/proc/cpu.h:L7-L7`：struct cpu
    代码片段：`typedef struct cpu {`
- 宏名重合：与 2025 dwg 在“设备驱动”维度发现 1 个同名定义：BACKSPACE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/console.c:L25-L25`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100  // erase the last output character`
  - `kernel/dev/console.c:L9-L9`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100      // 回退`
- 宏名重合：与 2025 dwg 在“文件系统”维度发现 1 个同名定义：SYS_fstat。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `codex-basic-syscall-bundle/references/lib/syscall.h:L9-L9`：macro SYS_fstat
    代码片段：`#define SYS_fstat SYS_fstat64`
  - `user/syscall_num.h:L23-L23`：macro SYS_fstat
    代码片段：`#define SYS_fstat        80`
- 宏名重合：与 2025 dwg 在“调度与任务管理”维度发现 1 个同名定义：NPROC。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/param.h:L1-L1`：macro NPROC
    代码片段：`#define NPROC        64  // maximum number of processes`
  - `include/common.h:L26-L26`：macro NPROC
    代码片段：`#define NPROC 64       // 最大进程数量`
- 结构体/类型重合：与 2025 dwg 在“文件系统”维度发现 2 个同名定义：file, dirent。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/file.h:L1-L1`：struct file
    代码片段：`struct file {`
  - `include/fs/file.h:L37-L37`：struct file
    代码片段：`typedef struct file {`
  - `kernel/fs.h:L59-L59`：struct dirent
    代码片段：`struct dirent {`
  - `mkfs/mkfs.c:L40-L40`：struct dirent
    代码片段：`typedef struct dirent {`
- 结构体/类型重合：与 2025 dwg 在“中断与异常”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/proc.h:L69-L69`：struct trapframe
    代码片段：`struct trapframe {`
  - `include/proc/proc.h:L40-L40`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 结构体/类型重合：与 2025 dwg 在“内存管理”维度发现 2 个同名定义：trapframe, mmap_region。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/proc.h:L69-L69`：struct trapframe
    代码片段：`struct trapframe {`
  - `include/proc/proc.h:L40-L40`：struct trapframe
    代码片段：`typedef struct trapframe {`
  - `kernel/proc.h:L153-L153`：struct mmap_region
    代码片段：`struct mmap_region {`
  - `include/mem/mmap.h:L20-L20`：struct mmap_region
    代码片段：`typedef struct mmap_region {`
- 结构体/类型重合：与 2025 dwg 在“同步机制”维度发现 2 个同名定义：sleeplock, spinlock。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/sleeplock.h:L2-L2`：struct sleeplock
    代码片段：`struct sleeplock {`
  - `include/lib/lock.h:L13-L13`：struct sleeplock
    代码片段：`typedef struct sleeplock {`
  - `kernel/spinlock.h:L2-L2`：struct spinlock
    代码片段：`struct spinlock {`
  - `include/lib/lock.h:L6-L6`：struct spinlock
    代码片段：`typedef struct spinlock {`
- 结构体/类型重合：与 2025 dwg 在“系统调用”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/proc.h:L69-L69`：struct trapframe
    代码片段：`struct trapframe {`
  - `include/proc/proc.h:L40-L40`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 文件路径重合：与 2025 dwg 在“设备驱动”维度出现同名文件源码路径 `kernel/uart.c` / `kernel/dev/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/dev/uart.c:L1-L3`：关键词命中
    代码片段：`// low-level driver routines for 16550a UART. #include "memlayout.h"`
- 文件路径重合：与 2025 dwg 在“设备驱动”维度出现同名文件源码路径 `kernel/virtio.h` / `include/dev/virtio.h`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `include/dev/virtio.h:L14-L18`：macro VIRTIO_MMIO_MAGIC_VALUE
    代码片段：`// virtio mmio control registers, mapped starting at 0x10001000. // from qemu virtio_mmio.h #define VIRTIO_MMIO_MAGIC_VALUE 0x000 // 0x74726976 #define VIRTIO_MMIO_VERSION 0x004...`
- 文件路径重合：与 2025 dwg 在“文件系统”维度出现同名文件源码路径 `kernel/file.c` / `kernel/fs/file.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/file.c:L105-L109`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `kernel/fs/file.c:L43-L47`：关键词命中
    代码片段：`file_t* file_create_dev(char* path, uint16 major, uint16 minor) { // FAT32 Phase 1: Skip disk inode creation for devices // Just allocate a file structure`
- 文件路径重合：与 2025 dwg 在“中断与异常”维度出现同名文件源码路径 `kernel/plic.c` / `kernel/dev/plic.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/dev/plic.c:L1-L3`：关键词命中
    代码片段：`// Platform-level interrupt controller #include "memlayout.h"`
- 文件路径重合：与 2025 dwg 在“调度与任务管理”维度出现同名文件源码路径 `kernel/proc.c` / `kernel/proc/proc.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/proc.c:L8-L12`：关键词命中
    代码片段：`#endif #include "spinlock.h" #include "proc.h" #include "defs.h"`
  - `kernel/proc/proc.c:L3-L7`：关键词命中
    代码片段：`#include "mem/pmem.h" #include "mem/vmem.h" #include "proc/cpu.h" #include "proc/initcode.h" #include "common.h"`
- 文件路径重合：与 2025 dwg 在“调度与任务管理”维度出现同名文件源码路径 `kernel/proc.h` / `include/proc/proc.h`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/proc.h:L46-L50`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `include/proc/proc.h:L18-L22`：struct context
    代码片段：`// context 定义 typedef struct context { uint64 ra; // 返回地址 uint64 sp; // 栈指针`

## 相似点

- 与 2025 dwg 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 dwg 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
- 与 2025 dwg 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs.h:L8-L12`：关键词命中
    代码片段：`// Disk layout: // [ boot block | super block | log | inode blocks | //                                          free bit map | data blocks] //`
- 与 2025 dwg 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L32-L36`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
- 与 2025 dwg 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/vm.c:L10-L14`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/vm-la.c:L27-L31`：关键词命中
    代码片段：`pagetable_t kpgtbl; kpgtbl = (pagetable_t) kalloc(); memset(kpgtbl, 0, PGSIZE); proc_mapstacks(kpgtbl);`
- 与 2025 dwg 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc.c:L8-L12`：关键词命中
    代码片段：`#endif #include "spinlock.h" #include "proc.h" #include "defs.h"`
  - `kernel/proc.h:L46-L50`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2025 dwg 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs.c:L15-L19`：关键词命中
    代码片段：`#include "param.h" #include "stat.h" #include "spinlock.h" #include "proc.h" #include "sleeplock.h"`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "fs.h"`
- 与 2025 dwg 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap.c:L59-L63`：关键词命中
    代码片段：`kexit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h"`
- 与 2025 dwg 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 dwg 的语言构成不同：待测作品为 {'markdown': 15110, 'build': 636, 'c': 21332, 'asm': 49345}，历史样本为 {'json': 48, 'make': 26, 'markdown': 3706, 'build': 285, 'c': 9534, 'asm': 2251}。（置信度：medium）

## 可能创新点

- 待测作品包含 52 个历史样本 2025 dwg 中未见的独特函数/类型定义（如 `procstate`, `le16_to_cpu`, `le32_to_cpu`, `linux_dirent64_probe`, `child_func`, `ext4_extent_header`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/proc.h:L151-L151`：enum procstate
    代码片段：`enum procstate { UNUSED, USED, SLEEPING, RUNNABLE, RUNNING, ZOMBIE };`
  - `kernel/ext4.c:L27-L27`：fn le16_to_cpu
    代码片段：`static inline ushort le16_to_cpu(ushort x) {`
- 待测作品代码规模（86,423 LOC）显著大于历史样本 2025 dwg（15,850 LOC），可能包含更多功能模块或更完整的实现，但也可能包含非内核代码，需人工确认。（置信度：low）

## 附录：核验摘要

- 关键结论数：37
- 含证据关键结论数：36（97.3%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
