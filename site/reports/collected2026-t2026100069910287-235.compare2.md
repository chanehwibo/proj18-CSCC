# 2026 T2026100069910287 比较报告

- 对比历史仓库：2024 NPUcore-IMPACT!!!
- 生成时间：2026-07-02T04:57:51.905954+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 NPUcore-IMPACT!!!（来源：赛事历史作品）：画像相似度 score=9.19；同属 ucore-variant 风格; 语言构成相似度 0.82; OS 维度重合度 1.00; 代码规模接近度 0.54

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/osdriver/mod.rs:L4-L8`：关键词命中
    代码片段：`use alloc::{boxed::Box, sync::Arc, vec::Vec}; use driver::{ CHAR_DEVICE, DeviceType, block::{dw_mshc::MMC, virtblk::VirtBlkDevice},`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `lib/ext4/src/fs.rs:L1-L5`：关键词命中
    代码片段：`use alloc::{ffi::CString, sync::Arc}; use config::{inode::InodeType, vfs::MountFlags}; use systype::error::SysResult; use vfs::{`
  - `lib/fat32/src/fs.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{boxed::Box, sync::Arc}; use config::vfs::MountFlags; use driver::BlockDevice; use systype::error::SysResult;`
  - `os/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod file_trait; mod filesystem; // mod inode; mod layout; pub mod poll;`
  - `os/src/fs/cache.rs:L193-L197`：关键词命中
    代码片段：`/// Every time kernel tried to alloc this pagecache this number will increase 1(at most 1) /// Every time out of memory occurred this number will decrease 1(at least 0) /// When...`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/trap/mod.rs:L5-L9`：关键词命中
    代码片段：`pub mod trap_syscall; use arch::trap::enable_interrupt; pub use trap_handler::trap_handler;`
  - `lib/arch/src/trap/mod.rs:L3-L7`：关键词命中
    代码片段：`define_arch_mods!(); /// An abstract representation of the trap mode. pub enum TrapMode { /// Traps into a specific address.`
  - `os/src/timer.rs:L18-L22`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/main.rs:L36-L40`：关键词命中
    代码片段：`mod syscall; mod task; mod timer; // use crate::arch::config::DISK_IMAGE_BASE;`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/vm/mod.rs:L9-L13`：关键词命中
    代码片段：`pub mod iomap; mod page_table; pub use page_table::switch_to_kernel_page_table;`
  - `lib/config/src/mm.rs:L95-L99`：关键词命中
    代码片段：`/// Width of page offset pub const PAGE_OFFSET_WIDTH: usize = 12; /// Width of a physical address in Sv39 pub const PA_WIDTH_SV39: usize = 56; /// Width of a virtual address in...`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `os/src/mm/map_area.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::Debug; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L9-L13`：关键词命中
    代码片段：`pub mod sig_members; pub mod signal; pub mod task; pub mod taskf; pub mod threadgroup;`
  - `kernel/src/task/tid.rs:L26-L30`：关键词命中
    代码片段：`/// 'tid_alloc()' can look for an unused id from ['INIT_PROC_ID'] /// and allocate it for a new thread. pub fn tid_alloc() -> TidHandle { match TID_ALLOCATOR.lock().alloc() {`
  - `os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `os/src/task/task.rs:L380-L384`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/vm/shm.rs:L3-L7`：关键词命中
    代码片段：`use config::mm::{MMAP_END, MMAP_START, PAGE_SIZE}; use mm::address::VirtAddr; use mutex::ShareMutex; use shm::SharedMemory; use systype::{`
  - `kernel/src/logging.rs:L1-L5`：关键词命中
    代码片段：`use core::{ fmt, sync::atomic::{AtomicBool, AtomicUsize, Ordering}, };`
  - `os/src/fs/mod.rs:L35-L39`：关键词命中
    代码片段：`use lazy_static::*; use log::warn; use spin::Mutex; lazy_static! {`
  - `os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::{arch::BLOCK_SZ, config::PAGE_SIZE, drivers::BLOCK_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/syscall/fs.rs:L241-L245`：关键词命中
    代码片段：`///   files such as stdout. When user calls 'printf' in user space without 'close(STDOUT)' + 'dup', the 'fd' is STDOUT ///   by default. /// - This is a 'async' syscall, which m...`
  - `kernel/src/syscall/mod.rs:L37-L41`：关键词命中
    代码片段：`use user::*; use crate::syscall::time::{sys_adjtimex, sys_clock_adjtime, sys_clock_getres, sys_clock_settime}; pub async fn syscall(syscall_no: usize, args: [usize; 6]) -> usize {`
  - `os/src/syscall/fs.rs:L548-L552`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `os/src/syscall/mod.rs:L115-L119`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现 4 个同名定义：open, lseek, new, create。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `user/src/lib.rs:L128-L128`：fn open
    代码片段：`pub fn open(dirfd: i32, pathname: &str, flags: OpenFlags, mode: InodeMode) -> isize {`
  - `os/src/fs/directory_tree.rs:L269-L269`：fn open
    代码片段：`pub fn open(`
  - `user/src/lib.rs:L138-L138`：fn lseek
    代码片段：`pub fn lseek(fd: usize, offset: isize, whence: usize) -> isize {`
  - `os/src/fs/ext4_file.rs:L102-L102`：fn lseek
    代码片段：`fn lseek(&self, offset: isize, whence: super::SeekWhence) -> Result<usize, isize> {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度发现 3 个同名定义：new, cmp, partial_cmp。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `lib/timer/src/async_timer.rs:L24-L24`：fn new
    代码片段：`pub fn new(timeout: Duration, future: F) -> Self {`
  - `os/src/timer.rs:L105-L105`：fn new
    代码片段：`pub fn new() -> Self {`
  - `lib/timer/src/timer.rs:L26-L26`：fn cmp
    代码片段：`fn cmp(&self, other: &Self) -> core::cmp::Ordering {`
  - `os/src/timer.rs:L89-L89`：fn cmp
    代码片段：`fn cmp(&self, other: &Self) -> Ordering {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“内存管理”维度发现 3 个同名定义：handle_alloc_error, read_page, from。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `user/src/lib.rs:L38-L38`：fn handle_alloc_error
    代码片段：`pub fn handle_alloc_error(layout: core::alloc::Layout) -> ! {`
  - `user/src/lib.rs:L35-L35`：fn handle_alloc_error
    代码片段：`pub fn handle_alloc_error(layout: core::alloc::Layout) -> ! {`
  - `lib/vfs/src/file.rs:L295-L295`：fn read_page
    代码片段：`pub async fn read_page(&self, pos: usize) -> SysResult<Arc<Page>> {`
  - `os/src/fs/swap.rs:L46-L46`：fn read_page
    代码片段：`fn read_page(block_ids: &[usize], buf: &mut [u8]) {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“同步机制”维度发现 4 个同名定义：read, read_page, write, new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `lib/vfs/src/file.rs:L260-L260`：fn read
    代码片段：`pub async fn read(&self, buf: &mut [u8]) -> SysResult<usize> {`
  - `os/src/fs/cache.rs:L15-L15`：fn read
    代码片段：`fn read<T, V>(&self, offset: usize, f: impl FnOnce(&T) -> V) -> V;`
  - `lib/vfs/src/file.rs:L295-L295`：fn read_page
    代码片段：`pub async fn read_page(&self, pos: usize) -> SysResult<Arc<Page>> {`
  - `os/src/fs/swap.rs:L46-L46`：fn read_page
    代码片段：`fn read_page(block_ids: &[usize], buf: &mut [u8]) {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `user/src/console.rs:L11-L11`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `user/src/console.rs:L10-L10`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度发现 1 个同名定义：yield_。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `user/src/lib.rs:L170-L170`：fn yield_
    代码片段：`pub fn yield_() -> isize {`
  - `user/src/usr_call.rs:L14-L14`：fn yield_
    代码片段：`pub fn yield_() -> isize {`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现 1 个同名定义：Stdout。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `user/src/console.rs:L8-L8`：struct Stdout
    代码片段：`struct Stdout;`
  - `user/src/console.rs:L7-L7`：struct Stdout
    代码片段：`struct Stdout;`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现 2 个同名定义：File, Stat。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `lib/vfs/src/file.rs:L51-L51`：trait File
    代码片段：`pub trait File: Send + Sync + DowncastSync {`
  - `os/src/fs/file_trait.rs:L14-L14`：trait File
    代码片段：`pub trait File: DowncastSync {`
  - `lib/vfs/src/stat.rs:L5-L5`：struct Stat
    代码片段：`pub struct Stat {`
  - `os/src/fs/layout.rs:L99-L99`：struct Stat
    代码片段：`pub struct Stat {`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度发现 1 个同名定义：ITimerVal。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `lib/systype/src/time/itime.rs:L7-L7`：struct ITimerVal
    代码片段：`pub struct ITimerVal {`
  - `os/src/timer.rs:L263-L263`：struct ITimerVal
    代码片段：`pub struct ITimerVal {`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“同步机制”维度发现 1 个同名定义：File。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `lib/vfs/src/file.rs:L51-L51`：trait File
    代码片段：`pub trait File: Send + Sync + DowncastSync {`
  - `os/src/fs/file_trait.rs:L14-L14`：trait File
    代码片段：`pub trait File: DowncastSync {`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“系统调用”维度出现同名文件源码路径 `kernel/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/syscall/fs.rs:L241-L245`：关键词命中
    代码片段：`///   files such as stdout. When user calls 'printf' in user space without 'close(STDOUT)' + 'dup', the 'fd' is STDOUT ///   by default. /// - This is a 'async' syscall, which m...`
  - `os/src/syscall/fs.rs:L548-L552`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`

## 相似点

- 与 2024 NPUcore-IMPACT!!! 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/osdriver/mod.rs:L4-L8`：关键词命中
    代码片段：`use alloc::{boxed::Box, sync::Arc, vec::Vec}; use driver::{ CHAR_DEVICE, DeviceType, block::{dw_mshc::MMC, virtblk::VirtBlkDevice},`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `lib/ext4/src/fs.rs:L1-L5`：关键词命中
    代码片段：`use alloc::{ffi::CString, sync::Arc}; use config::{inode::InodeType, vfs::MountFlags}; use systype::error::SysResult; use vfs::{`
  - `lib/fat32/src/fs.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{boxed::Box, sync::Arc}; use config::vfs::MountFlags; use driver::BlockDevice; use systype::error::SysResult;`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/trap/mod.rs:L5-L9`：关键词命中
    代码片段：`pub mod trap_syscall; use arch::trap::enable_interrupt; pub use trap_handler::trap_handler;`
  - `lib/arch/src/trap/mod.rs:L3-L7`：关键词命中
    代码片段：`define_arch_mods!(); /// An abstract representation of the trap mode. pub enum TrapMode { /// Traps into a specific address.`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/vm/mod.rs:L9-L13`：关键词命中
    代码片段：`pub mod iomap; mod page_table; pub use page_table::switch_to_kernel_page_table;`
  - `lib/config/src/mm.rs:L95-L99`：关键词命中
    代码片段：`/// Width of page offset pub const PAGE_OFFSET_WIDTH: usize = 12; /// Width of a physical address in Sv39 pub const PA_WIDTH_SV39: usize = 56; /// Width of a virtual address in...`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L9-L13`：关键词命中
    代码片段：`pub mod sig_members; pub mod signal; pub mod task; pub mod taskf; pub mod threadgroup;`
  - `kernel/src/task/tid.rs:L26-L30`：关键词命中
    代码片段：`/// 'tid_alloc()' can look for an unused id from ['INIT_PROC_ID'] /// and allocate it for a new thread. pub fn tid_alloc() -> TidHandle { match TID_ALLOCATOR.lock().alloc() {`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/vm/shm.rs:L3-L7`：关键词命中
    代码片段：`use config::mm::{MMAP_END, MMAP_START, PAGE_SIZE}; use mm::address::VirtAddr; use mutex::ShareMutex; use shm::SharedMemory; use systype::{`
  - `kernel/src/logging.rs:L1-L5`：关键词命中
    代码片段：`use core::{ fmt, sync::atomic::{AtomicBool, AtomicUsize, Ordering}, };`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/syscall/fs.rs:L241-L245`：关键词命中
    代码片段：`///   files such as stdout. When user calls 'printf' in user space without 'close(STDOUT)' + 'dup', the 'fd' is STDOUT ///   by default. /// - This is a 'async' syscall, which m...`
  - `kernel/src/syscall/mod.rs:L37-L41`：关键词命中
    代码片段：`use user::*; use crate::syscall::time::{sys_adjtimex, sys_clock_adjtime, sys_clock_getres, sys_clock_settime}; pub async fn syscall(syscall_no: usize, args: [usize; 6]) -> usize {`
- 与 2024 NPUcore-IMPACT!!! 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 NPUcore-IMPACT!!! 的语言构成不同：待测作品为 {'json': 20, 'build': 1026, 'text': 12254, 'markdown': 251, 'toml': 44, 'rust': 63275, 'asm': 508}，历史样本为 {'json': 20, 'build': 338, 'markdown': 743, 'rust': 177629, 'asm': 10}。（置信度：medium）

## 可能创新点

- 待测作品包含 267 个历史样本 2024 NPUcore-IMPACT!!! 中未见的独特函数/类型定义（如 `check_permission`, `read_through_page_cache`, `resolve_symlink_through`, `symlink`, `write_through_page_cache`, `base_link`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `lib/vfs/src/inode.rs:L240-L240`：fn check_permission
    代码片段：`fn check_permission(&self, euid: u32, egid: u32, groups: &[u32], access: AccessFlags) -> bool {`
  - `lib/vfs/src/file.rs:L325-L325`：fn read_through_page_cache
    代码片段：`async fn read_through_page_cache(&self, mut buf: &mut [u8], pos: usize) -> SysResult<usize> {`

## 附录：核验摘要

- 关键结论数：26
- 含证据关键结论数：26（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
