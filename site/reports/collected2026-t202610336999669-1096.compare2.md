# 2026 T202610336999669 比较报告

- 对比历史仓库：2022 代码掐架队
- 生成时间：2026-07-01T20:57:48.421129+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 代码掐架队（来源：赛事历史作品）：画像相似度 score=9.34；同属 rcore-variant 风格; 语言构成相似度 0.96; OS 维度重合度 1.00; 代码规模接近度 0.42

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 代码掐架队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/drivers/mod.rs:L1-L5`：关键词命中
    代码片段：`//! StellarOS 设备驱动模块（§六.B per-component 分层） //! //! - block/:  VirtIO-Blk 块设备驱动（MMIO/PCI 双 transport） //! - net/:    VirtIO-Net 网卡驱动 + NetDevice trait + NET_DEVICES 注册表 //! - vi...`
  - `os/src/drivers/irq/mod.rs:L5-L9`：关键词命中
    代码片段：`//! - loongarch64：EIOINTC/PCH-PIC 尚未接入，全部为 no-op； //!   运行期由 'arch::has_external_interrupts()=false' 保证不依赖外部中断 //!   （virtio-net 由 timer tick 的 poll_interfaces 驱动）。 #[cfg(target...`
  - `codes/os/src/console.rs:L13-L17`：关键词命中
    代码片段：`} static CONSOLE: Mutex<ConsoleInner> = Mutex::new(ConsoleInner {}); impl Write for Stdout {`
  - `codes/os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2022 代码掐架队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! 分层目录结构（2026-06-11 子文件夹化重整）： //!   - vfs/      VFS 核心：VfsNode trait（mod.rs）+ MountTable/MountedFs（mount.rs） //!               + xattr + record_lock（POSIX/OFD 记录锁） //!   -...`
  - `os/src/fs/fifo.rs:L16-L20`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicU32, Ordering}; use crate::fs::vfs::{NodeStat, VfsNode}; use crate::mm::UserBuffer; use crate::sync::SpinNoIrqLock;`
  - `codes/os/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`mod pipe; pub mod stdio; mod inode; mod mount; pub mod finfo;`
  - `codes/os/src/fs/finfo.rs:L63-L67`：关键词命中
    代码片段：`} pub fn new(name:&str, inode:usize, offset:isize, reclen: u16, d_type: u8)->Self{ let mut dirent = Self{ d_ino:inode,`
- 与 2022 代码掐架队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/drivers/irq/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! 共享驱动（block/net）只调用本模块的统一接口，不直接耦合具体控制器： //! - riscv64：PLIC（'plic.rs'）。 //! - loongarch64：EIOINTC/PCH-PIC 尚未接入，全部为 no-op； //!   运行期由 'arch::has_external_interrupts()=false...`
  - `os/src/drivers/irq/plic.rs:L1-L3`：关键词命中
    代码片段：`//! PLIC (Platform-Level Interrupt Controller) 驱动 — SMP 多核版本 //! //! QEMU virt 机器 PLIC 基地址：0x0c00_0000`
  - `codes/os/src/trap/mod.rs:L7-L11`：关键词命中
    代码片段：`use crate::syscall::{syscall, test}; use crate::task::*; use crate::timer::set_next_trigger; use riscv::register::{ mtvec::TrapMode,`
  - `codes/os/src/sbi.rs:L29-L33`：关键词命中
    代码片段：`} pub fn set_timer(timer: usize) { sbi_call(SBI_SET_TIMER, timer, 0, 0); }`
- 与 2022 代码掐架队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`#[cfg(feature = "asid")] pub mod asid; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/asid.rs:L3-L7`：关键词命中
    代码片段：`//! # 为什么需要 //! 本内核用 trampoline 模型：内核与用户是**独立页表**，故每次 trap(syscall/异常/中断) //! 进出都要切 'satp' 并 'sfence.vma' **全刷 TLB**（trap.S 入核 line76 / 返用户 line91）。 //! 全刷丢掉整个 TLB、后续大量重填，是 sysc...`
  - `codes/os/src/mm/mod.rs:L1-L3`：关键词命中
    代码片段：`mod heap_allocator; mod address; mod frame_allocator;`
  - `codes/os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{PageTable, PageTableEntry, PTEFlags}; use super::{VirtPageNum, VirtAddr, PhysPageNum, PhysAddr}; use super::{FrameTracker, frame_alloc, frame_add_ref, enquire_refcou...`
- 与 2022 代码掐架队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L25-L29`：关键词命中
    代码片段：`// 从 1 起:首个分配的任务(initproc)拿 tid/pid=1,对齐 Linux(init=pid 1; // pid 0 为概念上的 swapper/idle,本内核 idle 非 tid 化任务故 0 直接保留不分配)。 // 修正 getpgid01//proc/1/stat/sched_setscheduler01/setprior...`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! 任务管理模块 - Linux 风格统一 Task 模型 + 有栈协程调度 //! //! 架构：`
  - `codes/os/src/task/mod.rs:L6-L10`：关键词命中
    代码片段：`mod info; mod switch; mod task; mod resource; pub use resource::RLimit;`
  - `codes/os/src/task/info.rs:L87-L91`：关键词命中
    代码片段：`pub const RUSAGE_SELF:isize = 0; /* The calling process.  */ pub const RUSAGE_CHILDREN:isize = -1; /* All of its terminated child processes.  */ pub const RUSAGE_THREAD:isize =...`
- 与 2022 代码掐架队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/sync/mod.rs:L5-L9`：关键词命中
    代码片段：`//! - 'UPSafeCellRaw<T>' / 'UPIntrFreeCell<T>': 已废弃的单核原语（保留在 ['up'](os/src/sync/up.rs) 模块内兼容旧设计） mod spin_lock; pub mod up; pub mod wait_queue;`
  - `os/src/sync/spin_lock.rs:L7-L11`：关键词命中
    代码片段：`//! - **获取锁时**：先关闭本核 S-mode 中断（'sstatus.SIE'），再自旋等待 //! - **释放锁时**：先释放自旋锁，再恢复中断状态 //! - **嵌套安全**：使用 per-lock SIE 保存，支持嵌套获取不同的锁 //! - **SMP-ready**：底层使用 'spin::Mutex' 的原子自旋 //!`
  - `codes/os/src/main.rs:L79-L83`：关键词命中
    代码片段：`lazy_static! { static ref CORE2_FLAG: Arc<Mutex<Core2flag>> = Arc::new(Mutex::new(Core2flag { is_in: false })); }`
  - `codes/os/src/console.rs:L1-L5`：关键词命中
    代码片段：`use crate::sbi::console_putchar; use core::fmt::{self, Write}; use spin::Mutex; struct Stdout;`
- 与 2022 代码掐架队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/mm.rs:L1366-L1370`：关键词命中
    代码片段：`} /// sys_mseal(addr, len, flags) -> 0 / -errno（Linux 6.10，syscall 462，E-3） /// /// 内存封印：封印后该区间的 munmap/mprotect/mremap/mmap(MAP_FIXED 覆盖)/`
  - `os/src/syscall/nr.rs:L1-L3`：关键词命中
    代码片段：`//! Linux syscall numbers shared by the syscall dispatcher. //! //! RISC-V64 and LoongArch64 both use the Linux asm-generic syscall table for`
  - `codes/os/src/syscall/fs.rs:L961-L965`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: isize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { if dirfd == A...`
  - `codes/os/src/syscall/mod.rs:L86-L90`：关键词命中
    代码片段：`// println!("test: run sys_getppid 1000000 times, start {:?}",start); for _ in 0..1000000{ syscall(SYSCALL_GETPPID,[0,0,0,0,0,0]); // unsafe{ //     asm!(`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2022 代码掐架队 在“内存管理”维度发现片段级代码相似度 0.81 （token=0.71, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：activate, fn, kernel_space, pub。（置信度：high）
  证据：
  - `os/src/mm/mod.rs:L46-L50`：fn activate_kernel_space
    代码片段：`/// Boot hart 已完成 KERNEL_SPACE 初始化，secondary hart 只需写入 satp /// 以共享同一份内核页表。 pub fn activate_kernel_space() { KERNEL_SPACE.exclusive_access().activate(); }`
  - `codes/os/src/mm/mod.rs:L36-L40`：fn init_othercore
    代码片段：`} pub fn init_othercore(){ KERNEL_SPACE.lock().activate(); }`
- 与 2022 代码掐架队 在“调度与任务管理”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：alloc, fn, if, let, mut, pop, pub, recycled。（置信度：high）
  证据：
  - `os/src/task/id.rs:L31-L35`：fn alloc
    代码片段：`} } pub fn alloc(&mut self) -> usize { if let Some(id) = self.recycled.pop() { id`
  - `codes/os/src/task/id.rs:L21-L25`：fn alloc
    代码片段：`} } pub fn alloc(&mut self) -> usize { if let Some(id) = self.recycled.pop() { id`
- 与 2022 代码掐架队 在“内存管理”维度发现片段级代码相似度 0.71 （token=0.62, structure=0.86）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, heap_allocator, init, init_heap, pub。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L34-L38`：fn init
    代码片段：`}; pub fn init() { heap_allocator::init_heap(); slab::boot_selftest();`
  - `codes/os/src/mm/mod.rs:L29-L33`：fn init
    代码片段：`pub use heap_allocator::HEAP_ALLOCATOR; pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`
- 与 2022 代码掐架队 在“调度与任务管理”维度发现片段级代码相似度 0.68 （token=0.71, structure=0.62）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, impl, new, pub, recycleallocator, self。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L21-L25`：fn new
    代码片段：`impl RecycleAllocator { pub fn new() -> Self { Self { // 从 1 起:首个分配的任务(initproc)拿 tid/pid=1,对齐 Linux(init=pid 1;`
  - `codes/os/src/task/id.rs:L15-L19`：fn new
    代码片段：`impl RecycleAllocator { pub fn new() -> Self { RecycleAllocator { current: 0,`
- 文件路径重合：与 2022 代码掐架队 在“调度与任务管理”维度出现同名文件源码路径 `os/src/task/id.rs` / `codes/os/src/task/id.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L25-L29`：关键词命中
    代码片段：`// 从 1 起:首个分配的任务(initproc)拿 tid/pid=1,对齐 Linux(init=pid 1; // pid 0 为概念上的 swapper/idle,本内核 idle 非 tid 化任务故 0 直接保留不分配)。 // 修正 getpgid01//proc/1/stat/sched_setscheduler01/setprior...`
  - `codes/os/src/task/id.rs:L9-L13`：struct RecycleAllocator
    代码片段：`use spin::Mutex; pub struct RecycleAllocator { current: usize, recycled: Vec<usize>,`

## 相似点

- 与 2022 代码掐架队 同属 rcore-variant 风格。（置信度：medium）
- 与 2022 代码掐架队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/drivers/mod.rs:L1-L5`：关键词命中
    代码片段：`//! StellarOS 设备驱动模块（§六.B per-component 分层） //! //! - block/:  VirtIO-Blk 块设备驱动（MMIO/PCI 双 transport） //! - net/:    VirtIO-Net 网卡驱动 + NetDevice trait + NET_DEVICES 注册表 //! - vi...`
  - `os/src/drivers/irq/mod.rs:L5-L9`：关键词命中
    代码片段：`//! - loongarch64：EIOINTC/PCH-PIC 尚未接入，全部为 no-op； //!   运行期由 'arch::has_external_interrupts()=false' 保证不依赖外部中断 //!   （virtio-net 由 timer tick 的 poll_interfaces 驱动）。 #[cfg(target...`
- 与 2022 代码掐架队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! 分层目录结构（2026-06-11 子文件夹化重整）： //!   - vfs/      VFS 核心：VfsNode trait（mod.rs）+ MountTable/MountedFs（mount.rs） //!               + xattr + record_lock（POSIX/OFD 记录锁） //!   -...`
  - `os/src/fs/fifo.rs:L16-L20`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicU32, Ordering}; use crate::fs::vfs::{NodeStat, VfsNode}; use crate::mm::UserBuffer; use crate::sync::SpinNoIrqLock;`
- 与 2022 代码掐架队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/drivers/irq/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! 共享驱动（block/net）只调用本模块的统一接口，不直接耦合具体控制器： //! - riscv64：PLIC（'plic.rs'）。 //! - loongarch64：EIOINTC/PCH-PIC 尚未接入，全部为 no-op； //!   运行期由 'arch::has_external_interrupts()=false...`
  - `os/src/drivers/irq/plic.rs:L1-L3`：关键词命中
    代码片段：`//! PLIC (Platform-Level Interrupt Controller) 驱动 — SMP 多核版本 //! //! QEMU virt 机器 PLIC 基地址：0x0c00_0000`
- 与 2022 代码掐架队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`#[cfg(feature = "asid")] pub mod asid; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/asid.rs:L3-L7`：关键词命中
    代码片段：`//! # 为什么需要 //! 本内核用 trampoline 模型：内核与用户是**独立页表**，故每次 trap(syscall/异常/中断) //! 进出都要切 'satp' 并 'sfence.vma' **全刷 TLB**（trap.S 入核 line76 / 返用户 line91）。 //! 全刷丢掉整个 TLB、后续大量重填，是 sysc...`
- 与 2022 代码掐架队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L25-L29`：关键词命中
    代码片段：`// 从 1 起:首个分配的任务(initproc)拿 tid/pid=1,对齐 Linux(init=pid 1; // pid 0 为概念上的 swapper/idle,本内核 idle 非 tid 化任务故 0 直接保留不分配)。 // 修正 getpgid01//proc/1/stat/sched_setscheduler01/setprior...`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! 任务管理模块 - Linux 风格统一 Task 模型 + 有栈协程调度 //! //! 架构：`
- 与 2022 代码掐架队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/sync/mod.rs:L5-L9`：关键词命中
    代码片段：`//! - 'UPSafeCellRaw<T>' / 'UPIntrFreeCell<T>': 已废弃的单核原语（保留在 ['up'](os/src/sync/up.rs) 模块内兼容旧设计） mod spin_lock; pub mod up; pub mod wait_queue;`
  - `os/src/sync/spin_lock.rs:L7-L11`：关键词命中
    代码片段：`//! - **获取锁时**：先关闭本核 S-mode 中断（'sstatus.SIE'），再自旋等待 //! - **释放锁时**：先释放自旋锁，再恢复中断状态 //! - **嵌套安全**：使用 per-lock SIE 保存，支持嵌套获取不同的锁 //! - **SMP-ready**：底层使用 'spin::Mutex' 的原子自旋 //!`
- 与 2022 代码掐架队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/mm.rs:L1366-L1370`：关键词命中
    代码片段：`} /// sys_mseal(addr, len, flags) -> 0 / -errno（Linux 6.10，syscall 462，E-3） /// /// 内存封印：封印后该区间的 munmap/mprotect/mremap/mmap(MAP_FIXED 覆盖)/`
  - `os/src/syscall/nr.rs:L1-L3`：关键词命中
    代码片段：`//! Linux syscall numbers shared by the syscall dispatcher. //! //! RISC-V64 and LoongArch64 both use the Linux asm-generic syscall table for`
- 与 2022 代码掐架队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 代码掐架队 的语言构成不同：待测作品为 {'build': 6783, 'markdown': 14760, 'toml': 168, 'json': 272, 'text': 313, 'rust': 638842, 'asm': 4519, 'c': 6328, 'yaml': 28}，历史样本为 {'json': 62, 'build': 2072, 'markdown': 7242, 'asm': 658, 'text': 1942, 'rust': 152844, 'c': 2166, 'toml': 15, 'yaml': 79}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：19
- 含证据关键结论数：19（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
