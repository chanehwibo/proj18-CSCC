# 2026 T2026104239911569 比较报告

- 对比历史仓库：2025 Chronix
- 生成时间：2026-07-02T05:00:54.978072+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 Chronix（来源：赛事历史作品）：画像相似度 score=9.20；同属 ucore-variant 风格; 语言构成相似度 0.81; OS 维度重合度 1.00; 代码规模接近度 0.59

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 Chronix 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/io/uart.rs:L1-L5`：关键词命中
    代码片段：`use crate::arch; pub struct Uart { base: usize, }`
  - `kernel/src/io/console.rs:L5-L9`：关键词命中
    代码片段：`static CONSOLE_LOCK: SpinLock = SpinLock::new(); struct Console; impl Write for Console {`
  - `os/src/drivers/mod.rs:L2-L5`：关键词命中
    代码片段：`pub mod dma; pub mod net; pub mod serial; pub use block::BLOCK_DEVICE;`
  - `os/src/drivers/net/mod.rs:L1-L4`：关键词命中
    代码片段：`#[allow(unused)] pub mod virtio_net; pub mod loopback; use core::{mem, ptr::NonNull};`
- 与 2025 Chronix 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L3-L7`：关键词命中
    代码片段：`pub mod dirent; pub mod file; pub mod inode; pub mod path; pub mod common_file;`
  - `kernel/src/fs/path.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use super::inode::InodeRef; pub fn normalize_path(cwd: &str, path: &str) -> Option<String> {`
  - `os/src/fs/fs.rs:L4-L8`：关键词命中
    代码片段：`use alloc::sync::{Arc, Weak}; use crate::{fs::{tmpfs::{dentry::TmpDentry, inode::{InodeContent, TmpInode, TmpSysInode}}, vfs::{dentry::{global_find_dentry, global_update_dentry,...`
  - `os/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`//! file system module: offer the file system interface //! define the file trait //! impl File for OSInode in 'inode.rs' //! impl Stdin and Stdout in 'stdio.rs' #![allow(missin...`
- 与 2025 Chronix 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/timer/mod.rs:L1-L3`：关键词命中
    代码片段：`// kernel/src/timer/mod.rs #[cfg(target_arch = "riscv64")]`
  - `kernel/src/timer/riscv64.rs:L11-L15`：关键词命中
    代码片段：`/* * 每多少次 timer interrupt 调度一次。 * 调试阶段可以设成 1，方便看到抢占。 */`
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality //! //! For rCore, we have a single trap entry point, namely '__alltraps'. At`
  - `os/src/timer/ffi.rs:L1-L4`：关键词命中
    代码片段：`use alloc::collections::btree_map::Values; use crate::timer::get_current_time_ns; use super::{get_current_time_ms, NSEC_PER_SEC};`
- 与 2025 Chronix 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`pub mod address; pub mod config; pub mod frame_allocator; pub mod heap; pub mod page_table;`
  - `kernel/src/mm/heap.rs:L7-L11`：关键词命中
    代码片段：`#[global_allocator] static HEAP_ALLOCATOR: LockedHeap<32> = LockedHeap::<32>::new(); static KERNEL_HEAP_START: AtomicUsize = AtomicUsize::new(0);`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `os/src/mm/user.rs:L2-L6`：关键词命中
    代码片段：`use alloc::sync::Arc; use hal::{addr::{VirtAddr, VirtAddrHal}, constant::{Constant, ConstantsHal}, pagetable::MapPerm, println}; use crate::{mm::vm::UserVmPagesLocker, processor...`
- 与 2025 Chronix 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`mod processor; mod switch; mod thread; mod process; mod manager_wrapper;`
  - `kernel/src/task/thread.rs:L50-L54`：关键词命中
    代码片段：`pub tickets: usize, pub stride: usize, pub pass: usize,`
  - `os/src/task/fs.rs:L1-L3`：关键词命中
    代码片段：`//! file system support for Task use alloc::{sync::Arc, vec::Vec};`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Task management implementation //! //! Everything about task management, like starting and switching tasks is`
- 与 2025 Chronix 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/sync/spin.rs:L4-L8`：关键词命中
    代码片段：`/// 简单的自旋锁 pub struct Mutex<T> { locked: core::sync::atomic::AtomicBool, data: UnsafeCell<T>,`
  - `kernel/src/sync/sync.rs:L2-L6`：关键词命中
    代码片段：`// 最简单有效的自旋锁，保护 UART 等共享资源 use core::sync::atomic::{AtomicBool, Ordering}; pub struct SpinLock {`
  - `os/src/sync/up.rs:L2-L6`：关键词命中
    代码片段：`use core::cell::{UnsafeCell, RefMut,RefCell}; use core::sync::atomic::{AtomicBool, Ordering}; use core::ops::{Deref, DerefMut}; use log::info;`
  - `os/src/sync/mod.rs:L6-L10`：关键词命中
    代码片段：`extern crate alloc; /// safe cell type for uniprocessor systems pub mod mutex; pub mod lazy;`
- 与 2025 Chronix 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/syscall/mod.rs:L46-L50`：关键词命中
    代码片段：`pub fn syscall(id: usize, args: [usize; 6]) -> isize { match id { SYSCALL_EXIT => process::sys_exit(args[0] as i32),`
  - `kernel/src/trap/riscv64/mod.rs:L89-L93`：关键词命中
    代码片段：`let syscall_id = cx.x[17]; let args = [cx.x[10], cx.x[11], cx.x[12],cx.x[13], cx.x[14], cx.x[15]]; cx.x[10] = handle_syscall(syscall_id, args) as usize; }`
  - `os/src/syscall/fd.rs:L1-L5`：关键词命中
    代码片段：`use alloc::sync::Arc; use crate::{fs::{tmpfs::{dentry::TmpDentry, inode::{EmptyFile, TmpSysInode}}, vfs::inode::InodeMode, OpenFlags}, syscall::{SysError, SysResult, SyscallId},...`
  - `os/src/syscall/fs.rs:L17-L21`：关键词命中
    代码片段：`use crate::processor::processor::{current_processor,current_task,current_user_token}; /// syscall: write pub async fn sys_write(fd: usize, buf: usize, len: usize) -> SysResult {...`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 Chronix 在“同步机制”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：exclusive_access, fn, get, inner, mut, pub, self, t。（置信度：high）
  证据：
  - `kernel/src/sync/up.rs:L14-L18`：fn exclusive_access
    代码片段：`} pub fn exclusive_access(&self) -> &mut T { unsafe { &mut *self.inner.get() } }`
  - `os/src/sync/up.rs:L35-L39`：fn exclusive_access
    代码片段：`} /// Panic if the data has been borrowed. pub fn exclusive_access(&self) -> &mut T { unsafe { &mut *self.inner.get()`
- 函数/符号名重合：与 2025 Chronix 在“设备驱动”维度发现 4 个同名定义：new, block_size, read_block, write_block。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/block/blockio.rs:L14-L14`：fn new
    代码片段：`pub fn new(dev: Arc<dyn BlockDevice>, num_sectors: u64) -> Self {`
  - `os/src/devices/buffer_cache.rs:L18-L18`：fn new
    代码片段：`pub fn new() -> Self {`
  - `kernel/src/block/device.rs:L2-L2`：fn block_size
    代码片段：`fn block_size(&self) -> usize;`
  - `os/src/devices/mod.rs:L151-L151`：fn block_size
    代码片段：`fn block_size(&self) -> usize;`
- 函数/符号名重合：与 2025 Chronix 在“文件系统”维度发现 4 个同名定义：len, readable, writable, read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/fs/common_file.rs:L28-L28`：fn len
    代码片段：`fn len(&self) -> usize {`
  - `os/src/fs/utils.rs:L40-L40`：fn len
    代码片段：`fn len(&self) -> usize {`
  - `kernel/src/fs/common_file.rs:L55-L55`：fn readable
    代码片段：`fn readable(&self) -> bool {`
  - `os/src/fs/mod.rs:L274-L274`：fn readable
    代码片段：`pub fn readable(&self) -> bool {`
- 函数/符号名重合：与 2025 Chronix 在“同步机制”维度发现 4 个同名定义：new, read, write, block_size。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/block/blockio.rs:L14-L14`：fn new
    代码片段：`pub fn new(dev: Arc<dyn BlockDevice>, num_sectors: u64) -> Self {`
  - `os/src/fs/pipe.rs:L49-L49`：fn new
    代码片段：`fn new(pipe: Arc<SpinNoIrqLock<Pipe>>, is_reader: bool) -> Arc<Self> {`
  - `kernel/src/block/blockio.rs:L28-L28`：fn read
    代码片段：`fn read(&mut self, buf: &mut [u8]) -> Result<usize, ()> {`
  - `os/src/devices/mod.rs:L197-L197`：fn read
    代码片段：`async fn read(&self, buf: &mut [u8]) -> usize;`
- 与 2025 Chronix 在“同步机制”维度发现片段级代码相似度 0.68 （token=0.56, structure=0.89）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：cell, core, pub, struct, t, unsafecell, use。（置信度：medium）
  证据：
  - `kernel/src/sync/up.rs:L1-L5`：struct UPSafeCell
    代码片段：`use core::cell::UnsafeCell; pub struct UPSafeCell<T> { inner: UnsafeCell<T>, }`
  - `os/src/sync/lazy.rs:L1-L3`：关键词命中
    代码片段：`use core::{cell::UnsafeCell, ops::{Deref, DerefMut}, sync::atomic::{AtomicBool, Ordering}}; pub struct SyncLazyCell<T> {`
- 结构体/类型重合：与 2025 Chronix 在“文件系统”维度发现 3 个同名定义：Pipe, Stdin, Stdout。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/fs/pipe.rs:L9-L9`：struct Pipe
    代码片段：`pub struct Pipe {`
  - `os/src/fs/pipe.rs:L18-L18`：struct Pipe
    代码片段：`pub struct Pipe {`
  - `kernel/src/fs/stdio.rs:L5-L5`：struct Stdin
    代码片段：`pub struct Stdin;`
  - `os/src/fs/stdio.rs:L11-L11`：struct Stdin
    代码片段：`pub struct Stdin;`
- 函数/符号名重合：与 2025 Chronix 在“内存管理”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/fs/common_file.rs:L97-L97`：fn new
    代码片段：`pub fn new(entries: Vec<DirEntry>) -> Self {`
  - `user/src/bin/user_shell.rs:L30-L30`：fn new
    代码片段：`pub fn new(command: &str) -> Self {`
- 结构体/类型重合：与 2025 Chronix 在“设备驱动”维度发现 1 个同名定义：BlockDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/block/device.rs:L1-L1`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync {`
  - `os/src/devices/mod.rs:L146-L146`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync + Any {`
- 结构体/类型重合：与 2025 Chronix 在“同步机制”维度发现 1 个同名定义：BlockDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/block/device.rs:L1-L1`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync {`
  - `os/src/devices/mod.rs:L146-L146`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync + Any {`
- 文件路径重合：与 2025 Chronix 在“同步机制”维度出现同名文件源码路径 `kernel/src/sync/up.rs` / `os/src/sync/up.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/sync/up.rs:L1-L5`：struct UPSafeCell
    代码片段：`use core::cell::UnsafeCell; pub struct UPSafeCell<T> { inner: UnsafeCell<T>, }`
  - `os/src/sync/up.rs:L2-L6`：关键词命中
    代码片段：`use core::cell::{UnsafeCell, RefMut,RefCell}; use core::sync::atomic::{AtomicBool, Ordering}; use core::ops::{Deref, DerefMut}; use log::info;`
- 文件路径重合：与 2025 Chronix 在“系统调用”维度出现同名文件源码路径 `kernel/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/syscall/fs.rs:L3-L7`：fn sys_read
    代码片段：`use crate::fs; pub fn sys_read(fd: usize, user_buf: usize, len: usize) -> isize { if len == 0 { return 0;`
  - `os/src/syscall/fs.rs:L17-L21`：关键词命中
    代码片段：`use crate::processor::processor::{current_processor,current_task,current_user_token}; /// syscall: write pub async fn sys_write(fd: usize, buf: usize, len: usize) -> SysResult {...`

## 相似点

- 与 2025 Chronix 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 Chronix 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/io/uart.rs:L1-L5`：关键词命中
    代码片段：`use crate::arch; pub struct Uart { base: usize, }`
  - `kernel/src/io/console.rs:L5-L9`：关键词命中
    代码片段：`static CONSOLE_LOCK: SpinLock = SpinLock::new(); struct Console; impl Write for Console {`
- 与 2025 Chronix 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L3-L7`：关键词命中
    代码片段：`pub mod dirent; pub mod file; pub mod inode; pub mod path; pub mod common_file;`
  - `kernel/src/fs/path.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use super::inode::InodeRef; pub fn normalize_path(cwd: &str, path: &str) -> Option<String> {`
- 与 2025 Chronix 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/timer/mod.rs:L1-L3`：关键词命中
    代码片段：`// kernel/src/timer/mod.rs #[cfg(target_arch = "riscv64")]`
  - `kernel/src/timer/riscv64.rs:L11-L15`：关键词命中
    代码片段：`/* * 每多少次 timer interrupt 调度一次。 * 调试阶段可以设成 1，方便看到抢占。 */`
- 与 2025 Chronix 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`pub mod address; pub mod config; pub mod frame_allocator; pub mod heap; pub mod page_table;`
  - `kernel/src/mm/heap.rs:L7-L11`：关键词命中
    代码片段：`#[global_allocator] static HEAP_ALLOCATOR: LockedHeap<32> = LockedHeap::<32>::new(); static KERNEL_HEAP_START: AtomicUsize = AtomicUsize::new(0);`
- 与 2025 Chronix 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`mod processor; mod switch; mod thread; mod process; mod manager_wrapper;`
  - `kernel/src/task/thread.rs:L50-L54`：关键词命中
    代码片段：`pub tickets: usize, pub stride: usize, pub pass: usize,`
- 与 2025 Chronix 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/sync/spin.rs:L4-L8`：关键词命中
    代码片段：`/// 简单的自旋锁 pub struct Mutex<T> { locked: core::sync::atomic::AtomicBool, data: UnsafeCell<T>,`
  - `kernel/src/sync/sync.rs:L2-L6`：关键词命中
    代码片段：`// 最简单有效的自旋锁，保护 UART 等共享资源 use core::sync::atomic::{AtomicBool, Ordering}; pub struct SpinLock {`
- 与 2025 Chronix 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/syscall/mod.rs:L46-L50`：关键词命中
    代码片段：`pub fn syscall(id: usize, args: [usize; 6]) -> isize { match id { SYSCALL_EXIT => process::sys_exit(args[0] as i32),`
  - `kernel/src/trap/riscv64/mod.rs:L89-L93`：关键词命中
    代码片段：`let syscall_id = cx.x[17]; let args = [cx.x[10], cx.x[11], cx.x[12],cx.x[13], cx.x[14], cx.x[15]]; cx.x[10] = handle_syscall(syscall_id, args) as usize; }`
- 与 2025 Chronix 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 Chronix 的语言构成不同：待测作品为 {'toml': 86, 'build': 1255, 'markdown': 6949, 'asm': 2289, 'rust': 71100, 'text': 1226, 'c': 12383, 'json': 70, 'yaml': 34}，历史样本为 {'json': 20, 'build': 353, 'markdown': 2055, 'toml': 139, 'make': 405, 'rust': 43626, 'asm': 381}。（置信度：medium）

## 可能创新点

- 待测作品包含 215 个历史样本 2025 Chronix 中未见的独特函数/类型定义（如 `discover_disks`, `make_region`, `init_global_from_phys_base`, `load`, `parse_virtio_pci_caps`, `used_ring_ptr`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/src/block/discover_disks.rs:L6-L6`：fn discover_disks
    代码片段：`pub fn discover_disks() -> (Option<Arc<dyn BlockDevice>>, Option<Arc<dyn BlockDevice>>) {`
  - `kernel/src/block/virtio_pci.rs:L48-L48`：fn make_region
    代码片段：`fn make_region(addr: PciAddress, bar: u8, offset: u32, length: u32) -> VirtioPciRegion {`

## 附录：核验摘要

- 关键结论数：26
- 含证据关键结论数：26（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
