# 2026 T2026104869910402 比较报告

- 对比历史仓库：2024 刘奶奶找牛奶奶买牛奶队
- 生成时间：2026-07-02T09:02:31.825938+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 刘奶奶找牛奶奶买牛奶队（来源：赛事历史作品）：画像相似度 score=8.98；同属 rcore-variant 风格; 语言构成相似度 0.71; OS 维度重合度 1.00; 代码规模接近度 0.57

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 刘奶奶找牛奶奶买牛奶队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
  - `os/src/drivers/bus/mod.rs:L1-L1`：关键词命中
    代码片段：`pub mod virtio;`
  - `os/src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/block/mod.rs:L1-L3`：关键词命中
    代码片段：`mod virtio_blk; mod block_device;`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L13-L17`：关键词命中
    代码片段：`pub use fdtable::FdTable; pub use filesystem::vfs::{chdir, dcache_children, dentry_cache_cap, dentry_cache_hits, dentry_cache_len, dentry_cache_misses, get_cwd, get_root_dentry,...`
  - `os/src/fs/fdtable.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use filesystem::vfs::File as VfsFile; use filesystem::vfs_defs::{OpenFlags, VfsError, VfsRet};`
  - `os/src/fs/mod.rs:L1-L4`：关键词命中
    代码片段：`#![allow(unused)] mod inode; mod pipe; mod stdio;`
  - `os/src/fatfs/fs.rs:L76-L80`：关键词命中
    代码片段：`let cluster = { let mut fat = self.fat_slice(); match table_alloc_cluster(&mut fat, prev_cluster, hint, self.total_clusters) { Ok(x) => x,`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/timer.rs:L8-L12`：关键词命中
    代码片段：`use alloc::sync::Arc; use lazy_static::*; use polyhal::timer::{current_time, set_next_timer}; const TICKS_PER_SEC: usize = 100;`
  - `os/src/trap/mod.rs:L11-L15`：关键词命中
    代码片段：`SigInfo, SIGBUS, SIGSEGV, SIGILL, SIGTRAP, BUS_ADRERR, SEGV_MAPERR, ILL_ILLOPC, TRAP_BRKPT, }; use crate::timer::{check_timer, set_next_trigger}; use crate::config::PAGE_SIZE; u...`
  - `os/src/timer.rs:L81-L85`：关键词命中
    代码片段：`let current_ms = get_time_ms(); let mut timers = TIMERS.exclusive_access(); while let Some(timer) = timers.peek() { if timer.expire_ms <= current_ms { wakeup_task(Arc::clone(&ti...`
  - `os/src/trap/mod.rs:L7-L11`：关键词命中
    代码片段：`current_user_token, exit_current_and_run_next, suspend_current_and_run_next, SignalFlags, }; use crate::timer::{check_timer, set_next_trigger}; use core::arch::{asm, global_asm}...`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/elf.rs:L74-L78`：关键词命中
    代码片段：`/// '"/lib/ld-linux-riscv64-lp64d.so.1"'。 pub interp_path: Option<String>, /// 所有 PT_LOAD 段，按文件顺序排列。供 'MemorySet::from_elf()' 消费。 pub segments: Vec<ElfSegment>, }`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`mod address; mod elf; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{frame_alloc, FrameTracker}; use super::{PTEFlags, PageTable, PageTableEntry}; use super::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum};`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L5-L9`：关键词命中
    代码片段：`}; use crate::mm::{KERNEL_SPACE, PhysPageNum, VirtAddr}; use crate::task::boot_page_table; use polyhal::{MappingFlags, MappingSize::Page4KB}; use crate::sync::UPIntrFreeCell;`
  - `os/src/task/mod.rs:L13-L17`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
  - `os/src/task/mod.rs:L7-L11`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
  - `os/src/task/switch.S:L13-L17`：关键词命中
    代码片段：`#     next_task_cx_ptr: *const TaskContext # ) # save kernel stack of current task sd sp, 8(a0) # save ra & s0~s11 of current execution`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/sync/mod.rs:L8-L12`：关键词命中
    代码片段：`pub use ksync::{ Condvar, Mutex, MutexBlocking, MutexSpin, Semaphore, UPIntrFreeCell, UPIntrRefMut, UPSafeCellRaw, };`
  - `os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`// use alloc::{collections::VecDeque, sync::Arc}; // pub trait Mutex: Sync + Send { //     fn lock(&self); //     fn unlock(&self);`
  - `os/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`mod condvar; mod mutex; mod semaphore;`
  - `os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub trait Mutex: Sync + Send { fn lock(&self); fn unlock(&self);`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L19-L23`：关键词命中
    代码片段：`/// unlinkat flags: 允许删除目录项指向的是目录（否则返回 EPERM）。 const AT_REMOVEDIR: u32 = 0x200; /// 路径型 syscall flags: 不跟随最终分量的符号链接。 const AT_SYMLINK_NOFOLLOW: usize = 0x100; /// 路径型 syscall fl...`
  - `os/src/syscall/mod.rs:L194-L198`：关键词命中
    代码片段：`use polyhal_trap::trapframe::TrapFrameArgs; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> SyscallRet { let tid = crate::task::current_task() .and_then(|t| {`
  - `os/src/syscall/mod.rs:L48-L52`：关键词命中
    代码片段：`use sync::*; use thread::*; use crate::syscall::sys_info::sys_uname; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> isize {`
  - `os/vendor/sbi-rt/src/binary.rs:L8-L12`：关键词命中
    代码片段：`unsafe { core::arch::asm!( "ecall", in("a7") eid, in("a6") fid,`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 刘奶奶找牛奶奶买牛奶队 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：core, fmt, for, impl, self, stdout, struct, use。（置信度：high）
  证据：
  - `os/src/console.rs:L3-L7`：struct Stdout
    代码片段：`use core::fmt::{self, Write}; struct Stdout; impl Write for Stdout {`
  - `os/src/console.rs:L2-L6`：struct Stdout
    代码片段：`use core::fmt::{self, Write}; struct Stdout; impl Write for Stdout {`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：args, arguments, fmt, fn, print, pub, stdout, unwrap。（置信度：high）
  证据：
  - `os/src/console.rs:L14-L18`：fn print
    代码片段：`} pub fn print(args: fmt::Arguments) { Stdout.write_fmt(args).unwrap(); }`
  - `os/src/console.rs:L13-L17`：fn print
    代码片段：`} pub fn print(args: fmt::Arguments) { Stdout.write_fmt(args).unwrap(); }`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“调度与任务管理”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：current, lazy_static, pub, recycleallocator, recycled, struct, use, usize。（置信度：high）
  证据：
  - `os/src/task/id.rs:L14-L18`：struct RecycleAllocator
    代码片段：`use lazy_static::*; pub struct RecycleAllocator { current: usize, recycled: Vec<usize>,`
  - `os/src/task/id.rs:L9-L13`：struct RecycleAllocator
    代码片段：`use lazy_static::*; pub struct RecycleAllocator { current: usize, recycled: Vec<usize>,`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“调度与任务管理”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：allow, clippy, mod, module_inception, self, switch, task, taskuserres。（置信度：high）
  证据：
  - `os/src/task/mod.rs:L13-L17`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
  - `os/src/task/mod.rs:L7-L11`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“系统调用”维度发现片段级代码相似度 0.59 （token=0.50, structure=0.75）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：args, crate, fn, pub, syscall, syscall_id, use, usize。（置信度：medium）
  证据：
  - `os/src/syscall/mod.rs:L194-L198`：关键词命中
    代码片段：`use polyhal_trap::trapframe::TrapFrameArgs; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> SyscallRet { let tid = crate::task::current_task() .and_then(|t| {`
  - `os/src/syscall/mod.rs:L48-L52`：关键词命中
    代码片段：`use sync::*; use thread::*; use crate::syscall::sys_info::sys_uname; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> isize {`
- 函数/符号名重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“内存管理”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `filesystem/ext4_rs/src/ext4_impls/balloc.rs:L16-L16`：fn new
    代码片段：`fn new(bitmap: &[u8], free_blocks: u64) -> Self {`
  - `user/src/bin/user_shell.rs:L30-L30`：fn new
    代码片段：`pub fn new(command: &str) -> Self {`
- 函数/符号名重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“同步机制”维度发现 2 个同名定义：block, new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `filesystem/ext4_rs/src/ext4_defs/inode.rs:L176-L176`：fn block
    代码片段：`pub fn block(&self) -> [u32; 15] {`
  - `user/src/bin/barrier_condvar.rs:L32-L32`：fn block
    代码片段：`pub fn block(&self) {`
  - `filesystem/ext4_rs/src/ext4_impls/balloc.rs:L16-L16`：fn new
    代码片段：`fn new(bitmap: &[u8], free_blocks: u64) -> Self {`
  - `user/src/bin/barrier_condvar.rs:L25-L25`：fn new
    代码片段：`pub fn new() -> Self {`
- 文件路径重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“设备驱动”维度出现同路径源码路径 `os/src/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
  - `os/src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
- 文件路径重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“中断与异常”维度出现同路径源码路径 `os/src/timer.rs` / `os/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/timer.rs:L8-L12`：关键词命中
    代码片段：`use alloc::sync::Arc; use lazy_static::*; use polyhal::timer::{current_time, set_next_timer}; const TICKS_PER_SEC: usize = 100;`
  - `os/src/timer.rs:L81-L85`：关键词命中
    代码片段：`let current_ms = get_time_ms(); let mut timers = TIMERS.exclusive_access(); while let Some(timer) = timers.peek() { if timer.expire_ms <= current_ms { wakeup_task(Arc::clone(&ti...`
- 文件路径重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“调度与任务管理”维度出现同路径源码路径 `os/src/task/id.rs` / `os/src/task/id.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/task/id.rs:L5-L9`：关键词命中
    代码片段：`}; use crate::mm::{KERNEL_SPACE, PhysPageNum, VirtAddr}; use crate::task::boot_page_table; use polyhal::{MappingFlags, MappingSize::Page4KB}; use crate::sync::UPIntrFreeCell;`
  - `os/src/task/id.rs:L9-L13`：struct RecycleAllocator
    代码片段：`use lazy_static::*; pub struct RecycleAllocator { current: usize, recycled: Vec<usize>,`
- 文件路径重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“同步机制”维度出现同路径源码路径 `os/src/sync/mutex.rs` / `os/src/sync/mutex.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`// use alloc::{collections::VecDeque, sync::Arc}; // pub trait Mutex: Sync + Send { //     fn lock(&self); //     fn unlock(&self);`
  - `os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub trait Mutex: Sync + Send { fn lock(&self); fn unlock(&self);`
- 文件路径重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“系统调用”维度出现同路径源码路径 `os/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/syscall/fs.rs:L19-L23`：关键词命中
    代码片段：`/// unlinkat flags: 允许删除目录项指向的是目录（否则返回 EPERM）。 const AT_REMOVEDIR: u32 = 0x200; /// 路径型 syscall flags: 不跟随最终分量的符号链接。 const AT_SYMLINK_NOFOLLOW: usize = 0x100; /// 路径型 syscall fl...`
  - `os/src/syscall/fs.rs:L6-L10`：fn sys_write
    代码片段：`use core::mem::size_of; pub fn sys_write(fd: usize, buf: *const u8, len: usize) -> isize { let token = current_user_token(); let process = current_process();`
- 文件路径重合：与 2024 刘奶奶找牛奶奶买牛奶队 在“文件系统”维度出现同名文件源码路径 `os/src/syscall/fs.rs` / `os/src/fatfs/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L6-L10`：关键词命中
    代码片段：`use super::stub::{sys_getegid, sys_geteuid, sys_getgid, sys_getuid}; use crate::fs::{self, InodeMode, Kstat, OpenFlags, SeekFlags, Statfs, VfsError, VfsFile}; use crate::fs::Den...`
  - `os/src/fatfs/fs.rs:L76-L80`：关键词命中
    代码片段：`let cluster = { let mut fat = self.fat_slice(); match table_alloc_cluster(&mut fat, prev_cluster, hint, self.total_clusters) { Ok(x) => x,`

## 相似点

- 与 2024 刘奶奶找牛奶奶买牛奶队 同属 rcore-variant 风格。（置信度：medium）
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
  - `os/src/drivers/bus/mod.rs:L1-L1`：关键词命中
    代码片段：`pub mod virtio;`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L13-L17`：关键词命中
    代码片段：`pub use fdtable::FdTable; pub use filesystem::vfs::{chdir, dcache_children, dentry_cache_cap, dentry_cache_hits, dentry_cache_len, dentry_cache_misses, get_cwd, get_root_dentry,...`
  - `os/src/fs/fdtable.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use filesystem::vfs::File as VfsFile; use filesystem::vfs_defs::{OpenFlags, VfsError, VfsRet};`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/timer.rs:L8-L12`：关键词命中
    代码片段：`use alloc::sync::Arc; use lazy_static::*; use polyhal::timer::{current_time, set_next_timer}; const TICKS_PER_SEC: usize = 100;`
  - `os/src/trap/mod.rs:L11-L15`：关键词命中
    代码片段：`SigInfo, SIGBUS, SIGSEGV, SIGILL, SIGTRAP, BUS_ADRERR, SEGV_MAPERR, ILL_ILLOPC, TRAP_BRKPT, }; use crate::timer::{check_timer, set_next_trigger}; use crate::config::PAGE_SIZE; u...`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/elf.rs:L74-L78`：关键词命中
    代码片段：`/// '"/lib/ld-linux-riscv64-lp64d.so.1"'。 pub interp_path: Option<String>, /// 所有 PT_LOAD 段，按文件顺序排列。供 'MemorySet::from_elf()' 消费。 pub segments: Vec<ElfSegment>, }`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`mod address; mod elf; mod frame_allocator; mod heap_allocator; mod memory_set;`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L5-L9`：关键词命中
    代码片段：`}; use crate::mm::{KERNEL_SPACE, PhysPageNum, VirtAddr}; use crate::task::boot_page_table; use polyhal::{MappingFlags, MappingSize::Page4KB}; use crate::sync::UPIntrFreeCell;`
  - `os/src/task/mod.rs:L13-L17`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/sync/mod.rs:L8-L12`：关键词命中
    代码片段：`pub use ksync::{ Condvar, Mutex, MutexBlocking, MutexSpin, Semaphore, UPIntrFreeCell, UPIntrRefMut, UPSafeCellRaw, };`
  - `os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`// use alloc::{collections::VecDeque, sync::Arc}; // pub trait Mutex: Sync + Send { //     fn lock(&self); //     fn unlock(&self);`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L19-L23`：关键词命中
    代码片段：`/// unlinkat flags: 允许删除目录项指向的是目录（否则返回 EPERM）。 const AT_REMOVEDIR: u32 = 0x200; /// 路径型 syscall flags: 不跟随最终分量的符号链接。 const AT_SYMLINK_NOFOLLOW: usize = 0x100; /// 路径型 syscall fl...`
  - `os/src/syscall/mod.rs:L194-L198`：关键词命中
    代码片段：`use polyhal_trap::trapframe::TrapFrameArgs; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> SyscallRet { let tid = crate::task::current_task() .and_then(|t| {`
- 与 2024 刘奶奶找牛奶奶买牛奶队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 刘奶奶找牛奶奶买牛奶队 的语言构成不同：待测作品为 {'json': 281998, 'build': 9736, 'markdown': 25849, 'toml': 158, 'rust': 644628, 'text': 277, 'asm': 436, 'yaml': 260}，历史样本为 {'json': 80, 'build': 4070, 'markdown': 14544, 'text': 6192, 'rust': 390243, 'asm': 845, 'toml': 9587, 'c': 27863, 'yaml': 115, 'make': 664}。（置信度：medium）

## 可能创新点

- 待测作品包含 376 个历史样本 2024 刘奶奶找牛奶奶买牛奶队 中未见的独特函数/类型定义（如 `set_extent_block_checksum`, `fuse_mkdir`, `fuse_rename`, `get_pblock_idx`, `balloc_free_blocks`, `write_entry`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `filesystem/ext4_rs/src/ext4_impls/extents.rs:L1289-L1289`：fn set_extent_block_checksum
    代码片段：`fn set_extent_block_checksum(&self, inode_ref: &Ext4InodeRef, block_addr: usize) -> Result<()> {`
  - `filesystem/ext4_rs/src/fuse_interface/mod.rs:L163-L163`：fn fuse_mkdir
    代码片段：`pub fn fuse_mkdir(&mut self, parent: u64, name: &str, mode: u32, umask: u32) -> Result<usize> {`

## 附录：核验摘要

- 关键结论数：28
- 含证据关键结论数：28（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
