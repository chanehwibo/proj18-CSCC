# 2026 T202610486999573 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t202610486999573-2679`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：90
- 代码/文本行数：14764
- 主要语言：c 13874 LOC, asm 365 LOC, build 327 LOC, markdown 172 LOC, make 26 LOC

## 总览

2026 T202610486999573 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 c: 13874 LOC, asm: 365 LOC, build: 327 LOC, markdown: 172 LOC。仓库包含 90 个已扫描文件、约 14764 行可分析文本，当前抽取到 419 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 5 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, kernel/Makefile, kernel/boot/Makefile, kernel/dev/Makefile, kernel/fs/Makefile。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`include common.mk KERN = kernel`
  - `kernel/Makefile:L1-L3`：构建入口
    代码片段：`include ../common.mk MODULES = $(shell ls -d */)`
  - `kernel/boot/Makefile:L1-L3`：构建入口
    代码片段：`# 头文件 INLCUDES := ../../include`
  - `kernel/dev/Makefile:L1-L3`：构建入口
    代码片段：`# 头文件 INLCUDES := ../../include`
  - `kernel/fs/Makefile:L1-L3`：构建入口
    代码片段：`# 头文件 INLCUDES := ../../include`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn swtch, struct cpu, struct context, struct trapframe, enum proc_state。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/proc/cpu.c` | L1-L3 | 关键词命中 |
| `kernel/proc/proc.c` | L4-L8 | 关键词命中 |
| `kernel/proc/switch.S` | L1-L4 | 关键词命中 |
| `kernel/proc/switch.S` | L12-L16 | fn swtch |
| `include/proc/cpu.h` | L13-L17 | struct cpu |
| `include/proc/proc.h` | L30-L34 | struct context |

### 关键代码片段

  - `kernel/proc/cpu.c:L1-L3`：关键词命中
    代码片段：`#include "proc/cpu.h" #include "riscv.h"`
  - `kernel/proc/proc.c:L4-L8`：关键词命中
    代码片段：`#include "dev/uart.h" #include "mem/vmem.h" #include "proc/cpu.h" #include "proc/initcode.h" #include "trap/trap.h"`
  - `kernel/proc/switch.S:L1-L4`：关键词命中
    代码片段：`/* * switch.S - 上下文切换 swtch * * 原型：void swtch(context_t *old, context_t *new);`
  - `kernel/proc/switch.S:L12-L16`：fn swtch
    代码片段：`.globl swtch swtch: sd ra, 0(a0) sd sp, 8(a0)`

### 相关符号

`fn swtch` at `kernel/proc/switch.S:L12`、`struct cpu` at `include/proc/cpu.h:L13`、`struct context` at `include/proc/proc.h:L30`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：macro PGSIZE, macro PTE_TO_PA, macro PTE_V, macro PTE_R, macro PTE_W。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/fs/elf.c` | L7-L11 | 关键词命中 |
| `kernel/fs/ext4.c` | L308-L312 | 关键词命中 |
| `kernel/fs/file.c` | L433-L437 | 关键词命中 |
| `include/common.h` | L24-L28 | macro PGSIZE |
| `include/mem/vmem.h` | L57-L61 | macro PTE_TO_PA |
| `include/mem/vmem.h` | L60-L64 | macro PTE_V |

### 关键代码片段

  - `kernel/fs/elf.c:L7-L11`：关键词命中
    代码片段：`*       1) 按页分配物理页并映射到用户页表 *       2) 从 elf_data 拷贝文件内容（filesz），其余 memsz 区域保持为 0（BSS） *   - 输出入口地址 e_entry 与 heap_top（最高段末尾按页对齐） */`
  - `kernel/fs/ext4.c:L308-L312`：关键词命中
    代码片段：`uint64 dir_size = inode_size_bytes(&dir); if (dir_size == 0 || dir_size > PGSIZE) { dir_size = PGSIZE; }`
  - `kernel/fs/file.c:L433-L437`：关键词命中
    代码片段：`uint32 done = 0; while (done < to_read) { uint32 page_idx = (pos + done) / PGSIZE; uint32 page_off = (pos + done) % PGSIZE; if (page_idx >= RAMFS_MAX_PAGES || rf->pages[page_idx...`
  - `include/common.h:L24-L28`：macro PGSIZE
    代码片段：`// 页面大小 4KB #define PGSIZE 4096 // 内核保留的物理页数量（防止用户程序耗尽内存）`

### 相关符号

`macro PGSIZE` at `include/common.h:L24`、`macro PTE_TO_PA` at `include/mem/vmem.h:L57`、`macro PTE_V` at `include/mem/vmem.h:L60`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn kernel_vector, fn timer_vector, fn sys_getuid, fn sys_geteuid, fn sys_getgid。（置信度：medium）
  - 静态识别到 22 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/syscall/syscall.c` | L3-L7 | 关键词命中 |
| `kernel/syscall/sysfunc.c` | L3-L7 | 关键词命中 |
| `include/syscall/sysnum.h` | L6-L10 | 关键词命中 |
| `kernel/trap/trap.S` | L19-L23 | fn kernel_vector |
| `kernel/trap/trap.S` | L105-L109 | fn timer_vector |
| `kernel/syscall/sysfunc.c` | L66-L70 | fn sys_getuid |

### 关键代码片段

  - `kernel/syscall/syscall.c:L3-L7`：关键词命中
    代码片段：`#include "mem/mmap.h" #include "mem/vmem.h" #include "syscall/syscall.h" #include "syscall/sysnum.h" #include "syscall/sysfunc.h"`
  - `kernel/syscall/sysfunc.c:L3-L7`：关键词命中
    代码片段：`* * 目标与定位： *   - 为 oscomp / riscv-syscalls-testing 等测试提供“够用”的 Linux 风格 syscall 语义。 *   - 这是一个兼容层而非完整的 Linux：很多 syscall 仅实现最小子集，或直接返回固定值。 *`
  - `include/syscall/sysnum.h:L6-L10`：关键词命中
    代码片段：`* * 说明： *   - 这里定义的是一组“与 Linux RISC-V 对齐”的 syscall 编号，用于 syscall 分发层。 *   - 本内核并不实现完整 Linux syscall 表；只覆盖 oscomp / riscv-syscalls-testing *     等测试用例会触发到的子集。`
  - `kernel/trap/trap.S:L19-L23`：fn kernel_vector
    代码片段：`.globl kernel_vector .align 4 kernel_vector: # 为 32 个通用寄存器预留空间：32 * 8 = 256 字节`

### 相关符号

`fn kernel_vector` at `kernel/trap/trap.S:L19`、`fn timer_vector` at `kernel/trap/trap.S:L105`、`fn sys_getuid` at `kernel/syscall/sysfunc.c:L66`、`fn sys_getuid` at `kernel/syscall/sysfunc.c:L66`、`fn sys_geteuid` at `kernel/syscall/sysfunc.c:L67`、`fn sys_getgid` at `kernel/syscall/sysfunc.c:L68`、`fn sys_getegid` at `kernel/syscall/sysfunc.c:L69`、`macro __SYSCALL_H__` at `include/syscall/syscall.h:L1`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct buf_node, fn __attribute__, struct ramfile, struct pipe, struct kstat。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/fs/fs.c` | L4-L8 | 关键词命中 |
| `kernel/fs/dir.c` | L7-L11 | 关键词命中 |
| `kernel/fs/ext4.c` | L109-L113 | 关键词命中 |
| `kernel/fs/buf.c` | L29-L33 | struct buf_node |
| `kernel/fs/ext4.c` | L15-L19 | fn __attribute__ |
| `kernel/fs/file.c` | L30-L34 | struct ramfile |

### 关键代码片段

  - `kernel/fs/fs.c:L4-L8`：关键词命中
    代码片段：`* 当前版本的 fs_init() 做了两件事： *   1) 初始化 buf cache，并从磁盘读取超级块到内存副本 sb； *   2) 运行一段 inode 读写自测逻辑（便于实验阶段验证 inode/data 分配链路）。 * * 注意：自测结束后会进入 while(1) 死循环。`
  - `kernel/fs/dir.c:L7-L11`：关键词命中
    代码片段：`* * 锁语义： *   - 目录项的查找/增删/遍历都要求调用者持有父目录 inode 的睡眠锁（pip->slk）。 *   - path_* 系列会按需对中间目录 inode 加锁、查找目录项、再移动到下一段。 */`
  - `kernel/fs/ext4.c:L109-L113`：关键词命中
    代码片段：`typedef struct __attribute__((packed)) { uint32 inode; uint16 rec_len; uint8  name_len;`
  - `kernel/fs/buf.c:L29-L33`：struct buf_node
    代码片段：`* 说明：buf_t 是第一个成员，因此 (buf_node_t*)buf_t 指针转换是合法的。 */ typedef struct buf_node { buf_t buf; struct buf_node* next;`

### 相关符号

`struct buf_node` at `kernel/fs/buf.c:L29`、`fn __attribute__` at `kernel/fs/ext4.c:L15`、`struct ramfile` at `kernel/fs/file.c:L30`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct spinlock, struct sleeplock。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/lib/spinlock.c` | L1-L4 | 关键词命中 |
| `kernel/lib/sleeplock.c` | L13-L17 | 关键词命中 |
| `include/lib/lock.h` | L1-L4 | 关键词命中 |
| `include/lib/lock.h` | L16-L20 | struct spinlock |
| `include/lib/lock.h` | L37-L41 | struct sleeplock |

### 关键代码片段

  - `kernel/lib/spinlock.c:L1-L4`：关键词命中
    代码片段：`/* * spinlock.c - 自旋锁与可嵌套的关/开中断 * * 这份实现的核心思路：`
  - `kernel/lib/sleeplock.c:L13-L17`：关键词命中
    代码片段：`*/ #include "lib/lock.h" #include "proc/proc.h" #include "proc/cpu.h"`
  - `include/lib/lock.h:L1-L4`：关键词命中
    代码片段：`/* * lock.h - 内核同步原语 * * 本项目提供两类锁：`
  - `include/lib/lock.h:L16-L20`：struct spinlock
    代码片段：`#include "common.h" typedef struct spinlock { int locked;   // 0/1：是否持有 char* name;   // 调试用名称`

### 相关符号

`struct spinlock` at `include/lib/lock.h:L16`、`struct sleeplock` at `include/lib/lock.h:L37`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn kernel_vector, fn timer_vector, fn trampoline, fn user_vector, fn user_return。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/dev/plic.c` | L1-L4 | 关键词命中 |
| `kernel/dev/timer.c` | L1-L5 | 关键词命中 |
| `kernel/trap/trap.S` | L1-L4 | 关键词命中 |
| `kernel/trap/trap.S` | L19-L23 | fn kernel_vector |
| `kernel/trap/trap.S` | L105-L109 | fn timer_vector |
| `kernel/trap/trampoline.S` | L21-L25 | fn trampoline |

### 关键代码片段

  - `kernel/dev/plic.c:L1-L4`：关键词命中
    代码片段：`/* * plic.c - PLIC（Platform-level interrupt controller） * * 负责外部中断（external interrupt）的分发：`
  - `kernel/dev/timer.c:L1-L5`：关键词命中
    代码片段：`#include "lib/lock.h" #include "lib/print.h" #include "dev/timer.h" #include "memlayout.h" #include "riscv.h"`
  - `kernel/trap/trap.S:L1-L4`：关键词命中
    代码片段：`/* * trap.S - trap 汇编入口 * * kernel_vector：S-mode 下的 trap 入口。`
  - `kernel/trap/trap.S:L19-L23`：fn kernel_vector
    代码片段：`.globl kernel_vector .align 4 kernel_vector: # 为 32 个通用寄存器预留空间：32 * 8 = 256 字节`

### 相关符号

`fn kernel_vector` at `kernel/trap/trap.S:L19`、`fn timer_vector` at `kernel/trap/trap.S:L105`、`fn trampoline` at `kernel/trap/trampoline.S:L21`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：macro VIRTIO_MMIO_MAGIC_VALUE, macro VIRTIO_MMIO_VERSION, macro VIRTIO_MMIO_DEVICE_ID, macro VIRTIO_MMIO_VENDOR_ID, macro VIRTIO_MMIO_DEVICE_FEATURES。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/dev/uart.c` | L1-L4 | 关键词命中 |
| `kernel/dev/virtio.c` | L1-L4 | 关键词命中 |
| `kernel/fs/buf.c` | L143-L147 | 关键词命中 |
| `include/dev/virtio.h` | L23-L27 | macro VIRTIO_MMIO_MAGIC_VALUE |
| `include/dev/virtio.h` | L24-L28 | macro VIRTIO_MMIO_VERSION |
| `include/dev/virtio.h` | L25-L29 | macro VIRTIO_MMIO_DEVICE_ID |

### 关键代码片段

  - `kernel/dev/uart.c:L1-L4`：关键词命中
    代码片段：`/* * uart.c - 16550a UART 驱动（轮询 + 中断回显） * * 提供：`
  - `kernel/dev/virtio.c:L1-L4`：关键词命中
    代码片段：`/* * virtio.c - QEMU virtio-blk（虚拟磁盘）驱动 * * QEMU 侧常见配置：-device virtio-blk-device,drive=x0,bus=virtio-mmio-bus.0`
  - `kernel/fs/buf.c:L143-L147`：关键词命中
    代码片段：`sleeplock_acquire(&bn->buf.slk); // 从磁盘读取 virtio_disk_rw(&bn->buf, false); return &bn->buf; }`
  - `include/dev/virtio.h:L23-L27`：macro VIRTIO_MMIO_MAGIC_VALUE
    代码片段：`// virtio mmio control registers, mapped starting at 0x10001000. // from qemu virtio_mmio.h #define VIRTIO_MMIO_MAGIC_VALUE 0x000 // 0x74726976 #define VIRTIO_MMIO_VERSION 0x004...`

### 相关符号

`macro VIRTIO_MMIO_MAGIC_VALUE` at `include/dev/virtio.h:L23`、`macro VIRTIO_MMIO_VERSION` at `include/dev/virtio.h:L24`、`macro VIRTIO_MMIO_DEVICE_ID` at `include/dev/virtio.h:L25`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
