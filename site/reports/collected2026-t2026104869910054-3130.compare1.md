# 2026 T2026104869910054 比较报告

- 对比历史仓库：2024 萌新
- 生成时间：2026-07-02T05:01:06.573205+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 萌新（来源：赛事历史作品）：画像相似度 score=9.66；同属 rcore-variant 风格; 语言构成相似度 0.95; OS 维度重合度 1.00; 代码规模接近度 0.76

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 萌新 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/driver/src/bus.rs:L1-L5`：关键词命中
    代码片段：`//! Bus abstraction layer. //! //! Currently only MMIO bus is supported (used by VirtIO legacy MMIO and //! NS16550A UART on RISC-V platforms). PCI is a stub for future expansio...`
  - `crates/driver/src/trait.rs:L1-L3`：关键词命中
    代码片段：`//! Device trait definitions for the GoodOS driver subsystem. //! //! Defines the core traits that all device drivers must implement:`
  - `modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use driver_common::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 与 2024 萌新 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/fs/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! GoodOS 文件系统层 (VFS)。 //! //! 提供统一的文件系统接口，支持多种文件系统实现：`
  - `crates/fs/src/file.rs:L4-L8`：关键词命中
    代码片段：`//! 减少各文件系统实现的重复代码。 use crate::vfs::traits::File; use crate::vfs::FsResult;`
  - `crates/rust-fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `modules/axfs/src/fs/fatfs.rs:L32-L36`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
- 与 2024 萌新 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/kernel/src/trap_handler.rs:L1-L3`：关键词命中
    代码片段：`// Trap 分发器：时钟、系统调用、缺页。HAL 解码后回调本模块进行子系统分发。 use crate::log;`
  - `crates/hal/src/rv64/trap.S:L1-L3`：关键词命中
    代码片段：`# RISC-V trap assembly with an xv6/148-style trampoline. # # TrapContext layout (50 * 8 = 400 bytes):`
  - `modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `modules/axtrap/src/trap.rs:L1-L4`：关键词命中
    代码片段：`pub fn handle_irq(irq_num: usize, from_user: bool) { #[cfg(feature = "irq")] { let guard = kernel_guard::NoPreempt::new();`
- 与 2024 萌新 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/mm/src/cow.rs:L1-L5`：关键词命中
    代码片段：`// Copy-on-write — stub. // // COW lets multiple processes share read-only physical pages.  When a process // writes to a COW page a page fault fires; the handler allocates a pr...`
  - `crates/mm/src/lib.rs:L2-L6`：关键词命中
    代码片段：`// // 提供地址空间管理、物理页分配、页表操作、用户态/内核态拷贝和 ELF 加载。 // 具体地址常量由 'platform' crate 提供；架构相关操作（TLB 刷新、satp 写入） // 通过 cfg 守卫区分 RISC-V / LoongArch。 //`
  - `modules/axhal/src/lib.rs:L19-L23`：关键词命中
    代码片段：`//! - 'smp': Enable SMP (symmetric multiprocessing) support. //! - 'fp_simd': Enable floating-point and SIMD support. //! - 'paging': Enable page table manipulation. //! - 'irq'...`
  - `crates/taskctx/src/task.rs:L187-L191`：关键词命中
    代码片段：`// pub trap_frame: UnsafeCell<TrapFrame>, #[cfg(feature = "monolithic")] /// the page table token of the process which the task belongs to pub page_table_token: UnsafeCell<usize>,`
- 与 2024 萌新 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/task/src/task.rs:L1-L3`：关键词命中
    代码片段：`// Task —— simple task status type. use crate::task_id::Pid;`
  - `crates/task/src/task_id.rs:L1-L5`：关键词命中
    代码片段：`// Pid/Tid 全局分配器。 // // Pid/Tid share one Linux-style task-id namespace. use core::sync::atomic::{AtomicUsize, Ordering};`
  - `crates/taskctx/src/task.rs:L13-L17`：关键词命中
    代码片段：`}; /// A unique identifier for a thread. #[derive(Debug, Clone, Copy, Eq, PartialEq)] pub struct TaskId(pub u64);`
  - `api/linux_syscall_api/src/syscall_task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! 提供和 task 模块相关的 syscall mod task_syscall_id;`
- 与 2024 萌新 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/task/src/task_id.rs:L3-L7`：关键词命中
    代码片段：`// Pid/Tid share one Linux-style task-id namespace. use core::sync::atomic::{AtomicUsize, Ordering}; /// 进程 ID。`
  - `crates/mm/src/lib.rs:L20-L24`：关键词命中
    代码片段：`extern crate alloc; use core::sync::atomic::{AtomicUsize, Ordering}; mod aspace;`
  - `modules/axfs/src/root.rs:L11-L15`：关键词命中
    代码片段：`use axfs_vfs::{VfsNodeAttr, VfsNodeOps, VfsNodeRef, VfsNodeType, VfsOps, VfsResult}; use lazy_init::LazyInit; use spinlock::SpinNoIrq; #[allow(unused)]`
  - `crates/taskctx/src/task.rs:L10-L14`：关键词命中
    代码片段：`cell::UnsafeCell, fmt, sync::atomic::{AtomicBool, AtomicI32, AtomicU64, AtomicU8, AtomicUsize, Ordering}, };`
- 与 2024 萌新 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/syscall/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! Provides the 'syscall_instrument!' macro and the central dispatch function. //! All syscall implementations live in 'imp/' submodules, categorised by domain. #![no_std]`
  - `crates/syscall/src/consts.rs:L1-L5`：关键词命中
    代码片段：`// System call number constants. // // These match the Linux asm-generic syscall ABI used by RISC-V and LoongArch. pub const SYS_LSEEK: usize = 62;`
  - `modules/axtrap/src/trap.rs:L14-L16`：关键词命中
    代码片段：`#[allow(unused)] #[cfg(feature = "monolithic")] pub use linux_syscall_api::trap::{handle_page_fault, handle_syscall,handle_signals};`
  - `api/linux_syscall_api/src/trap.rs:L2-L6`：关键词命中
    代码片段：`pub use axhal::{mem::VirtAddr, paging::MappingFlags,arch::TrapFrame}; use crate::syscall::syscall; fn time_stat_from_kernel_to_user() {`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 萌新 在“中断与异常”维度发现片段级代码相似度 0.60 （token=0.50, structure=0.78）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：cfg, fn, pub。（置信度：medium）
  证据：
  - `crates/kernel/src/trap_handler.rs:L20-L24`：fn init
    代码片段：`/// 注册 trap 向量和内核回调。 pub fn init() { #[cfg(target_arch = "riscv64")] {`
  - `modules/axtrap/src/trap.rs:L1-L3`：fn handle_irq
    代码片段：`pub fn handle_irq(irq_num: usize, from_user: bool) { #[cfg(feature = "irq")] {`
- 与 2024 萌新 在“调度与任务管理”维度发现片段级代码相似度 0.72 （token=0.56, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fetch_add, fn, ordering, pub。（置信度：medium）
  证据：
  - `crates/task/src/task_id.rs:L25-L29`：fn alloc_pid
    代码片段：`/// 分配一个新 PID。 /// pub fn alloc_pid() -> Pid { NEXT_TASK_ID.fetch_add(1, Ordering::SeqCst) }`
  - `crates/taskctx/src/task.rs:L20-L24`：fn new
    代码片段：`impl TaskId { /// Create a new task ID. pub fn new() -> Self { Self(ID_COUNTER.fetch_add(1, Ordering::Relaxed)) }`
- 与 2024 萌新 在“调度与任务管理”维度发现片段级代码相似度 0.59 （token=0.55, structure=0.67）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, debug, derive, eq, partialeq, pub。（置信度：medium）
  证据：
  - `crates/task/src/task.rs:L3-L7`：enum TaskStatus
    代码片段：`#[derive(Clone, Copy, Debug, PartialEq, Eq)] pub enum TaskStatus { Ready, Running,`
  - `crates/taskctx/src/task.rs:L13-L17`：关键词命中
    代码片段：`}; /// A unique identifier for a thread. #[derive(Debug, Clone, Copy, Eq, PartialEq)] pub struct TaskId(pub u64);`
- 与 2024 萌新 在“同步机制”维度发现片段级代码相似度 0.68 （token=0.50, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：atomic, atomicusize, ordering, sync。（置信度：medium）
  证据：
  - `crates/task/src/task_id.rs:L3-L7`：关键词命中
    代码片段：`// Pid/Tid share one Linux-style task-id namespace. use core::sync::atomic::{AtomicUsize, Ordering}; /// 进程 ID。`
  - `crates/taskctx/src/task.rs:L10-L14`：关键词命中
    代码片段：`cell::UnsafeCell, fmt, sync::atomic::{AtomicBool, AtomicI32, AtomicU64, AtomicU8, AtomicUsize, Ordering}, };`
- 与 2024 萌新 在“系统调用”维度发现片段级代码相似度 0.60 （token=0.50, structure=0.78）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：cfg, fn, pub。（置信度：medium）
  证据：
  - `crates/kernel/src/trap_handler.rs:L20-L24`：fn init
    代码片段：`/// 注册 trap 向量和内核回调。 pub fn init() { #[cfg(target_arch = "riscv64")] {`
  - `modules/axtrap/src/trap.rs:L1-L3`：fn handle_irq
    代码片段：`pub fn handle_irq(irq_num: usize, from_user: bool) { #[cfg(feature = "irq")] {`
- 函数/符号名重合：与 2024 萌新 在“设备驱动”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/driver/src/impls/block/ramdisk.rs:L20-L20`：fn new
    代码片段：`pub fn new(name: &'static str, block_size: usize, num_blocks: usize) -> Self {`
  - `modules/axnet/src/smoltcp_impl/mod.rs:L160-L160`：fn new
    代码片段：`fn new(name: &'static str, dev: AxNetDevice, ether_addr: EthernetAddress) -> Self {`
- 函数/符号名重合：与 2024 萌新 在“文件系统”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/fs/src/block_cache.rs:L77-L77`：fn new
    代码片段：`pub fn new(device: &'a dyn BlockDevice) -> Self {`
  - `modules/axtask/src/fd_manager.rs:L25-L25`：fn new
    代码片段：`pub fn new(fd_table: Vec<Option<Arc<dyn FileIO>>>, limit: usize) -> Self {`
- 函数/符号名重合：与 2024 萌新 在“同步机制”维度发现 1 个同名定义：drop。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/fs/src/block_cache.rs:L222-L222`：fn drop
    代码片段：`fn drop(&mut self) {`
  - `modules/axtask/src/sync/mutex.rs:L195-L195`：fn drop
    代码片段：`fn drop(&mut self) {`
- 文件路径重合：与 2024 萌新 在“调度与任务管理”维度出现同名文件源码路径 `crates/task/src/task.rs` / `crates/taskctx/src/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `crates/task/src/task.rs:L1-L3`：关键词命中
    代码片段：`// Task —— simple task status type. use crate::task_id::Pid;`
  - `crates/taskctx/src/task.rs:L13-L17`：关键词命中
    代码片段：`}; /// A unique identifier for a thread. #[derive(Debug, Clone, Copy, Eq, PartialEq)] pub struct TaskId(pub u64);`

## 相似点

- 与 2024 萌新 同属 rcore-variant 风格。（置信度：medium）
- 与 2024 萌新 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/driver/src/bus.rs:L1-L5`：关键词命中
    代码片段：`//! Bus abstraction layer. //! //! Currently only MMIO bus is supported (used by VirtIO legacy MMIO and //! NS16550A UART on RISC-V platforms). PCI is a stub for future expansio...`
  - `crates/driver/src/trait.rs:L1-L3`：关键词命中
    代码片段：`//! Device trait definitions for the GoodOS driver subsystem. //! //! Defines the core traits that all device drivers must implement:`
- 与 2024 萌新 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/fs/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! GoodOS 文件系统层 (VFS)。 //! //! 提供统一的文件系统接口，支持多种文件系统实现：`
  - `crates/fs/src/file.rs:L4-L8`：关键词命中
    代码片段：`//! 减少各文件系统实现的重复代码。 use crate::vfs::traits::File; use crate::vfs::FsResult;`
- 与 2024 萌新 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/kernel/src/trap_handler.rs:L1-L3`：关键词命中
    代码片段：`// Trap 分发器：时钟、系统调用、缺页。HAL 解码后回调本模块进行子系统分发。 use crate::log;`
  - `crates/hal/src/rv64/trap.S:L1-L3`：关键词命中
    代码片段：`# RISC-V trap assembly with an xv6/148-style trampoline. # # TrapContext layout (50 * 8 = 400 bytes):`
- 与 2024 萌新 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/mm/src/cow.rs:L1-L5`：关键词命中
    代码片段：`// Copy-on-write — stub. // // COW lets multiple processes share read-only physical pages.  When a process // writes to a COW page a page fault fires; the handler allocates a pr...`
  - `crates/mm/src/lib.rs:L2-L6`：关键词命中
    代码片段：`// // 提供地址空间管理、物理页分配、页表操作、用户态/内核态拷贝和 ELF 加载。 // 具体地址常量由 'platform' crate 提供；架构相关操作（TLB 刷新、satp 写入） // 通过 cfg 守卫区分 RISC-V / LoongArch。 //`
- 与 2024 萌新 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/task/src/task.rs:L1-L3`：关键词命中
    代码片段：`// Task —— simple task status type. use crate::task_id::Pid;`
  - `crates/task/src/task_id.rs:L1-L5`：关键词命中
    代码片段：`// Pid/Tid 全局分配器。 // // Pid/Tid share one Linux-style task-id namespace. use core::sync::atomic::{AtomicUsize, Ordering};`
- 与 2024 萌新 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/task/src/task_id.rs:L3-L7`：关键词命中
    代码片段：`// Pid/Tid share one Linux-style task-id namespace. use core::sync::atomic::{AtomicUsize, Ordering}; /// 进程 ID。`
  - `crates/mm/src/lib.rs:L20-L24`：关键词命中
    代码片段：`extern crate alloc; use core::sync::atomic::{AtomicUsize, Ordering}; mod aspace;`
- 与 2024 萌新 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/syscall/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! Provides the 'syscall_instrument!' macro and the central dispatch function. //! All syscall implementations live in 'imp/' submodules, categorised by domain. #![no_std]`
  - `crates/syscall/src/consts.rs:L1-L5`：关键词命中
    代码片段：`// System call number constants. // // These match the Linux asm-generic syscall ABI used by RISC-V and LoongArch. pub const SYS_LSEEK: usize = 62;`
- 与 2024 萌新 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 萌新 的语言构成不同：待测作品为 {'toml': 44, 'build': 693, 'markdown': 681, 'rust': 46328, 'asm': 618}，历史样本为 {'json': 20, 'build': 2043, 'text': 2, 'markdown': 2753, 'toml': 170, 'make': 147, 'rust': 61101, 'asm': 427, 'c': 150}。（置信度：medium）

## 可能创新点

- 待测作品包含 302 个历史样本 2024 萌新 中未见的独特函数/类型定义（如 `raw_read_cache_block`, `raw_write_cache_block`, `raw_read_cache_blocks`, `read_bytes_from`, `get_scratch_block_device`, `lookup_child_ino`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `crates/fs/src/impls/ext4/super_block.rs:L462-L462`：fn raw_read_cache_block
    代码片段：`fn raw_read_cache_block(dev: &'static dyn BlockDevice, base: usize, data: &mut [u8; EXT4_CACHE_BLOCK_SIZE]) -> Result<(), ()> {`
  - `crates/fs/src/impls/ext4/super_block.rs:L498-L498`：fn raw_write_cache_block
    代码片段：`fn raw_write_cache_block(dev: &'static dyn BlockDevice, base: usize, data: &[u8; EXT4_CACHE_BLOCK_SIZE]) -> Result<(), ()> {`

## 附录：核验摘要

- 关键结论数：24
- 含证据关键结论数：24（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
