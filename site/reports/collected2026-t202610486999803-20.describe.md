# 2026 T202610486999803 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t202610486999803-20`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：3528
- 代码/文本行数：378123
- 主要语言：c 347925 LOC, markdown 15529 LOC, asm 11000 LOC, build 2900 LOC, make 534 LOC

## 总览

2026 T202610486999803 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 c: 347925 LOC, markdown: 15529 LOC, asm: 11000 LOC, build: 2900 LOC。仓库包含 3528 个已扫描文件、约 378123 行可分析文本，当前抽取到 31127 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, user/Makefile, user/external/mksh-cvs2git/Makefile, user/external/musl/Makefile, user/external/musl/build-aarch64/Makefile。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`# A20OS Makefile # Parallel build`
  - `user/Makefile:L1-L3`：构建入口
    代码片段：`ARCH ?= riscv64 OPT ?= -O3`
  - `user/external/mksh-cvs2git/Makefile:L1-L3`：构建入口
    代码片段：`# $MirOS: src/bin/mksh/Makefile,v 1.200 2025/12/23 19:26:15 tg Exp $ #- # Copyright (c) 2003, 2004, 2005, 2006, 2007, 2008, 2009, 2010,`
  - `user/external/musl/Makefile:L1-L3`：构建入口
    代码片段：`# # Makefile for musl (requires GNU make) #`
  - `user/external/musl/build-aarch64/Makefile:L1-L1`：构建入口
    代码片段：`../Makefile`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn proc_make_ready, fn proc_block_until, fn idle_loop, fn proc_init, fn proc_alloc。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/proc/pid.c` | L1-L3 | 关键词命中 |
| `kernel/proc/exec.c` | L16-L20 | 关键词命中 |
| `kernel/proc/exit.c` | L1-L3 | 关键词命中 |
| `kernel/proc/proc.c` | L185-L189 | fn proc_make_ready |
| `kernel/proc/proc.c` | L214-L218 | fn proc_block_until |
| `kernel/proc/proc.c` | L225-L229 | fn idle_loop |

### 关键代码片段

  - `kernel/proc/pid.c:L1-L3`：关键词命中
    代码片段：`#include "proc/proc.h" #include "proc/proc_internal.h" #include "core/lock.h"`
  - `kernel/proc/exec.c:L16-L20`：关键词命中
    代码片段：`*/ #include "proc/proc.h" #include "proc/proc_internal.h" #include "proc/signal.h"`
  - `kernel/proc/exit.c:L1-L3`：关键词命中
    代码片段：`#include "proc/proc.h" #include "proc/proc_internal.h" #include "proc/signal.h"`
  - `kernel/proc/proc.c:L185-L189`：fn proc_make_ready
    代码片段：`} void proc_make_ready(task_t *t) { if (!t || t->state == PROC_UNUSED || t->state == PROC_ZOMBIE) return;`

### 相关符号

`fn proc_make_ready` at `kernel/proc/proc.c:L185`、`fn proc_block_until` at `kernel/proc/proc.c:L214`、`fn idle_loop` at `kernel/proc/proc.c:L225`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn pte_user_readable, fn pte_user_writable, fn pt_level_size, fn mm_init, fn frame_free。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/mm/mm.c` | L11-L15 | 关键词命中 |
| `kernel/mm/vm.c` | L20-L24 | 关键词命中 |
| `kernel/mm/elf.c` | L3-L7 | 关键词命中 |
| `kernel/mm/mm.c` | L11-L15 | fn pte_user_readable |
| `kernel/mm/mm.c` | L15-L19 | fn pte_user_writable |
| `kernel/mm/mm.c` | L19-L23 | fn pt_level_size |

### 关键代码片段

  - `kernel/mm/mm.c:L11-L15`：关键词命中
    代码片段：`#include "proc/proc.h" static inline int pte_user_readable(uint64_t pte) { return arch_pte_is_leaf(pte) && (pte & PTE_U) && (pte & PTE_R); }`
  - `kernel/mm/vm.c:L20-L24`：关键词命中
    代码片段：`*   total_vm/rss/locked_vm while holding the address-space write exclusion *   defined by MM_LOCK_MODEL. * - Page-table write paths live in COW/demand faults, file/VMO faults, *...`
  - `kernel/mm/elf.c:L3-L7`：关键词命中
    代码片段：`* * Loads ELF executables into a new per-process address space with: *   - User segments mapped at ELF virtual addresses (PTE_USER) *   - Kernel space identity-mapped (PTE_KERN)...`
  - `kernel/mm/mm.c:L11-L15`：fn pte_user_readable
    代码片段：`#include "proc/proc.h" static inline int pte_user_readable(uint64_t pte) { return arch_pte_is_leaf(pte) && (pte & PTE_U) && (pte & PTE_R); }`

### 相关符号

`fn pte_user_readable` at `kernel/mm/mm.c:L11`、`fn pte_user_writable` at `kernel/mm/mm.c:L15`、`fn pt_level_size` at `kernel/mm/mm.c:L19`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn fetch_user_insn, fn dump_trap_context, fn dump_kernel_backtrace, fn dump_fault_pte, fn deliver_user_sync_signal。（置信度：medium）
  - 静态识别到 404 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/syscall/syscall.c` | L1-L4 | 关键词命中 |
| `kernel/abi/linux/sys_signal.c` | L99-L103 | 关键词命中 |
| `kernel/abi/linux/syscall_impl.h` | L26-L30 | 关键词命中 |
| `kernel/core/trap.c` | L14-L18 | fn fetch_user_insn |
| `kernel/core/trap.c` | L22-L26 | fn dump_trap_context |
| `kernel/core/trap.c` | L38-L42 | fn dump_kernel_backtrace |

### 关键代码片段

  - `kernel/syscall/syscall.c:L1-L4`：关键词命中
    代码片段：`/* * A20OS syscall dispatcher. * * Keep ABI dispatch here; concrete syscall implementations live in sys_*.c.`
  - `kernel/abi/linux/sys_signal.c:L99-L103`：关键词命中
    代码片段：`int64_t sys_sigreturn(trap_context_t *ctx) { /* * Linux syscall 139 is rt_sigreturn on RISC-V, LoongArch and AArch64. * Restoring from task->sig_saved_ctx is not sufficient: it...`
  - `kernel/abi/linux/syscall_impl.h:L26-L30`：关键词命中
    代码片段：`extern int syscall_sleep_diag_count; /* LINUX_ABI_BOUNDARY_CONTRACT: Linux syscall files translate user ABI shapes, * flags, and errno values, then call core kernel APIs; they m...`
  - `kernel/core/trap.c:L14-L18`：fn fetch_user_insn
    代码片段：`#include "core/klog.h" static int fetch_user_insn(task_t *task, uint64_t va, uint32_t *insn_out) { if (!task || !task->mm || !task->mm->pgdir || !insn_out) return 0;`

### 相关符号

`fn fetch_user_insn` at `kernel/core/trap.c:L14`、`fn dump_trap_context` at `kernel/core/trap.c:L22`、`fn dump_kernel_backtrace` at `kernel/core/trap.c:L38`、`macro _SYSCALL_H` at `kernel/include/sys/syscall.h:L1`、`macro SYSCALL_PROFILE_MAX` at `kernel/include/sys/syscall.h:L5`、`macro CONFIG_SYSCALL_PROFILE` at `kernel/include/sys/syscall.h:L8`、`struct syscall_prof` at `kernel/include/sys/syscall.h:L11`、`fn syscall_entry` at `kernel/arch/x86_64/trap/trap.S:L300`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：fn relative_path_stays_beneath, fn vfs_open, fn vfs_dirfd_path, fn vfs_openat2, fn vfs_close。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/fs/vfs.c` | L1-L4 | 关键词命中 |
| `kernel/fs/ext4.c` | L1-L5 | 关键词命中 |
| `kernel/fs/fat32.c` | L6-L10 | 关键词命中 |
| `kernel/fs/vfs.c` | L65-L69 | fn relative_path_stays_beneath |
| `kernel/fs/vfs.c` | L86-L90 | fn vfs_open |
| `kernel/fs/vfs.c` | L206-L210 | fn vfs_dirfd_path |

### 关键代码片段

  - `kernel/fs/vfs.c:L1-L4`：关键词命中
    代码片段：`/* * A20OS — Virtual Filesystem (VFS) * * Provides a unified fd-based interface over:`
  - `kernel/fs/ext4.c:L1-L5`：关键词命中
    代码片段：`#include "fs/ext4.h" #include "fs/file.h" #include "fs/vfs.h" #include "fs/block_cache.h" #include "mm/mm.h"`
  - `kernel/fs/fat32.c:L6-L10`：关键词命中
    代码片段：`*   - Directory traversal (8.3 + LFN) *   - mkdir, unlink, rename *   - FAT cluster chain management * * Uses bcache_read_bytes / bcache_write_bytes for disk I/O.`
  - `kernel/fs/vfs.c:L65-L69`：fn relative_path_stays_beneath
    代码片段：`* ============================================================ */ static int relative_path_stays_beneath(const char *relpath) { int depth = 0; const char *p = relpath;`

### 相关符号

`fn relative_path_stays_beneath` at `kernel/fs/vfs.c:L65`、`fn vfs_open` at `kernel/fs/vfs.c:L86`、`fn vfs_dirfd_path` at `kernel/fs/vfs.c:L206`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn wait_queue_init, fn wait_queue_prepare, fn wait_queue_finish, fn wait_queue_sleep, fn wait_queue_wake_one。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/core/sync.c` | L9-L13 | 关键词命中 |
| `kernel/include/core/lock.h` | L11-L15 | 关键词命中 |
| `kernel/include/core/sync.h` | L2-L6 | 关键词命中 |
| `kernel/core/sync.c` | L6-L10 | fn wait_queue_init |
| `kernel/core/sync.c` | L14-L18 | fn wait_queue_prepare |
| `kernel/core/sync.c` | L35-L39 | fn wait_queue_finish |

### 关键代码片段

  - `kernel/core/sync.c:L9-L13`：关键词命中
    代码片段：`if (!q) return; spin_init(&q->lock); spin_set_debug(&q->lock, "wait_queue", q); q->head = NULL;`
  - `kernel/include/core/lock.h:L11-L15`：关键词命中
    代码片段：`/* * Global lock order contract (outermost -> innermost). * For the full driver-private lock contracts see docs/driver-lock-order.md. *`
  - `kernel/include/core/sync.h:L2-L6`：关键词命中
    代码片段：`#define _CORE_SYNC_H #include "core/lock.h" #include "core/types.h"`
  - `kernel/core/sync.c:L6-L10`：fn wait_queue_init
    代码片段：`#define COMPLETION_DONE_ALL ((unsigned)-1) void wait_queue_init(wait_queue_t *q) { if (!q) return;`

### 相关符号

`fn wait_queue_init` at `kernel/core/sync.c:L6`、`fn wait_queue_prepare` at `kernel/core/sync.c:L14`、`fn wait_queue_finish` at `kernel/core/sync.c:L35`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn fetch_user_insn, fn dump_trap_context, fn dump_kernel_backtrace, fn dump_fault_pte, fn deliver_user_sync_signal。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/core/trap.c` | L1-L3 | 关键词命中 |
| `kernel/include/core/trap.h` | L7-L11 | 关键词命中 |
| `kernel/include/core/timer.h` | L5-L9 | 关键词命中 |
| `kernel/core/trap.c` | L14-L18 | fn fetch_user_insn |
| `kernel/core/trap.c` | L22-L26 | fn dump_trap_context |
| `kernel/core/trap.c` | L38-L42 | fn dump_kernel_backtrace |

### 关键代码片段

  - `kernel/core/trap.c:L1-L3`：关键词命中
    代码片段：`#include "core/trap.h" #include "proc/proc.h" #include "proc/signal.h"`
  - `kernel/include/core/trap.h:L7-L11`：关键词命中
    代码片段：`/* * Architecture-specific types and symbols are now in arch/trap.h, * pulled in via arch.h (included from defs.h): *   - trap_context_t, task_context_t`
  - `kernel/include/core/timer.h:L5-L9`：关键词命中
    代码片段：`#include "core/consts.h" /* Timer frequency — provided by arch/platform.h. */ #define TICKS_PER_SEC   ARCH_TIMER_FREQ #define MS_TO_TICKS(ms) ((uint64_t)(ms) * TICKS_PER_SEC / 1...`
  - `kernel/core/trap.c:L14-L18`：fn fetch_user_insn
    代码片段：`#include "core/klog.h" static int fetch_user_insn(task_t *task, uint64_t va, uint32_t *insn_out) { if (!task || !task->mm || !task->mm->pgdir || !insn_out) return 0;`

### 相关符号

`fn fetch_user_insn` at `kernel/core/trap.c:L14`、`fn dump_trap_context` at `kernel/core/trap.c:L22`、`fn dump_kernel_backtrace` at `kernel/core/trap.c:L38`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：fn pty_init, fn pty_alloc, fn pty_release, fn pty_master_read, fn pty_master_write。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/drivers/char/uart.c` | L1-L3 | 关键词命中 |
| `kernel/drivers/block/loop.c` | L54-L58 | 关键词命中 |
| `kernel/drivers/bus/pci_bus.c` | L1-L4 | 关键词命中 |
| `kernel/drivers/char/pty.c` | L52-L56 | fn pty_init |
| `kernel/drivers/char/pty.c` | L62-L66 | fn pty_alloc |
| `kernel/drivers/char/pty.c` | L97-L101 | fn pty_release |

### 关键代码片段

  - `kernel/drivers/char/uart.c:L1-L3`：关键词命中
    代码片段：`#include "drivers/char/uart.h" #include "drivers/core/driver_core.h" #include "drivers/core/driver_class.h"`
  - `kernel/drivers/block/loop.c:L54-L58`：关键词命中
    代码片段：`for (int i = 0; i < MAX_LOOP_DEVS; i++) { memset(&g_loop[i], 0, sizeof(g_loop[i])); /* LOCK_ORDER: initialize each per-device loop lock at boot. */ spin_init(&g_loop[i].lock); g...`
  - `kernel/drivers/bus/pci_bus.c:L1-L4`：关键词命中
    代码片段：`/* * PCI Bus — device enumeration via ECAM or Type-0/1 config cycles * * Used by LoongArch QEMU virt and future PC platforms.`
  - `kernel/drivers/char/pty.c:L52-L56`：fn pty_init
    代码片段：`static spinlock_t g_pty_alloc_lock; void pty_init(void) { /* LOCK_ORDER: initialize the allocation lock before any pty_alloc() call. */ spin_init(&g_pty_alloc_lock);`

### 相关符号

`fn pty_init` at `kernel/drivers/char/pty.c:L52`、`fn pty_alloc` at `kernel/drivers/char/pty.c:L62`、`fn pty_release` at `kernel/drivers/char/pty.c:L97`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
