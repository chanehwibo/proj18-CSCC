# 2026 T202610008999750 比较报告

- 对比历史仓库：2025 AstranciA
- 生成时间：2026-07-02T04:58:31.458943+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 AstranciA（来源：赛事历史作品）：画像相似度 score=9.54；同属 ucore-variant 风格; 语言构成相似度 0.87; OS 维度重合度 1.00; 代码规模接近度 0.80

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 AstranciA 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `vendor/arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `AstrancE/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `AstrancE/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 与 2025 AstranciA 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/cargo/fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `vendor/arceos/modules/axfs/src/fs/fatfs.rs:L34-L38`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
  - `AstrancE/modules/axfs/src/fs/fatfs.rs:L34-L38`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
  - `AstrancE/modules/axfs/src/fs/lwext4_rust.rs:L129-L133`：关键词命中
    代码片段：`info.bavail = free_blocks; // inode 总数 info.files = sblock.inodes_count as u64;`
- 与 2025 AstranciA 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `vendor/arceos/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
  - `AstrancE/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `AstrancE/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
- 与 2025 AstranciA 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `core/src/mm.rs:L26-L30`：关键词命中
    代码片段：`pub fn copy_from_kernel(aspace: &mut AddrSpace) -> AxResult { if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use se...`
  - `vendor/arceos/modules/axmm/src/backend/alloc.rs:L2-L6`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PhysAddr, VirtAddr};`
  - `AstrancE/modules/axmm/src/heap.rs:L9-L13`：关键词命中
    代码片段：`pub struct HeapSpace { max_heap_size: usize, heap_base: usize, heap_top: AtomicUsize, }`
  - `AstrancE/modules/axmm/src/backend/alloc.rs:L3-L7`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use bitflags::Flags; use memory_addr::{Frame...`
- 与 2025 AstranciA 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. use core::{`
  - `src/mm.rs:L40-L44`：关键词命中
    代码片段：`"{} ({:?}): segmentation fault at {:#x}, exit!", curr.id_name(), curr.task_ext().thread, vaddr );`
  - `AstrancE/modules/axfs/src/lib.rs:L49-L53`：关键词命中
    代码片段：`use axdriver::{AxDeviceContainer, prelude::*}; pub use axfs_vfs::{VfsNodeOps, VfsOps, VfsNodeType, VfsResult, VfsError}; pub mod proc { pub use axfs_procfs::*; }`
  - `AstrancE/modules/axfs/src/root.rs:L254-L258`：关键词命中
    代码片段：`let proc_root = mounts::procfs().unwrap(); root_dir // should not fail .mount("/proc", proc_root.clone()) .expect("fail to mount procfs at /proc"); PROC_ROOT.init_once(proc_root...`
- 与 2025 AstranciA 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `core/src/task.rs:L4-L8`：关键词命中
    代码片段：`alloc::Layout, cell::RefCell, sync::atomic::{AtomicUsize, Ordering}, time::Duration, };`
  - `vendor/arceos/modules/axfs/src/root.rs:L7-L11`：关键词命中
    代码片段：`use axfs_vfs::{VfsNodeAttr, VfsNodeOps, VfsNodeRef, VfsNodeType, VfsOps, VfsResult}; use axns::{ResArc, def_resource}; use axsync::Mutex; use lazyinit::LazyInit; use spin::RwLock;`
  - `AstrancE/modules/axfs/src/lib.rs:L42-L46`：关键词命中
    代码片段：`pub mod root; use api::create_dir; use axsync::Mutex; use lazyinit::LazyInit; pub use root::{CURRENT_DIR, CURRENT_DIR_PATH, ROOT_DIR, PROC_ROOT};`
  - `AstrancE/modules/axfs/src/fops.rs:L14-L18`：关键词命中
    代码片段：`use core::ffi::{c_char, c_int, c_void}; use core::fmt; use spin::Mutex; /// Alias of ['axfs_vfs::VfsNodeType'].`
- 与 2025 AstranciA 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_api::*;`
  - `vendor/cargo/syscalls/src/syscall/arm.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %r5 // // %r7 is used for the syscall number. // // %r0 is reused for the syscall return value.`
  - `AstrancE/modules/axhal/src/arch/riscv/trap.rs:L66-L70`：关键词命中
    代码片段：`Trap::Exception(E::UserEnvCall) => { tf.sepc += 4; tf.regs.a0 = crate::trap::handle_syscall(tf, tf.regs.a7) as usize; } Trap::Exception(E::LoadPageFault) => {`
  - `AstrancE/modules/axhal/src/arch/x86_64/trap.rs:L50-L54`：关键词命中
    代码片段：`} #[cfg(feature = "uspace")] LEGACY_SYSCALL_VECTOR => super::syscall::handle_syscall(tf), IRQ_VECTOR_START..=IRQ_VECTOR_END => { handle_trap!(IRQ, tf.vector as _);`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 AstranciA 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：axdriver_base, cfg, crate, devicetype, feature, self, use, virtio。（置信度：high）
  证据：
  - `vendor/arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `AstrancE/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 与 2025 AstranciA 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：const, device_type, devicetype, pub, trait, virtiodevmeta。（置信度：high）
  证据：
  - `vendor/arceos/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `AstrancE/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
- 与 2025 AstranciA 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：disk, expect, fatfs, filesystem, format_volume, fsoptions, inner, let。（置信度：high）
  证据：
  - `vendor/arceos/modules/axfs/src/fs/fatfs.rs:L34-L38`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
  - `AstrancE/modules/axfs/src/fs/fatfs.rs:L34-L38`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
- 与 2025 AstranciA 在“中断与异常”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：allow, crate, dead_code, dispatch_irq_common, fn, handle, if, irq_handler_table。（置信度：high）
  证据：
  - `vendor/arceos/modules/axhal/src/irq.rs:L15-L19`：fn dispatch_irq_common
    代码片段：`/// Platform-independent IRQ dispatching. #[allow(dead_code)] pub(crate) fn dispatch_irq_common(irq_num: usize) { trace!("IRQ {}", irq_num); if !IRQ_HANDLER_TABLE.handle(irq_num) {`
  - `AstrancE/modules/axhal/src/irq.rs:L15-L19`：fn dispatch_irq_common
    代码片段：`/// Platform-independent IRQ dispatching. #[allow(dead_code)] pub(crate) fn dispatch_irq_common(irq_num: usize) { trace!("IRQ {}", irq_num); if !IRQ_HANDLER_TABLE.handle(irq_num) {`
- 与 2025 AstranciA 在“中断与异常”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：allow, bool, crate, dead_code, fn, handler, if, irq_handler_table。（置信度：high）
  证据：
  - `vendor/arceos/modules/axhal/src/irq.rs:L27-L31`：fn register_handler_common
    代码片段：`/// the registration failed. #[allow(dead_code)] pub(crate) fn register_handler_common(irq_num: usize, handler: IrqHandler) -> bool { if irq_num < MAX_IRQ_COUNT && IRQ_HANDLER_T...`
  - `AstrancE/modules/axhal/src/irq.rs:L27-L31`：fn register_handler_common
    代码片段：`/// the registration failed. #[allow(dead_code)] pub(crate) fn register_handler_common(irq_num: usize, handler: IrqHandler) -> bool { if irq_num < MAX_IRQ_COUNT && IRQ_HANDLER_T...`
- 与 2025 AstranciA 在“内存管理”维度发现片段级代码相似度 0.84 （token=0.76, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：axalloc, axhal, global_allocator, mappingflags, mem, memory_addr, pagesize, pagetable。（置信度：high）
  证据：
  - `vendor/arceos/modules/axmm/src/backend/alloc.rs:L2-L6`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PhysAddr, VirtAddr};`
  - `AstrancE/modules/axmm/src/backend/alloc.rs:L3-L7`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use bitflags::Flags; use memory_addr::{Frame...`
- 函数/符号名重合：与 2025 AstranciA 在“中断与异常”维度发现 1 个同名定义：handle_syscall。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/syscall.rs:L11-L11`：fn handle_syscall
    代码片段：`fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize {`
  - `AstrancE/ulib/axsyscall/src/lib.rs:L40-L40`：fn handle_syscall
    代码片段：`pub fn handle_syscall(tf: &mut axhal::arch::TrapFrame, syscall_num: usize) -> Option<isize> {`
- 函数/符号名重合：与 2025 AstranciA 在“内存管理”维度发现 2 个同名定义：handle_page_fault, handle_syscall。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/mm.rs:L13-L13`：fn handle_page_fault
    代码片段：`fn handle_page_fault(vaddr: VirtAddr, access_flags: MappingFlags, is_user: bool) -> bool {`
  - `AstrancE/ulib/axmono/src/mm/mod.rs:L438-L438`：fn handle_page_fault
    代码片段：`fn handle_page_fault(vaddr: VirtAddr, access_flags: MappingFlags, is_user: bool) -> bool {`
  - `src/syscall.rs:L11-L11`：fn handle_syscall
    代码片段：`fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize {`
  - `AstrancE/ulib/axsyscall/src/lib.rs:L40-L40`：fn handle_syscall
    代码片段：`pub fn handle_syscall(tf: &mut axhal::arch::TrapFrame, syscall_num: usize) -> Option<isize> {`
- 函数/符号名重合：与 2025 AstranciA 在“系统调用”维度发现 2 个同名定义：handle_syscall, from。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/syscall.rs:L11-L11`：fn handle_syscall
    代码片段：`fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize {`
  - `AstrancE/ulib/axsyscall/src/lib.rs:L40-L40`：fn handle_syscall
    代码片段：`pub fn handle_syscall(tf: &mut axhal::arch::TrapFrame, syscall_num: usize) -> Option<isize> {`
  - `vendor/cargo/syscalls/src/args.rs:L39-L39`：fn from
    代码片段：`fn from(args: &[usize; 6]) -> Self {`
  - `AstrancE/ulib/axsyscall/src/syscall_imp/errno.rs:L553-L553`：fn from
    代码片段：`fn from(value: axerrno::LinuxError) -> Self {`
- 文件路径重合：与 2025 AstranciA 在“设备驱动”维度出现同名文件源码路径 `vendor/arceos/modules/axdriver/src/virtio.rs` / `AstrancE/modules/axdriver/src/virtio.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `AstrancE/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
- 文件路径重合：与 2025 AstranciA 在“设备驱动”维度出现同名文件源码路径 `vendor/arceos/modules/axdriver/src/drivers.rs` / `AstrancE/modules/axdriver/src/drivers.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `AstrancE/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 文件路径重合：与 2025 AstranciA 在“文件系统”维度出现同名文件源码路径 `vendor/arceos/modules/axfs/src/fs/fatfs.rs` / `AstrancE/modules/axfs/src/fs/fatfs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axfs/src/fs/fatfs.rs:L34-L38`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
  - `AstrancE/modules/axfs/src/fs/fatfs.rs:L34-L38`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
- 文件路径重合：与 2025 AstranciA 在“中断与异常”维度出现同名文件源码路径 `vendor/arceos/modules/axhal/src/irq.rs` / `AstrancE/modules/axhal/src/irq.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `AstrancE/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
- 文件路径重合：与 2025 AstranciA 在“中断与异常”维度出现同名文件源码路径 `vendor/arceos/modules/axhal/src/trap.rs` / `AstrancE/modules/axhal/src/trap.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
  - `AstrancE/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
- 文件路径重合：与 2025 AstranciA 在“内存管理”维度出现同名文件源码路径 `vendor/arceos/modules/axmm/src/backend/alloc.rs` / `AstrancE/modules/axmm/src/backend/alloc.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axmm/src/backend/alloc.rs:L2-L6`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PhysAddr, VirtAddr};`
  - `AstrancE/modules/axmm/src/backend/alloc.rs:L3-L7`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use bitflags::Flags; use memory_addr::{Frame...`

## 相似点

- 与 2025 AstranciA 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 AstranciA 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `vendor/arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 与 2025 AstranciA 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/cargo/fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `vendor/arceos/modules/axfs/src/fs/fatfs.rs:L34-L38`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
- 与 2025 AstranciA 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `vendor/arceos/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
- 与 2025 AstranciA 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `core/src/mm.rs:L26-L30`：关键词命中
    代码片段：`pub fn copy_from_kernel(aspace: &mut AddrSpace) -> AxResult { if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use se...`
  - `vendor/arceos/modules/axmm/src/backend/alloc.rs:L2-L6`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PhysAddr, VirtAddr};`
- 与 2025 AstranciA 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. use core::{`
  - `src/mm.rs:L40-L44`：关键词命中
    代码片段：`"{} ({:?}): segmentation fault at {:#x}, exit!", curr.id_name(), curr.task_ext().thread, vaddr );`
- 与 2025 AstranciA 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `core/src/task.rs:L4-L8`：关键词命中
    代码片段：`alloc::Layout, cell::RefCell, sync::atomic::{AtomicUsize, Ordering}, time::Duration, };`
  - `vendor/arceos/modules/axfs/src/root.rs:L7-L11`：关键词命中
    代码片段：`use axfs_vfs::{VfsNodeAttr, VfsNodeOps, VfsNodeRef, VfsNodeType, VfsOps, VfsResult}; use axns::{ResArc, def_resource}; use axsync::Mutex; use lazyinit::LazyInit; use spin::RwLock;`
- 与 2025 AstranciA 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_api::*;`
  - `vendor/cargo/syscalls/src/syscall/arm.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %r5 // // %r7 is used for the syscall number. // // %r0 is reused for the syscall return value.`
- 与 2025 AstranciA 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 AstranciA 的语言构成不同：待测作品为 {'yaml': 14, 'json': 53, 'build': 3856, 'markdown': 4696, 'toml': 1063, 'rust': 120477, 'text': 38, 'c': 39653, 'make': 1316, 'asm': 983}，历史样本为 {'json': 32, 'toml': 1161, 'markdown': 4958, 'build': 3990, 'rust': 76368, 'c': 38127, 'make': 1389, 'asm': 1081, 'text': 7567, 'yaml': 36}。（置信度：medium）

## 可能创新点

- 待测作品包含 157 个历史样本 2025 AstranciA 中未见的独特函数/类型定义（如 `accept_ipv4_mapped_ipv6_part`, `accept_number`, `accept_mac_joined_with`, `syscall`, `name_and_description`, `parse_errno`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `vendor/cargo/smoltcp/src/parsers.rs:L151-L151`：fn accept_ipv4_mapped_ipv6_part
    代码片段：`fn accept_ipv4_mapped_ipv6_part(&mut self, parts: &mut [u16], idx: &mut usize) -> Result<()> {`
  - `vendor/cargo/smoltcp/src/parsers.rs:L109-L109`：fn accept_number
    代码片段：`fn accept_number(&mut self, max_digits: usize, max_value: u32, hex: bool) -> Result<u32> {`

## 附录：核验摘要

- 关键结论数：30
- 含证据关键结论数：30（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
