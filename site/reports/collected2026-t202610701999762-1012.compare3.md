# 2026 T202610701999762 比较报告

- 对比历史仓库：2022 代码掐架队
- 生成时间：2026-07-02T05:03:22.722274+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 代码掐架队（来源：赛事历史作品）：画像相似度 score=7.87；同属 rcore-variant 风格; 语言构成相似度 0.14; OS 维度重合度 1.00; 代码规模接近度 0.59

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 代码掐架队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/l0/80_device/device_core/body.rs:L1-L3`：关键词命中
    代码片段：`#[path = "virtio_mmio_blk.rs"] mod virtio_mmio_blk; #[cfg(target_arch = "loongarch64")]`
  - `kernel/src/l0/10_early_output/early_console/api.rs:L5-L9`：关键词命中
    代码片段：`/// - 'bytes'：要输出或填充的字节缓冲区。 pub fn bytes(bytes: &[u8]) { crate::arch::console::write_bytes(bytes); }`
  - `codes/os/src/console.rs:L13-L17`：关键词命中
    代码片段：`} static CONSOLE: Mutex<ConsoleInner> = Mutex::new(ConsoleInner {}); impl Write for Stdout {`
  - `codes/os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2022 代码掐架队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/l1/30_vfs/vfs_fd/api.rs:L106-L110`：关键词命中
    代码片段：`#[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct Metadata { pub inode: u64, pub kind: NodeKind, pub mode: u16,`
  - `kernel/src/l1/30_vfs/vfs_fd/devfs.rs:L49-L53`：关键词命中
    代码片段：`display, }, inode: file_inode(kind) as u32, kind: NodeKind::CharDevice, })`
  - `codes/os/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`mod pipe; pub mod stdio; mod inode; mod mount; pub mod finfo;`
  - `codes/os/src/fs/finfo.rs:L63-L67`：关键词命中
    代码片段：`} pub fn new(name:&str, inode:usize, offset:isize, reclen: u16, d_type: u8)->Self{ let mut dirent = Self{ d_ino:inode,`
- 与 2022 代码掐架队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/arch/trap.rs:L2-L6`：关键词命中
    代码片段：`pub enum TrapKind { Unknown, Interrupt, Timer, PageFault,`
  - `kernel/src/l0/50_trap/trap_core/api.rs:L1-L3`：关键词命中
    代码片段：`pub mod trap { pub type TrapKind = crate::arch::trap::TrapKind; pub type TrapInfo = crate::arch::trap::TrapInfo;`
  - `codes/os/src/trap/mod.rs:L7-L11`：关键词命中
    代码片段：`use crate::syscall::{syscall, test}; use crate::task::*; use crate::timer::set_next_trigger; use riscv::register::{ mtvec::TrapMode,`
  - `codes/os/src/sbi.rs:L29-L33`：关键词命中
    代码片段：`} pub fn set_timer(timer: usize) { sbi_call(SBI_SET_TIMER, timer, 0, 0); }`
- 与 2022 代码掐架队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/arch/vm.rs:L15-L19`：关键词命中
    代码片段：`pub enum PageTableKind { Generic, Sv39, LoongArch64ThreeLevel, }`
  - `kernel/src/l0/30_page/page_map/api.rs:L8-L12`：关键词命中
    代码片段：`pub use crate::arch::PageFlags; /// PageTable 结构体，属于该能力对外暴露的稳定接口。 #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct PageTable {`
  - `codes/os/src/mm/mod.rs:L1-L3`：关键词命中
    代码片段：`mod heap_allocator; mod address; mod frame_allocator;`
  - `codes/os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{PageTable, PageTableEntry, PTEFlags}; use super::{VirtPageNum, VirtAddr, PhysPageNum, PhysAddr}; use super::{FrameTracker, frame_alloc, frame_add_ref, enquire_refcou...`
- 与 2022 代码掐架队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/arch/thread.rs:L7-L11`：关键词命中
    代码片段：`/// Kernel stack pointer. Kept at offset 0 for arch switch assembly. pub sp: usize, /// Kernel thread pointer / CPU-local pointer. Kept at offset 1. pub tp: usize, /// Callee-sa...`
  - `kernel/src/l1/20_task/task_mm_signal/api.rs:L6-L10`：关键词命中
    代码片段：`pub mod pid; pub mod signal; pub mod task; pub mod user_memory;`
  - `codes/os/src/task/mod.rs:L6-L10`：关键词命中
    代码片段：`mod info; mod switch; mod task; mod resource; pub use resource::RLimit;`
  - `codes/os/src/task/info.rs:L87-L91`：关键词命中
    代码片段：`pub const RUSAGE_SELF:isize = 0; /* The calling process.  */ pub const RUSAGE_CHILDREN:isize = -1; /* All of its terminated child processes.  */ pub const RUSAGE_THREAD:isize =...`
- 与 2022 代码掐架队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/arch/boot.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicUsize, Ordering}; #[derive(Debug, Clone, Copy, PartialEq, Eq)]`
  - `kernel/src/arch/trap.rs:L113-L117`：关键词命中
    代码片段：`true } use core::sync::atomic::{AtomicUsize, Ordering}; pub type PageFaultHandler = fn(usize, Option<AccessKind>) -> bool;`
  - `codes/os/src/main.rs:L79-L83`：关键词命中
    代码片段：`lazy_static! { static ref CORE2_FLAG: Arc<Mutex<Core2flag>> = Arc::new(Mutex::new(Core2flag { is_in: false })); }`
  - `codes/os/src/console.rs:L1-L5`：关键词命中
    代码片段：`use crate::sbi::console_putchar; use core::fmt::{self, Write}; use spin::Mutex; struct Stdout;`
- 与 2022 代码掐架队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/l2/40_table/syscall_table/api.rs:L41-L45`：关键词命中
    代码片段：`pub fn dispatch(args: Args) -> isize { match number::decode(args.number) { number::Syscall::Getcwd => file::getcwd(args.get(0), args.get(1)), number::Syscall::Dup => file::dup(a...`
  - `kernel/src/l2/50_syscall_entry/syscall_entry/api.rs:L59-L63`：关键词命中
    代码片段：`pub fn handle_trap(tf: &mut super::TrapFrame) { let number = need::syscall_raw::number(tf); let syscall = need::number::decode(number); let args = [ need::syscall_raw::arg(tf, 0),`
  - `codes/os/src/syscall/fs.rs:L961-L965`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: isize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { if dirfd == A...`
  - `codes/os/src/syscall/mod.rs:L86-L90`：关键词命中
    代码片段：`// println!("test: run sys_getppid 1000000 times, start {:?}",start); for _ in 0..1000000{ syscall(SYSCALL_GETPPID,[0,0,0,0,0,0]); // unsafe{ //     asm!(`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2022 代码掐架队 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/arch/console.rs:L27-L27`：fn write_str
    代码片段：`fn write_str(&mut self, text: &str) -> fmt::Result {`
  - `codes/user/src/console.rs:L11-L11`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 函数/符号名重合：与 2022 代码掐架队 在“内存管理”维度发现 2 个同名定义：new, insert。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crate/ext4_rs/src/ext4_impls/balloc.rs:L16-L16`：fn new
    代码片段：`fn new(bitmap: &[u8], free_blocks: u64) -> Self {`
  - `codes/user/vendor/buddy_system_allocator/src/frame.rs:L39-L39`：fn new
    代码片段：`pub fn new() -> Self {`
  - `crate/ext4_rs/src/ext4_impls/balloc.rs:L55-L55`：fn insert
    代码片段：`fn insert(&mut self, bgid: u32, cache: BlockGroupCache) {`
  - `codes/user/vendor/buddy_system_allocator/src/frame.rs:L71-L71`：fn insert
    代码片段：`pub fn insert(&mut self, range: Range<usize>) {`
- 函数/符号名重合：与 2022 代码掐架队 在“同步机制”维度发现 2 个同名定义：read, write。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/arch/barrier.rs:L7-L7`：fn read
    代码片段：`pub fn read() {`
  - `codes/user/vendor/spin/src/rw_lock.rs:L164-L164`：fn read
    代码片段：`pub fn read(&self) -> RwLockReadGuard<T> {`
  - `kernel/src/arch/barrier.rs:L12-L12`：fn write
    代码片段：`pub fn write() {`
  - `codes/user/vendor/spin/src/rw_lock.rs:L304-L304`：fn write
    代码片段：`pub fn write(&self) -> RwLockWriteGuard<T> {`
- 文件路径重合：与 2022 代码掐架队 在“设备驱动”维度出现同名文件源码路径 `kernel/src/arch/console.rs` / `codes/os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/arch/console.rs:L1-L5`：fn put_byte
    代码片段：`use core::fmt::{self, Write}; pub fn put_byte(byte: u8) { super::platform::console_putchar(byte); }`
  - `codes/os/src/console.rs:L13-L17`：关键词命中
    代码片段：`} static CONSOLE: Mutex<ConsoleInner> = Mutex::new(ConsoleInner {}); impl Write for Stdout {`

## 相似点

- 与 2022 代码掐架队 同属 rcore-variant 风格。（置信度：medium）
- 与 2022 代码掐架队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/l0/80_device/device_core/body.rs:L1-L3`：关键词命中
    代码片段：`#[path = "virtio_mmio_blk.rs"] mod virtio_mmio_blk; #[cfg(target_arch = "loongarch64")]`
  - `kernel/src/l0/10_early_output/early_console/api.rs:L5-L9`：关键词命中
    代码片段：`/// - 'bytes'：要输出或填充的字节缓冲区。 pub fn bytes(bytes: &[u8]) { crate::arch::console::write_bytes(bytes); }`
- 与 2022 代码掐架队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/l1/30_vfs/vfs_fd/api.rs:L106-L110`：关键词命中
    代码片段：`#[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct Metadata { pub inode: u64, pub kind: NodeKind, pub mode: u16,`
  - `kernel/src/l1/30_vfs/vfs_fd/devfs.rs:L49-L53`：关键词命中
    代码片段：`display, }, inode: file_inode(kind) as u32, kind: NodeKind::CharDevice, })`
- 与 2022 代码掐架队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/arch/trap.rs:L2-L6`：关键词命中
    代码片段：`pub enum TrapKind { Unknown, Interrupt, Timer, PageFault,`
  - `kernel/src/l0/50_trap/trap_core/api.rs:L1-L3`：关键词命中
    代码片段：`pub mod trap { pub type TrapKind = crate::arch::trap::TrapKind; pub type TrapInfo = crate::arch::trap::TrapInfo;`
- 与 2022 代码掐架队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/arch/vm.rs:L15-L19`：关键词命中
    代码片段：`pub enum PageTableKind { Generic, Sv39, LoongArch64ThreeLevel, }`
  - `kernel/src/l0/30_page/page_map/api.rs:L8-L12`：关键词命中
    代码片段：`pub use crate::arch::PageFlags; /// PageTable 结构体，属于该能力对外暴露的稳定接口。 #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct PageTable {`
- 与 2022 代码掐架队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/arch/thread.rs:L7-L11`：关键词命中
    代码片段：`/// Kernel stack pointer. Kept at offset 0 for arch switch assembly. pub sp: usize, /// Kernel thread pointer / CPU-local pointer. Kept at offset 1. pub tp: usize, /// Callee-sa...`
  - `kernel/src/l1/20_task/task_mm_signal/api.rs:L6-L10`：关键词命中
    代码片段：`pub mod pid; pub mod signal; pub mod task; pub mod user_memory;`
- 与 2022 代码掐架队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/arch/boot.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicUsize, Ordering}; #[derive(Debug, Clone, Copy, PartialEq, Eq)]`
  - `kernel/src/arch/trap.rs:L113-L117`：关键词命中
    代码片段：`true } use core::sync::atomic::{AtomicUsize, Ordering}; pub type PageFaultHandler = fn(usize, Option<AccessKind>) -> bool;`
- 与 2022 代码掐架队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/l2/40_table/syscall_table/api.rs:L41-L45`：关键词命中
    代码片段：`pub fn dispatch(args: Args) -> isize { match number::decode(args.number) { number::Syscall::Getcwd => file::getcwd(args.get(0), args.get(1)), number::Syscall::Dup => file::dup(a...`
  - `kernel/src/l2/50_syscall_entry/syscall_entry/api.rs:L59-L63`：关键词命中
    代码片段：`pub fn handle_trap(tf: &mut super::TrapFrame) { let number = need::syscall_raw::number(tf); let syscall = need::number::decode(number); let args = [ need::syscall_raw::arg(tf, 0),`
- 与 2022 代码掐架队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 代码掐架队 的语言构成不同：待测作品为 {'markdown': 301273, 'toml': 2615, 'yaml': 12, 'build': 482, 'make': 28, 'rust': 32745}，历史样本为 {'json': 62, 'build': 2072, 'markdown': 7242, 'asm': 658, 'text': 1942, 'rust': 152844, 'c': 2166, 'toml': 15, 'yaml': 79}。（置信度：medium）

## 可能创新点

- 待测作品包含 397 个历史样本 2022 代码掐架队 中未见的独特函数/类型定义（如 `create_with_attr`, `calculate_extent_block_checksum`, `set_extent_block_checksum`, `fuse_mkdir`, `get_pblock_idx`, `balloc_free_blocks`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `crate/ext4_rs/src/ext4_impls/file.rs:L134-L134`：fn create_with_attr
    代码片段：`pub fn create_with_attr(&self, parent: u32, name: &str, inode_mode: u16, uid:u16, gid: u16) -> Result<Ext4InodeRef> {`
  - `crate/ext4_rs/src/ext4_impls/extents.rs:L1264-L1264`：fn calculate_extent_block_checksum
    代码片段：`fn calculate_extent_block_checksum(&self, inode_ref: &Ext4InodeRef, data: &[u8], block_addr: usize) -> u32 {`

## 附录：核验摘要

- 关键结论数：19
- 含证据关键结论数：19（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
