# 2026 T2026104869910354 比较报告

- 对比历史仓库：2025 Chronix
- 生成时间：2026-07-02T09:02:26.327406+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 Chronix（来源：赛事历史作品）：画像相似度 score=9.23；同属 ucore-variant 风格; 语言构成相似度 0.75; OS 维度重合度 1.00; 代码规模接近度 0.72

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 Chronix 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `oskernel/os/src/console.rs:L119-L123`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `oskernel/os/src/drivers/bus/mod.rs:L1-L1`：关键词命中
    代码片段：`pub mod virtio;`
  - `os/src/drivers/mod.rs:L2-L5`：关键词命中
    代码片段：`pub mod dma; pub mod net; pub mod serial; pub use block::BLOCK_DEVICE;`
  - `os/src/drivers/net/mod.rs:L1-L4`：关键词命中
    代码片段：`#[allow(unused)] pub mod virtio_net; pub mod loopback; use core::{mem, ptr::NonNull};`
- 与 2025 Chronix 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `oskernel/os/src/fs/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod ext4; mod inode; mod pipe; mod stdio;`
  - `oskernel/easy-fs/src/efs.rs:L1-L4`：关键词命中
    代码片段：`use super::{ block_cache_sync_all, get_block_cache, Bitmap, BlockDevice, DiskInode, DiskInodeType, Inode, SuperBlock, };`
  - `os/src/fs/fs.rs:L4-L8`：关键词命中
    代码片段：`use alloc::sync::{Arc, Weak}; use crate::{fs::{tmpfs::{dentry::TmpDentry, inode::{InodeContent, TmpInode, TmpSysInode}}, vfs::{dentry::{global_find_dentry, global_update_dentry,...`
  - `os/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`//! file system module: offer the file system interface //! define the file trait //! impl File for OSInode in 'inode.rs' //! impl Stdin and Stdout in 'stdio.rs' #![allow(missin...`
- 与 2025 Chronix 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `oskernel/os/src/timer.rs:L59-L63`：关键词命中
    代码片段：`let current_ms = get_time_ms(); TIMERS.exclusive_session(|timers| { while let Some(timer) = timers.peek() { if timer.expire_ms <= current_ms { wakeup_task(Arc::clone(&timer.task));`
  - `oskernel/os/src/trap/mod.rs:L8-L12`：关键词命中
    代码片段：`suspend_current_and_run_next, }; use crate::timer::{check_timer, set_next_trigger}; use alloc::string::String; use core::arch::{asm, global_asm};`
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality //! //! For rCore, we have a single trap entry point, namely '__alltraps'. At`
  - `os/src/timer/ffi.rs:L1-L4`：关键词命中
    代码片段：`use alloc::collections::btree_map::Values; use crate::timer::get_current_time_ns; use super::{get_current_time_ms, NSEC_PER_SEC};`
- 与 2025 Chronix 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `oskernel/os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `oskernel/os/src/syscall/mm.rs:L1-L5`：关键词命中
    代码片段：`use super::{EINVAL, ENOMEM}; use crate::config::PAGE_SIZE; use crate::mm::{MapPermission, MemorySet, VirtAddr}; use crate::task::{current_process, current_task}; use core::arch:...`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `os/src/mm/user.rs:L2-L6`：关键词命中
    代码片段：`use alloc::sync::Arc; use hal::{addr::{VirtAddr, VirtAddrHal}, constant::{Constant, ConstantsHal}, pagetable::MapPerm, println}; use crate::{mm::vm::UserVmPagesLocker, processor...`
- 与 2025 Chronix 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `oskernel/os/src/task/mod.rs:L8-L12`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
  - `oskernel/os/src/task/switch.S:L13-L17`：关键词命中
    代码片段：`#     next_task_cx_ptr: *const TaskContext # ) # save kernel stack of current task sd sp, 8(a0) # save ra & s0~s11 of current execution`
  - `os/src/task/fs.rs:L1-L3`：关键词命中
    代码片段：`//! file system support for Task use alloc::{sync::Arc, vec::Vec};`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Task management implementation //! //! Everything about task management, like starting and switching tasks is`
- 与 2025 Chronix 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `oskernel/os/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`mod condvar; mod mutex; mod semaphore;`
  - `oskernel/os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub trait Mutex: Sync + Send { fn lock(&self); fn unlock(&self);`
  - `os/src/sync/up.rs:L2-L6`：关键词命中
    代码片段：`use core::cell::{UnsafeCell, RefMut,RefCell}; use core::sync::atomic::{AtomicBool, Ordering}; use core::ops::{Deref, DerefMut}; use log::info;`
  - `os/src/sync/mod.rs:L6-L10`：关键词命中
    代码片段：`extern crate alloc; /// safe cell type for uniprocessor systems pub mod mutex; pub mod lazy;`
- 与 2025 Chronix 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `oskernel/os/src/syscall/fs.rs:L773-L777`：关键词命中
    代码片段：`if SYSCALL_TRACE { println!( "[syscall] write fd={} len={} => {}(invalid-fd)", fd, len, -EBADF );`
  - `oskernel/os/src/syscall/mm.rs:L84-L88`：关键词命中
    代码片段：`if brk == 0 { if SYSCALL_TRACE { println!("[syscall] brk brk=0x{:x} => 0x{:x}", brk, old_brk); } return old_brk as isize;`
  - `os/src/syscall/fd.rs:L1-L5`：关键词命中
    代码片段：`use alloc::sync::Arc; use crate::{fs::{tmpfs::{dentry::TmpDentry, inode::{EmptyFile, TmpSysInode}}, vfs::inode::InodeMode, OpenFlags}, syscall::{SysError, SysResult, SyscallId},...`
  - `os/src/syscall/fs.rs:L17-L21`：关键词命中
    代码片段：`use crate::processor::processor::{current_processor,current_task,current_user_token}; /// syscall: write pub async fn sys_write(fd: usize, buf: usize, len: usize) -> SysResult {...`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 Chronix 在“同步机制”维度发现片段级代码相似度 0.89 （token=0.83, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：inner, pub, struct, t, upsafecell。（置信度：high）
  证据：
  - `oskernel/os/src/sync/up.rs:L12-L16`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>,`
  - `os/src/sync/up.rs:L13-L17`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: UnsafeCell<T>,`
- 与 2025 Chronix 在“同步机制”维度发现片段级代码相似度 0.89 （token=0.82, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, inner, new, pub, self, t, value。（置信度：high）
  证据：
  - `oskernel/os/src/sync/up.rs:L22-L26`：fn new
    代码片段：`/// User is responsible to guarantee that inner struct is only used in /// uniprocessor. pub unsafe fn new(value: T) -> Self { Self { inner: RefCell::new(value),`
  - `os/src/sync/up.rs:L23-L27`：fn new
    代码片段：`/// User is responsible to guarantee that inner struct is only used in /// uniprocessor. pub fn new(value: T) -> Self { Self { inner: UnsafeCell::new(value),`
- 函数/符号名重合：与 2025 Chronix 在“设备驱动”维度发现 4 个同名定义：alloc, dealloc, new, write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `oskernel/easy-fs/src/bitmap.rs:L28-L28`：fn alloc
    代码片段：`pub fn alloc(&self, block_device: &Arc<dyn BlockDevice>) -> Option<usize> {`
  - `os/src/devices/net.rs:L124-L124`：fn alloc
    代码片段：`pub fn alloc(self: &Arc<Self>) -> Option<NetBuf> {`
  - `oskernel/easy-fs/src/bitmap.rs:L56-L56`：fn dealloc
    代码片段：`pub fn dealloc(&self, block_device: &Arc<dyn BlockDevice>, bit: usize) {`
  - `os/src/devices/net.rs:L148-L148`：fn dealloc
    代码片段：`pub fn dealloc(&self, idx: usize) {`
- 函数/符号名重合：与 2025 Chronix 在“文件系统”维度发现 4 个同名定义：new, drop, read, is_valid。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `oskernel/easy-fs/src/bitmap.rs:L21-L21`：fn new
    代码片段：`pub fn new(start_block_id: usize, blocks: usize) -> Self {`
  - `os/src/fs/pipe.rs:L28-L28`：fn new
    代码片段：`pub fn new(capacity: usize) -> Self {`
  - `oskernel/easy-fs/src/block_cache.rs:L31-L31`：fn drop
    代码片段：`fn drop(&mut self) {`
  - `os/src/fs/pipe.rs:L112-L112`：fn drop
    代码片段：`fn drop(&mut self) {`
- 与 2025 Chronix 在“内存管理”维度发现片段级代码相似度 0.76 （token=0.71, structure=0.86）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, init, init_frame_allocator, init_heap, pub。（置信度：medium）
  证据：
  - `oskernel/os/src/mm/mod.rs:L16-L20`：fn init
    代码片段：`}; pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`
  - `os/src/mm/mod.rs:L65-L69`：fn init
    代码片段：`/// initiate heap allocator, frame allocator and kernel space pub fn init() { allocator::init_heap(); allocator::init_frame_allocator();`
- 函数/符号名重合：与 2025 Chronix 在“同步机制”维度发现 4 个同名定义：read_block, write_block, new, read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `oskernel/easy-fs-fuse/src/main.rs:L13-L13`：fn read_block
    代码片段：`fn read_block(&self, block_id: usize, buf: &mut [u8]) {`
  - `os/src/devices/buffer_cache.rs:L79-L79`：fn read_block
    代码片段：`pub fn read_block(&self, block_id: usize, buf: &mut [u8]) {`
  - `oskernel/easy-fs-fuse/src/main.rs:L20-L20`：fn write_block
    代码片段：`fn write_block(&self, block_id: usize, buf: &[u8]) {`
  - `os/src/devices/buffer_cache.rs:L115-L115`：fn write_block
    代码片段：`pub fn write_block(&self, block_id: usize, buf: &[u8]) {`
- 结构体/类型重合：与 2025 Chronix 在“设备驱动”维度发现 4 个同名定义：BlockDevice, Stdout, CharDevice, NetDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `oskernel/easy-fs/src/block_dev.rs:L3-L3`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync + Any {`
  - `os/src/devices/mod.rs:L146-L146`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync + Any {`
  - `oskernel/os/src/console.rs:L5-L5`：struct Stdout
    代码片段：`struct Stdout;`
  - `user/src/console.rs:L8-L8`：struct Stdout
    代码片段：`struct Stdout;`
- 结构体/类型重合：与 2025 Chronix 在“文件系统”维度发现 3 个同名定义：Pipe, Stdin, Stdout。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `oskernel/os/src/fs/pipe.rs:L8-L8`：struct Pipe
    代码片段：`pub struct Pipe {`
  - `os/src/fs/pipe.rs:L18-L18`：struct Pipe
    代码片段：`pub struct Pipe {`
  - `oskernel/os/src/fs/stdio.rs:L6-L6`：struct Stdin
    代码片段：`pub struct Stdin;`
  - `os/src/fs/stdio.rs:L11-L11`：struct Stdin
    代码片段：`pub struct Stdin;`
- 函数/符号名重合：与 2025 Chronix 在“中断与异常”维度发现 2 个同名定义：handle_irq, new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `oskernel/easy-fs-fuse/src/main.rs:L27-L27`：fn handle_irq
    代码片段：`fn handle_irq(&self) {`
  - `os/src/devices/manager.rs:L246-L246`：fn handle_irq
    代码片段：`pub fn handle_irq(&self) {`
  - `oskernel/os/src/drivers/plic.rs:L56-L56`：fn new
    代码片段：`pub unsafe fn new(base_addr: usize) -> Self {`
  - `os/src/devices/plic.rs:L14-L14`：fn new
    代码片段：`pub fn new(mmio_base: usize, mmio_size: usize) -> PLIC {`
- 函数/符号名重合：与 2025 Chronix 在“内存管理”维度发现 2 个同名定义：alloc, dealloc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `oskernel/easy-fs/src/bitmap.rs:L28-L28`：fn alloc
    代码片段：`pub fn alloc(&self, block_device: &Arc<dyn BlockDevice>) -> Option<usize> {`
  - `os/src/devices/net.rs:L124-L124`：fn alloc
    代码片段：`pub fn alloc(self: &Arc<Self>) -> Option<NetBuf> {`
  - `oskernel/easy-fs/src/bitmap.rs:L56-L56`：fn dealloc
    代码片段：`pub fn dealloc(&self, block_device: &Arc<dyn BlockDevice>, bit: usize) {`
  - `os/src/devices/net.rs:L148-L148`：fn dealloc
    代码片段：`pub fn dealloc(&self, idx: usize) {`
- 结构体/类型重合：与 2025 Chronix 在“中断与异常”维度发现 1 个同名定义：PLIC。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `oskernel/os/src/drivers/plic.rs:L2-L2`：struct PLIC
    代码片段：`pub struct PLIC {`
  - `os/src/devices/plic.rs:L4-L4`：struct PLIC
    代码片段：`pub struct PLIC {`
- 结构体/类型重合：与 2025 Chronix 在“同步机制”维度发现 2 个同名定义：BlockDevice, NetDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `oskernel/easy-fs/src/block_dev.rs:L3-L3`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync + Any {`
  - `os/src/devices/mod.rs:L146-L146`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync + Any {`
  - `oskernel/os/src/drivers/net/mod.rs:L15-L15`：trait NetDevice
    代码片段：`pub trait NetDevice: Send + Sync + Any {`
  - `os/src/devices/mod.rs:L160-L160`：trait NetDevice
    代码片段：`pub trait NetDevice: Send + Sync + Any {`
- 文件路径重合：与 2025 Chronix 在“同步机制”维度出现同名文件源码路径 `oskernel/os/src/sync/up.rs` / `os/src/sync/up.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `oskernel/os/src/sync/up.rs:L12-L16`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>,`
  - `os/src/sync/up.rs:L2-L6`：关键词命中
    代码片段：`use core::cell::{UnsafeCell, RefMut,RefCell}; use core::sync::atomic::{AtomicBool, Ordering}; use core::ops::{Deref, DerefMut}; use log::info;`
- 文件路径重合：与 2025 Chronix 在“系统调用”维度出现同名文件源码路径 `oskernel/os/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `oskernel/os/src/syscall/fs.rs:L773-L777`：关键词命中
    代码片段：`if SYSCALL_TRACE { println!( "[syscall] write fd={} len={} => {}(invalid-fd)", fd, len, -EBADF );`
  - `os/src/syscall/fs.rs:L17-L21`：关键词命中
    代码片段：`use crate::processor::processor::{current_processor,current_task,current_user_token}; /// syscall: write pub async fn sys_write(fd: usize, buf: usize, len: usize) -> SysResult {...`

## 相似点

- 与 2025 Chronix 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 Chronix 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `oskernel/os/src/console.rs:L119-L123`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `oskernel/os/src/drivers/bus/mod.rs:L1-L1`：关键词命中
    代码片段：`pub mod virtio;`
- 与 2025 Chronix 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `oskernel/os/src/fs/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod ext4; mod inode; mod pipe; mod stdio;`
  - `oskernel/easy-fs/src/efs.rs:L1-L4`：关键词命中
    代码片段：`use super::{ block_cache_sync_all, get_block_cache, Bitmap, BlockDevice, DiskInode, DiskInodeType, Inode, SuperBlock, };`
- 与 2025 Chronix 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `oskernel/os/src/timer.rs:L59-L63`：关键词命中
    代码片段：`let current_ms = get_time_ms(); TIMERS.exclusive_session(|timers| { while let Some(timer) = timers.peek() { if timer.expire_ms <= current_ms { wakeup_task(Arc::clone(&timer.task));`
  - `oskernel/os/src/trap/mod.rs:L8-L12`：关键词命中
    代码片段：`suspend_current_and_run_next, }; use crate::timer::{check_timer, set_next_trigger}; use alloc::string::String; use core::arch::{asm, global_asm};`
- 与 2025 Chronix 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `oskernel/os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `oskernel/os/src/syscall/mm.rs:L1-L5`：关键词命中
    代码片段：`use super::{EINVAL, ENOMEM}; use crate::config::PAGE_SIZE; use crate::mm::{MapPermission, MemorySet, VirtAddr}; use crate::task::{current_process, current_task}; use core::arch:...`
- 与 2025 Chronix 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `oskernel/os/src/task/mod.rs:L8-L12`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
  - `oskernel/os/src/task/switch.S:L13-L17`：关键词命中
    代码片段：`#     next_task_cx_ptr: *const TaskContext # ) # save kernel stack of current task sd sp, 8(a0) # save ra & s0~s11 of current execution`
- 与 2025 Chronix 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `oskernel/os/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`mod condvar; mod mutex; mod semaphore;`
  - `oskernel/os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub trait Mutex: Sync + Send { fn lock(&self); fn unlock(&self);`
- 与 2025 Chronix 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `oskernel/os/src/syscall/fs.rs:L773-L777`：关键词命中
    代码片段：`if SYSCALL_TRACE { println!( "[syscall] write fd={} len={} => {}(invalid-fd)", fd, len, -EBADF );`
  - `oskernel/os/src/syscall/mm.rs:L84-L88`：关键词命中
    代码片段：`if brk == 0 { if SYSCALL_TRACE { println!("[syscall] brk brk=0x{:x} => 0x{:x}", brk, old_brk); } return old_brk as isize;`
- 与 2025 Chronix 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 Chronix 的语言构成不同：待测作品为 {'markdown': 9090, 'build': 336, 'json': 21, 'rust': 22092, 'toml': 20, 'asm': 167}，历史样本为 {'json': 20, 'build': 353, 'markdown': 2055, 'toml': 139, 'make': 405, 'rust': 43626, 'asm': 381}。（置信度：medium）

## 可能创新点

- 待测作品包含 251 个历史样本 2025 Chronix 中未见的独特函数/类型定义（如 `open_file_for_fd`, `get_threshold`, `read_symlink_target`, `find_testcode_scripts`, `print_root_dir_entries`, `hart_id_with_priority`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `oskernel/os/src/fs/inode.rs:L225-L225`：fn open_file_for_fd
    代码片段：`pub fn open_file_for_fd(name: &str, flags: OpenFlags) -> Option<Arc<dyn File + Send + Sync>> {`
  - `oskernel/os/src/drivers/plic.rs:L105-L105`：fn get_threshold
    代码片段：`pub fn get_threshold(&mut self, hart_id: usize, target_priority: IntrTargetPriority) -> u32 {`

## 附录：核验摘要

- 关键结论数：29
- 含证据关键结论数：29（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
