# 2026 T2026100019911468 比较报告

- 对比历史仓库：2022 柯南队
- 生成时间：2026-07-02T04:57:37.029764+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 柯南队（来源：赛事历史作品）：画像相似度 score=9.60；同属 ucore-variant 风格; 语言构成相似度 0.84; OS 维度重合度 1.00; 代码规模接近度 0.92

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 柯南队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `xv6-k210/kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/hal/virtio_disk.c:L1-L4`：关键词命中
    代码片段：`// // driver for qemu's virtio disk device. // uses qemu's mmio interface to virtio. // qemu presents a "legacy" virtio interface.`
- 与 2022 柯南队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/file.c:L157-L161`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `xv6-k210/kernel/include/file.h:L33-L37`：关键词命中
    代码片段：`struct file*    filedup(struct file*); void            fileinit(void); int             fileread(struct file*, uint64, int n); int             filestat(struct file*, uint64 addr)...`
  - `kernel/fs/fs.c:L18-L22`：关键词命中
    代码片段：`/** * File system inode * */`
  - `kernel/fs/file.c:L112-L116`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
- 与 2022 柯南队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
  - `xv6-k210/kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
  - `kernel/timer.c:L1-L3`：关键词命中
    代码片段：`// Timer Interrupt handler #ifndef __DEBUG_timer`
  - `kernel/hal/plic.c:L5-L9`：关键词命中
    代码片段：`#include "hal/riscv.h" #include "sbi.h" #include "hal/plic.h" #include "sched/proc.h" #include "printf.h"`
- 与 2022 柯南队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`
  - `xv6-k210/kernel/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/kalloc.h" #include "include/string.h" #include "include/printf.h"`
  - `kernel/mm/pm.c:L39-L43`：关键词命中
    代码片段：`#define SINGLE_PAGE_NUM 		400 uint64 START_SINGLE = PHYSTOP - SINGLE_PAGE_NUM * PGSIZE; #define __enter_mul_cs \`
  - `kernel/mm/vm.c:L20-L24`：关键词命中
    代码片段：`#define MAX_PAGES_NUM 0x600 #define PTE_COW PTE_RSW1  // Use it to mark a COW page /*`
- 与 2022 柯南队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/proc.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/proc.h" #include "include/intr.h" #include "include/kalloc.h"`
  - `xv6-k210/kernel/include/proc.h:L32-L36`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `kernel/sched/proc.c:L5-L9`：关键词命中
    代码片段：`#endif #define __module_name__ 	"proc" #include "types.h"`
  - `kernel/sched/swtch.S:L1-L5`：关键词命中
    代码片段：`# Context switch # #   void swtch(struct context *old, struct context *new); # # Save current registers in old. Load from new.`
- 与 2022 柯南队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/bio.c:L18-L22`：关键词命中
    代码片段：`#include "include/types.h" #include "include/param.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/riscv.h"`
  - `xv6-k210/kernel/exec.c:L4-L8`：关键词命中
    代码片段：`#include "include/memlayout.h" #include "include/riscv.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/proc.h"`
  - `kernel/sync/spinlock.c:L5-L9`：关键词命中
    代码片段：`#include "param.h" #include "memlayout.h" #include "sync/spinlock.h" #include "hal/riscv.h" #include "sched/proc.h"`
  - `kernel/sync/sleeplock.c:L6-L10`：关键词命中
    代码片段：`#include "param.h" #include "memlayout.h" #include "sync/spinlock.h" #include "sched/proc.h" #include "sync/sleeplock.h"`
- 与 2022 柯南队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `xv6-k210/kernel/syscall.c:L6-L10`：关键词命中
    代码片段：`#include "include/spinlock.h" #include "include/proc.h" #include "include/syscall.h" #include "include/sysinfo.h" #include "include/kalloc.h"`
  - `xv6-k210/kernel/include/syscall.h:L10-L14`：关键词命中
    代码片段：`int argaddr(int n, uint64 *ip); int argstr(int n, char *buf, int max); void syscall(void); #endif`
  - `kernel/syscall/sysmem.c:L9-L13`：关键词命中
    代码片段：`#include "param.h" #include "errno.h" #include "syscall.h" #include "sched/proc.h" #include "mm/vm.h"`
  - `kernel/syscall/syscall.c:L3-L7`：关键词命中
    代码片段：`#endif #define __module_name__ 	"syscall" #include "types.h"`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2022 柯南队 在“设备驱动”维度发现片段级代码相似度 0.88 （token=0.91, structure=0.83）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：backspace, c, consputc, if, int, void。（置信度：high）
  证据：
  - `xv6-k210/kernel/console.c:L35-L39`：fn consputc
    代码片段：`} void consputc(int c) { if(c == BACKSPACE){ // if the user typed backspace, overwrite with a space.`
  - `kernel/console.c:L39-L43`：fn consputc
    代码片段：`#define C(x)  ((x)-'@')  // Control-x void consputc(int c) { if(c == BACKSPACE){ // if the user typed backspace, overwrite with a space.`
- 与 2022 柯南队 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：addr, f, file, fileread, int, n, r, struct。（置信度：high）
  证据：
  - `xv6-k210/kernel/file.c:L157-L161`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `kernel/fs/file.c:L112-L116`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
- 函数/符号名重合：与 2022 柯南队 在“中断与异常”维度发现 3 个同名定义：plicinit, timerinit, timer_tick。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/kernel/plic.c:L28-L28`：fn plicinit
    代码片段：`void plicinit(void) {`
  - `kernel/hal/plic.c:L24-L24`：fn plicinit
    代码片段：`void plicinit(void) {`
  - `xv6-k210/kernel/timer.c:L16-L16`：fn timerinit
    代码片段：`void timerinit() {`
  - `kernel/timer.c:L21-L21`：fn timerinit
    代码片段：`void timerinit() {`
- 与 2022 柯南队 在“中断与异常”维度发现片段级代码相似度 0.76 （token=0.75, structure=0.78）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：initlock, ticks, tickslock, timerinit, void。（置信度：medium）
  证据：
  - `xv6-k210/kernel/timer.c:L14-L18`：fn timerinit
    代码片段：`uint ticks; void timerinit() { initlock(&tickslock, "time"); #ifdef DEBUG`
  - `kernel/timer.c:L19-L23`：fn timerinit
    代码片段：`uint64 ticks; void timerinit() { initlock(&tickslock, "time"); ticks = 0;`
- 与 2022 柯南队 在“内存管理”维度发现片段级代码相似度 0.73 （token=0.67, structure=0.86）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：char, extern, next, run, struct。（置信度：medium）
  证据：
  - `xv6-k210/kernel/kalloc.c:L17-L21`：struct run
    代码片段：`extern char kernel_end[]; // first address after kernel. struct run { struct run *next; };`
  - `kernel/mm/pm.c:L24-L28`：struct run
    代码片段：`extern char boot_stack_top[];	// first address after kernel bootstack struct run { struct run *next; uint64 npage;`
- 与 2022 柯南队 在“系统调用”维度发现片段级代码相似度 0.65 （token=0.53, structure=0.86）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：int, scause, uint64。（置信度：medium）
  证据：
  - `xv6-k210/kernel/trap.c:L211-L215`：fn devintr
    代码片段：`//          1 if other device, //          0 if not recognized. int devintr(void) { uint64 scause = r_scause();`
  - `kernel/trap/trap.c:L244-L248`：fn handle_intr
    代码片段：`// check if it's an interrupt, and handle it // returns int handle_intr(uint64 scause) { #ifdef DEBUG static int cnt = 0;`
- 宏名重合：与 2022 柯南队 在“设备驱动”维度发现 4 个同名定义：BACKSPACE, INPUT_BUF, __CONSOLE_H, UART。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/kernel/console.c:L24-L24`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100`
  - `kernel/console.c:L38-L38`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100`
  - `xv6-k210/kernel/console.c:L51-L51`：macro INPUT_BUF
    代码片段：`#define INPUT_BUF 128`
  - `kernel/console.c:L56-L56`：macro INPUT_BUF
    代码片段：`#define INPUT_BUF 128`
- 宏名重合：与 2022 柯南队 在“文件系统”维度发现 4 个同名定义：MAXFILE, BUFSZ, VIRT_OFFSET, UART_V。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/xv6-user/usertests.c:L20-L20`：macro MAXFILE
    代码片段：`#define MAXFILE 512`
  - `xv6-user/usertests.c:L20-L20`：macro MAXFILE
    代码片段：`#define MAXFILE 512`
  - `xv6-k210/xv6-user/usertests.c:L21-L21`：macro BUFSZ
    代码片段：`#define BUFSZ  ((MAXOPBLOCKS+2)*BSIZE)`
  - `xv6-user/usertests.c:L21-L21`：macro BUFSZ
    代码片段：`#define BUFSZ  ((MAXOPBLOCKS+2)*BSIZE)`
- 宏名重合：与 2022 柯南队 在“中断与异常”维度发现 4 个同名定义：CLINT, CLINT_V, PLIC, PLIC_V。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/kernel/include/memlayout.h:L62-L62`：macro CLINT
    代码片段：`#define CLINT                   0x0L`
  - `include/memlayout.h:L54-L54`：macro CLINT
    代码片段：`#define CLINT                   0x02000000L`
  - `xv6-k210/kernel/include/memlayout.h:L63-L63`：macro CLINT_V
    代码片段：`#define CLINT_V                 0x0L`
  - `include/memlayout.h:L55-L55`：macro CLINT_V
    代码片段：`#define CLINT_V                 (CLINT + VIRT_OFFSET)`
- 宏名重合：与 2022 柯南队 在“同步机制”维度发现 4 个同名定义：SD_CMD16, SD_CMD17, SD_CMD24, BUFSZ。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/kernel/sdcard.c:L87-L87`：macro SD_CMD16
    代码片段：`#define SD_CMD16 	16 		// SET_BLOCK_SIZE`
  - `kernel/hal/sdcard.c:L119-L119`：macro SD_CMD16
    代码片段：`#define SD_CMD16 	16 		// SET_BLOCK_SIZE`
  - `xv6-k210/kernel/sdcard.c:L88-L88`：macro SD_CMD17
    代码片段：`#define SD_CMD17 	17 		// READ_SINGLE_BLOCK`
  - `kernel/hal/sdcard.c:L120-L120`：macro SD_CMD17
    代码片段：`#define SD_CMD17 	17 		// READ_SINGLE_BLOCK`
- 宏名重合：与 2022 柯南队 在“系统调用”维度发现 4 个同名定义：SYSCTL, SYSCTL_V, TRAPFRAME, NFILE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `xv6-k210/kernel/include/memlayout.h:L108-L108`：macro SYSCTL
    代码片段：`#define SYSCTL                  0x50440000`
  - `include/memlayout.h:L79-L79`：macro SYSCTL
    代码片段：`#define SYSCTL                  0x50440000`
  - `xv6-k210/kernel/include/memlayout.h:L118-L118`：macro SYSCTL_V
    代码片段：`#define SYSCTL_V                (0x50440000 + VIRT_OFFSET)`
  - `include/memlayout.h:L89-L89`：macro SYSCTL_V
    代码片段：`#define SYSCTL_V                (0x50440000 + VIRT_OFFSET)`
- 函数/符号名重合：与 2022 柯南队 在“设备驱动”维度发现 1 个同名定义：consputc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `xv6-k210/kernel/console.c:L37-L37`：fn consputc
    代码片段：`void consputc(int c) {`
  - `kernel/console.c:L41-L41`：fn consputc
    代码片段：`void consputc(int c) {`
- 函数/符号名重合：与 2022 柯南队 在“内存管理”维度发现 2 个同名定义：sd_get_response_R3_rest, sd_get_response_R7_rest。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `xv6-k210/kernel/sdcard.c:L113-L113`：fn sd_get_response_R3_rest
    代码片段：`static void sd_get_response_R3_rest(uint8 *frame) {`
  - `kernel/hal/sdcard.c:L170-L170`：fn sd_get_response_R3_rest
    代码片段：`static void sd_get_response_R3_rest(uint8 *frame) {`
  - `xv6-k210/kernel/sdcard.c:L121-L121`：fn sd_get_response_R7_rest
    代码片段：`static void sd_get_response_R7_rest(uint8 *frame) {`
  - `kernel/hal/sdcard.c:L178-L178`：fn sd_get_response_R7_rest
    代码片段：`static void sd_get_response_R7_rest(uint8 *frame) {`
- 函数/符号名重合：与 2022 柯南队 在“调度与任务管理”维度发现 1 个同名定义：switch_to_SPI_mode。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `xv6-k210/kernel/sdcard.c:L125-L125`：fn switch_to_SPI_mode
    代码片段：`static int switch_to_SPI_mode(void) {`
  - `kernel/hal/sdcard.c:L182-L182`：fn switch_to_SPI_mode
    代码片段：`static int switch_to_SPI_mode(void) {`
- 函数/符号名重合：与 2022 柯南队 在“同步机制”维度发现 1 个同名定义：check_block_size。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `xv6-k210/kernel/sdcard.c:L232-L232`：fn check_block_size
    代码片段：`static int check_block_size(void) {`
  - `kernel/hal/sdcard.c:L287-L287`：fn check_block_size
    代码片段：`static int check_block_size(void) {`
- 宏名重合：与 2022 柯南队 在“内存管理”维度发现 2 个同名定义：TRAMPOLINE, TRAPFRAME。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `xv6-k210/kernel/include/memlayout.h:L149-L149`：macro TRAMPOLINE
    代码片段：`#define TRAMPOLINE              (MAXVA - PGSIZE)`
  - `include/memlayout.h:L106-L106`：macro TRAMPOLINE
    代码片段：`#define TRAMPOLINE              (MAXVA - PGSIZE)`
  - `xv6-k210/kernel/include/memlayout.h:L167-L167`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME               (TRAMPOLINE - PGSIZE)`
  - `include/memlayout.h:L128-L128`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME               (VKSTACK + 2 * PGSIZE)`
- 文件路径重合：与 2022 柯南队 在“设备驱动”维度出现同名文件源码路径 `xv6-k210/kernel/console.c` / `kernel/console.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `xv6-k210/kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 文件路径重合：与 2022 柯南队 在“设备驱动”维度出现同名文件源码路径 `xv6-k210/kernel/virtio_disk.c` / `kernel/hal/virtio_disk.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `xv6-k210/kernel/virtio_disk.c:L196-L200`：struct virtio_blk_outhdr
    代码片段：`// qemu's virtio-blk.c reads them. struct virtio_blk_outhdr { uint32 type; uint32 reserved;`
  - `kernel/hal/virtio_disk.c:L1-L4`：关键词命中
    代码片段：`// // driver for qemu's virtio disk device. // uses qemu's mmio interface to virtio. // qemu presents a "legacy" virtio interface.`
- 文件路径重合：与 2022 柯南队 在“文件系统”维度出现同名文件源码路径 `xv6-k210/kernel/file.c` / `kernel/fs/file.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `xv6-k210/kernel/file.c:L157-L161`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `kernel/fs/file.c:L112-L116`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
- 文件路径重合：与 2022 柯南队 在“中断与异常”维度出现同名文件源码路径 `xv6-k210/kernel/plic.c` / `kernel/hal/plic.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `xv6-k210/kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
  - `kernel/hal/plic.c:L5-L9`：关键词命中
    代码片段：`#include "hal/riscv.h" #include "sbi.h" #include "hal/plic.h" #include "sched/proc.h" #include "printf.h"`
- 文件路径重合：与 2022 柯南队 在“中断与异常”维度出现同名文件源码路径 `xv6-k210/kernel/trap.c` / `kernel/trap/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `xv6-k210/kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
  - `kernel/trap/trap.c:L5-L9`：关键词命中
    代码片段：`#endif #define __module_name__ 	"trap" #include "types.h"`
- 文件路径重合：与 2022 柯南队 在“内存管理”维度出现同名文件源码路径 `xv6-k210/kernel/vm.c` / `kernel/mm/vm.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `xv6-k210/kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`
  - `kernel/mm/vm.c:L20-L24`：关键词命中
    代码片段：`#define MAX_PAGES_NUM 0x600 #define PTE_COW PTE_RSW1  // Use it to mark a COW page /*`

## 相似点

- 与 2022 柯南队 同属 ucore-variant 风格。（置信度：medium）
- 与 2022 柯南队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `xv6-k210/kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 与 2022 柯南队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/file.c:L157-L161`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `xv6-k210/kernel/include/file.h:L33-L37`：关键词命中
    代码片段：`struct file*    filedup(struct file*); void            fileinit(void); int             fileread(struct file*, uint64, int n); int             filestat(struct file*, uint64 addr)...`
- 与 2022 柯南队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
  - `xv6-k210/kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
- 与 2022 柯南队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`
  - `xv6-k210/kernel/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/kalloc.h" #include "include/string.h" #include "include/printf.h"`
- 与 2022 柯南队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/proc.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/proc.h" #include "include/intr.h" #include "include/kalloc.h"`
  - `xv6-k210/kernel/include/proc.h:L32-L36`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2022 柯南队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/bio.c:L18-L22`：关键词命中
    代码片段：`#include "include/types.h" #include "include/param.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/riscv.h"`
  - `xv6-k210/kernel/exec.c:L4-L8`：关键词命中
    代码片段：`#include "include/memlayout.h" #include "include/riscv.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/proc.h"`
- 与 2022 柯南队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `xv6-k210/kernel/syscall.c:L6-L10`：关键词命中
    代码片段：`#include "include/spinlock.h" #include "include/proc.h" #include "include/syscall.h" #include "include/sysinfo.h" #include "include/kalloc.h"`
  - `xv6-k210/kernel/include/syscall.h:L10-L14`：关键词命中
    代码片段：`int argaddr(int n, uint64 *ip); int argstr(int n, char *buf, int max); void syscall(void); #endif`
- 与 2022 柯南队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 柯南队 的语言构成不同：待测作品为 {'json': 35, 'build': 617, 'markdown': 5505, 'asm': 1191, 'text': 3372, 'c': 29141, 'toml': 14, 'rust': 1926}，历史样本为 {'json': 35, 'build': 376, 'markdown': 3649, 'c': 39154, 'asm': 521, 'toml': 14, 'rust': 1931}。（置信度：medium）

## 可能创新点

- 待测作品包含 110 个历史样本 2022 柯南队 中未见的独特函数/类型定义（如 `sd_write_data_dma`, `wcsncmp`, `snstr`, `wnstr`, `short_name_entry`, `long_name_entry`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `xv6-k210/kernel/sdcard.c:L39-L39`：fn sd_write_data_dma
    代码片段：`static void sd_write_data_dma(uint8 const *data_buff, uint32 length) {`
  - `xv6-k210/kernel/string.c:L125-L125`：fn wcsncmp
    代码片段：`int wcsncmp(wchar const *s1, wchar const *s2, int len) {`

## 附录：核验摘要

- 关键结论数：37
- 含证据关键结论数：37（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
