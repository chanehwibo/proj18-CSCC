# 2026 T2026104889910567 比较报告

- 对比历史仓库：2025 OSakura
- 生成时间：2026-07-02T09:03:17.799425+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 OSakura（来源：赛事历史作品）：画像相似度 score=9.81；同属 ucore-variant 风格; 语言构成相似度 0.96; OS 维度重合度 1.00; 代码规模接近度 0.89

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 OSakura 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/fs/uartdev.c:L1-L3`：关键词命中
    代码片段：`/* UART character device — provides stdin/stdout/stderr backing via VFS. * * The file's f_inode is NULL so file_read()/file_write() skip the VFS layer`
  - `src/hal/blk.c:L1-L3`：关键词命中
    代码片段：`#include <hal/blk.h> #include <misc/log.h> #include <misc/string.h>`
  - `OSakura-rv/kernel/dev/uart.c:L1-L3`：关键词命中
    代码片段：`// low-level driver routines for 16550a UART #include "memlayout.h" #include "dev/uart.h"`
  - `OSakura-rv/kernel/dev/console.c:L1-L4`：关键词命中
    代码片段：`#include <stdarg.h> #include "dev/console.h" #include "dev/uart.h" #include "mem/vmem.h"`
- 与 2025 OSakura 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/fs/vfs.c:L1-L3`：关键词命中
    代码片段：`#include <fs/vfs.h> #include <misc/log.h> #include <misc/string.h>`
  - `src/fs/file.c:L1-L4`：关键词命中
    代码片段：`#include <fs/file.h> #include <fs/inode.h> #include <fs/dentry.h> #include <misc/log.h>`
  - `OSakura-rv/kernel/fs/ext4/ext4_dir.c:L11-L15`：关键词命中
    代码片段：`// pip是一个目录, 在这个目录里寻找名为filename的文件 // 找到了返回inode 没找到返回NULL // 这个函数是从磁盘里读入inode的唯一路径 // 注意: pip应当有效且上锁`
  - `OSakura-rv/kernel/fs/ext4/ext4_sys.c:L271-L275`：关键词命中
    代码片段：`} } /* 到此获得了一个上了锁的inode */ file = ext4_file_alloc();`
- 与 2025 OSakura 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/trap/trap.c:L1-L3`：关键词命中
    代码片段：`#include <trap/trap.h> #include <task/proc.h> #include <task/sched.h>`
  - `src/trap/trampoline.S:L16-L20`：关键词命中
    代码片段：`uservec: # # trap.c sets stvec to point here, so # traps from user space start here, # in supervisor mode, but with a`
  - `OSakura-rv/kernel/dev/plic.c:L1-L3`：关键词命中
    代码片段：`// Platform-level interrupt controller #include "riscv.h"`
  - `OSakura-rv/kernel/dev/timer.c:L2-L6`：关键词命中
    代码片段：`#include "sbi.h" #include "riscv.h" #include "dev/timer.h" #include "lib/print.h"`
- 与 2025 OSakura 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/mm/pm.c:L4-L8`：关键词命中
    代码片段：`#include <misc/list.h> #include <mm/early.h> #include <mm/kalloc.h> #include <mm/buddy.h> #include <misc/log.h>`
  - `src/mm/vm.c:L1-L4`：关键词命中
    代码片段：`#include <misc/log.h> #include <mm/pagetable.h> #include <mm/vm.h> #include <mm/kalloc.h>`
  - `OSakura-rv/kernel/mem/kvm.c:L16-L20`：关键词命中
    代码片段：`// 申请L2内核页表空间 kernel_pagetable = (pgtbl_t)pmem_alloc_pages(1, true); // printf("kernel pagetable = %p\n",kernel_pagetable); assert(kernel_pagetable != NULL, "kvm.c->kvm_init: 1\...`
  - `OSakura-rv/kernel/mem/uvm.c:L41-L45`：关键词命中
    代码片段：`// 释放region资源, 同时释放占用的物理页 void uvm_region_free(pgtbl_t pagetable, vm_region_t* region) { uvm_unmappages(pagetable, region->start, region->npages, true);`
- 与 2025 OSakura 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/task/proc.c:L1-L3`：关键词命中
    代码片段：`#include <task/proc.h> #include <task/sched.h> #include <config.h>`
  - `src/task/sched.c:L1-L3`：关键词命中
    代码片段：`#include <task/sched.h> #include <misc/list.h> #include <misc/log.h>`
  - `OSakura-rv/kernel/proc/cpu.c:L1-L3`：关键词命中
    代码片段：`#include "proc/cpu.h" #include "lock/lock.h" #include "riscv.h"`
  - `OSakura-rv/kernel/proc/exec.c:L1-L3`：关键词命中
    代码片段：`#include "proc/elf.h" #include "proc/cpu.h" #include "mem/vmem.h"`
- 与 2025 OSakura 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/sync/spinlock.c:L1-L3`：关键词命中
    代码片段：`#include <sync/spinlock.h> #include <trap/trap.h> #include <arch/cpu.h>`
  - `src/fs/vfs.c:L5-L9`：关键词命中
    代码片段：`#include <misc/complier.h> #include <mm/kalloc.h> #include <sync/spinlock.h> static struct {`
  - `OSakura-rv/kernel/lock/spinlock.c:L1-L3`：关键词命中
    代码片段：`#include "lock/lock.h" #include "proc/cpu.h" #include "lib/print.h"`
  - `OSakura-rv/kernel/lock/sleeplock.c:L1-L4`：关键词命中
    代码片段：`#include "proc/cpu.h" #include "lock/lock.h" void sleeplock_init(sleeplock_t* lock, char* name)`
- 与 2025 OSakura 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/syscall/ipc.c:L18-L22`：关键词命中
    代码片段：`*   恢复保存的 saved_tf 到 trapframe，清除 in_signal_handler 标志 */ #include <syscall/syscall.h> #include <task/proc.h> #include <task/sched.h>`
  - `src/syscall/file.c:L17-L21`：关键词命中
    代码片段：`*   fdnum 仅提取 fd 号做基本校验，再手动 fd_free + vfs_close */ #include <syscall/syscall.h> #include <hal/uart.h> #include <task/proc.h>`
  - `OSakura-rv/kernel/syscall/syscall.c:L2-L6`：关键词命中
    代码片段：`#include "proc/cpu.h"          // myproc #include "mem/vmem.h"          // uvm_copy #include "syscall/sysnum.h" #include "syscall/sysfile.h" #include "syscall/sysproc.h"`
  - `OSakura-rv/kernel/syscall/sysfile.c:L1-L3`：关键词命中
    代码片段：`#include "syscall/sysfile.h" #include "syscall/syscall.h" #include "fs/procfs.h"`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 宏名重合：与 2025 OSakura 在“设备驱动”维度发现 4 个同名定义：UART_BASE, VIRTIO_CONFIG_S_ACKNOWLEDGE, VIRTIO_CONFIG_S_DRIVER, VIRTIO_CONFIG_S_DRIVER_OK。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/hal/uart.h:L6-L6`：macro UART_BASE
    代码片段：`#define UART_BASE 0x10000000ULL`
  - `OSakura-rv/include/memlayout.h:L32-L32`：macro UART_BASE
    代码片段：`#define UART_BASE  0x10000000ul`
  - `include/hal/virtio.h:L77-L77`：macro VIRTIO_CONFIG_S_ACKNOWLEDGE
    代码片段：`#define VIRTIO_CONFIG_S_ACKNOWLEDGE BIT(0)	  /* 设备已确认 */`
  - `OSakura-rv/include/dev/virtio.h:L26-L26`：macro VIRTIO_CONFIG_S_ACKNOWLEDGE
    代码片段：`#define VIRTIO_CONFIG_S_ACKNOWLEDGE 1`
- 宏名重合：与 2025 OSakura 在“文件系统”维度发现 4 个同名定义：O_RDONLY, O_WRONLY, O_RDWR, O_DIRECTORY。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/fs/fs_types.h:L49-L49`：macro O_RDONLY
    代码片段：`#define O_RDONLY 00000000`
  - `OSakura-rv/kernel/fs/fat32/fat32_sys.c:L17-L17`：macro O_RDONLY
    代码片段：`#define O_RDONLY    0x000`
  - `include/fs/fs_types.h:L50-L50`：macro O_WRONLY
    代码片段：`#define O_WRONLY 00000001`
  - `OSakura-rv/kernel/fs/fat32/fat32_sys.c:L18-L18`：macro O_WRONLY
    代码片段：`#define O_WRONLY    0x001`
- 宏名重合：与 2025 OSakura 在“中断与异常”维度发现 4 个同名定义：MSTATUS_MIE, SSTATUS_SPIE, SSTATUS_UPIE, SSTATUS_SIE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/hal/riscv.h:L20-L20`：macro MSTATUS_MIE
    代码片段：`#define MSTATUS_MIE (1L << 3)    // machine-mode interrupt enable.`
  - `OSakura-rv/include/riscv.h:L18-L18`：macro MSTATUS_MIE
    代码片段：`#define MSTATUS_MIE (1L << 3)    // machine-mode interrupt enable.`
  - `include/hal/riscv.h:L48-L48`：macro SSTATUS_SPIE
    代码片段：`#define SSTATUS_SPIE (1L << 5) // Supervisor Previous Interrupt Enable`
  - `OSakura-rv/include/riscv.h:L42-L42`：macro SSTATUS_SPIE
    代码片段：`#define SSTATUS_SPIE (1L << 5) // Supervisor Previous Interrupt Enable`
- 宏名重合：与 2025 OSakura 在“内存管理”维度发现 4 个同名定义：PTE_V, PTE_R, PTE_W, PTE_X。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/arch/loongarch64/mmu.h:L5-L5`：macro PTE_V
    代码片段：`#define PTE_V (1ULL << 0)`
  - `OSakura-rv/include/mem/vmem.h:L51-L51`：macro PTE_V
    代码片段：`#define PTE_V (1 << 0)`
  - `include/arch/loongarch64/mmu.h:L6-L6`：macro PTE_R
    代码片段：`#define PTE_R (1ULL << 1)`
  - `OSakura-rv/include/mem/vmem.h:L52-L52`：macro PTE_R
    代码片段：`#define PTE_R (1 << 1)`
- 结构体/类型重合：与 2025 OSakura 在“设备驱动”维度发现 4 个同名定义：virtq_desc, virtq_avail, virtq_used_elem, virtq_used。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/hal/virtio.h:L95-L95`：struct virtq_desc
    代码片段：`struct virtq_desc {`
  - `OSakura-rv/include/dev/virtio.h:L45-L45`：struct virtq_desc
    代码片段：`struct virtq_desc {`
  - `include/hal/virtio.h:L105-L105`：struct virtq_avail
    代码片段：`struct virtq_avail {`
  - `OSakura-rv/include/dev/virtio.h:L55-L55`：struct virtq_avail
    代码片段：`struct virtq_avail {`
- 结构体/类型重合：与 2025 OSakura 在“文件系统”维度发现 3 个同名定义：ext4_superblock, ext4_group_desc, ext4_inode。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/fs/ext4.h:L31-L31`：struct ext4_superblock
    代码片段：`struct ext4_superblock {`
  - `OSakura-rv/include/fs/ext4.h:L28-L28`：struct ext4_superblock
    代码片段：`typedef struct ext4_superblock {`
  - `include/fs/ext4.h:L126-L126`：struct ext4_group_desc
    代码片段：`struct ext4_group_desc {`
  - `OSakura-rv/include/fs/ext4.h:L40-L40`：struct ext4_group_desc
    代码片段：`typedef struct ext4_group_desc {`
- 宏名重合：与 2025 OSakura 在“同步机制”维度发现 1 个同名定义：BLOCK_SIZE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/config.h:L34-L34`：macro BLOCK_SIZE
    代码片段：`#define BLOCK_SIZE 512`
  - `OSakura-rv/include/common.h:L31-L31`：macro BLOCK_SIZE
    代码片段：`#define BLOCK_SIZE 4096           // block大小`
- 结构体/类型重合：与 2025 OSakura 在“中断与异常”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/arch/loongarch64/trap.h:L5-L5`：struct trapframe
    代码片段：`struct trapframe {`
  - `OSakura-rv/include/proc/proc.h:L15-L15`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 结构体/类型重合：与 2025 OSakura 在“内存管理”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/arch/loongarch64/trap.h:L5-L5`：struct trapframe
    代码片段：`struct trapframe {`
  - `OSakura-rv/include/proc/proc.h:L15-L15`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 结构体/类型重合：与 2025 OSakura 在“同步机制”维度发现 1 个同名定义：ext4_superblock。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/fs/ext4.h:L31-L31`：struct ext4_superblock
    代码片段：`struct ext4_superblock {`
  - `OSakura-rv/include/fs/ext4.h:L28-L28`：struct ext4_superblock
    代码片段：`typedef struct ext4_superblock {`
- 结构体/类型重合：与 2025 OSakura 在“系统调用”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/arch/loongarch64/trap.h:L5-L5`：struct trapframe
    代码片段：`struct trapframe {`
  - `OSakura-rv/include/proc/proc.h:L15-L15`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 文件路径重合：与 2025 OSakura 在“设备驱动”维度出现同名文件源码路径 `src/hal/uart.c` / `OSakura-rv/kernel/dev/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/hal/uart.c:L1-L3`：关键词命中
    代码片段：`#include <hal/uart.h> #include <hal/sbi.h> #include <hal/timer.h>`
  - `OSakura-rv/kernel/dev/uart.c:L1-L3`：关键词命中
    代码片段：`// low-level driver routines for 16550a UART #include "memlayout.h" #include "dev/uart.h"`
- 文件路径重合：与 2025 OSakura 在“调度与任务管理”维度出现同名文件源码路径 `src/task/proc.c` / `OSakura-rv/kernel/proc/proc.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/task/proc.c:L1-L3`：关键词命中
    代码片段：`#include <task/proc.h> #include <task/sched.h> #include <config.h>`
  - `OSakura-rv/kernel/proc/proc.c:L2-L6`：关键词命中
    代码片段：`#include "lib/str.h" #include "lib/print.h" #include "proc/cpu.h" #include "proc/initcode.h" #include "mem/pmem.h"`
- 文件路径重合：与 2025 OSakura 在“同步机制”维度出现同名文件源码路径 `src/sync/spinlock.c` / `OSakura-rv/kernel/lock/spinlock.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/sync/spinlock.c:L1-L3`：关键词命中
    代码片段：`#include <sync/spinlock.h> #include <trap/trap.h> #include <arch/cpu.h>`
  - `OSakura-rv/kernel/lock/spinlock.c:L1-L3`：关键词命中
    代码片段：`#include "lock/lock.h" #include "proc/cpu.h" #include "lib/print.h"`

## 相似点

- 与 2025 OSakura 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 OSakura 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/fs/uartdev.c:L1-L3`：关键词命中
    代码片段：`/* UART character device — provides stdin/stdout/stderr backing via VFS. * * The file's f_inode is NULL so file_read()/file_write() skip the VFS layer`
  - `src/hal/blk.c:L1-L3`：关键词命中
    代码片段：`#include <hal/blk.h> #include <misc/log.h> #include <misc/string.h>`
- 与 2025 OSakura 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/fs/vfs.c:L1-L3`：关键词命中
    代码片段：`#include <fs/vfs.h> #include <misc/log.h> #include <misc/string.h>`
  - `src/fs/file.c:L1-L4`：关键词命中
    代码片段：`#include <fs/file.h> #include <fs/inode.h> #include <fs/dentry.h> #include <misc/log.h>`
- 与 2025 OSakura 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/trap/trap.c:L1-L3`：关键词命中
    代码片段：`#include <trap/trap.h> #include <task/proc.h> #include <task/sched.h>`
  - `src/trap/trampoline.S:L16-L20`：关键词命中
    代码片段：`uservec: # # trap.c sets stvec to point here, so # traps from user space start here, # in supervisor mode, but with a`
- 与 2025 OSakura 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/mm/pm.c:L4-L8`：关键词命中
    代码片段：`#include <misc/list.h> #include <mm/early.h> #include <mm/kalloc.h> #include <mm/buddy.h> #include <misc/log.h>`
  - `src/mm/vm.c:L1-L4`：关键词命中
    代码片段：`#include <misc/log.h> #include <mm/pagetable.h> #include <mm/vm.h> #include <mm/kalloc.h>`
- 与 2025 OSakura 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/task/proc.c:L1-L3`：关键词命中
    代码片段：`#include <task/proc.h> #include <task/sched.h> #include <config.h>`
  - `src/task/sched.c:L1-L3`：关键词命中
    代码片段：`#include <task/sched.h> #include <misc/list.h> #include <misc/log.h>`
- 与 2025 OSakura 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/sync/spinlock.c:L1-L3`：关键词命中
    代码片段：`#include <sync/spinlock.h> #include <trap/trap.h> #include <arch/cpu.h>`
  - `src/fs/vfs.c:L5-L9`：关键词命中
    代码片段：`#include <misc/complier.h> #include <mm/kalloc.h> #include <sync/spinlock.h> static struct {`
- 与 2025 OSakura 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/syscall/ipc.c:L18-L22`：关键词命中
    代码片段：`*   恢复保存的 saved_tf 到 trapframe，清除 in_signal_handler 标志 */ #include <syscall/syscall.h> #include <task/proc.h> #include <task/sched.h>`
  - `src/syscall/file.c:L17-L21`：关键词命中
    代码片段：`*   fdnum 仅提取 fd 号做基本校验，再手动 fd_free + vfs_close */ #include <syscall/syscall.h> #include <hal/uart.h> #include <task/proc.h>`
- 与 2025 OSakura 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 OSakura 的语言构成不同：待测作品为 {'build': 240, 'markdown': 303, 'c': 17312, 'make': 42, 'asm': 709}，历史样本为 {'json': 20, 'build': 459, 'markdown': 15, 'make': 26, 'c': 20261, 'asm': 315}。（置信度：medium）

## 可能创新点

- 待测作品包含 46 个历史样本 2025 OSakura 中未见的独特函数/类型定义（如 `sbi_set_timer`, `sbi_console_putchar`, `sbi_console_getchar`, `sbi_clear_ipi`, `sbi_shutdown`, `address_space_operations`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `include/hal/sbi.h:L88-L88`：fn sbi_set_timer
    代码片段：`static inline struct sbiret sbi_set_timer(u64 stime_value) {`
  - `include/hal/sbi.h:L23-L23`：fn sbi_console_putchar
    代码片段：`static inline void sbi_console_putchar(int ch) {`

## 附录：核验摘要

- 关键结论数：29
- 含证据关键结论数：29（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
