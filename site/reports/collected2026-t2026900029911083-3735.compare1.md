# 2026 T2026900029911083 比较报告

- 对比历史仓库：2025 静春山
- 生成时间：2026-07-02T09:04:17.777377+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 静春山（来源：赛事历史作品）：画像相似度 score=9.49；同属 ucore-variant 风格; 语言构成相似度 0.86; OS 维度重合度 1.00; 代码规模接近度 0.77

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 静春山 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/drive/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/fs/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/drive/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/fs/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 与 2025 静春山 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/bio.c:L19-L23`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include <fs/vfs/fs.h> #include <fs/buf.h>`
  - `kernel/fs/pipe.c:L6-L10`：关键词命中
    代码片段：`#include "proc/proc.h" #include "fs/fcntl.h" #include "fs/vfs/file.h" #define PIPESIZE 512`
  - `kernel/fs/bio.c:L19-L23`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include <fs/vfs/fs.h> #include <fs/buf.h>`
  - `kernel/fs/pipe.c:L6-L10`：关键词命中
    代码片段：`#include "proc/proc.h" #include "fs/fcntl.h" #include "fs/vfs/file.h" #define PIPESIZE 512`
- 与 2025 静春山 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap/la/apic.c:L7-L11`：关键词命中
    代码片段：`#include <dev/pci/pci.h> // // the loongarch 7A1000 I/O Interrupt Controller Registers. //`
  - `kernel/trap/la/trap.c:L13-L17`：关键词命中
    代码片段：`#include "mem.h" struct spinlock interrupt; struct spinlock tickslock; uint ticks;`
  - `kernel/trap/la/apic.c:L7-L11`：关键词命中
    代码片段：`#include <dev/pci/pci.h> // // the loongarch 7A1000 I/O Interrupt Controller Registers. //`
  - `kernel/trap/la/trap.c:L13-L17`：关键词命中
    代码片段：`#include "mem.h" struct spinlock interrupt; struct spinlock tickslock; uint ticks;`
- 与 2025 静春山 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mm/vm.c:L12-L16`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/mm/buddy.c:L248-L252`：关键词命中
    代码片段：`// Also ensure it fits within our static buffer size int level = 0; uint64 size = PGSIZE;             // Start with page size int max_level = MAX_BUDDY_LEVEL;  // Allow managing...`
  - `kernel/mm/vm.c:L12-L16`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/mm/buddy.c:L242-L246`：关键词命中
    代码片段：`// Also ensure it fits within our static buffer size int level = 0; uint64 size = PGSIZE;             // Start with page size int max_level = MAX_BUDDY_LEVEL;  // Allow managing...`
- 与 2025 静春山 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/exec.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/lock.h"  // 假设锁的定义在这里 #include "proc/proc.h" #include "defs.h" #include "lib/elf.h"`
  - `kernel/proc/proc.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/lock.h" #include "proc/proc.h" #include "fs/vfs/fs.h"`
  - `kernel/proc/exec.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/lock.h"  // 假设锁的定义在这里 #include "proc/proc.h" #include "defs.h" #include "lib/elf.h"`
  - `kernel/proc/proc.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/lock.h" #include "proc/proc.h" #include "fs/vfs/fs.h"`
- 与 2025 静春山 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/lock/spinlock.c:L4-L8`：关键词命中
    代码片段：`#include "param.h" #include "memlayout.h" #include <lock/lock.h> #include "platform.h" #include <proc/proc.h>`
  - `kernel/lock/sleeplock.c:L6-L10`：关键词命中
    代码片段：`#include "param.h" #include "memlayout.h" #include <lock/lock.h> #include <proc/proc.h>`
  - `kernel/lock/spinlock.c:L4-L8`：关键词命中
    代码片段：`#include "param.h" #include "memlayout.h" #include <lock/lock.h> #include "platform.h" #include <proc/proc.h>`
  - `kernel/lock/sleeplock.c:L6-L10`：关键词命中
    代码片段：`#include "param.h" #include "memlayout.h" #include <lock/lock.h> #include <proc/proc.h>`
- 与 2025 静春山 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap/la/trap.c:L130-L134`：关键词命中
    代码片段：`if (p->killed) exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->era += 4;`
  - `kernel/trap/rv/trap.c:L117-L121`：关键词命中
    代码片段：`} // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/trap/la/trap.c:L130-L134`：关键词命中
    代码片段：`if (p->killed) exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->era += 4;`
  - `kernel/trap/rv/trap.c:L117-L121`：关键词命中
    代码片段：`} // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 静春山 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：defined, if, la2k1000, loongarch, uartinit, uartstart, vf2, void。（置信度：high）
  证据：
  - `kernel/drive/uart.c:L74-L78`：fn uartinit
    代码片段：`void uartstart(); void uartinit(void) { // 对 VF2 / LA2K1000：不修改波特率相关设置，沿用 U-Boot 配置；清 FIFO，打开 RX 中断 #if defined(VF2) || (defined(LOONGARCH) && defined(LA2K1000))`
  - `kernel/drive/uart.c:L74-L78`：fn uartinit
    代码片段：`void uartstart(); void uartinit(void) { // 对 VF2 / LA2K1000：不修改波特率相关设置，沿用 U-Boot 配置；清 FIFO，打开 RX 中断 #if defined(VF2) || (defined(LOONGARCH) && defined(LA2K1000))`
- 与 2025 静春山 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：acquire, c, int, uart_tx_lock, uartputc, void。（置信度：high）
  证据：
  - `kernel/drive/uart.c:L116-L120`：fn uartputc
    代码片段：`// from interrupts; it's only suitable for use // by write(). void uartputc(int c) { acquire(&uart_tx_lock);`
  - `kernel/drive/uart.c:L116-L120`：fn uartputc
    代码片段：`// from interrupts; it's only suitable for use // by write(). void uartputc(int c) { acquire(&uart_tx_lock);`
- 与 2025 静春山 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：b, bcache, binit, buf, struct, void。（置信度：high）
  证据：
  - `kernel/fs/bio.c:L36-L40`：fn binit
    代码片段：`} bcache; void binit(void) { struct buf *b;`
  - `kernel/fs/bio.c:L36-L40`：fn binit
    代码片段：`} bcache; void binit(void) { struct buf *b;`
- 与 2025 静春山 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：b, buf, bwrite, defined, holdingsleep, if, la2k1000, lock。（置信度：high）
  证据：
  - `kernel/fs/bio.c:L104-L108`：fn bwrite
    代码片段：`// Write b's contents to disk.  Must be locked. void bwrite(struct buf *b) { if (!holdingsleep(&b->lock)) panic("bwrite"); #if defined(VF2) || defined(LA2K1000)`
  - `kernel/fs/bio.c:L104-L108`：fn bwrite
    代码片段：`// Write b's contents to disk.  Must be locked. void bwrite(struct buf *b) { if (!holdingsleep(&b->lock)) panic("bwrite"); #if defined(VF2) || defined(LA2K1000)`
- 与 2025 静春山 在“中断与异常”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：if, kalloc, p, proc, sig_handlers, sig_init, struct, void。（置信度：high）
  证据：
  - `kernel/trap/signal.c:L12-L16`：fn sig_init
    代码片段：`// 初始化进程的信号处理系统 void sig_init(struct proc *p) { p->sig_handlers = (struct sig_handlers *)kalloc(); if (!p->sig_handlers) {`
  - `kernel/trap/signal.c:L12-L16`：fn sig_init
    代码片段：`// 初始化进程的信号处理系统 void sig_init(struct proc *p) { p->sig_handlers = (struct sig_handlers *)kalloc(); if (!p->sig_handlers) {`
- 与 2025 静春山 在“中断与异常”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：act, int, myproc, oldact, p, proc, sig_register, sigaction。（置信度：high）
  证据：
  - `kernel/trap/signal.c:L57-L61`：fn sig_register
    代码片段：`// 注册信号处理器 int sig_register(int signum, struct sigaction *act, struct sigaction *oldact) { struct proc *p = myproc();`
  - `kernel/trap/signal.c:L51-L55`：fn sig_register
    代码片段：`// 注册信号处理器 int sig_register(int signum, struct sigaction *act, struct sigaction *oldact) { struct proc *p = myproc();`
- 宏名重合：与 2025 静春山 在“设备驱动”维度发现 4 个同名定义：ENXIO, ENOTBLK, EBUSY, EXDEV。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/errno.h:L11-L11`：macro ENXIO
    代码片段：`#define ENXIO 6    /* No such device or address */`
  - `include/errno.h:L11-L11`：macro ENXIO
    代码片段：`#define ENXIO 6    /* No such device or address */`
  - `include/errno.h:L20-L20`：macro ENOTBLK
    代码片段：`#define ENOTBLK 15 /* Block device required */`
  - `include/errno.h:L20-L20`：macro ENOTBLK
    代码片段：`#define ENOTBLK 15 /* Block device required */`
- 宏名重合：与 2025 静春山 在“文件系统”维度发现 4 个同名定义：_DEFS_H, NELEM, CLONE_VM, CLONE_FS。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/defs.h:L2-L2`：macro _DEFS_H
    代码片段：`#define _DEFS_H`
  - `include/defs.h:L2-L2`：macro _DEFS_H
    代码片段：`#define _DEFS_H`
  - `include/defs.h:L227-L227`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x) / sizeof((x)[0]))`
  - `include/defs.h:L226-L226`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x) / sizeof((x)[0]))`
- 宏名重合：与 2025 静春山 在“中断与异常”维度发现 4 个同名定义：LOONGARCH_CSR_ECFG, LOONGARCH_CSR_ESTAT, LOONGARCH_CSR_ERAISE, LOONGARCH_CSR_EENTRY。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/asm.h:L10-L10`：macro LOONGARCH_CSR_ECFG
    代码片段：`#define LOONGARCH_CSR_ECFG 0x4   /* Exception config */`
  - `include/asm.h:L10-L10`：macro LOONGARCH_CSR_ECFG
    代码片段：`#define LOONGARCH_CSR_ECFG 0x4   /* Exception config */`
  - `include/asm.h:L11-L11`：macro LOONGARCH_CSR_ESTAT
    代码片段：`#define LOONGARCH_CSR_ESTAT 0x5  /* Exception status */`
  - `include/asm.h:L11-L11`：macro LOONGARCH_CSR_ESTAT
    代码片段：`#define LOONGARCH_CSR_ESTAT 0x5  /* Exception status */`
- 宏名重合：与 2025 静春山 在“内存管理”维度发现 4 个同名定义：BUDDY_MEMORY_ALLOCATION_H, CLONE_VM, EINTR, ENOMEM。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/buddy.h:L2-L2`：macro BUDDY_MEMORY_ALLOCATION_H
    代码片段：`#define BUDDY_MEMORY_ALLOCATION_H`
  - `include/buddy.h:L2-L2`：macro BUDDY_MEMORY_ALLOCATION_H
    代码片段：`#define BUDDY_MEMORY_ALLOCATION_H`
  - `include/defs.h:L238-L238`：macro CLONE_VM
    代码片段：`#define CLONE_VM 0x00000100`
  - `include/defs.h:L237-L237`：macro CLONE_VM
    代码片段：`#define CLONE_VM 0x00000100`
- 宏名重合：与 2025 静春山 在“调度与任务管理”维度发现 3 个同名定义：CLONE_THREAD, ESRCH, ECHILD。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/defs.h:L246-L246`：macro CLONE_THREAD
    代码片段：`#define CLONE_THREAD 0x00010000`
  - `include/defs.h:L245-L245`：macro CLONE_THREAD
    代码片段：`#define CLONE_THREAD 0x00010000`
  - `include/errno.h:L8-L8`：macro ESRCH
    代码片段：`#define ESRCH 3    /* No such process */`
  - `include/errno.h:L8-L8`：macro ESRCH
    代码片段：`#define ESRCH 3    /* No such process */`
- 宏名重合：与 2025 静春山 在“同步机制”维度发现 4 个同名定义：ENOTBLK, EDEADLK, ENOLCK, EWOULDBLOCK。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/errno.h:L20-L20`：macro ENOTBLK
    代码片段：`#define ENOTBLK 15 /* Block device required */`
  - `include/errno.h:L20-L20`：macro ENOTBLK
    代码片段：`#define ENOTBLK 15 /* Block device required */`
  - `include/errno.h:L42-L42`：macro EDEADLK
    代码片段：`#define EDEADLK 35         /* Resource deadlock would occur */`
  - `include/errno.h:L42-L42`：macro EDEADLK
    代码片段：`#define EDEADLK 35         /* Resource deadlock would occur */`
- 文件路径重合：与 2025 静春山 在“设备驱动”维度出现同路径源码路径 `kernel/drive/uart.c` / `kernel/drive/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/drive/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/drive/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
- 文件路径重合：与 2025 静春山 在“设备驱动”维度出现同路径源码路径 `kernel/fs/console.c` / `kernel/fs/console.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/fs/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 文件路径重合：与 2025 静春山 在“文件系统”维度出现同路径源码路径 `kernel/fs/bio.c` / `kernel/fs/bio.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/bio.c:L19-L23`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include <fs/vfs/fs.h> #include <fs/buf.h>`
  - `kernel/fs/bio.c:L19-L23`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include <fs/vfs/fs.h> #include <fs/buf.h>`
- 文件路径重合：与 2025 静春山 在“文件系统”维度出现同路径源码路径 `kernel/fs/pipe.c` / `kernel/fs/pipe.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/pipe.c:L6-L10`：关键词命中
    代码片段：`#include "proc/proc.h" #include "fs/fcntl.h" #include "fs/vfs/file.h" #define PIPESIZE 512`
  - `kernel/fs/pipe.c:L6-L10`：关键词命中
    代码片段：`#include "proc/proc.h" #include "fs/fcntl.h" #include "fs/vfs/file.h" #define PIPESIZE 512`
- 文件路径重合：与 2025 静春山 在“中断与异常”维度出现同路径源码路径 `kernel/trap/la/apic.c` / `kernel/trap/la/apic.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/trap/la/apic.c:L7-L11`：关键词命中
    代码片段：`#include <dev/pci/pci.h> // // the loongarch 7A1000 I/O Interrupt Controller Registers. //`
  - `kernel/trap/la/apic.c:L7-L11`：关键词命中
    代码片段：`#include <dev/pci/pci.h> // // the loongarch 7A1000 I/O Interrupt Controller Registers. //`
- 文件路径重合：与 2025 静春山 在“中断与异常”维度出现同路径源码路径 `kernel/trap/la/trap.c` / `kernel/trap/la/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/trap/la/trap.c:L13-L17`：关键词命中
    代码片段：`#include "mem.h" struct spinlock interrupt; struct spinlock tickslock; uint ticks;`
  - `kernel/trap/la/trap.c:L13-L17`：关键词命中
    代码片段：`#include "mem.h" struct spinlock interrupt; struct spinlock tickslock; uint ticks;`

## 相似点

- 与 2025 静春山 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 静春山 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/drive/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/fs/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 与 2025 静春山 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/bio.c:L19-L23`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include <fs/vfs/fs.h> #include <fs/buf.h>`
  - `kernel/fs/pipe.c:L6-L10`：关键词命中
    代码片段：`#include "proc/proc.h" #include "fs/fcntl.h" #include "fs/vfs/file.h" #define PIPESIZE 512`
- 与 2025 静春山 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap/la/apic.c:L7-L11`：关键词命中
    代码片段：`#include <dev/pci/pci.h> // // the loongarch 7A1000 I/O Interrupt Controller Registers. //`
  - `kernel/trap/la/trap.c:L13-L17`：关键词命中
    代码片段：`#include "mem.h" struct spinlock interrupt; struct spinlock tickslock; uint ticks;`
- 与 2025 静春山 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mm/vm.c:L12-L16`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/mm/buddy.c:L248-L252`：关键词命中
    代码片段：`// Also ensure it fits within our static buffer size int level = 0; uint64 size = PGSIZE;             // Start with page size int max_level = MAX_BUDDY_LEVEL;  // Allow managing...`
- 与 2025 静春山 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/exec.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/lock.h"  // 假设锁的定义在这里 #include "proc/proc.h" #include "defs.h" #include "lib/elf.h"`
  - `kernel/proc/proc.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/lock.h" #include "proc/proc.h" #include "fs/vfs/fs.h"`
- 与 2025 静春山 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/lock/spinlock.c:L4-L8`：关键词命中
    代码片段：`#include "param.h" #include "memlayout.h" #include <lock/lock.h> #include "platform.h" #include <proc/proc.h>`
  - `kernel/lock/sleeplock.c:L6-L10`：关键词命中
    代码片段：`#include "param.h" #include "memlayout.h" #include <lock/lock.h> #include <proc/proc.h>`
- 与 2025 静春山 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap/la/trap.c:L130-L134`：关键词命中
    代码片段：`if (p->killed) exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->era += 4;`
  - `kernel/trap/rv/trap.c:L117-L121`：关键词命中
    代码片段：`} // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
- 与 2025 静春山 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 静春山 的语言构成不同：待测作品为 {'build': 575, 'markdown': 21625, 'asm': 731, 'c': 59119, 'text': 1942}，历史样本为 {'json': 22, 'markdown': 7411, 'text': 1439, 'build': 891, 'c': 51250, 'asm': 924}。（置信度：medium）

## 可能创新点

- 待测作品包含 59 个历史样本 2025 静春山 中未见的独特函数/类型定义（如 `virtio_msix_status`, `ext4_bcache_clear_dirty`, `ext4_bcache_set_dirty`, `print_osuperbeauty_banner2`, `print_osuperbeauty_banner`, `pci_msix_tbl`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `include/dev/pci/virtio_pci.h:L92-L92`：enum virtio_msix_status
    代码片段：`enum virtio_msix_status { VIRTIO_MSIX_NONE = 0, VIRTIO_MSIX_DISABLED = 1, VIRTIO_MSIX_ENABLED = 2 };`
  - `include/fs/ext4/ext4_bcache.h:L181-L181`：fn ext4_bcache_clear_dirty
    代码片段：`static inline void ext4_bcache_clear_dirty(struct ext4_buf *buf) {`

## 附录：核验摘要

- 关键结论数：33
- 含证据关键结论数：33（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
