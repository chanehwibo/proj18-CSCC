# 2026 T202610486999573 比较报告

- 对比历史仓库：2025 别panic
- 生成时间：2026-07-02T05:01:45.956898+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 别panic（来源：赛事历史作品）：画像相似度 score=9.82；同属 ucore-variant 风格; 语言构成相似度 0.99; OS 维度重合度 1.00; 代码规模接近度 0.84

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 别panic 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/dev/uart.c:L1-L4`：关键词命中
    代码片段：`/* * uart.c - 16550a UART 驱动（轮询 + 中断回显） * * 提供：`
  - `kernel/dev/virtio.c:L1-L4`：关键词命中
    代码片段：`/* * virtio.c - QEMU virtio-blk（虚拟磁盘）驱动 * * QEMU 侧常见配置：-device virtio-blk-device,drive=x0,bus=virtio-mmio-bus.0`
  - `kernel/dev/uart.c:L1-L4`：关键词命中
    代码片段：`/** * @file uart.c * @brief 16550A UART驱动程序 *`
  - `kernel/dev/virtio.c:L1-L5`：关键词命中
    代码片段：`/* QEMU提供的虚拟磁盘的驱动 QEMUOPTS = -device virtio-blk-device,drive=x0,bus=virtio-mmio-bus.0 这个文件最终提供三个重要函数: virtio_init() // 初始化函数`
- 与 2025 别panic 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/fs.c:L4-L8`：关键词命中
    代码片段：`* 当前版本的 fs_init() 做了两件事： *   1) 初始化 buf cache，并从磁盘读取超级块到内存副本 sb； *   2) 运行一段 inode 读写自测逻辑（便于实验阶段验证 inode/data 分配链路）。 * * 注意：自测结束后会进入 while(1) 死循环。`
  - `kernel/fs/dir.c:L7-L11`：关键词命中
    代码片段：`* * 锁语义： *   - 目录项的查找/增删/遍历都要求调用者持有父目录 inode 的睡眠锁（pip->slk）。 *   - path_* 系列会按需对中间目录 inode 加锁、查找目录项、再移动到下一段。 */`
  - `kernel/fs/file.c:L122-L126`：关键词命中
    代码片段：`if (ff.type == FD_INODE) { // 关闭FAT32文件 fat32_close(&ff.fat); } else if (ff.type == FD_PIPE) { // 关闭管道`
  - `kernel/fs/fat32.c:L141-L145`：关键词命中
    代码片段：`current_path[1] = '\0'; printf("--- FAT32 SuperBlock ---\n"); printf("Bytes per Sector: %d\n", sb.bytes_per_sector); printf("Sector per Cluster: %d\n", sb.sector_per_cluster);`
- 与 2025 别panic 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/dev/plic.c:L1-L4`：关键词命中
    代码片段：`/* * plic.c - PLIC（Platform-level interrupt controller） * * 负责外部中断（external interrupt）的分发：`
  - `kernel/dev/timer.c:L1-L5`：关键词命中
    代码片段：`#include "lib/lock.h" #include "lib/print.h" #include "dev/timer.h" #include "memlayout.h" #include "riscv.h"`
  - `kernel/dev/plic.c:L1-L3`：关键词命中
    代码片段：`// Platform-level interrupt controller #include "memlayout.h"`
  - `kernel/dev/timer.c:L1-L4`：关键词命中
    代码片段：`/** * @file timer.c * @brief 定时器驱动（基于RISC-V CLINT） *`
- 与 2025 别panic 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/elf.c:L7-L11`：关键词命中
    代码片段：`*       1) 按页分配物理页并映射到用户页表 *       2) 从 elf_data 拷贝文件内容（filesz），其余 memsz 区域保持为 0（BSS） *   - 输出入口地址 e_entry 与 heap_top（最高段末尾按页对齐） */`
  - `kernel/fs/ext4.c:L308-L312`：关键词命中
    代码片段：`uint64 dir_size = inode_size_bytes(&dir); if (dir_size == 0 || dir_size > PGSIZE) { dir_size = PGSIZE; }`
  - `kernel/fs/elf.c:L84-L88`：关键词命中
    代码片段：`// 加载 ELF 文件到进程地址空间 int elf_load(struct fat32_file* file, pgtbl_t pgdir, uint64* entry_point, uint64* end_addr) { elf_hdr_t elf_hdr;`
  - `kernel/mem/uvm.c:L14-L18`：关键词命中
    代码片段：`// 遍历当前页表的所有项 for(int i = 0; i < 512; i++) { pte_t pte = pgtbl[i]; if((pte & PTE_V) && (pte & (PTE_R | PTE_W | PTE_X)) == 0) { // 这是一个指向下级页表的有效页表项`
- 与 2025 别panic 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/cpu.c:L1-L3`：关键词命中
    代码片段：`#include "proc/cpu.h" #include "riscv.h"`
  - `kernel/proc/proc.c:L4-L8`：关键词命中
    代码片段：`#include "dev/uart.h" #include "mem/vmem.h" #include "proc/cpu.h" #include "proc/initcode.h" #include "trap/trap.h"`
  - `kernel/proc/cpu.c:L1-L3`：关键词命中
    代码片段：`#include "proc/cpu.h" #include "riscv.h"`
  - `kernel/proc/proc.c:L2-L6`：关键词命中
    代码片段：`#include "mem/pmem.h" #include "mem/vmem.h" #include "proc/cpu.h" #include "proc/initcode.h" #include "memlayout.h"`
- 与 2025 别panic 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/lib/spinlock.c:L1-L4`：关键词命中
    代码片段：`/* * spinlock.c - 自旋锁与可嵌套的关/开中断 * * 这份实现的核心思路：`
  - `kernel/lib/sleeplock.c:L13-L17`：关键词命中
    代码片段：`*/ #include "lib/lock.h" #include "proc/proc.h" #include "proc/cpu.h"`
  - `kernel/lib/spinlock.c:L1-L4`：关键词命中
    代码片段：`/** * @file spinlock.c * @brief 自旋锁实现（支持中断嵌套和锁层次管理） *`
  - `include/lib/lock.h:L1-L4`：关键词命中
    代码片段：`/** * @file lock.h * @brief 自旋锁接口定义 *`
- 与 2025 别panic 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/syscall/syscall.c:L3-L7`：关键词命中
    代码片段：`#include "mem/mmap.h" #include "mem/vmem.h" #include "syscall/syscall.h" #include "syscall/sysnum.h" #include "syscall/sysfunc.h"`
  - `kernel/syscall/sysfunc.c:L3-L7`：关键词命中
    代码片段：`* * 目标与定位： *   - 为 oscomp / riscv-syscalls-testing 等测试提供“够用”的 Linux 风格 syscall 语义。 *   - 这是一个兼容层而非完整的 Linux：很多 syscall 仅实现最小子集，或直接返回固定值。 *`
  - `kernel/trap/trap_user.c:L4-L8`：关键词命中
    代码片段：`* * 处理从用户态陷入内核的事件： * - 系统调用（ecall指令） * - 页面错误 * - 时钟中断（调度）`
  - `kernel/syscall/syscall.c:L3-L7`：关键词命中
    代码片段：`//#include "mem/mmap.h" #include "mem/vmem.h" #include "syscall/syscall.h" #include "syscall/sysnum.h" #include "syscall/sysfunc.h"`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 别panic 在“设备驱动”维度发现片段级代码相似度 0.72 （token=0.71, structure=0.73）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：blk, bus, device, drive, mmio, qemu, virtio, x0。（置信度：medium）
  证据：
  - `kernel/dev/virtio.c:L1-L4`：关键词命中
    代码片段：`/* * virtio.c - QEMU virtio-blk（虚拟磁盘）驱动 * * QEMU 侧常见配置：-device virtio-blk-device,drive=x0,bus=virtio-mmio-bus.0`
  - `kernel/dev/virtio.c:L1-L5`：关键词命中
    代码片段：`/* QEMU提供的虚拟磁盘的驱动 QEMUOPTS = -device virtio-blk-device,drive=x0,bus=virtio-mmio-bus.0 这个文件最终提供三个重要函数: virtio_init() // 初始化函数`
- 与 2025 别panic 在“设备驱动”维度发现片段级代码相似度 0.68 （token=0.62, structure=0.78）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：buf, false, return, virtio_disk_rw。（置信度：medium）
  证据：
  - `kernel/fs/buf.c:L143-L147`：关键词命中
    代码片段：`sleeplock_acquire(&bn->buf.slk); // 从磁盘读取 virtio_disk_rw(&bn->buf, false); return &bn->buf; }`
  - `kernel/fs/buf.c:L119-L123`：关键词命中
    代码片段：`// 从磁盘读取数据 virtio_disk_rw(&node->buf, false); return &node->buf;`
- 与 2025 别panic 在“文件系统”维度发现片段级代码相似度 0.65 （token=0.69, structure=0.57）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：buf, buf_node, buf_t, next, struct, typedef。（置信度：medium）
  证据：
  - `kernel/fs/buf.c:L29-L33`：struct buf_node
    代码片段：`* 说明：buf_t 是第一个成员，因此 (buf_node_t*)buf_t 指针转换是合法的。 */ typedef struct buf_node { buf_t buf; struct buf_node* next;`
  - `kernel/fs/buf.c:L9-L13`：struct buf_node
    代码片段：`// 将buf包装成双向循环链表的node typedef struct buf_node { buf_t buf; struct buf_node* next;`
- 函数/符号名重合：与 2025 别panic 在“中断与异常”维度发现 4 个同名定义：trampoline, user_vector, user_return, kernel_vector。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/trap/trampoline.S:L23-L23`：fn trampoline
    代码片段：`trampoline:`
  - `kernel/trap/trampoline.S:L9-L9`：fn trampoline
    代码片段：`trampoline:`
  - `kernel/trap/trampoline.S:L28-L28`：fn user_vector
    代码片段：`user_vector:`
  - `kernel/trap/trampoline.S:L14-L14`：fn user_vector
    代码片段：`user_vector:`
- 与 2025 别panic 在“调度与任务管理”维度发现片段级代码相似度 0.75 （token=0.77, structure=0.71）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：cpu, int, noff, origin, struct, typedef。（置信度：medium）
  证据：
  - `include/proc/cpu.h:L13-L17`：struct cpu
    代码片段：`*/ typedef struct cpu { int noff;       // 关中断的深度 int origin;     // 第一次关中断前的状态`
  - `include/proc/cpu.h:L21-L25`：struct cpu
    代码片段：`* - ctx: 调度器上下文 */ typedef struct cpu { int noff;       // 关中断的深度 int origin;     // 第一次关中断前的状态`
- 与 2025 别panic 在“同步机制”维度发现片段级代码相似度 0.61 （token=0.62, structure=0.57）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：char, int, locked, name, spinlock, struct, typedef。（置信度：medium）
  证据：
  - `include/lib/lock.h:L16-L20`：struct spinlock
    代码片段：`#include "common.h" typedef struct spinlock { int locked;   // 0/1：是否持有 char* name;   // 调试用名称`
  - `include/lib/lock.h:L22-L26`：struct spinlock
    代码片段：`* - cpuid: 持有锁的CPU ID */ typedef struct spinlock { int locked; char* name;`
- 函数/符号名重合：与 2025 别panic 在“系统调用”维度发现 4 个同名定义：trampoline, user_vector, user_return, kernel_vector。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/trap/trampoline.S:L23-L23`：fn trampoline
    代码片段：`trampoline:`
  - `kernel/trap/trampoline.S:L9-L9`：fn trampoline
    代码片段：`trampoline:`
  - `kernel/trap/trampoline.S:L28-L28`：fn user_vector
    代码片段：`user_vector:`
  - `kernel/trap/trampoline.S:L14-L14`：fn user_vector
    代码片段：`user_vector:`
- 宏名重合：与 2025 别panic 在“设备驱动”维度发现 4 个同名定义：__UART_H__, VIRTIO_MMIO_MAGIC_VALUE, VIRTIO_MMIO_VERSION, VIRTIO_MMIO_DEVICE_ID。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/dev/uart.h:L2-L2`：macro __UART_H__
    代码片段：`#define __UART_H__`
  - `include/dev/uart.h:L2-L2`：macro __UART_H__
    代码片段：`#define __UART_H__`
  - `include/dev/virtio.h:L25-L25`：macro VIRTIO_MMIO_MAGIC_VALUE
    代码片段：`#define VIRTIO_MMIO_MAGIC_VALUE 0x000 // 0x74726976`
  - `include/dev/virtio.h:L16-L16`：macro VIRTIO_MMIO_MAGIC_VALUE
    代码片段：`#define VIRTIO_MMIO_MAGIC_VALUE 0x000 // 0x74726976`
- 宏名重合：与 2025 别panic 在“文件系统”维度发现 4 个同名定义：__BLOCK_BUF__, __ELF_H__, ELF_MAGIC, ET_EXEC。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/fs/buf.h:L19-L19`：macro __BLOCK_BUF__
    代码片段：`#define __BLOCK_BUF__`
  - `include/fs/buf.h:L2-L2`：macro __BLOCK_BUF__
    代码片段：`#define __BLOCK_BUF__`
  - `include/fs/elf.h:L10-L10`：macro __ELF_H__
    代码片段：`#define __ELF_H__`
  - `include/fs/elf.h:L2-L2`：macro __ELF_H__
    代码片段：`#define __ELF_H__`
- 宏名重合：与 2025 别panic 在“中断与异常”维度发现 4 个同名定义：__PLIC_H__, __TIMER_H__, INTERVAL, VIRTIO_MMIO_INTERRUPT_STATUS。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/dev/plic.h:L2-L2`：macro __PLIC_H__
    代码片段：`#define __PLIC_H__`
  - `include/dev/plic.h:L2-L2`：macro __PLIC_H__
    代码片段：`#define __PLIC_H__`
  - `include/dev/timer.h:L2-L2`：macro __TIMER_H__
    代码片段：`#define __TIMER_H__`
  - `include/dev/timer.h:L12-L12`：macro __TIMER_H__
    代码片段：`#define __TIMER_H__`
- 宏名重合：与 2025 别panic 在“内存管理”维度发现 4 个同名定义：__COMMON_H__, NULL, NCPU, PGSIZE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/common.h:L3-L3`：macro __COMMON_H__
    代码片段：`#define __COMMON_H__`
  - `include/common.h:L15-L15`：macro __COMMON_H__
    代码片段：`#define __COMMON_H__`
  - `include/common.h:L20-L20`：macro NULL
    代码片段：`#define NULL ((void*)0)`
  - `include/common.h:L31-L31`：macro NULL
    代码片段：`#define NULL ((void*)0)`
- 宏名重合：与 2025 别panic 在“调度与任务管理”维度发现 4 个同名定义：__CPU_H__, __PROC_H__, NPROC, SYS_sched_yield。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/proc/cpu.h:L2-L2`：macro __CPU_H__
    代码片段：`#define __CPU_H__`
  - `include/proc/cpu.h:L9-L9`：macro __CPU_H__
    代码片段：`#define __CPU_H__`
  - `include/proc/proc.h:L2-L2`：macro __PROC_H__
    代码片段：`#define __PROC_H__`
  - `include/proc/proc.h:L13-L13`：macro __PROC_H__
    代码片段：`#define __PROC_H__`
- 宏名重合：与 2025 别panic 在“同步机制”维度发现 4 个同名定义：BLOCK_SIZE, __BLOCK_BUF__, INODE_PER_BLOCK, ENTRY_PER_BLOCK。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/common.h:L32-L32`：macro BLOCK_SIZE
    代码片段：`#define BLOCK_SIZE 1024`
  - `include/common.h:L38-L38`：macro BLOCK_SIZE
    代码片段：`#define BLOCK_SIZE 512`
  - `include/fs/buf.h:L19-L19`：macro __BLOCK_BUF__
    代码片段：`#define __BLOCK_BUF__`
  - `include/fs/buf.h:L2-L2`：macro __BLOCK_BUF__
    代码片段：`#define __BLOCK_BUF__`
- 结构体/类型重合：与 2025 别panic 在“文件系统”维度发现 4 个同名定义：buf, dirent, file, super_block。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/fs/buf.h:L23-L23`：struct buf
    代码片段：`typedef struct buf {`
  - `include/fs/buf.h:L6-L6`：struct buf
    代码片段：`typedef struct buf {`
  - `include/fs/dir.h:L22-L22`：struct dirent
    代码片段：`typedef struct dirent {`
  - `mkfs/mkfs.c:L38-L38`：struct dirent
    代码片段：`typedef struct dirent {`
- 结构体/类型重合：与 2025 别panic 在“内存管理”维度发现 3 个同名定义：trapframe, page_node, alloc_region。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/proc/proc.h:L52-L52`：struct trapframe
    代码片段：`typedef struct trapframe {`
  - `include/proc/proc.h:L67-L67`：struct trapframe
    代码片段：`typedef struct trapframe {`
  - `kernel/mem/pmem.c:L29-L29`：struct page_node
    代码片段：`typedef struct page_node {`
  - `include/mem/pmem.h:L32-L32`：struct page_node
    代码片段：`typedef struct page_node {`
- 函数/符号名重合：与 2025 别panic 在“调度与任务管理”维度发现 1 个同名定义：swtch。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/proc/switch.S:L14-L14`：fn swtch
    代码片段：`swtch:`
  - `kernel/proc/swtch.S:L7-L7`：fn swtch
    代码片段：`swtch:`
- 结构体/类型重合：与 2025 别panic 在“调度与任务管理”维度发现 4 个同名定义：cpu, context, trapframe, proc_state。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/proc/cpu.h:L15-L15`：struct cpu
    代码片段：`typedef struct cpu {`
  - `include/proc/cpu.h:L23-L23`：struct cpu
    代码片段：`typedef struct cpu {`
  - `include/proc/proc.h:L32-L32`：struct context
    代码片段：`typedef struct context {`
  - `include/proc/proc.h:L36-L36`：struct context
    代码片段：`typedef struct context {`
- 文件路径重合：与 2025 别panic 在“设备驱动”维度出现同路径源码路径 `kernel/dev/uart.c` / `kernel/dev/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/dev/uart.c:L1-L4`：关键词命中
    代码片段：`/* * uart.c - 16550a UART 驱动（轮询 + 中断回显） * * 提供：`
  - `kernel/dev/uart.c:L1-L4`：关键词命中
    代码片段：`/** * @file uart.c * @brief 16550A UART驱动程序 *`
- 文件路径重合：与 2025 别panic 在“设备驱动”维度出现同路径源码路径 `kernel/dev/virtio.c` / `kernel/dev/virtio.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/dev/virtio.c:L1-L4`：关键词命中
    代码片段：`/* * virtio.c - QEMU virtio-blk（虚拟磁盘）驱动 * * QEMU 侧常见配置：-device virtio-blk-device,drive=x0,bus=virtio-mmio-bus.0`
  - `kernel/dev/virtio.c:L1-L5`：关键词命中
    代码片段：`/* QEMU提供的虚拟磁盘的驱动 QEMUOPTS = -device virtio-blk-device,drive=x0,bus=virtio-mmio-bus.0 这个文件最终提供三个重要函数: virtio_init() // 初始化函数`
- 结构体/类型重合：与 2025 别panic 在“设备驱动”维度发现 1 个同名定义：buf_node。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/fs/buf.c:L31-L31`：struct buf_node
    代码片段：`typedef struct buf_node {`
  - `kernel/fs/buf.c:L11-L11`：struct buf_node
    代码片段：`typedef struct buf_node {`
- 文件路径重合：与 2025 别panic 在“文件系统”维度出现同路径源码路径 `kernel/fs/buf.c` / `kernel/fs/buf.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/buf.c:L29-L33`：struct buf_node
    代码片段：`* 说明：buf_t 是第一个成员，因此 (buf_node_t*)buf_t 指针转换是合法的。 */ typedef struct buf_node { buf_t buf; struct buf_node* next;`
  - `kernel/fs/buf.c:L9-L13`：struct buf_node
    代码片段：`// 将buf包装成双向循环链表的node typedef struct buf_node { buf_t buf; struct buf_node* next;`
- 文件路径重合：与 2025 别panic 在“文件系统”维度出现同路径源码路径 `kernel/fs/file.c` / `kernel/fs/file.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/file.c:L30-L34`：struct ramfile
    代码片段：`#define RAMFS_MAX_PAGES 16  // 每个文件最多 64KB typedef struct ramfile { int used; int ref;`
  - `kernel/fs/file.c:L122-L126`：关键词命中
    代码片段：`if (ff.type == FD_INODE) { // 关闭FAT32文件 fat32_close(&ff.fat); } else if (ff.type == FD_PIPE) { // 关闭管道`
- 文件路径重合：与 2025 别panic 在“中断与异常”维度出现同路径源码路径 `kernel/dev/plic.c` / `kernel/dev/plic.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/dev/plic.c:L1-L4`：关键词命中
    代码片段：`/* * plic.c - PLIC（Platform-level interrupt controller） * * 负责外部中断（external interrupt）的分发：`
  - `kernel/dev/plic.c:L1-L3`：关键词命中
    代码片段：`// Platform-level interrupt controller #include "memlayout.h"`
- 文件路径重合：与 2025 别panic 在“中断与异常”维度出现同路径源码路径 `kernel/dev/timer.c` / `kernel/dev/timer.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/dev/timer.c:L1-L5`：关键词命中
    代码片段：`#include "lib/lock.h" #include "lib/print.h" #include "dev/timer.h" #include "memlayout.h" #include "riscv.h"`
  - `kernel/dev/timer.c:L1-L4`：关键词命中
    代码片段：`/** * @file timer.c * @brief 定时器驱动（基于RISC-V CLINT） *`
- 结构体/类型重合：与 2025 别panic 在“中断与异常”维度发现 2 个同名定义：timer, trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/dev/timer.h:L16-L16`：struct timer
    代码片段：`typedef struct timer {`
  - `include/dev/timer.h:L23-L23`：struct timer
    代码片段：`typedef struct timer {`
  - `include/proc/proc.h:L52-L52`：struct trapframe
    代码片段：`typedef struct trapframe {`
  - `include/proc/proc.h:L67-L67`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 结构体/类型重合：与 2025 别panic 在“同步机制”维度发现 2 个同名定义：super_block, spinlock。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/fs/fs.h:L25-L25`：struct super_block
    代码片段：`typedef struct super_block {`
  - `mkfs/mkfs.c:L13-L13`：struct super_block
    代码片段：`typedef struct super_block {`
  - `include/lib/lock.h:L18-L18`：struct spinlock
    代码片段：`typedef struct spinlock {`
  - `include/lib/lock.h:L24-L24`：struct spinlock
    代码片段：`typedef struct spinlock {`

## 相似点

- 与 2025 别panic 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 别panic 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/dev/uart.c:L1-L4`：关键词命中
    代码片段：`/* * uart.c - 16550a UART 驱动（轮询 + 中断回显） * * 提供：`
  - `kernel/dev/virtio.c:L1-L4`：关键词命中
    代码片段：`/* * virtio.c - QEMU virtio-blk（虚拟磁盘）驱动 * * QEMU 侧常见配置：-device virtio-blk-device,drive=x0,bus=virtio-mmio-bus.0`
- 与 2025 别panic 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/fs.c:L4-L8`：关键词命中
    代码片段：`* 当前版本的 fs_init() 做了两件事： *   1) 初始化 buf cache，并从磁盘读取超级块到内存副本 sb； *   2) 运行一段 inode 读写自测逻辑（便于实验阶段验证 inode/data 分配链路）。 * * 注意：自测结束后会进入 while(1) 死循环。`
  - `kernel/fs/dir.c:L7-L11`：关键词命中
    代码片段：`* * 锁语义： *   - 目录项的查找/增删/遍历都要求调用者持有父目录 inode 的睡眠锁（pip->slk）。 *   - path_* 系列会按需对中间目录 inode 加锁、查找目录项、再移动到下一段。 */`
- 与 2025 别panic 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/dev/plic.c:L1-L4`：关键词命中
    代码片段：`/* * plic.c - PLIC（Platform-level interrupt controller） * * 负责外部中断（external interrupt）的分发：`
  - `kernel/dev/timer.c:L1-L5`：关键词命中
    代码片段：`#include "lib/lock.h" #include "lib/print.h" #include "dev/timer.h" #include "memlayout.h" #include "riscv.h"`
- 与 2025 别panic 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/elf.c:L7-L11`：关键词命中
    代码片段：`*       1) 按页分配物理页并映射到用户页表 *       2) 从 elf_data 拷贝文件内容（filesz），其余 memsz 区域保持为 0（BSS） *   - 输出入口地址 e_entry 与 heap_top（最高段末尾按页对齐） */`
  - `kernel/fs/ext4.c:L308-L312`：关键词命中
    代码片段：`uint64 dir_size = inode_size_bytes(&dir); if (dir_size == 0 || dir_size > PGSIZE) { dir_size = PGSIZE; }`
- 与 2025 别panic 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/cpu.c:L1-L3`：关键词命中
    代码片段：`#include "proc/cpu.h" #include "riscv.h"`
  - `kernel/proc/proc.c:L4-L8`：关键词命中
    代码片段：`#include "dev/uart.h" #include "mem/vmem.h" #include "proc/cpu.h" #include "proc/initcode.h" #include "trap/trap.h"`
- 与 2025 别panic 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/lib/spinlock.c:L1-L4`：关键词命中
    代码片段：`/* * spinlock.c - 自旋锁与可嵌套的关/开中断 * * 这份实现的核心思路：`
  - `kernel/lib/sleeplock.c:L13-L17`：关键词命中
    代码片段：`*/ #include "lib/lock.h" #include "proc/proc.h" #include "proc/cpu.h"`
- 与 2025 别panic 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/syscall/syscall.c:L3-L7`：关键词命中
    代码片段：`#include "mem/mmap.h" #include "mem/vmem.h" #include "syscall/syscall.h" #include "syscall/sysnum.h" #include "syscall/sysfunc.h"`
  - `kernel/syscall/sysfunc.c:L3-L7`：关键词命中
    代码片段：`* * 目标与定位： *   - 为 oscomp / riscv-syscalls-testing 等测试提供“够用”的 Linux 风格 syscall 语义。 *   - 这是一个兼容层而非完整的 Linux：很多 syscall 仅实现最小子集，或直接返回固定值。 *`
- 与 2025 别panic 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 别panic 的语言构成不同：待测作品为 {'build': 327, 'markdown': 172, 'make': 26, 'c': 13874, 'asm': 365}，历史样本为 {'json': 20, 'make': 26, 'build': 250, 'markdown': 73, 'c': 11493, 'asm': 305}。（置信度：medium）

## 可能创新点

- 待测作品包含 13 个历史样本 2025 别panic 中未见的独特函数/类型定义（如 `__attribute__`, `sys_getegid`, `sys_geteuid`, `sys_getgid`, `sys_getuid`, `mmap_region_node`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/fs/ext4.c:L110-L110`：fn __attribute__
    代码片段：`typedef struct __attribute__((packed)) {`
  - `kernel/syscall/sysfunc.c:L71-L71`：fn sys_getegid
    代码片段：`uint64 sys_getegid(void) { return 0; }`

## 附录：核验摘要

- 关键结论数：41
- 含证据关键结论数：41（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
