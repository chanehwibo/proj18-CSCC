# 2026 T2026102569910192 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026102569910192-3896`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：12325
- 代码/文本行数：4196372
- 主要语言：rust 3726200 LOC, json 294862 LOC, markdown 86964 LOC, build 41930 LOC, c 28156 LOC

## 总览

2026 T2026102569910192 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 3726200 LOC, json: 294862 LOC, markdown: 86964 LOC, build: 41930 LOC。仓库包含 12325 个已扫描文件、约 4196372 行可分析文本，当前抽取到 266423 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, SWTC/Makefile, SWTC-la/Cargo.toml, SWTC-la/Makefile, SWTC-la/xapi/Cargo.toml。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`SWTC_DIR := SWTC SWTC_KERNEL := $(SWTC_DIR)/kernel SWTC_USER := $(SWTC_DIR)/user`
  - `SWTC/Makefile:L1-L1`：构建入口
    代码片段：`all:`
  - `SWTC-la/Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] resolver = "2" members = [`
  - `SWTC-la/Makefile:L1-L3`：构建入口
    代码片段：`TOOLCHAIN ?= nightly-2025-05-20 ROOT_DIR  := $(CURDIR)`
  - `SWTC-la/xapi/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "xapi" version.workspace = true`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn validate_cap_header, fn sys_capget, fn sys_capset, fn check_signals, fn check_fatal_signals。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `SWTC-la/xapi/src/task/ctl.rs` | L7-L11 | 关键词命中 |
| `SWTC-la/xapi/src/task/mod.rs` | L5-L9 | 关键词命中 |
| `SWTC-la/xapi/src/task/cred.rs` | L2-L6 | 关键词命中 |
| `SWTC-la/xapi/src/task/ctl.rs` | L9-L13 | fn validate_cap_header |
| `SWTC-la/xapi/src/task/ctl.rs` | L20-L24 | fn sys_capget |
| `SWTC-la/xapi/src/task/ctl.rs` | L38-L42 | fn sys_capset |

### 关键代码片段

  - `SWTC-la/xapi/src/task/ctl.rs:L7-L11`：关键词命中
    代码片段：`}; use xcore::task::{get_process, with_uspace}; fn validate_cap_header(header: &mut __user_cap_header_struct) -> LinuxResult<()> {`
  - `SWTC-la/xapi/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod exit; mod futex; mod schedule; mod signal; mod thread;`
  - `SWTC-la/xapi/src/task/cred.rs:L2-L6`：关键词命中
    代码片段：`use axerrno::{LinuxError, LinuxResult}; use xcore::task::with_xprocess; use xuspace::{UserConstPtr, UserPtr, UserSpaceAccess};`
  - `SWTC-la/xapi/src/task/ctl.rs:L9-L13`：fn validate_cap_header
    代码片段：`use xcore::task::{get_process, with_uspace}; fn validate_cap_header(header: &mut __user_cap_header_struct) -> LinuxResult<()> { match header.version { _LINUX_CAPABILITY_VERSION_...`

### 相关符号

`fn validate_cap_header` at `SWTC-la/xapi/src/task/ctl.rs:L9`、`fn sys_capget` at `SWTC-la/xapi/src/task/ctl.rs:L20`、`fn sys_capset` at `SWTC-la/xapi/src/task/ctl.rs:L38`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn handle_page_fault, struct UserBuffer, fn new, fn get, fn get_mut。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `SWTC/kernel/src/mm/mod.rs` | L1-L5 | 关键词命中 |
| `SWTC/kernel/src/mm/shm.rs` | L7-L11 | 关键词命中 |
| `SWTC-la/xapi/src/mm/brk.rs` | L10-L14 | 关键词命中 |
| `SWTC-la/src/mm.rs` | L12-L16 | fn handle_page_fault |
| `SWTC/kernel/src/mm/buf.rs` | L12-L16 | struct UserBuffer |
| `SWTC/kernel/src/mm/buf.rs` | L19-L23 | fn new |

### 关键代码片段

  - `SWTC/kernel/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `SWTC/kernel/src/mm/shm.rs:L7-L11`：关键词命中
    代码片段：`use crate::{ config::mm::{PAGE_SIZE, PAGE_SIZE_BITS}, mm::{memory_space::vm_area::VmAreaType, page_table::PTEFlags, MapPermission, PageBuilder}, processor::current_process, stac...`
  - `SWTC-la/xapi/src/mm/brk.rs:L10-L14`：关键词命中
    代码片段：`with_xprocess(|xprocess| { let mut return_val: isize = xprocess.get_heap_top() as isize; let heap_bottom = xprocess.get_heap_bottom(); if addr != 0 && addr >= heap_bottom && add...`
  - `SWTC-la/src/mm.rs:L12-L16`：fn handle_page_fault
    代码片段：`#[register_trap_handler(PAGE_FAULT)] fn handle_page_fault(vaddr: VirtAddr, access_flags: MappingFlags, is_user: bool) -> bool { // warn!( //     "Page fault at {:#x}, access_fla...`

### 相关符号

`fn handle_page_fault` at `SWTC-la/src/mm.rs:L12`、`struct UserBuffer` at `SWTC/kernel/src/mm/buf.rs:L12`、`fn new` at `SWTC/kernel/src/mm/buf.rs:L19`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn handle_syscall_impl, fn handle_syscall, struct TrapContext, struct UserFloatContext, fn new。（置信度：medium）
  - 静态识别到 1075 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `SWTC-la/src/syscall.rs` | L4-L8 | 关键词命中 |
| `SWTC/kernel/src/syscall/fs.rs` | L34-L38 | 关键词命中 |
| `SWTC/kernel/src/syscall/mm.rs` | L13-L17 | 关键词命中 |
| `SWTC-la/src/syscall.rs` | L11-L15 | fn handle_syscall_impl |
| `SWTC-la/src/syscall.rs` | L632-L636 | fn handle_syscall |
| `SWTC/kernel/src/trap/ctx.rs` | L8-L12 | struct TrapContext |

### 关键代码片段

  - `SWTC-la/src/syscall.rs:L4-L8`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use syscalls::Sysno;`
  - `SWTC/kernel/src/syscall/fs.rs:L34-L38`：关键词命中
    代码片段：`use crate::stack_trace; use crate::sync::Event; use crate::syscall::PollEvents; use crate::timer::io_multiplex::{IOMultiplexFormat, IOMultiplexFuture, RawFdSetRWE}; use crate::t...`
  - `SWTC/kernel/src/syscall/mm.rs:L13-L17`：关键词命中
    代码片段：`processor::current_process, stack_trace, syscall::{MmapFlags, MmapProt}, utils::error::{SyscallErr, SyscallRet}, };`
  - `SWTC-la/src/syscall.rs:L11-L15`：fn handle_syscall_impl
    代码片段：`use xcore::time::{time_stat_from_kernel_to_user, time_stat_from_user_to_kernel}; fn handle_syscall_impl(tf: &mut TrapFrame, sysno: Sysno) -> LinuxResult<isize> { match sysno { /...`

### 相关符号

`fn handle_syscall_impl` at `SWTC-la/src/syscall.rs:L11`、`fn handle_syscall` at `SWTC-la/src/syscall.rs:L632`、`struct TrapContext` at `SWTC/kernel/src/trap/ctx.rs:L8`、`fn handle_syscall_impl` at `SWTC-la/src/syscall.rs:L11`、`fn handle_syscall` at `SWTC-la/src/syscall.rs:L632`、`fn sys_getcwd` at `SWTC/kernel/src/syscall/fs.rs:L48`、`fn sys_dup` at `SWTC/kernel/src/syscall/fs.rs:L66`、`fn sys_dup3` at `SWTC/kernel/src/syscall/fs.rs:L82`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：fn sys_read, fn readv_impl, fn writev_impl, fn sys_readv, fn sys_write。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `SWTC-la/xapi/src/fs/io.rs` | L167-L171 | 关键词命中 |
| `SWTC/kernel/src/fs/ffi.rs` | L2-L6 | 关键词命中 |
| `SWTC/kernel/src/fs/mod.rs` | L7-L11 | 关键词命中 |
| `SWTC-la/xapi/src/fs/io.rs` | L25-L29 | fn sys_read |
| `SWTC-la/xapi/src/fs/io.rs` | L36-L40 | fn readv_impl |
| `SWTC-la/xapi/src/fs/io.rs` | L65-L69 | fn writev_impl |

### 关键代码片段

  - `SWTC-la/xapi/src/fs/io.rs:L167-L171`：关键词命中
    代码片段：`with_fs(AT_FDCWD, path, |fs| { PAGE_CACHE_MANAGER .get_cache(fs.write_file(path)?.access(FileFlags::WRITE)?.inode()) .map(|inode| inode.evict_from_pos(length as _)) .unwrap_or(O...`
  - `SWTC/kernel/src/fs/ffi.rs:L2-L6`：关键词命中
    代码片段：`use super::fat32::SECTOR_SIZE; use super::{Inode, InodeMode}; use crate::timer::current_time_duration; use crate::timer::ffi::TimeSpec;`
  - `SWTC/kernel/src/fs/mod.rs:L7-L11`：关键词命中
    代码片段：`pub mod file_system; mod hash_key; pub mod inode; mod page_cache; pub mod pipe;`
  - `SWTC-la/xapi/src/fs/io.rs:L25-L29`：fn sys_read
    代码片段：`/// * 'buf' - Buffer to read data into /// * 'len' - Length of data to read pub fn sys_read(fd: i32, buf: UserPtr<u8>, len: usize) -> LinuxResult<isize> { let buf = with_uspace(...`

### 相关符号

`fn sys_read` at `SWTC-la/xapi/src/fs/io.rs:L25`、`fn readv_impl` at `SWTC-la/xapi/src/fs/io.rs:L36`、`fn writev_impl` at `SWTC-la/xapi/src/fs/io.rs:L65`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct FutexQueue, fn new, fn emplace_waiter, fn add_waiter, fn remove_waiter。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `SWTC/kernel/src/sync/mod.rs` | L8-L12 | 关键词命中 |
| `SWTC/kernel/src/sync/futex.rs` | L146-L150 | 关键词命中 |
| `SWTC/kernel/src/sync/mailbox.rs` | L9-L13 | 关键词命中 |
| `SWTC/kernel/src/sync/futex.rs` | L17-L21 | struct FutexQueue |
| `SWTC/kernel/src/sync/futex.rs` | L21-L25 | fn new |
| `SWTC/kernel/src/sync/futex.rs` | L26-L30 | fn emplace_waiter |

### 关键代码片段

  - `SWTC/kernel/src/sync/mod.rs:L8-L12`：关键词命中
    代码片段：`mod mailbox; /// Different kinds of mutex pub mod mutex; pub use mailbox::Event;`
  - `SWTC/kernel/src/sync/futex.rs:L146-L150`：关键词命中
    代码片段：`/// The reason why we use Arc here is that we may need to change the /// addr this thread is waiting for(i.e. futex queue) through another thread. /// The reason why we use Unsa...`
  - `SWTC/kernel/src/sync/mailbox.rs:L9-L13`：关键词命中
    代码片段：`use crate::stack_trace; use super::mutex::SpinNoIrqLock; bitflags! {`
  - `SWTC/kernel/src/sync/futex.rs:L17-L21`：struct FutexQueue
    代码片段：`/// Futex queue that stores: uaddr -> waiters(tid -> waiter) pub struct FutexQueue(pub BTreeMap<VirtAddr, BTreeMap<usize, FutexWaiter>>); impl FutexQueue {`

### 相关符号

`struct FutexQueue` at `SWTC/kernel/src/sync/futex.rs:L17`、`fn new` at `SWTC/kernel/src/sync/futex.rs:L21`、`fn emplace_waiter` at `SWTC/kernel/src/sync/futex.rs:L26`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：struct TrapContext, struct UserFloatContext, fn new, fn mark_save_if_needed, fn yield_task。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `SWTC/kernel/src/trap/ctx.rs` | L5-L9 | 关键词命中 |
| `SWTC/kernel/src/trap/mod.rs` | L1-L3 | 关键词命中 |
| `SWTC/kernel/src/trap/trap.S` | L63-L67 | 关键词命中 |
| `SWTC/kernel/src/trap/ctx.rs` | L8-L12 | struct TrapContext |
| `SWTC/kernel/src/trap/ctx.rs` | L41-L45 | struct UserFloatContext |
| `SWTC/kernel/src/trap/ctx.rs` | L50-L54 | fn new |

### 关键代码片段

  - `SWTC/kernel/src/trap/ctx.rs:L5-L9`：关键词命中
    代码片段：`use riscv::register::sstatus::{self, Sstatus, FS, SPP}; /// Trap context structure containing sstatus, sepc and registers #[derive(Clone, Copy, Debug)] #[repr(C)]`
  - `SWTC/kernel/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality mod ctx; /// Kernel trap handler`
  - `SWTC/kernel/src/trap/trap.S:L63-L67`：关键词命中
    代码片段：`# switch to user space # let sscratch store the trap context's address csrw sscratch, a0 # save kernel callee-saved regs`
  - `SWTC/kernel/src/trap/ctx.rs:L8-L12`：struct TrapContext
    代码片段：`#[derive(Clone, Copy, Debug)] #[repr(C)] pub struct TrapContext { /// user-to-kernel should save: /// general regs[0..31]`

### 相关符号

`struct TrapContext` at `SWTC/kernel/src/trap/ctx.rs:L8`、`struct UserFloatContext` at `SWTC/kernel/src/trap/ctx.rs:L41`、`fn new` at `SWTC/kernel/src/trap/ctx.rs:L50`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：fn intr_handler, trait BlockDevice, fn read_block, fn write_block, trait CharDevice。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `SWTC/kernel/src/driver/mod.rs` | L12-L16 | 关键词命中 |
| `SWTC/kernel/src/driver/plic.rs` | L4-L8 | 关键词命中 |
| `SWTC/kernel/src/driver/sbi/mod.rs` | L43-L47 | 关键词命中 |
| `SWTC/kernel/src/driver/mod.rs` | L28-L32 | fn intr_handler |
| `SWTC/kernel/src/driver/mod.rs` | L96-L100 | trait BlockDevice |
| `SWTC/kernel/src/driver/mod.rs` | L98-L102 | fn read_block |

### 关键代码片段

  - `SWTC/kernel/src/driver/mod.rs:L12-L16`：关键词命中
    代码片段：`use self::{ fu740::{sdcard::SDCard, uart::UART, IntrSource}, plic::{initplic, PLIC}, qemu::virtio_blk::VirtIOBlock,`
  - `SWTC/kernel/src/driver/plic.rs:L4-L8`：关键词命中
    代码片段：`use core::{ops::BitAnd, panic}; use crate::{driver::Mutex, println, sync::mutex::SpinNoIrqLock, utils::error::GeneralRet}; pub struct PLIC { base_addr: usize,`
  - `SWTC/kernel/src/driver/sbi/mod.rs:L43-L47`：关键词命中
    代码片段：`sbi_call(SBI_SET_TIMER, timer, 0, 0); } /// use sbi call to putchar in console (qemu uart handler) pub fn console_putchar(c: usize) { sbi_call(SBI_CONSOLE_PUTCHAR, c, 0, 0);`
  - `SWTC/kernel/src/driver/mod.rs:L28-L32`：fn intr_handler
    代码片段：`#[cfg(not(feature = "board_u740"))] pub fn intr_handler() { let mut plic = PLIC::new(0xffff_ffc0_0c00_0000); let hart_id = local_hart().hart_id();`

### 相关符号

`fn intr_handler` at `SWTC/kernel/src/driver/mod.rs:L28`、`trait BlockDevice` at `SWTC/kernel/src/driver/mod.rs:L96`、`fn read_block` at `SWTC/kernel/src/driver/mod.rs:L98`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
