# 2026 T202610699999765 比较报告

- 对比历史仓库：2022 代码掐架队
- 生成时间：2026-07-02T05:03:20.853731+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 代码掐架队（来源：赛事历史作品）：画像相似度 score=9.36；同属 rcore-variant 风格; 语言构成相似度 0.97; OS 维度重合度 1.00; 代码规模接近度 0.43

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 代码掐架队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console.rs:L5-L9`：关键词命中
    代码片段：`use spin::Mutex; /// Kernel output writer for console struct KernelOutput;`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device drivers //! //! This module provides device driver implementations:`
  - `codes/os/src/console.rs:L13-L17`：关键词命中
    代码片段：`} static CONSOLE: Mutex<ConsoleInner> = Mutex::new(ConsoleInner {}); impl Write for Stdout {`
  - `codes/os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2022 代码掐架队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! This module provides: //! - Virtual filesystem (VFS) abstraction //! - FAT32 and EXT4 filesystem support //! - File descriptor management`
  - `os/src/fs/vfs.rs:L12-L16`：关键词命中
    代码片段：`use super::filesystem::{pre_mount, FS_Type}; // VFS trait, 实现了该trait的文件系统都应该可以直接 // 被 NPUcore 支持 pub trait VFS: DowncastSync {`
  - `codes/os/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`mod pipe; pub mod stdio; mod inode; mod mount; pub mod finfo;`
  - `codes/os/src/fs/finfo.rs:L63-L67`：关键词命中
    代码片段：`} pub fn new(name:&str, inode:usize, offset:isize, reclen: u16, d_type: u8)->Self{ let mut dirent = Self{ d_ino:inode,`
- 与 2022 代码掐架队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/timer.rs:L52-L56`：关键词命中
    代码片段：`} let i = get_time() / freq; //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/utils/interrupt_guard.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt state RAII guard //! //! Provides automatic interrupt state management using RAII pattern`
  - `codes/os/src/trap/mod.rs:L7-L11`：关键词命中
    代码片段：`use crate::syscall::{syscall, test}; use crate::task::*; use crate::timer::set_next_trigger; use riscv::register::{ mtvec::TrapMode,`
  - `codes/os/src/sbi.rs:L29-L33`：关键词命中
    代码片段：`} pub fn set_timer(timer: usize) { sbi_call(SBI_SET_TIMER, timer, 0, 0); }`
- 与 2022 代码掐架队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L4-L8`：关键词命中
    代码片段：`//! - Physical frame allocation (stack-based and bitmap-based strategies) //! - Virtual address space management //! - Page table operations //! - Heap allocation //!`
  - `os/src/mm/map_area.rs:L9-L13`：关键词命中
    代码片段：`use core::{fmt::Debug, mem}; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
  - `codes/os/src/mm/mod.rs:L1-L3`：关键词命中
    代码片段：`mod heap_allocator; mod address; mod frame_allocator;`
  - `codes/os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{PageTable, PageTableEntry, PTEFlags}; use super::{VirtPageNum, VirtAddr, PhysPageNum, PhysAddr}; use super::{FrameTracker, frame_alloc, frame_add_ref, enquire_refcou...`
- 与 2022 代码掐架队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L8-L12`：关键词命中
    代码片段：`pub mod signal; pub mod state_machine; pub mod task; pub mod threads;`
  - `os/src/task/pid.rs:L3-L7`：关键词命中
    代码片段：`//! This module provides: //! - PID allocation with recycling //! - Thread ID allocation //! - RAII-based PID handle for automatic deallocation`
  - `codes/os/src/task/mod.rs:L6-L10`：关键词命中
    代码片段：`mod info; mod switch; mod task; mod resource; pub use resource::RLimit;`
  - `codes/os/src/task/info.rs:L87-L91`：关键词命中
    代码片段：`pub const RUSAGE_SELF:isize = 0; /* The calling process.  */ pub const RUSAGE_CHILDREN:isize = -1; /* All of its terminated child processes.  */ pub const RUSAGE_THREAD:isize =...`
- 与 2022 代码掐架队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/main.rs:L107-L111`：关键词命中
    代码片段：`use core::hint::spin_loop; use core::sync::atomic::{AtomicBool, Ordering}; #[link_section = ".data"]`
  - `os/src/timer.rs:L2-L6`：关键词命中
    代码片段：`use core::cmp::Ordering; use core::ops::{Add, AddAssign, Sub}; use core::sync::atomic::{AtomicI64, Ordering as AtomicOrdering}; pub use crate::hal::{get_clock_freq, get_time};`
  - `codes/os/src/main.rs:L79-L83`：关键词命中
    代码片段：`lazy_static! { static ref CORE2_FLAG: Arc<Mutex<Core2flag>> = Arc::new(Mutex::new(Core2flag { is_in: false })); }`
  - `codes/os/src/console.rs:L1-L5`：关键词命中
    代码片段：`use crate::sbi::console_putchar; use core::fmt::{self, Write}; use spin::Mutex; struct Stdout;`
- 与 2022 代码掐架队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L5160-L5164`：关键词命中
    代码片段：`fn is_loongarch_musl_zero_readlink_buffer(buf: *mut u8, bufsiz: usize) -> bool { // The bundled LoongArch musl turns readlink*(..., size=0) into a dummy // one-byte syscall and...`
  - `os/src/syscall/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! This module implements the kernel's system call interface, providing: //! - Unified syscall dispatch mechanism via function pointer table //! - Context abstraction for s...`
  - `codes/os/src/syscall/fs.rs:L961-L965`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: isize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { if dirfd == A...`
  - `codes/os/src/syscall/mod.rs:L86-L90`：关键词命中
    代码片段：`// println!("test: run sys_getppid 1000000 times, start {:?}",start); for _ in 0..1000000{ syscall(SYSCALL_GETPPID,[0,0,0,0,0,0]); // unsafe{ //     asm!(`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2022 代码掐架队 在“内存管理”维度发现片段级代码相似度 0.95 （token=0.92, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, frame_allocator, heap_allocator, init, init_frame_allocator, init_heap, pub。（置信度：high）
  证据：
  - `os/src/mm/mod.rs:L54-L58`：fn init
    代码片段：`/// Initialize the memory management subsystem pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`
  - `codes/os/src/mm/mod.rs:L29-L33`：fn init
    代码片段：`pub use heap_allocator::HEAP_ALLOCATOR; pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`
- 函数/符号名重合：与 2022 代码掐架队 在“内存管理”维度发现 3 个同名定义：new, alloc, dealloc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `dependency/riscv/src/addr/sv39.rs:L13-L13`：fn new
    代码片段：`fn new(addr: usize) -> Self {`
  - `codes/user/vendor/buddy_system_allocator/src/frame.rs:L39-L39`：fn new
    代码片段：`pub fn new() -> Self {`
  - `dependency/riscv/src/paging/frame_alloc.rs:L7-L7`：fn alloc
    代码片段：`fn alloc(&mut self) -> Option<FrameWith<P>>;`
  - `codes/user/vendor/buddy_system_allocator/src/frame.rs:L76-L76`：fn alloc
    代码片段：`pub fn alloc(&mut self, count: usize) -> Option<usize> {`
- 与 2022 代码掐架队 在“调度与任务管理”维度发现片段级代码相似度 0.68 （token=0.58, structure=0.88）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, impl, new, pub, self。（置信度：medium）
  证据：
  - `os/src/task/elf.rs:L117-L121`：fn new
    代码片段：`impl AuxvEntry { pub fn new(auxv_type: AuxvType, auxv_val: usize) -> Self { Self { auxv_type,`
  - `codes/os/src/task/id.rs:L15-L19`：fn new
    代码片段：`impl RecycleAllocator { pub fn new() -> Self { RecycleAllocator { current: 0,`
- 函数/符号名重合：与 2022 代码掐架队 在“同步机制”维度发现 1 个同名定义：drop。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `dependency/dep_iso/src/block/ahci.rs:L500-L500`：fn drop
    代码片段：`fn drop(&mut self) {`
  - `codes/user/vendor/spin/src/rw_lock.rs:L681-L681`：fn drop
    代码片段：`fn drop(&mut self) {`
- 结构体/类型重合：与 2022 代码掐架队 在“内存管理”维度发现 1 个同名定义：FrameAllocator。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `dependency/riscv/src/paging/frame_alloc.rs:L19-L19`：trait FrameAllocator
    代码片段：`pub trait FrameAllocator {`
  - `codes/user/vendor/buddy_system_allocator/src/frame.rs:L28-L28`：struct FrameAllocator
    代码片段：`pub struct FrameAllocator {`
- 文件路径重合：与 2022 代码掐架队 在“设备驱动”维度出现同名文件源码路径 `os/src/console.rs` / `codes/os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/console.rs:L5-L9`：关键词命中
    代码片段：`use spin::Mutex; /// Kernel output writer for console struct KernelOutput;`
  - `codes/os/src/console.rs:L13-L17`：关键词命中
    代码片段：`} static CONSOLE: Mutex<ConsoleInner> = Mutex::new(ConsoleInner {}); impl Write for Stdout {`
- 文件路径重合：与 2022 代码掐架队 在“中断与异常”维度出现同名文件源码路径 `os/src/timer.rs` / `codes/os/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/timer.rs:L52-L56`：关键词命中
    代码片段：`} let i = get_time() / freq; //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `codes/os/src/timer.rs:L9-L13`：fn get_time
    代码片段：`pub const NSEC_PER_SEC: usize = 1000_000_000; pub fn get_time() -> usize { let mut time:usize = 0; unsafe{`
- 文件路径重合：与 2022 代码掐架队 在“内存管理”维度出现同名文件源码路径 `os/src/mm/memory_set.rs` / `codes/os/src/mm/memory_set.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/mm/memory_set.rs:L1-L5`：关键词命中
    代码片段：`use super::address::VPNRange; use super::map_area::*; use super::page_table::{copy_to_user, copy_to_user_array, PageTable}; use super::{FrameTracker, PhysAddr, PhysPageNum, Virt...`
  - `codes/os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{PageTable, PageTableEntry, PTEFlags}; use super::{VirtPageNum, VirtAddr, PhysPageNum, PhysAddr}; use super::{FrameTracker, frame_alloc, frame_add_ref, enquire_refcou...`
- 文件路径重合：与 2022 代码掐架队 在“系统调用”维度出现同名文件源码路径 `os/src/syscall/fs.rs` / `codes/os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L5160-L5164`：关键词命中
    代码片段：`fn is_loongarch_musl_zero_readlink_buffer(buf: *mut u8, bufsiz: usize) -> bool { // The bundled LoongArch musl turns readlink*(..., size=0) into a dummy // one-byte syscall and...`
  - `codes/os/src/syscall/fs.rs:L961-L965`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: isize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { if dirfd == A...`

## 相似点

- 与 2022 代码掐架队 同属 rcore-variant 风格。（置信度：medium）
- 与 2022 代码掐架队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console.rs:L5-L9`：关键词命中
    代码片段：`use spin::Mutex; /// Kernel output writer for console struct KernelOutput;`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device drivers //! //! This module provides device driver implementations:`
- 与 2022 代码掐架队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! This module provides: //! - Virtual filesystem (VFS) abstraction //! - FAT32 and EXT4 filesystem support //! - File descriptor management`
  - `os/src/fs/vfs.rs:L12-L16`：关键词命中
    代码片段：`use super::filesystem::{pre_mount, FS_Type}; // VFS trait, 实现了该trait的文件系统都应该可以直接 // 被 NPUcore 支持 pub trait VFS: DowncastSync {`
- 与 2022 代码掐架队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/timer.rs:L52-L56`：关键词命中
    代码片段：`} let i = get_time() / freq; //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/utils/interrupt_guard.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt state RAII guard //! //! Provides automatic interrupt state management using RAII pattern`
- 与 2022 代码掐架队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L4-L8`：关键词命中
    代码片段：`//! - Physical frame allocation (stack-based and bitmap-based strategies) //! - Virtual address space management //! - Page table operations //! - Heap allocation //!`
  - `os/src/mm/map_area.rs:L9-L13`：关键词命中
    代码片段：`use core::{fmt::Debug, mem}; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2022 代码掐架队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L8-L12`：关键词命中
    代码片段：`pub mod signal; pub mod state_machine; pub mod task; pub mod threads;`
  - `os/src/task/pid.rs:L3-L7`：关键词命中
    代码片段：`//! This module provides: //! - PID allocation with recycling //! - Thread ID allocation //! - RAII-based PID handle for automatic deallocation`
- 与 2022 代码掐架队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/main.rs:L107-L111`：关键词命中
    代码片段：`use core::hint::spin_loop; use core::sync::atomic::{AtomicBool, Ordering}; #[link_section = ".data"]`
  - `os/src/timer.rs:L2-L6`：关键词命中
    代码片段：`use core::cmp::Ordering; use core::ops::{Add, AddAssign, Sub}; use core::sync::atomic::{AtomicI64, Ordering as AtomicOrdering}; pub use crate::hal::{get_clock_freq, get_time};`
- 与 2022 代码掐架队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L5160-L5164`：关键词命中
    代码片段：`fn is_loongarch_musl_zero_readlink_buffer(buf: *mut u8, bufsiz: usize) -> bool { // The bundled LoongArch musl turns readlink*(..., size=0) into a dummy // one-byte syscall and...`
  - `os/src/syscall/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! This module implements the kernel's system call interface, providing: //! - Unified syscall dispatch mechanism via function pointer table //! - Context abstraction for s...`
- 与 2022 代码掐架队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 代码掐架队 的语言构成不同：待测作品为 {'build': 6639, 'markdown': 21272, 'c': 1673, 'cpp': 311, 'yaml': 116, 'rust': 592255, 'asm': 1647, 'text': 644, 'toml': 9867, 'make': 529, 'json': 90}，历史样本为 {'json': 62, 'build': 2072, 'markdown': 7242, 'asm': 658, 'text': 1942, 'rust': 152844, 'c': 2166, 'toml': 15, 'yaml': 79}。（置信度：medium）

## 可能创新点

- 待测作品包含 208 个历史样本 2022 代码掐架队 中未见的独特函数/类型定义（如 `write32`, `Address`, `write16`, `Rv32PageTableWith`, `Rv39PageTableWith`, `write8`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `dependency/dep_pci/pci/src/lib.rs:L113-L113`：fn write32
    代码片段：`pub unsafe fn write32<T: PortOps>(self, _ops: &T, loc: Location, offset: u16, val: u32) {`
  - `dependency/riscv/src/addr/mod.rs:L1-L1`：trait Address
    代码片段：`pub trait Address: core::fmt::Debug + Copy + Clone + PartialEq + Eq + PartialOrd + Ord {`
- 待测作品代码规模（635,043 LOC）显著大于历史样本 2022 代码掐架队（167,080 LOC），可能包含更多功能模块或更完整的实现，但也可能包含非内核代码，需人工确认。（置信度：low）

## 附录：核验摘要

- 关键结论数：25
- 含证据关键结论数：24（96.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
