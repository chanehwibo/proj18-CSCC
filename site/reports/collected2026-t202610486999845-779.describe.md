# 2026 T202610486999845 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t202610486999845-779`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：87
- 代码/文本行数：97927
- 主要语言：rust 96864 LOC, build 593 LOC, asm 221 LOC, markdown 205 LOC, toml 23 LOC

## 总览

2026 T202610486999845 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 96864 LOC, build: 593 LOC, asm: 221 LOC, markdown: 205 LOC。仓库包含 87 个已扫描文件、约 97927 行可分析文本，当前抽取到 3389 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 5 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, os-la/Cargo.toml, os-la/Makefile, os-la/build.rs, os/Cargo.toml。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`DOCKER_TAG ?= zhouzhouyi/os-contest:20260104 JUDGE_DOCKER_IMAGE ?= zhouzhouyi/os-contest:20260510 TESTSUITE_DIR ?= ../testsuits-for-oskernel`
  - `os-la/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "os" version = "0.1.0"`
  - `os-la/Makefile:L1-L3`：构建入口
    代码片段：`TARGET := loongarch64-unknown-none MODE := release CARGO_TARGET_DIR ?= target-la`
  - `os-la/build.rs:L1-L3`：构建入口
    代码片段：`use std::env; use std::fs::{File, read_dir}; use std::io::{Result, Write};`
  - `os/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "os" version = "0.1.0"`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct UserStackAux, struct PageTable, struct PageFrame, struct KernelStack, fn top。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/task/mod.rs` | L1-L3 | 关键词命中 |
| `os/src/task/user.rs` | L1-L5 | 关键词命中 |
| `os-la/src/task/mod.rs` | L1-L3 | 关键词命中 |
| `os/src/task/user.rs` | L72-L76 | struct UserStackAux |
| `os/src/task/user.rs` | L135-L139 | struct PageTable |
| `os/src/task/user.rs` | L141-L145 | struct PageFrame |

### 关键代码片段

  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User-task runtime services. //! //! This layer groups address-space setup, user-stack construction, and the`
  - `os/src/task/user.rs:L1-L5`：关键词命中
    代码片段：`//! Minimal user address space and enter/return path for contest ELFs. //! //! The 'task' layer owns user-memory setup and the supervisor-to-user handoff, //! keeping execution...`
  - `os-la/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User-task runtime services. //! //! This layer groups address-space setup, user-stack construction, and the`
  - `os/src/task/user.rs:L72-L76`：struct UserStackAux
    代码片段：`const MAX_READY_THREADS: usize = 64; pub(crate) struct UserStackAux { pub entry: usize, pub phdr: usize,`

### 相关符号

`struct UserStackAux` at `os/src/task/user.rs:L72`、`struct PageTable` at `os/src/task/user.rs:L135`、`struct PageFrame` at `os/src/task/user.rs:L141`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：struct PageTable, struct PageTable。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/arch.rs` | L62-L66 | 关键词命中 |
| `os/src/task/user.rs` | L15-L19 | 关键词命中 |
| `os/src/syscall/fs.rs` | L192-L196 | 关键词命中 |
| `os/src/task/user.rs` | L135-L139 | struct PageTable |
| `os-la/src/task/user.rs` | L136-L140 | struct PageTable |

### 关键代码片段

  - `os/src/arch.rs:L62-L66`：关键词命中
    代码片段：`} /// Write 'satp'. #[inline] pub unsafe fn write_satp(bits: usize) {`
  - `os/src/task/user.rs:L15-L19`：关键词命中
    代码片段：`const SATP_MODE_SV39: usize = 8usize << 60; const PTE_V: u64 = 1 << 0; const PTE_R: u64 = 1 << 1; const PTE_W: u64 = 1 << 2;`
  - `os/src/syscall/fs.rs:L192-L196`：关键词命中
    代码片段：`const PROC_CMDLINE_CONTENT: &[u8] = b"root=/dev/vda rw\n"; const PROC_CPUINFO_CONTENT: &[u8] = b"processor\t: 0\nhart\t\t: 0\nisa\t\t: rv64imafdch\nmmu\t\t: sv39\nuarch\t\t: qem...`
  - `os/src/task/user.rs:L135-L139`：struct PageTable
    代码片段：`#[repr(align(4096))] #[derive(Clone, Copy)] struct PageTable { entries: [u64; 512], }`

### 相关符号

`struct PageTable` at `os/src/task/user.rs:L135`、`struct PageTable` at `os-la/src/task/user.rs:L136`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn init, fn trap_handler, fn fatal_trap_signal, fn __alltraps, fn __restore。（置信度：medium）
  - 静态识别到 586 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/syscall/fs.rs` | L1-L3 | 关键词命中 |
| `os/src/syscall/mod.rs` | L369-L373 | 关键词命中 |
| `os-la/src/syscall/fs.rs` | L1-L3 | 关键词命中 |
| `os/src/trap/mod.rs` | L13-L17 | fn init |
| `os/src/trap/mod.rs` | L24-L28 | fn trap_handler |
| `os/src/trap/mod.rs` | L87-L91 | fn fatal_trap_signal |

### 关键代码片段

  - `os/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File, directory, and fd-table syscall subset. //! //! The basic tests run one user program at a time, so a compact global runtime`
  - `os/src/syscall/mod.rs:L369-L373`：关键词命中
    代码片段：`}; /// Result of handling one syscall. pub enum SyscallResult { /// Return this value to user register 'a0'.`
  - `os-la/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File, directory, and fd-table syscall subset. //! //! The basic tests run one user program at a time, so a compact global runtime`
  - `os/src/trap/mod.rs:L13-L17`：fn init
    代码片段：`/// Initialize 'stvec'. pub fn init() { unsafe extern "C" { safe fn __alltraps();`

### 相关符号

`fn init` at `os/src/trap/mod.rs:L13`、`fn trap_handler` at `os/src/trap/mod.rs:L24`、`fn fatal_trap_signal` at `os/src/trap/mod.rs:L87`、`fn sys_umask` at `os/src/syscall/fs.rs:L2186`、`fn sys_openat` at `os/src/syscall/fs.rs:L2297`、`fn sys_openat2` at `os/src/syscall/fs.rs:L2732`、`fn sys_name_to_handle_at` at `os/src/syscall/fs.rs:L2822`、`fn sys_open_by_handle_at` at `os/src/syscall/fs.rs:L2872`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：enum NodeKind, struct LookupResult, struct Metadata, struct Ext4, struct Inode。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/fs/ext4.rs` | L23-L27 | 关键词命中 |
| `os-la/src/fs/ext4.rs` | L23-L27 | 关键词命中 |
| `os/src/syscall/fs.rs` | L798-L802 | 关键词命中 |
| `os/src/fs/ext4.rs` | L28-L32 | enum NodeKind |
| `os/src/fs/ext4.rs` | L39-L43 | struct LookupResult |
| `os/src/fs/ext4.rs` | L48-L52 | struct Metadata |

### 关键代码片段

  - `os/src/fs/ext4.rs:L23-L27`：关键词命中
    代码片段：`const MAX_BLOCK_SIZE: usize = 4096; /// EXT4 root inode number. pub(crate) const ROOT_INODE: u32 = 2;`
  - `os-la/src/fs/ext4.rs:L23-L27`：关键词命中
    代码片段：`const MAX_BLOCK_SIZE: usize = 4096; /// EXT4 root inode number. pub(crate) const ROOT_INODE: u32 = 2;`
  - `os/src/syscall/fs.rs:L798-L802`：关键词命中
    代码片段：`struct FdEntry { kind: u8, inode: u32, mem_index: usize, shared_offset: usize,`
  - `os/src/fs/ext4.rs:L28-L32`：enum NodeKind
    代码片段：`/// File kind returned by directory lookup. #[derive(Clone, Copy, Eq, PartialEq)] pub(crate) enum NodeKind { /// Regular file. File,`

### 相关符号

`enum NodeKind` at `os/src/fs/ext4.rs:L28`、`struct LookupResult` at `os/src/fs/ext4.rs:L39`、`struct Metadata` at `os/src/fs/ext4.rs:L48`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct UPSafeCell, fn new, fn exclusive_access, struct UPSafeCell, fn new。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/task/user.rs` | L9-L13 | 关键词命中 |
| `os/src/syscall/fs.rs` | L1387-L1391 | 关键词命中 |
| `os-la/src/task/user.rs` | L9-L13 | 关键词命中 |
| `os/src/sync/up.rs` | L12-L16 | struct UPSafeCell |
| `os/src/sync/up.rs` | L23-L27 | fn new |
| `os/src/sync/up.rs` | L30-L34 | fn exclusive_access |

### 关键代码片段

  - `os/src/task/user.rs:L9-L13`：关键词命中
    代码片段：`use core::arch::{asm, global_asm}; use core::mem::size_of; use core::sync::atomic::{Ordering, compiler_fence}; const PAGE_SIZE: usize = 4096;`
  - `os/src/syscall/fs.rs:L1387-L1391`：关键词命中
    代码片段：`used: bool, counter: u64, semaphore: bool, }`
  - `os-la/src/task/user.rs:L9-L13`：关键词命中
    代码片段：`use core::arch::{asm, global_asm}; use core::mem::size_of; use core::sync::atomic::{Ordering, compiler_fence}; const PAGE_SIZE: usize = 4096;`
  - `os/src/sync/up.rs:L12-L16`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>, // RefCell 是 Rust 标准库提供的一种智能指针类型，用...`

### 相关符号

`struct UPSafeCell` at `os/src/sync/up.rs:L12`、`fn new` at `os/src/sync/up.rs:L23`、`fn exclusive_access` at `os/src/sync/up.rs:L30`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn init, fn trap_handler, fn fatal_trap_signal, fn __alltraps, fn __restore。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/trap/mod.rs` | L1-L3 | 关键词命中 |
| `os/src/trap/trap.S` | L41-L45 | 关键词命中 |
| `os-la/src/trap/mod.rs` | L1-L3 | 关键词命中 |
| `os/src/trap/mod.rs` | L13-L17 | fn init |
| `os/src/trap/mod.rs` | L24-L28 | fn trap_handler |
| `os/src/trap/mod.rs` | L87-L91 | fn fatal_trap_signal |

### 关键代码片段

  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V trap handling for user-mode contest programs. mod context;`
  - `os/src/trap/trap.S:L41-L45`：关键词命中
    代码片段：`__restore: # case1: start running app by __restore # case2: back to U after handling trap mv sp, a0 # now sp->kernel stack(after allocated), sscratch->user stack`
  - `os-la/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! LoongArch64 trap handling for user-mode contest programs. mod context;`
  - `os/src/trap/mod.rs:L13-L17`：fn init
    代码片段：`/// Initialize 'stvec'. pub fn init() { unsafe extern "C" { safe fn __alltraps();`

### 相关符号

`fn init` at `os/src/trap/mod.rs:L13`、`fn trap_handler` at `os/src/trap/mod.rs:L24`、`fn fatal_trap_signal` at `os/src/trap/mod.rs:L87`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct Stdout, fn write_str, fn print, fn putchar, struct VirtqDesc。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/console.rs` | L1-L3 | 关键词命中 |
| `os/src/drivers/mod.rs` | L1-L3 | 关键词命中 |
| `os-la/src/drivers/mod.rs` | L1-L3 | 关键词命中 |
| `os/src/console.rs` | L4-L8 | struct Stdout |
| `os/src/console.rs` | L7-L11 | fn write_str |
| `os/src/console.rs` | L15-L19 | fn print |

### 关键代码片段

  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use crate::sbi::console_putchar;`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device-driver entry points used by the current contest kernel. //! //! Drivers expose small, capability-oriented APIs to the rest of the kernel so`
  - `os-la/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device-driver entry points used by the current contest kernel. //! //! Drivers expose small, capability-oriented APIs to the rest of the kernel so`
  - `os/src/console.rs:L4-L8`：struct Stdout
    代码片段：`use core::fmt::{self, Write}; struct Stdout; impl Write for Stdout {`

### 相关符号

`struct Stdout` at `os/src/console.rs:L4`、`fn write_str` at `os/src/console.rs:L7`、`fn print` at `os/src/console.rs:L15`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
