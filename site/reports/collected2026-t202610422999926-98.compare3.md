# 2026 T202610422999926 比较报告

- 对比历史仓库：2024 chaos
- 生成时间：2026-07-02T09:02:08.631667+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 chaos（来源：赛事历史作品）：画像相似度 score=9.89；同属 ucore-variant 风格; 语言构成相似度 0.97; OS 维度重合度 1.00; 代码规模接近度 0.94

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 chaos 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = u64;`
  - `os/vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use core::{ arch::asm,`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! block device driver pub mod block;`
- 与 2024 chaos 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/user_bringup_posix_fs.rs:L6-L10`：关键词命中
    代码片段：`pub fn run_stage_posix_fs_meta() { info!("[bringup][posix-fs-meta] BEGIN"); #[cfg(not(feature = "vfs-bridge"))] { warn!("[posix-fs-meta] vfs-bridge off: skip");`
  - `os/components/wateros-base/base-config/src/fs.rs:L1-L5`：关键词命中
    代码片段：`//! 根卷文件 I/O 与页缓存、块设备 LBA 缓存的 bring-up 尺度常量。 //! //! VFS 大文件分流、全局页缓存与 'impl-block-cache' 装饰器均从此处读取默认值。 /// 文件页缓存行大小（字节）；read/write 按该粒度加载与淘汰。`
  - `os/src/fs/fs.rs:L1-L5`：关键词命中
    代码片段：`use alloc::{collections::BTreeMap, sync::Arc}; use super::{inode::Inode, path::Path}; pub trait FileSystem: Send + Sync {`
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`use defs::OpenFlags; use dentry::Dentry; use ext4::fs::Ext4FS; use fs::FileSystemManager;`
- 与 2024 chaos 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/trap_handler.rs:L1-L3`：关键词命中
    代码片段：`//! **组合层内核 trap 路由**：实现各架构 'trap_entry_rust' 交出的 'TrapCause' //! 分支与返回路径， //! 经 ['arch_api_v0::kernel_trap::register_kernel_trap_handler'] 挂接到`
  - `os/components/wateros-platform/platform-arch/arch-api/api-v0/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 帧、异常与中断原因，以及与用户态系统调用 ABI 的读写桥接（**纯架构语义**）。 //! //! ## 'TrapCause' 与架构敏感解码`
  - `os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V timer-related functionality use alloc::{collections::BinaryHeap, sync::Arc};`
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality //! //! For rCore, we have a single trap entry point, namely '__alltraps'. At`
- 与 2024 chaos 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/components/wateros-mm/mm-api/api-v0/src/mmap.rs:L9-L13`：关键词命中
    代码片段：`use crate::error::MmResult; use crate::flags::MapFlags; use crate::frame_allocator::PhysicalFrameAllocator; use crate::perm::PagePerm;`
  - `os/src/main.rs:L10-L14`：关键词命中
    代码片段：`//!    'platform::time::set_frequency_hz'；再初始化控制台与日志、堆分配器，再做 //!    'platform::arch' 与 MM //!   （frame 范围、Sv39、内核页表等；含 'mm' 自检日志）。 //! 3. 初始化任务与组合层 trap 路由（'task::init'、'trap_ha...`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `os/src/mm/address.rs:L112-L116`：关键词命中
    代码片段：`// Self(v & ((1 << VA_WIDTH_SV39) - 1)) let tmp = (v as isize >> VA_WIDTH_SV39) as isize; // 检查传入地址是否合法（SV39标准 assert!(tmp == 0 || tmp == -1, "invalid va: {:#x}", v); Self(v)`
- 与 2024 chaos 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/components/wateros-task/task-api/api-v0/src/task.rs:L32-L36`：关键词命中
    代码片段：`pub enum TaskBlockReason { /// 主动让出 CPU。 Yield, /// 因定时睡眠而阻塞。 Sleep,`
  - `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs:L1-L3`：关键词命中
    代码片段：`//! 任务相关系统调用：'yield'、'exit'、'waitpid'、'getpid'/'getppid'/'gettid'、 //! 'times'、'uname'、'prctl'、'getrlimit'/'setrlimit'。`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Implementation of process ['ProcessControlBlock'] and task(thread) ['TaskControlBlock'] management mechanism //! //! Here is the entry for task scheduling required by other...`
  - `os/src/task/res.rs:L1-L3`：关键词命中
    代码片段：`//! Allocator for pid, task user resource, kernel stack using a simple recycle strategy. use alloc::{`
- 与 2024 chaos 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/vendor/ext4plus/src/sync.rs:L4-L8`：关键词命中
    代码片段：`mod async_inner { /// A mutual exclusion primitive useful for protecting shared data. pub struct Mutex<T> { inner: async_lock::Mutex<T>, }`
  - `os/vendor/concurrent-queue/src/sync.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization facade to choose between 'core' primitives and 'loom' primitives. #[cfg(all(feature = "portable-atomic", not(loom)))] mod sync_impl { pub(crate) use core::cell;`
  - `os/src/sync/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization and interior mutability primitives mod condvar; pub mod mutex; mod semaphore;`
  - `os/src/sync/condvar.rs:L5-L9`：关键词命中
    代码片段：`// use alloc::{collections::VecDeque, sync::Arc}; // use super::mutex::MutexSupport; // /// Condition variable structure`
- 与 2024 chaos 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/components/wateros-klog/src/syscall.rs:L1-L3`：关键词命中
    代码片段：`//! 'sys_syslog' 语义（内核缓冲侧）；用户指针由 'wateros-syscall' 负责拷贝。 //! 本模块代码由AI完成`
  - `os/components/wateros-abi/abi-api/api-v0/src/syscall_args.rs:L1-L5`：关键词命中
    代码片段：`//! 系统调用参数在寄存器/陷阱帧与内核分发层之间的 C 布局承载形式。 //! //! 槽位数量由 'wateros-base-config' 的 'config::syscall::MAX_SYSCALL_ARGS' 决定， //! 与 ABI 约定的参数个数上限一致。`
  - `os/src/syscall/fs.rs:L20-L24`：关键词命中
    代码片段：`}, mm::{translated_byte_buffer, translated_refmut, translated_str}, syscall::{ errno::{EACCES, EBADF, EBUSY, ENOENT, ENOTDIR, ENOTTY}, Dirent,`
  - `os/src/syscall/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Implementation of syscalls //! //! The single entry point to all system calls, ['syscall()'], is called //! whenever userspace wishes to perform a system call using the 'eca...`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 chaos 在“同步机制”维度发现片段级代码相似度 0.68 （token=0.62, structure=0.80）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, inner, new, pub, self, t。（置信度：medium）
  证据：
  - `os/vendor/ext4plus/src/sync.rs:L10-L14`：fn new
    代码片段：`impl<T> Mutex<T> { /// Create a new mutex in an unlocked state ready for use. pub fn new(t: T) -> Self { Self { inner: async_lock::Mutex::new(t),`
  - `os/src/sync/up.rs:L24-L28`：fn new
    代码片段：`/// User is responsible to guarantee that inner struct is only used in /// uniprocessor. pub unsafe fn new(value: T) -> Self { Self { inner: RefCell::new(value),`
- 与 2024 chaos 在“同步机制”维度发现片段级代码相似度 0.61 （token=0.53, structure=0.75）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：inner, pub, struct, t。（置信度：medium）
  证据：
  - `os/vendor/ext4plus/src/sync.rs:L4-L8`：关键词命中
    代码片段：`mod async_inner { /// A mutual exclusion primitive useful for protecting shared data. pub struct Mutex<T> { inner: async_lock::Mutex<T>, }`
  - `os/src/sync/up.rs:L14-L18`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>,`
- 函数/符号名重合：与 2024 chaos 在“内存管理”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/components/wateros-driver/driver-network/network-impl/impl-dummy/src/lib.rs:L22-L22`：fn new
    代码片段：`pub fn new(mac: [u8; 6]) -> Self {`
  - `user/src/bin/user_shell.rs:L30-L30`：fn new
    代码片段：`pub fn new(command: &str) -> Self {`
- 函数/符号名重合：与 2024 chaos 在“同步机制”维度发现 1 个同名定义：from。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/components/wateros-driver/driver-block/block-api/api-v0/src/lib.rs:L22-L22`：fn from
    代码片段：`fn from(value: usize) -> Self { Self(value as u64) }`
  - `os/src/timer.rs:L316-L316`：fn from
    代码片段：`pub fn from(clock_id: usize) -> Self {`
- 文件路径重合：与 2024 chaos 在“文件系统”维度出现同名文件源码路径 `os/components/wateros-base/base-config/src/fs.rs` / `os/src/fs/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/components/wateros-base/base-config/src/fs.rs:L1-L5`：关键词命中
    代码片段：`//! 根卷文件 I/O 与页缓存、块设备 LBA 缓存的 bring-up 尺度常量。 //! //! VFS 大文件分流、全局页缓存与 'impl-block-cache' 装饰器均从此处读取默认值。 /// 文件页缓存行大小（字节）；read/write 按该粒度加载与淘汰。`
  - `os/src/fs/fs.rs:L1-L5`：关键词命中
    代码片段：`use alloc::{collections::BTreeMap, sync::Arc}; use super::{inode::Inode, path::Path}; pub trait FileSystem: Send + Sync {`
- 文件路径重合：与 2024 chaos 在“调度与任务管理”维度出现同名文件源码路径 `os/components/wateros-task/task-api/api-v0/src/task.rs` / `os/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/components/wateros-task/task-api/api-v0/src/task.rs:L32-L36`：关键词命中
    代码片段：`pub enum TaskBlockReason { /// 主动让出 CPU。 Yield, /// 因定时睡眠而阻塞。 Sleep,`
  - `os/src/task/task.rs:L1-L3`：关键词命中
    代码片段：`//! Types related to task management & Functions for completely changing TCB use alloc::{`
- 文件路径重合：与 2024 chaos 在“调度与任务管理”维度出现同名文件源码路径 `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs` / `os/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs:L1-L3`：关键词命中
    代码片段：`//! 任务相关系统调用：'yield'、'exit'、'waitpid'、'getpid'/'getppid'/'gettid'、 //! 'times'、'uname'、'prctl'、'getrlimit'/'setrlimit'。`
  - `os/src/task/task.rs:L1-L3`：关键词命中
    代码片段：`//! Types related to task management & Functions for completely changing TCB use alloc::{`
- 文件路径重合：与 2024 chaos 在“同步机制”维度出现同名文件源码路径 `os/vendor/ext4plus/src/sync.rs` / `os/src/syscall/sync.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/vendor/ext4plus/src/sync.rs:L4-L8`：关键词命中
    代码片段：`mod async_inner { /// A mutual exclusion primitive useful for protecting shared data. pub struct Mutex<T> { inner: async_lock::Mutex<T>, }`
  - `os/src/syscall/sync.rs:L45-L49`：关键词命中
    代码片段：`} // /// mutex create syscall // pub fn sys_mutex_create(blocking: bool) -> isize { //     trace!(`
- 文件路径重合：与 2024 chaos 在“同步机制”维度出现同名文件源码路径 `os/vendor/concurrent-queue/src/sync.rs` / `os/src/syscall/sync.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/vendor/concurrent-queue/src/sync.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization facade to choose between 'core' primitives and 'loom' primitives. #[cfg(all(feature = "portable-atomic", not(loom)))] mod sync_impl { pub(crate) use core::cell;`
  - `os/src/syscall/sync.rs:L45-L49`：关键词命中
    代码片段：`} // /// mutex create syscall // pub fn sys_mutex_create(blocking: bool) -> isize { //     trace!(`

## 相似点

- 与 2024 chaos 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 chaos 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = u64;`
  - `os/vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2024 chaos 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/user_bringup_posix_fs.rs:L6-L10`：关键词命中
    代码片段：`pub fn run_stage_posix_fs_meta() { info!("[bringup][posix-fs-meta] BEGIN"); #[cfg(not(feature = "vfs-bridge"))] { warn!("[posix-fs-meta] vfs-bridge off: skip");`
  - `os/components/wateros-base/base-config/src/fs.rs:L1-L5`：关键词命中
    代码片段：`//! 根卷文件 I/O 与页缓存、块设备 LBA 缓存的 bring-up 尺度常量。 //! //! VFS 大文件分流、全局页缓存与 'impl-block-cache' 装饰器均从此处读取默认值。 /// 文件页缓存行大小（字节）；read/write 按该粒度加载与淘汰。`
- 与 2024 chaos 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/trap_handler.rs:L1-L3`：关键词命中
    代码片段：`//! **组合层内核 trap 路由**：实现各架构 'trap_entry_rust' 交出的 'TrapCause' //! 分支与返回路径， //! 经 ['arch_api_v0::kernel_trap::register_kernel_trap_handler'] 挂接到`
  - `os/components/wateros-platform/platform-arch/arch-api/api-v0/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 帧、异常与中断原因，以及与用户态系统调用 ABI 的读写桥接（**纯架构语义**）。 //! //! ## 'TrapCause' 与架构敏感解码`
- 与 2024 chaos 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/components/wateros-mm/mm-api/api-v0/src/mmap.rs:L9-L13`：关键词命中
    代码片段：`use crate::error::MmResult; use crate::flags::MapFlags; use crate::frame_allocator::PhysicalFrameAllocator; use crate::perm::PagePerm;`
  - `os/src/main.rs:L10-L14`：关键词命中
    代码片段：`//!    'platform::time::set_frequency_hz'；再初始化控制台与日志、堆分配器，再做 //!    'platform::arch' 与 MM //!   （frame 范围、Sv39、内核页表等；含 'mm' 自检日志）。 //! 3. 初始化任务与组合层 trap 路由（'task::init'、'trap_ha...`
- 与 2024 chaos 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/components/wateros-task/task-api/api-v0/src/task.rs:L32-L36`：关键词命中
    代码片段：`pub enum TaskBlockReason { /// 主动让出 CPU。 Yield, /// 因定时睡眠而阻塞。 Sleep,`
  - `os/components/wateros-syscall/syscall-impl/impl-kernel/src/sys/task.rs:L1-L3`：关键词命中
    代码片段：`//! 任务相关系统调用：'yield'、'exit'、'waitpid'、'getpid'/'getppid'/'gettid'、 //! 'times'、'uname'、'prctl'、'getrlimit'/'setrlimit'。`
- 与 2024 chaos 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/vendor/ext4plus/src/sync.rs:L4-L8`：关键词命中
    代码片段：`mod async_inner { /// A mutual exclusion primitive useful for protecting shared data. pub struct Mutex<T> { inner: async_lock::Mutex<T>, }`
  - `os/vendor/concurrent-queue/src/sync.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization facade to choose between 'core' primitives and 'loom' primitives. #[cfg(all(feature = "portable-atomic", not(loom)))] mod sync_impl { pub(crate) use core::cell;`
- 与 2024 chaos 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/components/wateros-klog/src/syscall.rs:L1-L3`：关键词命中
    代码片段：`//! 'sys_syslog' 语义（内核缓冲侧）；用户指针由 'wateros-syscall' 负责拷贝。 //! 本模块代码由AI完成`
  - `os/components/wateros-abi/abi-api/api-v0/src/syscall_args.rs:L1-L5`：关键词命中
    代码片段：`//! 系统调用参数在寄存器/陷阱帧与内核分发层之间的 C 布局承载形式。 //! //! 槽位数量由 'wateros-base-config' 的 'config::syscall::MAX_SYSCALL_ARGS' 决定， //! 与 ABI 约定的参数个数上限一致。`
- 与 2024 chaos 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 chaos 的语言构成不同：待测作品为 {'build': 8075, 'markdown': 14040, 'toml': 307, 'rust': 419201, 'asm': 728, 'text': 2102, 'json': 387, 'yaml': 128}，历史样本为 {'yaml': 63, 'json': 76, 'markdown': 12073, 'build': 3169, 'toml': 9675, 'c': 1011, 'asm': 791, 'text': 268, 'rust': 388593}。（置信度：medium）

## 可能创新点

- 待测作品包含 273 个历史样本 2024 chaos 中未见的独特函数/类型定义（如 `socket_setsockopt`, `socket_sendto`, `socket_getsockopt`, `socket_recvfrom`, `socket_bind`, `socket_accept`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `os/components/wateros-driver/driver-network/src/lib.rs:L947-L947`：fn socket_setsockopt
    代码片段：`pub fn socket_setsockopt(handle: SocketHandle, level: usize, optname: usize, optval: &[u8]) -> Result<(), &'static str> {`
  - `os/components/wateros-driver/driver-network/src/lib.rs:L745-L745`：fn socket_sendto
    代码片段：`pub fn socket_sendto(handle: SocketHandle, data: &[u8], ip: [u8; 4], port: u16) -> Result<usize, &'static str> {`

## 附录：核验摘要

- 关键结论数：24
- 含证据关键结论数：24（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
