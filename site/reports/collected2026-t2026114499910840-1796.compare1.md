# 2026 T2026114499910840 比较报告

- 对比历史仓库：2023 种田队
- 生成时间：2026-07-01T21:19:29.437014+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2023 种田队（来源：赛事历史作品）：画像相似度 score=9.53；同属 ucore-variant 风格; 语言构成相似度 0.91; OS 维度重合度 1.00; 代码规模接近度 0.71

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2023 种田队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/drivers/fb.c:L137-L141`：关键词命中
    代码片段：`sysfs_write_node(modes, content, strlen(content), 0); vfs_node_t *device = sysfs_child_append(node, "device", true); vfs_node_t *subsystem = sysfs_child_append_symlink(device, "...`
  - `kernel/src/drivers/fb.h:L35-L39`：关键词命中
    代码片段：`/* (physical address) */ uint32_t mmio_len;     /* length of Memory Mapped I/O  */ uint32_t accel;        /* Indicate to driver which	*/ /*  specific chip/card we have	*/ uint16...`
  - `kern/dev/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kern/driver/sd.c:L320-L324`：关键词命中
    代码片段：`int sdInit() { REG32(uart, UART_REG_TXCTRL) = UART_TXEN; sd_poweron(3000);`
- 与 2023 种田队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs/dev.c:L1-L4`：关键词命中
    代码片段：`#include <libs/keys.h> #include <fs/vfs/vfs.h> #include <fs/sys.h> #include <block/partition.h>`
  - `kernel/src/fs/dev.h:L1-L5`：关键词命中
    代码片段：`#pragma once #include <fs/vfs/vfs.h> #include <dev/device.h> #include <libs/mutex.h>`
  - `kern/fs/fd/fd.c:L4-L8`：关键词命中
    代码片段：`#include <fs/file.h> #include <fs/pipe.h> #include <fs/vfs.h> #include <lib/log.h> #include <lib/string.h>`
  - `kern/fs/cluster.c:L35-L39`：关键词命中
    代码片段：`return -E_UNKNOWN_FS; } fs->superBlock.bpb.bytes_per_sec = bpb->BPB_BytsPerSec; fs->superBlock.bpb.sec_per_clus = bpb->BPB_SecPerClus; fs->superBlock.bpb.rsvd_sec_cnt = bpb->BPB...`
- 与 2023 种田队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/irq/softirq.c:L1-L3`：关键词命中
    代码片段：`#include <irq/softirq.h> static softirq_handler_t softirq_handlers[SOFTIRQ_MAX] = {0};`
  - `kernel/src/irq/irq_manager.c:L1-L3`：关键词命中
    代码片段：`#include <irq/irq_manager.h> #include <drivers/logger.h> #include <arch/arch.h>`
  - `kern/dev/plic.c:L4-L8`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC) // RISCV平台级中断控制器 //`
  - `kern/dev/timer.c:L5-L9`：关键词命中
    代码片段：`#include "riscv.h" #include "types.h" #include <dev/timer.h> #include <sys/time.h> #include <trap/trap.h>`
- 与 2023 种田队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/mm/mm.c:L427-L431`：关键词命中
    代码片段：`} uint64_t map_change_attribute_range(uint64_t *pgdir, uint64_t vaddr, uint64_t len, uint64_t flags) { for (uint64_t va = vaddr; va < vaddr + len; va += PAGE_SIZE) {`
  - `kernel/src/mm/mm.h:L4-L8`：关键词命中
    代码片段：`#include <mm/hhdm.h> #include <mm/buddy.h> #include <mm/page_table.h> #include <arch/arch.h> #include <mm/vma.h>`
  - `kern/mm/mmu.c:L26-L30`：关键词命中
    代码片段：`// // 等待之前对页表的写操作结束 // sfence_vma(); extern Pte *kernPd; w_satp(MAKE_SATP(kernPd));`
  - `kern/mm/pmm.c:L9-L13`：关键词命中
    代码片段：`#include <lib/log.h> #include <lib/string.h> #include <mm/pmm.h> #include <param.h> #include <proc/proc.h>`
- 与 2023 种田队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs/proc.h:L1-L3`：关键词命中
    代码片段：`#pragma once #include <fs/proc/proc.h>`
  - `kernel/src/task/ns.c:L1-L4`：关键词命中
    代码片段：`#include <fs/vfs/vfs.h> #include <task/ns.h> #include <task/task.h> #include <arch/task_abi.h>`
  - `kern/proc/cpu.c:L4-L8`：关键词命中
    代码片段：`#include <lib/string.h> #include <lock/mutex.h> #include <proc/cpu.h> #include <proc/thread.h> #include <riscv.h>`
  - `kern/proc/elf.c:L4-L8`：关键词命中
    代码片段：`#include <lib/transfer.h> #include <mm/vmm.h> #include <proc/proc.h> #include <proc/thread.h> #include <proc/dynamic_link.h>`
- 与 2023 种田队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/acpi/mutex.c:L1-L3`：关键词命中
    代码片段：`#include <uacpi/platform/atomic.h> #include <uacpi/internal/mutex.h> #include <uacpi/internal/log.h>`
  - `kernel/src/libs/mutex.c:L1-L3`：关键词命中
    代码片段：`#include <libs/mutex.h> #include <task/task.h>`
  - `kern/lock/lock.c:L1-L4`：关键词命中
    代码片段：`#include <lib/log.h> #include <lock/lock.h> #include <proc/cpu.h> #include <riscv.h>`
  - `kern/lock/mutex.c:L1-L5`：关键词命中
    代码片段：`#include <lib/log.h> #include <lib/printf.h> #include <lock/mutex.h> #include <proc/cpu.h> #include <proc/sleep.h>`
- 与 2023 种田队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs/fs_syscall.h:L568-L572`：关键词命中
    代码片段：`* delegates to vfs_read_file(). * Notes: a zero-length request still goes through the normal file path instead * of being short-circuited in the syscall wrapper. */ uint64_t sys...`
  - `kernel/src/mm/mm_syscall.h:L144-L148`：关键词命中
    代码片段：`* Notes: mincore() is often used as a hinting or observability tool rather * than a correctness primitive. Callers should not assume that residency * answers stay true after the...`
  - `kern/syscall/sys_fs.c:L17-L21`：关键词命中
    代码片段：`#include <proc/thread.h> #include <sys/errno.h> #include <sys/syscall.h> #include <sys/syscall_fs.h> #include <sys/time.h>`
  - `kern/syscall/sys_mm.c:L5-L9`：关键词命中
    代码片段：`#include <proc/proc.h> #include <proc/thread.h> #include <sys/syscall.h> #define PASSIVE_THRESHOLD 0x2000000`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 宏名重合：与 2023 种田队 在“文件系统”维度发现 1 个同名定义：offsetof。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/freestnd-c-hdrs/stddef.h:L42-L42`：macro offsetof
    代码片段：`#define offsetof(s, m) __builtin_offsetof(s, m)`
  - `include/types.h:L45-L45`：macro offsetof
    代码片段：`#define offsetof(type, member) ((size_t) & ((type *)0)->member)`
- 文件路径重合：与 2023 种田队 在“同步机制”维度出现同名文件源码路径 `kernel/src/acpi/mutex.c` / `kern/lock/mutex.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/acpi/mutex.c:L1-L3`：关键词命中
    代码片段：`#include <uacpi/platform/atomic.h> #include <uacpi/internal/mutex.h> #include <uacpi/internal/log.h>`
  - `kern/lock/mutex.c:L1-L5`：关键词命中
    代码片段：`#include <lib/log.h> #include <lib/printf.h> #include <lock/mutex.h> #include <proc/cpu.h> #include <proc/sleep.h>`
- 文件路径重合：与 2023 种田队 在“同步机制”维度出现同名文件源码路径 `kernel/src/libs/mutex.c` / `kern/lock/mutex.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/libs/mutex.c:L1-L3`：关键词命中
    代码片段：`#include <libs/mutex.h> #include <task/task.h>`
  - `kern/lock/mutex.c:L1-L5`：关键词命中
    代码片段：`#include <lib/log.h> #include <lib/printf.h> #include <lock/mutex.h> #include <proc/cpu.h> #include <proc/sleep.h>`

## 相似点

- 与 2023 种田队 同属 ucore-variant 风格。（置信度：medium）
- 与 2023 种田队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/drivers/fb.c:L137-L141`：关键词命中
    代码片段：`sysfs_write_node(modes, content, strlen(content), 0); vfs_node_t *device = sysfs_child_append(node, "device", true); vfs_node_t *subsystem = sysfs_child_append_symlink(device, "...`
  - `kernel/src/drivers/fb.h:L35-L39`：关键词命中
    代码片段：`/* (physical address) */ uint32_t mmio_len;     /* length of Memory Mapped I/O  */ uint32_t accel;        /* Indicate to driver which	*/ /*  specific chip/card we have	*/ uint16...`
- 与 2023 种田队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs/dev.c:L1-L4`：关键词命中
    代码片段：`#include <libs/keys.h> #include <fs/vfs/vfs.h> #include <fs/sys.h> #include <block/partition.h>`
  - `kernel/src/fs/dev.h:L1-L5`：关键词命中
    代码片段：`#pragma once #include <fs/vfs/vfs.h> #include <dev/device.h> #include <libs/mutex.h>`
- 与 2023 种田队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/irq/softirq.c:L1-L3`：关键词命中
    代码片段：`#include <irq/softirq.h> static softirq_handler_t softirq_handlers[SOFTIRQ_MAX] = {0};`
  - `kernel/src/irq/irq_manager.c:L1-L3`：关键词命中
    代码片段：`#include <irq/irq_manager.h> #include <drivers/logger.h> #include <arch/arch.h>`
- 与 2023 种田队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/mm/mm.c:L427-L431`：关键词命中
    代码片段：`} uint64_t map_change_attribute_range(uint64_t *pgdir, uint64_t vaddr, uint64_t len, uint64_t flags) { for (uint64_t va = vaddr; va < vaddr + len; va += PAGE_SIZE) {`
  - `kernel/src/mm/mm.h:L4-L8`：关键词命中
    代码片段：`#include <mm/hhdm.h> #include <mm/buddy.h> #include <mm/page_table.h> #include <arch/arch.h> #include <mm/vma.h>`
- 与 2023 种田队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs/proc.h:L1-L3`：关键词命中
    代码片段：`#pragma once #include <fs/proc/proc.h>`
  - `kernel/src/task/ns.c:L1-L4`：关键词命中
    代码片段：`#include <fs/vfs/vfs.h> #include <task/ns.h> #include <task/task.h> #include <arch/task_abi.h>`
- 与 2023 种田队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/acpi/mutex.c:L1-L3`：关键词命中
    代码片段：`#include <uacpi/platform/atomic.h> #include <uacpi/internal/mutex.h> #include <uacpi/internal/log.h>`
  - `kernel/src/libs/mutex.c:L1-L3`：关键词命中
    代码片段：`#include <libs/mutex.h> #include <task/task.h>`
- 与 2023 种田队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs/fs_syscall.h:L568-L572`：关键词命中
    代码片段：`* delegates to vfs_read_file(). * Notes: a zero-length request still goes through the normal file path instead * of being short-circuited in the syscall wrapper. */ uint64_t sys...`
  - `kernel/src/mm/mm_syscall.h:L144-L148`：关键词命中
    代码片段：`* Notes: mincore() is often used as a hinting or observability tool rather * than a correctness primitive. Callers should not assume that residency * answers stay true after the...`
- 与 2023 种田队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2023 种田队 的语言构成不同：待测作品为 {'markdown': 670, 'c': 299469, 'asm': 2076, 'text': 61}，历史样本为 {'yaml': 21, 'json': 20, 'make': 88, 'build': 540, 'markdown': 3567, 'c': 415312, 'text': 55, 'asm': 480, 'cpp': 36715}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：17
- 含证据关键结论数：17（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
