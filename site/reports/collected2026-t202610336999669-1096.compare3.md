# 2026 T202610336999669 比较报告

- 对比历史仓库：2025 sudo_pacman_Syu
- 生成时间：2026-07-02T05:00:23.647733+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 sudo_pacman_Syu（来源：赛事历史作品）：画像相似度 score=9.23；同属 rcore-variant 风格; 语言构成相似度 0.98; OS 维度重合度 1.00; 代码规模接近度 0.26

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 sudo_pacman_Syu 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/drivers/mod.rs:L1-L5`：关键词命中
    代码片段：`//! StellarOS 设备驱动模块（§六.B per-component 分层） //! //! - block/:  VirtIO-Blk 块设备驱动（MMIO/PCI 双 transport） //! - net/:    VirtIO-Net 网卡驱动 + NetDevice trait + NET_DEVICES 注册表 //! - vi...`
  - `os/src/drivers/irq/mod.rs:L5-L9`：关键词命中
    代码片段：`//! - loongarch64：EIOINTC/PCH-PIC 尚未接入，全部为 no-op； //!   运行期由 'arch::has_external_interrupts()=false' 保证不依赖外部中断 //!   （virtio-net 由 timer tick 的 poll_interfaces 驱动）。 #[cfg(target...`
  - `src/kernel/console.rs:L2-L6`：关键词命中
    代码片段：`use alloc::sync::Arc; static CONSOLE: Spin<Option<Arc<Terminal>>> = Spin::new(None); pub fn set_console(terminal: Arc<Terminal>) -> KResult<()> {`
  - `src/kernel/pcie/driver.rs:L1-L4`：关键词命中
    代码片段：`use super::{ device::{PCIDevice, PCIE_DEVICES}, error::PciError, };`
- 与 2025 sudo_pacman_Syu 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! 分层目录结构（2026-06-11 子文件夹化重整）： //!   - vfs/      VFS 核心：VfsNode trait（mod.rs）+ MountTable/MountedFs（mount.rs） //!               + xattr + record_lock（POSIX/OFD 记录锁） //!   -...`
  - `os/src/fs/fifo.rs:L16-L20`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicU32, Ordering}; use crate::fs::vfs::{NodeStat, VfsNode}; use crate::mm::UserBuffer; use crate::sync::SpinNoIrqLock;`
  - `src/fs/shm.rs:L7-L11`：关键词命中
    代码片段：`use crate::{ fs::tmpfs::{DirectoryInode, FileInode, TmpFs}, kernel::{constants::ENOSPC, vfs::inode::Mode}, prelude::KResult, };`
  - `src/fs/ext4.rs:L4-L8`：关键词命中
    代码片段：`use crate::kernel::task::block_on; use crate::kernel::timer::Ticks; use crate::kernel::vfs::inode::{AtomicMode, Mode}; use crate::{ io::{Buffer, ByteBuffer, Stream},`
- 与 2025 sudo_pacman_Syu 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/drivers/irq/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! 共享驱动（block/net）只调用本模块的统一接口，不直接耦合具体控制器： //! - riscv64：PLIC（'plic.rs'）。 //! - loongarch64：EIOINTC/PCH-PIC 尚未接入，全部为 no-op； //!   运行期由 'arch::has_external_interrupts()=false...`
  - `os/src/drivers/irq/plic.rs:L1-L3`：关键词命中
    代码片段：`//! PLIC (Platform-Level Interrupt Controller) 驱动 — SMP 多核版本 //! //! QEMU virt 机器 PLIC 基地址：0x0c00_0000`
  - `src/kernel/timer.rs:L170-L174`：关键词命中
    代码片段：`pub fn timer_interrupt() { if CPU::local().cpuid() != 0 { // Only the BSP should handle the timer interrupt. return; }`
  - `src/kernel/interrupt.rs:L1-L5`：关键词命中
    代码片段：`use super::mem::handle_kernel_page_fault; use super::task::block_on; use super::timer::timer_interrupt; use crate::kernel::constants::EINVAL; use crate::prelude::*;`
- 与 2025 sudo_pacman_Syu 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`#[cfg(feature = "asid")] pub mod asid; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/asid.rs:L3-L7`：关键词命中
    代码片段：`//! # 为什么需要 //! 本内核用 trampoline 模型：内核与用户是**独立页表**，故每次 trap(syscall/异常/中断) //! 进出都要切 'satp' 并 'sfence.vma' **全刷 TLB**（trap.S 入核 line76 / 返用户 line91）。 //! 全刷丢掉整个 TLB、后续大量重填，是 sysc...`
  - `src/kernel/mem/mm_area.rs:L8-L12`：关键词命中
    代码片段：`use core::cmp; use eonix_mm::address::{AddrOps as _, VAddr, VRange}; use eonix_mm::page_table::{PageAttribute, RawAttribute, PTE}; use eonix_mm::paging::{PAGE_SIZE, PFN};`
  - `src/kernel/mem/mm_list.rs:L17-L21`：关键词命中
    代码片段：`}; use eonix_mm::address::PAddr; use eonix_mm::page_table::PageAttribute; use eonix_mm::paging::PFN; use eonix_mm::{`
- 与 2025 sudo_pacman_Syu 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L25-L29`：关键词命中
    代码片段：`// 从 1 起:首个分配的任务(initproc)拿 tid/pid=1,对齐 Linux(init=pid 1; // pid 0 为概念上的 swapper/idle,本内核 idle 非 tid 化任务故 0 直接保留不分配)。 // 修正 getpgid01//proc/1/stat/sched_setscheduler01/setprior...`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! 任务管理模块 - Linux 风格统一 Task 模型 + 有栈协程调度 //! //! 架构：`
  - `src/kernel/task.rs:L8-L12`：关键词命中
    代码片段：`mod session; mod signal; mod thread; pub use clone::{do_clone, CloneArgs, CloneFlags};`
  - `src/kernel/task/clone.rs:L2-L6`：关键词命中
    代码片段：`kernel::{ syscall::{procops::parse_user_tls, UserMut}, task::{alloc_pid, ProcessBuilder, ProcessList, Thread, ThreadBuilder}, user::UserPointerMut, },`
- 与 2025 sudo_pacman_Syu 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/sync/mod.rs:L5-L9`：关键词命中
    代码片段：`//! - 'UPSafeCellRaw<T>' / 'UPIntrFreeCell<T>': 已废弃的单核原语（保留在 ['up'](os/src/sync/up.rs) 模块内兼容旧设计） mod spin_lock; pub mod up; pub mod wait_queue;`
  - `os/src/sync/spin_lock.rs:L7-L11`：关键词命中
    代码片段：`//! - **获取锁时**：先关闭本核 S-mode 中断（'sstatus.SIE'），再自旋等待 //! - **释放锁时**：先释放自旋锁，再恢复中断状态 //! - **嵌套安全**：使用 per-lock SIE 保存，支持嵌套获取不同的锁 //! - **SMP-ready**：底层使用 'spin::Mutex' 的原子自旋 //!`
  - `src/sync.rs:L1-L4`：关键词命中
    代码片段：`mod arcswap; mod condvar; pub use arcswap::ArcSwap;`
  - `src/sync/arcswap.rs:L3-L7`：关键词命中
    代码片段：`fmt::{self, Debug, Formatter}, ptr::NonNull, sync::atomic::{AtomicPtr, Ordering}, }; use pointers::BorrowedArc;`
- 与 2025 sudo_pacman_Syu 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/mm.rs:L1366-L1370`：关键词命中
    代码片段：`} /// sys_mseal(addr, len, flags) -> 0 / -errno（Linux 6.10，syscall 462，E-3） /// /// 内存封印：封印后该区间的 munmap/mprotect/mremap/mmap(MAP_FIXED 覆盖)/`
  - `os/src/syscall/nr.rs:L1-L3`：关键词命中
    代码片段：`//! Linux syscall numbers shared by the syscall dispatcher. //! //! RISC-V64 and LoongArch64 both use the Linux asm-generic syscall table for`
  - `src/kernel/syscall/net.rs:L6-L10`：关键词命中
    代码片段：`use crate::io::IntoStream; use crate::kernel::constants::{EAFNOSUPPORT, EBADF, EINVAL, ENOTSOCK}; use crate::kernel::syscall::file_rw::IoVec; use crate::kernel::syscall::User; u...`
  - `src/kernel/syscall/file_rw.rs:L4-L8`：关键词命中
    代码片段：`EBADF, EFAULT, EINVAL, ENOENT, ENOTDIR, SEEK_CUR, SEEK_END, SEEK_SET, }; use crate::kernel::syscall::UserMut; use crate::kernel::task::Thread; use crate::kernel::timer::sleep;`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 sudo_pacman_Syu 在“同步机制”维度发现片段级代码相似度 0.59 （token=0.50, structure=0.75）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, impl, new, pub, self, t。（置信度：medium）
  证据：
  - `os/src/sync/up.rs:L11-L15`：fn new
    代码片段：`impl<T> UPSafeCellRaw<T> { pub unsafe fn new(value: T) -> Self { Self { inner: UnsafeCell::new(value),`
  - `src/sync/arcswap.rs:L15-L19`：fn new
    代码片段：`impl<T> ArcSwap<T> { pub fn new(data: T) -> Self { let pointer = Arc::into_raw(Arc::new(data)); Self {`
- 函数/符号名重合：与 2025 sudo_pacman_Syu 在“设备驱动”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/smoltcp-local/src/iface/interface/mod.rs:L161-L161`：fn new
    代码片段：`pub fn new<D>(config: Config, device: &mut D, now: Instant) -> Self`
  - `src/driver/e1000e.rs:L88-L88`：fn new
    代码片段：`fn new(base: PAddr) -> Self {`
- 函数/符号名重合：与 2025 sudo_pacman_Syu 在“同步机制”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/smoltcp-local/examples/loopback.rs:L28-L28`：fn new
    代码片段：`pub fn new() -> Clock {`
  - `src/fs/ext4.rs:L41-L41`：fn new
    代码片段：`pub fn new(device: Arc<BlockDevice>) -> Self {`
- 文件路径重合：与 2025 sudo_pacman_Syu 在“内存管理”维度出现同名文件源码路径 `os/src/syscall/mm.rs` / `src/kernel/syscall/mm.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/syscall/mm.rs:L202-L206`：关键词命中
    代码片段：`// 校验映射范围 [map_addr, map_addr+len) 不溢出且整体落在用户地址空间内 // （Sv39 用户下半 [0, 0x40_0000_0000)）。否则 VirtAddr::from 会按 39 位掩码把 // 高地址截低，构造出 start>end 的 VPNRange 触发内核 panic。 // 典型触发：mmap15 用...`
  - `src/kernel/syscall/mm.rs:L19-L23`：fn from_arg
    代码片段：`impl FromSyscallArg for UserMmapProtocol { fn from_arg(value: usize) -> UserMmapProtocol { UserMmapProtocol::from_bits_truncate(value as u32) }`

## 相似点

- 与 2025 sudo_pacman_Syu 同属 rcore-variant 风格。（置信度：medium）
- 与 2025 sudo_pacman_Syu 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/drivers/mod.rs:L1-L5`：关键词命中
    代码片段：`//! StellarOS 设备驱动模块（§六.B per-component 分层） //! //! - block/:  VirtIO-Blk 块设备驱动（MMIO/PCI 双 transport） //! - net/:    VirtIO-Net 网卡驱动 + NetDevice trait + NET_DEVICES 注册表 //! - vi...`
  - `os/src/drivers/irq/mod.rs:L5-L9`：关键词命中
    代码片段：`//! - loongarch64：EIOINTC/PCH-PIC 尚未接入，全部为 no-op； //!   运行期由 'arch::has_external_interrupts()=false' 保证不依赖外部中断 //!   （virtio-net 由 timer tick 的 poll_interfaces 驱动）。 #[cfg(target...`
- 与 2025 sudo_pacman_Syu 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! 分层目录结构（2026-06-11 子文件夹化重整）： //!   - vfs/      VFS 核心：VfsNode trait（mod.rs）+ MountTable/MountedFs（mount.rs） //!               + xattr + record_lock（POSIX/OFD 记录锁） //!   -...`
  - `os/src/fs/fifo.rs:L16-L20`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicU32, Ordering}; use crate::fs::vfs::{NodeStat, VfsNode}; use crate::mm::UserBuffer; use crate::sync::SpinNoIrqLock;`
- 与 2025 sudo_pacman_Syu 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/drivers/irq/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! 共享驱动（block/net）只调用本模块的统一接口，不直接耦合具体控制器： //! - riscv64：PLIC（'plic.rs'）。 //! - loongarch64：EIOINTC/PCH-PIC 尚未接入，全部为 no-op； //!   运行期由 'arch::has_external_interrupts()=false...`
  - `os/src/drivers/irq/plic.rs:L1-L3`：关键词命中
    代码片段：`//! PLIC (Platform-Level Interrupt Controller) 驱动 — SMP 多核版本 //! //! QEMU virt 机器 PLIC 基地址：0x0c00_0000`
- 与 2025 sudo_pacman_Syu 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`#[cfg(feature = "asid")] pub mod asid; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/asid.rs:L3-L7`：关键词命中
    代码片段：`//! # 为什么需要 //! 本内核用 trampoline 模型：内核与用户是**独立页表**，故每次 trap(syscall/异常/中断) //! 进出都要切 'satp' 并 'sfence.vma' **全刷 TLB**（trap.S 入核 line76 / 返用户 line91）。 //! 全刷丢掉整个 TLB、后续大量重填，是 sysc...`
- 与 2025 sudo_pacman_Syu 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L25-L29`：关键词命中
    代码片段：`// 从 1 起:首个分配的任务(initproc)拿 tid/pid=1,对齐 Linux(init=pid 1; // pid 0 为概念上的 swapper/idle,本内核 idle 非 tid 化任务故 0 直接保留不分配)。 // 修正 getpgid01//proc/1/stat/sched_setscheduler01/setprior...`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! 任务管理模块 - Linux 风格统一 Task 模型 + 有栈协程调度 //! //! 架构：`
- 与 2025 sudo_pacman_Syu 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/sync/mod.rs:L5-L9`：关键词命中
    代码片段：`//! - 'UPSafeCellRaw<T>' / 'UPIntrFreeCell<T>': 已废弃的单核原语（保留在 ['up'](os/src/sync/up.rs) 模块内兼容旧设计） mod spin_lock; pub mod up; pub mod wait_queue;`
  - `os/src/sync/spin_lock.rs:L7-L11`：关键词命中
    代码片段：`//! - **获取锁时**：先关闭本核 S-mode 中断（'sstatus.SIE'），再自旋等待 //! - **释放锁时**：先释放自旋锁，再恢复中断状态 //! - **嵌套安全**：使用 per-lock SIE 保存，支持嵌套获取不同的锁 //! - **SMP-ready**：底层使用 'spin::Mutex' 的原子自旋 //!`
- 与 2025 sudo_pacman_Syu 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/mm.rs:L1366-L1370`：关键词命中
    代码片段：`} /// sys_mseal(addr, len, flags) -> 0 / -errno（Linux 6.10，syscall 462，E-3） /// /// 内存封印：封印后该区间的 munmap/mprotect/mremap/mmap(MAP_FIXED 覆盖)/`
  - `os/src/syscall/nr.rs:L1-L3`：关键词命中
    代码片段：`//! Linux syscall numbers shared by the syscall dispatcher. //! //! RISC-V64 and LoongArch64 both use the Linux asm-generic syscall table for`
- 与 2025 sudo_pacman_Syu 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 sudo_pacman_Syu 的语言构成不同：待测作品为 {'build': 6783, 'markdown': 14760, 'toml': 168, 'json': 272, 'text': 313, 'rust': 638842, 'asm': 4519, 'c': 6328, 'yaml': 28}，历史样本为 {'json': 36, 'toml': 86, 'build': 455, 'markdown': 1434, 'text': 40, 'rust': 39368}。（置信度：medium）

## 可能创新点

- 待测作品包含 327 个历史样本 2025 sudo_pacman_Syu 中未见的独特函数/类型定义（如 `dispatch_ipv4_frag`, `igmp_leave_packet`, `poll_delay`, `accept_ipv4_mapped_ipv6_part`, `get_source_address_ipv4`, `get_source_address_ipv6`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `os/smoltcp-local/src/iface/interface/ipv4.rs:L340-L340`：fn dispatch_ipv4_frag
    代码片段：`pub(super) fn dispatch_ipv4_frag<Tx: TxToken>(&mut self, tx_token: Tx, frag: &mut Fragmenter) {`
  - `os/smoltcp-local/src/iface/interface/ipv4.rs:L430-L430`：fn igmp_leave_packet
    代码片段：`pub(super) fn igmp_leave_packet<'any>(&self, group_addr: Ipv4Address) -> Option<Packet<'any>> {`
- 待测作品代码规模（672,013 LOC）显著大于历史样本 2025 sudo_pacman_Syu（41,419 LOC），可能包含更多功能模块或更完整的实现，但也可能包含非内核代码，需人工确认。（置信度：low）

## 附录：核验摘要

- 关键结论数：20
- 含证据关键结论数：19（95.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
