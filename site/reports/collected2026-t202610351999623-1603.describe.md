# 2026 T202610351999623 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t202610351999623-1603`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：160
- 代码/文本行数：37784
- 主要语言：rust 25750 LOC, markdown 11117 LOC, build 508 LOC, asm 350 LOC, toml 37 LOC

## 总览

2026 T202610351999623 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 25750 LOC, markdown: 11117 LOC, build: 508 LOC, asm: 350 LOC。仓库包含 160 个已扫描文件、约 37784 行可分析文本，当前抽取到 978 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 3 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, kernel/Cargo.toml, kernel/build.rs, kernel/src/arch/riscv64/boot/linker.ld, user/hello/Cargo.toml。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`# OSKernel Makefile # # 用法:`
  - `kernel/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "kernel" version = "0.1.0"`
  - `kernel/build.rs:L1-L3`：构建入口
    代码片段：`//! 构建脚本 //! //! 负责设置链接脚本路径`
  - `kernel/src/arch/riscv64/boot/linker.ld:L1-L3`：构建入口
    代码片段：`/* * RISC-V 64 链接脚本 *`
  - `user/hello/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "hello" version = "0.1.0"`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct FdCallbacks, fn register_fd_callbacks, fn fd_close_resource, fn fd_clone_resource, fn current_fd_kind。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/sched/fd.rs` | L9-L13 | 关键词命中 |
| `kernel/src/sched/mod.rs` | L4-L8 | 关键词命中 |
| `kernel/src/sched/exec.rs` | L36-L40 | 关键词命中 |
| `kernel/src/sched/fd.rs` | L18-L22 | struct FdCallbacks |
| `kernel/src/sched/fd.rs` | L28-L32 | fn register_fd_callbacks |
| `kernel/src/sched/fd.rs` | L33-L37 | fn fd_close_resource |

### 关键代码片段

  - `kernel/src/sched/fd.rs:L9-L13`：关键词命中
    代码片段：`use crate::hal::irq; use crate::sched::task::{FdKind, MAX_FDS}; // ─── FdCallbacks: fd 资源管理回调 ────────────────────────`
  - `kernel/src/sched/mod.rs:L4-L8`：关键词命中
    代码片段：`/// 子模块职责: /// - pid.rs     : PID 位图分配器 + PidHandle RAII 自动回收 /// - task.rs    : 进程控制块 (TCB) 定义 + 内核栈分配/释放 /// - manager.rs : 全局任务管理器 + 时间片轮转调度器 /// - exec.rs    : exec 路径 (ELF...`
  - `kernel/src/sched/exec.rs:L36-L40`：关键词命中
    代码片段：`assert!( super::manager::MANAGER.initialized, "task manager not initialized" ); let cur = super::manager::MANAGER.current;`
  - `kernel/src/sched/fd.rs:L18-L22`：struct FdCallbacks
    代码片段：`/// 避免 sched 模块直接依赖 ipc::pipe 和 fs 模块。 /// 由 main.rs 在初始化时注册。 pub struct FdCallbacks { /// 关闭一个 fd 资源 (递减引用计数, 必要时释放) pub close: fn(FdKind),`

### 相关符号

`struct FdCallbacks` at `kernel/src/sched/fd.rs:L18`、`fn register_fd_callbacks` at `kernel/src/sched/fd.rs:L28`、`fn fd_close_resource` at `kernel/src/sched/fd.rs:L33`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：enum VmAreaType, struct MmapFileInfo, fn mmap_file_alloc, fn mmap_file_free, fn mmap_file_retain。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/mm/fault.rs` | L23-L27 | 关键词命中 |
| `kernel/src/mm/frame.rs` | L247-L251 | 关键词命中 |
| `kernel/src/mm/address.rs` | L10-L14 | 关键词命中 |
| `kernel/src/mm/area.rs` | L20-L24 | enum VmAreaType |
| `kernel/src/mm/area.rs` | L37-L41 | struct MmapFileInfo |
| `kernel/src/mm/area.rs` | L59-L63 | fn mmap_file_alloc |

### 关键代码片段

  - `kernel/src/mm/fault.rs:L23-L27`：关键词命中
    代码片段：`let vpn = VirtPageNum::from(fault_addr / PAGE_SIZE); // 如果已经有映射, 可能是 COW 写保护, 否则属于权限异常。 // COW 页即使经过 mprotect 调整，也必须先私有化，不能让共享帧 // 重新以可写方式暴露给父子进程。`
  - `kernel/src/mm/frame.rs:L247-L251`：关键词命中
    代码片段：`} /// 查询某物理帧的引用计数（用于 mprotect COW 隔离） pub fn frame_ref_count(ppn: PhysPageNum) -> u16 { let (_, idx) = ppn_to_index(ppn);`
  - `kernel/src/mm/address.rs:L10-L14`：关键词命中
    代码片段：`pub const PAGE_SIZE_BITS: usize = 12; // 页面大小的位数（2^12 = 4096） // 页表最大级数（RISC-V Sv39 支持 3 级页表） pub const MAX_PAGE_LEVELS: usize = 5; // 目前没有超过该级数的页表设计`
  - `kernel/src/mm/area.rs:L20-L24`：enum VmAreaType
    代码片段：`#[derive(Copy, Clone, Debug, PartialEq)] #[repr(u8)] pub enum VmAreaType { /// 匿名映射 (mmap MAP_ANONYMOUS) Anonymous = 0,`

### 相关符号

`enum VmAreaType` at `kernel/src/mm/area.rs:L20`、`struct MmapFileInfo` at `kernel/src/mm/area.rs:L37`、`fn mmap_file_alloc` at `kernel/src/mm/area.rs:L59`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：struct UserTrapFrame, enum TrapCause, fn as_arch, fn as_arch_mut, fn syscall_args。（置信度：medium）
  - 静态识别到 97 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/syscall/io.rs` | L1-L4 | 关键词命中 |
| `kernel/src/syscall/mod.rs` | L1-L3 | 关键词命中 |
| `kernel/src/syscall/sig.rs` | L1-L3 | 关键词命中 |
| `kernel/src/hal/trap.rs` | L5-L9 | struct UserTrapFrame |
| `kernel/src/hal/trap.rs` | L9-L13 | enum TrapCause |
| `kernel/src/hal/trap.rs` | L34-L38 | fn as_arch |

### 关键代码片段

  - `kernel/src/syscall/io.rs:L1-L4`：关键词命中
    代码片段：`use super::check::{validate_user_buffer, validate_user_buffer_write}; /// syscall/io.rs — I/O 类系统调用 /// /// 所有文件I/O通过VFS层分发:`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`/// syscall/ — 系统调用框架 /// /// 微内核用户态 ↔ 内核的唯一合法通道。`
  - `kernel/src/syscall/sig.rs:L1-L3`：关键词命中
    代码片段：`/// syscall/sig.rs — 信号相关系统调用 /// /// 从 process.rs 拆分而来，职责:`
  - `kernel/src/hal/trap.rs:L5-L9`：struct UserTrapFrame
    代码片段：`/// 该类型与 'crate::arch::riscv64::trap::Context' 保持完全一致的内存布局。 #[repr(transparent)] pub struct UserTrapFrame(crate::arch::riscv64::trap::Context); /// 架构无关的 trap 原因枚举。`

### 相关符号

`struct UserTrapFrame` at `kernel/src/hal/trap.rs:L5`、`enum TrapCause` at `kernel/src/hal/trap.rs:L9`、`fn as_arch` at `kernel/src/hal/trap.rs:L34`、`fn syscall_args` at `kernel/src/hal/trap.rs:L46`、`fn sys_write` at `kernel/src/syscall/io.rs:L17`、`fn sys_read` at `kernel/src/syscall/io.rs:L59`、`fn sys_close` at `kernel/src/syscall/io.rs:L101`、`fn sys_pipe2` at `kernel/src/syscall/io.rs:L112`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：enum InodeType, struct StatFs, struct Stat, struct LinuxDirent64, struct GlobalFile。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/fs/mod.rs` | L6-L10 | 关键词命中 |
| `kernel/src/fs/vfs.rs` | L1-L4 | 关键词命中 |
| `kernel/src/fs/path.rs` | L1-L3 | 关键词命中 |
| `kernel/src/fs/mod.rs` | L74-L78 | enum InodeType |
| `kernel/src/fs/mod.rs` | L85-L89 | struct StatFs |
| `kernel/src/fs/mod.rs` | L124-L128 | struct Stat |

### 关键代码片段

  - `kernel/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod procfs; pub mod ramfs; mod vfs; // ─── 常量 ─────────────────────────────────────────────────`
  - `kernel/src/fs/vfs.rs:L1-L4`：关键词命中
    代码片段：`use super::{ devfs, ext4, procfs, ramfs, FsOps, FsType, GlobalFile, Inode, InodeType, MountEntry, MAX_FILES, MAX_INODES, MAX_MOUNTS, MOUNT_DEVFS, MOUNT_EXT4, MOUNT_PROCFS, MOUNT...`
  - `kernel/src/fs/path.rs:L1-L3`：关键词命中
    代码片段：`use super::vfs::{get_ops, get_or_alloc_inode_locked, INODE_TABLE, MOUNT_TABLE, ROOT_INODE_IDX}; use super::{FsType, InodeType, MAX_MOUNTS, NAME_BUF_LEN, S_IFCHR, S_IFDIR, S_IFMT...`
  - `kernel/src/fs/mod.rs:L74-L78`：enum InodeType
    代码片段：`#[derive(Copy, Clone, Eq, PartialEq)] pub enum InodeType { Free, File,`

### 相关符号

`enum InodeType` at `kernel/src/fs/mod.rs:L74`、`struct StatFs` at `kernel/src/fs/mod.rs:L85`、`struct Stat` at `kernel/src/fs/mod.rs:L124`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/klog.rs` | L10-L14 | 关键词命中 |
| `kernel/src/arch/riscv64/uart.rs` | L10-L14 | 关键词命中 |
| `kernel/src/arch/riscv64/trap/timer.rs` | L25-L29 | 关键词命中 |

### 关键代码片段

  - `kernel/src/klog.rs:L10-L14`：关键词命中
    代码片段：`///   3 = Debug  — 开发调试 ///   4 = Trace  — 热路径追踪 use core::sync::atomic::{AtomicU8, Ordering}; // ─── 日志级别常量 ────────────────────────────────────────`
  - `kernel/src/arch/riscv64/uart.rs:L10-L14`：关键词命中
    代码片段：`//! 注意: 使用了硬编码 QEMU UART 基址址, 本应由 DTB/SBI 提供 use core::sync::atomic::{AtomicUsize, Ordering}; /// UART 基地址 (QEMU virt 平台)`
  - `kernel/src/arch/riscv64/trap/timer.rs:L25-L29`：关键词命中
    代码片段：`///   回调由 main.rs 在初始化阶段通过 register_tick_handler() 注册 ///   arch 模块完全不知道 sched 模块的存在 use core::sync::atomic::{AtomicBool, AtomicU64, AtomicUsize, Ordering}; const SBI_EXT_BASE:...`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn disable, fn restore, fn is_disabled, fn with_disabled, fn with_interrupts_disabled。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/hal/irq.rs` | L4-L8 | 关键词命中 |
| `kernel/src/hal/trap.rs` | L1-L3 | 关键词命中 |
| `kernel/src/arch/riscv64/plic.rs` | L1-L3 | 关键词命中 |
| `kernel/src/hal/irq.rs` | L3-L7 | fn disable |
| `kernel/src/hal/irq.rs` | L9-L13 | fn restore |
| `kernel/src/hal/irq.rs` | L15-L19 | fn is_disabled |

### 关键代码片段

  - `kernel/src/hal/irq.rs:L4-L8`：关键词命中
    代码片段：`#[inline] pub fn disable() -> bool { crate::arch::riscv64::trap::interrupt::disable() }`
  - `kernel/src/hal/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 上下文抽象。 /// 用户态 trap 帧。`
  - `kernel/src/arch/riscv64/plic.rs:L1-L3`：关键词命中
    代码片段：`/// arch/riscv64/plic.rs — PLIC (Platform-Level Interrupt Controller) 驱动 /// PLIC 是 RISC-V 平台的标准中断控制器, 负责管理外部设备中断 (如 UART)。 ///`
  - `kernel/src/hal/irq.rs:L3-L7`：fn disable
    代码片段：`/// 关闭 S-mode 中断，返回调用前是否开启。 #[inline] pub fn disable() -> bool { crate::arch::riscv64::trap::interrupt::disable() }`

### 相关符号

`fn disable` at `kernel/src/hal/irq.rs:L3`、`fn restore` at `kernel/src/hal/irq.rs:L9`、`fn is_disabled` at `kernel/src/hal/irq.rs:L15`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct TtyBackend, fn register_backend, fn is_backend_registered, fn raw_putc, fn raw_getc。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/tty/driver.rs` | L1-L3 | 关键词命中 |
| `kernel/src/drivers/mod.rs` | L2-L6 | 关键词命中 |
| `kernel/src/hal/console.rs` | L4-L8 | 关键词命中 |
| `kernel/src/tty/driver.rs` | L9-L13 | struct TtyBackend |
| `kernel/src/tty/driver.rs` | L20-L24 | fn register_backend |
| `kernel/src/tty/driver.rs` | L27-L31 | fn is_backend_registered |

### 关键代码片段

  - `kernel/src/tty/driver.rs:L1-L3`：关键词命中
    代码片段：`/// tty/driver.rs — TTY 硬件后端管理 /// /// 管理 TTY 后端 (UART 等) 的注册和底层 I/O。`
  - `kernel/src/drivers/mod.rs:L2-L6`：关键词命中
    代码片段：`/// /// 子模块: ///   virtio_blk.rs — Virtio MMIO 块设备驱动 (QEMU virt 平台) pub mod virtio_blk;`
  - `kernel/src/hal/console.rs:L4-L8`：关键词命中
    代码片段：`#[inline] pub fn putc(c: u8) { crate::arch::riscv64::uart::uart_putc(c); }`
  - `kernel/src/tty/driver.rs:L9-L13`：struct TtyBackend
    代码片段：`/// TTY 硬件后端回调 pub struct TtyBackend { /// 发送一个字节 (后端负责 \n→\r\n 等 output processing) pub putc: fn(u8),`

### 相关符号

`struct TtyBackend` at `kernel/src/tty/driver.rs:L9`、`fn register_backend` at `kernel/src/tty/driver.rs:L20`、`fn is_backend_registered` at `kernel/src/tty/driver.rs:L27`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：15
- 含证据关键结论数：15（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
