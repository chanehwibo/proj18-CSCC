# 2026 T2026103369910012 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026103369910012-831`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：172
- 代码/文本行数：53045
- 主要语言：text 25110 LOC, rust 23154 LOC, markdown 3582 LOC, build 697 LOC, make 467 LOC

## 总览

2026 T2026103369910012 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 text: 25110 LOC, rust: 23154 LOC, markdown: 3582 LOC, build: 697 LOC。仓库包含 172 个已扫描文件、约 53045 行可分析文本，当前抽取到 1800 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（98/100）
- 已确认 OS 维度：7/7；高置信维度：6/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 78/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | medium | 3 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, Makefile, axfs-ng-local/Cargo.toml, kernel/Cargo.toml, make/Makefile。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] resolver = "2" members = ["kernel"]`
  - `Makefile:L1-L3`：构建入口
    代码片段：`# !!!!!绝对禁止使用cargo clean！！！！！ # Build Options export ARCH := riscv64`
  - `axfs-ng-local/Cargo.toml:L1-L3`：构建入口
    代码片段：`# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO # # When uploading crates to the registry Cargo will automatically`
  - `kernel/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "starry-kernel" version.workspace = true`
  - `make/Makefile:L1-L3`：构建入口
    代码片段：`# Available arguments: # * General options: #     - 'ARCH': Target architecture: x86_64, riscv64, aarch64, loongarch64`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct AssumeSync, fn deref, struct Thread, impl Thread, fn new。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/task/mod.rs` | L1-L3 | 关键词命中 |
| `kernel/src/task/ops.rs` | L28-L32 | 关键词命中 |
| `kernel/src/task/stat.rs` | L5-L9 | 关键词命中 |
| `kernel/src/task/mod.rs` | L33-L37 | struct AssumeSync |
| `kernel/src/task/mod.rs` | L40-L44 | fn deref |
| `kernel/src/task/mod.rs` | L46-L50 | struct Thread |

### 关键代码片段

  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. mod futex;`
  - `kernel/src/task/ops.rs:L28-L32`：关键词命中
    代码片段：`static SESSION_TABLE: RwLock<WeakMap<Pid, Weak<Session>>> = RwLock::new(WeakMap::new()); /// Cleanup expired entries in the task tables. /// /// This function is intended to be...`
  - `kernel/src/task/stat.rs:L5-L9`：关键词命中
    代码片段：`use starry_signal::Signo; use crate::task::AsThread; /// Represents the '/proc/[pid]/stat' file.`
  - `kernel/src/task/mod.rs:L33-L37`：struct AssumeSync
    代码片段：`///  A wrapper type that assumes the inner type is 'Sync'. #[repr(transparent)] pub struct AssumeSync<T>(pub T); unsafe impl<T> Sync for AssumeSync<T> {}`

### 相关符号

`struct AssumeSync` at `kernel/src/task/mod.rs:L33`、`fn deref` at `kernel/src/task/mod.rs:L40`、`struct Thread` at `kernel/src/task/mod.rs:L46`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：struct IoVec, struct IoVectorBuf, fn new, fn read_with, fn fill_with。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/mm/access.rs` | L87-L91 | 关键词命中 |
| `kernel/src/mm/loader.rs` | L40-L44 | 关键词命中 |
| `kernel/src/mm/aspace/mod.rs` | L5-L9 | 关键词命中 |
| `kernel/src/mm/io.rs` | L8-L12 | struct IoVec |
| `kernel/src/mm/io.rs` | L14-L18 | struct IoVectorBuf |
| `kernel/src/mm/io.rs` | L21-L25 | fn new |

### 关键代码片段

  - `kernel/src/mm/access.rs:L87-L91`：关键词命中
    代码片段：`// TODO: this is inefficient, but we have to do this instead of // querying the page table since the page might has not been // allocated yet. let curr = current();`
  - `kernel/src/mm/loader.rs:L40-L44`：关键词命中
    代码片段：`#[cfg(not(any(target_arch = "aarch64", target_arch = "loongarch64")))] { // ARMv8 (aarch64) and LoongArch64 use separate page tables for user space // (aarch64: TTBR0_EL1, Loong...`
  - `kernel/src/mm/aspace/mod.rs:L5-L9`：关键词命中
    代码片段：`use axhal::{ mem::phys_to_virt, paging::{MappingFlags, PageTable}, trap::PageFaultFlags, };`
  - `kernel/src/mm/io.rs:L8-L12`：struct IoVec
    代码片段：`#[repr(C)] #[derive(Debug, Copy, Clone, AnyBitPattern)] pub struct IoVec { pub iov_base: *mut u8, pub iov_len: isize,`

### 相关符号

`struct IoVec` at `kernel/src/mm/io.rs:L8`、`struct IoVectorBuf` at `kernel/src/mm/io.rs:L14`、`fn new` at `kernel/src/mm/io.rs:L21`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn handle_syscall, fn valid_uid_or_einval, fn can_set_uid, fn can_set_gid, fn sys_getuid。（置信度：medium）
  - 静态识别到 233 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/syscall/mod.rs` | L20-L24 | 关键词命中 |
| `kernel/src/syscall/fs/ctl.rs` | L133-L137 | 关键词命中 |
| `kernel/src/syscall/net/io.rs` | L13-L17 | 关键词命中 |
| `kernel/src/syscall/mod.rs` | L20-L24 | fn handle_syscall |
| `kernel/src/syscall/sys.rs` | L22-L26 | fn valid_uid_or_einval |
| `kernel/src/syscall/sys.rs` | L34-L38 | fn can_set_uid |

### 关键代码片段

  - `kernel/src/syscall/mod.rs:L20-L24`：关键词命中
    代码片段：`}; pub fn handle_syscall(uctx: &mut UserContext) { let Some(sysno) = Sysno::new(uctx.sysno()) else { warn!("Invalid syscall number: {}", uctx.sysno());`
  - `kernel/src/syscall/fs/ctl.rs:L133-L137`：关键词命中
    代码片段：`let mode = mode & !current().as_thread().proc_data.umask(); let mode = NodePermission::from_bits_truncate(mode as u16); let uid = crate::syscall::sys::sys_geteuid()? as u32; let...`
  - `kernel/src/syscall/net/io.rs:L13-L17`：关键词命中
    代码片段：`file::{FileLike, Socket, add_file_like}, mm::{IoVec, IoVectorBuf, UserConstPtr, UserPtr, VmBytes, VmBytesMut}, syscall::net::{CMsg, CMsgBuilder}, };`
  - `kernel/src/syscall/mod.rs:L20-L24`：fn handle_syscall
    代码片段：`}; pub fn handle_syscall(uctx: &mut UserContext) { let Some(sysno) = Sysno::new(uctx.sysno()) else { warn!("Invalid syscall number: {}", uctx.sysno());`

### 相关符号

`fn handle_syscall` at `kernel/src/syscall/mod.rs:L20`、`fn valid_uid_or_einval` at `kernel/src/syscall/sys.rs:L22`、`fn can_set_uid` at `kernel/src/syscall/sys.rs:L34`、`fn handle_syscall` at `kernel/src/syscall/mod.rs:L20`、`fn sys_getuid` at `kernel/src/syscall/sys.rs:L48`、`fn sys_geteuid` at `kernel/src/syscall/sys.rs:L53`、`fn sys_getgid` at `kernel/src/syscall/sys.rs:L58`、`fn sys_getegid` at `kernel/src/syscall/sys.rs:L63`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：fn with_fs, enum ResolveAtResult, fn into_file, fn stat, fn resolve_at。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/file/fs.rs` | L101-L105 | 关键词命中 |
| `kernel/src/pseudofs/fs.rs` | L98-L102 | 关键词命中 |
| `axfs-ng-local/src/fs/mod.rs` | L6-L10 | 关键词命中 |
| `kernel/src/file/fs.rs` | L18-L22 | fn with_fs |
| `kernel/src/file/fs.rs` | L28-L32 | enum ResolveAtResult |
| `kernel/src/file/fs.rs` | L34-L38 | fn into_file |

### 关键代码片段

  - `kernel/src/file/fs.rs:L101-L105`：关键词命中
    代码片段：`Kstat { dev: metadata.device, ino: metadata.inode, mode, nlink: nlink as _,`
  - `kernel/src/pseudofs/fs.rs:L98-L102`：关键词命中
    代码片段：`let metadata = Metadata { device: 0, inode: ino, nlink: 1, mode,`
  - `axfs-ng-local/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`mod ext4; pub(crate) type DefaultFilesystem = ext4::Ext4Filesystem; } else if #[cfg(feature = "fat")] { mod fat; pub(crate) type DefaultFilesystem = fat::FatFilesystem;`
  - `kernel/src/file/fs.rs:L18-L22`：fn with_fs
    代码片段：`use crate::file::{IoDst, IoSrc}; pub fn with_fs<R>(dirfd: c_int, f: impl FnOnce(&mut FsContext) -> AxResult<R>) -> AxResult<R> { let mut fs = FS_CONTEXT.lock(); if dirfd == AT_F...`

### 相关符号

`fn with_fs` at `kernel/src/file/fs.rs:L18`、`enum ResolveAtResult` at `kernel/src/file/fs.rs:L28`、`fn into_file` at `kernel/src/file/fs.rs:L34`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn assert_unsigned, fn sys_futex, fn sys_get_robust_list, fn sys_set_robust_list, fn sys_membarrier。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/syscall/sync/membarrier.rs` | L1-L3 | 关键词命中 |
| `kernel/src/entry.rs` | L6-L10 | 关键词命中 |
| `kernel/src/file/fs.rs` | L3-L7 | 关键词命中 |
| `kernel/src/syscall/sync/futex.rs` | L12-L16 | fn assert_unsigned |
| `kernel/src/syscall/sync/futex.rs` | L20-L24 | fn sys_futex |
| `kernel/src/syscall/sync/futex.rs` | L116-L120 | fn sys_get_robust_list |

### 关键代码片段

  - `kernel/src/syscall/sync/membarrier.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{Ordering, compiler_fence}; use axerrno::{AxError, AxResult};`
  - `kernel/src/entry.rs:L6-L10`：关键词命中
    代码片段：`use axfs::FS_CONTEXT; use axhal::uspace::UserContext; use axsync::Mutex; use axtask::{AxTaskExt, spawn_task}; use starry_process::{Pid, Process};`
  - `kernel/src/file/fs.rs:L3-L7`：关键词命中
    代码片段：`ffi::c_int, hint::likely, sync::atomic::{AtomicBool, Ordering}, task::Context, };`
  - `kernel/src/syscall/sync/futex.rs:L12-L16`：fn assert_unsigned
    代码片段：`}; fn assert_unsigned(value: u32) -> AxResult<u32> { if (value as i32) < 0 { Err(AxError::InvalidInput)`

### 相关符号

`fn assert_unsigned` at `kernel/src/syscall/sync/futex.rs:L12`、`fn sys_futex` at `kernel/src/syscall/sync/futex.rs:L20`、`fn sys_get_robust_list` at `kernel/src/syscall/sync/futex.rs:L116`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn time_value_from_nanos, struct Entry, fn eq, fn partial_cmp, fn cmp。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/task/timer.rs` | L49-L53 | 关键词命中 |
| `kernel/src/task/mod.rs` | L6-L10 | 关键词命中 |
| `kernel/src/task/ops.rs` | L110-L114 | 关键词命中 |
| `kernel/src/task/timer.rs` | L17-L21 | fn time_value_from_nanos |
| `kernel/src/task/timer.rs` | L23-L27 | struct Entry |
| `kernel/src/task/timer.rs` | L28-L32 | fn eq |

### 关键代码片段

  - `kernel/src/task/timer.rs:L49-L53`：关键词命中
    代码片段：`} /// The type of interval timer. #[repr(i32)] #[allow(non_camel_case_types)]`
  - `kernel/src/task/mod.rs:L6-L10`：关键词命中
    代码片段：`mod signal; mod stat; mod timer; mod user;`
  - `kernel/src/task/ops.rs:L110-L114`：关键词命中
    代码片段：`} /// Poll the timer pub fn poll_timer(task: &TaskInner) { let Some(thr) = task.try_as_thread() else {`
  - `kernel/src/task/timer.rs:L17-L21`：fn time_value_from_nanos
    代码片段：`use crate::task::poll_timer; fn time_value_from_nanos(nanos: usize) -> TimeValue { let secs = nanos as u64 / NANOS_PER_SEC; let nsecs = nanos as u64 - secs * NANOS_PER_SEC;`

### 相关符号

`fn time_value_from_nanos` at `kernel/src/task/timer.rs:L17`、`struct Entry` at `kernel/src/task/timer.rs:L23`、`fn eq` at `kernel/src/task/timer.rs:L28`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：medium）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 相关符号包括：enum DeviceMmap, trait DeviceOps, fn read_at, fn write_at, fn ioctl。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/pseudofs/device.rs` | L14-L18 | enum DeviceMmap |
| `kernel/src/pseudofs/device.rs` | L26-L30 | trait DeviceOps |
| `kernel/src/pseudofs/device.rs` | L28-L32 | fn read_at |

### 关键代码片段

  - `kernel/src/pseudofs/device.rs:L14-L18`：enum DeviceMmap
    代码片段：`/// Mmap behavior for devices. pub enum DeviceMmap { /// The device is not mappable. None,`
  - `kernel/src/pseudofs/device.rs:L26-L30`：trait DeviceOps
    代码片段：`/// Trait for device operations. pub trait DeviceOps: Send + Sync { /// Reads data from the device at the specified offset. fn read_at(&self, buf: &mut [u8], offset: u64) -> Vfs...`
  - `kernel/src/pseudofs/device.rs:L28-L32`：fn read_at
    代码片段：`pub trait DeviceOps: Send + Sync { /// Reads data from the device at the specified offset. fn read_at(&self, buf: &mut [u8], offset: u64) -> VfsResult<usize>; /// Writes data to...`

### 相关符号

`enum DeviceMmap` at `kernel/src/pseudofs/device.rs:L14`、`trait DeviceOps` at `kernel/src/pseudofs/device.rs:L26`、`fn read_at` at `kernel/src/pseudofs/device.rs:L28`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：15
- 含证据关键结论数：15（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：1
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
