# 2026 T2026105749910760 比较报告

- 对比历史仓库：2025 reXvapor
- 生成时间：2026-07-02T05:02:47.928664+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 reXvapor（来源：赛事历史作品）：画像相似度 score=9.92；同属 ucore-variant 风格; 语言构成相似度 0.99; OS 维度重合度 1.00; 代码规模接近度 0.94

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 reXvapor 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/arch/qemu/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/fs/virtio_disk.c:L1-L4`：关键词命中
    代码片段：`// // driver for qemu's virtio disk device. // uses qemu's mmio interface to virtio. //`
  - `kernel/arch/qemu/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/fs/virtio_disk.c:L1-L4`：关键词命中
    代码片段：`// // driver for qemu's virtio disk device. // uses qemu's mmio interface to virtio. //`
- 与 2025 reXvapor 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `kernel/fs/file.c:L154-L158`：关键词命中
    代码片段：`size_t fileread(struct file *f, int user_dst, uint64 addr, size_t n, int64_t off) { size_t r = 0;`
  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `kernel/fs/file.c:L154-L158`：关键词命中
    代码片段：`size_t fileread(struct file *f, int user_dst, uint64 addr, size_t n, int64_t off) { size_t r = 0;`
- 与 2025 reXvapor 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sched/trap.c:L115-L119`：关键词命中
    代码片段：`// trampoline已经换栈和页表 // // handle an interrupt, exception, or system call from user space. // called from trampoline.S //`
  - `kernel/arch/qemu/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/sched/trap.c:L115-L119`：关键词命中
    代码片段：`// trampoline已经换栈和页表 // // handle an interrupt, exception, or system call from user space. // called from trampoline.S //`
  - `kernel/arch/qemu/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
- 与 2025 reXvapor 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mm/vm.c:L8-L12`：关键词命中
    代码片段：`#include "xv6fs.h" #include "proc.h" #include "kalloc.h" /*`
  - `kernel/mm/mmap.c:L7-L11`：关键词命中
    代码片段：`static uint64 allocvma() { struct vma_struct *vma = (struct vma_struct *)kalloc(); if (vma == 0) { return 0;`
  - `kernel/mm/vm.c:L8-L12`：关键词命中
    代码片段：`#include "xv6fs.h" #include "proc.h" #include "kalloc.h" /*`
  - `kernel/mm/mmap.c:L7-L11`：关键词命中
    代码片段：`static uint64 allocvma() { struct vma_struct *vma = (struct vma_struct *)kalloc(); if (vma == 0) { return 0;`
- 与 2025 reXvapor 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sched/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "sched.h"`
  - `kernel/sched/trap.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "thread.h"`
  - `kernel/sched/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "sched.h"`
  - `kernel/sched/trap.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "thread.h"`
- 与 2025 reXvapor 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/atomic/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "cond.h" #include "semaphore.h" /// @brief init a semaphore`
  - `kernel/include/semaphore.h:L3-L7`：关键词命中
    代码片段：`#include "types.h" #include "spinlock.h" #include "cond.h"`
  - `kernel/atomic/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "cond.h" #include "semaphore.h" /// @brief init a semaphore`
  - `kernel/include/semaphore.h:L3-L7`：关键词命中
    代码片段：`#include "types.h" #include "spinlock.h" #include "cond.h"`
- 与 2025 reXvapor 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "thread.h"`
  - `kernel/sched/trap.c:L155-L159`：关键词命中
    代码片段：`thread_exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. // p->trapframe->epc += 4;`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "thread.h"`
  - `kernel/sched/trap.c:L155-L159`：关键词命中
    代码片段：`thread_exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. // p->trapframe->epc += 4;`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 reXvapor 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：bdev_ops, close, int, minor, open, struct。（置信度：high）
  证据：
  - `kernel/include/device.h:L4-L8`：struct bdev_ops
    代码片段：`// Block device switch table entry. struct bdev_ops { int (*open)(int minor); int (*close)(int minor);`
  - `kernel/include/device.h:L4-L8`：struct bdev_ops
    代码片段：`// Block device switch table entry. struct bdev_ops { int (*open)(int minor); int (*close)(int minor);`
- 与 2025 reXvapor 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：bdev, bdev_ops, int, major, ops, struct。（置信度：high）
  证据：
  - `kernel/include/device.h:L9-L13`：struct bdev
    代码片段：`}; struct bdev { int major; struct bdev_ops *ops;`
  - `kernel/include/device.h:L9-L13`：struct bdev
    代码片段：`}; struct bdev { int major; struct bdev_ops *ops;`
- 与 2025 reXvapor 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：enode, eok, ext4_inode_is_type, ext4_inode_mode_directory, false, has_children, if, inode。（置信度：high）
  证据：
  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
- 与 2025 reXvapor 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：addr, f, file, fileread, int, int64_t, n, off。（置信度：high）
  证据：
  - `kernel/fs/file.c:L154-L158`：关键词命中
    代码片段：`size_t fileread(struct file *f, int user_dst, uint64 addr, size_t n, int64_t off) { size_t r = 0;`
  - `kernel/fs/file.c:L154-L158`：关键词命中
    代码片段：`size_t fileread(struct file *f, int user_dst, uint64 addr, size_t n, int64_t off) { size_t r = 0;`
- 与 2025 reXvapor 在“中断与异常”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：acquire, boot_hart, extern, get_ticksnow, int, t, ticks, tickslock。（置信度：high）
  证据：
  - `kernel/sched/trap.c:L18-L22`：fn get_ticksnow
    代码片段：`uint ticks = 0; extern volatile int boot_hart; uint get_ticksnow(void) { uint t; acquire(&tickslock);`
  - `kernel/sched/trap.c:L18-L22`：fn get_ticksnow
    代码片段：`uint ticks = 0; extern volatile int boot_hart; uint get_ticksnow(void) { uint t; acquire(&tickslock);`
- 与 2025 reXvapor 在“中断与异常”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：devintr, extern, int, pgfault_handler, static, void。（置信度：high）
  证据：
  - `kernel/sched/trap.c:L33-L37`：fn pgfault_handler
    代码片段：`extern int devintr(); static void pgfault_handler() { #ifdef __ARCH_RISCV`
  - `kernel/sched/trap.c:L33-L37`：fn pgfault_handler
    代码片段：`extern int devintr(); static void pgfault_handler() { #ifdef __ARCH_RISCV`
- 函数/符号名重合：与 2025 reXvapor 在“文件系统”维度发现 4 个同名定义：flags2perm, floadseg, init_ext4_fpool, init_ext4_ipool。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/fs/exec.c:L34-L34`：fn flags2perm
    代码片段：`unsigned long int flags2perm(int flags) {`
  - `kernel/fs/exec.c:L34-L34`：fn flags2perm
    代码片段：`unsigned long int flags2perm(int flags) {`
  - `kernel/fs/exec.c:L375-L375`：fn floadseg
    代码片段：`static int floadseg(pagetable_t pagetable, struct file *f, uint64 va, uint offset, uint sz){`
  - `kernel/fs/exec.c:L375-L375`：fn floadseg
    代码片段：`static int floadseg(pagetable_t pagetable, struct file *f, uint64 va, uint offset, uint sz){`
- 函数/符号名重合：与 2025 reXvapor 在“内存管理”维度发现 4 个同名定义：floadseg, ext4_vmknod, ext4_vmkdir, fdalloc_lteq。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/fs/exec.c:L375-L375`：fn floadseg
    代码片段：`static int floadseg(pagetable_t pagetable, struct file *f, uint64 va, uint offset, uint sz){`
  - `kernel/fs/exec.c:L375-L375`：fn floadseg
    代码片段：`static int floadseg(pagetable_t pagetable, struct file *f, uint64 va, uint offset, uint sz){`
  - `kernel/fs/ext4fs.c:L759-L759`：fn ext4_vmknod
    代码片段：`int ext4_vmknod(const char *pathname, mode_t mode, dev_t dev) {`
  - `kernel/fs/ext4fs.c:L759-L759`：fn ext4_vmknod
    代码片段：`int ext4_vmknod(const char *pathname, mode_t mode, dev_t dev) {`
- 函数/符号名重合：与 2025 reXvapor 在“调度与任务管理”维度发现 4 个同名定义：is_exc_rcfile, init_procfs, procfs_unlink, procfs_rename。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/fs/file.c:L221-L221`：fn is_exc_rcfile
    代码片段：`int is_exc_rcfile(struct proc *p) {`
  - `kernel/fs/file.c:L221-L221`：fn is_exc_rcfile
    代码片段：`int is_exc_rcfile(struct proc *p) {`
  - `kernel/fs/procfs.c:L84-L84`：fn init_procfs
    代码片段：`int init_procfs() {`
  - `kernel/fs/procfs.c:L46-L46`：fn init_procfs
    代码片段：`int init_procfs() {`
- 函数/符号名重合：与 2025 reXvapor 在“同步机制”维度发现 4 个同名定义：cond_init, cond_wait, cond_signal, cond_broadcast。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/atomic/cond.c:L14-L14`：fn cond_init
    代码片段：`void cond_init(struct cond *cond, char *name) {`
  - `kernel/atomic/cond.c:L14-L14`：fn cond_init
    代码片段：`void cond_init(struct cond *cond, char *name) {`
  - `kernel/atomic/cond.c:L19-L19`：fn cond_wait
    代码片段：`int cond_wait(struct cond *cond, struct spinlock *mutex) {`
  - `kernel/atomic/cond.c:L19-L19`：fn cond_wait
    代码片段：`int cond_wait(struct cond *cond, struct spinlock *mutex) {`
- 函数/符号名重合：与 2025 reXvapor 在“系统调用”维度发现 4 个同名定义：ext4_vstatfs, fdalloc_spec, get_abpath_from_dirfd, sys_dup3。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/fs/ext4fs.c:L1206-L1206`：fn ext4_vstatfs
    代码片段：`int ext4_vstatfs(struct vfs_filesystem *fs, struct statfs *buf) {`
  - `kernel/fs/ext4fs.c:L1208-L1208`：fn ext4_vstatfs
    代码片段：`int ext4_vstatfs(struct vfs_filesystem *fs, struct statfs *buf) {`
  - `kernel/fs/sysfile.c:L73-L73`：fn fdalloc_spec
    代码片段：`static int fdalloc_spec(struct file *f, int spec_fd) {`
  - `kernel/fs/sysfile.c:L72-L72`：fn fdalloc_spec
    代码片段：`static int fdalloc_spec(struct file *f, int spec_fd) {`
- 宏名重合：与 2025 reXvapor 在“文件系统”维度发现 4 个同名定义：BSIZE, ADD_AUXV, EXT4_MP_LOCK, EXT4_MP_UNLOCK。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/fs/blockdev.c:L41-L41`：macro BSIZE
    代码片段：`#define BSIZE 512`
  - `kernel/fs/blockdev.c:L41-L41`：macro BSIZE
    代码片段：`#define BSIZE 512`
  - `kernel/fs/exec.c:L17-L17`：macro ADD_AUXV
    代码片段：`#define ADD_AUXV(type, val) \`
  - `kernel/fs/exec.c:L17-L17`：macro ADD_AUXV
    代码片段：`#define ADD_AUXV(type, val) \`
- 宏名重合：与 2025 reXvapor 在“内存管理”维度发现 4 个同名定义：WNOHANG, ext4_balloc_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_ialloc_bitmap_csum。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/wait.h:L3-L3`：macro WNOHANG
    代码片段：`#define WNOHANG		0x00000001 /* return immediately if no child has exited.*/`
  - `include/wait.h:L3-L3`：macro WNOHANG
    代码片段：`#define WNOHANG		0x00000001 /* return immediately if no child has exited.*/`
  - `kernel/fs/ext4_balloc.c:L103-L103`：macro ext4_balloc_bitmap_csum
    代码片段：`#define ext4_balloc_bitmap_csum(...) 0`
  - `kernel/fs/ext4_balloc.c:L103-L103`：macro ext4_balloc_bitmap_csum
    代码片段：`#define ext4_balloc_bitmap_csum(...) 0`
- 宏名重合：与 2025 reXvapor 在“同步机制”维度发现 4 个同名定义：BSIZE, EXT4_MP_LOCK, EXT4_MP_UNLOCK, EXT4_EXT_GET_LEN。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/fs/blockdev.c:L41-L41`：macro BSIZE
    代码片段：`#define BSIZE 512`
  - `kernel/fs/blockdev.c:L41-L41`：macro BSIZE
    代码片段：`#define BSIZE 512`
  - `kernel/fs/ext4.c:L61-L61`：macro EXT4_MP_LOCK
    代码片段：`#define EXT4_MP_LOCK(_m)                                                       \`
  - `kernel/fs/ext4.c:L61-L61`：macro EXT4_MP_LOCK
    代码片段：`#define EXT4_MP_LOCK(_m)                                                       \`
- 结构体/类型重合：与 2025 reXvapor 在“文件系统”维度发现 4 个同名定义：ext4_block_devices, ext4_dx_sort_entry, ext4_extent_path, ext4_extent。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/fs/ext4.c:L76-L76`：struct ext4_block_devices
    代码片段：`struct ext4_block_devices {`
  - `kernel/fs/ext4.c:L76-L76`：struct ext4_block_devices
    代码片段：`struct ext4_block_devices {`
  - `kernel/fs/ext4_dir_idx.c:L197-L197`：struct ext4_dx_sort_entry
    代码片段：`struct ext4_dx_sort_entry {`
  - `kernel/fs/ext4_dir_idx.c:L197-L197`：struct ext4_dx_sort_entry
    代码片段：`struct ext4_dx_sort_entry {`
- 函数/符号名重合：与 2025 reXvapor 在“中断与异常”维度发现 2 个同名定义：do_splice, sys_splice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/fs/sysfile.c:L1965-L1965`：fn do_splice
    代码片段：`size_t do_splice(struct file *in_f, struct file *out_f, off_t *off_addr, size_t len, int inis_pipe) {`
  - `kernel/fs/sysfile.c:L1912-L1912`：fn do_splice
    代码片段：`size_t do_splice(struct file *in_f, struct file *out_f, off_t *off_addr, size_t len, int inis_pipe) {`
  - `kernel/fs/sysfile.c:L2031-L2031`：fn sys_splice
    代码片段：`uint64 sys_splice() {`
  - `kernel/fs/sysfile.c:L1978-L1978`：fn sys_splice
    代码片段：`uint64 sys_splice() {`
- 宏名重合：与 2025 reXvapor 在“调度与任务管理”维度发现 1 个同名定义：__WNOTHREAD。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/wait.h:L10-L10`：macro __WNOTHREAD
    代码片段：`#define __WNOTHREAD	0x20000000	/* Don't wait on children of other threads in this group */`
  - `include/wait.h:L10-L10`：macro __WNOTHREAD
    代码片段：`#define __WNOTHREAD	0x20000000	/* Don't wait on children of other threads in this group */`
- 宏名重合：与 2025 reXvapor 在“系统调用”维度发现 1 个同名定义：EXT4_XATTR_INDEX_SYSTEM。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/fs/ext4_xattr.c:L57-L57`：macro EXT4_XATTR_INDEX_SYSTEM
    代码片段：`#define EXT4_XATTR_INDEX_SYSTEM         7`
  - `kernel/fs/ext4_xattr.c:L57-L57`：macro EXT4_XATTR_INDEX_SYSTEM
    代码片段：`#define EXT4_XATTR_INDEX_SYSTEM         7`
- 文件路径重合：与 2025 reXvapor 在“设备驱动”维度出现同路径源码路径 `kernel/arch/qemu/uart.c` / `kernel/arch/qemu/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/arch/qemu/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/arch/qemu/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
- 文件路径重合：与 2025 reXvapor 在“设备驱动”维度出现同路径源码路径 `kernel/fs/virtio_disk.c` / `kernel/fs/virtio_disk.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/virtio_disk.c:L1-L4`：关键词命中
    代码片段：`// // driver for qemu's virtio disk device. // uses qemu's mmio interface to virtio. //`
  - `kernel/fs/virtio_disk.c:L1-L4`：关键词命中
    代码片段：`// // driver for qemu's virtio disk device. // uses qemu's mmio interface to virtio. //`
- 结构体/类型重合：与 2025 reXvapor 在“设备驱动”维度发现 1 个同名定义：ext4_block_devices。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/fs/ext4.c:L76-L76`：struct ext4_block_devices
    代码片段：`struct ext4_block_devices {`
  - `kernel/fs/ext4.c:L76-L76`：struct ext4_block_devices
    代码片段：`struct ext4_block_devices {`
- 文件路径重合：与 2025 reXvapor 在“文件系统”维度出现同路径源码路径 `kernel/fs/ext4.c` / `kernel/fs/ext4.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
- 文件路径重合：与 2025 reXvapor 在“文件系统”维度出现同路径源码路径 `kernel/fs/file.c` / `kernel/fs/file.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/file.c:L154-L158`：关键词命中
    代码片段：`size_t fileread(struct file *f, int user_dst, uint64 addr, size_t n, int64_t off) { size_t r = 0;`
  - `kernel/fs/file.c:L154-L158`：关键词命中
    代码片段：`size_t fileread(struct file *f, int user_dst, uint64 addr, size_t n, int64_t off) { size_t r = 0;`
- 文件路径重合：与 2025 reXvapor 在“中断与异常”维度出现同路径源码路径 `kernel/sched/trap.c` / `kernel/sched/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/sched/trap.c:L115-L119`：关键词命中
    代码片段：`// trampoline已经换栈和页表 // // handle an interrupt, exception, or system call from user space. // called from trampoline.S //`
  - `kernel/sched/trap.c:L115-L119`：关键词命中
    代码片段：`// trampoline已经换栈和页表 // // handle an interrupt, exception, or system call from user space. // called from trampoline.S //`
- 文件路径重合：与 2025 reXvapor 在“中断与异常”维度出现同路径源码路径 `kernel/arch/qemu/plic.c` / `kernel/arch/qemu/plic.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/arch/qemu/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/arch/qemu/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
- 结构体/类型重合：与 2025 reXvapor 在“同步机制”维度发现 1 个同名定义：ext4_block_devices。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/fs/ext4.c:L76-L76`：struct ext4_block_devices
    代码片段：`struct ext4_block_devices {`
  - `kernel/fs/ext4.c:L76-L76`：struct ext4_block_devices
    代码片段：`struct ext4_block_devices {`

## 相似点

- 与 2025 reXvapor 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 reXvapor 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/arch/qemu/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/fs/virtio_disk.c:L1-L4`：关键词命中
    代码片段：`// // driver for qemu's virtio disk device. // uses qemu's mmio interface to virtio. //`
- 与 2025 reXvapor 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `kernel/fs/file.c:L154-L158`：关键词命中
    代码片段：`size_t fileread(struct file *f, int user_dst, uint64 addr, size_t n, int64_t off) { size_t r = 0;`
- 与 2025 reXvapor 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sched/trap.c:L115-L119`：关键词命中
    代码片段：`// trampoline已经换栈和页表 // // handle an interrupt, exception, or system call from user space. // called from trampoline.S //`
  - `kernel/arch/qemu/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
- 与 2025 reXvapor 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mm/vm.c:L8-L12`：关键词命中
    代码片段：`#include "xv6fs.h" #include "proc.h" #include "kalloc.h" /*`
  - `kernel/mm/mmap.c:L7-L11`：关键词命中
    代码片段：`static uint64 allocvma() { struct vma_struct *vma = (struct vma_struct *)kalloc(); if (vma == 0) { return 0;`
- 与 2025 reXvapor 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sched/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "sched.h"`
  - `kernel/sched/trap.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "thread.h"`
- 与 2025 reXvapor 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/atomic/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "cond.h" #include "semaphore.h" /// @brief init a semaphore`
  - `kernel/include/semaphore.h:L3-L7`：关键词命中
    代码片段：`#include "types.h" #include "spinlock.h" #include "cond.h"`
- 与 2025 reXvapor 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "thread.h"`
  - `kernel/sched/trap.c:L155-L159`：关键词命中
    代码片段：`thread_exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. // p->trapframe->epc += 4;`
- 与 2025 reXvapor 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 reXvapor 的语言构成不同：待测作品为 {'build': 474, 'markdown': 8369, 'make': 1, 'c': 54880, 'asm': 713}，历史样本为 {'json': 20, 'build': 470, 'markdown': 7279, 'make': 1, 'c': 52130, 'asm': 700}。（置信度：medium）

## 可能创新点

- 待测作品包含 51 个历史样本 2025 reXvapor 中未见的独特函数/类型定义（如 `pci_get_device_by_bus`, `procfs_emit_bytes`, `procfs_getdents`, `procfs_render_file`, `vfs_xv6fs_readi`, `get_absolute_path`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/arch/loongarch/pci.c:L373-L373`：fn pci_get_device_by_bus
    代码片段：`pci_device_t* pci_get_device_by_bus(unsigned int bus, unsigned int dev,unsigned int function){`
  - `kernel/fs/procfs.c:L309-L309`：fn procfs_emit_bytes
    代码片段：`static int procfs_emit_bytes(int user_dst, uint64 dst, const char *src, size_t len) {`

## 附录：核验摘要

- 关键结论数：41
- 含证据关键结论数：41（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
