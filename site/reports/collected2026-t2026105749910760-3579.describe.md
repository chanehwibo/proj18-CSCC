# 2026 T2026105749910760 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026105749910760-3579`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：259
- 代码/文本行数：64437
- 主要语言：c 54880 LOC, markdown 8369 LOC, asm 713 LOC, build 474 LOC, make 1 LOC

## 总览

2026 T2026105749910760 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 c: 54880 LOC, markdown: 8369 LOC, asm: 713 LOC, build: 474 LOC。仓库包含 259 个已扫描文件、约 64437 行可分析文本，当前抽取到 2844 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, kernel/Makefile, user/Makefile。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`KERNEL_DIR=kernel USER_DIR := user MKFS_DIR=mkfs`
  - `kernel/Makefile:L1-L3`：构建入口
    代码片段：`BUILD_DIR=../build/kernel INCLUDE_DIR=include USER_DIR=../user`
  - `user/Makefile:L1-L3`：构建入口
    代码片段：`BUILD_DIR=../build/user INITDIR=$(BUILD_DIR)/init INCLUDE_DIR=include`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn allocpid, fn delete_child, fn append_child, fn init_rlimit, fn wait4。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/sched/proc.c` | L4-L8 | 关键词命中 |
| `kernel/sched/trap.c` | L5-L9 | 关键词命中 |
| `kernel/sched/sched.c` | L1-L5 | 关键词命中 |
| `kernel/sched/proc.c` | L25-L29 | fn allocpid |
| `kernel/sched/proc.c` | L93-L97 | fn delete_child |
| `kernel/sched/proc.c` | L109-L113 | fn append_child |

### 关键代码片段

  - `kernel/sched/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "sched.h"`
  - `kernel/sched/trap.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "thread.h"`
  - `kernel/sched/sched.c:L1-L5`：关键词命中
    代码片段：`#include "types.h" #include "queue.h" #include "proc.h" #include "thread.h" #include "sched.h"`
  - `kernel/sched/proc.c:L25-L29`：fn allocpid
    代码片段：`atomic_t nextpid; static int inline allocpid(){ return atomic_inc_return(&nextpid);} // cpu table`

### 相关符号

`fn allocpid` at `kernel/sched/proc.c:L25`、`fn delete_child` at `kernel/sched/proc.c:L93`、`fn append_child` at `kernel/sched/proc.c:L109`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn walk_va, fn uvmadd, fn map_ustack, fn kvmmake, fn kvminithart。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/mm/vm.c` | L8-L12 | 关键词命中 |
| `kernel/mm/mmap.c` | L7-L11 | 关键词命中 |
| `kernel/mm/kalloc.c` | L30-L34 | 关键词命中 |
| `kernel/mm/vm.c` | L122-L126 | fn walk_va |
| `kernel/mm/vm.c` | L450-L454 | fn uvmadd |
| `kernel/mm/vm.c` | L611-L615 | fn map_ustack |

### 关键代码片段

  - `kernel/mm/vm.c:L8-L12`：关键词命中
    代码片段：`#include "xv6fs.h" #include "proc.h" #include "kalloc.h" /*`
  - `kernel/mm/mmap.c:L7-L11`：关键词命中
    代码片段：`static uint64 allocvma() { struct vma_struct *vma = (struct vma_struct *)kalloc(); if (vma == 0) { return 0;`
  - `kernel/mm/kalloc.c:L30-L34`：关键词命中
    代码片段：`char *p; p = (char *)PGROUNDUP((uint64)pa_start); for (; p + PGSIZE <= (char *)pa_end; p += PGSIZE) kfree(p); }`
  - `kernel/mm/vm.c:L122-L126`：fn walk_va
    代码片段：`/// @param pagetable given pagetable /// @param va  given va void walk_va(pagetable_t pagetable, uint64 va) { pte_t *pte; pte = walk(pagetable, va, 0);`

### 相关符号

`fn walk_va` at `kernel/mm/vm.c:L122`、`fn uvmadd` at `kernel/mm/vm.c:L450`、`fn map_ustack` at `kernel/mm/vm.c:L611`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn arglong, fn argulong, fn arguint32, fn arguint64, fn get_ticksnow。（置信度：medium）
  - 静态识别到 61 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/syscall.c` | L5-L9 | 关键词命中 |
| `kernel/sched/trap.c` | L155-L159 | 关键词命中 |
| `kernel/include/sbi.h` | L301-L305 | 关键词命中 |
| `kernel/syscall.c` | L71-L75 | fn arglong |
| `kernel/syscall.c` | L74-L78 | fn argulong |
| `kernel/syscall.c` | L78-L82 | fn arguint32 |

### 关键代码片段

  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "thread.h"`
  - `kernel/sched/trap.c:L155-L159`：关键词命中
    代码片段：`thread_exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. // p->trapframe->epc += 4;`
  - `kernel/include/sbi.h:L301-L305`：关键词命中
    代码片段：`register uint64 a7 asm("a7") = eid; register uint64 ret asm("a0"); asm volatile("ecall" : "=r"(ret) : "0"(a0), "r"(a1), "r"(a2), "r"(a3), "r"(a7));`
  - `kernel/syscall.c:L71-L75`：fn arglong
    代码片段：`*ip = argraw(n); } void arglong(int n, long *lip) { *lip = (long) argraw(n); }`

### 相关符号

`fn arglong` at `kernel/syscall.c:L71`、`fn argulong` at `kernel/syscall.c:L74`、`fn arguint32` at `kernel/syscall.c:L78`、`macro __SYSCALL_H` at `kernel/include/syscall.h:L1`、`fn sys_mmap` at `kernel/mm/mmap.c:L70`、`fn sys_munmap` at `kernel/mm/mmap.c:L104`、`fn sys_mprotect` at `kernel/mm/mmap.c:L314`、`fn sys_clock_nanosleep` at `kernel/sysproc.c:L51`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct logheader, struct log, fn initlog, fn install_trans, fn read_head。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/fs/ext4.c` | L153-L157 | 关键词命中 |
| `kernel/fs/file.c` | L154-L158 | 关键词命中 |
| `kernel/fs/xv6fs.c` | L2-L6 | 关键词命中 |
| `kernel/fs/log.c` | L33-L37 | struct logheader |
| `kernel/fs/log.c` | L38-L42 | struct log |
| `kernel/fs/log.c` | L56-L60 | fn initlog |

### 关键代码片段

  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `kernel/fs/file.c:L154-L158`：关键词命中
    代码片段：`size_t fileread(struct file *f, int user_dst, uint64 addr, size_t n, int64_t off) { size_t r = 0;`
  - `kernel/fs/xv6fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs/log.c:L33-L37`：struct logheader
    代码片段：`// Contents of the header block, used for both the on-disk header block // and to keep track in memory of logged block# before commit. struct logheader { int n; int block[LOGSIZE];`

### 相关符号

`struct logheader` at `kernel/fs/log.c:L33`、`struct log` at `kernel/fs/log.c:L38`、`fn initlog` at `kernel/fs/log.c:L56`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn sema_init, fn sema_wait, fn sema_signal, struct semaphore, fn cond_wait。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/atomic/semaphore.c` | L1-L4 | 关键词命中 |
| `kernel/include/semaphore.h` | L3-L7 | 关键词命中 |
| `kernel/fs/bio.c` | L1-L5 | 关键词命中 |
| `kernel/atomic/semaphore.c` | L6-L10 | fn sema_init |
| `kernel/atomic/semaphore.c` | L15-L19 | fn sema_wait |
| `kernel/atomic/semaphore.c` | L30-L34 | fn sema_signal |

### 关键代码片段

  - `kernel/atomic/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "cond.h" #include "semaphore.h" /// @brief init a semaphore`
  - `kernel/include/semaphore.h:L3-L7`：关键词命中
    代码片段：`#include "types.h" #include "spinlock.h" #include "cond.h"`
  - `kernel/fs/bio.c:L1-L5`：关键词命中
    代码片段：`// Buffer cache. // // bcache -- spinlock // buf -- sleeplock // The buffer cache is a linked list of buf structures holding`
  - `kernel/atomic/semaphore.c:L6-L10`：fn sema_init
    代码片段：`/// @param value semaphore's value /// @param name semaphore's name void sema_init(sem *s, int value, char *name) { s->value = value; s->wakeup = 0;`

### 相关符号

`fn sema_init` at `kernel/atomic/semaphore.c:L6`、`fn sema_wait` at `kernel/atomic/semaphore.c:L15`、`fn sema_signal` at `kernel/atomic/semaphore.c:L30`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn get_ticksnow, fn pgfault_handler, fn dodebug, fn trapinithart, struct tms。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/sched/trap.c` | L115-L119 | 关键词命中 |
| `kernel/arch/qemu/plic.c` | L6-L10 | 关键词命中 |
| `kernel/mm/vm.c` | L38-L42 | 关键词命中 |
| `kernel/sched/trap.c` | L18-L22 | fn get_ticksnow |
| `kernel/sched/trap.c` | L33-L37 | fn pgfault_handler |
| `kernel/sched/trap.c` | L208-L212 | fn dodebug |

### 关键代码片段

  - `kernel/sched/trap.c:L115-L119`：关键词命中
    代码片段：`// trampoline已经换栈和页表 // // handle an interrupt, exception, or system call from user space. // called from trampoline.S //`
  - `kernel/arch/qemu/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/mm/vm.c:L38-L42`：关键词命中
    代码片段：`kvmmap(kpgtbl, VIRTIO0, VIRTIO0, PGSIZE, PTE_R | PTE_W); // PLIC kvmmap(kpgtbl, PLIC, PLIC, 0x400000, PTE_R | PTE_W);`
  - `kernel/sched/trap.c:L18-L22`：fn get_ticksnow
    代码片段：`uint ticks = 0; extern volatile int boot_hart; uint get_ticksnow(void) { uint t; acquire(&tickslock);`

### 相关符号

`fn get_ticksnow` at `kernel/sched/trap.c:L18`、`fn pgfault_handler` at `kernel/sched/trap.c:L33`、`fn dodebug` at `kernel/sched/trap.c:L208`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct bdev_ops, struct bdev, macro VIRTIO_MMIO_MAGIC_VALUE, macro VIRTIO_MMIO_VERSION, macro VIRTIO_MMIO_DEVICE_ID。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/arch/qemu/uart.c` | L1-L4 | 关键词命中 |
| `kernel/fs/virtio_disk.c` | L1-L4 | 关键词命中 |
| `kernel/include/virtio.h` | L1-L4 | 关键词命中 |
| `kernel/include/device.h` | L4-L8 | struct bdev_ops |
| `kernel/include/device.h` | L9-L13 | struct bdev |
| `kernel/include/virtio.h` | L10-L14 | macro VIRTIO_MMIO_MAGIC_VALUE |

### 关键代码片段

  - `kernel/arch/qemu/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/fs/virtio_disk.c:L1-L4`：关键词命中
    代码片段：`// // driver for qemu's virtio disk device. // uses qemu's mmio interface to virtio. //`
  - `kernel/include/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `kernel/include/device.h:L4-L8`：struct bdev_ops
    代码片段：`// Block device switch table entry. struct bdev_ops { int (*open)(int minor); int (*close)(int minor);`

### 相关符号

`struct bdev_ops` at `kernel/include/device.h:L4`、`struct bdev` at `kernel/include/device.h:L9`、`macro VIRTIO_MMIO_MAGIC_VALUE` at `kernel/include/virtio.h:L10`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
