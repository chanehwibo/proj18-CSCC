# 2026 T2026104869910188 比较报告

- 对比历史仓库：2022 图漏图森破
- 生成时间：2026-07-01T21:03:26.878094+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 图漏图森破（来源：赛事历史作品）：画像相似度 score=9.38；同属 ucore-variant 风格; 语言构成相似度 0.92; OS 维度重合度 1.00; 代码规模接近度 0.54

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 图漏图森破 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `kernel/driver/Sd.c:L1-L5`：关键词命中
    代码片段：`#include <Platform.h> #include <Spi.h> #include <Uart.h> #include <Driver.h> #include <MemoryConfig.h>`
  - `kernel/driver/Print.c:L1-L3`：关键词命中
    代码片段：`#include <Driver.h> #include <Riscv.h> #include <Spinlock.h>`
- 与 2022 图漏图森破 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/file.c:L3-L7`：关键词命中
    代码片段：`// // filestat, fileclose may need vfs file operations? #include "types.h"`
  - `kernel/file.h:L1-L4`：关键词命中
    代码片段：`#include "kernel/xv6_fs.h" #include "kernel/vfs/vfs.h" #define PIPESIZE 512`
  - `kernel/fs/Fat.c:L1-L5`：关键词命中
    代码片段：`#include <Defs.h> #include "Bio.h" #include "Fat.h" #include "Sleeplock.h" #include "Spinlock.h"`
  - `kernel/fs/File.c:L101-L105`：关键词命中
    代码片段：`// Read from file f. // addr is a user virtual address. int fileread(struct File* f, bool isUser, u64 addr, int n) { int r = 0;`
- 与 2022 图漏图森破 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L34-L38`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/trap/Exec.c:L9-L13`：关键词命中
    代码片段：`#include <Sysarg.h> #include <Debug.h> #include <Trap.h> #include <Sysfile.h> #include <uapi/linux/Auxvec.h>`
  - `kernel/trap/Trap.c:L1-L5`：关键词命中
    代码片段：`#include <Riscv.h> #include <Driver.h> #include <Timer.h> #include <Trap.h> #include <Process.h>`
- 与 2022 图漏图森破 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/vm.c:L11-L15`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/page.h:L37-L41`：关键词命中
    代码片段：`// PFN is KERNBASE-relative so that mem_map[0] = page at KERNBASE (0x80000000). // This keeps all indices within [0, (PHYSTOP-KERNBASE)/PGSIZE). #define PFN_DOWN(pa)     (((uint...`
  - `kernel/memory/Page.c:L18-L22`：关键词命中
    代码片段：`} int pageRemove(u64 *pgdir, u64 va) { u64 *pte; u64 pa = pageLookup(pgdir, va, &pte);`
  - `kernel/memory/MemoryInit.c:L37-L41`：关键词命中
    代码片段：`static void virtualMemory() { u64 va, pa; pageInsert(kernelPageDirectory, UART_V, UART, PTE_READ | PTE_WRITE | PTE_ACCESSED | PTE_DIRTY); va = CLINT_V, pa = CLINT; for (u64 i =...`
- 与 2022 图漏图森破 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "file.h"`
  - `kernel/proc.h:L42-L46`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `kernel/yield/Thread.c:L1-L3`：关键词命中
    代码片段：`#include <Thread.h> #include <Riscv.h> #include <MemoryConfig.h>`
  - `kernel/fs/Fat.c:L12-L16`：关键词命中
    代码片段：`#include <FileSystem.h> #include <Sysfile.h> #include <Thread.h> #include <Riscv.h>`
- 与 2022 图漏图森破 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "xv6_fs.h"`
  - `kernel/bio.c:L17-L21`：关键词命中
    代码片段：`#include "types.h" #include "param.h" #include "spinlock.h" #include "sleeplock.h" #include "arch.h"`
  - `kernel/lock/Spinlock.c:L1-L3`：关键词命中
    代码片段：`#include "Spinlock.h" #include "Hart.h" #include "Interrupt.h"`
  - `kernel/lock/Sleeplock.c:L3-L7`：关键词命中
    代码片段：`#include "Sleeplock.h" #include "Process.h" #include "Spinlock.h" #include "Driver.h" #include <Debug.h>`
- 与 2022 图漏图森破 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap.c:L85-L89`：关键词命中
    代码片段：`// Save init's registers for epc corruption recovery if(p->pid == 1) { p->saved_epc = p->trapframe->epc + 4;  // instruction after ecall p->saved_ra = p->trapframe->ra; p->saved...`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "errno.h"`
  - `kernel/trap/Syscall.c:L1-L3`：关键词命中
    代码片段：`#include <Syscall.h> #include <Driver.h> #include <Process.h>`
  - `kernel/system/SignalTrampoline.S:L4-L6`：关键词命中
    代码片段：`signalTrampoline: li a7, 139 ecall`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 宏名重合：与 2022 图漏图森破 在“设备驱动”维度发现 4 个同名定义：VIRTIO0, ENXIO, ENOTBLK, EBUSY。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/arch/loongarch/memlayout.h:L35-L35`：macro VIRTIO0
    代码片段：`#define VIRTIO0 0x10001000`
  - `include/MemoryConfig.h:L9-L9`：macro VIRTIO0
    代码片段：`#define VIRTIO0                 0x10001000`
  - `kernel/errno.h:L11-L11`：macro ENXIO
    代码片段：`#define ENXIO        6   /* No such device or address */`
  - `include/Error.h:L20-L20`：macro ENXIO
    代码片段：`#define	ENXIO		 6	/* No such device or address */`
- 宏名重合：与 2022 图漏图森破 在“文件系统”维度发现 4 个同名定义：ENOENT, EBADF, EEXIST, ENFILE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/errno.h:L7-L7`：macro ENOENT
    代码片段：`#define ENOENT       2   /* No such file or directory */`
  - `include/Error.h:L16-L16`：macro ENOENT
    代码片段：`#define	ENOENT		 2	/* No such file or directory */`
  - `kernel/errno.h:L13-L13`：macro EBADF
    代码片段：`#define EBADF        9   /* Bad file number */`
  - `include/Error.h:L23-L23`：macro EBADF
    代码片段：`#define	EBADF		 9	/* Bad file number */`
- 宏名重合：与 2022 图漏图森破 在“中断与异常”维度发现 4 个同名定义：SIE_STIE, PLIC, SSTATUS_SPIE, SSTATUS_UPIE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/arch/loongarch/arch.h:L147-L147`：macro SIE_STIE
    代码片段：`#define SIE_STIE (1L << 11) // timer interrupt (ECFG bit 11 for timer)`
  - `include/Riscv.h:L71-L71`：macro SIE_STIE
    代码片段：`#define SIE_STIE (1L << 5) // timer`
  - `kernel/arch/loongarch/memlayout.h:L51-L51`：macro PLIC
    代码片段：`#define PLIC 0L`
  - `include/MemoryConfig.h:L16-L16`：macro PLIC
    代码片段：`#define PLIC                    0x0c000000L`
- 宏名重合：与 2022 图漏图森破 在“内存管理”维度发现 4 个同名定义：MAKE_SATP, PGSIZE, PA2PTE, PTE2PA。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/arch/loongarch/arch.h:L323-L323`：macro MAKE_SATP
    代码片段：`#define MAKE_SATP(pagetable) ((uint64)(pagetable))`
  - `include/Riscv.h:L152-L152`：macro MAKE_SATP
    代码片段：`#define MAKE_SATP(pagetable) (SATP_SV39 | (((u64)pagetable) >> 12))`
  - `kernel/arch/loongarch/arch.h:L618-L618`：macro PGSIZE
    代码片段：`#define PGSIZE 4096 // bytes per page`
  - `include/Page.h:L14-L14`：macro PGSIZE
    代码片段：`#define PGSIZE (4096)`
- 宏名重合：与 2022 图漏图森破 在“调度与任务管理”维度发现 3 个同名定义：ESRCH, ECHILD, NOFILE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/errno.h:L8-L8`：macro ESRCH
    代码片段：`#define ESRCH        3   /* No such process */`
  - `include/Error.h:L17-L17`：macro ESRCH
    代码片段：`#define	ESRCH		 3	/* No such process */`
  - `kernel/errno.h:L14-L14`：macro ECHILD
    代码片段：`#define ECHILD      10   /* No child processes */`
  - `include/Error.h:L24-L24`：macro ECHILD
    代码片段：`#define	ECHILD		10	/* No child processes */`
- 宏名重合：与 2022 图漏图森破 在“同步机制”维度发现 1 个同名定义：ENOTBLK。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/errno.h:L19-L19`：macro ENOTBLK
    代码片段：`#define ENOTBLK     15   /* Block device required */`
  - `include/Error.h:L29-L29`：macro ENOTBLK
    代码片段：`#define	ENOTBLK		15	/* Block device required */`
- 结构体/类型重合：与 2022 图漏图森破 在“文件系统”维度发现 2 个同名定义：file, devsw。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/file.h:L15-L15`：struct file
    代码片段：`struct file {`
  - `include/File.h:L21-L21`：struct File
    代码片段：`typedef struct File {`
  - `kernel/file.h:L53-L53`：struct devsw
    代码片段：`struct devsw {`
  - `include/File.h:L39-L39`：struct devsw
    代码片段：`struct devsw {`
- 文件路径重合：与 2022 图漏图森破 在“设备驱动”维度出现同名文件源码路径 `kernel/console.c` / `kernel/driver/Console.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/driver/Console.c:L1-L4`：关键词命中
    代码片段：`#include <Type.h> #include <Driver.h> #include <Uart.h> #include <Spinlock.h>`
- 文件路径重合：与 2022 图漏图森破 在“文件系统”维度出现同名文件源码路径 `kernel/file.c` / `kernel/fs/File.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/file.c:L3-L7`：关键词命中
    代码片段：`// // filestat, fileclose may need vfs file operations? #include "types.h"`
  - `kernel/fs/File.c:L101-L105`：关键词命中
    代码片段：`// Read from file f. // addr is a user virtual address. int fileread(struct File* f, bool isUser, u64 addr, int n) { int r = 0;`
- 文件路径重合：与 2022 图漏图森破 在“中断与异常”维度出现同名文件源码路径 `kernel/trap.c` / `kernel/trap/Trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/trap.c:L34-L38`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/trap/Trap.c:L1-L5`：关键词命中
    代码片段：`#include <Riscv.h> #include <Driver.h> #include <Timer.h> #include <Trap.h> #include <Process.h>`
- 文件路径重合：与 2022 图漏图森破 在“同步机制”维度出现同名文件源码路径 `kernel/bio.c` / `kernel/fs/Bio.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/bio.c:L17-L21`：关键词命中
    代码片段：`#include "types.h" #include "param.h" #include "spinlock.h" #include "sleeplock.h" #include "arch.h"`
  - `kernel/fs/Bio.c:L17-L21`：关键词命中
    代码片段：`// #include "param.h" #include "Sleeplock.h" #include "Spinlock.h" // #include "riscv.h" // #include "Defs.h"`
- 文件路径重合：与 2022 图漏图森破 在“系统调用”维度出现同名文件源码路径 `kernel/syscall.c` / `kernel/trap/Syscall.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "errno.h"`
  - `kernel/trap/Syscall.c:L1-L3`：关键词命中
    代码片段：`#include <Syscall.h> #include <Driver.h> #include <Process.h>`
- 文件路径重合：与 2022 图漏图森破 在“系统调用”维度出现同名文件源码路径 `kernel/syscall.h` / `root/usr/usr/include/syscall.h`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/syscall.h:L1-L4`：macro SYS_io_setup
    代码片段：`// System call numbers — Linux 5.10 RISC-V ABI #define SYS_io_setup     0 #define SYS_io_destroy   1 #define SYS_io_submit    2`
  - `root/usr/usr/include/syscall.h:L1-L1`：关键词命中
    代码片段：`#include <sys/syscall.h>`

## 相似点

- 与 2022 图漏图森破 同属 ucore-variant 风格。（置信度：medium）
- 与 2022 图漏图森破 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
- 与 2022 图漏图森破 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/file.c:L3-L7`：关键词命中
    代码片段：`// // filestat, fileclose may need vfs file operations? #include "types.h"`
  - `kernel/file.h:L1-L4`：关键词命中
    代码片段：`#include "kernel/xv6_fs.h" #include "kernel/vfs/vfs.h" #define PIPESIZE 512`
- 与 2022 图漏图森破 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L34-L38`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
- 与 2022 图漏图森破 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/vm.c:L11-L15`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/page.h:L37-L41`：关键词命中
    代码片段：`// PFN is KERNBASE-relative so that mem_map[0] = page at KERNBASE (0x80000000). // This keeps all indices within [0, (PHYSTOP-KERNBASE)/PGSIZE). #define PFN_DOWN(pa)     (((uint...`
- 与 2022 图漏图森破 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "file.h"`
  - `kernel/proc.h:L42-L46`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2022 图漏图森破 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "xv6_fs.h"`
  - `kernel/bio.c:L17-L21`：关键词命中
    代码片段：`#include "types.h" #include "param.h" #include "spinlock.h" #include "sleeplock.h" #include "arch.h"`
- 与 2022 图漏图森破 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap.c:L85-L89`：关键词命中
    代码片段：`// Save init's registers for epc corruption recovery if(p->pid == 1) { p->saved_epc = p->trapframe->epc + 4;  // instruction after ecall p->saved_ra = p->trapframe->ra; p->saved...`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "errno.h"`
- 与 2022 图漏图森破 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 图漏图森破 的语言构成不同：待测作品为 {'yaml': 62, 'build': 413, 'markdown': 5535, 'c': 184838, 'asm': 1214, 'make': 256, 'text': 13}，历史样本为 {'json': 20, 'make': 28, 'build': 326, 'markdown': 3993, 'c': 405628, 'asm': 408, 'cpp': 36715, 'text': 55}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：27
- 含证据关键结论数：27（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
