# 2026 T202610486999570 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t202610486999570-2543`
- 风格：rcore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：5283
- 代码/文本行数：2344218
- 主要语言：rust 1849272 LOC, json 282040 LOC, markdown 98447 LOC, text 41103 LOC, c 34156 LOC

## 总览

2026 T202610486999570 是一个 rcore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 1849272 LOC, json: 282040 LOC, markdown: 98447 LOC, text: 41103 LOC。仓库包含 5283 个已扫描文件、约 2344218 行可分析文本，当前抽取到 150285 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, arch/Cargo.toml, easy-fs-fuse/Cargo.toml, easy-fs/Cargo.toml, os/Cargo.toml。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`DOCKER_NAME ?= rcore-docker LOG ?= OFF .PHONY: docker build_docker rv la all debug clean`
  - `arch/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "arch" version = "0.1.0"`
  - `easy-fs-fuse/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "easy-fs-fuse" version = "0.1.0"`
  - `easy-fs/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "easy-fs" version = "0.1.0"`
  - `os/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "os" version = "0.1.0"`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn pool_pop, fn pool_push, struct RecycleAllocator, fn new, fn new_with_start。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/task/id.rs` | L1-L3 | 关键词命中 |
| `os/src/task/mod.rs` | L12-L16 | 关键词命中 |
| `os/src/task/tls.rs` | L1-L3 | 关键词命中 |
| `os/src/task/id.rs` | L26-L30 | fn pool_pop |
| `os/src/task/id.rs` | L30-L34 | fn pool_push |
| `os/src/task/id.rs` | L38-L42 | struct RecycleAllocator |

### 关键代码片段

  - `os/src/task/id.rs:L1-L3`：关键词命中
    代码片段：`//! Task pid and user resource implementation. use super::ProcessControlBlock;`
  - `os/src/task/mod.rs:L12-L16`：关键词命中
    代码片段：`#[allow(clippy::module_inception)] #[allow(rustdoc::private_intra_doc_links)] mod task; mod tls; use crate::config::USER_STACK_TOP as USER_ADDR_MAX;`
  - `os/src/task/tls.rs:L1-L3`：关键词命中
    代码片段：`//! Thread Local Storage (TLS) support for RISC-V //! //! RISC-V uses musl's TLS_ABOVE_TP layout:`
  - `os/src/task/id.rs:L26-L30`：fn pool_pop
    代码片段：`} fn pool_pop() -> Option<Vec<u128>> { KSTACK_POOL.exclusive_access().pop() }`

### 相关符号

`fn pool_pop` at `os/src/task/id.rs:L26`、`fn pool_push` at `os/src/task/id.rs:L30`、`struct RecycleAllocator` at `os/src/task/id.rs:L38`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn init, fn kernel_page_table_token_if_ready, fn init_heap, fn handle_alloc_error, struct SharedFilePageKey。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/mm/mod.rs` | L1-L5 | 关键词命中 |
| `os/src/heap_alloc.rs` | L6-L10 | 关键词命中 |
| `os/src/mm/memory_set.rs` | L1-L3 | 关键词命中 |
| `os/src/mm/mod.rs` | L31-L35 | fn init |
| `os/src/mm/mod.rs` | L39-L43 | fn kernel_page_table_token_if_ready |
| `os/src/heap_alloc.rs` | L12-L16 | fn init_heap |

### 关键代码片段

  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `os/src/heap_alloc.rs:L6-L10`：关键词命中
    代码片段：`#[global_allocator] /// heap allocator instance static HEAP_ALLOCATOR: LockedHeap = LockedHeap::empty(); /// heap space ([u8; KERNEL_HEAP_SIZE])`
  - `os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`//! Implementation of ['MapArea'] and ['MemorySet']. use super::address_space_policy; use super::{frame_alloc, FrameTracker};`
  - `os/src/mm/mod.rs:L31-L35`：fn init
    代码片段：`/// initiate heap allocator, frame allocator and kernel space pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`

### 相关符号

`fn init` at `os/src/mm/mod.rs:L31`、`fn kernel_page_table_token_if_ready` at `os/src/mm/mod.rs:L39`、`fn init_heap` at `os/src/heap_alloc.rs:L12`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn handle_user_syscall, fn handle_user_time_interrupt, fn kernel_interrupt_dispatch, fn user_trap_loop, fn __alltraps。（置信度：medium）
  - 静态识别到 577 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/syscall/fs.rs` | L768-L772 | 关键词命中 |
| `os/src/net/syscall.rs` | L14-L18 | 关键词命中 |
| `os/src/syscall/mod.rs` | L1-L5 | 关键词命中 |
| `os/src/trap/mod.rs` | L29-L33 | fn handle_user_syscall |
| `os/src/trap/mod.rs` | L36-L40 | fn handle_user_time_interrupt |
| `os/src/trap/mod.rs` | L44-L48 | fn kernel_interrupt_dispatch |

### 关键代码片段

  - `os/src/syscall/fs.rs:L768-L772`：关键词命中
    代码片段：`pub fn sys_write(fd: usize, buf: *const u8, len: usize) -> isize { let pid = current_process().pid.0; if crate::syscall::should_trace_syscall(pid) && fd != 1 { syscall!("kernel:...`
  - `os/src/net/syscall.rs:L14-L18`：关键词命中
    代码片段：`use crate::fs::{open_file, path_is_dir, OpenFlags}; use crate::mm::translated_refmut; use crate::syscall::user_mem::{self, UserReadPolicy, UserWritePolicy}; use crate::task::{ c...`
  - `os/src/syscall/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Implementation of syscalls //! //! The single entry point to all system calls, ['syscall()'], is called //! whenever userspace wishes to perform a system call using the 'eca...`
  - `os/src/trap/mod.rs:L29-L33`：fn handle_user_syscall
    代码片段：`pub use user_trap_loop as user_trap_entry; fn handle_user_syscall() { let trap_cx = current_trap_cx(); trap_cx.sepc += 4;`

### 相关符号

`fn handle_user_syscall` at `os/src/trap/mod.rs:L29`、`fn handle_user_time_interrupt` at `os/src/trap/mod.rs:L36`、`fn kernel_interrupt_dispatch` at `os/src/trap/mod.rs:L44`、`fn handle_user_syscall` at `os/src/trap/mod.rs:L29`、`fn sys_write` at `os/src/syscall/fs.rs:L766`、`fn sys_read` at `os/src/syscall/fs.rs:L824`、`fn sys_openat` at `os/src/syscall/fs.rs:L881`、`fn sys_name_to_handle_at` at `os/src/syscall/fs.rs:L1057`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：trait File, fn readable, fn writable, fn read, fn write。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/fs/mod.rs` | L1-L3 | 关键词命中 |
| `os/src/fs/memfd.rs` | L1-L4 | 关键词命中 |
| `os/src/syscall/fs.rs` | L138-L142 | 关键词命中 |
| `os/src/fs/mod.rs` | L20-L24 | trait File |
| `os/src/fs/mod.rs` | L22-L26 | fn readable |
| `os/src/fs/mod.rs` | L24-L28 | fn writable |

### 关键代码片段

  - `os/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`//! File trait & inode(dir, file, pipe, stdin, stdout) mod epoll;`
  - `os/src/fs/memfd.rs:L1-L4`：关键词命中
    代码片段：`//! In-memory anonymous file for memfd_create(2). use super::vfs::{VfsInode, VfsNodeKind}; use super::{File, PollEvents}; use crate::mm::UserBuffer;`
  - `os/src/syscall/fs.rs:L138-L142`：关键词命中
    代码片段：`fn inode_for_path(path: &str) -> Option<Arc<dyn VfsInode>> { open_file(path, OpenFlags::empty()).and_then(|file| file.inode()) }`
  - `os/src/fs/mod.rs:L20-L24`：trait File
    代码片段：`/// trait File for all file types pub trait File: Send + Sync { /// the file readable? fn readable(&self) -> bool;`

### 相关符号

`trait File` at `os/src/fs/mod.rs:L20`、`fn readable` at `os/src/fs/mod.rs:L22`、`fn writable` at `os/src/fs/mod.rs:L24`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct UPSafeCell, fn new, fn exclusive_access, fn try_exclusive_access, struct UPSafeCellRaw。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/sync/up.rs` | L120-L124 | 关键词命中 |
| `os/src/sync/mod.rs` | L1-L5 | 关键词命中 |
| `os/src/sync/mutex.rs` | L8-L12 | 关键词命中 |
| `os/src/sync/up.rs` | L13-L17 | struct UPSafeCell |
| `os/src/sync/up.rs` | L24-L28 | fn new |
| `os/src/sync/up.rs` | L30-L34 | fn exclusive_access |

### 关键代码片段

  - `os/src/sync/up.rs:L120-L124`：关键词命中
    代码片段：`} /// Guard returned by ['UPIntrMutex::lock']. pub type UPIntrMutexGuard<'a, T> = UPIntrRefMut<'a, T>; /// Shared guard returned by ['UPIntrRwLock::read'].`
  - `os/src/sync/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization and interior mutability primitives mod condvar; mod mutex; mod semaphore;`
  - `os/src/sync/mutex.rs:L8-L12`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub trait Mutex: Sync + Send { fn lock(&self); fn unlock(&self);`
  - `os/src/sync/up.rs:L13-L17`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>,`

### 相关符号

`struct UPSafeCell` at `os/src/sync/up.rs:L13`、`fn new` at `os/src/sync/up.rs:L24`、`fn exclusive_access` at `os/src/sync/up.rs:L30`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：struct TimerCondVar, fn eq, fn partial_cmp, fn cmp, fn with_timers_read。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/timer.rs` | L1-L3 | 关键词命中 |
| `os/src/trap/mod.rs` | L1-L3 | 关键词命中 |
| `os/src/drivers/plic.rs` | L1-L4 | 关键词命中 |
| `os/src/timer.rs` | L17-L21 | struct TimerCondVar |
| `os/src/timer.rs` | L23-L27 | fn eq |
| `os/src/timer.rs` | L31-L35 | fn partial_cmp |

### 关键代码片段

  - `os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! Timer management: hardware timer + task scheduling queue. #![allow(missing_docs)]`
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling for the kernel. //! //! User-mode traps are handled in 'user_trap_loop()' (arch-specific).`
  - `os/src/drivers/plic.rs:L1-L4`：关键词命中
    代码片段：`#[allow(clippy::upper_case_acronyms)] /// PLIC (Platform-Level Interrupt Controller) MMIO accessor. pub struct PLIC { base_addr: usize,`
  - `os/src/timer.rs:L17-L21`：struct TimerCondVar
    代码片段：`static CHECK_TIMER_COUNT: AtomicU64 = AtomicU64::new(0); pub struct TimerCondVar { pub expire_ms: usize, pub task: Arc<TaskControlBlock>,`

### 相关符号

`struct TimerCondVar` at `os/src/timer.rs:L17`、`fn eq` at `os/src/timer.rs:L23`、`fn partial_cmp` at `os/src/timer.rs:L31`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct Stdout, fn write_str, fn print, fn getchar, struct PLIC。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/console.rs` | L1-L3 | 关键词命中 |
| `os/src/drivers/mod.rs` | L1-L3 | 关键词命中 |
| `os/src/drivers/bus/mod.rs` | L1-L3 | 关键词命中 |
| `os/src/console.rs` | L4-L8 | struct Stdout |
| `os/src/console.rs` | L7-L11 | fn write_str |
| `os/src/console.rs` | L15-L19 | fn print |

### 关键代码片段

  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console output for text mode. use core::fmt::{self, Write};`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`/// Block device drivers. pub mod block; /// Bus and DMA helpers.`
  - `os/src/drivers/bus/mod.rs:L1-L3`：关键词命中
    代码片段：`/// VirtIO bus support and DMA helpers. #[cfg(target_arch = "riscv64")] #[path = "virtio_rv.rs"]`
  - `os/src/console.rs:L4-L8`：struct Stdout
    代码片段：`use arch::{console_getchar as raw_getchar, console_putchar as raw_putchar}; struct Stdout; impl Write for Stdout {`

### 相关符号

`struct Stdout` at `os/src/console.rs:L4`、`fn write_str` at `os/src/console.rs:L7`、`fn print` at `os/src/console.rs:L15`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
