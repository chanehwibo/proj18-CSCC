# 2026 T202610475999595 比较报告

- 对比历史仓库：2025 达达乐队
- 生成时间：2026-07-01T21:03:21.613013+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 达达乐队（来源：赛事历史作品）：画像相似度 score=9.86；同属 ucore-variant 风格; 语言构成相似度 0.98; OS 维度重合度 1.00; 代码规模接近度 0.91

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 达达乐队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `kernel/dev/uart.c:L1-L4`：关键词命中
    代码片段：`/* * uart.c - 16550a UART 驱动（轮询 + 中断回显） * * 提供：`
  - `kernel/dev/virtio.c:L1-L4`：关键词命中
    代码片段：`/* * virtio.c - QEMU virtio-blk（虚拟磁盘）驱动 * * QEMU 侧常见配置：-device virtio-blk-device,drive=x0,bus=virtio-mmio-bus.0`
- 与 2025 达达乐队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs.h:L7-L11`：关键词命中
    代码片段：`// Disk layout: // [ boot block | super block | log | inode blocks | //                                          free bit map | data blocks] //`
  - `kernel/fs/fs.c:L4-L8`：关键词命中
    代码片段：`* 当前版本的 fs_init() 做了两件事： *   1) 初始化 buf cache，并从磁盘读取超级块到内存副本 sb； *   2) 运行一段 inode 读写自测逻辑（便于实验阶段验证 inode/data 分配链路）。 * * 注意：自测结束后会进入 while(1) 死循环。`
  - `kernel/fs/dir.c:L7-L11`：关键词命中
    代码片段：`* * 锁语义： *   - 目录项的查找/增删/遍历都要求调用者持有父目录 inode 的睡眠锁（pip->slk）。 *   - path_* 系列会按需对中间目录 inode 加锁、查找目录项、再移动到下一段。 */`
- 与 2025 达达乐队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L31-L35`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/dev/plic.c:L1-L4`：关键词命中
    代码片段：`/* * plic.c - PLIC（Platform-level interrupt controller） * * 负责外部中断（external interrupt）的分发：`
  - `kernel/dev/timer.c:L1-L5`：关键词命中
    代码片段：`#include "lib/lock.h" #include "lib/print.h" #include "dev/timer.h" #include "memlayout.h" #include "riscv.h"`
- 与 2025 达达乐队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/vm.c:L10-L14`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/kalloc.c:L36-L40`：关键词命中
    代码片段：`char *p; p = (char*)PGROUNDUP((uint64)pa_start); for(; p + PGSIZE <= (char*)pa_end; p += PGSIZE) kfree(p); }`
  - `kernel/fs/elf.c:L7-L11`：关键词命中
    代码片段：`*       1) 按页分配物理页并映射到用户页表 *       2) 从 elf_data 拷贝文件内容（filesz），其余 memsz 区域保持为 0（BSS） *   - 输出入口地址 e_entry 与 heap_top（最高段末尾按页对齐） */`
  - `kernel/fs/file.c:L433-L437`：关键词命中
    代码片段：`uint32 done = 0; while (done < to_read) { uint32 page_idx = (pos + done) / PGSIZE; uint32 page_off = (pos + done) % PGSIZE; if (page_idx >= RAMFS_MAX_PAGES || rf->pages[page_idx...`
- 与 2025 达达乐队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "riscv.h" #include "spinlock.h" #include "proc.h" #include "defs.h"`
  - `kernel/proc.h:L22-L26`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `kernel/proc/cpu.c:L1-L3`：关键词命中
    代码片段：`#include "proc/cpu.h" #include "riscv.h"`
  - `kernel/proc/proc.c:L4-L8`：关键词命中
    代码片段：`#include "dev/uart.h" #include "mem/vmem.h" #include "proc/cpu.h" #include "proc/initcode.h" #include "trap/trap.h"`
- 与 2025 达达乐队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs.c:L15-L19`：关键词命中
    代码片段：`#include "param.h" #include "stat.h" #include "spinlock.h" #include "proc.h" #include "sleeplock.h"`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "riscv.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "fs.h"`
  - `kernel/lib/spinlock.c:L1-L4`：关键词命中
    代码片段：`/* * spinlock.c - 自旋锁与可嵌套的关/开中断 * * 这份实现的核心思路：`
  - `kernel/lib/sleeplock.c:L13-L17`：关键词命中
    代码片段：`*/ #include "lib/lock.h" #include "proc/proc.h" #include "proc/cpu.h"`
- 与 2025 达达乐队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap.c:L58-L62`：关键词命中
    代码片段：`kexit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h"`
  - `kernel/syscall/syscall.c:L3-L7`：关键词命中
    代码片段：`#include "mem/mmap.h" #include "mem/vmem.h" #include "syscall/syscall.h" #include "syscall/sysnum.h" #include "syscall/sysfunc.h"`
  - `kernel/syscall/sysfunc.c:L3-L7`：关键词命中
    代码片段：`* * 目标与定位： *   - 为 oscomp / riscv-syscalls-testing 等测试提供“够用”的 Linux 风格 syscall 语义。 *   - 这是一个兼容层而非完整的 Linux：很多 syscall 仅实现最小子集，或直接返回固定值。 *`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 达达乐队 在“调度与任务管理”维度发现片段级代码相似度 0.92 （token=0.88, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：context, ra, sp, struct, uint64。（置信度：high）
  证据：
  - `kernel/proc.h:L1-L5`：struct context
    代码片段：`#include "types.h" // Saved registers for kernel context switches. struct context { uint64 ra; uint64 sp;`
  - `include/proc/proc.h:L30-L34`：struct context
    代码片段：`// context：内核态上下文切换时保存的寄存器集合。 // swtch(old, new) 会把当前 CPU 的寄存器保存到 old，并从 new 恢复。 typedef struct context { uint64 ra; // 返回地址：下次执行从哪里开始 uint64 sp; // 栈指针：栈在哪里`
- 与 2025 达达乐队 在“调度与任务管理”维度发现片段级代码相似度 0.66 （token=0.58, structure=0.80）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：cpu, int, noff, struct。（置信度：medium）
  证据：
  - `kernel/proc.h:L22-L26`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `include/proc/cpu.h:L13-L17`：struct cpu
    代码片段：`*/ typedef struct cpu { int noff;       // 关中断的深度 int origin;     // 第一次关中断前的状态`
- 宏名重合：与 2025 达达乐队 在“设备驱动”维度发现 4 个同名定义：Reg, RHR, THR, IER。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/uart.c:L16-L16`：macro Reg
    代码片段：`#define Reg(reg) ((volatile unsigned char *)(UART0 + (reg)))`
  - `kernel/dev/uart.c:L40-L40`：macro Reg
    代码片段：`#define Reg(reg)         ((volatile unsigned char *)(UART_BASE + reg))`
  - `kernel/uart.c:L22-L22`：macro RHR
    代码片段：`#define RHR 0                 // receive holding register (for input bytes)`
  - `kernel/dev/uart.c:L23-L23`：macro RHR
    代码片段：`#define RHR 0                 // receive holding register (for input bytes)`
- 宏名重合：与 2025 达达乐队 在“中断与异常”维度发现 4 个同名定义：PLIC_PRIORITY, PLIC_PENDING, PLIC_SENABLE, PLIC_SPRIORITY。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/memlayout.h:L30-L30`：macro PLIC_PRIORITY
    代码片段：`#define PLIC_PRIORITY (PLIC + 0x0)`
  - `include/memlayout.h:L15-L15`：macro PLIC_PRIORITY
    代码片段：`#define PLIC_PRIORITY(id) (PLIC_BASE + (id) * 4)`
  - `kernel/memlayout.h:L31-L31`：macro PLIC_PENDING
    代码片段：`#define PLIC_PENDING (PLIC + 0x1000)`
  - `include/memlayout.h:L16-L16`：macro PLIC_PENDING
    代码片段：`#define PLIC_PENDING (PLIC_BASE + 0x1000)`
- 宏名重合：与 2025 达达乐队 在“内存管理”维度发现 4 个同名定义：TRAMPOLINE, KSTACK, TRAPFRAME, SATP_SV39。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/memlayout.h:L44-L44`：macro TRAMPOLINE
    代码片段：`#define TRAMPOLINE (MAXVA - PGSIZE)`
  - `include/memlayout.h:L40-L40`：macro TRAMPOLINE
    代码片段：`#define TRAMPOLINE (VA_MAX - PGSIZE)`
  - `kernel/memlayout.h:L48-L48`：macro KSTACK
    代码片段：`#define KSTACK(p) (TRAMPOLINE - ((p)+1)* 2*PGSIZE)`
  - `include/memlayout.h:L47-L47`：macro KSTACK
    代码片段：`#define KSTACK(cpu) (TRAMPOLINE - ((cpu) + 1) * 2 * PGSIZE)`
- 宏名重合：与 2025 达达乐队 在“系统调用”维度发现 4 个同名定义：TRAPFRAME, SYS_fork, SYS_exit, SYS_wait。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/memlayout.h:L59-L59`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME (TRAMPOLINE - PGSIZE)`
  - `include/memlayout.h:L43-L43`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME (TRAMPOLINE - PGSIZE)`
  - `kernel/syscall.h:L2-L2`：macro SYS_fork
    代码片段：`#define SYS_fork    1`
  - `user/syscall_num.h:L8-L8`：macro SYS_fork
    代码片段：`#define SYS_fork         4`
- 结构体/类型重合：与 2025 达达乐队 在“文件系统”维度发现 3 个同名定义：file, inode, dirent。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/file.h:L2-L2`：struct file
    代码片段：`struct file {`
  - `include/fs/file.h:L33-L33`：struct file
    代码片段：`typedef struct file {`
  - `kernel/file.h:L18-L18`：struct inode
    代码片段：`struct inode {`
  - `include/fs/inode.h:L44-L44`：struct inode
    代码片段：`typedef struct inode {`
- 函数/符号名重合：与 2025 达达乐队 在“调度与任务管理”维度发现 1 个同名定义：swtch。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/swtch.S:L9-L9`：fn swtch
    代码片段：`swtch:`
  - `kernel/proc/switch.S:L14-L14`：fn swtch
    代码片段：`swtch:`
- 结构体/类型重合：与 2025 达达乐队 在“调度与任务管理”维度发现 4 个同名定义：context, cpu, trapframe, proc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/proc.h:L3-L3`：struct context
    代码片段：`struct context {`
  - `include/proc/proc.h:L32-L32`：struct context
    代码片段：`typedef struct context {`
  - `kernel/proc.h:L23-L23`：struct cpu
    代码片段：`struct cpu {`
  - `include/proc/cpu.h:L15-L15`：struct cpu
    代码片段：`typedef struct cpu {`
- 宏名重合：与 2025 达达乐队 在“文件系统”维度发现 2 个同名定义：PGSHIFT, SYS_fstat。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/riscv.h:L353-L353`：macro PGSHIFT
    代码片段：`#define PGSHIFT 12  // bits of offset within a page`
  - `include/riscv.h:L300-L300`：macro PGSHIFT
    代码片段：`#define PGSHIFT 12  // bits of offset within a page`
  - `kernel/syscall.h:L9-L9`：macro SYS_fstat
    代码片段：`#define SYS_fstat   8`
  - `user/syscall_num.h:L19-L19`：macro SYS_fstat
    代码片段：`#define SYS_fstat        14`
- 宏名重合：与 2025 达达乐队 在“调度与任务管理”维度发现 2 个同名定义：NPROC, SYS_sched_yield。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/param.h:L1-L1`：macro NPROC
    代码片段：`#define NPROC        64  // maximum number of processes`
  - `include/proc/proc.h:L22-L22`：macro NPROC
    代码片段：`#define NPROC 64`
  - `kernel/syscall.h:L29-L29`：macro SYS_sched_yield
    代码片段：`#define SYS_sched_yield 124`
  - `include/syscall/sysnum.h:L40-L40`：macro SYS_sched_yield
    代码片段：`#define SYS_sched_yield  124`
- 结构体/类型重合：与 2025 达达乐队 在“中断与异常”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/proc.h:L44-L44`：struct trapframe
    代码片段：`struct trapframe {`
  - `include/proc/proc.h:L52-L52`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 结构体/类型重合：与 2025 达达乐队 在“内存管理”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/proc.h:L44-L44`：struct trapframe
    代码片段：`struct trapframe {`
  - `include/proc/proc.h:L52-L52`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 结构体/类型重合：与 2025 达达乐队 在“同步机制”维度发现 2 个同名定义：sleeplock, spinlock。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/sleeplock.h:L4-L4`：struct sleeplock
    代码片段：`struct sleeplock {`
  - `include/lib/lock.h:L39-L39`：struct sleeplock
    代码片段：`typedef struct sleeplock {`
  - `kernel/spinlock.h:L2-L2`：struct spinlock
    代码片段：`struct spinlock {`
  - `include/lib/lock.h:L18-L18`：struct spinlock
    代码片段：`typedef struct spinlock {`
- 结构体/类型重合：与 2025 达达乐队 在“系统调用”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/proc.h:L44-L44`：struct trapframe
    代码片段：`struct trapframe {`
  - `include/proc/proc.h:L52-L52`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 文件路径重合：与 2025 达达乐队 在“设备驱动”维度出现同名文件源码路径 `kernel/uart.c` / `kernel/dev/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/dev/uart.c:L1-L4`：关键词命中
    代码片段：`/* * uart.c - 16550a UART 驱动（轮询 + 中断回显） * * 提供：`
- 文件路径重合：与 2025 达达乐队 在“设备驱动”维度出现同名文件源码路径 `kernel/virtio.h` / `include/dev/virtio.h`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `include/dev/virtio.h:L23-L27`：macro VIRTIO_MMIO_MAGIC_VALUE
    代码片段：`// virtio mmio control registers, mapped starting at 0x10001000. // from qemu virtio_mmio.h #define VIRTIO_MMIO_MAGIC_VALUE 0x000 // 0x74726976 #define VIRTIO_MMIO_VERSION 0x004...`
- 文件路径重合：与 2025 达达乐队 在“文件系统”维度出现同名文件源码路径 `kernel/fs.c` / `kernel/fs/fs.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs/fs.c:L4-L8`：关键词命中
    代码片段：`* 当前版本的 fs_init() 做了两件事： *   1) 初始化 buf cache，并从磁盘读取超级块到内存副本 sb； *   2) 运行一段 inode 读写自测逻辑（便于实验阶段验证 inode/data 分配链路）。 * * 注意：自测结束后会进入 while(1) 死循环。`
- 文件路径重合：与 2025 达达乐队 在“文件系统”维度出现同名文件源码路径 `kernel/file.c` / `kernel/fs/file.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/file.c:L105-L109`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `kernel/fs/file.c:L2-L6`：关键词命中
    代码片段：`* file.c - 打开文件（file_t）层实现 * * 该层负责把 inode/dir/设备读写统一为“打开文件实例”： *   - file_t 记录 read/write 权限、当前偏移 offset、引用计数 ref *   - ftable 是全局打开文件表，用自旋锁 lk_ftable 保护`
- 文件路径重合：与 2025 达达乐队 在“中断与异常”维度出现同名文件源码路径 `kernel/plic.c` / `kernel/dev/plic.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/dev/plic.c:L1-L4`：关键词命中
    代码片段：`/* * plic.c - PLIC（Platform-level interrupt controller） * * 负责外部中断（external interrupt）的分发：`
- 文件路径重合：与 2025 达达乐队 在“调度与任务管理”维度出现同名文件源码路径 `kernel/proc.c` / `kernel/proc/proc.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "riscv.h" #include "spinlock.h" #include "proc.h" #include "defs.h"`
  - `kernel/proc/proc.c:L4-L8`：关键词命中
    代码片段：`#include "dev/uart.h" #include "mem/vmem.h" #include "proc/cpu.h" #include "proc/initcode.h" #include "trap/trap.h"`

## 相似点

- 与 2025 达达乐队 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 达达乐队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
- 与 2025 达达乐队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs.h:L7-L11`：关键词命中
    代码片段：`// Disk layout: // [ boot block | super block | log | inode blocks | //                                          free bit map | data blocks] //`
- 与 2025 达达乐队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L31-L35`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
- 与 2025 达达乐队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/vm.c:L10-L14`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/kalloc.c:L36-L40`：关键词命中
    代码片段：`char *p; p = (char*)PGROUNDUP((uint64)pa_start); for(; p + PGSIZE <= (char*)pa_end; p += PGSIZE) kfree(p); }`
- 与 2025 达达乐队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "riscv.h" #include "spinlock.h" #include "proc.h" #include "defs.h"`
  - `kernel/proc.h:L22-L26`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2025 达达乐队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs.c:L15-L19`：关键词命中
    代码片段：`#include "param.h" #include "stat.h" #include "spinlock.h" #include "proc.h" #include "sleeplock.h"`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "riscv.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "fs.h"`
- 与 2025 达达乐队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap.c:L58-L62`：关键词命中
    代码片段：`kexit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h"`
- 与 2025 达达乐队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 达达乐队 的语言构成不同：待测作品为 {'markdown': 207, 'build': 195, 'c': 11886, 'asm': 276}，历史样本为 {'json': 20, 'make': 26, 'build': 287, 'markdown': 170, 'c': 10506, 'asm': 356}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：35
- 含证据关键结论数：35（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
