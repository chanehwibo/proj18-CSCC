# 2026 T2026104869910625 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026104869910625-752`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：183
- 代码/文本行数：55129
- 主要语言：c 51320 LOC, markdown 2143 LOC, build 869 LOC, asm 797 LOC

## 总览

2026 T2026104869910625 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 c: 51320 LOC, markdown: 2143 LOC, build: 869 LOC, asm: 797 LOC。仓库包含 183 个已扫描文件、约 55129 行可分析文本，当前抽取到 2268 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 5 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 7 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 3 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, hal/loongarch/Makefile, hal/riscv/Makefile, hsai/Makefile, kernel/Makefile。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`# 带有export的变量会在递归调用子目录的Makefile时传递下去`
  - `hal/loongarch/Makefile:L1-L3`：构建入口
    代码片段：`#编译.o文件到build/loongarch/kernel目录 #调用make all (对应loongarch)`
  - `hal/riscv/Makefile:L1-L3`：构建入口
    代码片段：`#编译.o文件到build/riscv/kernel目录 #调用make riscv`
  - `hsai/Makefile:L1-L3`：构建入口
    代码片段：`#编译.o文件到build/*/kernel目录，*可以为loongarch或riscv #调用make all (对应loongarch)或make riscv`
  - `kernel/Makefile:L1-L3`：构建入口
    代码片段：`#编译.o文件到build/*/kernel目录，*可以为loongarch或riscv #调用make all (对应loongarch)或make riscv`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn swtch, fn swtch。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/thread.c` | L1-L3 | 关键词命中 |
| `include/kernel/thread.h` | L29-L33 | 关键词命中 |
| `kernel/cpu.c` | L18-L22 | 关键词命中 |
| `hal/riscv/switch.S` | L7-L11 | fn swtch |
| `hal/loongarch/swtch.S` | L6-L10 | fn swtch |

### 关键代码片段

  - `kernel/thread.c:L1-L3`：关键词命中
    代码片段：`#include "thread.h" #include "pmem.h" #include "vmem.h"`
  - `include/kernel/thread.h:L29-L33`：关键词命中
    代码片段：`}; typedef struct thread { struct spinlock lock;`
  - `kernel/cpu.c:L18-L22`：关键词命中
    代码片段：`for (it = cpus; it < &cpus[NCPU]; it++) { it->proc = 0; it->intena = 0; it->noff = 0;`
  - `hal/riscv/switch.S:L7-L11`：fn swtch
    代码片段：`.globl swtch swtch: #14个寄存器 sd ra, 0(a0) sd sp, 8(a0)`

### 相关符号

`fn swtch` at `hal/riscv/switch.S:L7`、`fn swtch` at `hal/loongarch/swtch.S:L6`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：macro PGSIZE, macro PGSIZE, macro PTE_V, macro PTE_R, macro PTE_W。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/vma.c` | L22-L26 | 关键词命中 |
| `kernel/exec.c` | L31-L35 | 关键词命中 |
| `kernel/pmem.c` | L53-L57 | 关键词命中 |
| `include/kernel/virt.h` | L86-L90 | macro PGSIZE |
| `include/hal/riscv/riscv.h` | L341-L345 | macro PGSIZE |
| `include/hal/riscv/riscv.h` | L350-L354 | macro PTE_V |

### 关键代码片段

  - `kernel/vma.c:L22-L26`：关键词命中
    代码片段：`return NULL; } memset(vma, 0, PGSIZE); vma->type = NONE; vma->prev = vma->next = vma;`
  - `kernel/exec.c:L31-L35`：关键词命中
    代码片段：`void alloc_aux(uint64 *aux, uint64 atid, uint64 value); int loadaux(pgtbl_t pt, uint64 sp, uint64 stackbase, uint64 *aux); void debug_print_stack(pgtbl_t pagetable, uint64 sp, u...`
  - `kernel/pmem.c:L53-L57`：关键词命中
    代码片段：`uint64 get_buddy_addr(uint64 addr, int order) { uint64 block_size = PGSIZE << order; return addr ^ block_size; }`
  - `include/kernel/virt.h:L86-L90`：macro PGSIZE
    代码片段：`}; #define PGSIZE 4096 // #define BSIZE 1024`

### 相关符号

`macro PGSIZE` at `include/kernel/virt.h:L86`、`macro PGSIZE` at `include/hal/riscv/riscv.h:L341`、`macro PTE_V` at `include/hal/riscv/riscv.h:L350`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：macro SYS_write, macro SYS_getpid, macro SYS_eventfd2, macro SYS_epoll_create1, macro SYS_epoll_ctl。（置信度：medium）
  - 静态识别到 2 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/syscall.c` | L1-L5 | 关键词命中 |
| `hal/riscv/sbi.c` | L18-L22 | 关键词命中 |
| `include/kernel/syscall_ids.h` | L2-L6 | macro SYS_write |
| `include/kernel/syscall_ids.h` | L3-L7 | macro SYS_getpid |
| `include/kernel/syscall_ids.h` | L4-L8 | macro SYS_eventfd2 |
| `include/kernel/syscall_ids.h` | L1-L4 | macro __SYSCALL_IDS_H__ |

### 关键代码片段

  - `kernel/syscall.c:L1-L5`：关键词命中
    代码片段：`#include "types.h" #include "print.h" #include "trap.h" //这个必须要再hsai_trap.h前。syscall(struct trapframe *trapframe)参数表中的trapframe对外部不可见，所以先声明trapframe #include "hsai_trap.h" #incl...`
  - `hal/riscv/sbi.c:L18-L22`：关键词命中
    代码片段：`register uint64 a2 asm("a2") = arg2; register uint64 a7 asm("a7") = which; asm volatile("ecall" : "=r"(a0) : "r"(a0), "r"(a1), "r"(a2), "r"(a7)`
  - `include/kernel/syscall_ids.h:L2-L6`：macro SYS_write
    代码片段：`#define __SYSCALL_IDS_H__ #define SYS_write   64 #define SYS_getpid  172 #define SYS_eventfd2 19`
  - `include/kernel/syscall_ids.h:L3-L7`：macro SYS_getpid
    代码片段：`#define SYS_write   64 #define SYS_getpid  172 #define SYS_eventfd2 19 #define SYS_epoll_create1 20`

### 相关符号

`macro SYS_write` at `include/kernel/syscall_ids.h:L2`、`macro SYS_getpid` at `include/kernel/syscall_ids.h:L3`、`macro SYS_eventfd2` at `include/kernel/syscall_ids.h:L4`、`macro __SYSCALL_IDS_H__` at `include/kernel/syscall_ids.h:L1`、`macro _USYSCALL_H_` at `user/include/usercall.h:L1`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct ext4_block_devices, fn ext4_device_register, fn ext4_device_unregister, fn ext4_device_unregister_all, fn ext4_is_dots。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/fs/fs.c` | L201-L205 | 关键词命中 |
| `kernel/fs/ext4.c` | L146-L150 | 关键词命中 |
| `kernel/fs/file.c` | L694-L698 | 关键词命中 |
| `kernel/fs/ext4.c` | L73-L77 | struct ext4_block_devices |
| `kernel/fs/ext4.c` | L88-L92 | fn ext4_device_register |
| `kernel/fs/ext4.c` | L110-L114 | fn ext4_device_unregister |

### 关键代码片段

  - `kernel/fs/fs.c:L201-L205`：关键词命中
    代码片段：`void dir_init(void) { struct inode *ip; if ((ip=namei("/dev/null")) == NULL) vfs_ext4_mknod("/dev/null", T_CHR, DEVNULL);`
  - `kernel/fs/ext4.c:L146-L150`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `kernel/fs/file.c:L694-L698`：关键词命中
    代码片段：`*/ int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `kernel/fs/ext4.c:L73-L77`：struct ext4_block_devices
    代码片段：`/**@brief   Block devices descriptor.*/ struct ext4_block_devices { /**@brief   Block device name.*/`

### 相关符号

`struct ext4_block_devices` at `kernel/fs/ext4.c:L73`、`fn ext4_device_register` at `kernel/fs/ext4.c:L88`、`fn ext4_device_unregister` at `kernel/fs/ext4.c:L110`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/cpu.c` | L1-L4 | 关键词命中 |
| `kernel/test.c` | L5-L9 | 关键词命中 |
| `kernel/fs/fs.c` | L40-L44 | 关键词命中 |

### 关键代码片段

  - `kernel/cpu.c:L1-L4`：关键词命中
    代码片段：`#include "cpu.h" #include "spinlock.h" #ifdef RISCV #include "riscv.h"`
  - `kernel/test.c:L5-L9`：关键词命中
    代码片段：`#include "vmem.h" #include "string.h" #include "spinlock.h" #include "cpu.h" #if defined RISCV`
  - `kernel/fs/fs.c:L40-L44`：关键词命中
    代码片段：`* 该结构体用于保护对文件系统表的访问。 */ struct spinlock fs_table_lock; /**`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn timer_get_time, fn timer_get_ntime, enum Exception, struct trapframe, macro ISR。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/timer.c` | L1-L5 | 关键词命中 |
| `kernel/test.c` | L500-L504 | 关键词命中 |
| `kernel/vmem.c` | L35-L39 | 关键词命中 |
| `kernel/timer.c` | L297-L301 | fn timer_get_time |
| `kernel/timer.c` | L305-L309 | fn timer_get_ntime |
| `include/hsai/trap.h` | L13-L17 | enum Exception |

### 关键代码片段

  - `kernel/timer.c:L1-L5`：关键词命中
    代码片段：`#include "types.h" #include "spinlock.h" #include "timer.h" #include "print.h" #include "process.h"`
  - `kernel/test.c:L500-L504`：关键词命中
    代码片段：`void test_interrupt_nesting(void) { printf("Testing interrupt nesting...\n"); int old_intr = intr_get();`
  - `kernel/vmem.c:L35-L39`：关键词命中
    代码片段：`mappages(kernel_pagetable, VIRTIO0, VIRTIO0, PGSIZE, PTE_R | PTE_W); /*PLIC映射*/ mappages(kernel_pagetable, PLIC, PLIC, 0x400000, PTE_R | PTE_W);`
  - `kernel/timer.c:L297-L301`：fn timer_get_time
    代码片段：`timeval_t timer_get_time(){ timeval_t tv; uint64 clk = r_time();`

### 相关符号

`fn timer_get_time` at `kernel/timer.c:L297`、`fn timer_get_ntime` at `kernel/timer.c:L305`、`enum Exception` at `include/hsai/trap.h:L13`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct pci_msix, struct virtio_pci_hw, struct virtio_pci_cap, macro VIRTIO_PCI_CAP_COMMON_CFG, macro VIRTIO_PCI_CAP_NOTIFY_CFG。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/console.c` | L1-L4 | 关键词命中 |
| `kernel/driver/riscv/virt.c` | L1-L3 | 关键词命中 |
| `kernel/driver/loongarch/pci.c` | L6-L10 | 关键词命中 |
| `kernel/driver/loongarch/virtio_pci.c` | L13-L17 | struct pci_msix |
| `kernel/driver/loongarch/virtio_pci.c` | L21-L25 | struct virtio_pci_hw |
| `kernel/driver/loongarch/virtio_pci.c` | L39-L43 | struct virtio_pci_cap |

### 关键代码片段

  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/driver/riscv/virt.c:L1-L3`：关键词命中
    代码片段：`// virt.c - 最小化RISC-V VirtIO磁盘驱动 #include "virt.h" #include "print.h"`
  - `kernel/driver/loongarch/pci.c:L6-L10`：关键词命中
    代码片段：`#include "print.h" //< 在virtio_disk中 extern unsigned char bus1; extern unsigned char device1;`
  - `kernel/driver/loongarch/virtio_pci.c:L13-L17`：struct pci_msix
    代码片段：`/*pci.h*/ struct pci_msix { int bar_num;            // bar number int irq_num;            // interrupt number`

### 相关符号

`struct pci_msix` at `kernel/driver/loongarch/virtio_pci.c:L13`、`struct virtio_pci_hw` at `kernel/driver/loongarch/virtio_pci.c:L21`、`struct virtio_pci_cap` at `kernel/driver/loongarch/virtio_pci.c:L39`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：15
- 含证据关键结论数：15（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
