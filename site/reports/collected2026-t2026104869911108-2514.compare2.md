# 2026 T2026104869911108 比较报告

- 对比历史仓库：2022 Maturin
- 生成时间：2026-07-02T05:01:25.315953+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 Maturin（来源：赛事历史作品）：画像相似度 score=9.29；同属 ucore-variant 风格; 语言构成相似度 0.93; OS 维度重合度 1.00; 代码规模接近度 0.42

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 Maturin 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `arceos/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `kernel/src/console.rs:L21-L25`：关键词命中
    代码片段：`#[macro_export] macro_rules! info { () => ($crate::console::info(core::format_args!("\n"));); ($($arg:tt)*) => { $crate::console::info(core::format_args!($($arg)*));`
  - `kernel/src/drivers/block/mod.rs:L2-L6`：关键词命中
    代码片段：`use alloc::sync::Arc; mod virtio_block; mod block_device; pub use virtio_block::VirtIOBlock;`
- 与 2022 Maturin 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/rust-fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `arceos/modules/axfs/src/fs/fatfs.rs:L34-L38`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
  - `rust-fatfs/src/fs.rs:L21-L25`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `kernel/src/file/mod.rs:L9-L13`：关键词命中
    代码片段：`mod socket; mod stdio; mod vfs; use crate::timer::TimeSpec;`
- 与 2022 Maturin 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `arceos/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
  - `kernel/src/trap/mod.rs:L1-L5`：关键词命中
    代码片段：`//! 中断异常处理 //! //! 所有中断和异常的入口在 trap.S 中的 __alltraps，它会在保存上下文信息后跳转到本文件中的 trap_handler 函数 //! //! 在这个模块中，程序的执行流不一定正常。主要有三种可能：`
  - `kernel/src/trap/trap.S:L14-L18`：关键词命中
    代码片段：`bgtz sp, __user_trap_start # here is prework before kernel trap entry # in "__real_trap_entry", tp will be replaced by 34*8(sp), where we assumed is saved by __restore before. #...`
- 与 2022 Maturin 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `core/src/mm.rs:L26-L30`：关键词命中
    代码片段：`pub fn copy_from_kernel(aspace: &mut AddrSpace) -> AxResult { if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use se...`
  - `arceos/modules/axmm/src/backend/alloc.rs:L2-L6`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PhysAddr, VirtAddr};`
  - `kernel/src/memory/mod.rs:L4-L8`：关键词命中
    代码片段：`mod allocator; mod areas; mod page_table; mod user; mod vmm;`
  - `kernel/src/memory/vmm.rs:L4-L8`：关键词命中
    代码片段：`addr_to_page_id, align_down, align_up, cross_page, get_phys_memory_regions, page_count, page_id_to_addr, virt_to_phys, DiffSet, CutSet, PTEFlags, PageTable, PmAreaLazy, VirtAddr...`
- 与 2022 Maturin 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. use core::{`
  - `src/mm.rs:L30-L34`：关键词命中
    代码片段：`"{} ({:?}): segmentation fault at {:#x}, exit!", curr.id_name(), curr.task_ext().thread, vaddr );`
  - `kernel/src/task/mod.rs:L9-L13`：关键词命中
    代码片段：`mod cpu_local; mod kernel_stack; mod scheduler; mod switch; mod task;`
  - `kernel/src/task/task.rs:L25-L29`：关键词命中
    代码片段：`/// /// 目前来说，TCB外层可能是调度器或者 CpuLocal： /// 1. 如果它在调度器里，则 Scheduler 内部不会修改它，且从 Scheduler 里取出或者放入 TCB 是由调度器外部的 Mutex 保护的； /// 2. 如果它在 CpuLocal 里，则同时只会有一个核可以访问它，也不会冲突。 pub struct Tas...`
- 与 2022 Maturin 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `core/src/task.rs:L4-L8`：关键词命中
    代码片段：`alloc::Layout, cell::RefCell, sync::atomic::{AtomicUsize, Ordering}, time::Duration, };`
  - `arceos/modules/axfs/src/root.rs:L7-L11`：关键词命中
    代码片段：`use axfs_vfs::{VfsNodeAttr, VfsNodeOps, VfsNodeRef, VfsNodeType, VfsOps, VfsResult}; use axns::{ResArc, def_resource}; use axsync::Mutex; use lazyinit::LazyInit; use spin::RwLock;`
  - `rust-fatfs/src/fs.rs:L8-L12`：关键词命中
    代码片段：`use core::marker::PhantomData; use core::u32; use lock::Mutex; use crate::boot_sector::{format_boot_sector, BiosParameterBlock, BootSector};`
  - `kernel/src/console.rs:L36-L40`：关键词命中
    代码片段：`} /// 打印格式字串，使用与 print 不同的 Mutex 锁 #[macro_export] macro_rules! eprint {`
- 与 2022 Maturin 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_api::*;`
  - `vendor/syscalls/src/syscall/arm.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %r5 // // %r7 is used for the syscall number. // // %r0 is reused for the syscall return value.`
  - `kernel/src/syscall/fs.rs:L1-L5`：关键词命中
    代码片段：`//! 与读写、文件相关的系统调用 //! //! 注意获取 current_task 的时候都使用了 unwrap()，这意味着默认只有用户程序才会调用 syscall 模块进行操作。 //! 如果内核态异常中断需要处理， trap 只能利用其他模块，如 MemorySet::handle_kernel_page_fault 等`
  - `kernel/src/syscall/mod.rs:L43-L47`：关键词命中
    代码片段：`/// 处理系统调用 pub fn syscall(syscall_id: usize, args: [usize; 6]) -> isize { let syscall_id = if let Ok(id) = SyscallNo::try_from(syscall_id) { id`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2022 Maturin 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：crate, defaulttimeprovider, time, timeprovider, use。（置信度：high）
  证据：
  - `vendor/rust-fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `rust-fatfs/src/fs.rs:L21-L25`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
- 与 2022 Maturin 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, debug, derive, enum, eq, fat12, fattype。（置信度：high）
  证据：
  - `vendor/rust-fatfs/src/fs.rs:L28-L32`：enum FatType
    代码片段：`/// 'FatType' values are based on the size of File Allocation Table entry. #[derive(Copy, Clone, Eq, PartialEq, Debug)] pub enum FatType { /// 12 bits per FAT entry Fat12,`
  - `rust-fatfs/src/fs.rs:L29-L33`：enum FatType
    代码片段：`/// 'FatType' values are based on the size of File Allocation Table entry. #[derive(Copy, Clone, Eq, PartialEq, Debug)] pub enum FatType { /// 12 bits per FAT entry Fat12,`
- 函数/符号名重合：与 2022 Maturin 在“文件系统”维度发现 4 个同名定义：new, read, write, seek。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `api/src/file/fs.rs:L19-L19`：fn new
    代码片段：`pub fn new(inner: axfs::fops::File, path: String) -> Self {`
  - `rust-fatfs/src/dir.rs:L114-L114`：fn new
    代码片段：`pub(crate) fn new(stream: DirRawStream<'a, IO, TP, OCC>, fs: &'a FileSystem<IO, TP, OCC>) -> Self {`
  - `api/src/file/fs.rs:L38-L38`：fn read
    代码片段：`fn read(&self, buf: &mut [u8]) -> LinuxResult<usize> {`
  - `rust-fatfs/src/dir.rs:L60-L60`：fn read
    代码片段：`fn read(&mut self, buf: &mut [u8]) -> Result<usize, Self::Error> {`
- 函数/符号名重合：与 2022 Maturin 在“调度与任务管理”维度发现 1 个同名定义：process。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `api/src/imp/task/pidfd.rs:L38-L38`：fn process
    代码片段：`pub fn process(&self) -> Arc<Process> {`
  - `rust-fatfs/src/dir.rs:L880-L880`：fn process
    代码片段：`fn process(&mut self, data: &DirLfnEntryData) {`
- 函数/符号名重合：与 2022 Maturin 在“同步机制”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `api/src/imp/fs/eventpoll.rs:L143-L143`：fn new
    代码片段：`fn new(clock_id: __kernel_clockid_t) -> Self {`
  - `rust-fatfs/src/fs.rs:L248-L248`：fn new
    代码片段：`pub fn new() -> Self {`
- 结构体/类型重合：与 2022 Maturin 在“文件系统”维度发现 1 个同名定义：File。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `api/src/file/fs.rs:L13-L13`：struct File
    代码片段：`pub struct File {`
  - `rust-fatfs/src/file.rs:L15-L15`：struct File
    代码片段：`pub struct File<'a, IO: ReadWriteSeek, TP, OCC> {`
- 文件路径重合：与 2022 Maturin 在“设备驱动”维度出现同名文件源码路径 `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs` / `kernel/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs:L6-L10`：关键词命中
    代码片段：`const MAX_RW_SIZE: usize = 256; /// Writes a byte to the console. pub fn putchar(c: u8) { sbi_rt::console_write_byte(c);`
  - `kernel/src/console.rs:L21-L25`：关键词命中
    代码片段：`#[macro_export] macro_rules! info { () => ($crate::console::info(core::format_args!("\n"));); ($($arg:tt)*) => { $crate::console::info(core::format_args!($($arg)*));`
- 文件路径重合：与 2022 Maturin 在“文件系统”维度出现同名文件源码路径 `vendor/rust-fatfs/src/fs.rs` / `rust-fatfs/src/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `vendor/rust-fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `rust-fatfs/src/fs.rs:L21-L25`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
- 文件路径重合：与 2022 Maturin 在“中断与异常”维度出现同名文件源码路径 `arceos/modules/axhal/src/arch/riscv/trap.S` / `kernel/src/trap/trap.S`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `arceos/modules/axhal/src/arch/riscv/trap.S:L53-L57`：关键词命中
    代码片段：`.global trap_vector_base trap_vector_base: // sscratch == 0: trap from S mode // sscratch != 0: trap from U mode csrrw   sp, sscratch, sp            // swap sscratch and sp`
  - `kernel/src/trap/trap.S:L14-L18`：关键词命中
    代码片段：`bgtz sp, __user_trap_start # here is prework before kernel trap entry # in "__real_trap_entry", tp will be replaced by 34*8(sp), where we assumed is saved by __restore before. #...`
- 文件路径重合：与 2022 Maturin 在“调度与任务管理”维度出现同名文件源码路径 `core/src/task.rs` / `kernel/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. use core::{`
  - `kernel/src/task/task.rs:L25-L29`：关键词命中
    代码片段：`/// /// 目前来说，TCB外层可能是调度器或者 CpuLocal： /// 1. 如果它在调度器里，则 Scheduler 内部不会修改它，且从 Scheduler 里取出或者放入 TCB 是由调度器外部的 Mutex 保护的； /// 2. 如果它在 CpuLocal 里，则同时只会有一个核可以访问它，也不会冲突。 pub struct Tas...`

## 相似点

- 与 2022 Maturin 同属 ucore-variant 风格。（置信度：medium）
- 与 2022 Maturin 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `arceos/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 与 2022 Maturin 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/rust-fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `arceos/modules/axfs/src/fs/fatfs.rs:L34-L38`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
- 与 2022 Maturin 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `arceos/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
- 与 2022 Maturin 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `core/src/mm.rs:L26-L30`：关键词命中
    代码片段：`pub fn copy_from_kernel(aspace: &mut AddrSpace) -> AxResult { if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use se...`
  - `arceos/modules/axmm/src/backend/alloc.rs:L2-L6`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PhysAddr, VirtAddr};`
- 与 2022 Maturin 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. use core::{`
  - `src/mm.rs:L30-L34`：关键词命中
    代码片段：`"{} ({:?}): segmentation fault at {:#x}, exit!", curr.id_name(), curr.task_ext().thread, vaddr );`
- 与 2022 Maturin 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `core/src/task.rs:L4-L8`：关键词命中
    代码片段：`alloc::Layout, cell::RefCell, sync::atomic::{AtomicUsize, Ordering}, time::Duration, };`
  - `arceos/modules/axfs/src/root.rs:L7-L11`：关键词命中
    代码片段：`use axfs_vfs::{VfsNodeAttr, VfsNodeOps, VfsNodeRef, VfsNodeType, VfsOps, VfsResult}; use axns::{ResArc, def_resource}; use axsync::Mutex; use lazyinit::LazyInit; use spin::RwLock;`
- 与 2022 Maturin 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_api::*;`
  - `vendor/syscalls/src/syscall/arm.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %r5 // // %r7 is used for the syscall number. // // %r0 is reused for the syscall return value.`
- 与 2022 Maturin 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 Maturin 的语言构成不同：待测作品为 {'toml': 882, 'json': 241, 'markdown': 5088, 'build': 3073, 'rust': 115957, 'c': 29425, 'asm': 935, 'make': 1282, 'text': 81, 'yaml': 36}，历史样本为 {'json': 30, 'build': 508, 'markdown': 1713, 'toml': 9, 'yaml': 18, 'text': 394, 'rust': 27639, 'c': 9309, 'make': 29, 'asm': 177}。（置信度：medium）

## 可能创新点

- 待测作品包含 316 个历史样本 2022 Maturin 中未见的独特函数/类型定义（如 `sys_bind`, `sys_clock_getres`, `copy_fd_set`, `sys_faccessat`, `read_optional_offset`, `sys_nanosleep`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `api/src/imp/net.rs:L126-L126`：fn sys_bind
    代码片段：`pub fn sys_bind(fd: c_int, addr: UserConstPtr<sockaddr>, addrlen: socklen_t) -> LinuxResult<isize> {`
  - `api/src/imp/time.rs:L58-L58`：fn sys_clock_getres
    代码片段：`pub fn sys_clock_getres(clock_id: __kernel_clockid_t, ts: UserPtr<timespec>) -> LinuxResult<isize> {`
- 待测作品代码规模（157,000 LOC）显著大于历史样本 2022 Maturin（39,826 LOC），可能包含更多功能模块或更完整的实现，但也可能包含非内核代码，需人工确认。（置信度：low）

## 附录：核验摘要

- 关键结论数：26
- 含证据关键结论数：25（96.2%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
