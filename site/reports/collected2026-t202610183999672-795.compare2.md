# 2026 T202610183999672 比较报告

- 对比历史仓库：2022 代码掐架队
- 生成时间：2026-07-02T04:59:32.946212+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 代码掐架队（来源：赛事历史作品）：画像相似度 score=9.23；同属 rcore-variant 风格; 语言构成相似度 0.94; OS 维度重合度 1.00; 代码规模接近度 0.35

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 代码掐架队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console.rs:L5-L9`：关键词命中
    代码片段：`use log::{self, Level, LevelFilter, Log, Metadata, Record}; // la64: console_putchar 直接写 UART，无 SBI 序列化保护，因此需要 irq-save // 临界区确保整条 print 输出原子。rv64 虽 SBI ecall 有单字符原子性，但全局 // 关中断...`
  - `os/src/drivers/mod.rs:L1-L5`：关键词命中
    代码片段：`pub mod block; pub mod net; pub mod serial; pub use block::BLOCK_DEVICES;`
  - `codes/os/src/console.rs:L13-L17`：关键词命中
    代码片段：`} static CONSOLE: Mutex<ConsoleInner> = Mutex::new(ConsoleInner {}); impl Write for Stdout {`
  - `codes/os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2022 代码掐架队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L23-L27`：关键词命中
    代码片段：`// Xein add this pub mod dirent; // file_descriptor module removed — migrated to vfs::File mod inode; mod timestamp;`
  - `os/src/fs/poll.rs:L3-L7`：关键词命中
    代码片段：`use crate::{ config::PAGE_SIZE, fs::vfs::{event::EPollEvent, FdTable, File, PollWaitQueue}, mm::{UserPtr, UserSlice}, net::config::NET_INTERFACE,`
  - `codes/os/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`mod pipe; pub mod stdio; mod inode; mod mount; pub mod finfo;`
  - `codes/os/src/fs/finfo.rs:L63-L67`：关键词命中
    代码片段：`} pub fn new(name:&str, inode:usize, offset:isize, reclen: u16, d_type: u8)->Self{ let mut dirent = Self{ d_ino:inode,`
- 与 2022 代码掐架队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/hal/arch/riscv/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V trap 分发和返回路径。 //! //! 处理 syscall、缺页、设备中断、timer interrupt，并在返回用户态前完成信号`
  - `os/src/hal/arch/riscv/trap/context.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V trap context 和用户信号上下文布局。 //! //! 该文件定义从 trap 入口保存的通用寄存器、状态寄存器和 signal frame 需要的`
  - `codes/os/src/trap/mod.rs:L7-L11`：关键词命中
    代码片段：`use crate::syscall::{syscall, test}; use crate::task::*; use crate::timer::set_next_trigger; use riscv::register::{ mtvec::TrapMode,`
  - `codes/os/src/sbi.rs:L29-L33`：关键词命中
    代码片段：`} pub fn set_timer(timer: usize) { sbi_call(SBI_SET_TIMER, timer, 0, 0); }`
- 与 2022 代码掐架队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L7-L11`：关键词命中
    代码片段：`//! # TLB //! //! 任何修改 PTE 的路径必须立即刷新对应 TLB，或明确使用 //! '*_no_flush' 批量接口并在批量结束后调用 'flush_tlb()'。 //!`
  - `os/src/mm/vma.rs:L2-L6`：关键词命中
    代码片段：`//! //! 'Vma' 保存地址区间、权限、mmap flag、文件后端以及页帧状态表，并提供单页映射、 //! 解除映射、fork 继承、CoW、范围伸缩和 OOM 回收等操作。 //! //! # Semantics`
  - `codes/os/src/mm/mod.rs:L1-L3`：关键词命中
    代码片段：`mod heap_allocator; mod address; mod frame_allocator;`
  - `codes/os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{PageTable, PageTableEntry, PTEFlags}; use super::{VirtPageNum, VirtAddr, PhysPageNum, PhysAddr}; use super::{FrameTracker, frame_alloc, frame_add_ref, enquire_refcou...`
- 与 2022 代码掐架队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L28-L32`：关键词命中
    代码片段：`pub mod signal; mod sleep; mod task; pub mod threads;`
  - `os/src/task/pid.rs:L12-L16`：关键词命中
    代码片段：`use spin::Mutex; /// Matches the '/proc/sys/kernel/pid_max' value exposed by procfs. pub const DEFAULT_PID_MAX: usize = 32_768; const RESERVED_PID_REUSE_FLOOR: usize = 300;`
  - `codes/os/src/task/mod.rs:L6-L10`：关键词命中
    代码片段：`mod info; mod switch; mod task; mod resource; pub use resource::RLimit;`
  - `codes/os/src/task/info.rs:L87-L91`：关键词命中
    代码片段：`pub const RUSAGE_SELF:isize = 0; /* The calling process.  */ pub const RUSAGE_CHILDREN:isize = -1; /* All of its terminated child processes.  */ pub const RUSAGE_THREAD:isize =...`
- 与 2022 代码掐架队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/vfs/posix_lock.rs:L1-L3`：关键词命中
    代码片段：`//! Sharded POSIX record lock manager with blocking support. //! //! Uses 53 shards keyed by '(dev_id, inode_id)'.  Each shard maps a 'LockKey'`
  - `os/src/timer.rs:L432-L436`：关键词命中
    代码片段：`// ───────────────────────────────────────────────────────── use core::sync::atomic::{AtomicU64, Ordering as AtomicOrdering}; pub trait TimeSource {`
  - `codes/os/src/main.rs:L79-L83`：关键词命中
    代码片段：`lazy_static! { static ref CORE2_FLAG: Arc<Mutex<Core2flag>> = Arc::new(Mutex::new(Core2flag { is_in: false })); }`
  - `codes/os/src/console.rs:L1-L5`：关键词命中
    代码片段：`use crate::sbi::console_putchar; use core::fmt::{self, Write}; use spin::Mutex; struct Stdout;`
- 与 2022 代码掐架队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L9-L13`：关键词命中
    代码片段：`UserPtrMut, UserSlice, VirtAddr, }; use crate::syscall::utils::wait_io_core; use crate::task::{ current_task, current_user_token, find_process_by_pid, find_task_by_tid,`
  - `os/src/syscall/mod.rs:L17-L21`：关键词命中
    代码片段：`sys_timerfd_create, sys_timerfd_gettime, sys_timerfd_settime, TimerFdSpec, }; use crate::net::syscall::*; use core::convert::TryFrom; use flock::*;`
  - `codes/os/src/syscall/fs.rs:L961-L965`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: isize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { if dirfd == A...`
  - `codes/os/src/syscall/mod.rs:L86-L90`：关键词命中
    代码片段：`// println!("test: run sys_getppid 1000000 times, start {:?}",start); for _ in 0..1000000{ syscall(SYSCALL_GETPPID,[0,0,0,0,0,0]); // unsafe{ //     asm!(`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2022 代码掐架队 在“设备驱动”维度发现片段级代码相似度 0.59 （token=0.50, structure=0.75）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：args, arguments, fmt, fn, unwrap, write_fmt。（置信度：medium）
  证据：
  - `os/src/console.rs:L28-L32`：fn print
    代码片段：`} pub fn print(args: fmt::Arguments) { let irq_state = local_irq_save(); KernelOutput.write_fmt(args).unwrap();`
  - `codes/os/src/console.rs:L8-L12`：fn puts
    代码片段：`impl ConsoleInner { fn puts(&self, args: fmt::Arguments) { Stdout.write_fmt(args).unwrap(); }`
- 与 2022 代码掐架队 在“中断与异常”维度发现片段级代码相似度 0.58 （token=0.54, structure=0.67）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clock_freq, fn, let, pub, sec, ticks。（置信度：medium）
  证据：
  - `os/src/timer.rs:L45-L49`：fn ticks_to_ns
    代码片段：`/// multiplication so the product never overflows u64. #[inline] pub fn ticks_to_ns(ticks: u64) -> u64 { let freq = clock_freq(); let sec = ticks / freq;`
  - `codes/os/src/timer.rs:L20-L24`：fn get_timeval
    代码片段：`} pub fn get_timeval() -> TimeVal { let ticks = get_time(); let sec = ticks/CLOCK_FREQ;`
- 与 2022 代码掐架队 在“调度与任务管理”维度发现片段级代码相似度 0.68 （token=0.58, structure=0.88）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, impl, new, pub, self。（置信度：medium）
  证据：
  - `os/src/task/elf.rs:L79-L83`：fn new
    代码片段：`impl AuxvEntry { /// 构造一个 auxv 键值对。 pub fn new(auxv_type: AuxvType, auxv_val: usize) -> Self { Self { auxv_type,`
  - `codes/os/src/task/id.rs:L15-L19`：fn new
    代码片段：`impl RecycleAllocator { pub fn new() -> Self { RecycleAllocator { current: 0,`
- 函数/符号名重合：与 2022 代码掐架队 在“系统调用”维度发现 4 个同名定义：syscall, sys_dup, sys_open, sys_close。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `user/src/syscall.rs:L98-L98`：fn syscall
    代码片段：`fn syscall(id: usize, args: [usize; 3]) -> isize {`
  - `codes/user/src/syscall.rs:L69-L69`：fn syscall
    代码片段：`fn syscall(id: usize, args: [usize; 3]) -> isize {`
  - `user/src/syscall.rs:L167-L167`：fn sys_dup
    代码片段：`pub fn sys_dup(fd: usize) -> isize {`
  - `codes/user/src/syscall.rs:L82-L82`：fn sys_dup
    代码片段：`pub fn sys_dup(fd: usize) -> isize {`
- 函数/符号名重合：与 2022 代码掐架队 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `user/src/console.rs:L10-L10`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `codes/user/src/console.rs:L11-L11`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 函数/符号名重合：与 2022 代码掐架队 在“内存管理”维度发现 1 个同名定义：handle_alloc_error。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `user/src/lib.rs:L33-L33`：fn handle_alloc_error
    代码片段：`pub fn handle_alloc_error(layout: core::alloc::Layout) -> ! {`
  - `codes/user/src/lib.rs:L37-L37`：fn handle_alloc_error
    代码片段：`pub fn handle_alloc_error(layout: core::alloc::Layout) -> ! {`
- 函数/符号名重合：与 2022 代码掐架队 在“调度与任务管理”维度发现 2 个同名定义：sys_yield, yield_。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `user/src/syscall.rs:L216-L216`：fn sys_yield
    代码片段：`pub fn sys_yield() -> isize {`
  - `codes/user/src/syscall.rs:L141-L141`：fn sys_yield
    代码片段：`pub fn sys_yield() -> isize {`
  - `user/src/usr_call.rs:L38-L38`：fn yield_
    代码片段：`pub fn yield_() -> isize {`
  - `codes/user/src/lib.rs:L171-L171`：fn yield_
    代码片段：`pub fn yield_() -> isize { sys_yield() }`
- 结构体/类型重合：与 2022 代码掐架队 在“设备驱动”维度发现 1 个同名定义：Stdout。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `user/src/console.rs:L7-L7`：struct Stdout
    代码片段：`struct Stdout;`
  - `codes/user/src/console.rs:L8-L8`：struct Stdout
    代码片段：`struct Stdout;`
- 文件路径重合：与 2022 代码掐架队 在“设备驱动”维度出现同名文件源码路径 `os/src/console.rs` / `codes/os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/console.rs:L5-L9`：关键词命中
    代码片段：`use log::{self, Level, LevelFilter, Log, Metadata, Record}; // la64: console_putchar 直接写 UART，无 SBI 序列化保护，因此需要 irq-save // 临界区确保整条 print 输出原子。rv64 虽 SBI ecall 有单字符原子性，但全局 // 关中断...`
  - `codes/os/src/console.rs:L13-L17`：关键词命中
    代码片段：`} static CONSOLE: Mutex<ConsoleInner> = Mutex::new(ConsoleInner {}); impl Write for Stdout {`
- 文件路径重合：与 2022 代码掐架队 在“中断与异常”维度出现同名文件源码路径 `os/src/timer.rs` / `codes/os/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/timer.rs:L30-L34`：fn clock_freq
    代码片段：`/// Hardware counter frequency in Hz. #[inline(always)] pub fn clock_freq() -> u64 { get_clock_freq() as u64 }`
  - `codes/os/src/timer.rs:L9-L13`：fn get_time
    代码片段：`pub const NSEC_PER_SEC: usize = 1000_000_000; pub fn get_time() -> usize { let mut time:usize = 0; unsafe{`
- 文件路径重合：与 2022 代码掐架队 在“内存管理”维度出现同名文件源码路径 `os/src/mm/vma.rs` / `codes/os/src/mm/vma.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/mm/vma.rs:L2-L6`：关键词命中
    代码片段：`//! //! 'Vma' 保存地址区间、权限、mmap flag、文件后端以及页帧状态表，并提供单页映射、 //! 解除映射、fork 继承、CoW、范围伸缩和 OOM 回收等操作。 //! //! # Semantics`
  - `codes/os/src/mm/vma.rs:L7-L11`：struct MmapProts
    代码片段：`bitflags! { pub struct MmapProts: usize { const PROT_NONE = 0; const PROT_READ = 1;`
- 文件路径重合：与 2022 代码掐架队 在“系统调用”维度出现同名文件源码路径 `os/src/syscall/fs.rs` / `codes/os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L9-L13`：关键词命中
    代码片段：`UserPtrMut, UserSlice, VirtAddr, }; use crate::syscall::utils::wait_io_core; use crate::task::{ current_task, current_user_token, find_process_by_pid, find_task_by_tid,`
  - `codes/os/src/syscall/fs.rs:L961-L965`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: isize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { if dirfd == A...`

## 相似点

- 与 2022 代码掐架队 同属 rcore-variant 风格。（置信度：medium）
- 与 2022 代码掐架队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console.rs:L5-L9`：关键词命中
    代码片段：`use log::{self, Level, LevelFilter, Log, Metadata, Record}; // la64: console_putchar 直接写 UART，无 SBI 序列化保护，因此需要 irq-save // 临界区确保整条 print 输出原子。rv64 虽 SBI ecall 有单字符原子性，但全局 // 关中断...`
  - `os/src/drivers/mod.rs:L1-L5`：关键词命中
    代码片段：`pub mod block; pub mod net; pub mod serial; pub use block::BLOCK_DEVICES;`
- 与 2022 代码掐架队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L23-L27`：关键词命中
    代码片段：`// Xein add this pub mod dirent; // file_descriptor module removed — migrated to vfs::File mod inode; mod timestamp;`
  - `os/src/fs/poll.rs:L3-L7`：关键词命中
    代码片段：`use crate::{ config::PAGE_SIZE, fs::vfs::{event::EPollEvent, FdTable, File, PollWaitQueue}, mm::{UserPtr, UserSlice}, net::config::NET_INTERFACE,`
- 与 2022 代码掐架队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/hal/arch/riscv/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V trap 分发和返回路径。 //! //! 处理 syscall、缺页、设备中断、timer interrupt，并在返回用户态前完成信号`
  - `os/src/hal/arch/riscv/trap/context.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V trap context 和用户信号上下文布局。 //! //! 该文件定义从 trap 入口保存的通用寄存器、状态寄存器和 signal frame 需要的`
- 与 2022 代码掐架队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L7-L11`：关键词命中
    代码片段：`//! # TLB //! //! 任何修改 PTE 的路径必须立即刷新对应 TLB，或明确使用 //! '*_no_flush' 批量接口并在批量结束后调用 'flush_tlb()'。 //!`
  - `os/src/mm/vma.rs:L2-L6`：关键词命中
    代码片段：`//! //! 'Vma' 保存地址区间、权限、mmap flag、文件后端以及页帧状态表，并提供单页映射、 //! 解除映射、fork 继承、CoW、范围伸缩和 OOM 回收等操作。 //! //! # Semantics`
- 与 2022 代码掐架队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L28-L32`：关键词命中
    代码片段：`pub mod signal; mod sleep; mod task; pub mod threads;`
  - `os/src/task/pid.rs:L12-L16`：关键词命中
    代码片段：`use spin::Mutex; /// Matches the '/proc/sys/kernel/pid_max' value exposed by procfs. pub const DEFAULT_PID_MAX: usize = 32_768; const RESERVED_PID_REUSE_FLOOR: usize = 300;`
- 与 2022 代码掐架队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/vfs/posix_lock.rs:L1-L3`：关键词命中
    代码片段：`//! Sharded POSIX record lock manager with blocking support. //! //! Uses 53 shards keyed by '(dev_id, inode_id)'.  Each shard maps a 'LockKey'`
  - `os/src/timer.rs:L432-L436`：关键词命中
    代码片段：`// ───────────────────────────────────────────────────────── use core::sync::atomic::{AtomicU64, Ordering as AtomicOrdering}; pub trait TimeSource {`
- 与 2022 代码掐架队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L9-L13`：关键词命中
    代码片段：`UserPtrMut, UserSlice, VirtAddr, }; use crate::syscall::utils::wait_io_core; use crate::task::{ current_task, current_user_token, find_process_by_pid, find_task_by_tid,`
  - `os/src/syscall/mod.rs:L17-L21`：关键词命中
    代码片段：`sys_timerfd_create, sys_timerfd_gettime, sys_timerfd_settime, TimerFdSpec, }; use crate::net::syscall::*; use core::convert::TryFrom; use flock::*;`
- 与 2022 代码掐架队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 代码掐架队 的语言构成不同：待测作品为 {'json': 372, 'markdown': 99153, 'yaml': 93, 'build': 12118, 'c': 409, 'toml': 9933, 'rust': 981778, 'asm': 1177, 'text': 1086, 'make': 369}，历史样本为 {'json': 62, 'build': 2072, 'markdown': 7242, 'asm': 658, 'text': 1942, 'rust': 152844, 'c': 2166, 'toml': 15, 'yaml': 79}。（置信度：medium）

## 可能创新点

- 待测作品包含 194 个历史样本 2022 代码掐架队 中未见的独特函数/类型定义（如 `sys_linkat`, `version_and_date_from_rustc_verbose_version`, `sys_socketpair`, `socketpair`, `sys_timer_create`, `version_and_date_from_rustc_version`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `user/src/syscall.rs:L535-L535`：fn sys_linkat
    代码片段：`pub fn sys_linkat(olddirfd: isize, oldpath: &str, newdirfd: isize, newpath: &str, flags: u32) -> isize {`
  - `user/vendor/version_check/src/lib.rs:L138-L138`：fn version_and_date_from_rustc_verbose_version
    代码片段：`fn version_and_date_from_rustc_verbose_version(s: &str) -> (Option<String>, Option<String>) {`
- 待测作品代码规模（1,106,488 LOC）显著大于历史样本 2022 代码掐架队（167,080 LOC），可能包含更多功能模块或更完整的实现，但也可能包含非内核代码，需人工确认。（置信度：low）

## 附录：核验摘要

- 关键结论数：28
- 含证据关键结论数：27（96.4%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
