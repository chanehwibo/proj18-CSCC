# 2026 T202610213999576 比较报告

- 对比历史仓库：2022 caporustos
- 生成时间：2026-07-02T04:59:38.872849+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 caporustos（来源：赛事历史作品）：画像相似度 score=9.50；同属 rcore-variant 风格; 语言构成相似度 0.93; OS 维度重合度 1.00; 代码规模接近度 0.65

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 caporustos 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console.rs:L27-L31`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
  - `os/src/drivers/net.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO network device wrapper used by smoltcp. //! //! RISC-V QEMU can expose a VirtIO-net MMIO device. Other targets currently fall`
  - `codes/os/src/console.rs:L21-L25`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
  - `codes/os/src/drivers/block/mod.rs:L4-L8`：关键词命中
    代码片段：`mod sleep; mod spi; mod virtio_blk; mod virtio_fsimg;`
- 与 2022 caporustos 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! 这一层是整个 fs 子系统的入口 //! 对外只暴露 fd table 和 VFS API 这类上层需要使用的边界 //! ext4 raw 层 具体 filesystem 后端和块缓存都留在 fs 内部 //!`
  - `os/src/fs/fdtable.rs:L28-L32`：关键词命中
    代码片段：`/// 文件类型 /// /// 普通文件、目录以及 VFS-backed procfs/sysfs/devfs 节点统一使用 /// 'FileType::Vfs { file: Arc<VfsFile> }'。非 VFS 变体只表示标准流、 /// pipe/socket/eventfd/epoll 和少量设备状态机。`
  - `codes/os/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`mod pipe; pub mod stdio; mod inode; mod mount; pub mod finfo;`
  - `codes/os/src/fs/inode.rs:L36-L40`：关键词命中
    代码片段：`} // 此inode实际被当作文件 pub struct OSInode { readable: bool,`
- 与 2022 caporustos 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/time/timer.rs:L115-L119`：关键词命中
    代码片段：`/// 返回值：成功返回 0，失败返回负 errno。 pub fn sys_timer_create(clock_id: usize, sevp: *const u8, timer_id: *mut u8) -> isize { // timer id 是进程内 id；通知方式只实现 signal/thread_id 的测试可见语义。 if time...`
  - `os/src/arch/riscv/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`// ! Trap处理模块（RISC-V） // ! // ! 常见寄存器：`
  - `codes/os/src/trap/mod.rs:L6-L10`：关键词命中
    代码片段：`scause::{ self, Trap, Exception, Interrupt,`
  - `codes/os/src/sbi.rs:L30-L34`：关键词命中
    代码片段：`} pub fn set_timer(timer: usize) { sbi_call(SBI_SET_TIMER, timer, 0, 0); }`
- 与 2022 caporustos 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`#![allow(dead_code)] mod address; mod frame_allocator; mod layout; mod map_area;`
  - `os/src/mm/swap.rs:L20-L24`：关键词命中
    代码片段：`} /// 记录一次 COW 拆分事件。 pub fn on_cow_break() { COW_BREAK_TOTAL.fetch_add(1, Ordering::Relaxed);`
  - `codes/os/src/mm/mod.rs:L1-L3`：关键词命中
    代码片段：`mod heap_allocator; mod address; mod frame_allocator;`
  - `codes/os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{PageTable, PageTableEntry, PTEFlags}; use super::{VirtPageNum, VirtAddr, PhysPageNum, PhysAddr}; use super::{FrameTracker, frame_alloc, frame_add_ref, enquire_refcou...`
- 与 2022 caporustos 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/process/sched.rs:L42-L46`：关键词命中
    代码片段：`#[inline] fn pop_next_ready_fifo(pm: &mut crate::process::ProcessManager, current: usize) -> Option<usize> { // 借鉴点：RocketOS scheduler 使用 FIFO ready queue：fetch 从队首取任务， // yield...`
  - `os/src/main.rs:L80-L84`：关键词命中
    代码片段：`mm::init(); // 这些 runtime_state 是内核全局表，不依赖用户文件系统内容，必须在 trap/scheduler // 和用户态切换前完成初始化。 process::init_runtime_state();`
  - `codes/os/src/process/thread/mod.rs:L2-L5`：关键词命中
    代码片段：`pub(in crate::process) mod adt; pub(in crate::process) mod context; pub(in crate::process) mod scheduler; pub(in crate::process) mod manager;`
  - `codes/os/src/process/thread/core/tcb.rs:L4-L8`：关键词命中
    代码片段：`use crate::{process::{ process::core::pcb::ProcessControlBlock, thread::{adt::{tid::TidHandle, kernel_stack::KernelStack}, scheduler::info::SchedulePolicy}, thread::context::con...`
- 与 2022 caporustos 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/vfs_core/ops/lock_notify.rs:L33-L37`：关键词命中
    代码片段：`pub fn vfs_file_mandatory_locking_enabled(file: &VfsFile) -> bool { // Linux mandatory lock 由 setgid 位且 group-exec 清零的普通文件触发； // VFS fd 的判断必须来自打开 inode 的 metadata，而不是旧路径表。 vfs_f...`
  - `os/src/mm/mod.rs:L13-L17`：关键词命中
    代码片段：`use buddy_system_allocator::LockedHeap; use core::alloc::{GlobalAlloc, Layout}; use core::sync::atomic::{AtomicBool, AtomicUsize, Ordering}; pub use address::{PhysAddr, PhysPage...`
  - `codes/os/src/main.rs:L96-L100`：关键词命中
    代码片段：`lazy_static! { static ref CORE2_FLAG: Arc<Mutex<Core2flag>> = Arc::new(Mutex::new( Core2flag{ is_in:false,`
  - `codes/os/src/fs/pipe.rs:L1-L5`：关键词命中
    代码片段：`use super::File; use alloc::sync::{Arc, Weak}; use spin::Mutex; use crate::mm::UserBuffer; use crate::process::suspend_current_and_run_next;`
- 与 2022 caporustos 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Linux syscall 分发入口。 //! //! 本模块只负责 syscall number 到具体实现的 ABI 分派、运行态清理入口以及少量`
  - `os/src/syscall/net.rs:L12-L16`：关键词命中
    代码片段：`use crate::net::{self, SocketHandle}; use crate::process::{PROCESS_MANAGER, current_euid, current_nofile_limit}; use crate::syscall::fs::resolve_pathat_text; use crate::uapi::fs...`
  - `codes/os/src/syscall/fs.rs:L903-L907`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: isize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize{ if dirfd == AT...`
  - `codes/os/src/syscall/mod.rs:L79-L83`：关键词命中
    代码片段：`use num_derive::FromPrimitive; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> isize { gdb_print!(SYSCALL_ENABLE,"syscall-({}) arg0 = {}, arg1 = {}\n",syscall_id, args[0]...`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2022 caporustos 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：arg, console, crate, fmt, format_args, literal, macro_rules, print。（置信度：high）
  证据：
  - `os/src/console.rs:L27-L31`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
  - `codes/os/src/console.rs:L21-L25`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
- 与 2022 caporustos 在“设备驱动”维度发现片段级代码相似度 0.87 （token=0.80, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：c, fmt, fn, for, impl, in, mut, result。（置信度：high）
  证据：
  - `os/src/console.rs:L12-L16`：fn write_str
    代码片段：`impl Write for Stdout { fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.bytes() { putchar(c);`
  - `codes/os/src/console.rs:L6-L10`：fn write_str
    代码片段：`impl Write for Stdout { fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.chars() { console_putchar(c as usize);`
- 函数/符号名重合：与 2022 caporustos 在“文件系统”维度发现 3 个同名定义：lseek, new, page_offset。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `user/src/lib.rs:L240-L240`：fn lseek
    代码片段：`pub fn lseek(fd: usize, offset: isize, whence: usize) -> isize {`
  - `codes/os/src/fs/inode.rs:L245-L245`：fn lseek
    代码片段：`pub fn lseek(&self, offset: isize, whence: i32) -> isize {`
  - `os/src/fs/fdtable.rs:L136-L136`：fn new
    代码片段：`pub fn new() -> Self {`
  - `codes/os/src/fs/finfo.rs:L49-L49`：fn new
    代码片段：`pub fn new(d_ino: u64, d_off: i64, d_reclen: u16, d_type: u8) -> Self {`
- 与 2022 caporustos 在“中断与异常”维度发现片段级代码相似度 0.69 （token=0.58, structure=0.89）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, get_time, pub, usize。（置信度：medium）
  证据：
  - `os/src/arch/riscv/timer.rs:L19-L23`：fn get_time_us
    代码片段：`/// 获取当前时间（微秒） pub fn get_time_us() -> usize { get_time() * 1_000_000 / get_clock_freq() }`
  - `codes/os/src/timer.rs:L31-L35`：fn get_time_ns
    代码片段：`} pub fn get_time_ns() -> usize { (get_time() / (CLOCK_FREQ / USEC_PER_SEC)) * MSEC_PER_SEC }`
- 函数/符号名重合：与 2022 caporustos 在“内存管理”维度发现 4 个同名定义：handle_alloc_error, from, frame_dealloc, alloc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `user/src/lib.rs:L31-L31`：fn handle_alloc_error
    代码片段：`pub fn handle_alloc_error(layout: core::alloc::Layout) -> ! {`
  - `codes/user/src/lib.rs:L36-L36`：fn handle_alloc_error
    代码片段：`pub fn handle_alloc_error(layout: core::alloc::Layout) -> ! {`
  - `user/src/bin/boot/shell/command.rs:L64-L64`：fn from
    代码片段：`fn from(line: &str) -> Self {`
  - `codes/os/src/mm/address.rs:L44-L44`：fn from
    代码片段：`fn from(v: usize) -> Self { Self(v) }`
- 函数/符号名重合：与 2022 caporustos 在“系统调用”维度发现 4 个同名定义：syscall, sys_chdir, sys_close, sys_read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `user/src/syscall.rs:L52-L52`：fn syscall
    代码片段：`fn syscall(id: usize, args: [usize; 6]) -> isize {`
  - `codes/user/src/syscall.rs:L67-L67`：fn syscall
    代码片段：`fn syscall(id: usize, args: [usize; 6]) -> isize {`
  - `user/src/syscall.rs:L89-L89`：fn sys_chdir
    代码片段：`pub fn sys_chdir(path: *const u8) -> isize {`
  - `codes/user/src/syscall.rs:L96-L96`：fn sys_chdir
    代码片段：`pub fn sys_chdir(path: &str) -> isize {`
- 函数/符号名重合：与 2022 caporustos 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `user/src/console.rs:L7-L7`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `codes/user/src/console.rs:L11-L11`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 结构体/类型重合：与 2022 caporustos 在“内存管理”维度发现 4 个同名定义：PhysAddr, VirtAddr, PhysPageNum, VirtPageNum。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `os/src/mm/address.rs:L14-L14`：struct PhysAddr
    代码片段：`pub struct PhysAddr(pub usize);`
  - `codes/os/src/mm/address.rs:L8-L8`：struct PhysAddr
    代码片段：`pub struct PhysAddr(pub usize);`
  - `os/src/mm/address.rs:L18-L18`：struct VirtAddr
    代码片段：`pub struct VirtAddr(pub usize);`
  - `codes/os/src/mm/address.rs:L12-L12`：struct VirtAddr
    代码片段：`pub struct VirtAddr(pub usize);`
- 函数/符号名重合：与 2022 caporustos 在“调度与任务管理”维度发现 2 个同名定义：yield_, sys_yield。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `user/src/lib.rs:L285-L285`：fn yield_
    代码片段：`pub fn yield_() -> isize {`
  - `codes/user/src/lib.rs:L739-L739`：fn yield_
    代码片段：`pub fn yield_() -> isize {`
  - `user/src/syscall.rs:L179-L179`：fn sys_yield
    代码片段：`pub fn sys_yield() -> isize {`
  - `codes/user/src/syscall.rs:L135-L135`：fn sys_yield
    代码片段：`pub fn sys_yield() -> isize {`
- 结构体/类型重合：与 2022 caporustos 在“设备驱动”维度发现 1 个同名定义：Stdout。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `user/src/console.rs:L4-L4`：struct Stdout
    代码片段：`struct Stdout;`
  - `codes/user/src/console.rs:L8-L8`：struct Stdout
    代码片段：`struct Stdout;`
- 文件路径重合：与 2022 caporustos 在“设备驱动”维度出现同名文件源码路径 `os/src/console.rs` / `codes/os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/console.rs:L27-L31`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
  - `codes/os/src/console.rs:L21-L25`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
- 文件路径重合：与 2022 caporustos 在“中断与异常”维度出现同名文件源码路径 `os/src/syscall/time/timer.rs` / `codes/os/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/syscall/time/timer.rs:L115-L119`：关键词命中
    代码片段：`/// 返回值：成功返回 0，失败返回负 errno。 pub fn sys_timer_create(clock_id: usize, sevp: *const u8, timer_id: *mut u8) -> isize { // timer id 是进程内 id；通知方式只实现 signal/thread_id 的测试可见语义。 if time...`
  - `codes/os/src/timer.rs:L9-L13`：fn get_time
    代码片段：`pub const NSEC_PER_SEC: usize = 1000_000_000; pub fn get_time() -> usize { // let mut time: usize = 0; // unsafe{`
- 文件路径重合：与 2022 caporustos 在“中断与异常”维度出现同名文件源码路径 `os/src/arch/riscv/timer.rs` / `codes/os/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/arch/riscv/timer.rs:L7-L11`：fn get_time
    代码片段：`/// 读取当前时间计数器 pub fn get_time() -> usize { time::read() }`
  - `codes/os/src/timer.rs:L9-L13`：fn get_time
    代码片段：`pub const NSEC_PER_SEC: usize = 1000_000_000; pub fn get_time() -> usize { // let mut time: usize = 0; // unsafe{`

## 相似点

- 与 2022 caporustos 同属 rcore-variant 风格。（置信度：medium）
- 与 2022 caporustos 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console.rs:L27-L31`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
  - `os/src/drivers/net.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO network device wrapper used by smoltcp. //! //! RISC-V QEMU can expose a VirtIO-net MMIO device. Other targets currently fall`
- 与 2022 caporustos 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! 这一层是整个 fs 子系统的入口 //! 对外只暴露 fd table 和 VFS API 这类上层需要使用的边界 //! ext4 raw 层 具体 filesystem 后端和块缓存都留在 fs 内部 //!`
  - `os/src/fs/fdtable.rs:L28-L32`：关键词命中
    代码片段：`/// 文件类型 /// /// 普通文件、目录以及 VFS-backed procfs/sysfs/devfs 节点统一使用 /// 'FileType::Vfs { file: Arc<VfsFile> }'。非 VFS 变体只表示标准流、 /// pipe/socket/eventfd/epoll 和少量设备状态机。`
- 与 2022 caporustos 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/time/timer.rs:L115-L119`：关键词命中
    代码片段：`/// 返回值：成功返回 0，失败返回负 errno。 pub fn sys_timer_create(clock_id: usize, sevp: *const u8, timer_id: *mut u8) -> isize { // timer id 是进程内 id；通知方式只实现 signal/thread_id 的测试可见语义。 if time...`
  - `os/src/arch/riscv/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`// ! Trap处理模块（RISC-V） // ! // ! 常见寄存器：`
- 与 2022 caporustos 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`#![allow(dead_code)] mod address; mod frame_allocator; mod layout; mod map_area;`
  - `os/src/mm/swap.rs:L20-L24`：关键词命中
    代码片段：`} /// 记录一次 COW 拆分事件。 pub fn on_cow_break() { COW_BREAK_TOTAL.fetch_add(1, Ordering::Relaxed);`
- 与 2022 caporustos 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/process/sched.rs:L42-L46`：关键词命中
    代码片段：`#[inline] fn pop_next_ready_fifo(pm: &mut crate::process::ProcessManager, current: usize) -> Option<usize> { // 借鉴点：RocketOS scheduler 使用 FIFO ready queue：fetch 从队首取任务， // yield...`
  - `os/src/main.rs:L80-L84`：关键词命中
    代码片段：`mm::init(); // 这些 runtime_state 是内核全局表，不依赖用户文件系统内容，必须在 trap/scheduler // 和用户态切换前完成初始化。 process::init_runtime_state();`
- 与 2022 caporustos 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/vfs_core/ops/lock_notify.rs:L33-L37`：关键词命中
    代码片段：`pub fn vfs_file_mandatory_locking_enabled(file: &VfsFile) -> bool { // Linux mandatory lock 由 setgid 位且 group-exec 清零的普通文件触发； // VFS fd 的判断必须来自打开 inode 的 metadata，而不是旧路径表。 vfs_f...`
  - `os/src/mm/mod.rs:L13-L17`：关键词命中
    代码片段：`use buddy_system_allocator::LockedHeap; use core::alloc::{GlobalAlloc, Layout}; use core::sync::atomic::{AtomicBool, AtomicUsize, Ordering}; pub use address::{PhysAddr, PhysPage...`
- 与 2022 caporustos 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Linux syscall 分发入口。 //! //! 本模块只负责 syscall number 到具体实现的 ABI 分派、运行态清理入口以及少量`
  - `os/src/syscall/net.rs:L12-L16`：关键词命中
    代码片段：`use crate::net::{self, SocketHandle}; use crate::process::{PROCESS_MANAGER, current_euid, current_nofile_limit}; use crate::syscall::fs::resolve_pathat_text; use crate::uapi::fs...`
- 与 2022 caporustos 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 caporustos 的语言构成不同：待测作品为 {'json': 20, 'build': 490, 'markdown': 2444, 'text': 7260, 'toml': 26, 'rust': 90921, 'asm': 550}，历史样本为 {'json': 20, 'build': 685, 'markdown': 3328, 'asm': 148, 'text': 60, 'rust': 54342, 'yaml': 427, 'toml': 22}。（置信度：medium）

## 可能创新点

- 待测作品包含 316 个历史样本 2022 caporustos 中未见的独特函数/类型定义（如 `mmap`, `finalize_unix_socket_release_if_last`, `emit_first_fields`, `sys_ppoll`, `make_case_path`, `sys_clone`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `user/src/lib.rs:L376-L376`：fn mmap
    代码片段：`pub fn mmap(addr: usize, len: usize, prot: usize, flags: usize, fd: usize, offset: usize) -> isize {`
  - `os/src/fs/fdtable.rs:L373-L373`：fn finalize_unix_socket_release_if_last
    代码片段：`pub(crate) fn finalize_unix_socket_release_if_last(handle: &crate::net::local::UnixSocketHandle) {`

## 附录：核验摘要

- 关键结论数：28
- 含证据关键结论数：28（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
