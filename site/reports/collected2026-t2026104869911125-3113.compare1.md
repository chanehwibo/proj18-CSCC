# 2026 T2026104869911125 比较报告

- 对比历史仓库：2025 F7LY
- 生成时间：2026-07-01T21:04:13.527442+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 F7LY（来源：赛事历史作品）：画像相似度 score=9.63；同属 ucore-variant 风格; 语言构成相似度 0.90; OS 维度重合度 1.00; 代码规模接近度 0.83

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 F7LY 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/devs/uart.cc:L1-L3`：关键词命中
    代码片段：`#include "uart.hh" #include "printer.hh" #include "console.hh"`
  - `kernel/devs/uart.hh:L191-L194`：关键词命中
    代码片段：`} }; extern UartManager k_uart; // 全局的uart管理器 }`
  - `kernel/devs/uart.cc:L1-L3`：关键词命中
    代码片段：`#include "uart.hh" #include "printer.hh" #include "console.hh"`
  - `kernel/devs/uart.hh:L190-L193`：关键词命中
    代码片段：`} }; extern UartManager k_uart; // 全局的uart管理器 }`
- 与 2025 F7LY 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/bio.cc:L21-L25`：关键词命中
    代码片段：`#include "platform.hh" #include "fs/vfs/fs.hh" #include "fs/buf.hh" #include "devs/device_manager.hh"`
  - `kernel/fs/buf.hh:L1-L4`：关键词命中
    代码片段：`#pragma once #include "fs/vfs/fs.hh" #include "sleeplock.hh" struct buf {`
  - `kernel/fs/bio.cc:L21-L25`：关键词命中
    代码片段：`#include "platform.hh" #include "fs/vfs/fs.hh" #include "fs/buf.hh" #include "devs/device_manager.hh"`
  - `kernel/fs/buf.hh:L1-L4`：关键词命中
    代码片段：`#pragma once #include "fs/vfs/fs.hh" #include "sleeplock.hh" struct buf {`
- 与 2025 F7LY 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap/riscv/plic.cc:L3-L7`：关键词命中
    代码片段：`#include "mem/memlayout.hh" #include "platform.hh" #include "plic.hh" #include "printer.hh" #include "cpu.hh"`
  - `kernel/trap/riscv/plic.hh:L7-L11`：关键词命中
    代码片段：`void inithart(); int claim(); void complete(int irq); };`
  - `kernel/trap/riscv/plic.cc:L3-L7`：关键词命中
    代码片段：`#include "mem/memlayout.hh" #include "platform.hh" #include "plic.hh" #include "printer.hh" #include "cpu.hh"`
  - `kernel/trap/riscv/plic.hh:L7-L11`：关键词命中
    代码片段：`void inithart(); int claim(); void complete(int irq); };`
- 与 2025 F7LY 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/vm_area.hh:L138-L142`：关键词命中
    代码片段：`uint64 page_index_for_va(uint64 va) const { return (PGROUNDDOWN(va) - PGROUNDDOWN(addr)) / PGSIZE; }`
  - `kernel/proc/vm_object.cc:L150-L154`：关键词命中
    代码片段：`{ memmove(page, src, copy_bytes); if (zero_fill_tail && copy_bytes < PGSIZE) { memset(reinterpret_cast<char *>(page) + copy_bytes, 0, PGSIZE - copy_bytes);`
  - `kernel/mem/loongarch/page.hh:L51-L55`：关键词命中
    代码片段：`dir3_vpn_mask = pt_bits_mask << dir3_vpn_shift, pte_width = 0x0UL };`
  - `kernel/mem/heap_memory_manager.cc:L1-L4`：关键词命中
    代码片段：`#include "heap_memory_manager.hh" #ifdef RISCV #include "mem/riscv/pagetable.hh"`
- 与 2025 F7LY 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/pipe.cc:L16-L20`：关键词命中
    代码片段：`#include "printer.hh" #include "signal.hh" #include "scheduler.hh" // #include "fs/vfs/fs_defs.hh" #include "sys/syscall_defs.hh"`
  - `kernel/proc/pipe.hh:L22-L26`：关键词命中
    代码片段：`class PageTable; } namespace proc { class Pcb;`
  - `kernel/proc/pipe.cc:L17-L21`：关键词命中
    代码片段：`// #include "fs/vfs/fs_defs.hh" #include "sys/syscall_defs.hh" namespace proc { namespace ipc`
  - `kernel/proc/pipe.hh:L18-L22`：关键词命中
    代码片段：`} namespace proc { class ProcessManager;`
- 与 2025 F7LY 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/libs/semaphore.cc:L1-L4`：关键词命中
    代码片段：`#include "semaphore.hh" #include "proc_manager.hh" #include "scheduler.hh"`
  - `kernel/libs/semaphore.hh:L1-L5`：关键词命中
    代码片段：`#pragma once #include "types.hh" #include "spinlock.hh" #include "param.h" #include "platform.hh"`
  - `kernel/libs/semaphore.cc:L1-L4`：关键词命中
    代码片段：`#include "semaphore.hh" #include "proc_manager.hh" #include "scheduler.hh"`
  - `kernel/libs/semaphore.hh:L1-L5`：关键词命中
    代码片段：`#pragma once #include "types.hh" #include "spinlock.hh" #include "param.h" #include "platform.hh"`
- 与 2025 F7LY 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sys/sysproc.cc:L7-L11`：关键词命中
    代码片段：`#include "proc/proc_manager.hh" namespace syscall { uint64 SyscallHandler::sys_getppid()`
  - `kernel/sys/syscall_abi.hh:L4-L8`：关键词命中
    代码片段：`#include "fs/vfs/file/socket_defs.hh" namespace syscall::abi { // 这里集中保存“用户态可见的 Linux ABI 布局”和常量。`
  - `kernel/sys/syscall_defs.hh:L1-L4`：关键词命中
    代码片段：`#pragma once namespace syscall {`
  - `kernel/sys/syscall_handler.cc:L64-L68`：关键词命中
    代码片段：`#include "fs/debug.hh" #include "interrupt_stats.hh" namespace syscall { // 创建全局的 SyscallHandler 实例`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 F7LY 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：extern, k_uart, uartmanager。（置信度：high）
  证据：
  - `kernel/devs/uart.hh:L191-L194`：关键词命中
    代码片段：`} }; extern UartManager k_uart; // 全局的uart管理器 }`
  - `kernel/devs/uart.hh:L190-L193`：关键词命中
    代码片段：`} }; extern UartManager k_uart; // 全局的uart管理器 }`
- 与 2025 F7LY 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：chardevice, class, private, public, register_debug_uart, uart_port, uartmanager, void。（置信度：high）
  证据：
  - `kernel/devs/uart.hh:L7-L11`：class UartManager
    代码片段：`{ void register_debug_uart(CharDevice *uart_port); class UartManager : public CharDevice { private:`
  - `kernel/devs/uart.hh:L7-L11`：class UartManager
    代码片段：`{ void register_debug_uart(CharDevice *uart_port); class UartManager : public CharDevice { private:`
- 与 2025 F7LY 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：buf, disk, int, struct, valid。（置信度：high）
  证据：
  - `kernel/fs/buf.hh:L2-L6`：struct buf
    代码片段：`#include "fs/vfs/fs.hh" #include "sleeplock.hh" struct buf { int valid;   // has data been read from disk? int disk;    // does disk "own" buf?`
  - `kernel/fs/buf.hh:L2-L6`：struct buf
    代码片段：`#include "fs/vfs/fs.hh" #include "sleeplock.hh" struct buf { int valid;   // has data been read from disk? int disk;    // does disk "own" buf?`
- 与 2025 F7LY 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：struct, uint16, winsize, ws_col, ws_row。（置信度：high）
  证据：
  - `kernel/fs/ioctl.hh:L124-L128`：struct winsize
    代码片段：`#define TCIOFLUSH 2    /* Flush both pending input and output */ struct winsize { uint16 ws_row;        /* rows， in character */ uint16 ws_col;        /* columns, in characters */`
  - `kernel/fs/ioctl.hh:L124-L128`：struct winsize
    代码片段：`#define TCIOFLUSH 2    /* Flush both pending input and output */ struct winsize { uint16 ws_row;        /* rows， in character */ uint16 ws_col;        /* columns, in characters */`
- 与 2025 F7LY 在“中断与异常”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：claim, complete, inithart, int, irq, void。（置信度：high）
  证据：
  - `kernel/trap/riscv/plic.hh:L7-L11`：关键词命中
    代码片段：`void inithart(); int claim(); void complete(int irq); };`
  - `kernel/trap/riscv/plic.hh:L7-L11`：关键词命中
    代码片段：`void inithart(); int claim(); void complete(int irq); };`
- 与 2025 F7LY 在“中断与异常”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：class, friend, timermanager, tmm, trap_manager。（置信度：high）
  证据：
  - `kernel/trap/riscv/trap.hh:L7-L11`：class trap_manager
    代码片段：`} class trap_manager { friend  class tmm::TimerManager;`
  - `kernel/trap/riscv/trap.hh:L7-L11`：class trap_manager
    代码片段：`} class trap_manager { friend  class tmm::TimerManager;`
- 文件路径重合：与 2025 F7LY 在“设备驱动”维度出现同路径源码路径 `kernel/devs/uart.cc` / `kernel/devs/uart.cc`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/devs/uart.cc:L1-L3`：关键词命中
    代码片段：`#include "uart.hh" #include "printer.hh" #include "console.hh"`
  - `kernel/devs/uart.cc:L1-L3`：关键词命中
    代码片段：`#include "uart.hh" #include "printer.hh" #include "console.hh"`
- 文件路径重合：与 2025 F7LY 在“设备驱动”维度出现同路径源码路径 `kernel/devs/uart.hh` / `kernel/devs/uart.hh`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/devs/uart.hh:L191-L194`：关键词命中
    代码片段：`} }; extern UartManager k_uart; // 全局的uart管理器 }`
  - `kernel/devs/uart.hh:L190-L193`：关键词命中
    代码片段：`} }; extern UartManager k_uart; // 全局的uart管理器 }`
- 文件路径重合：与 2025 F7LY 在“文件系统”维度出现同路径源码路径 `kernel/fs/bio.cc` / `kernel/fs/bio.cc`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/bio.cc:L21-L25`：关键词命中
    代码片段：`#include "platform.hh" #include "fs/vfs/fs.hh" #include "fs/buf.hh" #include "devs/device_manager.hh"`
  - `kernel/fs/bio.cc:L21-L25`：关键词命中
    代码片段：`#include "platform.hh" #include "fs/vfs/fs.hh" #include "fs/buf.hh" #include "devs/device_manager.hh"`
- 文件路径重合：与 2025 F7LY 在“文件系统”维度出现同路径源码路径 `kernel/fs/buf.hh` / `kernel/fs/buf.hh`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/buf.hh:L1-L4`：关键词命中
    代码片段：`#pragma once #include "fs/vfs/fs.hh" #include "sleeplock.hh" struct buf {`
  - `kernel/fs/buf.hh:L1-L4`：关键词命中
    代码片段：`#pragma once #include "fs/vfs/fs.hh" #include "sleeplock.hh" struct buf {`
- 文件路径重合：与 2025 F7LY 在“中断与异常”维度出现同路径源码路径 `kernel/trap/riscv/plic.cc` / `kernel/trap/riscv/plic.cc`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/trap/riscv/plic.cc:L3-L7`：关键词命中
    代码片段：`#include "mem/memlayout.hh" #include "platform.hh" #include "plic.hh" #include "printer.hh" #include "cpu.hh"`
  - `kernel/trap/riscv/plic.cc:L3-L7`：关键词命中
    代码片段：`#include "mem/memlayout.hh" #include "platform.hh" #include "plic.hh" #include "printer.hh" #include "cpu.hh"`
- 文件路径重合：与 2025 F7LY 在“中断与异常”维度出现同路径源码路径 `kernel/trap/riscv/plic.hh` / `kernel/trap/riscv/plic.hh`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/trap/riscv/plic.hh:L7-L11`：关键词命中
    代码片段：`void inithart(); int claim(); void complete(int irq); };`
  - `kernel/trap/riscv/plic.hh:L7-L11`：关键词命中
    代码片段：`void inithart(); int claim(); void complete(int irq); };`

## 相似点

- 与 2025 F7LY 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 F7LY 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/devs/uart.cc:L1-L3`：关键词命中
    代码片段：`#include "uart.hh" #include "printer.hh" #include "console.hh"`
  - `kernel/devs/uart.hh:L191-L194`：关键词命中
    代码片段：`} }; extern UartManager k_uart; // 全局的uart管理器 }`
- 与 2025 F7LY 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/bio.cc:L21-L25`：关键词命中
    代码片段：`#include "platform.hh" #include "fs/vfs/fs.hh" #include "fs/buf.hh" #include "devs/device_manager.hh"`
  - `kernel/fs/buf.hh:L1-L4`：关键词命中
    代码片段：`#pragma once #include "fs/vfs/fs.hh" #include "sleeplock.hh" struct buf {`
- 与 2025 F7LY 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap/riscv/plic.cc:L3-L7`：关键词命中
    代码片段：`#include "mem/memlayout.hh" #include "platform.hh" #include "plic.hh" #include "printer.hh" #include "cpu.hh"`
  - `kernel/trap/riscv/plic.hh:L7-L11`：关键词命中
    代码片段：`void inithart(); int claim(); void complete(int irq); };`
- 与 2025 F7LY 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/vm_area.hh:L138-L142`：关键词命中
    代码片段：`uint64 page_index_for_va(uint64 va) const { return (PGROUNDDOWN(va) - PGROUNDDOWN(addr)) / PGSIZE; }`
  - `kernel/proc/vm_object.cc:L150-L154`：关键词命中
    代码片段：`{ memmove(page, src, copy_bytes); if (zero_fill_tail && copy_bytes < PGSIZE) { memset(reinterpret_cast<char *>(page) + copy_bytes, 0, PGSIZE - copy_bytes);`
- 与 2025 F7LY 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/pipe.cc:L16-L20`：关键词命中
    代码片段：`#include "printer.hh" #include "signal.hh" #include "scheduler.hh" // #include "fs/vfs/fs_defs.hh" #include "sys/syscall_defs.hh"`
  - `kernel/proc/pipe.hh:L22-L26`：关键词命中
    代码片段：`class PageTable; } namespace proc { class Pcb;`
- 与 2025 F7LY 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/libs/semaphore.cc:L1-L4`：关键词命中
    代码片段：`#include "semaphore.hh" #include "proc_manager.hh" #include "scheduler.hh"`
  - `kernel/libs/semaphore.hh:L1-L5`：关键词命中
    代码片段：`#pragma once #include "types.hh" #include "spinlock.hh" #include "param.h" #include "platform.hh"`
- 与 2025 F7LY 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sys/sysproc.cc:L7-L11`：关键词命中
    代码片段：`#include "proc/proc_manager.hh" namespace syscall { uint64 SyscallHandler::sys_getppid()`
  - `kernel/sys/syscall_abi.hh:L4-L8`：关键词命中
    代码片段：`#include "fs/vfs/file/socket_defs.hh" namespace syscall::abi { // 这里集中保存“用户态可见的 Linux ABI 布局”和常量。`
- 与 2025 F7LY 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 F7LY 的语言构成不同：待测作品为 {'markdown': 17111, 'build': 509, 'asm': 839, 'cpp': 140790, 'c': 115996, 'text': 22702}，历史样本为 {'json': 20, 'build': 409, 'markdown': 2603, 'cpp': 101948, 'text': 19838, 'c': 115992, 'asm': 680}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：26
- 含证据关键结论数：26（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
