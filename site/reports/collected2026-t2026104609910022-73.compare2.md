# 2026 T2026104609910022 比较报告

- 对比历史仓库：2022 马坡岭杂合子
- 生成时间：2026-07-02T09:02:19.416201+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 马坡岭杂合子（来源：赛事历史作品）：画像相似度 score=9.59；同属 rcore-variant 风格; 语言构成相似度 0.93; OS 维度重合度 1.00; 代码规模接近度 0.72

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 马坡岭杂合子 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/drivers/block.rs:L20-L24`：关键词命中
    代码片段：`#[cfg(feature = "arch-riscv64")] mod virtio_mmio { use super::{BlockProbe, BLOCK_SIZE}; use core::mem::size_of;`
  - `src-la/drivers/block.rs:L20-L24`：关键词命中
    代码片段：`#[cfg(feature = "arch-riscv64")] mod virtio_mmio { use super::{BlockProbe, BLOCK_SIZE}; use core::mem::size_of;`
  - `os/src/console.rs:L29-L33`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L5`：关键词命中
    代码片段：`mod block; mod plic; mod uart; use core::any::Any; use k210_pac::Interrupt;`
- 与 2022 马坡岭杂合子 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`use crate::println; pub mod fd_table; pub mod vfs; pub mod ext4; pub mod manager;`
  - `src/fs/ext4.rs:L1-L3`：关键词命中
    代码片段：`//! EXT4 file system layer ? inode lookup, file read use core::cell::Cell;`
  - `os/src/fs/mod.rs:L1-L4`：关键词命中
    代码片段：`#![allow(unused)] mod inode; mod pipe; mod stdio;`
  - `os/src/fs/vfs.rs:L10-L14`：关键词命中
    代码片段：`use crate::{ fatfs::{file::Inode, io::SeekFrom}, mm::UserBuffer, sync::{SpinMutex, UPSafeCell},`
- 与 2022 马坡岭杂合子 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/trap/mod.rs:L5-L9`：关键词命中
    代码片段：`current_task, current_trap_cx, do_wake_expired, suspend_current_and_run_next, }; use crate::timer::set_next_trigger; pub use context::{GeneralRegs, TrapContext}; mod context;`
  - `src/trap/context.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 上下文 — 保存/恢复用户态寄存器 /// 通用寄存器（RISC-V 64）`
  - `os/src/timer.rs:L257-L261`：关键词命中
    代码片段：`pub fn singal_wake_futex(tid: usize) { let mut timer = FUTEX_TIMERS.lock().unwrap(); loop { let idx = timer`
  - `os/src/irq/mod.rs:L12-L16`：关键词命中
    代码片段：`}; use core::fmt::Debug; use k210_pac::Interrupt; use lazy_static::lazy_static; use riscv::register::sie;`
- 与 2022 马坡岭杂合子 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`use crate::{print, println}; mod address; mod heap_allocator; mod frame_allocator; mod page_table;`
  - `src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use crate::mm::address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum, VPNRange}; use crate::mm::page_table::{PTEFlags, PageTableEntry, PageTable}; use crate::mm::frame_allocato...`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use super::swap::SwapPage; use super::{frame_alloc, FrameTracker}; use super::{PTEFlags, PageTable, PageTableEntry}; use super::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum};`
- 与 2022 马坡岭杂合子 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`pub mod pid; pub mod processor; pub mod task; use crate::timer::get_time;`
  - `src/task/pid.rs:L14-L18`：关键词命中
    代码片段：`impl PidAllocator { pub fn new() -> Self { // Linux userspace reserves pid 0 for the scheduler/idle context. Self { current: 1, recycled: Vec::new() } }`
  - `os/src/task/id.rs:L11-L15`：关键词命中
    代码片段：`use crate::sync::SpinMutex; use crate::syscall::{MmapFlag, MmapProt}; use crate::task::{current_process, TaskControlBlock}; use alloc::collections:: BTreeMap; use alloc::{`
  - `os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; mod switch; mod task; use crate::{ fatfs::{`
- 与 2022 马坡岭杂合子 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/task/mod.rs:L10-L14`：关键词命中
    代码片段：`use crate::config::CLOCK_FREQ; use lazy_static::*; use spin::Mutex; const PER_TASK_TIMEOUT: usize = 10 * CLOCK_FREQ;`
  - `src/task/pid.rs:L3-L7`：关键词命中
    代码片段：`use crate::config::{KERNEL_HEAP_SIZE, PAGE_SIZE}; use alloc::vec::Vec; use spin::Mutex; use lazy_static::*;`
  - `os/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`mod mutex; mod semaphore; mod signal;`
  - `os/src/sync/spin.rs:L3-L7`：关键词命中
    代码片段：`cell::UnsafeCell, ops::{Deref, DerefMut}, sync::atomic::{AtomicBool, Ordering}, }; use core::{fmt::Debug, sync::atomic::AtomicIsize};`
- 与 2022 马坡岭杂合子 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/syscall/mod.rs:L6-L10`：关键词命中
    代码片段：`use errno::*; // Linux RISC-V 64 syscall IDs（只列出我们需要的） pub const SYS_WRITE: usize = 64; pub const SYS_EVENTFD2: usize = 19;`
  - `src/trap/context.rs:L57-L61`：关键词命中
    代码片段：`/// 保留字段（汇编未使用，仅用于对齐） pub reserved: usize,          // 32*8 /// 发生 ecall 前的原始 a0（syscall 重启时需要） pub origin_a0: usize,         // 33*8 /// 陷入时的 sstatus（SPP/SPIE 等）`
  - `os/src/syscall/fs.rs:L29-L33`：关键词命中
    代码片段：`use crate::sync::SpinMutex; use crate::sync::UPSafeCell; use crate::syscall::errno::ENOENT; use crate::task::current_task; use crate::task::suspend_current_and_run_next;`
  - `os/src/syscall/mod.rs:L117-L121`：关键词命中
    代码片段：`// use thread::*; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> isize { match syscall_id { SYSCALL_GET_CWD => sys_getcwd(args[0] as *const u8, args[1]),`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2022 马坡岭杂合子 在“设备驱动”维度发现片段级代码相似度 0.94 （token=0.95, structure=0.92）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：arg, console, crate, fmt, format_args, literal, macro_rules, print。（置信度：high）
  证据：
  - `src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print {                        // 定义print宏,接受一个格式字符串和可选的参数 ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?));  // 格式...`
  - `os/src/console.rs:L29-L33`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
- 与 2022 马坡岭杂合子 在“系统调用”维度发现片段级代码相似度 0.84 （token=0.75, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：__alltraps, extern, fn。（置信度：high）
  证据：
  - `src/trap/mod.rs:L18-L22`：fn __alltraps
    代码片段：`extern "C" { pub fn __alltraps(); pub fn __restore(); }`
  - `os/src/trap/mod.rs:L28-L32`：fn set_kernel_trap_entry
    代码片段：`} fn set_kernel_trap_entry() { extern "C" { fn __alltraps();`
- 函数/符号名重合：与 2022 马坡岭杂合子 在“设备驱动”维度发现 4 个同名定义：write_str, enabled, log, flush。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src-la/console.rs:L7-L7`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {   // 创建write_str函数,输出字符串s中的每个字符`
  - `os/src/console.rs:L15-L15`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `src-la/console.rs:L43-L43`：fn enabled
    代码片段：`fn enabled(&self, _metadata: &log::Metadata) -> bool { true }`
  - `os/src/console.rs:L62-L62`：fn enabled
    代码片段：`fn enabled(&self, _metadata: &Metadata) -> bool {`
- 函数/符号名重合：与 2022 马坡岭杂合子 在“文件系统”维度发现 4 个同名定义：new, get_mut, read, write。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src-la/fs/ext4.rs:L28-L28`：fn new
    代码片段：`pub fn new() -> Result<Self, ()> {`
  - `os/src/fatfs/block_cache.rs:L21-L21`：fn new
    代码片段：`pub fn new(block_id: usize, block_device: Arc<dyn BlockDevice>) -> Self {`
  - `src-la/fs/fd_table.rs:L80-L80`：fn get_mut
    代码片段：`pub fn get_mut(&mut self, fd: usize) -> Option<&mut FileDescriptor> {`
  - `os/src/fatfs/block_cache.rs:L47-L47`：fn get_mut
    代码片段：`pub fn get_mut<T>(&mut self, offset: usize) -> &mut T`
- 与 2022 马坡岭杂合子 在“内存管理”维度发现片段级代码相似度 0.72 （token=0.58, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：frame_alloc, frametracker, pagetable, pagetableentry, physaddr, physpagenum, pteflags, use。（置信度：medium）
  证据：
  - `src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use crate::mm::address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum, VPNRange}; use crate::mm::page_table::{PTEFlags, PageTableEntry, PageTable}; use crate::mm::frame_allocato...`
  - `os/src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use super::swap::SwapPage; use super::{frame_alloc, FrameTracker}; use super::{PTEFlags, PageTable, PageTableEntry}; use super::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum};`
- 与 2022 马坡岭杂合子 在“调度与任务管理”维度发现片段级代码相似度 0.64 （token=0.60, structure=0.70）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：current, fn, impl, new, pub, self。（置信度：medium）
  证据：
  - `src/task/pid.rs:L14-L18`：关键词命中
    代码片段：`impl PidAllocator { pub fn new() -> Self { // Linux userspace reserves pid 0 for the scheduler/idle context. Self { current: 1, recycled: Vec::new() } }`
  - `os/src/task/id.rs:L25-L29`：fn new
    代码片段：`impl RecycleAllocator { pub fn new(current: usize) -> Self { RecycleAllocator { current,`
- 结构体/类型重合：与 2022 马坡岭杂合子 在“设备驱动”维度发现 3 个同名定义：Stdout, SimpleLogger, BlockDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src-la/console.rs:L4-L4`：struct Stdout
    代码片段：`struct Stdout;                              // 创建结构体Stdout,作为标准输出的实现`
  - `os/src/console.rs:L9-L9`：struct Stdout
    代码片段：`struct Stdout(Arc<dyn UartDevice>);`
  - `src-la/console.rs:L40-L40`：struct SimpleLogger
    代码片段：`struct SimpleLogger;`
  - `os/src/console.rs:L59-L59`：struct SimpleLogger
    代码片段：`struct SimpleLogger;`
- 结构体/类型重合：与 2022 马坡岭杂合子 在“文件系统”维度发现 3 个同名定义：OpenFlags, DirEntry, KStat。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src-la/fs/vfs.rs:L2-L2`：struct OpenFlags
    代码片段：`pub struct OpenFlags(pub usize);`
  - `os/src/fs/inode.rs:L94-L94`：struct OpenFlags
    代码片段：`pub struct OpenFlags: u32 {`
  - `src-la/syscall/fs.rs:L594-L594`：struct DirEntry
    代码片段：`struct DirEntry { ino: u32, file_type: u8, name: Vec<u8> }`
  - `os/src/fatfs/dir_entry.rs:L353-L353`：struct DirEntry
    代码片段：`pub struct DirEntry {`
- 函数/符号名重合：与 2022 马坡岭杂合子 在“同步机制”维度发现 2 个同名定义：read_block, write_block。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src-la/drivers/block.rs:L6-L6`：fn read_block
    代码片段：`fn read_block(&self, block_id: usize, buf: &mut [u8]);`
  - `os/src/drivers/mod.rs:L14-L14`：fn read_block
    代码片段：`fn read_block(&self, block_id: usize, buf: &mut [u8]);`
  - `src-la/drivers/block.rs:L7-L7`：fn write_block
    代码片段：`fn write_block(&self, block_id: usize, buf: &[u8]);`
  - `os/src/drivers/mod.rs:L15-L15`：fn write_block
    代码片段：`fn write_block(&self, block_id: usize, buf: &[u8]);`
- 结构体/类型重合：与 2022 马坡岭杂合子 在“同步机制”维度发现 1 个同名定义：BlockDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src-la/drivers/block.rs:L5-L5`：trait BlockDevice
    代码片段：`pub trait BlockDevice {`
  - `os/src/drivers/mod.rs:L13-L13`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync + Any {`
- 文件路径重合：与 2022 马坡岭杂合子 在“设备驱动”维度出现同名文件源码路径 `src/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print {                        // 定义print宏,接受一个格式字符串和可选的参数 ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?));  // 格式...`
  - `os/src/console.rs:L29-L33`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
- 文件路径重合：与 2022 马坡岭杂合子 在“文件系统”维度出现同名文件源码路径 `src/fs/vfs.rs` / `os/src/fs/vfs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/fs/vfs.rs:L1-L4`：struct OpenFlags
    代码片段：`#[derive(Clone, Copy, Debug, Eq, PartialEq)] pub struct OpenFlags(pub usize); impl OpenFlags {`
  - `os/src/fs/vfs.rs:L10-L14`：关键词命中
    代码片段：`use crate::{ fatfs::{file::Inode, io::SeekFrom}, mm::UserBuffer, sync::{SpinMutex, UPSafeCell},`
- 文件路径重合：与 2022 马坡岭杂合子 在“内存管理”维度出现同名文件源码路径 `src/mm/memory_set.rs` / `os/src/mm/memory_set.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use crate::mm::address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum, VPNRange}; use crate::mm::page_table::{PTEFlags, PageTableEntry, PageTable}; use crate::mm::frame_allocato...`
  - `os/src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use super::swap::SwapPage; use super::{frame_alloc, FrameTracker}; use super::{PTEFlags, PageTable, PageTableEntry}; use super::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum};`
- 文件路径重合：与 2022 马坡岭杂合子 在“内存管理”维度出现同名文件源码路径 `src/mm/page_table.rs` / `os/src/mm/page_table.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/mm/page_table.rs:L1-L5`：关键词命中
    代码片段：`use bitflags::*; use crate::mm::address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum}; use crate::mm::frame_allocator::{frame_alloc, frame_dealloc, FrameTracker}; use alloc::v...`
  - `os/src/mm/page_table.rs:L1-L3`：关键词命中
    代码片段：`use super::{frame_alloc, FrameTracker, PhysAddr, PhysPageNum, StepByOne, VirtAddr, VirtPageNum}; use _core::ops::{Index, IndexMut}; use alloc::string::String;`
- 文件路径重合：与 2022 马坡岭杂合子 在“系统调用”维度出现同名文件源码路径 `src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/syscall/fs.rs:L25-L29`：fn sys_write
    代码片段：`/// represented by fd 1/2 there, so this keeps regular files, pipes and stdout /// on one ABI path. pub fn sys_write(fd: usize, buf: usize, count: usize) -> isize { let token =...`
  - `os/src/syscall/fs.rs:L29-L33`：关键词命中
    代码片段：`use crate::sync::SpinMutex; use crate::sync::UPSafeCell; use crate::syscall::errno::ENOENT; use crate::task::current_task; use crate::task::suspend_current_and_run_next;`

## 相似点

- 与 2022 马坡岭杂合子 同属 rcore-variant 风格。（置信度：medium）
- 与 2022 马坡岭杂合子 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/drivers/block.rs:L20-L24`：关键词命中
    代码片段：`#[cfg(feature = "arch-riscv64")] mod virtio_mmio { use super::{BlockProbe, BLOCK_SIZE}; use core::mem::size_of;`
  - `src-la/drivers/block.rs:L20-L24`：关键词命中
    代码片段：`#[cfg(feature = "arch-riscv64")] mod virtio_mmio { use super::{BlockProbe, BLOCK_SIZE}; use core::mem::size_of;`
- 与 2022 马坡岭杂合子 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`use crate::println; pub mod fd_table; pub mod vfs; pub mod ext4; pub mod manager;`
  - `src/fs/ext4.rs:L1-L3`：关键词命中
    代码片段：`//! EXT4 file system layer ? inode lookup, file read use core::cell::Cell;`
- 与 2022 马坡岭杂合子 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/trap/mod.rs:L5-L9`：关键词命中
    代码片段：`current_task, current_trap_cx, do_wake_expired, suspend_current_and_run_next, }; use crate::timer::set_next_trigger; pub use context::{GeneralRegs, TrapContext}; mod context;`
  - `src/trap/context.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 上下文 — 保存/恢复用户态寄存器 /// 通用寄存器（RISC-V 64）`
- 与 2022 马坡岭杂合子 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`use crate::{print, println}; mod address; mod heap_allocator; mod frame_allocator; mod page_table;`
  - `src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use crate::mm::address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum, VPNRange}; use crate::mm::page_table::{PTEFlags, PageTableEntry, PageTable}; use crate::mm::frame_allocato...`
- 与 2022 马坡岭杂合子 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`pub mod pid; pub mod processor; pub mod task; use crate::timer::get_time;`
  - `src/task/pid.rs:L14-L18`：关键词命中
    代码片段：`impl PidAllocator { pub fn new() -> Self { // Linux userspace reserves pid 0 for the scheduler/idle context. Self { current: 1, recycled: Vec::new() } }`
- 与 2022 马坡岭杂合子 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/task/mod.rs:L10-L14`：关键词命中
    代码片段：`use crate::config::CLOCK_FREQ; use lazy_static::*; use spin::Mutex; const PER_TASK_TIMEOUT: usize = 10 * CLOCK_FREQ;`
  - `src/task/pid.rs:L3-L7`：关键词命中
    代码片段：`use crate::config::{KERNEL_HEAP_SIZE, PAGE_SIZE}; use alloc::vec::Vec; use spin::Mutex; use lazy_static::*;`
- 与 2022 马坡岭杂合子 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/syscall/mod.rs:L6-L10`：关键词命中
    代码片段：`use errno::*; // Linux RISC-V 64 syscall IDs（只列出我们需要的） pub const SYS_WRITE: usize = 64; pub const SYS_EVENTFD2: usize = 19;`
  - `src/trap/context.rs:L57-L61`：关键词命中
    代码片段：`/// 保留字段（汇编未使用，仅用于对齐） pub reserved: usize,          // 32*8 /// 发生 ecall 前的原始 a0（syscall 重启时需要） pub origin_a0: usize,         // 33*8 /// 陷入时的 sstatus（SPP/SPIE 等）`
- 与 2022 马坡岭杂合子 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 马坡岭杂合子 的语言构成不同：待测作品为 {'build': 462, 'markdown': 235, 'toml': 32, 'text': 44, 'rust': 22107, 'asm': 360, 'c': 26}，历史样本为 {'json': 20, 'build': 430, 'markdown': 2001, 'text': 241, 'c': 245, 'toml': 10, 'rust': 30628, 'asm': 696, 'yaml': 87}。（置信度：medium）

## 可能创新点

- 待测作品包含 344 个历史样本 2022 马坡岭杂合子 中未见的独特函数/类型定义（如 `install_handle_with_descriptor`, `sys_mount`, `sys_linkat`, `sys_splice`, `socket_pair`, `sys_fchownat`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `src-la/fs/manager.rs:L1911-L1911`：fn install_handle_with_descriptor
    代码片段：`fn install_handle_with_descriptor(&mut self, handle: FileHandle, mut descriptor: FileDescriptor) -> Result<usize, isize> {`
  - `src-la/syscall/fs.rs:L808-L808`：fn sys_mount
    代码片段：`pub fn sys_mount(_special: *const u8, _dir: *const u8, _fstype: *const u8, _flags: usize, _data: *const u8) -> isize { 0 }`

## 附录：核验摘要

- 关键结论数：30
- 含证据关键结论数：30（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
