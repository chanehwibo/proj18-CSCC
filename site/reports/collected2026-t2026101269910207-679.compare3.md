# 2026 T2026101269910207 比较报告

- 对比历史仓库：2023 LostWakeup
- 生成时间：2026-07-02T04:59:10.326105+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2023 LostWakeup（来源：赛事历史作品）：画像相似度 score=9.70；同属 ucore-variant 风格; 语言构成相似度 0.95; OS 维度重合度 1.00; 代码规模接近度 0.80

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2023 LostWakeup 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/pm/pm.c:L8-L12`：关键词命中
    代码片段：`* 2012-06-02     Bernard      the first version * 2018-08-02     Tanek        split run and sleep modes, support custom mode * 2019-04-28     Zero-Free    improve PM mode and de...`
  - `rt-thread/components/drivers/ofw/io.c:L268-L272`：关键词命中
    代码片段：`*          ranges = <0x0 0xe0000000 0x00100000>; * *          serial@4600 { *              device_type = "serial"; *              reg = <0x4600 0x100>;`
  - `src/kernel/main.c:L19-L23`：关键词命中
    代码片段：`void plicinit(void); void plicinithart(void); void virtio_disk_init(void); void binit(void); void fileinit(void);`
  - `src/driver/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 与 2023 LostWakeup 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/pci/procfs.c:L67-L71`：关键词命中
    代码片段：`size_t res = count; rt_ubase_t pm_flags; struct proc_dentry *dentry = file->vnode->data; struct rt_pci_device *pdev = dentry->data;`
  - `rt-thread/components/dfs/dfs_v2/src/dfs_fs.c:L271-L275`：关键词命中
    代码片段：`/* it's the root file system */ /* the mount point dentry is the same as root dentry. */ /* Create root filesystem mount point */ DLOG(msg, "dfs", "mnt", DLOG_MSG, "mnt_parent =...`
  - `src/fs/dev.c:L1-L5`：关键词命中
    代码片段：`#include "common.h" #include "kernel/trap.h" #include "fs/vfs/fs_macro.h" #include "proc/pcb_life.h" #include "memory/allocator.h"`
  - `src/fs/mpage.c:L4-L8`：关键词命中
    代码片段：`#include "lib/riscv.h" #include "lib/radix-tree.h" #include "fs/vfs/fs.h" #include "fs/bio.h" #include "fs/mpage.h"`
- 与 2023 LostWakeup 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/ofw/irq.c:L27-L31`：关键词命中
    代码片段：`int interrupt_cells = -RT_EEMPTY; rt_ofw_prop_read_u32(np, "#interrupt-cells", (rt_uint32_t *)&interrupt_cells); return interrupt_cells;`
  - `rt-thread/components/drivers/pci/irq.c:L11-L15`：关键词命中
    代码片段：`#include <rtthread.h> #define DBG_TAG "pci.irq" #define DBG_LVL DBG_INFO #include <rtdbg.h>`
  - `src/kernel/trap.c:L1-L3`：关键词命中
    代码片段：`#include "kernel/plic.h" #include "kernel/trap.h" #include "kernel/cpu.h"`
  - `include/kernel/trap.h:L9-L13`：关键词命中
    代码片段：`struct file; // per-process data for the trap handling code in trampoline.S. // sits in a page by itself just under the trampoline page in the // user page table. not specially...`
- 与 2023 LostWakeup 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/mm/mm_anon.c:L6-L10`：关键词命中
    代码片段：`* Change Logs: * Date           Author       Notes * 2023-08-19     Shell        Support PRIVATE mapping and COW */`
  - `rt-thread/components/mm/mm_page.c:L1248-L1252`：关键词命中
    代码片段：`pages_alloc_handler = _early_pages_alloc; /* doing the page table bushiness */ if (rt_aspace_load_page(&rt_kernel_space, (void *)init_mpr_align_start, init_mpr_npage)) {`
  - `src/mm/mm.c:L7-L11`：关键词命中
    代码片段：`#include "memory/vma.h" /* allocate a mm_struct, with root pagetable and head_vma */ struct mm_struct *alloc_mm() { struct mm_struct *mm;`
  - `src/mm/vm.c:L17-L21`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
- 与 2023 LostWakeup 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/pm/pm.c:L1-L4`：关键词命中
    代码片段：`/* * Copyright (c) 2006-2024 RT-Thread Development Team * * SPDX-License-Identifier: Apache-2.0`
  - `rt-thread/components/drivers/ofw/io.c:L1-L4`：关键词命中
    代码片段：`/* * Copyright (c) 2006-2022, RT-Thread Development Team * * SPDX-License-Identifier: Apache-2.0`
  - `src/mm/mm.c:L4-L8`：关键词命中
    代码片段：`#include "memory/allocator.h" #include "debug.h" #include "proc/pcb_mm.h" #include "memory/vma.h"`
  - `src/mm/vm.c:L7-L11`：关键词命中
    代码片段：`#include "debug.h" #include "memory/allocator.h" #include "proc/pcb_life.h" #include "proc/pcb_mm.h" #include "lib/list.h"`
- 与 2023 LostWakeup 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/dma/dma.c:L24-L28`：关键词命中
    代码片段：`if (rt_thread_self()) { rt_mutex_take(&ctrl->mutex, RT_WAITING_FOREVER); } }`
  - `rt-thread/components/drivers/led/led.c:L165-L169`：关键词命中
    代码片段：`led->sysdata = RT_NULL; rt_spin_lock_init(&led->spinlock); if (!led->ops->set_period && led->ops->set_state)`
  - `src/mm/mm.c:L18-L22`：关键词命中
    代码片段：`INIT_LIST_HEAD(&mm->head_vma); // semaphore sema_init(&mm->mmap_sem, 1, "mm_semaphore");`
  - `src/fs/bio.c:L1-L5`：关键词命中
    代码片段：`#include "common.h" #include "param.h" #include "atomic/spinlock.h" #include "lib/riscv.h" #include "fs/bio.h"`
- 与 2023 LostWakeup 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/lwp/lwp_syscall.c:L7-L11`：关键词命中
    代码片段：`* Date           Author       Notes * 2018-06-10     Bernard      first version * 2021-02-03     lizhirui     add limit condition for network syscall and add 64-bit arch support...`
  - `rt-thread/components/lwp/syscall_generic.h:L7-L11`：关键词命中
    代码片段：`* Date           Author       Notes * 2022-11-10     RT-Thread    The first version * 2023-03-13     WangXiaoyao  syscall metadata as structure */ #ifndef __SYSCALL_DATA_H__`
  - `src/kernel/syscall.c:L10-L14`：关键词命中
    代码片段：`#include "syscall_gen/syscall_num.h" #include "debug.h" #include "kernel/syscall.h" extern atomic_t pages_cnt;`
  - `include/kernel/syscall.h:L4-L8`：关键词命中
    代码片段：`struct file; // syscall.c uint64 argraw(int); int argint(int, int *);`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 未发现达到阈值的路径、符号、结构体/宏或片段级代码相似线索；当前仅保留功能维度重合证据。

## 相似点

- 与 2023 LostWakeup 同属 ucore-variant 风格。（置信度：medium）
- 与 2023 LostWakeup 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/pm/pm.c:L8-L12`：关键词命中
    代码片段：`* 2012-06-02     Bernard      the first version * 2018-08-02     Tanek        split run and sleep modes, support custom mode * 2019-04-28     Zero-Free    improve PM mode and de...`
  - `rt-thread/components/drivers/ofw/io.c:L268-L272`：关键词命中
    代码片段：`*          ranges = <0x0 0xe0000000 0x00100000>; * *          serial@4600 { *              device_type = "serial"; *              reg = <0x4600 0x100>;`
- 与 2023 LostWakeup 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/pci/procfs.c:L67-L71`：关键词命中
    代码片段：`size_t res = count; rt_ubase_t pm_flags; struct proc_dentry *dentry = file->vnode->data; struct rt_pci_device *pdev = dentry->data;`
  - `rt-thread/components/dfs/dfs_v2/src/dfs_fs.c:L271-L275`：关键词命中
    代码片段：`/* it's the root file system */ /* the mount point dentry is the same as root dentry. */ /* Create root filesystem mount point */ DLOG(msg, "dfs", "mnt", DLOG_MSG, "mnt_parent =...`
- 与 2023 LostWakeup 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/ofw/irq.c:L27-L31`：关键词命中
    代码片段：`int interrupt_cells = -RT_EEMPTY; rt_ofw_prop_read_u32(np, "#interrupt-cells", (rt_uint32_t *)&interrupt_cells); return interrupt_cells;`
  - `rt-thread/components/drivers/pci/irq.c:L11-L15`：关键词命中
    代码片段：`#include <rtthread.h> #define DBG_TAG "pci.irq" #define DBG_LVL DBG_INFO #include <rtdbg.h>`
- 与 2023 LostWakeup 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/mm/mm_anon.c:L6-L10`：关键词命中
    代码片段：`* Change Logs: * Date           Author       Notes * 2023-08-19     Shell        Support PRIVATE mapping and COW */`
  - `rt-thread/components/mm/mm_page.c:L1248-L1252`：关键词命中
    代码片段：`pages_alloc_handler = _early_pages_alloc; /* doing the page table bushiness */ if (rt_aspace_load_page(&rt_kernel_space, (void *)init_mpr_align_start, init_mpr_npage)) {`
- 与 2023 LostWakeup 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/pm/pm.c:L1-L4`：关键词命中
    代码片段：`/* * Copyright (c) 2006-2024 RT-Thread Development Team * * SPDX-License-Identifier: Apache-2.0`
  - `rt-thread/components/drivers/ofw/io.c:L1-L4`：关键词命中
    代码片段：`/* * Copyright (c) 2006-2022, RT-Thread Development Team * * SPDX-License-Identifier: Apache-2.0`
- 与 2023 LostWakeup 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/dma/dma.c:L24-L28`：关键词命中
    代码片段：`if (rt_thread_self()) { rt_mutex_take(&ctrl->mutex, RT_WAITING_FOREVER); } }`
  - `rt-thread/components/drivers/led/led.c:L165-L169`：关键词命中
    代码片段：`led->sysdata = RT_NULL; rt_spin_lock_init(&led->spinlock); if (!led->ops->set_period && led->ops->set_state)`
- 与 2023 LostWakeup 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/lwp/lwp_syscall.c:L7-L11`：关键词命中
    代码片段：`* Date           Author       Notes * 2018-06-10     Bernard      first version * 2021-02-03     lizhirui     add limit condition for network syscall and add 64-bit arch support...`
  - `rt-thread/components/lwp/syscall_generic.h:L7-L11`：关键词命中
    代码片段：`* Date           Author       Notes * 2022-11-10     RT-Thread    The first version * 2023-03-13     WangXiaoyao  syscall metadata as structure */ #ifndef __SYSCALL_DATA_H__`
- 与 2023 LostWakeup 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2023 LostWakeup 的语言构成不同：待测作品为 {'json': 362, 'markdown': 13785, 'build': 318, 'c': 860328, 'asm': 33452, 'text': 5182, 'yaml': 3094, 'rust': 3462, 'cpp': 1671}，历史样本为 {'json': 20, 'build': 13569, 'markdown': 7891, 'text': 17100, 'c': 1107103, 'make': 88, 'asm': 8391, 'cpp': 38149, 'yaml': 14}。（置信度：medium）

## 可能创新点

- 待测作品包含 12 个历史样本 2023 LostWakeup 中未见的独特函数/类型定义（如 `rt_interrupt_context`, `lwp_siginfo_ext`, `lwp_sigqueue`, `lwp_siginfo`, `loongarch_user_after_exit`, `lwp_thread_signal`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `rt-thread/include/rtdef.h:L755-L755`：struct rt_interrupt_context
    代码片段：`typedef struct rt_interrupt_context {`
  - `rt-thread/include/rtdef.h:L801-L801`：struct lwp_siginfo_ext
    代码片段：`typedef struct lwp_siginfo_ext {`

## 附录：核验摘要

- 关键结论数：15
- 含证据关键结论数：15（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
