# 2026 T2026105749910760 比较报告

- 对比历史仓库：2025 智核速启队
- 生成时间：2026-07-02T09:03:30.746781+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 智核速启队（来源：赛事历史作品）：画像相似度 score=9.73；同属 ucore-variant 风格; 语言构成相似度 0.93; OS 维度重合度 1.00; 代码规模接近度 0.87

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 智核速启队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/arch/qemu/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/fs/virtio_disk.c:L1-L4`：关键词命中
    代码片段：`// // driver for qemu's virtio disk device. // uses qemu's mmio interface to virtio. //`
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/driver/riscv/virt.c:L1-L3`：关键词命中
    代码片段：`// virt.c - 最小化RISC-V VirtIO磁盘驱动 #include "virt.h" #include "print.h"`
- 与 2025 智核速启队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `kernel/fs/file.c:L154-L158`：关键词命中
    代码片段：`size_t fileread(struct file *f, int user_dst, uint64 addr, size_t n, int64_t off) { size_t r = 0;`
  - `kernel/fs/fs.c:L201-L205`：关键词命中
    代码片段：`void dir_init(void) { struct inode *ip; if ((ip = namei("/dev/null")) == NULL) vfs_ext4_mknod("/dev/null", T_CHR, (DEVNULL << 8) | 0);`
  - `kernel/fs/ext4.c:L170-L174`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false;`
- 与 2025 智核速启队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sched/trap.c:L115-L119`：关键词命中
    代码片段：`// trampoline已经换栈和页表 // // handle an interrupt, exception, or system call from user space. // called from trampoline.S //`
  - `kernel/arch/qemu/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/test.c:L500-L504`：关键词命中
    代码片段：`void test_interrupt_nesting(void) { printf("Testing interrupt nesting...\n"); int old_intr = intr_get();`
  - `kernel/vmem.c:L46-L50`：关键词命中
    代码片段：`mappages(kernel_pagetable, VIRTIO0, VIRTIO0, PGSIZE, PTE_R | PTE_W); /*PLIC映射*/ mappages(kernel_pagetable, PLIC, PLIC, 0x400000, PTE_R | PTE_W);`
- 与 2025 智核速启队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mm/vm.c:L8-L12`：关键词命中
    代码片段：`#include "xv6fs.h" #include "proc.h" #include "kalloc.h" /*`
  - `kernel/mm/mmap.c:L7-L11`：关键词命中
    代码片段：`static uint64 allocvma() { struct vma_struct *vma = (struct vma_struct *)kalloc(); if (vma == 0) { return 0;`
  - `kernel/vma.c:L38-L42`：关键词命中
    代码片段：`return NULL; } memset(vma, 0, PGSIZE); vma->type = NONE; vma->prev = vma->next = vma;`
  - `kernel/exec.c:L32-L36`：关键词命中
    代码片段：`void alloc_aux(uint64 *aux, uint64 atid, uint64 value); int loadaux(pgtbl_t pt, uint64 sp, uint64 stackbase, uint64 *aux); void debug_print_stack(pgtbl_t pagetable, uint64 sp, u...`
- 与 2025 智核速启队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sched/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "sched.h"`
  - `kernel/sched/trap.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "thread.h"`
  - `kernel/thread.c:L1-L3`：关键词命中
    代码片段：`#include "thread.h" #include "pmem.h" #include "vmem.h"`
  - `include/kernel/thread.h:L12-L16`：关键词命中
    代码片段：`// 前向声明 typedef struct proc proc_t; typedef struct trapframe trapframe_t; typedef struct vma vma_t;`
- 与 2025 智核速启队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/atomic/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "cond.h" #include "semaphore.h" /// @brief init a semaphore`
  - `kernel/include/semaphore.h:L3-L7`：关键词命中
    代码片段：`#include "types.h" #include "spinlock.h" #include "cond.h"`
  - `kernel/cpu.c:L1-L4`：关键词命中
    代码片段：`#include "cpu.h" #include "spinlock.h" #ifdef RISCV #include "riscv.h"`
  - `kernel/pmem.c:L4-L8`：关键词命中
    代码片段：`#include "string.h" #include "types.h" #include "spinlock.h" #include <stdbool.h> #if defined RISCV`
- 与 2025 智核速启队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "thread.h"`
  - `kernel/sched/trap.c:L155-L159`：关键词命中
    代码片段：`thread_exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. // p->trapframe->epc += 4;`
  - `kernel/syscall.c:L1-L5`：关键词命中
    代码片段：`#include "types.h" #include "print.h" #include "trap.h" //这个必须要再hsai_trap.h前。syscall(struct trapframe *trapframe)参数表中的trapframe对外部不可见，所以先声明trapframe #include "hsai_trap.h" #incl...`
  - `hsai/hsai_trap.c:L31-L35`：关键词命中
    代码片段：`register uint64 a2 asm("a2") = arg2; register uint64 a7 asm("a7") = which; asm volatile("ecall" : "=r"(a0) : "r"(a0), "r"(a1), "r"(a2), "r"(a7)`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 智核速启队 在“文件系统”维度发现片段级代码相似度 0.93 （token=0.89, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：enode, ext4_inode_is_type, ext4_inode_mode_directory, false, has_children, if, inode, sb。（置信度：high）
  证据：
  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `kernel/fs/ext4.c:L170-L174`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false;`
- 与 2025 智核速启队 在“系统调用”维度发现片段级代码相似度 0.84 （token=0.75, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：a0, a1, a2, a7, asm, register, uint64, volatile。（置信度：high）
  证据：
  - `kernel/include/sbi.h:L301-L305`：关键词命中
    代码片段：`register uint64 a7 asm("a7") = eid; register uint64 ret asm("a0"); asm volatile("ecall" : "=r"(ret) : "0"(a0), "r"(a1), "r"(a2), "r"(a3), "r"(a7));`
  - `hal/riscv/sbi.c:L25-L29`：关键词命中
    代码片段：`register uint64 a2 asm("a2") = arg2; register uint64 a7 asm("a7") = which; asm volatile("ecall" : "=r"(a0) : "r"(a0), "r"(a1), "r"(a2), "r"(a7)`
- 与 2025 智核速启队 在“文件系统”维度发现片段级代码相似度 0.73 （token=0.68, structure=0.82）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：addr, f, file, fileread, int, n, r, struct。（置信度：medium）
  证据：
  - `kernel/fs/file.c:L154-L158`：关键词命中
    代码片段：`size_t fileread(struct file *f, int user_dst, uint64 addr, size_t n, int64_t off) { size_t r = 0;`
  - `kernel/fs/file.c:L330-L334`：关键词命中
    代码片段：`* @return int 状态码，0成功，-1失败 */ int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
- 宏名重合：与 2025 智核速启队 在“文件系统”维度发现 4 个同名定义：EXT4_MP_LOCK, EXT4_MP_UNLOCK, EXT4_DIR_ENTRY_OFFSET_TERM, ext4_balloc_bitmap_csum。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/fs/ext4.c:L61-L61`：macro EXT4_MP_LOCK
    代码片段：`#define EXT4_MP_LOCK(_m)                                                       \`
  - `kernel/fs/ext4.c:L73-L73`：macro EXT4_MP_LOCK
    代码片段：`#define EXT4_MP_LOCK(_m)            \`
  - `kernel/fs/ext4.c:L68-L68`：macro EXT4_MP_UNLOCK
    代码片段：`#define EXT4_MP_UNLOCK(_m)                                                     \`
  - `kernel/fs/ext4.c:L81-L81`：macro EXT4_MP_UNLOCK
    代码片段：`#define EXT4_MP_UNLOCK(_m)            \`
- 宏名重合：与 2025 智核速启队 在“内存管理”维度发现 4 个同名定义：ext4_balloc_bitmap_csum, ext4_balloc_verify_bitmap_csum, ext4_ialloc_bitmap_csum, ext4_ialloc_verify_bitmap_csum。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/fs/ext4_balloc.c:L103-L103`：macro ext4_balloc_bitmap_csum
    代码片段：`#define ext4_balloc_bitmap_csum(...) 0`
  - `kernel/fs/ext4_balloc.c:L96-L96`：macro ext4_balloc_bitmap_csum
    代码片段：`#define ext4_balloc_bitmap_csum(...) 0`
  - `kernel/fs/ext4_balloc.c:L149-L149`：macro ext4_balloc_verify_bitmap_csum
    代码片段：`#define ext4_balloc_verify_bitmap_csum(...) true`
  - `kernel/fs/ext4_balloc.c:L132-L132`：macro ext4_balloc_verify_bitmap_csum
    代码片段：`#define ext4_balloc_verify_bitmap_csum(...) true`
- 宏名重合：与 2025 智核速启队 在“同步机制”维度发现 4 个同名定义：EXT4_MP_LOCK, EXT4_MP_UNLOCK, EXT4_EXT_GET_LEN, ext4_ext_block_csum。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/fs/ext4.c:L61-L61`：macro EXT4_MP_LOCK
    代码片段：`#define EXT4_MP_LOCK(_m)                                                       \`
  - `kernel/fs/ext4.c:L73-L73`：macro EXT4_MP_LOCK
    代码片段：`#define EXT4_MP_LOCK(_m)            \`
  - `kernel/fs/ext4.c:L68-L68`：macro EXT4_MP_UNLOCK
    代码片段：`#define EXT4_MP_UNLOCK(_m)                                                     \`
  - `kernel/fs/ext4.c:L81-L81`：macro EXT4_MP_UNLOCK
    代码片段：`#define EXT4_MP_UNLOCK(_m)            \`
- 结构体/类型重合：与 2025 智核速启队 在“文件系统”维度发现 4 个同名定义：ext4_dx_sort_entry, ext4_extent_path, ext4_extent, ext4_extent_index。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/fs/ext4_dir_idx.c:L197-L197`：struct ext4_dx_sort_entry
    代码片段：`struct ext4_dx_sort_entry {`
  - `kernel/fs/ext4_dir_idx.c:L161-L161`：struct ext4_dx_sort_entry
    代码片段：`struct ext4_dx_sort_entry {`
  - `kernel/fs/ext4_extent.c:L59-L59`：struct ext4_extent_path
    代码片段：`struct ext4_extent_path {`
  - `kernel/fs/ext4_extent.c:L58-L58`：struct ext4_extent_path
    代码片段：`struct ext4_extent_path {`
- 宏名重合：与 2025 智核速启队 在“设备驱动”维度发现 2 个同名定义：BACKSPACE, INPUT_BUF_SIZE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/arch/qemu/console.c:L25-L25`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100`
  - `kernel/console.c:L34-L34`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100`
  - `kernel/arch/qemu/console.c:L48-L48`：macro INPUT_BUF_SIZE
    代码片段：`#define INPUT_BUF_SIZE 128`
  - `kernel/console.c:L62-L62`：macro INPUT_BUF_SIZE
    代码片段：`#define INPUT_BUF_SIZE 128`
- 文件路径重合：与 2025 智核速启队 在“文件系统”维度出现同路径源码路径 `kernel/fs/ext4.c` / `kernel/fs/ext4.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `kernel/fs/ext4.c:L170-L174`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false;`
- 文件路径重合：与 2025 智核速启队 在“文件系统”维度出现同路径源码路径 `kernel/fs/file.c` / `kernel/fs/file.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/file.c:L154-L158`：关键词命中
    代码片段：`size_t fileread(struct file *f, int user_dst, uint64 addr, size_t n, int64_t off) { size_t r = 0;`
  - `kernel/fs/file.c:L330-L334`：关键词命中
    代码片段：`* @return int 状态码，0成功，-1失败 */ int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
- 文件路径重合：与 2025 智核速启队 在“系统调用”维度出现同路径源码路径 `kernel/syscall.c` / `kernel/syscall.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "thread.h"`
  - `kernel/syscall.c:L1-L5`：关键词命中
    代码片段：`#include "types.h" #include "print.h" #include "trap.h" //这个必须要再hsai_trap.h前。syscall(struct trapframe *trapframe)参数表中的trapframe对外部不可见，所以先声明trapframe #include "hsai_trap.h" #incl...`

## 相似点

- 与 2025 智核速启队 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 智核速启队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/arch/qemu/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/fs/virtio_disk.c:L1-L4`：关键词命中
    代码片段：`// // driver for qemu's virtio disk device. // uses qemu's mmio interface to virtio. //`
- 与 2025 智核速启队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/ext4.c:L153-L157`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
  - `kernel/fs/file.c:L154-L158`：关键词命中
    代码片段：`size_t fileread(struct file *f, int user_dst, uint64 addr, size_t n, int64_t off) { size_t r = 0;`
- 与 2025 智核速启队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sched/trap.c:L115-L119`：关键词命中
    代码片段：`// trampoline已经换栈和页表 // // handle an interrupt, exception, or system call from user space. // called from trampoline.S //`
  - `kernel/arch/qemu/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
- 与 2025 智核速启队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mm/vm.c:L8-L12`：关键词命中
    代码片段：`#include "xv6fs.h" #include "proc.h" #include "kalloc.h" /*`
  - `kernel/mm/mmap.c:L7-L11`：关键词命中
    代码片段：`static uint64 allocvma() { struct vma_struct *vma = (struct vma_struct *)kalloc(); if (vma == 0) { return 0;`
- 与 2025 智核速启队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sched/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "sched.h"`
  - `kernel/sched/trap.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "thread.h"`
- 与 2025 智核速启队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/atomic/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "cond.h" #include "semaphore.h" /// @brief init a semaphore`
  - `kernel/include/semaphore.h:L3-L7`：关键词命中
    代码片段：`#include "types.h" #include "spinlock.h" #include "cond.h"`
- 与 2025 智核速启队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "thread.h"`
  - `kernel/sched/trap.c:L155-L159`：关键词命中
    代码片段：`thread_exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. // p->trapframe->epc += 4;`
- 与 2025 智核速启队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 智核速启队 的语言构成不同：待测作品为 {'build': 474, 'markdown': 8369, 'make': 1, 'c': 54880, 'asm': 713}，历史样本为 {'json': 20, 'build': 764, 'markdown': 4306, 'c': 68447, 'asm': 823, 'text': 55}。（置信度：medium）

## 可能创新点

- 待测作品在“同步机制”维度的实现深度显著高于历史样本 2025 智核速启队（6 条证据 vs 3 条），可能存在更完整或更精细的实现。（置信度：low）
  证据：
  - `kernel/atomic/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "cond.h" #include "semaphore.h" /// @brief init a semaphore`
  - `kernel/include/semaphore.h:L3-L7`：关键词命中
    代码片段：`#include "types.h" #include "spinlock.h" #include "cond.h"`
- 待测作品包含 184 个历史样本 2025 智核速启队 中未见的独特函数/类型定义（如 `do_copy_file_range`, `rw_sharp_2off`, `procfs_write`, `rw_sharp_outoff`, `generic_utimensat`, `procfs_read`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/fs/sysfile.c:L2063-L2063`：fn do_copy_file_range
    代码片段：`uint64 do_copy_file_range(struct file *in_f, struct file *out_f, __nullable off_t *offin_addr, __nullable off_t *offout_addr, size_t len) {`
  - `kernel/fs/sysfile.c:L1628-L1628`：fn rw_sharp_2off
    代码片段：`static int rw_sharp_2off(struct file *out_f, struct file *in_f, void *kbuf, off_t in_off, off_t out_off, uint64 count) {`

## 附录：核验摘要

- 关键结论数：27
- 含证据关键结论数：27（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
