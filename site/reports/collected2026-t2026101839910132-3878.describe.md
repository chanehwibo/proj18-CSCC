# 2026 T2026101839910132 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026101839910132-3878`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：2138
- 代码/文本行数：596867
- 主要语言：rust 558755 LOC, markdown 17507 LOC, toml 9892 LOC, build 7097 LOC, text 1710 LOC

## 总览

2026 T2026101839910132 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 558755 LOC, markdown: 17507 LOC, toml: 9892 LOC, build: 7097 LOC。仓库包含 2138 个已扫描文件、约 596867 行可分析文本，当前抽取到 38296 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（96/100）
- 已确认 OS 维度：7/7；高置信维度：6/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 76/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | medium | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, GCore/Makefile, GCore/os/Cargo.toml, GCore/os/Makefile, GCore/user/Cargo.toml。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`MODE ?= release BLK_MODE ?= virt LOG ?= error`
  - `GCore/Makefile:L1-L3`：构建入口
    代码片段：`MODE ?= release FS_MODE ?= fat32 BLK_MODE ?= virt`
  - `GCore/os/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "os" version = "0.1.0"`
  - `GCore/os/Makefile:L1-L3`：构建入口
    代码片段：`ARCHS = rv64 CURR_ARCH ?= la64 FS_MODE ?= ext4`
  - `GCore/user/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "user_lib" version = "0.1.0"`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：enum AuxvType, struct AuxvEntry, fn new, struct ELFInfo, fn load_elf_interp。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `GCore/os/src/task/mod.rs` | L5-L9 | 关键词命中 |
| `GCore/os/src/task/task.rs` | L517-L521 | 关键词命中 |
| `GCore/os/src/task/signal.rs` | L16-L20 | 关键词命中 |
| `GCore/os/src/task/elf.rs` | L14-L18 | enum AuxvType |
| `GCore/os/src/task/elf.rs` | L63-L67 | struct AuxvEntry |
| `GCore/os/src/task/elf.rs` | L69-L73 | fn new |

### 关键代码片段

  - `GCore/os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `GCore/os/src/task/task.rs:L517-L521`：关键词命中
    代码片段：`// 销毁所有其他同一线程组的任务 manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
  - `GCore/os/src/task/signal.rs:L16-L20`：关键词命中
    代码片段：`}; use crate::syscall::errno::*; use crate::task::manager::wait_with_timeout; use crate::task::{block_current_and_run_next, exit_current_and_run_next, exit_group_and_run_next};...`
  - `GCore/os/src/task/elf.rs:L14-L18`：enum AuxvType
    代码片段：`#[allow(non_camel_case_types, unused)] #[repr(usize)] pub enum AuxvType { NULL = 0, IGNORE = 1,`

### 相关符号

`enum AuxvType` at `GCore/os/src/task/elf.rs:L14`、`struct AuxvEntry` at `GCore/os/src/task/elf.rs:L63`、`fn new` at `GCore/os/src/task/elf.rs:L69`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn init, enum ZramError, struct ZramTracker, fn drop, struct Zram。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `GCore/os/src/mm/mod.rs` | L1-L4 | 关键词命中 |
| `GCore/os/src/mm/map_area.rs` | L1-L5 | 关键词命中 |
| `GCore/os/src/mm/memory_set.rs` | L1-L4 | 关键词命中 |
| `GCore/os/src/mm/mod.rs` | L36-L40 | fn init |
| `GCore/os/src/mm/zram.rs` | L6-L10 | enum ZramError |
| `GCore/os/src/mm/zram.rs` | L17-L21 | struct ZramTracker |

### 关键代码片段

  - `GCore/os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `GCore/os/src/mm/map_area.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::Debug; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
  - `GCore/os/src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use super::map_area::*; use super::page_table::PageTable; use super::{PhysAddr, PhysPageNum, StepByOne, VirtAddr, VirtPageNum}; use crate::config::*;`
  - `GCore/os/src/mm/mod.rs:L36-L40`：fn init
    代码片段：`}; pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`

### 相关符号

`fn init` at `GCore/os/src/mm/mod.rs:L36`、`enum ZramError` at `GCore/os/src/mm/zram.rs:L6`、`struct ZramTracker` at `GCore/os/src/mm/zram.rs:L17`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：medium）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口或兼容层线索，但源码片段显示部分调用仍是 stub/未实现，应按接口线索而非完整系统调用实现解读。（置信度：medium）
  - 相关符号包括：fn sys_splice, fn __openat, fn sys_getcwd, fn sys_lseek, fn sys_read。（置信度：medium）
  - 静态识别到 162 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `GCore/os/src/syscall/fs.rs` | L683-L687 | 关键词命中 |
| `GCore/os/src/syscall/mod.rs` | L168-L172 | 关键词命中 |
| `GCore/os/src/syscall/errno.rs` | L82-L86 | 关键词命中 |
| `GCore/os/src/syscall/fs.rs` | L24-L28 | fn sys_splice |
| `GCore/os/src/syscall/fs.rs` | L142-L146 | fn __openat |
| `GCore/os/src/syscall/fs.rs` | L157-L161 | fn sys_getcwd |

### 关键代码片段

  - `GCore/os/src/syscall/fs.rs:L683-L687`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `GCore/os/src/syscall/mod.rs:L168-L172`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`
  - `GCore/os/src/syscall/errno.rs:L82-L86`：关键词命中
    代码片段：`/// Invalid system call number /// # Note /// This error code is special: arch syscall entry code will return /// -ENOSYS if users try to call a syscall that doesn't exist.  To...`
  - `GCore/os/src/syscall/fs.rs:L24-L28`：fn sys_splice
    代码片段：`// todo pub fn sys_splice( fd_in: usize, off_in: *mut usize,`

### 相关符号

`fn sys_splice` at `GCore/os/src/syscall/fs.rs:L24`、`fn __openat` at `GCore/os/src/syscall/fs.rs:L142`、`fn sys_getcwd` at `GCore/os/src/syscall/fs.rs:L157`、`fn sys_splice` at `GCore/os/src/syscall/fs.rs:L24`、`fn sys_getcwd` at `GCore/os/src/syscall/fs.rs:L157`、`fn sys_lseek` at `GCore/os/src/syscall/fs.rs:L188`、`fn sys_read` at `GCore/os/src/syscall/fs.rs:L213`、`fn sys_write` at `GCore/os/src/syscall/fs.rs:L236`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：fn flush_preload, fn sinitproc, fn einitproc, fn sbash, fn ebash。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `GCore/os/src/fs/mod.rs` | L13-L17 | 关键词命中 |
| `GCore/os/src/fs/vfs.rs` | L11-L15 | 关键词命中 |
| `GCore/os/src/fs/cache.rs` | L198-L202 | 关键词命中 |
| `GCore/os/src/fs/mod.rs` | L49-L53 | fn flush_preload |
| `GCore/os/src/fs/mod.rs` | L51-L55 | fn sinitproc |
| `GCore/os/src/fs/mod.rs` | L52-L56 | fn einitproc |

### 关键代码片段

  - `GCore/os/src/fs/mod.rs:L13-L17`：关键词命中
    代码片段：`pub mod dirent; pub mod file_descriptor; mod inode; mod timestamp; mod vfs;`
  - `GCore/os/src/fs/vfs.rs:L11-L15`：关键词命中
    代码片段：`use super::filesystem::{pre_mount, FS_Type}; pub trait VFS: DowncastSync { fn close(&self) { unreachable!()`
  - `GCore/os/src/fs/cache.rs:L198-L202`：关键词命中
    代码片段：`/// 每次内核尝试分配这个页的时候，这个数字（优先级）会增加1,并且最多为1 /// 每次发生oom的情况，这个数字（优先级）会减少1，并且至少为0 /// 当其变为0的时候，并且Arc的强引用数量为1（one in inode），这个PageCache会被释放 priority: usize, page_ptr: &'static mut [u8;...`
  - `GCore/os/src/fs/mod.rs:L49-L53`：fn flush_preload
    代码片段：`} #[allow(unused)] pub fn flush_preload() { extern "C" { fn sinitproc();`

### 相关符号

`fn flush_preload` at `GCore/os/src/fs/mod.rs:L49`、`fn sinitproc` at `GCore/os/src/fs/mod.rs:L51`、`fn einitproc` at `GCore/os/src/fs/mod.rs:L52`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn get_single_cache, fn get_all_caches, fn get_single_cache, fn get_all_caches, fn alloc_buffer_cache。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `GCore/os/src/timer.rs` | L309-L313 | 关键词命中 |
| `GCore/os/src/fs/vfs.rs` | L39-L43 | 关键词命中 |
| `GCore/os/src/fs/swap.rs` | L1-L4 | 关键词命中 |
| `GCore/os/src/net/tcp.rs` | L406-L410 | fn get_single_cache |
| `GCore/os/src/net/tcp.rs` | L407-L411 | fn get_all_caches |
| `GCore/os/src/net/udp.rs` | L283-L287 | fn get_single_cache |

### 关键代码片段

  - `GCore/os/src/timer.rs:L309-L313`：关键词命中
    代码片段：`} use core::sync::atomic::{AtomicU64}; /// 启动以来的时间源（秒），例如通过读取 mtime / TSC`
  - `GCore/os/src/fs/vfs.rs:L39-L43`：关键词命中
    代码片段：`pub fn open_fs( block_device: Arc<dyn BlockDevice>, index_cache_mgr: Arc<spin::Mutex<BlockCacheManager>>, ) -> Arc<dyn VFS> { let fs_type = pre_mount();`
  - `GCore/os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::{config::PAGE_SIZE, drivers::BLOCK_DEVICE, hal::BLOCK_SZ};`
  - `GCore/os/src/net/tcp.rs:L406-L410`：fn get_single_cache
    代码片段：`fn truncate_size(&self, _new_size: usize) -> Result<(), isize>{Err(EINVAL)} fn set_timestamp(&self, _ctime: Option<usize>, _atime: Option<usize>, _mtime: Option<usize>){} fn get...`

### 相关符号

`fn get_single_cache` at `GCore/os/src/net/tcp.rs:L406`、`fn get_all_caches` at `GCore/os/src/net/tcp.rs:L407`、`fn get_single_cache` at `GCore/os/src/net/udp.rs:L283`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn get_time_sec, fn get_time_ms, fn get_time_us, fn get_time_ns, fn current_time_duration。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `GCore/os/src/timer.rs` | L19-L23 | 关键词命中 |
| `GCore/os/src/hal/arch/riscv/trap/mod.rs` | L17-L21 | 关键词命中 |
| `GCore/os/src/hal/arch/riscv/trap/context.rs` | L73-L77 | 关键词命中 |
| `GCore/os/src/timer.rs` | L17-L21 | fn get_time_sec |
| `GCore/os/src/timer.rs` | L24-L28 | fn get_time_ms |
| `GCore/os/src/timer.rs` | L31-L35 | fn get_time_us |

### 关键代码片段

  - `GCore/os/src/timer.rs:L19-L23`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `GCore/os/src/hal/arch/riscv/trap/mod.rs:L17-L21`：关键词命中
    代码片段：`use riscv::register::{ mtvec::TrapMode, scause::{self, Exception, Interrupt, Trap}, sepc, sie, stval, stvec, };`
  - `GCore/os/src/hal/arch/riscv/trap/context.rs:L73-L77`：关键词命中
    代码片段：`#[repr(C)] #[derive(Debug, Clone, Copy)] /// The trap cotext containing the user context and the supervisor level pub struct TrapContext { /// The registers to be preserved.`
  - `GCore/os/src/timer.rs:L17-L21`：fn get_time_sec
    代码片段：`/// Return current time measured by seconds. pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time...`

### 相关符号

`fn get_time_sec` at `GCore/os/src/timer.rs:L17`、`fn get_time_ms` at `GCore/os/src/timer.rs:L24`、`fn get_time_us` at `GCore/os/src/timer.rs:L31`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct KernelOutput, fn write_str, fn print, fn log_init, struct Logger。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `GCore/os/src/console.rs` | L32-L36 | 关键词命中 |
| `GCore/os/src/drivers/mod.rs` | L1-L4 | 关键词命中 |
| `GCore/os/src/drivers/block/mod.rs` | L3-L7 | 关键词命中 |
| `GCore/os/src/console.rs` | L5-L9 | struct KernelOutput |
| `GCore/os/src/console.rs` | L8-L12 | fn write_str |
| `GCore/os/src/console.rs` | L25-L29 | fn print |

### 关键代码片段

  - `GCore/os/src/console.rs:L32-L36`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `GCore/os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
  - `GCore/os/src/drivers/block/mod.rs:L3-L7`：关键词命中
    代码片段：`mod sata_blk; #[cfg(feature = "block_virt")] mod virtio_blk; #[cfg(feature = "block_virt_pci")] mod virtio_blk_pci;`
  - `GCore/os/src/console.rs:L5-L9`：struct KernelOutput
    代码片段：`// won't require lock, but unlikely to cause problem struct KernelOutput; impl Write for KernelOutput {`

### 相关符号

`struct KernelOutput` at `GCore/os/src/console.rs:L5`、`fn write_str` at `GCore/os/src/console.rs:L8`、`fn print` at `GCore/os/src/console.rs:L25`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
