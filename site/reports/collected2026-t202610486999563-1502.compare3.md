# 2026 T202610486999563 比较报告

- 对比历史仓库：2025 BlueStar
- 生成时间：2026-07-02T05:01:33.591902+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 BlueStar（来源：赛事历史作品）：画像相似度 score=9.71；同属 ucore-variant 风格; 语言构成相似度 0.94; OS 维度重合度 1.00; 代码规模接近度 0.83

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 BlueStar 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = u64;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
  - `kernel/src/console.rs:L8-L12`：关键词命中
    代码片段：`#[macro_export] /// Kernel print function,it can output to serial macro_rules! kprint { ($fmt: literal $(, $($arg: tt)+)?) => {`
  - `kernel/src/driver/mod.rs:L1-L2`：关键词命中
    代码片段：`mod virtio_blk; pub use self::virtio_blk::*;`
- 与 2025 BlueStar 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/syscall/src/fs_domain.rs:L22-L26`：关键词命中
    代码片段：`SYS_FCNTL => ctx .dispatcher .sys_fcntl(args, ctx.procs, ctx.scheduler, ctx.vfs), SYS_IOCTL => ctx.dispatcher.sys_ioctl(args, ctx.procs), SYS_FLOCK => ctx.dispatcher.sys_flock(a...`
  - `crates/vfs/src/lib.rs:L295-L299`：关键词命中
    代码片段：`pub fn new() -> Self { let root = Arc::new(Node::directory("/")); let mut vfs = Self { root, mounts: Vec::new(),`
  - `kernel/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`pub mod vfs; pub mod fs_backend; pub mod component;`
  - `kernel/src/fs/vfs/api.rs:L5-L9`：关键词命中
    代码片段：`use log::error; use crate::task::{TASK_MANAER, TASK_MANAGER_INIT}; use crate::fs::vfs::{File, KStat, MountFs, OpenFlags, ROOTFS,  VfsFsError, VfsStat}; use alloc::format; use al...`
- 与 2025 BlueStar 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/device/blk.rs:L89-L93`：关键词命中
    代码片段：`} /// Acknowledges a pending interrupt, if any. /// /// Returns true if there was an interrupt to acknowledge.`
  - `vendor/virtio-drivers/src/device/rng.rs:L52-L56`：关键词命中
    代码片段：`} /// Acknowledge interrupt. pub fn ack_interrupt(&mut self) -> InterruptStatus { self.transport.ack_interrupt()`
  - `kernel/src/trap/mod.rs:L4-L8`：关键词命中
    代码片段：`use crate::{config::*, task::TASK_MANAER, time::set_next_time_interupt}; use log::{debug, error, }; use riscv::register::{scause::{self, Exception, Trap},  sstatus::{self, SPP,...`
  - `kernel/src/time/timer.rs:L27-L31`：关键词命中
    代码片段：`///设置下一次时钟中断(不带中断检查，太耗时间，所有耗时操作其实都不应该出现在这里)，mtimecmp使用原始tick计数 pub fn set_next_time_interupt(){ //需要考虑调用误差，即使错过也没事，只是提前触发中断(mtime < mtimecmp)`
- 与 2025 BlueStar 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/mm/src/lib.rs:L161-L165`：关键词命中
    代码片段：`program_break: usize, next_mapping_base: usize, page_table: Option<PageTableSpace>, dirty: bool, frame_allocator: Mutex<FrameAllocator>,`
  - `vendor/virtio-drivers/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM //! to interact with VirtIO devices provided by the VMM (such as QEMU or...`
  - `kernel/src/memory/mod.rs:L2-L6`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod memset;`
  - `kernel/src/memory/memset.rs:L6-L10`：关键词命中
    代码片段：`use log::{debug, error, trace}; use core::arch::asm; use riscv::register::satp; use crate::fs::vfs::File; use crate::task::{TASK_MANAER};`
- 与 2025 BlueStar 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/syscall/src/task_domain.rs:L12-L16`：关键词命中
    代码片段：`Some(match sysno { SYS_SET_TID_ADDRESS => ctx.dispatcher.sys_set_tid_address(args, ctx.procs), SYS_SCHED_YIELD => ctx.dispatcher.sys_sched_yield(ctx.scheduler), SYS_EXIT => ctx...`
  - `crates/proc/src/lib.rs:L184-L188`：关键词命中
    代码片段：`pub sigsuspend_saved_mask: Option<u64>, pub is_thread: bool, /// Set to true when a signal frame has been dispatched into the thread's /// trap frame (sepc redirected to signal...`
  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`mod task; mod process; use crate::fs::vfs::{OpenFlags, vfs_open};`
  - `kernel/src/task/task.rs:L19-L23`：关键词命中
    代码片段：`use crate::memory::*; use crate::sbi::shutdown; use crate::task::Signal; use crate::task::file_loader; use log::debug;`
- 与 2025 BlueStar 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
  - `third_party/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
  - `kernel/src/sync/up.rs:L19-L23`：关键词命中
    代码片段：`#[track_caller] pub fn lock(&self)->core::cell::RefMut<'_,T>{ match self.inner.try_borrow_mut() { Ok(g) => g,`
  - `kernel/vendor/spin/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! Locks that have the same behaviour as a mutex. //! //! The ['Mutex'] in the root of the crate, can be configured using the 'ticket_mutex' feature.`
- 与 2025 BlueStar 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/syscall/src/lib.rs:L537-L541`：关键词命中
    代码片段：`} fn log_user_path_fault(process: &proc::Process, syscall: &str, path_ptr: usize) { log_always(&format!( "whuse-ltp:path-fault syscall={} tgid={} tid={} name={} cwd={} ptr={:#x}...`
  - `crates/user-init/src/lib.rs:L33-L37`：关键词命中
    代码片段：`li a2, init_msg_end - init_msg li a7, 64 ecall li a0, 0`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`mod syscall; use log::{error}; use crate::memory::VirAddr;`
  - `kernel/src/syscall/syscall.rs:L1272-L1276`：关键词命中
    代码片段：`/// 返回：成功返回映射起始地址；失败返回 -1 /// /// 参数说明（Linux riscv64 ABI，用户态用 ecall 传参）： /// 'addr'  : 映射起始虚拟地址（用户 hint）。若带 'MAP_FIXED' 则必须使用该地址。 /// 'len'   : 映射长度（字节）。内核按页对齐到覆盖该区间。`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 BlueStar 在“内存管理”维度发现片段级代码相似度 0.61 （token=0.52, structure=0.78）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, debug, derive, pub, struct。（置信度：medium）
  证据：
  - `crates/mm/src/lib.rs:L58-L62`：struct MappingArea
    代码片段：`#[derive(Clone, Debug)] pub struct MappingArea { pub start: usize, pub len: usize,`
  - `kernel/src/memory/memset.rs:L36-L40`：struct VirNumRange
    代码片段：`///开始和结束，一个范围,自动[start,end] start地址自动向下取整，end也向下取整，因为virnumrange用于代码映射，防止代码缺失, startva/PAGE =num+offset ,从num开始，endva/pagesize=endva+offset由于闭区间所以向下取整,防止多映射 #[derive(Debug,Clone...`
- 与 2025 BlueStar 在“系统调用”维度发现片段级代码相似度 0.63 （token=0.50, structure=0.88）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：asm, in, inlateout, unsafe。（置信度：medium）
  证据：
  - `crates/hal-riscv64-virt/src/lib.rs:L763-L767`：关键词命中
    代码片段：`unsafe { core::arch::asm!( "ecall", inlateout("a0") timer_ticks as usize => error, in("a6") SBI_FID_SET_TIMER,`
  - `kernel/src/sbi.rs:L18-L22`：关键词命中
    代码片段：`unsafe { asm!( "ecall", inlateout("x10") arg0 => result, in("x11") arg1,`
- 函数/符号名重合：与 2025 BlueStar 在“设备驱动”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/fs-ext4/src/lib.rs:L235-L235`：fn new
    代码片段：`fn new(device: &'static dyn HalBlockDevice) -> Self {`
  - `user/src/console.rs:L33-L33`：fn new
    代码片段：`pub fn new(len:usize)->Self{`

## 相似点

- 与 2025 BlueStar 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 BlueStar 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = u64;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2025 BlueStar 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/syscall/src/fs_domain.rs:L22-L26`：关键词命中
    代码片段：`SYS_FCNTL => ctx .dispatcher .sys_fcntl(args, ctx.procs, ctx.scheduler, ctx.vfs), SYS_IOCTL => ctx.dispatcher.sys_ioctl(args, ctx.procs), SYS_FLOCK => ctx.dispatcher.sys_flock(a...`
  - `crates/vfs/src/lib.rs:L295-L299`：关键词命中
    代码片段：`pub fn new() -> Self { let root = Arc::new(Node::directory("/")); let mut vfs = Self { root, mounts: Vec::new(),`
- 与 2025 BlueStar 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/device/blk.rs:L89-L93`：关键词命中
    代码片段：`} /// Acknowledges a pending interrupt, if any. /// /// Returns true if there was an interrupt to acknowledge.`
  - `vendor/virtio-drivers/src/device/rng.rs:L52-L56`：关键词命中
    代码片段：`} /// Acknowledge interrupt. pub fn ack_interrupt(&mut self) -> InterruptStatus { self.transport.ack_interrupt()`
- 与 2025 BlueStar 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/mm/src/lib.rs:L161-L165`：关键词命中
    代码片段：`program_break: usize, next_mapping_base: usize, page_table: Option<PageTableSpace>, dirty: bool, frame_allocator: Mutex<FrameAllocator>,`
  - `vendor/virtio-drivers/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM //! to interact with VirtIO devices provided by the VMM (such as QEMU or...`
- 与 2025 BlueStar 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/syscall/src/task_domain.rs:L12-L16`：关键词命中
    代码片段：`Some(match sysno { SYS_SET_TID_ADDRESS => ctx.dispatcher.sys_set_tid_address(args, ctx.procs), SYS_SCHED_YIELD => ctx.dispatcher.sys_sched_yield(ctx.scheduler), SYS_EXIT => ctx...`
  - `crates/proc/src/lib.rs:L184-L188`：关键词命中
    代码片段：`pub sigsuspend_saved_mask: Option<u64>, pub is_thread: bool, /// Set to true when a signal frame has been dispatched into the thread's /// trap frame (sepc redirected to signal...`
- 与 2025 BlueStar 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
  - `third_party/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
- 与 2025 BlueStar 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/syscall/src/lib.rs:L537-L541`：关键词命中
    代码片段：`} fn log_user_path_fault(process: &proc::Process, syscall: &str, path_ptr: usize) { log_always(&format!( "whuse-ltp:path-fault syscall={} tgid={} tid={} name={} cwd={} ptr={:#x}...`
  - `crates/user-init/src/lib.rs:L33-L37`：关键词命中
    代码片段：`li a2, init_msg_end - init_msg li a7, 64 ecall li a0, 0`
- 与 2025 BlueStar 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 BlueStar 的语言构成不同：待测作品为 {'toml': 76, 'markdown': 6853, 'build': 3579, 'rust': 240449, 'asm': 81, 'json': 161, 'text': 285, 'yaml': 34}，历史样本为 {'json': 53, 'build': 2684, 'markdown': 10844, 'toml': 9593, 'c': 336, 'rust': 279800, 'asm': 641, 'text': 5383, 'yaml': 51}。（置信度：medium）

## 可能创新点

- 待测作品包含 266 个历史样本 2025 BlueStar 中未见的独特函数/类型定义（如 `process_needs_forced_preempt`, `log_block_probe_span`, `share`, `should_dispatch_pending_signals_after_syscall`, `map_anonymous_at`, `install_host_range`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `crates/kernel-core/src/lib_riscv.inc.rs:L3213-L3213`：fn process_needs_forced_preempt
    代码片段：`fn process_needs_forced_preempt(name: &str, now: u64, iozone_busybox_window_until_ns: u64) -> bool {`
  - `crates/kernel-core/src/lib_riscv.inc.rs:L3272-L3272`：fn log_block_probe_span
    代码片段：`fn log_block_probe_span(device: &'static dyn hal_api::HalBlockDevice, start: usize, count: usize) {`

## 附录：核验摘要

- 关键结论数：18
- 含证据关键结论数：18（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
