# 2026 T2026102699910462 比较报告

- 对比历史仓库：2025 NoAxiom
- 生成时间：2026-07-02T09:01:30.196606+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 NoAxiom（来源：赛事历史作品）：画像相似度 score=9.84；同属 rcore-variant 风格; 语言构成相似度 0.96; OS 维度重合度 1.00; 代码规模接近度 0.92

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 NoAxiom 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/virtio.rs:L6-L10`：关键词命中
    代码片段：`use myos_mm::{PAGE_SIZE, PageAllocation, PhysAddr, VirtAddr}; use virtio_drivers::{ BufferDirection, Hal, device::blk::{SECTOR_SIZE, VirtIOBlk},`
  - `kernel/src/console.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt; use myos_runtime::console::ByteConsole; /// 将当前架构的早期控制台适配到公共格式化设施。`
  - `NoAxiom/vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `NoAxiom/vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2025 NoAxiom 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L11-L15`：关键词命中
    代码片段：`use crate::process::Process; const ROOT_LOCK: LockClass = LockClass::new("vfs.root", LockRank::Vfs, 0); const TREE_LOCK: LockClass = LockClass::new("vfs.tree", LockRank::Vfs, 0)...`
  - `kernel/src/rng.rs:L10-L14`：关键词命中
    代码片段：`use crate::lockdep::{LockClass, LockRank}; const RNG_LOCK: LockClass = LockClass::new("rng.pool", LockRank::Vfs, 5); const CHACHA_KEY_SIZE: usize = 32;`
  - `NoAxiom/lib/fatfs/src/fs.rs:L24-L28`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `NoAxiom/kernel/src/fs/mod.rs:L8-L12`：关键词命中
    代码片段：`pub mod path; pub mod pipe; pub mod vfs; use arch::{Arch, ArchInt};`
- 与 2025 NoAxiom 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/irq.rs:L2-L6`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug, Eq, PartialEq)] pub enum InterruptSource { Timer, Software, External,`
  - `kernel/src/trap.rs:L4-L8`：关键词命中
    代码片段：`pub fn initialize() { crate::arch::trap::initialize(); crate::println!("trap subsystem:");`
  - `NoAxiom/kernel/src/time/timer.rs:L1-L4`：关键词命中
    代码片段：`//! reference: Phoenix //! - time maneger: https://gitlab.eduxiji.net/educg-group-22026-2376550/T202418123993075-1053/-/blob/265a693df30be6e11d9b96a3466bd5a70cd0f6af/modules/tim...`
  - `NoAxiom/kernel/src/trap/ext_int.rs:L1-L3`：关键词命中
    代码片段：`//! external interrupt handler pub fn ext_int_handler() {`
- 与 2025 NoAxiom 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/vm.rs:L19-L23`：关键词命中
    代码片段：`static KERNEL_PAGE_TABLE: IrqSpinLock<Option<RuntimePageTable>> = IrqSpinLock::new_with_class( None, LockClass::new("kernel_page_table", LockRank::PageTable, 1), );`
  - `kernel/src/memory.rs:L236-L240`：关键词命中
    代码片段：`pub struct EarlyMemoryState { frame_allocator: EarlyFrameAllocator<MEMORY_MAP_CAPACITY>, boot_page_table: crate::arch::memory::paging::BootPageTable,`
  - `NoAxiom/kernel/src/mm/mod.rs:L4-L8`：关键词命中
    代码片段：`pub mod memory_set; pub mod mmap_manager; pub mod page_table; pub mod permission; pub mod shm;`
  - `NoAxiom/lib/config/src/mm.rs:L12-L16`：关键词命中
    代码片段：`/// raw vpn & ppn width: 9 pub const PAGE_NUM_WIDTH: usize = PAGE_WIDTH - 3; /// page table entry per page: 512 pub const PTE_PER_PAGE: usize = 1 << PAGE_NUM_WIDTH;`
- 与 2025 NoAxiom 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L47-L51`：关键词命中
    代码片段：`type ContextSwitch = ( *mut crate::arch::task::Context, *const crate::arch::task::Context, );`
  - `kernel/src/task/stack.rs:L3-L7`：关键词命中
    代码片段：`const KERNEL_STACK_SIZE: usize = 64 * 1024; const KERNEL_STACK_ALIGNMENT: usize = PAGE_SIZE; // M6-B r3: architecture-owned fresh-task bootstrap reserve. // // The architecture...`
  - `NoAxiom/kernel/src/task/mod.rs:L9-L13`：关键词命中
    代码片段：`pub mod signal; pub mod status; pub mod task; pub mod task_main; pub mod taskid;`
  - `NoAxiom/kernel/src/task/pcb.rs:L6-L10`：关键词命中
    代码片段：`use bitflags::Flags; use super::{exit::ExitCode, status::TaskStatus, Task}; use crate::{ include::process::{robust_list::RobustList, TaskFlags},`
- 与 2025 NoAxiom 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/irq_lock.rs:L2-L6`：关键词命中
    代码片段：`marker::PhantomData, ops::{Deref, DerefMut}, sync::atomic::{AtomicUsize, Ordering}, };`
  - `vendor/rust-src/library/alloc/src/sync.rs:L5-L9`：关键词命中
    代码片段：`//! See the ['Arc<T>'][Arc] documentation for more details. //! //! **Note**: This module is only available on platforms that support atomic //! loads and stores of pointers. Th...`
  - `NoAxiom/vendor/concurrent-queue/src/sync.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization facade to choose between 'core' primitives and 'loom' primitives. #[cfg(all(feature = "portable-atomic", not(loom)))] mod sync_impl { pub(crate) use core::cell;`
  - `NoAxiom/vendor/crossbeam-utils/src/sync/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! * ['Parker'], a thread parking primitive. //! * ['ShardedLock'], a sharded reader-writer lock with fast concurrent reads. //! * ['WaitGroup'], for synchronizing the begi...`
- 与 2025 NoAxiom 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/trap.rs:L84-L88`：关键词命中
    代码片段：`match frame.cause_code() { BREAKPOINT => handle_breakpoint(frame), USER_ECALL if frame.previous_mode_was_user() => crate::user::handle_syscall(frame), INSTRUCTION_PAGE_FAULT =>...`
  - `kernel/src/syscall.rs:L1-L3`：关键词命中
    代码片段：`//! Linux asm-generic 64-bit syscall ABI shared by both SudoOS ports. //! //! This module owns ABI facts only. Syscall policy remains in the dispatcher so`
  - `NoAxiom/kernel/src/syscall/fs.rs:L8-L12`：关键词命中
    代码片段：`use ksync::assert_no_lock; use super::{Syscall, SyscallResult}; use crate::{ constant::fs::{`
  - `NoAxiom/kernel/src/syscall/io.rs:L5-L9`：关键词命中
    代码片段：`use ksync::assert_no_lock; use super::{Syscall, SyscallResult}; use crate::{ include::{`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 NoAxiom 在“设备驱动”维度发现片段级代码相似度 0.69 （token=0.52, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：debug, derive, pub, struct, usize。（置信度：medium）
  证据：
  - `kernel/src/device.rs:L69-L73`：struct DeviceResources
    代码片段：`#[derive(Clone, Copy, Debug)] pub struct DeviceResources { pub mmio_base: Option<usize>, pub mmio_size: Option<usize>,`
  - `NoAxiom/vendor/virtio-drivers/src/hal.rs:L10-L14`：struct Dma
    代码片段：`/// A region of contiguous physical memory used for DMA. #[derive(Debug)] pub struct Dma<H: Hal> { paddr: usize, vaddr: NonNull<u8>,`
- 与 2025 NoAxiom 在“文件系统”维度发现片段级代码相似度 0.69 （token=0.53, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, derive, enum。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L47-L51`：enum DeviceKind
    代码片段：`#[derive(Clone, Copy)] enum DeviceKind { Null, Zero,`
  - `NoAxiom/lib/fatfs/src/fs.rs:L32-L36`：enum FatType
    代码片段：`/// 'FatType' values are based on the size of File Allocation Table entry. #[derive(Copy, Clone, Eq, PartialEq, Debug)] pub enum FatType { /// 12 bits per FAT entry Fat12,`
- 与 2025 NoAxiom 在“调度与任务管理”维度发现片段级代码相似度 0.63 （token=0.61, structure=0.67）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：default, fn, for, impl, new, self。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L68-L72`：fn default
    代码片段：`impl Default for PreemptGuard { fn default() -> Self { Self::new() }`
  - `NoAxiom/kernel/src/task/pcb.rs:L35-L39`：fn default
    代码片段：`impl Default for PCB { fn default() -> Self { Self { children: Vec::new(),`
- 文件路径重合：与 2025 NoAxiom 在“中断与异常”维度出现同名文件源码路径 `kernel/src/timer.rs` / `NoAxiom/kernel/src/time/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/timer.rs:L102-L106`：关键词命中
    代码片段：`Ok(TimerHandle { owner, slot: u16::try_from(slot).expect("timer slot index exceeds u16"), generation, })`
  - `NoAxiom/kernel/src/time/timer.rs:L1-L4`：关键词命中
    代码片段：`//! reference: Phoenix //! - time maneger: https://gitlab.eduxiji.net/educg-group-22026-2376550/T202418123993075-1053/-/blob/265a693df30be6e11d9b96a3466bd5a70cd0f6af/modules/tim...`
- 文件路径重合：与 2025 NoAxiom 在“同步机制”维度出现同名文件源码路径 `vendor/rust-src/library/alloc/src/sync.rs` / `NoAxiom/vendor/concurrent-queue/src/sync.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `vendor/rust-src/library/alloc/src/sync.rs:L5-L9`：关键词命中
    代码片段：`//! See the ['Arc<T>'][Arc] documentation for more details. //! //! **Note**: This module is only available on platforms that support atomic //! loads and stores of pointers. Th...`
  - `NoAxiom/vendor/concurrent-queue/src/sync.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization facade to choose between 'core' primitives and 'loom' primitives. #[cfg(all(feature = "portable-atomic", not(loom)))] mod sync_impl { pub(crate) use core::cell;`

## 相似点

- 与 2025 NoAxiom 同属 rcore-variant 风格。（置信度：medium）
- 与 2025 NoAxiom 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/virtio.rs:L6-L10`：关键词命中
    代码片段：`use myos_mm::{PAGE_SIZE, PageAllocation, PhysAddr, VirtAddr}; use virtio_drivers::{ BufferDirection, Hal, device::blk::{SECTOR_SIZE, VirtIOBlk},`
  - `kernel/src/console.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt; use myos_runtime::console::ByteConsole; /// 将当前架构的早期控制台适配到公共格式化设施。`
- 与 2025 NoAxiom 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L11-L15`：关键词命中
    代码片段：`use crate::process::Process; const ROOT_LOCK: LockClass = LockClass::new("vfs.root", LockRank::Vfs, 0); const TREE_LOCK: LockClass = LockClass::new("vfs.tree", LockRank::Vfs, 0)...`
  - `kernel/src/rng.rs:L10-L14`：关键词命中
    代码片段：`use crate::lockdep::{LockClass, LockRank}; const RNG_LOCK: LockClass = LockClass::new("rng.pool", LockRank::Vfs, 5); const CHACHA_KEY_SIZE: usize = 32;`
- 与 2025 NoAxiom 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/irq.rs:L2-L6`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug, Eq, PartialEq)] pub enum InterruptSource { Timer, Software, External,`
  - `kernel/src/trap.rs:L4-L8`：关键词命中
    代码片段：`pub fn initialize() { crate::arch::trap::initialize(); crate::println!("trap subsystem:");`
- 与 2025 NoAxiom 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/vm.rs:L19-L23`：关键词命中
    代码片段：`static KERNEL_PAGE_TABLE: IrqSpinLock<Option<RuntimePageTable>> = IrqSpinLock::new_with_class( None, LockClass::new("kernel_page_table", LockRank::PageTable, 1), );`
  - `kernel/src/memory.rs:L236-L240`：关键词命中
    代码片段：`pub struct EarlyMemoryState { frame_allocator: EarlyFrameAllocator<MEMORY_MAP_CAPACITY>, boot_page_table: crate::arch::memory::paging::BootPageTable,`
- 与 2025 NoAxiom 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L47-L51`：关键词命中
    代码片段：`type ContextSwitch = ( *mut crate::arch::task::Context, *const crate::arch::task::Context, );`
  - `kernel/src/task/stack.rs:L3-L7`：关键词命中
    代码片段：`const KERNEL_STACK_SIZE: usize = 64 * 1024; const KERNEL_STACK_ALIGNMENT: usize = PAGE_SIZE; // M6-B r3: architecture-owned fresh-task bootstrap reserve. // // The architecture...`
- 与 2025 NoAxiom 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/irq_lock.rs:L2-L6`：关键词命中
    代码片段：`marker::PhantomData, ops::{Deref, DerefMut}, sync::atomic::{AtomicUsize, Ordering}, };`
  - `vendor/rust-src/library/alloc/src/sync.rs:L5-L9`：关键词命中
    代码片段：`//! See the ['Arc<T>'][Arc] documentation for more details. //! //! **Note**: This module is only available on platforms that support atomic //! loads and stores of pointers. Th...`
- 与 2025 NoAxiom 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/trap.rs:L84-L88`：关键词命中
    代码片段：`match frame.cause_code() { BREAKPOINT => handle_breakpoint(frame), USER_ECALL if frame.previous_mode_was_user() => crate::user::handle_syscall(frame), INSTRUCTION_PAGE_FAULT =>...`
  - `kernel/src/syscall.rs:L1-L3`：关键词命中
    代码片段：`//! Linux asm-generic 64-bit syscall ABI shared by both SudoOS ports. //! //! This module owns ABI facts only. Syscall policy remains in the dispatcher so`
- 与 2025 NoAxiom 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 NoAxiom 的语言构成不同：待测作品为 {'markdown': 23954, 'build': 11383, 'asm': 6300, 'rust': 1585288, 'toml': 198, 'json': 69, 'c': 44494, 'text': 10457, 'yaml': 79, 'make': 664}，历史样本为 {'json': 243, 'build': 19323, 'markdown': 40029, 'toml': 9956, 'yaml': 246, 'rust': 1771839, 'asm': 577, 'text': 324, 'cpp': 67, 'c': 96}。（置信度：medium）

## 可能创新点

- 待测作品包含 303 个历史样本 2025 NoAxiom 中未见的独特函数/类型定义（如 `mmio_virtual_address`, `ram_virtual_address`, `switch_user_address_space`, `call2`, `hart_start`, `write_entry`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `arch/riscv64/src/memory/phys_access.rs:L44-L44`：fn mmio_virtual_address
    代码片段：`pub fn mmio_virtual_address(physical: PhysAddr, size: usize) -> Result<VirtAddr, PhysAccessError> {`
  - `arch/riscv64/src/memory/phys_access.rs:L17-L17`：fn ram_virtual_address
    代码片段：`pub fn ram_virtual_address(physical: PhysAddr, size: usize) -> Result<VirtAddr, PhysAccessError> {`

## 附录：核验摘要

- 关键结论数：20
- 含证据关键结论数：20（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
