# 2026 T202610701999762 比较报告

- 对比历史仓库：2023 BiteTheDisk
- 生成时间：2026-07-02T09:03:55.897121+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2023 BiteTheDisk（来源：赛事历史作品）：画像相似度 score=8.04；同属 rcore-variant 风格; 语言构成相似度 0.14; OS 维度重合度 1.00; 代码规模接近度 0.77

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2023 BiteTheDisk 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/l0/80_device/device_core/body.rs:L1-L3`：关键词命中
    代码片段：`#[path = "virtio_mmio_blk.rs"] mod virtio_mmio_blk; #[cfg(target_arch = "loongarch64")]`
  - `kernel/src/l0/10_early_output/early_console/api.rs:L5-L9`：关键词命中
    代码片段：`/// - 'bytes'：要输出或填充的字节缓冲区。 pub fn bytes(bytes: &[u8]) { crate::arch::console::write_bytes(bytes); }`
  - `kernel/src/console.rs:L36-L40`：关键词命中
    代码片段：`macro_rules! print { ($fmt:literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); }; }`
  - `kernel/src/drivers/mod.rs:L25-L29`：关键词命中
    代码片段：`}); /// Initialize platform specific device drivers. pub fn init() { #[cfg(feature = "fu740")]`
- 与 2023 BiteTheDisk 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/l1/30_vfs/vfs_fd/api.rs:L106-L110`：关键词命中
    代码片段：`#[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct Metadata { pub inode: u64, pub kind: NodeKind, pub mode: u16,`
  - `kernel/src/l1/30_vfs/vfs_fd/devfs.rs:L49-L53`：关键词命中
    代码片段：`display, }, inode: file_inode(kind) as u32, kind: NodeKind::CharDevice, })`
  - `crates/nix/src/fs.rs:L89-L93`：关键词命中
    代码片段：`#[derive(Debug)] pub struct Dirent { d_ino: usize,             // inode number d_off: isize,             // offset from 0 to next dirent d_reclen: u16,            // length of t...`
  - `kernel/src/fs/page.rs:L29-L33`：关键词命中
    代码片段：`/// Data block state pub data_states: [DataState; PAGE_SIZE / BLOCK_SIZE], /// Inode that this page related to inode: Weak<VirtFile>, }`
- 与 2023 BiteTheDisk 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/arch/trap.rs:L2-L6`：关键词命中
    代码片段：`pub enum TrapKind { Unknown, Interrupt, Timer, PageFault,`
  - `kernel/src/l0/50_trap/trap_core/api.rs:L1-L3`：关键词命中
    代码片段：`pub mod trap { pub type TrapKind = crate::arch::trap::TrapKind; pub type TrapInfo = crate::arch::trap::TrapInfo;`
  - `kernel/src/timer.rs:L48-L52`：关键词命中
    代码片段：`} /// Setup the next time interrupt time. pub fn set_next_trigger() { set_timer((get_time() + CLOCK_FREQ / TIME_SLICE) as u64);`
  - `kernel/src/trap/mod.rs:L3-L7`：关键词命中
    代码片段：`pub use context::*; pub use handler::*; use riscv::register::scause::{Interrupt, Trap}; use crate::consts::TRAMPOLINE;`
- 与 2023 BiteTheDisk 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/arch/vm.rs:L15-L19`：关键词命中
    代码片段：`pub enum PageTableKind { Generic, Sv39, LoongArch64ThreeLevel, }`
  - `kernel/src/l0/30_page/page_map/api.rs:L8-L12`：关键词命中
    代码片段：`pub use crate::arch::PageFlags; /// PageTable 结构体，属于该能力对外暴露的稳定接口。 #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct PageTable {`
  - `kernel/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod kernel_heap_allocator; mod kvmm;`
  - `kernel/src/mm/kvmm.rs:L6-L10`：关键词命中
    代码片段：`mm::{ vm_area::{VmArea, VmAreaType}, MapPermission, MapType, MemorySet, }, };`
- 与 2023 BiteTheDisk 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/arch/thread.rs:L7-L11`：关键词命中
    代码片段：`/// Kernel stack pointer. Kept at offset 0 for arch switch assembly. pub sp: usize, /// Kernel thread pointer / CPU-local pointer. Kept at offset 1. pub tp: usize, /// Callee-sa...`
  - `kernel/src/l1/20_task/task_mm_signal/api.rs:L6-L10`：关键词命中
    代码片段：`pub mod pid; pub mod signal; pub mod task; pub mod user_memory;`
  - `kernel/src/task/mod.rs:L7-L11`：关键词命中
    代码片段：`mod signal; mod switch; mod task; pub use context::*; pub use id::*;`
  - `kernel/src/task/task.rs:L66-L70`：关键词命中
    代码片段：`pub parent: Option<Weak<TaskControlBlock>>, // child process and thread collection pub children: Vec<Arc<TaskControlBlock>>,`
- 与 2023 BiteTheDisk 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/arch/boot.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicUsize, Ordering}; #[derive(Debug, Clone, Copy, PartialEq, Eq)]`
  - `kernel/src/arch/trap.rs:L113-L117`：关键词命中
    代码片段：`true } use core::sync::atomic::{AtomicUsize, Ordering}; pub type PageFaultHandler = fn(usize, Option<AccessKind>) -> bool;`
  - `kernel/vendor/spin/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! Locks that have the same behaviour as a mutex. //! //! The ['Mutex'] in the root of the crate, can be configured using the 'ticket_mutex' feature.`
  - `kernel/vendor/lock_api/src/lib.rs:L6-L10`：关键词命中
    代码片段：`// copied, modified, or distributed except according to those terms. //! This library provides type-safe and fully-featured 'Mutex' and 'RwLock' //! types which wrap a simple ra...`
- 与 2023 BiteTheDisk 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/l2/40_table/syscall_table/api.rs:L41-L45`：关键词命中
    代码片段：`pub fn dispatch(args: Args) -> isize { match number::decode(args.number) { number::Syscall::Getcwd => file::getcwd(args.get(0), args.get(1)), number::Syscall::Dup => file::dup(a...`
  - `kernel/src/l2/50_syscall_entry/syscall_entry/api.rs:L59-L63`：关键词命中
    代码片段：`pub fn handle_trap(tf: &mut super::TrapFrame) { let number = need::syscall_raw::number(tf); let syscall = need::number::decode(number); let args = [ need::syscall_raw::arg(tf, 0),`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Syscall Implementation //! //! About syscall id in RISC-V: https://marcin.juszkiewicz.com.pl/download/tables/syscalls.html`
  - `kernel/src/trap/trampoline.S:L139-L142`：关键词命中
    代码片段：`.align 2 user_sigreturn: li	a7, 139 # syscall SIGRETURN ecall`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2023 BiteTheDisk 在“内存管理”维度发现片段级代码相似度 0.60 （token=0.54, structure=0.73）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, debug, derive, pub, struct, usize。（置信度：medium）
  证据：
  - `kernel/src/arch/vm.rs:L6-L10`：struct AddressLayout
    代码片段：`#[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct AddressLayout { pub physical_memory_offset: usize, pub kernel_offset: usize,`
  - `crates/nix/src/mm.rs:L16-L20`：struct MmapFlags
    代码片段：`bitflags! { #[derive(Clone, Copy, Debug)] pub struct MmapFlags: usize { /// File mapping. Used for file initialization.(for compatibility, can be ignored) const MAP_FILE = 0;`
- 与 2023 BiteTheDisk 在“系统调用”维度发现片段级代码相似度 0.64 （token=0.52, structure=0.88）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, derive, enum, eq, partialeq, pub。（置信度：medium）
  证据：
  - `kernel/src/arch/trap.rs:L1-L4`：enum TrapKind
    代码片段：`#[derive(Debug, Clone, Copy, PartialEq, Eq)] pub enum TrapKind { Unknown, Interrupt,`
  - `kernel/src/syscall/mod.rs:L18-L22`：enum SyscallId
    代码片段：`#[allow(dead_code)] #[derive(Copy, Clone, PartialEq, Eq, PartialOrd, Ord)] pub enum SyscallId { $( $id = $v,`
- 函数/符号名重合：与 2023 BiteTheDisk 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/arch/console.rs:L27-L27`：fn write_str
    代码片段：`fn write_str(&mut self, text: &str) -> fmt::Result {`
  - `kernel/src/console.rs:L19-L19`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 文件路径重合：与 2023 BiteTheDisk 在“设备驱动”维度出现同名文件源码路径 `kernel/src/arch/console.rs` / `kernel/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/arch/console.rs:L1-L5`：fn put_byte
    代码片段：`use core::fmt::{self, Write}; pub fn put_byte(byte: u8) { super::platform::console_putchar(byte); }`
  - `kernel/src/console.rs:L36-L40`：关键词命中
    代码片段：`macro_rules! print { ($fmt:literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); }; }`

## 相似点

- 与 2023 BiteTheDisk 同属 rcore-variant 风格。（置信度：medium）
- 与 2023 BiteTheDisk 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/l0/80_device/device_core/body.rs:L1-L3`：关键词命中
    代码片段：`#[path = "virtio_mmio_blk.rs"] mod virtio_mmio_blk; #[cfg(target_arch = "loongarch64")]`
  - `kernel/src/l0/10_early_output/early_console/api.rs:L5-L9`：关键词命中
    代码片段：`/// - 'bytes'：要输出或填充的字节缓冲区。 pub fn bytes(bytes: &[u8]) { crate::arch::console::write_bytes(bytes); }`
- 与 2023 BiteTheDisk 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/l1/30_vfs/vfs_fd/api.rs:L106-L110`：关键词命中
    代码片段：`#[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct Metadata { pub inode: u64, pub kind: NodeKind, pub mode: u16,`
  - `kernel/src/l1/30_vfs/vfs_fd/devfs.rs:L49-L53`：关键词命中
    代码片段：`display, }, inode: file_inode(kind) as u32, kind: NodeKind::CharDevice, })`
- 与 2023 BiteTheDisk 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/arch/trap.rs:L2-L6`：关键词命中
    代码片段：`pub enum TrapKind { Unknown, Interrupt, Timer, PageFault,`
  - `kernel/src/l0/50_trap/trap_core/api.rs:L1-L3`：关键词命中
    代码片段：`pub mod trap { pub type TrapKind = crate::arch::trap::TrapKind; pub type TrapInfo = crate::arch::trap::TrapInfo;`
- 与 2023 BiteTheDisk 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/arch/vm.rs:L15-L19`：关键词命中
    代码片段：`pub enum PageTableKind { Generic, Sv39, LoongArch64ThreeLevel, }`
  - `kernel/src/l0/30_page/page_map/api.rs:L8-L12`：关键词命中
    代码片段：`pub use crate::arch::PageFlags; /// PageTable 结构体，属于该能力对外暴露的稳定接口。 #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct PageTable {`
- 与 2023 BiteTheDisk 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/arch/thread.rs:L7-L11`：关键词命中
    代码片段：`/// Kernel stack pointer. Kept at offset 0 for arch switch assembly. pub sp: usize, /// Kernel thread pointer / CPU-local pointer. Kept at offset 1. pub tp: usize, /// Callee-sa...`
  - `kernel/src/l1/20_task/task_mm_signal/api.rs:L6-L10`：关键词命中
    代码片段：`pub mod pid; pub mod signal; pub mod task; pub mod user_memory;`
- 与 2023 BiteTheDisk 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/arch/boot.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicUsize, Ordering}; #[derive(Debug, Clone, Copy, PartialEq, Eq)]`
  - `kernel/src/arch/trap.rs:L113-L117`：关键词命中
    代码片段：`true } use core::sync::atomic::{AtomicUsize, Ordering}; pub type PageFaultHandler = fn(usize, Option<AccessKind>) -> bool;`
- 与 2023 BiteTheDisk 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/l2/40_table/syscall_table/api.rs:L41-L45`：关键词命中
    代码片段：`pub fn dispatch(args: Args) -> isize { match number::decode(args.number) { number::Syscall::Getcwd => file::getcwd(args.get(0), args.get(1)), number::Syscall::Dup => file::dup(a...`
  - `kernel/src/l2/50_syscall_entry/syscall_entry/api.rs:L59-L63`：关键词命中
    代码片段：`pub fn handle_trap(tf: &mut super::TrapFrame) { let number = need::syscall_raw::number(tf); let syscall = need::number::decode(number); let args = [ need::syscall_raw::arg(tf, 0),`
- 与 2023 BiteTheDisk 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2023 BiteTheDisk 的语言构成不同：待测作品为 {'markdown': 301273, 'toml': 2615, 'yaml': 12, 'build': 482, 'make': 28, 'rust': 32745}，历史样本为 {'json': 83, 'build': 3797, 'markdown': 14373, 'toml': 8819, 'rust': 428744, 'asm': 519, 'c': 48, 'yaml': 427}。（置信度：medium）

## 可能创新点

- 待测作品包含 401 个历史样本 2023 BiteTheDisk 中未见的独特函数/类型定义（如 `create_with_attr`, `calculate_extent_block_checksum`, `set_extent_block_checksum`, `fuse_mkdir`, `get_pblock_idx`, `balloc_free_blocks`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `crate/ext4_rs/src/ext4_impls/file.rs:L134-L134`：fn create_with_attr
    代码片段：`pub fn create_with_attr(&self, parent: u32, name: &str, inode_mode: u16, uid:u16, gid: u16) -> Result<Ext4InodeRef> {`
  - `crate/ext4_rs/src/ext4_impls/extents.rs:L1264-L1264`：fn calculate_extent_block_checksum
    代码片段：`fn calculate_extent_block_checksum(&self, inode_ref: &Ext4InodeRef, data: &[u8], block_addr: usize) -> u32 {`

## 附录：核验摘要

- 关键结论数：19
- 含证据关键结论数：19（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
