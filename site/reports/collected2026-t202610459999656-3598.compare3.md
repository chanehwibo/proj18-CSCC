# 2026 T202610459999656 比较报告

- 对比历史仓库：2025 如有名字队
- 生成时间：2026-07-02T05:01:01.864321+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 如有名字队（来源：赛事历史作品）：画像相似度 score=9.17；同属 rcore-variant 风格; 语言构成相似度 0.73; OS 维度重合度 1.00; 代码规模接近度 0.70

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 如有名字队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/drivers.rs:L5-L7`：关键词命中
    代码片段：`pub mod block; pub mod platform; pub mod virtio;`
  - `os/src/net/device.rs:L7-L11`：关键词命中
    代码片段：`use smoltcp::phy::{self, DeviceCapabilities, Medium}; use smoltcp::time::Instant; use virtio_drivers::device::net::RxBuffer; use crate::drivers::virtio::net::VirtioNetDevice;`
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use core::fmt::{self, Write};`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`pub mod device; use device::*; pub mod disk;`
- 与 2025 如有名字队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/pty.rs:L9-L13`：关键词命中
    代码片段：`fs::{ file_handle::{FileLike, PollEvents}, utils::{AccessMode, InodeMode, InodeType, Metadata, StatusFlags, SuperBlock, NAME_MAX}, }, hal::VirtAddr,`
  - `os/src/fs/ext4.rs:L38-L42`：关键词命中
    代码片段：`fs::utils::{ CreateNode, CreateOwner, DirSink, FileAttributeFlags, FileAttributes, FileSystem, FsFlags, Inode, InodeMode, InodeType, Metadata, StatxAttributes, StatxMetadata, Su...`
  - `os/src/fs/mod.rs:L3-L7`：关键词命中
    代码片段：`pub mod fsidx; mod fstruct; mod inode; mod mount; mod pipe;`
  - `os/src/fs/pipe.rs:L172-L176`：关键词命中
    代码片段：`pub fn splice_from_pipe( pipe: Arc<dyn File>, inode: Arc<OSInode>, offset: *const isize, len: usize,`
- 与 2025 如有名字队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/hal/irq.rs:L8-L12`：关键词命中
    代码片段：`#[inline] pub fn enable_local() { arch::irq::enable_local(); }`
  - `os/src/hal/trap.rs:L13-L17`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug)] pub enum InterruptKind { Timer, External, Software,`
  - `os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! Timer module using the 'Time' abstraction layer. use polyhal::time::Time;`
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Kernel trap and interrupt handling pub mod interrupts; use crate::config::TIMER_IRQ;`
- 与 2025 如有名字队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm.rs:L11-L15`：关键词命中
    代码片段：`pub mod page_fault_handler; pub mod page_prop; pub mod page_table; pub mod reclaim; pub mod status;`
  - `os/src/mm/init.rs:L9-L13`：关键词命中
    代码片段：`meta::init as init_frame_metadata, }, mm::{kspace::init_kernel_page_table, page_table::boot_pt}, };`
  - `os/src/mm/mod.rs:L1-L3`：关键词命中
    代码片段：`mod frame_allocator; mod group; mod heap_allocator;`
  - `os/src/mm/shm.rs:L8-L12`：关键词命中
    代码片段：`}; use super::{frame_alloc, FrameTracker, MapPermission}; use lazy_static::*;`
- 与 2025 如有名字队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/thread.rs:L12-L16`：关键词命中
    代码片段：`use crate::{ fs::fs_context::{FsCloneFlags, FsContext}, hal::{task::TaskContext, HartId, HartMask}, mm::heap::HeapAllocError, process::Process,`
  - `os/src/hal/task.rs:L14-L18`：关键词命中
    代码片段：`/// - 可以通过 ['TaskContext::set_stack_pointer'] 与 ['TaskContext::set_instruction_pointer'] ///   初始化一个“首次运行”的内核线程。 pub use arch::task::TaskContext; /// 在两个任务上下文之间切换。`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Task management implementation //! //! Everything about task management, like starting and switching tasks is`
  - `os/src/task/task.rs:L12-L16`：关键词命中
    代码片段：`use crate::syscall::CloneFlags; use crate::syscall::RLimit; use crate::task::aux_::{Aux, AuxType}; use crate::timer::TimeData; use crate::utils::{get_abs_path, is_abs_path, SysE...`
- 与 2025 如有名字队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/sync.rs:L2-L6`：关键词命中
    代码片段：`pub mod guard; pub mod mutex; pub mod once; pub mod rcu;`
  - `os/src/sync/rcu.rs:L7-L11`：关键词命中
    代码片段：`mem::ManuallyDrop, ops::Deref, sync::atomic::{AtomicBool, Ordering}, };`
  - `os/src/fs/mod.rs:L21-L25`：关键词命中
    代码片段：`use alloc::sync::Arc; use lazy_static::lazy_static; use spin::Mutex; use usedfiles::*;`
  - `os/src/mm/shm.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{collections::BTreeMap, sync::Arc, vec::Vec}; use spin::Mutex; use crate::{`
- 与 2025 如有名字队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall.rs:L163-L167`：关键词命中
    代码片段：`} /// 验证 message-header 坏地址在 syscall 边界返回 'EFAULT'。 #[cfg(feature = "ktest")] pub(crate) fn ktest_socket_message_badaddr_contract() -> crate::Result<()> {`
  - `os/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! syscall：文件系统相关分组 extern crate alloc;`
  - `os/src/syscall/fs.rs:L15-L19`：关键词命中
    代码片段：`}; use crate::signal::SignalFlags; use crate::syscall::option::{ fcntl_cmd_name, FdSet, F_DUPFD, F_DUPFD_CLOEXEC, F_GETFD, F_GETFL, F_SETFD, };`
  - `os/src/syscall/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Implementation of syscalls //! //! The single entry point to all system calls, ['syscall()'], is called //! whenever userspace wishes to perform a system call using the 'eca...`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 如有名字队 在“中断与异常”维度发现片段级代码相似度 0.63 （token=0.50, structure=0.88）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, debug, derive, pub。（置信度：medium）
  证据：
  - `os/src/hal/trap.rs:L13-L17`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug)] pub enum InterruptKind { Timer, External, Software,`
  - `os/src/timer.rs:L7-L11`：struct Timespec
    代码片段：`#[derive(Debug, Clone, Copy)] pub struct Timespec { pub tv_sec: usize,  //秒 pub tv_nsec: usize, //纳秒`
- 文件路径重合：与 2025 如有名字队 在“系统调用”维度出现同路径源码路径 `os/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! syscall：文件系统相关分组 extern crate alloc;`
  - `os/src/syscall/fs.rs:L15-L19`：关键词命中
    代码片段：`}; use crate::signal::SignalFlags; use crate::syscall::option::{ fcntl_cmd_name, FdSet, F_DUPFD, F_DUPFD_CLOEXEC, F_GETFD, F_GETFL, F_SETFD, };`
- 文件路径重合：与 2025 如有名字队 在“设备驱动”维度出现同名文件源码路径 `os/src/hal/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/hal/console.rs:L18-L22`：关键词命中
    代码片段：`pub fn init_from_boot() { CONSOLE_BACKEND.call_once(|| { if let Some(u) = boot_info().uart { let mmio_base = arch::mm::phys_to_virt_io(u.base).as_usize(); // SAFETY: 'mmio_base'...`
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use core::fmt::{self, Write};`
- 文件路径重合：与 2025 如有名字队 在“调度与任务管理”维度出现同名文件源码路径 `os/src/hal/task.rs` / `os/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/hal/task.rs:L14-L18`：关键词命中
    代码片段：`/// - 可以通过 ['TaskContext::set_stack_pointer'] 与 ['TaskContext::set_instruction_pointer'] ///   初始化一个“首次运行”的内核线程。 pub use arch::task::TaskContext; /// 在两个任务上下文之间切换。`
  - `os/src/task/task.rs:L12-L16`：关键词命中
    代码片段：`use crate::syscall::CloneFlags; use crate::syscall::RLimit; use crate::task::aux_::{Aux, AuxType}; use crate::timer::TimeData; use crate::utils::{get_abs_path, is_abs_path, SysE...`

## 相似点

- 与 2025 如有名字队 同属 rcore-variant 风格。（置信度：medium）
- 与 2025 如有名字队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/drivers.rs:L5-L7`：关键词命中
    代码片段：`pub mod block; pub mod platform; pub mod virtio;`
  - `os/src/net/device.rs:L7-L11`：关键词命中
    代码片段：`use smoltcp::phy::{self, DeviceCapabilities, Medium}; use smoltcp::time::Instant; use virtio_drivers::device::net::RxBuffer; use crate::drivers::virtio::net::VirtioNetDevice;`
- 与 2025 如有名字队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/pty.rs:L9-L13`：关键词命中
    代码片段：`fs::{ file_handle::{FileLike, PollEvents}, utils::{AccessMode, InodeMode, InodeType, Metadata, StatusFlags, SuperBlock, NAME_MAX}, }, hal::VirtAddr,`
  - `os/src/fs/ext4.rs:L38-L42`：关键词命中
    代码片段：`fs::utils::{ CreateNode, CreateOwner, DirSink, FileAttributeFlags, FileAttributes, FileSystem, FsFlags, Inode, InodeMode, InodeType, Metadata, StatxAttributes, StatxMetadata, Su...`
- 与 2025 如有名字队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/hal/irq.rs:L8-L12`：关键词命中
    代码片段：`#[inline] pub fn enable_local() { arch::irq::enable_local(); }`
  - `os/src/hal/trap.rs:L13-L17`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug)] pub enum InterruptKind { Timer, External, Software,`
- 与 2025 如有名字队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm.rs:L11-L15`：关键词命中
    代码片段：`pub mod page_fault_handler; pub mod page_prop; pub mod page_table; pub mod reclaim; pub mod status;`
  - `os/src/mm/init.rs:L9-L13`：关键词命中
    代码片段：`meta::init as init_frame_metadata, }, mm::{kspace::init_kernel_page_table, page_table::boot_pt}, };`
- 与 2025 如有名字队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/thread.rs:L12-L16`：关键词命中
    代码片段：`use crate::{ fs::fs_context::{FsCloneFlags, FsContext}, hal::{task::TaskContext, HartId, HartMask}, mm::heap::HeapAllocError, process::Process,`
  - `os/src/hal/task.rs:L14-L18`：关键词命中
    代码片段：`/// - 可以通过 ['TaskContext::set_stack_pointer'] 与 ['TaskContext::set_instruction_pointer'] ///   初始化一个“首次运行”的内核线程。 pub use arch::task::TaskContext; /// 在两个任务上下文之间切换。`
- 与 2025 如有名字队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/sync.rs:L2-L6`：关键词命中
    代码片段：`pub mod guard; pub mod mutex; pub mod once; pub mod rcu;`
  - `os/src/sync/rcu.rs:L7-L11`：关键词命中
    代码片段：`mem::ManuallyDrop, ops::Deref, sync::atomic::{AtomicBool, Ordering}, };`
- 与 2025 如有名字队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall.rs:L163-L167`：关键词命中
    代码片段：`} /// 验证 message-header 坏地址在 syscall 边界返回 'EFAULT'。 #[cfg(feature = "ktest")] pub(crate) fn ktest_socket_message_badaddr_contract() -> crate::Result<()> {`
  - `os/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! syscall：文件系统相关分组 extern crate alloc;`
- 与 2025 如有名字队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 如有名字队 的语言构成不同：待测作品为 {'build': 6236, 'markdown': 11526, 'toml': 84, 'rust': 588336, 'asm': 532, 'text': 6096, 'json': 249, 'yaml': 60, 'make': 52, 'c': 119018}，历史样本为 {'json': 281978, 'build': 9776, 'text': 482, 'markdown': 22278, 'toml': 9717, 'yaml': 87, 'c': 27884, 'asm': 928, 'rust': 762578, 'make': 664}。（置信度：medium）

## 可能创新点

- 待测作品包含 342 个历史样本 2025 如有名字队 中未见的独特函数/类型定义（如 `stop_and_requeue_front`, `probe_mmio_network`, `shared_desc_from_output`, `submit`, `complete_used`, `probe_mmio_blocks`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `os/src/drivers/virtio/blk/core.rs:L127-L127`：fn stop_and_requeue_front
    代码片段：`fn stop_and_requeue_front(&mut self, request: DispatchRequest, resource_release_epoch: usize) {`
  - `os/src/drivers/virtio/net/probe.rs:L157-L157`：fn probe_mmio_network
    代码片段：`fn probe_mmio_network(boot: &crate::hal::BootInfo) -> Result<Option<(Nic, u32)>, ProbeError> {`

## 附录：核验摘要

- 关键结论数：19
- 含证据关键结论数：19（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
