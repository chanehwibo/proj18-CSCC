# 2026 T2026100069910232 比较报告

- 对比历史仓库：2023 Starry
- 生成时间：2026-07-01T20:55:29.365335+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2023 Starry（来源：赛事历史作品）：画像相似度 score=9.64；同属 ucore-variant 风格; 语言构成相似度 0.96; OS 维度重合度 1.00; 代码规模接近度 0.73

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2023 Starry 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/vendor/uart_16550/src/mmio.rs:L6-L10`：关键词命中
    代码片段：`use crate::{LineStsFlags, WouldBlockError}; /// A memory-mapped UART. #[derive(Debug)] pub struct MmioSerialPort {`
  - `src/vendor/axdriver/src/drivers.rs:L9-L13`：关键词命中
    代码片段：`pub use super::dummy::*; use crate::AxDeviceEnum; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use driver_common::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 与 2023 Starry 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/kernel/src/file/fs.rs:L487-L491`：关键词命中
    代码片段：`Kstat { dev: metadata.device, ino: metadata.inode, mode, nlink,`
  - `src/kernel/src/file/mod.rs:L45-L49`：关键词命中
    代码片段：`}, }; // Inode-flags helpers are defined as 'pub fn' / 'pub const' below — no // separate 'pub use self::' needed. use crate::task::{AX_FILE_LIMIT, AsThread};`
  - `vendor/fatfs/src/fs.rs:L21-L25`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `modules/axfs/src/fs/fatfs.rs:L32-L36`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
- 与 2023 Starry 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/kernel/src/task/timer.rs:L50-L54`：关键词命中
    代码片段：`} /// The type of interval timer. #[repr(i32)] #[allow(non_camel_case_types)]`
  - `src/vendor/axcpu/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use memory_addr::VirtAddr;`
  - `modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use crate_interface::{call_interface, def_interface};`
- 与 2023 Starry 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/kernel/src/mm/access.rs:L87-L91`：关键词命中
    代码片段：`// TODO: this is inefficient, but we have to do this instead of // querying the page table since the page might has not been // allocated yet. let curr = current();`
  - `src/kernel/src/mm/loader.rs:L38-L42`：关键词命中
    代码片段：`#[cfg(not(any(target_arch = "aarch64", target_arch = "loongarch64")))] { // ARMv8 (aarch64) and LoongArch64 use separate page tables for user space // (aarch64: TTBR0_EL1, Loong...`
  - `modules/axhal/src/lib.rs:L18-L22`：关键词命中
    代码片段：`//! - 'smp': Enable SMP (symmetric multiprocessing) support. //! - 'fp_simd': Enable floating-point and SIMD support. //! - 'paging': Enable page table manipulation. //! - 'irq'...`
  - `modules/axhal/src/trap.rs:L5-L9`：关键词命中
    代码片段：`use crate::arch::TrapFrame; use memory_addr::VirtAddr; use page_table::MappingFlags; /// Trap handler interface.`
- 与 2023 Starry 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. //! //! 本模块负责：`
  - `src/kernel/src/task/ops.rs:L18-L22`：关键词命中
    代码片段：`use super::{ futex_table_for, send_signal_thread_inner, send_signal_to_process, send_signal_to_thread, AsThread, FutexKey, ProcessData, Thread, TimerState, }; use crate::file::r...`
  - `vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`
  - `vendor/futures/src/task.rs:L6-L10`：关键词命中
    代码片段：`//! through one large state machine. //! //! A "task" is the unit of abstraction for what is driving this state machine //! and tree of futures forward. A task is used to poll f...`
- 与 2023 Starry 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/kernel/src/file/record_lock.rs:L1-L3`：关键词命中
    代码片段：`//! POSIX and OFD record lock manager. //! //! POSIX record locks are **process-owned**, operate on **inode byte ranges**,`
  - `src/kernel/src/syscall/sync/futex.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::Ordering; use axerrno::{AxError, AxResult, LinuxError};`
  - `vendor/futures/src/sync/bilock.rs:L7-L11`：关键词命中
    代码片段：`use std::ops::{Deref, DerefMut}; use std::sync::Arc; use std::sync::atomic::AtomicUsize; use std::sync::atomic::Ordering::SeqCst;`
  - `vendor/futures/src/sync/oneshot.rs:L2-L6`：关键词命中
    代码片段：`use std::sync::Arc; use std::sync::atomic::AtomicBool; use std::sync::atomic::Ordering::SeqCst; use std::error::Error;`
- 与 2023 Starry 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/kernel/src/syscall/mod.rs:L68-L72`：关键词命中
    代码片段：`} pub fn handle_syscall(uctx: &mut UserContext) { let Some(sysno) = Sysno::new(uctx.sysno()) else { warn!("Invalid syscall number: {}", uctx.sysno());`
  - `src/kernel/src/syscall/fs/ctl.rs:L227-L231`：关键词命中
    代码片段：`} // Directory buffer for getdents64 syscall struct DirBuffer { buf: Vec<u8>,`
  - `modules/axhal/src/trap.rs:L21-L25`：关键词命中
    代码片段：`// 需要分离用户态使用 #[cfg(feature = "monolithic")] fn handle_syscall(syscall_id: usize, args: [usize; 6]) -> isize; #[cfg(feature = "paging")]`
  - `modules/axhal/src/arch/riscv/trap.rs:L15-L19`：关键词命中
    代码片段：`#[cfg(feature = "monolithic")] use crate::trap::handle_syscall; use super::{disable_irqs, enable_irqs, TrapFrame};`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2023 Starry 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：core, marker, nonnull, phantomdata, physaddr, ptr, pub, type。（置信度：high）
  证据：
  - `src/vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
- 与 2023 Starry 在“设备驱动”维度发现片段级代码相似度 0.79 （token=0.72, structure=0.92）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：cfg, crate, feature, self, use, virtio, virtiodevmeta。（置信度：high）
  证据：
  - `src/vendor/axdriver/src/drivers.rs:L9-L13`：关键词命中
    代码片段：`pub use super::dummy::*; use crate::AxDeviceEnum; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use driver_common::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 函数/符号名重合：与 2023 Starry 在“设备驱动”维度发现 3 个同名定义：generated_name_override, generated_link_name_override, is_valid_identifier。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src/vendor/bindgen/callbacks.rs:L38-L38`：fn generated_name_override
    代码片段：`fn generated_name_override(`
  - `vendor/bindgen/callbacks.rs:L41-L41`：fn generated_name_override
    代码片段：`fn generated_name_override(`
  - `src/vendor/bindgen/callbacks.rs:L47-L47`：fn generated_link_name_override
    代码片段：`fn generated_link_name_override(`
  - `vendor/bindgen/callbacks.rs:L50-L50`：fn generated_link_name_override
    代码片段：`fn generated_link_name_override(`
- 函数/符号名重合：与 2023 Starry 在“文件系统”维度发现 4 个同名定义：include_file, offset_of_field, get_included_file_name, location。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src/vendor/bindgen/callbacks.rs:L106-L106`：fn include_file
    代码片段：`fn include_file(&self, _filename: &str) {}`
  - `vendor/bindgen/callbacks.rs:L103-L103`：fn include_file
    代码片段：`fn include_file(&self, _filename: &str) {}`
  - `src/vendor/bindgen/clang.rs:L895-L895`：fn offset_of_field
    代码片段：`pub(crate) fn offset_of_field(&self) -> Result<usize, LayoutError> {`
  - `vendor/bindgen/clang.rs:L893-L893`：fn offset_of_field
    代码片段：`pub(crate) fn offset_of_field(&self) -> Result<usize, LayoutError> {`
- 与 2023 Starry 在“文件系统”维度发现片段级代码相似度 0.63 （token=0.50, structure=0.88）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, debug, derive, pub。（置信度：medium）
  证据：
  - `src/kernel/src/file/fs.rs:L42-L46`：struct VfsCredentials
    代码片段：`#[derive(Debug, Clone, Copy)] pub struct VfsCredentials { pub uid: u32, pub gid: u32,`
  - `vendor/fatfs/src/fs.rs:L29-L33`：enum FatType
    代码片段：`/// 'FatType' values are based on the size of File Allocation Table entry. #[derive(Copy, Clone, Eq, PartialEq, Debug)] pub enum FatType { /// 12 bits per FAT entry Fat12,`
- 函数/符号名重合：与 2023 Starry 在“内存管理”维度发现 4 个同名定义：process_comment, raw_comment, comment, kind。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src/vendor/bindgen/callbacks.rs:L146-L146`：fn process_comment
    代码片段：`fn process_comment(&self, _comment: &str) -> Option<String> {`
  - `vendor/bindgen/callbacks.rs:L135-L135`：fn process_comment
    代码片段：`fn process_comment(&self, _comment: &str) -> Option<String> {`
  - `src/vendor/bindgen/clang.rs:L393-L393`：fn raw_comment
    代码片段：`pub(crate) fn raw_comment(&self) -> Option<String> {`
  - `vendor/bindgen/clang.rs:L391-L391`：fn raw_comment
    代码片段：`pub(crate) fn raw_comment(&self) -> Option<String> {`
- 与 2023 Starry 在“同步机制”维度发现片段级代码相似度 0.70 （token=0.54, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：debug, derive, option, struct。（置信度：medium）
  证据：
  - `src/kernel/src/file/record_lock.rs:L42-L46`：struct LockInterval
    代码片段：`/// means "through EOF" (infinite range). #[derive(Debug, Clone)] pub struct LockInterval { pub start: i64, pub end: Option<i64>,`
  - `vendor/futures/src/sync/bilock.rs:L37-L41`：struct Inner
    代码片段：`#[derive(Debug)] struct Inner<T> { state: AtomicUsize, inner: Option<UnsafeCell<T>>,`
- 与 2023 Starry 在“同步机制”维度发现片段级代码相似度 0.67 （token=0.54, structure=0.91）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：debug, derive, pub, struct。（置信度：medium）
  证据：
  - `src/kernel/src/file/record_lock.rs:L42-L46`：struct LockInterval
    代码片段：`/// means "through EOF" (infinite range). #[derive(Debug, Clone)] pub struct LockInterval { pub start: i64, pub end: Option<i64>,`
  - `vendor/futures/src/sync/bilock.rs:L32-L36`：struct BiLock
    代码片段：`/// these two and then using each independently in a futures-powered fashion. #[derive(Debug)] pub struct BiLock<T> { inner: Arc<Inner<T>>, }`
- 结构体/类型重合：与 2023 Starry 在“文件系统”维度发现 3 个同名定义：File, UnsavedFile, DepfileSpec。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src/vendor/bindgen/clang.rs:L1746-L1746`：struct File
    代码片段：`pub(crate) struct File {`
  - `vendor/bindgen/clang.rs:L1736-L1736`：struct File
    代码片段：`pub(crate) struct File {`
  - `src/vendor/bindgen/clang.rs:L2026-L2026`：struct UnsavedFile
    代码片段：`pub(crate) struct UnsavedFile {`
  - `vendor/bindgen/clang.rs:L1916-L1916`：struct UnsavedFile
    代码片段：`pub(crate) struct UnsavedFile {`
- 结构体/类型重合：与 2023 Starry 在“内存管理”维度发现 4 个同名定义：Comment, CommentChildrenIterator, CommentAttribute, CommentAttributesIterator。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src/vendor/bindgen/clang.rs:L1649-L1649`：struct Comment
    代码片段：`pub(crate) struct Comment {`
  - `vendor/bindgen/clang.rs:L1639-L1639`：struct Comment
    代码片段：`pub(crate) struct Comment {`
  - `src/vendor/bindgen/clang.rs:L1685-L1685`：struct CommentChildrenIterator
    代码片段：`pub(crate) struct CommentChildrenIterator {`
  - `vendor/bindgen/clang.rs:L1675-L1675`：struct CommentChildrenIterator
    代码片段：`pub(crate) struct CommentChildrenIterator {`
- 函数/符号名重合：与 2023 Starry 在“调度与任务管理”维度发现 2 个同名定义：process_comment, dump_preprocessed_input。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/vendor/bindgen/callbacks.rs:L146-L146`：fn process_comment
    代码片段：`fn process_comment(&self, _comment: &str) -> Option<String> {`
  - `vendor/bindgen/callbacks.rs:L135-L135`：fn process_comment
    代码片段：`fn process_comment(&self, _comment: &str) -> Option<String> {`
  - `src/vendor/bindgen/lib.rs:L386-L386`：fn dump_preprocessed_input
    代码片段：`pub fn dump_preprocessed_input(&self) -> io::Result<()> {`
  - `vendor/bindgen/lib.rs:L346-L346`：fn dump_preprocessed_input
    代码片段：`pub fn dump_preprocessed_input(&self) -> io::Result<()> {`
- 函数/符号名重合：与 2023 Starry 在“同步机制”维度发现 1 个同名定义：blocklisted_type_implements_trait。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/vendor/bindgen/callbacks.rs:L121-L121`：fn blocklisted_type_implements_trait
    代码片段：`fn blocklisted_type_implements_trait(`
  - `vendor/bindgen/callbacks.rs:L118-L118`：fn blocklisted_type_implements_trait
    代码片段：`fn blocklisted_type_implements_trait(`
- 结构体/类型重合：与 2023 Starry 在“中断与异常”维度发现 1 个同名定义：Timer。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/vendor/bindgen/time.rs:L6-L6`：struct Timer
    代码片段：`pub struct Timer<'a> {`
  - `vendor/bindgen/time.rs:L6-L6`：struct Timer
    代码片段：`pub struct Timer<'a> {`
- 结构体/类型重合：与 2023 Starry 在“系统调用”维度发现 1 个同名定义：ParseCallbacks。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/vendor/bindgen/callbacks.rs:L24-L24`：trait ParseCallbacks
    代码片段：`pub trait ParseCallbacks: fmt::Debug {`
  - `vendor/bindgen/callbacks.rs:L27-L27`：trait ParseCallbacks
    代码片段：`pub trait ParseCallbacks: fmt::Debug {`
- 文件路径重合：与 2023 Starry 在“设备驱动”维度出现同名文件源码路径 `src/vendor/axdriver/src/drivers.rs` / `modules/axdriver/src/drivers.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/vendor/axdriver/src/drivers.rs:L9-L13`：关键词命中
    代码片段：`pub use super::dummy::*; use crate::AxDeviceEnum; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use driver_common::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 文件路径重合：与 2023 Starry 在“设备驱动”维度出现同名文件源码路径 `src/vendor/virtio-drivers/src/hal.rs` / `vendor/virtio-drivers/src/hal.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
- 文件路径重合：与 2023 Starry 在“文件系统”维度出现同名文件源码路径 `src/kernel/src/file/fs.rs` / `vendor/fatfs/src/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/kernel/src/file/fs.rs:L487-L491`：关键词命中
    代码片段：`Kstat { dev: metadata.device, ino: metadata.inode, mode, nlink,`
  - `vendor/fatfs/src/fs.rs:L21-L25`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
- 文件路径重合：与 2023 Starry 在“中断与异常”维度出现同名文件源码路径 `src/vendor/axcpu/src/trap.rs` / `modules/axhal/src/trap.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/vendor/axcpu/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use memory_addr::VirtAddr;`
  - `modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use crate_interface::{call_interface, def_interface};`
- 文件路径重合：与 2023 Starry 在“中断与异常”维度出现同名文件源码路径 `src/vendor/axcpu/src/trap.rs` / `modules/axruntime/src/trap.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/vendor/axcpu/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use memory_addr::VirtAddr;`
  - `modules/axruntime/src/trap.rs:L1-L3`：关键词命中
    代码片段：`/// 仅用作非宏内核下的trap入口 struct TrapHandlerImpl;`
- 文件路径重合：与 2023 Starry 在“调度与任务管理”维度出现同名文件源码路径 `src/vendor/x86/src/task.rs` / `vendor/x86/src/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`
  - `vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`

## 相似点

- 与 2023 Starry 同属 ucore-variant 风格。（置信度：medium）
- 与 2023 Starry 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/vendor/uart_16550/src/mmio.rs:L6-L10`：关键词命中
    代码片段：`use crate::{LineStsFlags, WouldBlockError}; /// A memory-mapped UART. #[derive(Debug)] pub struct MmioSerialPort {`
  - `src/vendor/axdriver/src/drivers.rs:L9-L13`：关键词命中
    代码片段：`pub use super::dummy::*; use crate::AxDeviceEnum; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 与 2023 Starry 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/kernel/src/file/fs.rs:L487-L491`：关键词命中
    代码片段：`Kstat { dev: metadata.device, ino: metadata.inode, mode, nlink,`
  - `src/kernel/src/file/mod.rs:L45-L49`：关键词命中
    代码片段：`}, }; // Inode-flags helpers are defined as 'pub fn' / 'pub const' below — no // separate 'pub use self::' needed. use crate::task::{AX_FILE_LIMIT, AsThread};`
- 与 2023 Starry 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/kernel/src/task/timer.rs:L50-L54`：关键词命中
    代码片段：`} /// The type of interval timer. #[repr(i32)] #[allow(non_camel_case_types)]`
  - `src/vendor/axcpu/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use memory_addr::VirtAddr;`
- 与 2023 Starry 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/kernel/src/mm/access.rs:L87-L91`：关键词命中
    代码片段：`// TODO: this is inefficient, but we have to do this instead of // querying the page table since the page might has not been // allocated yet. let curr = current();`
  - `src/kernel/src/mm/loader.rs:L38-L42`：关键词命中
    代码片段：`#[cfg(not(any(target_arch = "aarch64", target_arch = "loongarch64")))] { // ARMv8 (aarch64) and LoongArch64 use separate page tables for user space // (aarch64: TTBR0_EL1, Loong...`
- 与 2023 Starry 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. //! //! 本模块负责：`
  - `src/kernel/src/task/ops.rs:L18-L22`：关键词命中
    代码片段：`use super::{ futex_table_for, send_signal_thread_inner, send_signal_to_process, send_signal_to_thread, AsThread, FutexKey, ProcessData, Thread, TimerState, }; use crate::file::r...`
- 与 2023 Starry 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/kernel/src/file/record_lock.rs:L1-L3`：关键词命中
    代码片段：`//! POSIX and OFD record lock manager. //! //! POSIX record locks are **process-owned**, operate on **inode byte ranges**,`
  - `src/kernel/src/syscall/sync/futex.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::Ordering; use axerrno::{AxError, AxResult, LinuxError};`
- 与 2023 Starry 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/kernel/src/syscall/mod.rs:L68-L72`：关键词命中
    代码片段：`} pub fn handle_syscall(uctx: &mut UserContext) { let Some(sysno) = Sysno::new(uctx.sysno()) else { warn!("Invalid syscall number: {}", uctx.sysno());`
  - `src/kernel/src/syscall/fs/ctl.rs:L227-L231`：关键词命中
    代码片段：`} // Directory buffer for getdents64 syscall struct DirBuffer { buf: Vec<u8>,`
- 与 2023 Starry 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2023 Starry 的语言构成不同：待测作品为 {'json': 287865, 'markdown': 85452, 'build': 45849, 'toml': 8377, 'rust': 3290946, 'make': 1135, 'text': 7604, 'asm': 1293, 'yaml': 273, 'c': 28008}，历史样本为 {'json': 282358, 'build': 31906, 'markdown': 71620, 'toml': 1265, 'rust': 4920352, 'yaml': 478, 'text': 28242, 'asm': 5288, 'c': 134844, 'cpp': 9645, 'make': 486}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：34
- 含证据关键结论数：34（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
