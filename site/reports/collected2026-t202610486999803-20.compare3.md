# 2026 T202610486999803 比较报告

- 对比历史仓库：2022 图漏图森破
- 生成时间：2026-07-02T05:02:00.399785+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 图漏图森破（来源：赛事历史作品）：画像相似度 score=9.69；同属 ucore-variant 风格; 语言构成相似度 0.92; OS 维度重合度 1.00; 代码规模接近度 0.86

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 图漏图森破 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/drivers/char/uart.c:L1-L3`：关键词命中
    代码片段：`#include "drivers/char/uart.h" #include "drivers/core/driver_core.h" #include "drivers/core/driver_class.h"`
  - `kernel/drivers/block/loop.c:L54-L58`：关键词命中
    代码片段：`for (int i = 0; i < MAX_LOOP_DEVS; i++) { memset(&g_loop[i], 0, sizeof(g_loop[i])); /* LOCK_ORDER: initialize each per-device loop lock at boot. */ spin_init(&g_loop[i].lock); g...`
  - `kernel/driver/Sd.c:L1-L5`：关键词命中
    代码片段：`#include <Platform.h> #include <Spi.h> #include <Uart.h> #include <Driver.h> #include <MemoryConfig.h>`
  - `kernel/driver/Print.c:L1-L3`：关键词命中
    代码片段：`#include <Driver.h> #include <Riscv.h> #include <Spinlock.h>`
- 与 2022 图漏图森破 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/vfs.c:L1-L4`：关键词命中
    代码片段：`/* * A20OS — Virtual Filesystem (VFS) * * Provides a unified fd-based interface over:`
  - `kernel/fs/ext4.c:L1-L5`：关键词命中
    代码片段：`#include "fs/ext4.h" #include "fs/file.h" #include "fs/vfs.h" #include "fs/block_cache.h" #include "mm/mm.h"`
  - `kernel/fs/Fat.c:L1-L5`：关键词命中
    代码片段：`#include <Defs.h> #include "Bio.h" #include "Fat.h" #include "Sleeplock.h" #include "Spinlock.h"`
  - `kernel/fs/File.c:L101-L105`：关键词命中
    代码片段：`// Read from file f. // addr is a user virtual address. int fileread(struct File* f, bool isUser, u64 addr, int n) { int r = 0;`
- 与 2022 图漏图森破 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/core/trap.c:L1-L3`：关键词命中
    代码片段：`#include "core/trap.h" #include "proc/proc.h" #include "proc/signal.h"`
  - `kernel/include/core/trap.h:L7-L11`：关键词命中
    代码片段：`/* * Architecture-specific types and symbols are now in arch/trap.h, * pulled in via arch.h (included from defs.h): *   - trap_context_t, task_context_t`
  - `kernel/trap/Exec.c:L9-L13`：关键词命中
    代码片段：`#include <Sysarg.h> #include <Debug.h> #include <Trap.h> #include <Sysfile.h> #include <uapi/linux/Auxvec.h>`
  - `kernel/trap/Trap.c:L1-L5`：关键词命中
    代码片段：`#include <Riscv.h> #include <Driver.h> #include <Timer.h> #include <Trap.h> #include <Process.h>`
- 与 2022 图漏图森破 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mm/mm.c:L11-L15`：关键词命中
    代码片段：`#include "proc/proc.h" static inline int pte_user_readable(uint64_t pte) { return arch_pte_is_leaf(pte) && (pte & PTE_U) && (pte & PTE_R); }`
  - `kernel/mm/vm.c:L20-L24`：关键词命中
    代码片段：`*   total_vm/rss/locked_vm while holding the address-space write exclusion *   defined by MM_LOCK_MODEL. * - Page-table write paths live in COW/demand faults, file/VMO faults, *...`
  - `kernel/memory/Page.c:L18-L22`：关键词命中
    代码片段：`} int pageRemove(u64 *pgdir, u64 va) { u64 *pte; u64 pa = pageLookup(pgdir, va, &pte);`
  - `kernel/memory/MemoryInit.c:L37-L41`：关键词命中
    代码片段：`static void virtualMemory() { u64 va, pa; pageInsert(kernelPageDirectory, UART_V, UART, PTE_READ | PTE_WRITE | PTE_ACCESSED | PTE_DIRTY); va = CLINT_V, pa = CLINT; for (u64 i =...`
- 与 2022 图漏图森破 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/pid.c:L1-L3`：关键词命中
    代码片段：`#include "proc/proc.h" #include "proc/proc_internal.h" #include "core/lock.h"`
  - `kernel/proc/exec.c:L16-L20`：关键词命中
    代码片段：`*/ #include "proc/proc.h" #include "proc/proc_internal.h" #include "proc/signal.h"`
  - `kernel/yield/Thread.c:L1-L3`：关键词命中
    代码片段：`#include <Thread.h> #include <Riscv.h> #include <MemoryConfig.h>`
  - `kernel/fs/Fat.c:L12-L16`：关键词命中
    代码片段：`#include <FileSystem.h> #include <Sysfile.h> #include <Thread.h> #include <Riscv.h>`
- 与 2022 图漏图森破 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/core/sync.c:L9-L13`：关键词命中
    代码片段：`if (!q) return; spin_init(&q->lock); spin_set_debug(&q->lock, "wait_queue", q); q->head = NULL;`
  - `kernel/include/core/lock.h:L11-L15`：关键词命中
    代码片段：`/* * Global lock order contract (outermost -> innermost). * For the full driver-private lock contracts see docs/driver-lock-order.md. *`
  - `kernel/lock/Spinlock.c:L1-L3`：关键词命中
    代码片段：`#include "Spinlock.h" #include "Hart.h" #include "Interrupt.h"`
  - `kernel/lock/Sleeplock.c:L3-L7`：关键词命中
    代码片段：`#include "Sleeplock.h" #include "Process.h" #include "Spinlock.h" #include "Driver.h" #include <Debug.h>`
- 与 2022 图漏图森破 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/syscall/syscall.c:L1-L4`：关键词命中
    代码片段：`/* * A20OS syscall dispatcher. * * Keep ABI dispatch here; concrete syscall implementations live in sys_*.c.`
  - `kernel/abi/linux/sys_signal.c:L99-L103`：关键词命中
    代码片段：`int64_t sys_sigreturn(trap_context_t *ctx) { /* * Linux syscall 139 is rt_sigreturn on RISC-V, LoongArch and AArch64. * Restoring from task->sig_saved_ctx is not sufficient: it...`
  - `kernel/trap/Syscall.c:L1-L3`：关键词命中
    代码片段：`#include <Syscall.h> #include <Driver.h> #include <Process.h>`
  - `kernel/system/SignalTrampoline.S:L4-L6`：关键词命中
    代码片段：`signalTrampoline: li a7, 139 ecall`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 宏名重合：与 2022 图漏图森破 在“内存管理”维度发现 3 个同名定义：PTE_COW, PTE_USER, PAGE_OFFSET。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/arch/aarch64/include/page_table.h:L44-L44`：macro PTE_COW
    代码片段：`#define PTE_COW            (1UL << 62)`
  - `include/Page.h:L71-L71`：macro PTE_COW
    代码片段：`#define PTE_COW (1ll << 8)`
  - `kernel/arch/aarch64/include/page_table.h:L50-L50`：macro PTE_USER
    代码片段：`#define PTE_USER           (PTE_V | PTE_R | PTE_W | PTE_X | PTE_U | PTE_A | PTE_D | PTE_MAT1 | PTE_LEAF)`
  - `include/Page.h:L68-L68`：macro PTE_USER
    代码片段：`#define PTE_USER (1ll << 4)`
- 宏名重合：与 2022 图漏图森破 在“文件系统”维度发现 1 个同名定义：PAGE_OFFSET。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/arch/aarch64/include/platform.h:L17-L17`：macro PAGE_OFFSET
    代码片段：`#define PAGE_OFFSET        0x0000008000000000UL`
  - `include/Page.h:L13-L13`：macro PAGE_OFFSET
    代码片段：`#define PAGE_OFFSET(x, y) ((x) & ((y)-1))`
- 文件路径重合：与 2022 图漏图森破 在“中断与异常”维度出现同名文件源码路径 `kernel/core/trap.c` / `kernel/trap/Trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/core/trap.c:L1-L3`：关键词命中
    代码片段：`#include "core/trap.h" #include "proc/proc.h" #include "proc/signal.h"`
  - `kernel/trap/Trap.c:L1-L5`：关键词命中
    代码片段：`#include <Riscv.h> #include <Driver.h> #include <Timer.h> #include <Trap.h> #include <Process.h>`
- 文件路径重合：与 2022 图漏图森破 在“系统调用”维度出现同名文件源码路径 `kernel/syscall/syscall.c` / `kernel/trap/Syscall.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/syscall/syscall.c:L1-L4`：关键词命中
    代码片段：`/* * A20OS syscall dispatcher. * * Keep ABI dispatch here; concrete syscall implementations live in sys_*.c.`
  - `kernel/trap/Syscall.c:L1-L3`：关键词命中
    代码片段：`#include <Syscall.h> #include <Driver.h> #include <Process.h>`
- 文件路径重合：与 2022 图漏图森破 在“系统调用”维度出现同名文件源码路径 `kernel/include/sys/syscall.h` / `root/usr/usr/include/syscall.h`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/include/sys/syscall.h:L1-L4`：macro _SYSCALL_H
    代码片段：`#ifndef _SYSCALL_H #define _SYSCALL_H #include "core/types.h"`
  - `root/usr/usr/include/syscall.h:L1-L1`：关键词命中
    代码片段：`#include <sys/syscall.h>`

## 相似点

- 与 2022 图漏图森破 同属 ucore-variant 风格。（置信度：medium）
- 与 2022 图漏图森破 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/drivers/char/uart.c:L1-L3`：关键词命中
    代码片段：`#include "drivers/char/uart.h" #include "drivers/core/driver_core.h" #include "drivers/core/driver_class.h"`
  - `kernel/drivers/block/loop.c:L54-L58`：关键词命中
    代码片段：`for (int i = 0; i < MAX_LOOP_DEVS; i++) { memset(&g_loop[i], 0, sizeof(g_loop[i])); /* LOCK_ORDER: initialize each per-device loop lock at boot. */ spin_init(&g_loop[i].lock); g...`
- 与 2022 图漏图森破 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/vfs.c:L1-L4`：关键词命中
    代码片段：`/* * A20OS — Virtual Filesystem (VFS) * * Provides a unified fd-based interface over:`
  - `kernel/fs/ext4.c:L1-L5`：关键词命中
    代码片段：`#include "fs/ext4.h" #include "fs/file.h" #include "fs/vfs.h" #include "fs/block_cache.h" #include "mm/mm.h"`
- 与 2022 图漏图森破 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/core/trap.c:L1-L3`：关键词命中
    代码片段：`#include "core/trap.h" #include "proc/proc.h" #include "proc/signal.h"`
  - `kernel/include/core/trap.h:L7-L11`：关键词命中
    代码片段：`/* * Architecture-specific types and symbols are now in arch/trap.h, * pulled in via arch.h (included from defs.h): *   - trap_context_t, task_context_t`
- 与 2022 图漏图森破 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mm/mm.c:L11-L15`：关键词命中
    代码片段：`#include "proc/proc.h" static inline int pte_user_readable(uint64_t pte) { return arch_pte_is_leaf(pte) && (pte & PTE_U) && (pte & PTE_R); }`
  - `kernel/mm/vm.c:L20-L24`：关键词命中
    代码片段：`*   total_vm/rss/locked_vm while holding the address-space write exclusion *   defined by MM_LOCK_MODEL. * - Page-table write paths live in COW/demand faults, file/VMO faults, *...`
- 与 2022 图漏图森破 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/pid.c:L1-L3`：关键词命中
    代码片段：`#include "proc/proc.h" #include "proc/proc_internal.h" #include "core/lock.h"`
  - `kernel/proc/exec.c:L16-L20`：关键词命中
    代码片段：`*/ #include "proc/proc.h" #include "proc/proc_internal.h" #include "proc/signal.h"`
- 与 2022 图漏图森破 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/core/sync.c:L9-L13`：关键词命中
    代码片段：`if (!q) return; spin_init(&q->lock); spin_set_debug(&q->lock, "wait_queue", q); q->head = NULL;`
  - `kernel/include/core/lock.h:L11-L15`：关键词命中
    代码片段：`/* * Global lock order contract (outermost -> innermost). * For the full driver-private lock contracts see docs/driver-lock-order.md. *`
- 与 2022 图漏图森破 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/syscall/syscall.c:L1-L4`：关键词命中
    代码片段：`/* * A20OS syscall dispatcher. * * Keep ABI dispatch here; concrete syscall implementations live in sys_*.c.`
  - `kernel/abi/linux/sys_signal.c:L99-L103`：关键词命中
    代码片段：`int64_t sys_sigreturn(trap_context_t *ctx) { /* * Linux syscall 139 is rt_sigreturn on RISC-V, LoongArch and AArch64. * Restoring from task->sig_saved_ctx is not sufficient: it...`
- 与 2022 图漏图森破 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 图漏图森破 的语言构成不同：待测作品为 {'build': 2900, 'markdown': 15529, 'c': 347925, 'asm': 11000, 'make': 534, 'text': 235}，历史样本为 {'json': 20, 'make': 28, 'build': 326, 'markdown': 3993, 'c': 405628, 'asm': 408, 'cpp': 36715, 'text': 55}。（置信度：medium）

## 可能创新点

- 待测作品包含 255 个历史样本 2022 图漏图森破 中未见的独特函数/类型定义（如 `sys_mremap`, `arch_trap_ctx_get_kernel_stack`, `sys_sigtimedwait`, `sys_setsockopt`, `arch_pci_config_write32`, `arch_trap_ctx_set_kernel_stack`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/abi/linux/sys_mm.c:L274-L274`：fn sys_mremap
    代码片段：`int64_t sys_mremap(uint64_t old_addr, size_t old_size, size_t new_size, int flags, uint64_t new_addr) {`
  - `kernel/arch/aarch64/include/trap_frame.h:L152-L152`：fn arch_trap_ctx_get_kernel_stack
    代码片段：`static inline uint64_t arch_trap_ctx_get_kernel_stack(const trap_context_t *ctx, uint64_t fallback) {`

## 附录：核验摘要

- 关键结论数：20
- 含证据关键结论数：20（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
