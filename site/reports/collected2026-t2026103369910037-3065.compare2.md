# 2026 T2026103369910037 比较报告

- 对比历史仓库：2024 NPUcore-重生之我是菜狗
- 生成时间：2026-07-02T09:01:42.281265+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 NPUcore-重生之我是菜狗（来源：赛事历史作品）：画像相似度 score=9.09；同属 ucore-variant 风格; 语言构成相似度 0.78; OS 维度重合度 1.00; 代码规模接近度 0.53

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 NPUcore-重生之我是菜狗 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `modules/axdriver/src/virtio.rs:L21-L25`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2024 NPUcore-重生之我是菜狗 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `modules/axfs-vfs/src/lib.rs:L35-L39`：关键词命中
    代码片段：`//! | ['symlink()'](VfsNodeOps::symlink) | Create a symbolic link | directory | //! //! [inodes]: https://en.wikipedia.org/wiki/Inode #![no_std]`
  - `modules/axfs/src/vfs/mod.rs:L1-L3`：关键词命中
    代码片段：`mod dentry; mod file; mod mount;`
  - `os/src/fs/mod.rs:L111-L115`：关键词命中
    代码片段：`pub fn get_cwd(&self) -> Option<String> { let inode = self.file.get_dirtree_node(); let inode = match inode { Some(inode) => inode,`
  - `os/src/fs/cache.rs:L193-L197`：关键词命中
    代码片段：`/// Every time kernel tried to alloc this pagecache this number will increase 1(at most 1) /// Every time out of memory occurred this number will decrease 1(at least 0) /// When...`
- 与 2024 NPUcore-重生之我是菜狗 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`// MeteorOS/axhal/src/trap.rs // 通用的陷阱处理程序接口和宏定义`
  - `os/src/timer.rs:L21-L25`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/arch/la64/trap/mod.rs:L3-L7`：关键词命中
    代码片段：`use self::context::GeneralRegs; use super::register::{self, Exception, Interrupt, Trap, ERA}; use super::{pre_start_init, MErrEntry}; use crate::arch::la64::laflex::LAFlexPageTa...`
- 与 2024 NPUcore-重生之我是菜狗 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `modules/axhal/src/lib.rs:L19-L23`：关键词命中
    代码片段：`//! - 'smp': Enable SMP (symmetric multiprocessing) support. //! - 'fp_simd': Enable floating-point and SIMD support. //! - 'paging': Enable page table manipulation. //! - 'irq'...`
  - `modules/axhal/src/trap.rs:L5-L9`：关键词命中
    代码片段：`use memory_addr::VirtAddr; #[cfg(feature = "paging")] pub use page_table::MappingFlags; #[cfg(not(feature = "paging"))] pub type MappingFlags = usize;`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `os/src/mm/map_area.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::Debug; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2024 NPUcore-重生之我是菜狗 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `modules/axprocess/src/task.rs:L1-L3`：关键词命中
    代码片段：`// modules/axprocess/src/task.rs // 线程管理`
  - `modules/axsyscall/src/task.rs:L3-L7`：关键词命中
    代码片段：`use axfs::vfs::OpenOptions; use axfs::vfs::VFS; /// Task/Process related system calls use axlog::{info, warn}; use axmem::{translated_ref, translated_refmut, translated_str};`
  - `os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `os/src/task/task.rs:L387-L391`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 与 2024 NPUcore-重生之我是菜狗 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `modules/axsyscall/src/sync.rs:L8-L12`：关键词命中
    代码片段：`current_task, get_time_ms, schedule, take_current_task, wakeup_task, }; use axsync::Mutex; use core::mem::size_of; use lazy_static::lazy_static;`
  - `modules/axfs/src/lib.rs:L21-L25`：关键词命中
    代码片段：`use axdriver::{AxDeviceContainer, prelude::*}; use axfs_vfs::VfsOps; use axsync::Mutex; use lazyinit::LazyInit;`
  - `os/src/fs/mod.rs:L34-L38`：关键词命中
    代码片段：`}; use lazy_static::*; use spin::Mutex; lazy_static! {`
  - `os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::{arch::BLOCK_SZ, config::PAGE_SIZE, drivers::BLOCK_DEVICE};`
- 与 2024 NPUcore-重生之我是菜狗 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `api/syslib/src/syscall.rs:L119-L123`：关键词命中
    代码片段：`/// Raw system call #[inline(always)] fn syscall(id: usize, args: [usize; 6]) -> isize { let mut ret: isize; unsafe {`
  - `modules/axhal/src/trap.rs:L37-L41`：关键词命中
    代码片段：`fn handle_irq(irq_num: usize); // 需要分离用户态使用 fn handle_syscall(syscall_id: usize, args: [usize; 6], tf: &mut TrapFrame) -> isize; #[cfg(feature = "paging")]`
  - `os/src/syscall/fs.rs:L663-L667`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `os/src/syscall/mod.rs:L122-L126`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2024 NPUcore-重生之我是菜狗 在“系统调用”维度发现 4 个同名定义：syscall, sys_dup, sys_close, sys_read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `api/syslib/src/syscall.rs:L121-L121`：fn syscall
    代码片段：`fn syscall(id: usize, args: [usize; 6]) -> isize {`
  - `user/src/syscall.rs:L82-L82`：fn syscall
    代码片段：`fn syscall(id: usize, args: [usize; 3]) -> isize {`
  - `api/syslib/src/syscall.rs:L147-L147`：fn sys_dup
    代码片段：`pub fn sys_dup(oldfd: usize) -> isize {`
  - `user/src/syscall.rs:L88-L88`：fn sys_dup
    代码片段：`pub fn sys_dup(fd: usize) -> isize {`
- 函数/符号名重合：与 2024 NPUcore-重生之我是菜狗 在“内存管理”维度发现 2 个同名定义：from, fmt。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/memory_addr/src/lib.rs:L262-L262`：fn from
    代码片段：`fn from(addr: usize) -> Self {`
  - `os/src/mm/address.rs:L56-L56`：fn from
    代码片段：`fn from(v: usize) -> Self {`
  - `crates/memory_addr/src/lib.rs:L349-L349`：fn fmt
    代码片段：`fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {`
  - `os/src/mm/address.rs:L23-L23`：fn fmt
    代码片段：`fn fmt(&self, f: &mut Formatter<'_>) -> fmt::Result {`
- 函数/符号名重合：与 2024 NPUcore-重生之我是菜狗 在“调度与任务管理”维度发现 1 个同名定义：sys_yield。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `api/syslib/src/syscall.rs:L516-L516`：fn sys_yield
    代码片段：`pub fn sys_yield() -> isize {`
  - `user/src/syscall.rs:L120-L120`：fn sys_yield
    代码片段：`pub fn sys_yield() -> isize {`
- 结构体/类型重合：与 2024 NPUcore-重生之我是菜狗 在“内存管理”维度发现 2 个同名定义：PhysAddr, VirtAddr。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/memory_addr/src/lib.rs:L75-L75`：struct PhysAddr
    代码片段：`pub struct PhysAddr(usize);`
  - `os/src/mm/address.rs:L7-L7`：struct PhysAddr
    代码片段：`pub struct PhysAddr(pub usize);`
  - `crates/memory_addr/src/lib.rs:L82-L82`：struct VirtAddr
    代码片段：`pub struct VirtAddr(usize);`
  - `os/src/mm/address.rs:L11-L11`：struct VirtAddr
    代码片段：`pub struct VirtAddr(pub usize);`
- 文件路径重合：与 2024 NPUcore-重生之我是菜狗 在“设备驱动”维度出现同名文件源码路径 `modules/axhal/src/platform/riscv64_qemu_virt/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `modules/axhal/src/platform/riscv64_qemu_virt/console.rs:L1-L3`：关键词命中
    代码片段：`/// Writes a byte to the console. pub fn putchar(c: u8) { #[allow(deprecated)]`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
- 文件路径重合：与 2024 NPUcore-重生之我是菜狗 在“调度与任务管理”维度出现同名文件源码路径 `modules/axprocess/src/task.rs` / `os/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `modules/axprocess/src/task.rs:L1-L3`：关键词命中
    代码片段：`// modules/axprocess/src/task.rs // 线程管理`
  - `os/src/task/task.rs:L387-L391`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 文件路径重合：与 2024 NPUcore-重生之我是菜狗 在“调度与任务管理”维度出现同名文件源码路径 `modules/axsyscall/src/task.rs` / `os/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `modules/axsyscall/src/task.rs:L3-L7`：关键词命中
    代码片段：`use axfs::vfs::OpenOptions; use axfs::vfs::VFS; /// Task/Process related system calls use axlog::{info, warn}; use axmem::{translated_ref, translated_refmut, translated_str};`
  - `os/src/task/task.rs:L387-L391`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`

## 相似点

- 与 2024 NPUcore-重生之我是菜狗 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 NPUcore-重生之我是菜狗 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `modules/axdriver/src/virtio.rs:L21-L25`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 与 2024 NPUcore-重生之我是菜狗 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `modules/axfs-vfs/src/lib.rs:L35-L39`：关键词命中
    代码片段：`//! | ['symlink()'](VfsNodeOps::symlink) | Create a symbolic link | directory | //! //! [inodes]: https://en.wikipedia.org/wiki/Inode #![no_std]`
  - `modules/axfs/src/vfs/mod.rs:L1-L3`：关键词命中
    代码片段：`mod dentry; mod file; mod mount;`
- 与 2024 NPUcore-重生之我是菜狗 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`// MeteorOS/axhal/src/trap.rs // 通用的陷阱处理程序接口和宏定义`
- 与 2024 NPUcore-重生之我是菜狗 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `modules/axhal/src/lib.rs:L19-L23`：关键词命中
    代码片段：`//! - 'smp': Enable SMP (symmetric multiprocessing) support. //! - 'fp_simd': Enable floating-point and SIMD support. //! - 'paging': Enable page table manipulation. //! - 'irq'...`
  - `modules/axhal/src/trap.rs:L5-L9`：关键词命中
    代码片段：`use memory_addr::VirtAddr; #[cfg(feature = "paging")] pub use page_table::MappingFlags; #[cfg(not(feature = "paging"))] pub type MappingFlags = usize;`
- 与 2024 NPUcore-重生之我是菜狗 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `modules/axprocess/src/task.rs:L1-L3`：关键词命中
    代码片段：`// modules/axprocess/src/task.rs // 线程管理`
  - `modules/axsyscall/src/task.rs:L3-L7`：关键词命中
    代码片段：`use axfs::vfs::OpenOptions; use axfs::vfs::VFS; /// Task/Process related system calls use axlog::{info, warn}; use axmem::{translated_ref, translated_refmut, translated_str};`
- 与 2024 NPUcore-重生之我是菜狗 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `modules/axsyscall/src/sync.rs:L8-L12`：关键词命中
    代码片段：`current_task, get_time_ms, schedule, take_current_task, wakeup_task, }; use axsync::Mutex; use core::mem::size_of; use lazy_static::lazy_static;`
  - `modules/axfs/src/lib.rs:L21-L25`：关键词命中
    代码片段：`use axdriver::{AxDeviceContainer, prelude::*}; use axfs_vfs::VfsOps; use axsync::Mutex; use lazyinit::LazyInit;`
- 与 2024 NPUcore-重生之我是菜狗 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `api/syslib/src/syscall.rs:L119-L123`：关键词命中
    代码片段：`/// Raw system call #[inline(always)] fn syscall(id: usize, args: [usize; 6]) -> isize { let mut ret: isize; unsafe {`
  - `modules/axhal/src/trap.rs:L37-L41`：关键词命中
    代码片段：`fn handle_irq(irq_num: usize); // 需要分离用户态使用 fn handle_syscall(syscall_id: usize, args: [usize; 6], tf: &mut TrapFrame) -> isize; #[cfg(feature = "paging")]`
- 与 2024 NPUcore-重生之我是菜狗 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 NPUcore-重生之我是菜狗 的语言构成不同：待测作品为 {'build': 2688, 'markdown': 1941, 'rust': 46785, 'c': 10575, 'toml': 297, 'asm': 482, 'make': 585}，历史样本为 {'json': 20, 'build': 395, 'markdown': 53, 'rust': 25306, 'asm': 306, 'text': 60, 'c': 383}。（置信度：medium）

## 可能创新点

- 待测作品包含 190 个历史样本 2024 NPUcore-重生之我是菜狗 中未见的独特函数/类型定义（如 `ax_udp_peek_from`, `ax_udp_recv_from`, `sys_linkat`, `ax_udp_send_to`, `sys_statx`, `sys_mount`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `api/meteoros_api/src/lib.rs:L322-L322`：fn ax_udp_peek_from
    代码片段：`pub fn ax_udp_peek_from(socket: &AxUdpSocketHandle, buf: &mut [u8]) -> AxResult<(usize, SocketAddr)>;`
  - `api/meteoros_api/src/lib.rs:L319-L319`：fn ax_udp_recv_from
    代码片段：`pub fn ax_udp_recv_from(socket: &AxUdpSocketHandle, buf: &mut [u8]) -> AxResult<(usize, SocketAddr)>;`

## 附录：核验摘要

- 关键结论数：22
- 含证据关键结论数：22（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
