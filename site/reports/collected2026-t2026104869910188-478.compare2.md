# 2026 T2026104869910188 比较报告

- 对比历史仓库：2024 蕾姆赛高队
- 生成时间：2026-07-02T09:02:23.568440+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 蕾姆赛高队（来源：赛事历史作品）：画像相似度 score=9.39；同属 ucore-variant 风格; 语言构成相似度 0.98; OS 维度重合度 1.00; 代码规模接近度 0.43

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 蕾姆赛高队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `include/virtio.h:L6-L10`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `include/console.h:L4-L8`：关键词命中
    代码片段：`void consoleinit(void); void consputc(int c); void consoleintr(int c); #endif`
- 与 2024 蕾姆赛高队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/file.c:L3-L7`：关键词命中
    代码片段：`// // filestat, fileclose may need vfs file operations? #include "types.h"`
  - `kernel/file.h:L1-L4`：关键词命中
    代码片段：`#include "kernel/xv6_fs.h" #include "kernel/vfs/vfs.h" #define PIPESIZE 512`
  - `src/fs/file.c:L139-L143`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0, rcnt = 0;`
  - `src/fs/ext4/ext4.c:L177-L181`：关键词命中
    代码片段：`/* Check if node is directory */ if (!ext4_inode_is_type(sb, enode->inode, EXT4_INODE_MODE_DIRECTORY)) { *has_children = false; return EOK;`
- 与 2024 蕾姆赛高队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L34-L38`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `src/kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
  - `src/kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
- 与 2024 蕾姆赛高队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/vm.c:L11-L15`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/page.h:L37-L41`：关键词命中
    代码片段：`// PFN is KERNBASE-relative so that mem_map[0] = page at KERNBASE (0x80000000). // This keeps all indices within [0, (PHYSTOP-KERNBASE)/PGSIZE). #define PFN_DOWN(pa)     (((uint...`
  - `src/mm/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`
  - `src/mm/mmap.c:L9-L13`：关键词命中
    代码片段：`#include "include/vm.h" #include "include/string.h" #include "include/kalloc.h" #include "include/ext4/ext4_init.h"`
- 与 2024 蕾姆赛高队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "file.h"`
  - `kernel/proc.h:L42-L46`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `src/mm/mm.c:L10-L14`：关键词命中
    代码片段：`#include "include/defs.h" #include "include/mm.h" #include "include/proc.h" // Create segment node`
  - `src/mm/vm.c:L6-L10`：关键词命中
    代码片段：`#include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h" #include "include/string.h"`
- 与 2024 蕾姆赛高队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "xv6_fs.h"`
  - `kernel/bio.c:L17-L21`：关键词命中
    代码片段：`#include "types.h" #include "param.h" #include "spinlock.h" #include "sleeplock.h" #include "arch.h"`
  - `src/fs/bio.c:L18-L22`：关键词命中
    代码片段：`#include "include/types.h" #include "include/param.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/riscv.h"`
  - `src/fs/file.c:L7-L11`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/param.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/ext4/ext4_init.h"`
- 与 2024 蕾姆赛高队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap.c:L85-L89`：关键词命中
    代码片段：`// Save init's registers for epc corruption recovery if(p->pid == 1) { p->saved_epc = p->trapframe->epc + 4;  // instruction after ecall p->saved_ra = p->trapframe->ra; p->saved...`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "errno.h"`
  - `src/kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "include/spinlock.h" #include "include/proc.h" #include "include/syscall.h" #include "include/sysinfo.h" #include "include/kalloc.h"`
  - `include/syscall.h:L61-L65`：关键词命中
    代码片段：`int argaddr(int n, uint64 *ip); int argstr(int n, char *buf, int max); void syscall(void); #endif`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 蕾姆赛高队 在“调度与任务管理”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：context, ra, sp, struct, uint64。（置信度：high）
  证据：
  - `kernel/proc.h:L6-L10`：struct context
    代码片段：`// Saved registers for kernel context switches. struct context { uint64 ra; uint64 sp;`
  - `include/proc.h:L14-L18`：struct context
    代码片段：`// Saved registers for kernel context switches. struct context { uint64 ra; uint64 sp;`
- 与 2024 蕾姆赛高队 在“调度与任务管理”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：context, cpu, proc, struct。（置信度：high）
  证据：
  - `kernel/proc.h:L41-L45`：struct cpu
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler().`
  - `include/proc.h:L34-L38`：struct cpu
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler().`
- 与 2024 蕾姆赛高队 在“系统调用”维度发现片段级代码相似度 0.66 （token=0.55, structure=0.88）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：a0, a7, asm, register, volatile。（置信度：medium）
  证据：
  - `kernel/arch/riscv/arch.h:L330-L334`：关键词命中
    代码片段：`register uint64 a6 asm("a6") = 0; register uint64 a7 asm("a7") = 0x54494D45; asm volatile("ecall" : "+r"(a0) : "r"(a6), "r"(a7)`
  - `include/sbi.h:L28-L32`：关键词命中
    代码片段：`register uintptr a3 asm ("a3") = (uintptr)(arg3);	\ register uintptr a7 asm ("a7") = (uintptr)(which);	\ asm volatile ("ecall"					\ : "+r" (a0)				\ : "r" (a1), "r" (a2), "r" (...`
- 宏名重合：与 2024 蕾姆赛高队 在“文件系统”维度发现 4 个同名定义：PGSHIFT, NELEM, NOFILE, CONSOLE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/arch/loongarch/arch.h:L619-L619`：macro PGSHIFT
    代码片段：`#define PGSHIFT 12  // bits of offset within a page`
  - `include/riscv.h:L338-L338`：macro PGSHIFT
    代码片段：`#define PGSHIFT 12  // bits of offset within a page`
  - `kernel/defs.h:L335-L335`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x)/sizeof((x)[0]))`
  - `include/defs.h:L254-L254`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x)/sizeof((x)[0]))`
- 宏名重合：与 2024 蕾姆赛高队 在“中断与异常”维度发现 4 个同名定义：SIE_STIE, PLIC, TRAPFRAME, SSTATUS_SPIE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/arch/loongarch/arch.h:L147-L147`：macro SIE_STIE
    代码片段：`#define SIE_STIE (1L << 11) // timer interrupt (ECFG bit 11 for timer)`
  - `include/riscv.h:L83-L83`：macro SIE_STIE
    代码片段：`#define SIE_STIE (1L << 5) // timer`
  - `kernel/arch/loongarch/memlayout.h:L51-L51`：macro PLIC
    代码片段：`#define PLIC 0L`
  - `include/memlayout.h:L57-L57`：macro PLIC
    代码片段：`#define PLIC                    0x0c000000L`
- 宏名重合：与 2024 蕾姆赛高队 在“内存管理”维度发现 4 个同名定义：MAKE_SATP, PGSIZE, PGSHIFT, PGROUNDUP。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/arch/loongarch/arch.h:L323-L323`：macro MAKE_SATP
    代码片段：`#define MAKE_SATP(pagetable) ((uint64)(pagetable))`
  - `include/riscv.h:L190-L190`：macro MAKE_SATP
    代码片段：`#define MAKE_SATP(pagetable) (SATP_SV39 | (((uint64)pagetable) >> 12))`
  - `kernel/arch/loongarch/arch.h:L618-L618`：macro PGSIZE
    代码片段：`#define PGSIZE 4096 // bytes per page`
  - `include/riscv.h:L337-L337`：macro PGSIZE
    代码片段：`#define PGSIZE 4096 // bytes per page`
- 宏名重合：与 2024 蕾姆赛高队 在“设备驱动”维度发现 2 个同名定义：VIRTIO0, CONSOLE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/arch/loongarch/memlayout.h:L35-L35`：macro VIRTIO0
    代码片段：`#define VIRTIO0 0x10001000`
  - `include/memlayout.h:L49-L49`：macro VIRTIO0
    代码片段：`#define VIRTIO0                 0x10001000`
  - `kernel/file.h:L60-L60`：macro CONSOLE
    代码片段：`#define CONSOLE 1`
  - `include/file.h:L51-L51`：macro CONSOLE
    代码片段：`#define CONSOLE 1`
- 宏名重合：与 2024 蕾姆赛高队 在“调度与任务管理”维度发现 1 个同名定义：NOFILE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/file.h:L33-L33`：macro NOFILE
    代码片段：`#define NOFILE 16  // max open files per process`
  - `include/param.h:L6-L6`：macro NOFILE
    代码片段：`#define NOFILE      128  // open files per process`
- 宏名重合：与 2024 蕾姆赛高队 在“系统调用”维度发现 1 个同名定义：TRAPFRAME。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/arch/loongarch/memlayout.h:L92-L92`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME  0x80045000UL`
  - `include/memlayout.h:L127-L127`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME               (TRAMPOLINE - PGSIZE)`
- 结构体/类型重合：与 2024 蕾姆赛高队 在“文件系统”维度发现 2 个同名定义：file, devsw。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/file.h:L15-L15`：struct file
    代码片段：`struct file {`
  - `include/file.h:L5-L5`：struct file
    代码片段：`struct file {`
  - `kernel/file.h:L53-L53`：struct devsw
    代码片段：`struct devsw {`
  - `include/file.h:L36-L36`：struct devsw
    代码片段：`struct devsw {`
- 结构体/类型重合：与 2024 蕾姆赛高队 在“中断与异常”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/arch/loongarch/arch.h:L492-L492`：struct trapframe
    代码片段：`struct trapframe {`
  - `include/trap.h:L17-L17`：struct trapframe
    代码片段：`struct trapframe {`
- 结构体/类型重合：与 2024 蕾姆赛高队 在“内存管理”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/arch/loongarch/arch.h:L492-L492`：struct trapframe
    代码片段：`struct trapframe {`
  - `include/trap.h:L17-L17`：struct trapframe
    代码片段：`struct trapframe {`
- 结构体/类型重合：与 2024 蕾姆赛高队 在“系统调用”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/arch/loongarch/arch.h:L492-L492`：struct trapframe
    代码片段：`struct trapframe {`
  - `include/trap.h:L17-L17`：struct trapframe
    代码片段：`struct trapframe {`
- 文件路径重合：与 2024 蕾姆赛高队 在“设备驱动”维度出现同名文件源码路径 `kernel/uart.c` / `src/driver/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `src/driver/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
- 文件路径重合：与 2024 蕾姆赛高队 在“设备驱动”维度出现同名文件源码路径 `kernel/virtio.h` / `include/virtio.h`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `include/virtio.h:L6-L10`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
- 文件路径重合：与 2024 蕾姆赛高队 在“文件系统”维度出现同名文件源码路径 `kernel/file.c` / `src/fs/file.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/file.c:L3-L7`：关键词命中
    代码片段：`// // filestat, fileclose may need vfs file operations? #include "types.h"`
  - `src/fs/file.c:L139-L143`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0, rcnt = 0;`
- 文件路径重合：与 2024 蕾姆赛高队 在“中断与异常”维度出现同名文件源码路径 `kernel/plic.c` / `src/kernel/plic.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `src/kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
- 文件路径重合：与 2024 蕾姆赛高队 在“中断与异常”维度出现同名文件源码路径 `kernel/trap.c` / `src/kernel/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/trap.c:L34-L38`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `src/kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
- 文件路径重合：与 2024 蕾姆赛高队 在“内存管理”维度出现同名文件源码路径 `kernel/vm.c` / `src/mm/vm.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/vm.c:L11-L15`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `src/mm/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`

## 相似点

- 与 2024 蕾姆赛高队 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 蕾姆赛高队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
- 与 2024 蕾姆赛高队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/file.c:L3-L7`：关键词命中
    代码片段：`// // filestat, fileclose may need vfs file operations? #include "types.h"`
  - `kernel/file.h:L1-L4`：关键词命中
    代码片段：`#include "kernel/xv6_fs.h" #include "kernel/vfs/vfs.h" #define PIPESIZE 512`
- 与 2024 蕾姆赛高队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L34-L38`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
- 与 2024 蕾姆赛高队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/vm.c:L11-L15`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/page.h:L37-L41`：关键词命中
    代码片段：`// PFN is KERNBASE-relative so that mem_map[0] = page at KERNBASE (0x80000000). // This keeps all indices within [0, (PHYSTOP-KERNBASE)/PGSIZE). #define PFN_DOWN(pa)     (((uint...`
- 与 2024 蕾姆赛高队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "file.h"`
  - `kernel/proc.h:L42-L46`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2024 蕾姆赛高队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "xv6_fs.h"`
  - `kernel/bio.c:L17-L21`：关键词命中
    代码片段：`#include "types.h" #include "param.h" #include "spinlock.h" #include "sleeplock.h" #include "arch.h"`
- 与 2024 蕾姆赛高队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap.c:L85-L89`：关键词命中
    代码片段：`// Save init's registers for epc corruption recovery if(p->pid == 1) { p->saved_epc = p->trapframe->epc + 4;  // instruction after ecall p->saved_ra = p->trapframe->ra; p->saved...`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "errno.h"`
- 与 2024 蕾姆赛高队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 蕾姆赛高队 的语言构成不同：待测作品为 {'yaml': 62, 'build': 413, 'markdown': 5535, 'c': 184838, 'asm': 1214, 'make': 256, 'text': 13}，历史样本为 {'json': 20, 'build': 226, 'markdown': 574, 'c': 48956, 'asm': 332}。（置信度：medium）

## 可能创新点

- 待测作品包含 96 个历史样本 2024 蕾姆赛高队 中未见的独特函数/类型定义（如 `ext4_bg_set_inode_table_first_block`, `ext4_xattr_get`, `ext4_bmap_is_bit_clr`, `ext4_bmap_bit_clr`, `ext4_dir_en_set_entry_len`, `ext4_dir_en_set_inode`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/fs-ext4/lwext4/include/ext4_block_group.h:L90-L90`：fn ext4_bg_set_inode_table_first_block
    代码片段：`static inline void ext4_bg_set_inode_table_first_block(struct ext4_bgroup *bg, struct ext4_sblock *s, uint64_t blk) {`
  - `kernel/fs-ext4/ext4_xattr_stub.c:L20-L20`：fn ext4_xattr_get
    代码片段：`int ext4_xattr_get(void *ref, uint32_t inode_no, const char *name, void *buf, size_t buf_size, size_t *data_size) {`
- 待测作品代码规模（192,331 LOC）显著大于历史样本 2024 蕾姆赛高队（50,108 LOC），可能包含更多功能模块或更完整的实现，但也可能包含非内核代码，需人工确认。（置信度：low）

## 附录：核验摘要

- 关键结论数：35
- 含证据关键结论数：34（97.1%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
