# 2026 T2026104879910415 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026104879910415-3331`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：7375
- 代码/文本行数：2492873
- 主要语言：rust 2018257 LOC, json 294026 LOC, markdown 118891 LOC, build 28805 LOC, c 10544 LOC

## 总览

2026 T2026104879910415 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 2018257 LOC, json: 294026 LOC, markdown: 118891 LOC, build: 28805 LOC。仓库包含 7375 个已扫描文件、约 2492873 行可分析文本，当前抽取到 147878 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, arceos/Cargo.toml, arceos/Makefile, arceos/api/arceos_api/Cargo.toml, arceos/api/arceos_posix_api/Cargo.toml。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`# 根级比赛交付入口。 # # 设计目标：`
  - `arceos/Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] resolver = "2"`
  - `arceos/Makefile:L1-L3`：构建入口
    代码片段：`# Available arguments: # * General options: #     - 'ARCH': Target architecture: x86_64, riscv64, aarch64, loongarch64, arm`
  - `arceos/api/arceos_api/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "arceos_api" version.workspace = true`
  - `arceos/api/arceos_posix_api/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "arceos_posix_api" version.workspace = true`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn stdio_fd_kind, fn canonical_stdio_fd, fn flush_stdio_line_buffer_locked, fn append_stdio_line_bytes, fn flush_stdio_line_buffer_for。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/task/fd.rs` | L23-L27 | 关键词命中 |
| `kernel/src/task/mod.rs` | L3-L7 | 关键词命中 |
| `kernel/src/task/path.rs` | L13-L17 | 关键词命中 |
| `kernel/src/task/fd.rs` | L47-L51 | fn stdio_fd_kind |
| `kernel/src/task/fd.rs` | L55-L59 | fn canonical_stdio_fd |
| `kernel/src/task/fd.rs` | L63-L67 | fn flush_stdio_line_buffer_locked |

### 关键代码片段

  - `kernel/src/task/fd.rs:L23-L27`：关键词命中
    代码片段：`use crate::fs::{CapacityLimitedRamfsWriteGuard, FileTimestamp, LOOP0_SIZE_BYTES}; use super::{Pid, Process, Thread, TimeNsCompatState}; /// Linux 标准输入 fd 编号。`
  - `kernel/src/task/mod.rs:L3-L7`：关键词命中
    代码片段：`//! 当前处于 'process-runtime-core' 的前两步之间： //! 1. 先把运行时状态从 'user.rs' 收拢到独立模块； //! 2. 再把一次性的 'MinimalThread' 状态升级成真正的 'Process/Thread/FdTable/ExitState' //!    形状。 //!`
  - `kernel/src/task/path.rs:L13-L17`：关键词命中
    代码片段：`use axstd::vec::Vec; use super::{Thread, fd::OpenResource}; /// Linux 'AT_FDCWD' 的语义值，表示“相对当前工作目录解析”。`
  - `kernel/src/task/fd.rs:L47-L51`：fn stdio_fd_kind
    代码片段：`const STDIO_LINE_BUFFER_LIMIT: usize = PAGE_SIZE_4K; fn stdio_fd_kind(fd: i32) -> Option<StdioKind> { match fd { 1 => Some(StdioKind::Stdout),`

### 相关符号

`fn stdio_fd_kind` at `kernel/src/task/fd.rs:L47`、`fn canonical_stdio_fd` at `kernel/src/task/fd.rs:L55`、`fn flush_stdio_line_buffer_locked` at `kernel/src/task/fd.rs:L63`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：struct UserVmLayout, fn new, struct BrkState, fn from_layout, fn accepts。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/task/memory.rs` | L22-L26 | 关键词命中 |
| `kernel/src/syscall/memory.rs` | L253-L257 | 关键词命中 |
| `arceos/modules/axmm/src/backend/alloc.rs` | L1-L5 | 关键词命中 |
| `kernel/src/task/memory.rs` | L20-L24 | struct UserVmLayout |
| `kernel/src/task/memory.rs` | L30-L34 | fn new |
| `kernel/src/task/memory.rs` | L63-L67 | struct BrkState |

### 关键代码片段

  - `kernel/src/task/memory.rs:L22-L26`：关键词命中
    代码片段：`pub struct UserVmLayout { pub image_end: usize, pub heap_base: usize, pub mmap_base: usize, pub stack_bottom: usize,`
  - `kernel/src/syscall/memory.rs:L253-L257`：关键词命中
    代码片段：`} fn heap_mapping_flags() -> MappingFlags { MappingFlags::READ | MappingFlags::WRITE | MappingFlags::USER }`
  - `arceos/modules/axmm/src/backend/alloc.rs:L1-L5`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable, PagingError}; use memory_addr::{MemoryAddr, P...`
  - `kernel/src/task/memory.rs:L20-L24`：struct UserVmLayout
    代码片段：`/// exec 安装后用户地址空间里后续运行时会继续依赖的稳定地标。 #[derive(Clone, Copy, Debug)] pub struct UserVmLayout { pub image_end: usize, pub heap_base: usize,`

### 相关符号

`struct UserVmLayout` at `kernel/src/task/memory.rs:L20`、`fn new` at `kernel/src/task/memory.rs:L30`、`struct BrkState` at `kernel/src/task/memory.rs:L63`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn _stext, fn _etext, fn _srodata, fn _erodata, fn _sdata。（置信度：medium）
  - 静态识别到 471 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/syscall/fs.rs` | L1-L3 | 关键词命中 |
| `kernel/src/syscall/io.rs` | L1-L3 | 关键词命中 |
| `kernel/src/syscall/mod.rs` | L1-L3 | 关键词命中 |
| `kernel/src/trap.rs` | L33-L37 | fn _stext |
| `kernel/src/trap.rs` | L34-L38 | fn _etext |
| `kernel/src/trap.rs` | L35-L39 | fn _srodata |

### 关键代码片段

  - `kernel/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! 最小文件、目录与 fd syscall 子集。 //! //! 这一层承接 'busybox-fs-time-ioctl' 的第 3 步：`
  - `kernel/src/syscall/io.rs:L1-L3`：关键词命中
    代码片段：`//! 最小 vector I/O 与 sendfile syscall 入口。 //! //! 当前承接 'network-perf-suite' 第 3 步：`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`//! 第 5 步最小 Linux syscall 分发层。 //! //! 这一层负责两件事：`
  - `kernel/src/trap.rs:L33-L37`：fn _stext
    代码片段：`#[cfg(target_arch = "riscv64")] unsafe extern "C" { fn _stext(); fn _etext(); fn _srodata();`

### 相关符号

`fn _stext` at `kernel/src/trap.rs:L33`、`fn _etext` at `kernel/src/trap.rs:L34`、`fn _srodata` at `kernel/src/trap.rs:L35`、`fn handle_minimal_syscall` at `kernel/src/trap.rs:L63`、`fn resume_syscall_thread` at `kernel/src/trap.rs:L403`、`fn syscall_vos_musl_pathconf` at `kernel/src/syscall/fs.rs:L1159`、`fn syscall_faccessat` at `kernel/src/syscall/fs.rs:L1314`、`fn syscall_faccessat2` at `kernel/src/syscall/fs.rs:L1319`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct RootDirEntry, struct RootFsMetadata, struct DiskReader, struct DiskReadError, fn fmt。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/fs/mod.rs` | L1-L5 | 关键词命中 |
| `kernel/src/fs/ext4.rs` | L1-L5 | 关键词命中 |
| `kernel/src/fs/myfs.rs` | L1-L5 | 关键词命中 |
| `kernel/src/fs/ext4.rs` | L26-L30 | struct RootDirEntry |
| `kernel/src/fs/ext4.rs` | L35-L39 | struct RootFsMetadata |
| `kernel/src/fs/ext4.rs` | L43-L47 | struct DiskReader |

### 关键代码片段

  - `kernel/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`//! 文件系统相关的最小封装层。 //! //! 当前 feature 不把 ext4 接到 ArceOS 的全局 rootfs 协议里做完整 VFS 适配，而是： //! - 用 'myfs.rs' 提供一个最薄的内存根文件系统，避免启动期默认 FAT rootfs 直接失败； //! - 用 'ext4.rs' 在 bring-up 逻辑里直接读...`
  - `kernel/src/fs/ext4.rs:L1-L5`：关键词命中
    代码片段：`//! ext4 只读访问封装。 //! //! 第 3 到第 6 步的目标不是把 ext4 正式并入 ArceOS VFS，而是先证明评测盘内容可读， //! 并支撑最小测试驱动，因此这里直接： //! 1. 复用启动阶段传给 'myfs' 的真实块设备；`
  - `kernel/src/fs/myfs.rs:L1-L5`：关键词命中
    代码片段：`//! ArceOS 自定义根文件系统接入点。 //! //! 启动早期如果仍让 ArceOS 走默认 FAT rootfs 路线，会在真正进入我们自己的 //! 'run_minimal_loop()' 之前就因为评测盘格式不匹配而失败。因此这里先用最薄的 //! 'RamFileSystem' 接住系统 rootfs 初始化，让后续 ext4 br...`
  - `kernel/src/fs/ext4.rs:L26-L30`：struct RootDirEntry
    代码片段：`/// 的细节直接散落到 init 编排逻辑中。 #[derive(Clone, Debug)] pub struct RootDirEntry { pub name: String, pub path: String,`

### 相关符号

`struct RootDirEntry` at `kernel/src/fs/ext4.rs:L26`、`struct RootFsMetadata` at `kernel/src/fs/ext4.rs:L35`、`struct DiskReader` at `kernel/src/fs/ext4.rs:L43`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct OnceLock, fn get, fn get_mut, fn set, fn get_or_init。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/init.rs` | L15-L19 | 关键词命中 |
| `kernel/src/user.rs` | L10-L14 | 关键词命中 |
| `kernel/src/fs/ext4.rs` | L11-L15 | 关键词命中 |
| `third_party/rust/vendor/once_cell_polyfill/src/sync/mod.rs` | L1-L4 | struct OnceLock |
| `third_party/rust/vendor/once_cell_polyfill/src/sync/mod.rs` | L7-L11 | fn get |
| `third_party/rust/vendor/once_cell_polyfill/src/sync/mod.rs` | L11-L15 | fn get_mut |

### 关键代码片段

  - `kernel/src/init.rs:L15-L19`：关键词命中
    代码片段：`use axstd::sync::Arc; use axstd::vec::Vec; use axsync::Mutex; use core::fmt::Write; use memory_addr::{MemoryAddr, PAGE_SIZE_4K, va};`
  - `kernel/src/user.rs:L10-L14`：关键词命中
    代码片段：`use memory_addr::{VirtAddr, va}; #[cfg(target_arch = "riscv64")] use core::sync::atomic::{AtomicBool, Ordering}; /// Linux RISC-V 'exit' syscall 号。`
  - `kernel/src/fs/ext4.rs:L11-L15`：关键词命中
    代码片段：`use axstd::string::{String, ToString}; use axstd::sync::Arc; use axstd::sync::Mutex; use axstd::vec::Vec; use core::fmt;`
  - `third_party/rust/vendor/once_cell_polyfill/src/sync/mod.rs:L1-L4`：struct OnceLock
    代码片段：`#[derive(Debug, Clone, Default, Eq, PartialEq)] pub struct OnceLock<T>(std::sync::OnceLock<T>); impl<T> OnceLock<T> {`

### 相关符号

`struct OnceLock` at `third_party/rust/vendor/once_cell_polyfill/src/sync/mod.rs:L1`、`fn get` at `third_party/rust/vendor/once_cell_polyfill/src/sync/mod.rs:L7`、`fn get_mut` at `third_party/rust/vendor/once_cell_polyfill/src/sync/mod.rs:L11`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn _stext, fn _etext, fn _srodata, fn _erodata, fn _sdata。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/trap.rs` | L1-L3 | 关键词命中 |
| `arceos/modules/axhal/src/irq.rs` | L1-L3 | 关键词命中 |
| `third_party/rust/vendor/axcpu/src/trap.rs` | L1-L3 | 关键词命中 |
| `kernel/src/trap.rs` | L33-L37 | fn _stext |
| `kernel/src/trap.rs` | L34-L38 | fn _etext |
| `kernel/src/trap.rs` | L35-L39 | fn _srodata |

### 关键代码片段

  - `kernel/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! trap/syscall 接入点。 //! //! 这里负责把架构 trap 入口和 'kernel/src/syscall/' 的 Linux syscall 分发层接起来，`
  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use axcpu::{`
  - `third_party/rust/vendor/axcpu/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use core::sync::atomic::{AtomicUsize, Ordering};`
  - `kernel/src/trap.rs:L33-L37`：fn _stext
    代码片段：`#[cfg(target_arch = "riscv64")] unsafe extern "C" { fn _stext(); fn _etext(); fn _srodata();`

### 相关符号

`fn _stext` at `kernel/src/trap.rs:L33`、`fn _etext` at `kernel/src/trap.rs:L34`、`fn _srodata` at `kernel/src/trap.rs:L35`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：trait VirtIoDevMeta, fn try_new, struct VirtIoNet, struct VirtIoBlk, struct VirtIoGpu。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `arceos/modules/axdriver/src/virtio.rs` | L21-L25 | 关键词命中 |
| `arceos/modules/axdriver/src/drivers.rs` | L6-L10 | 关键词命中 |
| `third_party/rust/vendor/uart_16550/src/mmio.rs` | L6-L10 | 关键词命中 |
| `arceos/modules/axdriver/src/virtio.rs` | L22-L26 | trait VirtIoDevMeta |
| `arceos/modules/axdriver/src/virtio.rs` | L28-L32 | fn try_new |
| `arceos/modules/axdriver/src/virtio.rs` | L33-L37 | struct VirtIoNet |

### 关键代码片段

  - `arceos/modules/axdriver/src/virtio.rs:L21-L25`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `third_party/rust/vendor/uart_16550/src/mmio.rs:L6-L10`：关键词命中
    代码片段：`use crate::{LineStsFlags, WouldBlockError}; /// A memory-mapped UART. #[derive(Debug)] pub struct MmioSerialPort {`
  - `arceos/modules/axdriver/src/virtio.rs:L22-L26`：trait VirtIoDevMeta
    代码片段：`/// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`

### 相关符号

`trait VirtIoDevMeta` at `arceos/modules/axdriver/src/virtio.rs:L22`、`fn try_new` at `arceos/modules/axdriver/src/virtio.rs:L28`、`struct VirtIoNet` at `arceos/modules/axdriver/src/virtio.rs:L33`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
