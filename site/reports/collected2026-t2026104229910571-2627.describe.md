# 2026 T2026104229910571 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026104229910571-2627`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：3033
- 代码/文本行数：910221
- 主要语言：rust 556257 LOC, json 281969 LOC, markdown 32567 LOC, c 27767 LOC, build 9556 LOC

## 总览

2026 T2026104229910571 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 556257 LOC, json: 281969 LOC, markdown: 32567 LOC, c: 27767 LOC。仓库包含 3033 个已扫描文件、约 910221 行可分析文本，当前抽取到 40302 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（96/100）
- 已确认 OS 维度：7/7；高置信维度：6/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 76/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | medium | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, kernel/Cargo.toml, kernel/Makefile, kernel/crates/devices/Cargo.toml, kernel/crates/executor/Cargo.toml。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`SHELL := /bin/bash RUSTUP_TOOLCHAIN := nightly-2025-01-18`
  - `kernel/Cargo.toml:L1-L3`：构建入口
    代码片段：`[profile.release] # debug = true`
  - `kernel/Makefile:L1-L3`：构建入口
    代码片段：`SHELL := /bin/bash include scripts/config.mk export BOARD ?= qemu`
  - `kernel/crates/devices/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "devices" version = "0.1.0"`
  - `kernel/crates/executor/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "executor" version = "0.1.0"`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn tr, fn load_tr, trait ElfExtra, fn get_ph_addr, fn dynsym。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `vendor/x86-0.52.0/src/task.rs` | L1-L3 | 关键词命中 |
| `kernel/kernel/src/tasks/mod.rs` | L7-L11 | 关键词命中 |
| `kernel/kernel/src/tasks/exec.rs` | L106-L110 | 关键词命中 |
| `vendor/x86-0.52.0/src/task.rs` | L9-L13 | fn tr |
| `vendor/x86-0.52.0/src/task.rs` | L21-L25 | fn load_tr |
| `kernel/kernel/src/tasks/elf.rs` | L8-L12 | trait ElfExtra |

### 关键代码片段

  - `vendor/x86-0.52.0/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`
  - `kernel/kernel/src/tasks/mod.rs:L7-L11`：关键词命中
    代码片段：`mod shm; mod stack; mod task; use self::initproc::initproc;`
  - `kernel/kernel/src/tasks/exec.rs:L106-L110`：关键词命中
    代码片段：`fn resolve_program(curr_dir: &PathBuf, requested: &str, envp: &[String]) -> Option<String> { if requested == "/proc/self/exe" { return Some(requested.to_string()); }`
  - `vendor/x86-0.52.0/src/task.rs:L9-L13`：fn tr
    代码片段：`/// # Safety /// Needs CPL 0. pub unsafe fn tr() -> segmentation::SegmentSelector { let segment: u16; asm!("str {0:x}",`

### 相关符号

`fn tr` at `vendor/x86-0.52.0/src/task.rs:L9`、`fn load_tr` at `vendor/x86-0.52.0/src/task.rs:L21`、`trait ElfExtra` at `kernel/kernel/src/tasks/elf.rs:L8`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn sys_brk, fn sys_mmap, fn sys_munmap, fn sys_mprotect, fn sys_msync。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/kernel/src/syscall/mm.rs` | L11-L15 | 关键词命中 |
| `kernel/crates/runtime/src/heap.rs` | L7-L11 | 关键词命中 |
| `kernel/crates/runtime/src/frame.rs` | L4-L8 | 关键词命中 |
| `kernel/kernel/src/syscall/mm.rs` | L15-L19 | fn sys_brk |
| `kernel/kernel/src/syscall/mm.rs` | L24-L28 | fn sys_mmap |
| `kernel/kernel/src/syscall/mm.rs` | L138-L142 | fn sys_munmap |

### 关键代码片段

  - `kernel/kernel/src/syscall/mm.rs:L11-L15`：关键词命中
    代码片段：`use syscalls::Errno; // The high 25bits in sv39 should be the same as bit 38. const MAP_AREA_START: usize = 0x2_0000_0000;`
  - `kernel/crates/runtime/src/heap.rs:L7-L11`：关键词命中
    代码片段：`// 堆大小 // const HEAP_SIZE: usize = 0x0180_0000; // pub const HEAP_SIZE: usize = 0x0180_0000;`
  - `kernel/crates/runtime/src/frame.rs:L4-L8`：关键词命中
    代码片段：`use bit_field::{BitArray, BitField}; use log::info; use polyhal::{consts::VIRT_ADDR_START, pa, pagetable::PAGE_SIZE, PhysAddr}; use sync::Mutex;`
  - `kernel/kernel/src/syscall/mm.rs:L15-L19`：fn sys_brk
    代码片段：`impl UserTaskContainer { pub fn sys_brk(&self, addr: usize) -> SysResult { let heap = self.task.pcb.lock().heap; debug!("sys_brk @ new: {:#x} old: {:#x}", addr, heap);`

### 相关符号

`fn sys_brk` at `kernel/kernel/src/syscall/mm.rs:L15`、`fn sys_mmap` at `kernel/kernel/src/syscall/mm.rs:L24`、`fn sys_munmap` at `kernel/kernel/src/syscall/mm.rs:L138`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：medium）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口或兼容层线索，但源码片段显示部分调用仍是 stub/未实现，应按接口线索而非完整系统调用实现解读。（置信度：medium）
  - 相关符号包括：struct EventFd, fn new, fn readat, fn writeat, fn poll。（置信度：medium）
  - 静态识别到 293 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/kernel/src/syscall/mm.rs` | L1-L4 | 关键词命中 |
| `kernel/kernel/src/syscall/mod.rs` | L39-L43 | 关键词命中 |
| `kernel/kernel/src/syscall/sys.rs` | L202-L206 | 关键词命中 |
| `kernel/kernel/src/syscall/fd.rs` | L55-L59 | struct EventFd |
| `kernel/kernel/src/syscall/fd.rs` | L61-L65 | fn new |
| `kernel/kernel/src/syscall/fd.rs` | L74-L78 | fn readat |

### 关键代码片段

  - `kernel/kernel/src/syscall/mm.rs:L1-L4`：关键词命中
    代码片段：`use super::SysResult; use crate::syscall::types::mm::map_mprot_to_flags; use crate::tasks::{MemArea, MemType}; use crate::user::UserTaskContainer;`
  - `kernel/kernel/src/syscall/mod.rs:L39-L43`：关键词命中
    代码片段：`let mut logged = UNSUPPORTED_SYSCALLS.lock(); if logged.iter().all(|seen| *seen != call_id) { warn!("unsupported syscall {} -> ENOSYS", call_id); logged.push(call_id); }`
  - `kernel/kernel/src/syscall/sys.rs:L202-L206`：关键词命中
    代码片段：`} // Linux getpriority(2) returns 20 - nice at the raw syscall layer. Ok(20) }`
  - `kernel/kernel/src/syscall/fd.rs:L55-L59`：struct EventFd
    代码片段：`const MS_RDONLY: usize = 1; pub struct EventFd { counter: Mutex<u64>, semaphore: bool,`

### 相关符号

`struct EventFd` at `kernel/kernel/src/syscall/fd.rs:L55`、`fn new` at `kernel/kernel/src/syscall/fd.rs:L61`、`fn readat` at `kernel/kernel/src/syscall/fd.rs:L74`、`fn sys_dup` at `kernel/kernel/src/syscall/fd.rs:L211`、`fn sys_dup3` at `kernel/kernel/src/syscall/fd.rs:L217`、`fn sys_dup2` at `kernel/kernel/src/syscall/fd.rs:L244`、`fn sys_read` at `kernel/kernel/src/syscall/fd.rs:L252`、`fn sys_write` at `kernel/kernel/src/syscall/fd.rs:L265`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：enum FatType, fn from_clusters, fn bits_per_fat_entry, fn min_clusters, fn max_clusters。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `vendor/fatfs-0.4.0/src/fs.rs` | L21-L25 | 关键词命中 |
| `kernel/filesystem/fs/src/lib.rs` | L9-L13 | 关键词命中 |
| `kernel/filesystem/fs/src/file.rs` | L1-L3 | 关键词命中 |
| `vendor/fatfs-0.4.0/src/fs.rs` | L29-L33 | enum FatType |
| `vendor/fatfs-0.4.0/src/fs.rs` | L43-L47 | fn from_clusters |
| `vendor/fatfs-0.4.0/src/fs.rs` | L53-L57 | fn bits_per_fat_entry |

### 关键代码片段

  - `vendor/fatfs-0.4.0/src/fs.rs:L21-L25`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `kernel/filesystem/fs/src/lib.rs:L9-L13`：关键词命中
    代码片段：`extern crate bitflags; pub mod dentry; #[cfg(root_fs = "fat32")] mod fatfs_shim;`
  - `kernel/filesystem/fs/src/file.rs:L1-L3`：关键词命中
    代码片段：`use crate::{dentry::get_mounted, pathbuf::PathBuf, WaitBlockingRead, WaitBlockingWrite}; use alloc::{string::String, sync::Arc, vec::Vec}; use libc_types::{`
  - `vendor/fatfs-0.4.0/src/fs.rs:L29-L33`：enum FatType
    代码片段：`/// 'FatType' values are based on the size of File Allocation Table entry. #[derive(Copy, Clone, Eq, PartialEq, Debug)] pub enum FatType { /// 12 bits per FAT entry Fat12,`

### 相关符号

`enum FatType` at `vendor/fatfs-0.4.0/src/fs.rs:L29`、`fn from_clusters` at `vendor/fatfs-0.4.0/src/fs.rs:L43`、`fn bits_per_fat_entry` at `vendor/fatfs-0.4.0/src/fs.rs:L53`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct LazyInit, fn init_by, fn is_init, fn try_get, fn check_init。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/crates/sync/src/lib.rs` | L2-L6 | 关键词命中 |
| `kernel/kernel/src/main.rs` | L50-L54 | 关键词命中 |
| `kernel/kernel/src/socket.rs` | L2-L6 | 关键词命中 |
| `kernel/crates/sync/src/lib.rs` | L11-L15 | struct LazyInit |
| `kernel/crates/sync/src/lib.rs` | L27-L31 | fn init_by |
| `kernel/crates/sync/src/lib.rs` | L33-L37 | fn is_init |

### 关键代码片段

  - `kernel/crates/sync/src/lib.rs:L2-L6`：关键词命中
    代码片段：`pub use spin::{ lazy::Lazy, Mutex, MutexGuard, RwLock, RwLockReadGuard, RwLockUpgradableGuard, RwLockWriteGuard, };`
  - `kernel/kernel/src/main.rs:L50-L54`：关键词命中
    代码片段：`use polyhal_trap::trapframe::{TrapFrame, TrapFrameArgs}; use runtime::frame::{frame_alloc_persist, frame_unalloc}; use sync::Mutex; use tasks::UserTask; use user::user_cow_int;`
  - `kernel/kernel/src/socket.rs:L2-L6`：关键词命中
    代码片段：`cmp, net::SocketAddrV4, sync::atomic::{AtomicUsize, Ordering}, };`
  - `kernel/crates/sync/src/lib.rs:L11-L15`：struct LazyInit
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; pub struct LazyInit<T> { inited: AtomicBool, data: UnsafeCell<MaybeUninit<T>>,`

### 相关符号

`struct LazyInit` at `kernel/crates/sync/src/lib.rs:L11`、`fn init_by` at `kernel/crates/sync/src/lib.rs:L27`、`fn is_init` at `kernel/crates/sync/src/lib.rs:L33`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：struct PLIC, impl Driver::for::PLIC, fn get_id, fn try_handle_interrupt, fn get_device_wrapper。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/crates/polyhal-trap/src/lib.rs` | L3-L6 | 关键词命中 |
| `kernel/driver/general-plic/src/lib.rs` | L4-L8 | 关键词命中 |
| `kernel/driver/general-plic/src/plic.rs` | L1-L4 | 关键词命中 |
| `kernel/driver/general-plic/src/lib.rs` | L14-L18 | struct PLIC |
| `kernel/driver/general-plic/src/lib.rs` | L18-L22 | impl Driver::for::PLIC |
| `kernel/driver/general-plic/src/lib.rs` | L19-L23 | fn get_id |

### 关键代码片段

  - `kernel/crates/polyhal-trap/src/lib.rs:L3-L6`：关键词命中
    代码片段：`#![feature(used_with_arg)] pub mod trap; pub mod trapframe;`
  - `kernel/driver/general-plic/src/lib.rs:L4-L8`：关键词命中
    代码片段：`extern crate alloc; mod plic; use alloc::sync::Arc;`
  - `kernel/driver/general-plic/src/plic.rs:L1-L4`：关键词命中
    代码片段：`/* base + 0x000000: Reserved (interrupt source 0 does not exist) base + 0x000004: Interrupt source 1 priority base + 0x000008: Interrupt source 2 priority`
  - `kernel/driver/general-plic/src/lib.rs:L14-L18`：struct PLIC
    代码片段：`}; pub struct PLIC { base: usize, }`

### 相关符号

`struct PLIC` at `kernel/driver/general-plic/src/lib.rs:L14`、`impl Driver::for::PLIC` at `kernel/driver/general-plic/src/lib.rs:L18`、`fn get_id` at `kernel/driver/general-plic/src/lib.rs:L19`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：fn init_mmio, fn virtio_device, fn enumerate_pci, fn virtio_device_probe, fn dump_bar_contents。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/driver/kvirtio/src/lib.rs` | L6-L10 | 关键词命中 |
| `kernel/driver/kramdisk/src/lib.rs` | L11-L15 | 关键词命中 |
| `kernel/driver/ns16550a/src/lib.rs` | L6-L10 | 关键词命中 |
| `kernel/driver/kvirtio/src/lib.rs` | L39-L43 | fn init_mmio |
| `kernel/driver/kvirtio/src/lib.rs` | L64-L68 | fn virtio_device |
| `kernel/driver/kvirtio/src/lib.rs` | L78-L82 | fn enumerate_pci |

### 关键代码片段

  - `kernel/driver/kvirtio/src/lib.rs:L6-L10`：关键词命中
    代码片段：`extern crate log; pub mod virtio_blk; pub mod virtio_impl; pub mod virtio_input;`
  - `kernel/driver/kramdisk/src/lib.rs:L11-L15`：关键词命中
    代码片段：`use alloc::sync::Arc; use devices::{ device::{BlkDriver, DeviceType, Driver}, driver_define, };`
  - `kernel/driver/ns16550a/src/lib.rs:L6-L10`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use devices::{ device::{DeviceType, Driver, UartDriver}, driver_define, fdt::Node,`
  - `kernel/driver/kvirtio/src/lib.rs:L39-L43`：fn init_mmio
    代码片段：`use crate::virtio_impl::HalImpl; pub fn init_mmio(node: &Node) -> Arc<dyn Driver> { if let Some(reg) = node.reg().and_then(|mut reg| reg.next()) { let paddr = reg.address as usize;`

### 相关符号

`fn init_mmio` at `kernel/driver/kvirtio/src/lib.rs:L39`、`fn virtio_device` at `kernel/driver/kvirtio/src/lib.rs:L64`、`fn enumerate_pci` at `kernel/driver/kvirtio/src/lib.rs:L78`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
