# 2026 T2026101269910482 比较报告

- 对比历史仓库：2022 无中生系统
- 生成时间：2026-07-02T09:00:59.835448+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 无中生系统（来源：赛事历史作品）：画像相似度 score=9.36；同属 ucore-variant 风格; 语言构成相似度 0.94; OS 维度重合度 1.00; 代码规模接近度 0.48

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 无中生系统 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L4-L8`：关键词命中
    代码片段：`macro_rules! print { ($($arg:tt)*) => ({ $crate::console::_print(format_args!($($arg)*)); }); }`
  - `kernel/src/driver/mod.rs:L1-L1`：关键词命中
    代码片段：`pub mod virtio_mmio;`
  - `toyos/src/console.rs:L32-L36`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
  - `toyos/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2022 无中生系统 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L2-L4`：关键词命中
    代码片段：`pub mod ext4; pub mod ramfs; pub mod vfs;`
  - `kernel/src/fs/vfs.rs:L1-L3`：关键词命中
    代码片段：`// VFS - Virtual File System layer use alloc::string::String; use alloc::vec::Vec;`
  - `toyos/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`mod pipe; pub mod stdio; mod inode; mod mount; pub mod finfo;`
  - `toyos/src/fs/finfo.rs:L51-L55`：关键词命中
    代码片段：`} pub fn new(name:&str, inode:usize, offset:isize, reclen: u16, d_type: u8)->Self{ let mut dirent = Self{ d_ino:inode,`
- 与 2022 无中生系统 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/trap/mod.rs:L1-L5`：关键词命中
    代码片段：`pub mod context; use crate::trap::context::TrapFrame; const SCAUSE_ECALL_U: usize = 8;`
  - `kernel/src/main.rs:L16-L20`：关键词命中
    代码片段：`pub mod syscall; mod task; mod trap; use memory::address::{PhysAddr, VirtAddr, PAGE_SIZE};`
  - `toyos/src/timer.rs:L56-L60`：关键词命中
    代码片段：`//time::read()返回计数器值 pub fn get_time() -> usize { //println!("timer/get_time"); time::read() as usize }`
  - `toyos/src/trap/mod.rs:L10-L14`：关键词命中
    代码片段：`scause::{ self, Trap, Exception, Interrupt,`
- 与 2022 无中生系统 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/memory/mod.rs:L2-L4`：关键词命中
    代码片段：`pub mod frame; pub mod heap; pub mod pagetable;`
  - `kernel/src/memory/heap.rs:L12-L16`：关键词命中
    代码片段：`pub struct SimpleHeap { head: *mut usize, // pointer to first free block (block starts with size field) heap_start: usize, heap_size: usize, used: usize,`
  - `toyos/src/memory/mod.rs:L1-L3`：关键词命中
    代码片段：`mod heap_allocator; mod page_table; mod memory_manager;`
  - `toyos/src/memory/address.rs:L1-L4`：关键词命中
    代码片段：`use crate::config::{PAGE_SIZE, PAGE_SIZE_BITS}; use super::PTE; use core::fmt::{self, Debug, Formatter};`
- 与 2022 无中生系统 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L1-L5`：关键词命中
    代码片段：`// Process management: minimal fork/exec/wait pub mod process; pub mod scheduler; use alloc::sync::Arc;`
  - `kernel/src/task/scheduler.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use crate::sync::SpinMutex; use crate::task::process::{Process, ProcessState}; use crate::task::alloc_pid; use crate::fs::vfs::FdTable;`
  - `toyos/src/task/mod.rs:L12-L16`：关键词命中
    代码片段：`pub use pid::{pid_alloc,KernelStack,PidHandle}; pub use processor::{ current_task, current_trap_cx, current_user_token, run_tasks, schedule, take_current_task, }; use crate::con...`
  - `toyos/src/task/processor.rs:L7-L11`：关键词命中
    代码片段：`pub struct Processor{ current:Option<Arc<TaskControlBlock>>, idle_task_cx:TaskContext,//表示当前处理器上的 idle 控制流的任务上下文，保存run_tasks中的状态，每次schedule利用该上下文回到run_tasks进行再次调度 }`
- 与 2022 无中生系统 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/sync.rs:L1-L5`：关键词命中
    代码片段：`use core::cell::UnsafeCell; use core::ops::{Deref, DerefMut}; use core::sync::atomic::{AtomicBool, Ordering}; pub struct SpinMutex<T> {`
  - `kernel/src/runner.rs:L868-L872`：关键词命中
    代码片段：`} stage_runner_success(staged_script, success_command.as_deref()); core::sync::atomic::compiler_fence(core::sync::atomic::Ordering::SeqCst); #[cfg(target_arch = "loongarch64")]...`
  - `toyos/src/sync/mod.rs:L1-L4`：关键词命中
    代码片段：`mod up; mod mutex; pub use up::UPSafeCell;`
  - `toyos/src/sync/mutex.rs:L7-L11`：关键词命中
    代码片段：`enum STATE{ unlock = 0, lock = 1, } /*自旋锁*/`
- 与 2022 无中生系统 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/syscall/mod.rs:L121-L125`：关键词命中
    代码片段：`} pub fn syscall(tf: &mut TrapFrame) { let sysno = tf.syscall_number(); *LAST_SYSCALL.lock() = sysno;`
  - `kernel/src/syscall/number.rs:L1-L3`：关键词命中
    代码片段：`// Linux RISC-V64 syscall numbers pub const SYS_GETCWD: usize = 17; pub const SYS_DUP3: usize = 24;`
  - `toyos/src/syscall/system_call.rs:L7-L11`：关键词命中
    代码片段：`use super::fs::*; pub fn syscall(syscall_id: usize, args: [usize; 7]) -> isize { match syscall_id{ SYS_WRITE => {sys_write(args[0], args[1] as *const u8, args[2])},`
  - `toyos/src/sbi.rs:L23-L27`：关键词命中
    代码片段：`unsafe { asm!( "ecall", inlateout("x10") arg0 => ret, in("x11") arg1,`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2022 无中生系统 在“设备驱动”维度发现片段级代码相似度 0.74 （token=0.73, structure=0.77）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：arg, console, crate, format_args, macro_rules, print, tt。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L4-L8`：关键词命中
    代码片段：`macro_rules! print { ($($arg:tt)*) => ({ $crate::console::_print(format_args!($($arg)*)); }); }`
  - `toyos/src/console.rs:L32-L36`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
- 与 2022 无中生系统 在“设备驱动”维度发现片段级代码相似度 0.71 （token=0.64, structure=0.85）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：c, fmt, fn, for, impl, in, mut, result。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L17-L21`：fn write_str
    代码片段：`struct Writer; impl fmt::Write for Writer { fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.bytes() { crate::arch::putchar(c);`
  - `toyos/src/console.rs:L8-L12`：fn write_str
    代码片段：`*/ impl Write for Stdout { fn write_str(&mut self, s: &str) ->fmt::Result { for c in s.chars() { console_putchar(c as usize);`
- 与 2022 无中生系统 在“同步机制”维度发现片段级代码相似度 0.68 （token=0.50, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：core, pub, struct, t, use。（置信度：medium）
  证据：
  - `kernel/src/sync.rs:L3-L7`：struct SpinMutex
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; pub struct SpinMutex<T> { locked: AtomicBool, data: UnsafeCell<T>,`
  - `toyos/src/sync/up.rs:L1-L5`：struct UPSafeCell
    代码片段：`use core::cell::{RefCell, RefMut}; pub struct UPSafeCell<T/*: ?Sized*/> { inner: RefCell<T>, }`
- 与 2022 无中生系统 在“同步机制”维度发现片段级代码相似度 0.65 （token=0.50, structure=0.92）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：_, fn, pub, self, t。（置信度：medium）
  证据：
  - `kernel/src/sync.rs:L26-L30`：fn try_lock
    代码片段：`} pub fn try_lock(&self) -> Option<SpinMutexGuard<'_, T>> { if self.locked.swap(true, Ordering::Acquire) { None`
  - `toyos/src/sync/up.rs:L14-L18`：fn exclusive_access
    代码片段：`} pub fn exclusive_access(&self) -> RefMut<'_, T> { self.inner.borrow_mut() }`
- 函数/符号名重合：与 2022 无中生系统 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L19-L19`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `user/src/console.rs:L9-L9`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 函数/符号名重合：与 2022 无中生系统 在“文件系统”维度发现 1 个同名定义：test_fstat。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `basic_tests/fstat.c:L10-L10`：fn test_fstat
    代码片段：`void test_fstat() {`
  - `user_C_program/user/src/oscomp/fstat.c:L10-L10`：fn test_fstat
    代码片段：`void test_fstat() {`
- 函数/符号名重合：与 2022 无中生系统 在“内存管理”维度发现 1 个同名定义：test_mmap。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `basic_tests/mmap.c:L13-L13`：fn test_mmap
    代码片段：`void test_mmap(void){`
  - `user_C_program/user/src/oscomp/mmap.c:L13-L13`：fn test_mmap
    代码片段：`void test_mmap(void){`
- 函数/符号名重合：与 2022 无中生系统 在“调度与任务管理”维度发现 1 个同名定义：test_yield。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `basic_tests/yield.c:L8-L8`：fn test_yield
    代码片段：`int test_yield(){`
  - `user_C_program/user/src/oscomp/yield.c:L8-L8`：fn test_yield
    代码片段：`int test_yield(){`
- 函数/符号名重合：与 2022 无中生系统 在“系统调用”维度发现 2 个同名定义：chdir, mkdir。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/syscall/context.rs:L40-L40`：fn chdir
    代码片段：`pub fn chdir(&self, path: &str) {`
  - `user_C_program/user/lib/syscall.c:L189-L189`：fn chdir
    代码片段：`int chdir(const char *path){`
  - `kernel/src/syscall/context.rs:L70-L70`：fn mkdir
    代码片段：`pub fn mkdir(&self, path: &str) -> bool {`
  - `user_C_program/user/lib/syscall.c:L193-L193`：fn mkdir
    代码片段：`int mkdir(const char *path, mode_t mode){`
- 宏名重合：与 2022 无中生系统 在“文件系统”维度发现 1 个同名定义：AT_FDCWD。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `basic_tests/fstat.c:L6-L6`：macro AT_FDCWD
    代码片段：`#define AT_FDCWD (-100) //相对路径`
  - `user_C_program/user/src/oscomp/fstat.c:L6-L6`：macro AT_FDCWD
    代码片段：`#define AT_FDCWD (-100) //相对路径`
- 文件路径重合：与 2022 无中生系统 在“设备驱动”维度出现同名文件源码路径 `kernel/src/console.rs` / `toyos/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L4-L8`：关键词命中
    代码片段：`macro_rules! print { ($($arg:tt)*) => ({ $crate::console::_print(format_args!($($arg)*)); }); }`
  - `toyos/src/console.rs:L32-L36`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`

## 相似点

- 与 2022 无中生系统 同属 ucore-variant 风格。（置信度：medium）
- 与 2022 无中生系统 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L4-L8`：关键词命中
    代码片段：`macro_rules! print { ($($arg:tt)*) => ({ $crate::console::_print(format_args!($($arg)*)); }); }`
  - `kernel/src/driver/mod.rs:L1-L1`：关键词命中
    代码片段：`pub mod virtio_mmio;`
- 与 2022 无中生系统 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L2-L4`：关键词命中
    代码片段：`pub mod ext4; pub mod ramfs; pub mod vfs;`
  - `kernel/src/fs/vfs.rs:L1-L3`：关键词命中
    代码片段：`// VFS - Virtual File System layer use alloc::string::String; use alloc::vec::Vec;`
- 与 2022 无中生系统 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/trap/mod.rs:L1-L5`：关键词命中
    代码片段：`pub mod context; use crate::trap::context::TrapFrame; const SCAUSE_ECALL_U: usize = 8;`
  - `kernel/src/main.rs:L16-L20`：关键词命中
    代码片段：`pub mod syscall; mod task; mod trap; use memory::address::{PhysAddr, VirtAddr, PAGE_SIZE};`
- 与 2022 无中生系统 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/memory/mod.rs:L2-L4`：关键词命中
    代码片段：`pub mod frame; pub mod heap; pub mod pagetable;`
  - `kernel/src/memory/heap.rs:L12-L16`：关键词命中
    代码片段：`pub struct SimpleHeap { head: *mut usize, // pointer to first free block (block starts with size field) heap_start: usize, heap_size: usize, used: usize,`
- 与 2022 无中生系统 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L1-L5`：关键词命中
    代码片段：`// Process management: minimal fork/exec/wait pub mod process; pub mod scheduler; use alloc::sync::Arc;`
  - `kernel/src/task/scheduler.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use crate::sync::SpinMutex; use crate::task::process::{Process, ProcessState}; use crate::task::alloc_pid; use crate::fs::vfs::FdTable;`
- 与 2022 无中生系统 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/sync.rs:L1-L5`：关键词命中
    代码片段：`use core::cell::UnsafeCell; use core::ops::{Deref, DerefMut}; use core::sync::atomic::{AtomicBool, Ordering}; pub struct SpinMutex<T> {`
  - `kernel/src/runner.rs:L868-L872`：关键词命中
    代码片段：`} stage_runner_success(staged_script, success_command.as_deref()); core::sync::atomic::compiler_fence(core::sync::atomic::Ordering::SeqCst); #[cfg(target_arch = "loongarch64")]...`
- 与 2022 无中生系统 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/syscall/mod.rs:L121-L125`：关键词命中
    代码片段：`} pub fn syscall(tf: &mut TrapFrame) { let sysno = tf.syscall_number(); *LAST_SYSCALL.lock() = sysno;`
  - `kernel/src/syscall/number.rs:L1-L3`：关键词命中
    代码片段：`// Linux RISC-V64 syscall numbers pub const SYS_GETCWD: usize = 17; pub const SYS_DUP3: usize = 24;`
- 与 2022 无中生系统 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 无中生系统 的语言构成不同：待测作品为 {'json': 20, 'markdown': 408, 'build': 124, 'c': 1012, 'text': 3, 'toml': 10, 'rust': 9561, 'asm': 15}，历史样本为 {'json': 28, 'build': 736, 'markdown': 1772, 'text': 312, 'c': 2167, 'asm': 644, 'rust': 27121, 'yaml': 79}。（置信度：medium）

## 可能创新点

- 待测作品包含 305 个历史样本 2022 无中生系统 中未见的独特函数/类型定义（如 `read_sector_pci_legacy_io_once`, `VirtqPciUsed`, `VirtqUsed`, `read_sector_mmio_once`, `alloc_shared_min`, `staged_script_label`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/src/driver/virtio_mmio.rs:L391-L391`：fn read_sector_pci_legacy_io_once
    代码片段：`fn read_sector_pci_legacy_io_once(&mut self, sector: u64, buf: &mut [u8; SECTOR_SIZE]) -> ReadResult {`
  - `kernel/src/driver/virtio_mmio.rs:L61-L61`：struct VirtqPciUsed
    代码片段：`struct VirtqPciUsed { flags: u16, idx: u16, ring: [VirtqUsedElem; PCI_QUEUE_SIZE], avail_event: u16 }`

## 附录：核验摘要

- 关键结论数：26
- 含证据关键结论数：26（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
