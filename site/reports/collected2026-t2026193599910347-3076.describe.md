# 2026 T2026193599910347 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026193599910347-3076`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：112
- 代码/文本行数：30965
- 主要语言：rust 23078 LOC, markdown 3741 LOC, text 3533 LOC, build 304 LOC, asm 299 LOC

## 总览

2026 T2026193599910347 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 23078 LOC, markdown: 3741 LOC, text: 3533 LOC, build: 304 LOC。仓库包含 112 个已扫描文件、约 30965 行可分析文本，当前抽取到 1255 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, Makefile, apps/fs_smoke/Cargo.toml, apps/fs_smoke/linker.ld, apps/net_bench/Cargo.toml。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] resolver = "2" members = [`
  - `Makefile:L1-L3`：构建入口
    代码片段：`SHELL := /bin/bash .PHONY: help fmt clippy build run gdb test-host test-qemu-smoke test-oscomp test-net-baseline test-net-perf rootfs-ext4 clean`
  - `apps/fs_smoke/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "fs_smoke" version = "0.1.0"`
  - `apps/fs_smoke/linker.ld:L1-L3`：构建入口
    代码片段：`ENTRY(_start) PHDRS`
  - `apps/net_bench/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "net_bench" version = "0.1.0"`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：enum TaskState, enum WaitReason, struct TaskControlBlock, fn new, fn with_entry。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `modules/axruntime/src/task.rs` | L1-L4 | 关键词命中 |
| `modules/axruntime/src/task_wait_queue.rs` | L4-L8 | 关键词命中 |
| `modules/axfs/src/memfs.rs` | L18-L22 | 关键词命中 |
| `modules/axruntime/src/task.rs` | L10-L14 | enum TaskState |
| `modules/axruntime/src/task.rs` | L19-L23 | enum WaitReason |
| `modules/axruntime/src/task.rs` | L31-L35 | struct TaskControlBlock |

### 关键代码片段

  - `modules/axruntime/src/task.rs:L1-L4`：关键词命中
    代码片段：`#![allow(dead_code)] //! Task control blocks, task table helpers, and state transitions. use core::mem::MaybeUninit;`
  - `modules/axruntime/src/task_wait_queue.rs:L4-L8`：关键词命中
    代码片段：`use core::cell::UnsafeCell; use crate::task::TaskId; /// Pure TaskId queue; task state transitions are owned by the runtime layer.`
  - `modules/axfs/src/memfs.rs:L18-L22`：关键词命中
    代码片段：`/// Inode identifier for the embedded /init image. pub const INIT_ID: InodeId = 5; /// Inode identifier for the /proc directory. pub const PROC_ID: InodeId = 6; /// Inode identi...`
  - `modules/axruntime/src/task.rs:L10-L14`：enum TaskState
    代码片段：`#[derive(Debug, Clone, Copy, PartialEq, Eq)] /// Task run state tracked by the scheduler. pub enum TaskState { Ready, Running,`

### 相关符号

`enum TaskState` at `modules/axruntime/src/task.rs:L10`、`enum WaitReason` at `modules/axruntime/src/task.rs:L19`、`struct TaskControlBlock` at `modules/axruntime/src/task.rs:L31`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：struct UserMapFlags, struct MemoryRegion, struct PhysAddr, struct VirtAddr, struct PhysPageNum。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `modules/axruntime/src/mm.rs` | L20-L24 | 关键词命中 |
| `modules/axruntime/src/task.rs` | L42-L46 | 关键词命中 |
| `modules/axruntime/src/trap.rs` | L185-L189 | 关键词命中 |
| `modules/axruntime/src/mm.rs` | L43-L47 | struct UserMapFlags |
| `modules/axruntime/src/mm.rs` | L54-L58 | struct MemoryRegion |
| `modules/axruntime/src/mm.rs` | L64-L68 | struct PhysAddr |

### 关键代码片段

  - `modules/axruntime/src/mm.rs:L20-L24`：关键词命中
    代码片段：`const IDENTITY_MAP_SIZE: usize = 1 << 30; const PTE_V: usize = 1 << 0; const PTE_R: usize = 1 << 1; const PTE_W: usize = 1 << 2;`
  - `modules/axruntime/src/task.rs:L42-L46`：关键词命中
    代码片段：`/// Kernel stack pointer. pub kernel_sp: usize, /// User page table root. pub user_root_pa: usize, /// User entry point.`
  - `modules/axruntime/src/trap.rs:L185-L189`：关键词命中
    代码片段：`} /// Enter user mode with the provided entry, stack, and page table. /// /// # Safety`
  - `modules/axruntime/src/mm.rs:L43-L47`：struct UserMapFlags
    代码片段：`#[derive(Clone, Copy)] /// 用户页映射权限。 pub struct UserMapFlags { /// 读权限。 pub read: bool,`

### 相关符号

`struct UserMapFlags` at `modules/axruntime/src/mm.rs:L43`、`struct MemoryRegion` at `modules/axruntime/src/mm.rs:L54`、`struct PhysAddr` at `modules/axruntime/src/mm.rs:L64`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：struct TrapFrame, fn __trap_vector, fn __trap_return, struct TrapFrameGuard, fn enter_trap。（置信度：medium）
  - 静态识别到 164 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `modules/axruntime/src/trap.rs` | L309-L313 | 关键词命中 |
| `modules/axruntime/src/syscall.rs` | L90-L94 | 关键词命中 |
| `modules/axruntime/src/sbi.rs` | L17-L21 | 关键词命中 |
| `modules/axruntime/src/trap.rs` | L12-L16 | struct TrapFrame |
| `modules/axruntime/src/trap.rs` | L88-L92 | fn __trap_vector |
| `modules/axruntime/src/trap.rs` | L89-L93 | fn __trap_return |

### 关键代码片段

  - `modules/axruntime/src/trap.rs:L309-L313`：关键词命中
    代码片段：`} } else if code == SCAUSE_USER_ECALL { crate::syscall::handle_syscall(tf); return; } else if code == SCAUSE_SUPERVISOR_ECALL {`
  - `modules/axruntime/src/syscall.rs:L90-L94`：关键词命中
    代码片段：`/// 根据 trapframe 分发系统调用。 pub fn handle_syscall(tf: &mut TrapFrame) { let ctx = SyscallContext::from_trap_frame(tf); let ret = dispatch(tf, ctx);`
  - `modules/axruntime/src/sbi.rs:L17-L21`：关键词命中
    代码片段：`fn sbi_call_legacy(which: usize, arg0: usize, arg1: usize, arg2: usize) -> usize { let ret: usize; // SAFETY: ecall follows the SBI legacy calling convention on RISC-V. unsafe {...`
  - `modules/axruntime/src/trap.rs:L12-L16`：struct TrapFrame
    代码片段：`#[repr(C)] /// Saved trap frame layout for RISC-V traps. pub struct TrapFrame { /// Return address. pub ra: usize,`

### 相关符号

`struct TrapFrame` at `modules/axruntime/src/trap.rs:L12`、`fn __trap_vector` at `modules/axruntime/src/trap.rs:L88`、`fn __trap_return` at `modules/axruntime/src/trap.rs:L89`、`struct SyscallContext` at `modules/axruntime/src/syscall.rs:L75`、`impl SyscallContext` at `modules/axruntime/src/syscall.rs:L80`、`fn handle_syscall` at `modules/axruntime/src/syscall.rs:L90`、`fn sys_exit` at `modules/axruntime/src/syscall.rs:L913`、`fn sys_exit_group` at `modules/axruntime/src/syscall.rs:L924`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct RootFsDevice, fn new, fn block_size, fn read_block, fn write_block。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `modules/axfs/src/lib.rs` | L1-L5 | 关键词命中 |
| `modules/axfs/src/ext4.rs` | L2-L6 | 关键词命中 |
| `modules/axfs/src/devfs.rs` | L3-L7 | 关键词命中 |
| `modules/axruntime/src/fs.rs` | L23-L27 | struct RootFsDevice |
| `modules/axruntime/src/fs.rs` | L29-L33 | fn new |
| `modules/axruntime/src/fs.rs` | L36-L40 | fn block_size |

### 关键代码片段

  - `modules/axfs/src/lib.rs:L1-L5`：关键词命中
    代码片段：`#![no_std] //! Filesystem implementations and VFS integration. pub mod block;`
  - `modules/axfs/src/ext4.rs:L2-L6`：关键词命中
    代码片段：`//! //! 中文注释：这里实现的是“最小可用”的 ext4 子集，主要面向 Aurora 的根文件系统。 //! - 支持：超级块/块组描述符/位图、inode、目录项遍历、读写/创建/截断。 //! - 寻址：同时支持 extent 树与传统间接块，但写入路径只实现了最小集。 //! - 简化：只在第 0 块组分配 inode/数据块，不做跨块组均衡。`
  - `modules/axfs/src/devfs.rs:L3-L7`：关键词命中
    代码片段：`use axvfs::{DirEntry, FileType, InodeId, Metadata, VfsError, VfsOps, VfsResult}; /// Root inode identifier for devfs. pub const ROOT_ID: InodeId = 1; /// Inode identifier for /d...`
  - `modules/axruntime/src/fs.rs:L23-L27`：struct RootFsDevice
    代码片段：`#[derive(Clone, Copy)] /// 初始 rootfs 的内存盘块设备。 pub struct RootFsDevice { size: usize, }`

### 相关符号

`struct RootFsDevice` at `modules/axruntime/src/fs.rs:L23`、`fn new` at `modules/axruntime/src/fs.rs:L29`、`fn block_size` at `modules/axruntime/src/fs.rs:L36`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn lock, fn lock, fn lock, fn lock, struct SpinLock。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `modules/axfs/src/ext4.rs` | L11-L15 | 关键词命中 |
| `modules/axfs/src/block.rs` | L4-L8 | 关键词命中 |
| `modules/axfs/src/fat32.rs` | L4-L8 | 关键词命中 |
| `modules/axfs/src/ext4.rs` | L85-L89 | fn lock |
| `modules/axfs/src/block.rs` | L70-L74 | fn lock |
| `modules/axfs/src/fat32.rs` | L48-L52 | fn lock |

### 关键代码片段

  - `modules/axfs/src/ext4.rs:L11-L15`：关键词命中
    代码片段：`use core::cell::UnsafeCell; use core::hint::spin_loop; use core::sync::atomic::{AtomicBool, Ordering}; use crate::block::{BlockCache, BlockDevice, BlockId};`
  - `modules/axfs/src/block.rs:L4-L8`：关键词命中
    代码片段：`use core::cell::UnsafeCell; use core::hint::spin_loop; use core::sync::atomic::{AtomicBool, Ordering}; /// Logical block identifier.`
  - `modules/axfs/src/fat32.rs:L4-L8`：关键词命中
    代码片段：`use core::cell::UnsafeCell; use core::hint::spin_loop; use core::sync::atomic::{AtomicBool, Ordering}; use crate::block::{BlockCache, BlockDevice, BlockId};`
  - `modules/axfs/src/ext4.rs:L85-L89`：fn lock
    代码片段：`// 获取互斥访问权（自旋等待）。 fn lock(&self) -> ScratchGuard<'_> { while self .locked`

### 相关符号

`fn lock` at `modules/axfs/src/ext4.rs:L85`、`fn lock` at `modules/axfs/src/block.rs:L70`、`fn lock` at `modules/axfs/src/fat32.rs:L48`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：struct TrapFrame, fn __trap_vector, fn __trap_return, struct TrapFrameGuard, fn enter_trap。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `modules/axruntime/src/trap.rs` | L1-L4 | 关键词命中 |
| `modules/axruntime/src/plic.rs` | L1-L3 | 关键词命中 |
| `modules/axruntime/src/task.rs` | L52-L56 | 关键词命中 |
| `modules/axruntime/src/trap.rs` | L12-L16 | struct TrapFrame |
| `modules/axruntime/src/trap.rs` | L88-L92 | fn __trap_vector |
| `modules/axruntime/src/trap.rs` | L89-L93 | fn __trap_return |

### 关键代码片段

  - `modules/axruntime/src/trap.rs:L1-L4`：关键词命中
    代码片段：`#![allow(dead_code)] //! Trap entry/exit handling and interrupt control helpers. use core::arch::asm;`
  - `modules/axruntime/src/plic.rs:L1-L3`：关键词命中
    代码片段：`//! PLIC (platform-level interrupt controller) helpers. use core::ptr;`
  - `modules/axruntime/src/task.rs:L52-L56`：关键词命中
    代码片段：`/// True if this is a user task. pub is_user: bool, // Pointer to the active trap frame on this task's kernel stack. // Valid only during trap handling; cleared on trap exit. //...`
  - `modules/axruntime/src/trap.rs:L12-L16`：struct TrapFrame
    代码片段：`#[repr(C)] /// Saved trap frame layout for RISC-V traps. pub struct TrapFrame { /// Return address. pub ra: usize,`

### 相关符号

`struct TrapFrame` at `modules/axruntime/src/trap.rs:L12`、`fn __trap_vector` at `modules/axruntime/src/trap.rs:L88`、`fn __trap_return` at `modules/axruntime/src/trap.rs:L89`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：fn new, fn new, fn new, fn init, struct Console。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `modules/axruntime/src/fs.rs` | L8-L12 | 关键词命中 |
| `modules/axruntime/src/console.rs` | L1-L3 | 关键词命中 |
| `modules/axruntime/src/virtio_blk.rs` | L1-L3 | 关键词命中 |
| `modules/axfs/src/ext4.rs` | L220-L224 | fn new |
| `modules/axfs/src/block.rs` | L108-L112 | fn new |
| `modules/axfs/src/fat32.rs` | L319-L323 | fn new |

### 关键代码片段

  - `modules/axruntime/src/fs.rs:L8-L12`：关键词命中
    代码片段：`use crate::dtb::VirtioMmioDevice; use crate::virtio_blk; const ROOTFS_BLOCK_SIZE: usize = 512;`
  - `modules/axruntime/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console output helpers and formatting macros. use core::fmt::{self, Write};`
  - `modules/axruntime/src/virtio_blk.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO 块设备（MMIO）驱动。 use core::cell::UnsafeCell;`
  - `modules/axfs/src/ext4.rs:L220-L224`：fn new
    代码片段：`/// 1) 使用 BlockCache 访问块设备 /// 2) 读取超级块并解析 pub fn new(device: &'a dyn BlockDevice) -> VfsResult<Self> { let cache = BlockCache::new(device); let block_size = cache.block_size();`

### 相关符号

`fn new` at `modules/axfs/src/ext4.rs:L220`、`fn new` at `modules/axfs/src/block.rs:L108`、`fn new` at `modules/axfs/src/fat32.rs:L319`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
