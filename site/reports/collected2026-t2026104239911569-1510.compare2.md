# 2026 T2026104239911569 比较报告

- 对比历史仓库：2024 NPUcore-IMPACT!!!
- 生成时间：2026-07-02T09:02:10.270760+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 NPUcore-IMPACT!!!（来源：赛事历史作品）：画像相似度 score=9.12；同属 ucore-variant 风格; 语言构成相似度 0.75; OS 维度重合度 1.00; 代码规模接近度 0.61

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/io/uart.rs:L1-L5`：关键词命中
    代码片段：`use crate::arch; pub struct Uart { base: usize, }`
  - `kernel/src/io/console.rs:L5-L9`：关键词命中
    代码片段：`static CONSOLE_LOCK: SpinLock = SpinLock::new(); struct Console; impl Write for Console {`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L3-L7`：关键词命中
    代码片段：`pub mod dirent; pub mod file; pub mod inode; pub mod path; pub mod common_file;`
  - `kernel/src/fs/path.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use super::inode::InodeRef; pub fn normalize_path(cwd: &str, path: &str) -> Option<String> {`
  - `os/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod file_trait; mod filesystem; // mod inode; mod layout; pub mod poll;`
  - `os/src/fs/cache.rs:L193-L197`：关键词命中
    代码片段：`/// Every time kernel tried to alloc this pagecache this number will increase 1(at most 1) /// Every time out of memory occurred this number will decrease 1(at least 0) /// When...`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/timer/mod.rs:L1-L3`：关键词命中
    代码片段：`// kernel/src/timer/mod.rs #[cfg(target_arch = "riscv64")]`
  - `kernel/src/timer/riscv64.rs:L11-L15`：关键词命中
    代码片段：`/* * 每多少次 timer interrupt 调度一次。 * 调试阶段可以设成 1，方便看到抢占。 */`
  - `os/src/timer.rs:L18-L22`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/main.rs:L36-L40`：关键词命中
    代码片段：`mod syscall; mod task; mod timer; // use crate::arch::config::DISK_IMAGE_BASE;`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`pub mod address; pub mod config; pub mod frame_allocator; pub mod heap; pub mod page_table;`
  - `kernel/src/mm/heap.rs:L7-L11`：关键词命中
    代码片段：`#[global_allocator] static HEAP_ALLOCATOR: LockedHeap<32> = LockedHeap::<32>::new(); static KERNEL_HEAP_START: AtomicUsize = AtomicUsize::new(0);`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `os/src/mm/map_area.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::Debug; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`mod processor; mod switch; mod thread; mod process; mod manager_wrapper;`
  - `kernel/src/task/thread.rs:L50-L54`：关键词命中
    代码片段：`pub tickets: usize, pub stride: usize, pub pass: usize,`
  - `os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `os/src/task/task.rs:L380-L384`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/sync/spin.rs:L4-L8`：关键词命中
    代码片段：`/// 简单的自旋锁 pub struct Mutex<T> { locked: core::sync::atomic::AtomicBool, data: UnsafeCell<T>,`
  - `kernel/src/sync/sync.rs:L2-L6`：关键词命中
    代码片段：`// 最简单有效的自旋锁，保护 UART 等共享资源 use core::sync::atomic::{AtomicBool, Ordering}; pub struct SpinLock {`
  - `os/src/fs/mod.rs:L35-L39`：关键词命中
    代码片段：`use lazy_static::*; use log::warn; use spin::Mutex; lazy_static! {`
  - `os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::{arch::BLOCK_SZ, config::PAGE_SIZE, drivers::BLOCK_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/syscall/mod.rs:L46-L50`：关键词命中
    代码片段：`pub fn syscall(id: usize, args: [usize; 6]) -> isize { match id { SYSCALL_EXIT => process::sys_exit(args[0] as i32),`
  - `kernel/src/trap/riscv64/mod.rs:L89-L93`：关键词命中
    代码片段：`let syscall_id = cx.x[17]; let args = [cx.x[10], cx.x[11], cx.x[12],cx.x[13], cx.x[14], cx.x[15]]; cx.x[10] = handle_syscall(syscall_id, args) as usize; }`
  - `os/src/syscall/fs.rs:L548-L552`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `os/src/syscall/mod.rs:L115-L119`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现 4 个同名定义：readable, writable, read, write。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/fs/common_file.rs:L55-L55`：fn readable
    代码片段：`fn readable(&self) -> bool {`
  - `os/src/fs/ext4_file.rs:L8-L8`：fn readable
    代码片段：`fn readable(&self) -> bool {`
  - `kernel/src/fs/common_file.rs:L59-L59`：fn writable
    代码片段：`fn writable(&self) -> bool {`
  - `os/src/fs/ext4_file.rs:L12-L12`：fn writable
    代码片段：`fn writable(&self) -> bool {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“内存管理”维度发现 3 个同名定义：new, fmt, from。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/fs/common_file.rs:L97-L97`：fn new
    代码片段：`pub fn new(entries: Vec<DirEntry>) -> Self {`
  - `os/src/mm/address.rs:L232-L232`：fn new
    代码片段：`pub fn new(start: T, end: T) -> Self {`
  - `kernel/src/mm/address.rs:L18-L18`：fn fmt
    代码片段：`fn fmt(&self, f: &mut Formatter<'_>) -> fmt::Result {`
  - `os/src/mm/address.rs:L24-L24`：fn fmt
    代码片段：`fn fmt(&self, f: &mut Formatter<'_>) -> fmt::Result {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“同步机制”维度发现 4 个同名定义：new, read, write, sync。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/block/blockio.rs:L14-L14`：fn new
    代码片段：`pub fn new(dev: Arc<dyn BlockDevice>, num_sectors: u64) -> Self {`
  - `os/src/fs/cache.rs:L81-L81`：fn new
    代码片段：`pub fn new(buffer_ptr: *mut [u8; BUFFER_SIZE]) -> Self {`
  - `kernel/src/block/blockio.rs:L28-L28`：fn read
    代码片段：`fn read(&mut self, buf: &mut [u8]) -> Result<usize, ()> {`
  - `os/src/fs/cache.rs:L15-L15`：fn read
    代码片段：`fn read<T, V>(&self, offset: usize, f: impl FnOnce(&T) -> V) -> V;`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现 2 个同名定义：read_block, write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/block/device.rs:L6-L6`：fn read_block
    代码片段：`fn read_block(&self, block_id: usize, buf: &mut [u8]) -> isize;`
  - `os/src/fs/cache.rs:L90-L90`：fn read_block
    代码片段：`pub fn read_block(&mut self, block_id: usize, block_device: &Arc<dyn BlockDevice>) {`
  - `kernel/src/io/console.rs:L10-L10`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `user/src/console.rs:L10-L10`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现 3 个同名定义：File, FileSystem, Stat。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/fs/file.rs:L13-L13`：trait File
    代码片段：`pub trait File: Send + Sync {`
  - `os/src/fs/file_trait.rs:L14-L14`：trait File
    代码片段：`pub trait File: DowncastSync {`
  - `kernel/src/fs/mount.rs:L7-L7`：trait FileSystem
    代码片段：`pub trait FileSystem: Send + Sync {`
  - `os/src/fs/filesystem.rs:L13-L13`：struct FileSystem
    代码片段：`pub struct FileSystem {`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“内存管理”维度发现 4 个同名定义：PhysAddr, VirtAddr, PhysPageNum, VirtPageNum。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/mm/address.rs:L6-L6`：struct PhysAddr
    代码片段：`pub struct PhysAddr(pub usize);`
  - `os/src/mm/address.rs:L8-L8`：struct PhysAddr
    代码片段：`pub struct PhysAddr(pub usize);`
  - `kernel/src/mm/address.rs:L9-L9`：struct VirtAddr
    代码片段：`pub struct VirtAddr(pub usize);`
  - `os/src/mm/address.rs:L12-L12`：struct VirtAddr
    代码片段：`pub struct VirtAddr(pub usize);`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“同步机制”维度发现 3 个同名定义：BlockCacheManager, BlockDevice, File。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/block/cache.rs:L135-L135`：struct BlockCacheManager
    代码片段：`pub struct BlockCacheManager {`
  - `os/src/fs/cache.rs:L97-L97`：struct BlockCacheManager
    代码片段：`pub struct BlockCacheManager {`
  - `kernel/src/block/device.rs:L1-L1`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync {`
  - `os/src/fs/mod.rs:L20-L20`：trait BlockDevice
    代码片段：`pub trait BlockDevice: crate::drivers::block::BlockDevice {}`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现 1 个同名定义：BlockDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/block/device.rs:L1-L1`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync {`
  - `os/src/fs/mod.rs:L20-L20`：trait BlockDevice
    代码片段：`pub trait BlockDevice: crate::drivers::block::BlockDevice {}`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“系统调用”维度发现 1 个同名定义：FileSystem。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/fs/mount.rs:L7-L7`：trait FileSystem
    代码片段：`pub trait FileSystem: Send + Sync {`
  - `os/src/fs/filesystem.rs:L13-L13`：struct FileSystem
    代码片段：`pub struct FileSystem {`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度出现同名文件源码路径 `kernel/src/io/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/io/console.rs:L5-L9`：关键词命中
    代码片段：`static CONSOLE_LOCK: SpinLock = SpinLock::new(); struct Console; impl Write for Console {`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度出现同名文件源码路径 `kernel/src/task/task.rs` / `os/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/task/task.rs:L15-L19`：enum TaskStatus
    代码片段：`#[derive(Clone, Copy, Debug, PartialEq, Eq)] pub enum TaskStatus { Ready, Running,`
  - `os/src/task/task.rs:L380-L384`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“系统调用”维度出现同名文件源码路径 `kernel/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/syscall/fs.rs:L3-L7`：fn sys_read
    代码片段：`use crate::fs; pub fn sys_read(fd: usize, user_buf: usize, len: usize) -> isize { if len == 0 { return 0;`
  - `os/src/syscall/fs.rs:L548-L552`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`

## 相似点

- 与 2024 NPUcore-IMPACT!!! 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/io/uart.rs:L1-L5`：关键词命中
    代码片段：`use crate::arch; pub struct Uart { base: usize, }`
  - `kernel/src/io/console.rs:L5-L9`：关键词命中
    代码片段：`static CONSOLE_LOCK: SpinLock = SpinLock::new(); struct Console; impl Write for Console {`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L3-L7`：关键词命中
    代码片段：`pub mod dirent; pub mod file; pub mod inode; pub mod path; pub mod common_file;`
  - `kernel/src/fs/path.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use super::inode::InodeRef; pub fn normalize_path(cwd: &str, path: &str) -> Option<String> {`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/timer/mod.rs:L1-L3`：关键词命中
    代码片段：`// kernel/src/timer/mod.rs #[cfg(target_arch = "riscv64")]`
  - `kernel/src/timer/riscv64.rs:L11-L15`：关键词命中
    代码片段：`/* * 每多少次 timer interrupt 调度一次。 * 调试阶段可以设成 1，方便看到抢占。 */`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`pub mod address; pub mod config; pub mod frame_allocator; pub mod heap; pub mod page_table;`
  - `kernel/src/mm/heap.rs:L7-L11`：关键词命中
    代码片段：`#[global_allocator] static HEAP_ALLOCATOR: LockedHeap<32> = LockedHeap::<32>::new(); static KERNEL_HEAP_START: AtomicUsize = AtomicUsize::new(0);`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`mod processor; mod switch; mod thread; mod process; mod manager_wrapper;`
  - `kernel/src/task/thread.rs:L50-L54`：关键词命中
    代码片段：`pub tickets: usize, pub stride: usize, pub pass: usize,`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/sync/spin.rs:L4-L8`：关键词命中
    代码片段：`/// 简单的自旋锁 pub struct Mutex<T> { locked: core::sync::atomic::AtomicBool, data: UnsafeCell<T>,`
  - `kernel/src/sync/sync.rs:L2-L6`：关键词命中
    代码片段：`// 最简单有效的自旋锁，保护 UART 等共享资源 use core::sync::atomic::{AtomicBool, Ordering}; pub struct SpinLock {`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/syscall/mod.rs:L46-L50`：关键词命中
    代码片段：`pub fn syscall(id: usize, args: [usize; 6]) -> isize { match id { SYSCALL_EXIT => process::sys_exit(args[0] as i32),`
  - `kernel/src/trap/riscv64/mod.rs:L89-L93`：关键词命中
    代码片段：`let syscall_id = cx.x[17]; let args = [cx.x[10], cx.x[11], cx.x[12],cx.x[13], cx.x[14], cx.x[15]]; cx.x[10] = handle_syscall(syscall_id, args) as usize; }`
- 与 2024 NPUcore-IMPACT!!! 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 NPUcore-IMPACT!!! 的语言构成不同：待测作品为 {'toml': 86, 'build': 1255, 'markdown': 6949, 'asm': 2289, 'rust': 71100, 'text': 1226, 'c': 12383, 'json': 70, 'yaml': 34}，历史样本为 {'json': 20, 'build': 338, 'markdown': 743, 'rust': 177629, 'asm': 10}。（置信度：medium）

## 可能创新点

- 待测作品包含 205 个历史样本 2024 NPUcore-IMPACT!!! 中未见的独特函数/类型定义（如 `discover_disks`, `make_region`, `init_global_from_phys_base`, `load`, `parse_virtio_pci_caps`, `used_ring_ptr`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/src/block/discover_disks.rs:L6-L6`：fn discover_disks
    代码片段：`pub fn discover_disks() -> (Option<Arc<dyn BlockDevice>>, Option<Arc<dyn BlockDevice>>) {`
  - `kernel/src/block/virtio_pci.rs:L48-L48`：fn make_region
    代码片段：`fn make_region(addr: PciAddress, bar: u8, offset: u32, length: u32) -> VirtioPciRegion {`

## 附录：核验摘要

- 关键结论数：27
- 含证据关键结论数：27（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
