# 2026 T2026104869910402 比较报告

- 对比历史仓库：2025 如有名字队
- 生成时间：2026-07-02T05:01:17.429427+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 如有名字队（来源：赛事历史作品）：画像相似度 score=9.77；同属 rcore-variant 风格; 语言构成相似度 0.95; OS 维度重合度 1.00; 代码规模接近度 0.87

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 如有名字队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
  - `os/src/drivers/bus/mod.rs:L1-L1`：关键词命中
    代码片段：`pub mod virtio;`
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use core::fmt::{self, Write};`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`pub mod device; use device::*; pub mod disk;`
- 与 2025 如有名字队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L13-L17`：关键词命中
    代码片段：`pub use fdtable::FdTable; pub use filesystem::vfs::{chdir, dcache_children, dentry_cache_cap, dentry_cache_hits, dentry_cache_len, dentry_cache_misses, get_cwd, get_root_dentry,...`
  - `os/src/fs/fdtable.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use filesystem::vfs::File as VfsFile; use filesystem::vfs_defs::{OpenFlags, VfsError, VfsRet};`
  - `os/src/fs/mod.rs:L3-L7`：关键词命中
    代码片段：`pub mod fsidx; mod fstruct; mod inode; mod mount; mod pipe;`
  - `os/src/fs/pipe.rs:L172-L176`：关键词命中
    代码片段：`pub fn splice_from_pipe( pipe: Arc<dyn File>, inode: Arc<OSInode>, offset: *const isize, len: usize,`
- 与 2025 如有名字队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/timer.rs:L8-L12`：关键词命中
    代码片段：`use alloc::sync::Arc; use lazy_static::*; use polyhal::timer::{current_time, set_next_timer}; const TICKS_PER_SEC: usize = 100;`
  - `os/src/trap/mod.rs:L11-L15`：关键词命中
    代码片段：`SigInfo, SIGBUS, SIGSEGV, SIGILL, SIGTRAP, BUS_ADRERR, SEGV_MAPERR, ILL_ILLOPC, TRAP_BRKPT, }; use crate::timer::{check_timer, set_next_trigger}; use crate::config::PAGE_SIZE; u...`
  - `os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! Timer module using the 'Time' abstraction layer. use polyhal::time::Time;`
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Kernel trap and interrupt handling pub mod interrupts; use crate::config::TIMER_IRQ;`
- 与 2025 如有名字队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/elf.rs:L74-L78`：关键词命中
    代码片段：`/// '"/lib/ld-linux-riscv64-lp64d.so.1"'。 pub interp_path: Option<String>, /// 所有 PT_LOAD 段，按文件顺序排列。供 'MemorySet::from_elf()' 消费。 pub segments: Vec<ElfSegment>, }`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`mod address; mod elf; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/mod.rs:L1-L3`：关键词命中
    代码片段：`mod frame_allocator; mod group; mod heap_allocator;`
  - `os/src/mm/shm.rs:L8-L12`：关键词命中
    代码片段：`}; use super::{frame_alloc, FrameTracker, MapPermission}; use lazy_static::*;`
- 与 2025 如有名字队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L5-L9`：关键词命中
    代码片段：`}; use crate::mm::{KERNEL_SPACE, PhysPageNum, VirtAddr}; use crate::task::boot_page_table; use polyhal::{MappingFlags, MappingSize::Page4KB}; use crate::sync::UPIntrFreeCell;`
  - `os/src/task/mod.rs:L13-L17`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Task management implementation //! //! Everything about task management, like starting and switching tasks is`
  - `os/src/task/task.rs:L12-L16`：关键词命中
    代码片段：`use crate::syscall::CloneFlags; use crate::syscall::RLimit; use crate::task::aux_::{Aux, AuxType}; use crate::timer::TimeData; use crate::utils::{get_abs_path, is_abs_path, SysE...`
- 与 2025 如有名字队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/sync/mod.rs:L8-L12`：关键词命中
    代码片段：`pub use ksync::{ Condvar, Mutex, MutexBlocking, MutexSpin, Semaphore, UPIntrFreeCell, UPIntrRefMut, UPSafeCellRaw, };`
  - `os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`// use alloc::{collections::VecDeque, sync::Arc}; // pub trait Mutex: Sync + Send { //     fn lock(&self); //     fn unlock(&self);`
  - `os/src/fs/mod.rs:L21-L25`：关键词命中
    代码片段：`use alloc::sync::Arc; use lazy_static::lazy_static; use spin::Mutex; use usedfiles::*;`
  - `os/src/mm/shm.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{collections::BTreeMap, sync::Arc, vec::Vec}; use spin::Mutex; use crate::{`
- 与 2025 如有名字队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L19-L23`：关键词命中
    代码片段：`/// unlinkat flags: 允许删除目录项指向的是目录（否则返回 EPERM）。 const AT_REMOVEDIR: u32 = 0x200; /// 路径型 syscall flags: 不跟随最终分量的符号链接。 const AT_SYMLINK_NOFOLLOW: usize = 0x100; /// 路径型 syscall fl...`
  - `os/src/syscall/mod.rs:L194-L198`：关键词命中
    代码片段：`use polyhal_trap::trapframe::TrapFrameArgs; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> SyscallRet { let tid = crate::task::current_task() .and_then(|t| {`
  - `os/src/syscall/fs.rs:L15-L19`：关键词命中
    代码片段：`}; use crate::signal::SignalFlags; use crate::syscall::option::{ fcntl_cmd_name, FdSet, F_DUPFD, F_DUPFD_CLOEXEC, F_GETFD, F_GETFL, F_SETFD, };`
  - `os/src/syscall/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Implementation of syscalls //! //! The single entry point to all system calls, ['syscall()'], is called //! whenever userspace wishes to perform a system call using the 'eca...`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 如有名字队 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：args, arguments, fmt, fn, print, pub, stdout, unwrap。（置信度：high）
  证据：
  - `os/src/console.rs:L14-L18`：fn print
    代码片段：`} pub fn print(args: fmt::Arguments) { Stdout.write_fmt(args).unwrap(); }`
  - `os/src/console.rs:L15-L19`：fn print
    代码片段：`} pub fn print(args: fmt::Arguments) { Stdout.write_fmt(args).unwrap(); }`
- 与 2025 如有名字队 在“设备驱动”维度发现片段级代码相似度 0.85 （token=0.77, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：c, fmt, fn, for, impl, in, mut, result。（置信度：high）
  证据：
  - `os/src/console.rs:L6-L10`：fn write_str
    代码片段：`impl Write for Stdout { fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.chars() { UART.write(c as u8);`
  - `os/src/console.rs:L7-L11`：fn write_str
    代码片段：`impl Write for Stdout { fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.bytes() { DebugConsole::putchar(c);`
- 与 2025 如有名字队 在“中断与异常”维度发现片段级代码相似度 0.84 （token=0.76, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, debug, derive, pub, struct, timespec, tv_nsec。（置信度：high）
  证据：
  - `os/src/timer.rs:L19-L23`：struct Timespec
    代码片段：`#[repr(C)] #[derive(Debug, Clone, Copy, Default)] pub struct Timespec { pub tv_sec: i64, pub tv_nsec: i64,`
  - `os/src/timer.rs:L7-L11`：struct Timespec
    代码片段：`#[derive(Debug, Clone, Copy)] pub struct Timespec { pub tv_sec: usize,  //秒 pub tv_nsec: usize, //纳秒`
- 与 2025 如有名字队 在“中断与异常”维度发现片段级代码相似度 0.75 （token=0.61, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, debug, derive, pub, struct, tv_sec。（置信度：medium）
  证据：
  - `os/src/timer.rs:L26-L30`：struct Timeval
    代码片段：`#[repr(C)] #[derive(Debug, Clone, Copy, Default)] pub struct Timeval { pub tv_sec: i64, pub tv_usec: i64,`
  - `os/src/timer.rs:L7-L11`：struct Timespec
    代码片段：`#[derive(Debug, Clone, Copy)] pub struct Timespec { pub tv_sec: usize,  //秒 pub tv_nsec: usize, //纳秒`
- 文件路径重合：与 2025 如有名字队 在“设备驱动”维度出现同路径源码路径 `os/src/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use core::fmt::{self, Write};`
- 文件路径重合：与 2025 如有名字队 在“中断与异常”维度出现同路径源码路径 `os/src/timer.rs` / `os/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/timer.rs:L8-L12`：关键词命中
    代码片段：`use alloc::sync::Arc; use lazy_static::*; use polyhal::timer::{current_time, set_next_timer}; const TICKS_PER_SEC: usize = 100;`
  - `os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! Timer module using the 'Time' abstraction layer. use polyhal::time::Time;`
- 文件路径重合：与 2025 如有名字队 在“内存管理”维度出现同路径源码路径 `os/src/mm/shm.rs` / `os/src/mm/shm.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/mm/shm.rs:L72-L76`：关键词命中
    代码片段：`let mut frames = Vec::with_capacity(pages); for _ in 0..pages { let frame = super::frame_alloc().ok_or(SyscallErrNo::ENOMEM)?; frames.push(frame); }`
  - `os/src/mm/shm.rs:L8-L12`：关键词命中
    代码片段：`}; use super::{frame_alloc, FrameTracker, MapPermission}; use lazy_static::*;`
- 文件路径重合：与 2025 如有名字队 在“系统调用”维度出现同路径源码路径 `os/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/syscall/fs.rs:L19-L23`：关键词命中
    代码片段：`/// unlinkat flags: 允许删除目录项指向的是目录（否则返回 EPERM）。 const AT_REMOVEDIR: u32 = 0x200; /// 路径型 syscall flags: 不跟随最终分量的符号链接。 const AT_SYMLINK_NOFOLLOW: usize = 0x100; /// 路径型 syscall fl...`
  - `os/src/syscall/fs.rs:L15-L19`：关键词命中
    代码片段：`}; use crate::signal::SignalFlags; use crate::syscall::option::{ fcntl_cmd_name, FdSet, F_DUPFD, F_DUPFD_CLOEXEC, F_GETFD, F_GETFL, F_SETFD, };`

## 相似点

- 与 2025 如有名字队 同属 rcore-variant 风格。（置信度：medium）
- 与 2025 如有名字队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
  - `os/src/drivers/bus/mod.rs:L1-L1`：关键词命中
    代码片段：`pub mod virtio;`
- 与 2025 如有名字队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L13-L17`：关键词命中
    代码片段：`pub use fdtable::FdTable; pub use filesystem::vfs::{chdir, dcache_children, dentry_cache_cap, dentry_cache_hits, dentry_cache_len, dentry_cache_misses, get_cwd, get_root_dentry,...`
  - `os/src/fs/fdtable.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use filesystem::vfs::File as VfsFile; use filesystem::vfs_defs::{OpenFlags, VfsError, VfsRet};`
- 与 2025 如有名字队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/timer.rs:L8-L12`：关键词命中
    代码片段：`use alloc::sync::Arc; use lazy_static::*; use polyhal::timer::{current_time, set_next_timer}; const TICKS_PER_SEC: usize = 100;`
  - `os/src/trap/mod.rs:L11-L15`：关键词命中
    代码片段：`SigInfo, SIGBUS, SIGSEGV, SIGILL, SIGTRAP, BUS_ADRERR, SEGV_MAPERR, ILL_ILLOPC, TRAP_BRKPT, }; use crate::timer::{check_timer, set_next_trigger}; use crate::config::PAGE_SIZE; u...`
- 与 2025 如有名字队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/elf.rs:L74-L78`：关键词命中
    代码片段：`/// '"/lib/ld-linux-riscv64-lp64d.so.1"'。 pub interp_path: Option<String>, /// 所有 PT_LOAD 段，按文件顺序排列。供 'MemorySet::from_elf()' 消费。 pub segments: Vec<ElfSegment>, }`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`mod address; mod elf; mod frame_allocator; mod heap_allocator; mod memory_set;`
- 与 2025 如有名字队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L5-L9`：关键词命中
    代码片段：`}; use crate::mm::{KERNEL_SPACE, PhysPageNum, VirtAddr}; use crate::task::boot_page_table; use polyhal::{MappingFlags, MappingSize::Page4KB}; use crate::sync::UPIntrFreeCell;`
  - `os/src/task/mod.rs:L13-L17`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
- 与 2025 如有名字队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/sync/mod.rs:L8-L12`：关键词命中
    代码片段：`pub use ksync::{ Condvar, Mutex, MutexBlocking, MutexSpin, Semaphore, UPIntrFreeCell, UPIntrRefMut, UPSafeCellRaw, };`
  - `os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`// use alloc::{collections::VecDeque, sync::Arc}; // pub trait Mutex: Sync + Send { //     fn lock(&self); //     fn unlock(&self);`
- 与 2025 如有名字队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L19-L23`：关键词命中
    代码片段：`/// unlinkat flags: 允许删除目录项指向的是目录（否则返回 EPERM）。 const AT_REMOVEDIR: u32 = 0x200; /// 路径型 syscall flags: 不跟随最终分量的符号链接。 const AT_SYMLINK_NOFOLLOW: usize = 0x100; /// 路径型 syscall fl...`
  - `os/src/syscall/mod.rs:L194-L198`：关键词命中
    代码片段：`use polyhal_trap::trapframe::TrapFrameArgs; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> SyscallRet { let tid = crate::task::current_task() .and_then(|t| {`
- 与 2025 如有名字队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 如有名字队 的语言构成不同：待测作品为 {'json': 281998, 'build': 9736, 'markdown': 25849, 'toml': 158, 'rust': 644628, 'text': 277, 'asm': 436, 'yaml': 260}，历史样本为 {'json': 281978, 'build': 9776, 'text': 482, 'markdown': 22278, 'toml': 9717, 'yaml': 87, 'c': 27884, 'asm': 928, 'rust': 762578, 'make': 664}。（置信度：medium）

## 可能创新点

- 待测作品包含 379 个历史样本 2025 如有名字队 中未见的独特函数/类型定义（如 `set_extent_block_checksum`, `fuse_mkdir`, `fuse_rename`, `get_pblock_idx`, `balloc_free_blocks`, `write_entry`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `filesystem/ext4_rs/src/ext4_impls/extents.rs:L1289-L1289`：fn set_extent_block_checksum
    代码片段：`fn set_extent_block_checksum(&self, inode_ref: &Ext4InodeRef, block_addr: usize) -> Result<()> {`
  - `filesystem/ext4_rs/src/fuse_interface/mod.rs:L163-L163`：fn fuse_mkdir
    代码片段：`pub fn fuse_mkdir(&mut self, parent: u64, name: &str, mode: u32, umask: u32) -> Result<usize> {`

## 附录：核验摘要

- 关键结论数：23
- 含证据关键结论数：23（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
