# 2026 T2026105549911448 比较报告

- 对比历史仓库：2024 NPUcore-IMPACT!!!
- 生成时间：2026-07-02T09:03:21.099659+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 NPUcore-IMPACT!!!（来源：赛事历史作品）：画像相似度 score=9.59；同属 ucore-variant 风格; 语言构成相似度 0.95; OS 维度重合度 1.00; 代码规模接近度 0.69

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! Kernel console output. //! //! Byte I/O is delegated to the active architecture backend`
  - `kernel/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device drivers (M7). /// PCI ECAM enumeration for the loongarch64 'virt' board (virtio-pci).`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`//! VFS skeleton. Minimal Inode/File/FdTable, in-memory only (M5). use alloc::collections::BTreeMap;`
  - `kernel/src/fs/ext2.rs:L1-L5`：关键词命中
    代码片段：`//! ext2 read/write filesystem on a block device (our writable scratch, x1). //! //! A real on-disk filesystem: superblock, block/inode bitmaps, on-disk //! inodes with direct +...`
  - `os/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod file_trait; mod filesystem; // mod inode; mod layout; pub mod poll;`
  - `os/src/fs/cache.rs:L193-L197`：关键词命中
    代码片段：`/// Every time kernel tried to alloc this pagecache this number will increase 1(at most 1) /// Every time out of memory occurred this number will decrease 1(at least 0) /// When...`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/arch/riscv64/trap.S:L1-L3`：关键词命中
    代码片段：`# Trap entry for both S-mode (kernel) and U-mode (user) traps. # # Convention:`
  - `kernel/src/arch/riscv64/trap.rs:L1-L3`：关键词命中
    代码片段：`//! S-mode trap dispatch (both kernel and user traps). use core::arch::global_asm;`
  - `os/src/timer.rs:L18-L22`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/main.rs:L36-L40`：关键词命中
    代码片段：`mod syscall; mod task; mod timer; // use crate::arch::config::DISK_IMAGE_BASE;`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/mm/mod.rs:L5-L9`：关键词命中
    代码片段：`pub mod heap; pub mod memory_set; pub mod page_table; pub use address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum, PAGE_SIZE};`
  - `kernel/src/mm/heap.rs:L42-L46`：关键词命中
    代码片段：`#[global_allocator] static HEAP_ALLOCATOR: PreemptHeap = PreemptHeap(LockedHeap::empty()); pub fn init() {`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `os/src/mm/map_area.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::Debug; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Tasks + cooperative scheduler (M5 part 2). //! //! Layout: each Task owns a kstack buffer with the per-task TrapFrame`
  - `kernel/src/main.rs:L35-L39`：关键词命中
    代码片段：`mod sync; mod syscall; mod task; mod vdso;`
  - `os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `os/src/task/task.rs:L380-L384`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/sync/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Locking primitives. //! //! 'Mutex' is a preempt-safe wrapper over 'spin::Mutex' (see 'spinlock'): while //! a lock is held it disables scheduler preemption, so a task holdi...`
  - `kernel/src/sync/futex.rs:L18-L22`：关键词命中
    代码片段：`use alloc::collections::{BTreeMap, VecDeque}; use alloc::sync::Arc; use core::sync::atomic::{AtomicI32, Ordering}; use crate::sync::Mutex; use spin::Lazy;`
  - `os/src/fs/mod.rs:L35-L39`：关键词命中
    代码片段：`use lazy_static::*; use log::warn; use spin::Mutex; lazy_static! {`
  - `os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::{arch::BLOCK_SZ, config::PAGE_SIZE, drivers::BLOCK_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/syscall/nr.rs:L1-L3`：关键词命中
    代码片段：`//! Linux generic riscv64 syscall numbers (selected subset). //! Source: include/uapi/asm-generic/unistd.h`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Syscall dispatch. pub mod keys;`
  - `os/src/syscall/fs.rs:L548-L552`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `os/src/syscall/mod.rs:L115-L119`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现片段级代码相似度 0.73 （token=0.67, structure=0.85）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fmt, fn, for, impl, in, mut, result, s。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L9-L13`：impl Write::for::Console
    代码片段：`struct Console; impl Write for Console { fn write_str(&mut self, s: &str) -> fmt::Result { for &b in s.as_bytes() {`
  - `os/src/console.rs:L10-L14`：fn write_str
    代码片段：`impl Write for KernelOutput { fn write_str(&mut self, s: &str) -> fmt::Result { let mut i = 0; for c in s.chars() {`
- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现片段级代码相似度 0.70 （token=0.63, structure=0.85）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fmt, fn, for, impl, in, mut, result, s。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L10-L14`：fn write_str
    代码片段：`impl Write for Console { fn write_str(&mut self, s: &str) -> fmt::Result { for &b in s.as_bytes() { crate::arch::console_put(b);`
  - `os/src/console.rs:L10-L14`：fn write_str
    代码片段：`impl Write for KernelOutput { fn write_str(&mut self, s: &str) -> fmt::Result { let mut i = 0; for c in s.chars() {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现 3 个同名定义：create, unlink, read_block。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/fs/ext2.rs:L937-L937`：fn create
    代码片段：`fn create(&self, name: &str, kind: FileType) -> Result<Arc<dyn Inode>> {`
  - `os/src/fs/directory_tree.rs:L263-L263`：fn create
    代码片段：`fn create(&self, name: &str, file_type: DiskInodeType) -> Result<Arc<dyn File>, isize> {`
  - `kernel/src/fs/ext2.rs:L1042-L1042`：fn unlink
    代码片段：`fn unlink(&self, name: &str) -> Result<()> {`
  - `os/src/fs/ext4_file.rs:L94-L94`：fn unlink
    代码片段：`fn unlink(&self, delete: bool) -> Result<(), isize> {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现 2 个同名定义：write_str, read_block。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L12-L12`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `user/src/console.rs:L10-L10`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `kernel/src/drivers/virtio_blk.rs:L370-L370`：fn read_block
    代码片段：`pub fn read_block(&self, sector: usize, buf: &mut [u8]) -> Result<(), ()> {`
  - `os/src/fs/cache.rs:L90-L90`：fn read_block
    代码片段：`pub fn read_block(&mut self, block_id: usize, block_device: &Arc<dyn BlockDevice>) {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“内存管理”维度发现 1 个同名定义：alloc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L514-L514`：fn alloc
    代码片段：`pub fn alloc(&self, file: Arc<File>, cloexec: bool) -> Result<i32> {`
  - `os/src/mm/frame_allocator.rs:L46-L46`：fn alloc
    代码片段：`fn alloc(&mut self) -> Option<FrameTracker>;`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“同步机制”维度发现 2 个同名定义：read_block, new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/drivers/virtio_blk.rs:L370-L370`：fn read_block
    代码片段：`pub fn read_block(&self, sector: usize, buf: &mut [u8]) -> Result<(), ()> {`
  - `os/src/fs/cache.rs:L90-L90`：fn read_block
    代码片段：`pub fn read_block(&mut self, block_id: usize, block_device: &Arc<dyn BlockDevice>) {`
  - `kernel/src/fs/notify.rs:L109-L109`：fn new
    代码片段：`pub fn new(nonblock: bool) -> Arc<Self> {`
  - `os/src/fs/cache.rs:L81-L81`：fn new
    代码片段：`pub fn new(buffer_ptr: *mut [u8; BUFFER_SIZE]) -> Self {`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现 1 个同名定义：BlockDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/drivers/virtio_blk.rs:L183-L183`：struct BlockDevice
    代码片段：`pub struct BlockDevice {`
  - `os/src/fs/mod.rs:L20-L20`：trait BlockDevice
    代码片段：`pub trait BlockDevice: crate::drivers::block::BlockDevice {}`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现 2 个同名定义：File, FdTable。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L356-L356`：struct File
    代码片段：`pub struct File {`
  - `os/src/fs/file_trait.rs:L14-L14`：trait File
    代码片段：`pub trait File: DowncastSync {`
  - `kernel/src/fs/mod.rs:L459-L459`：struct FdTable
    代码片段：`pub struct FdTable {`
  - `os/src/fs/mod.rs:L252-L252`：struct FdTable
    代码片段：`pub struct FdTable {`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“同步机制”维度发现 1 个同名定义：BlockDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/drivers/virtio_blk.rs:L183-L183`：struct BlockDevice
    代码片段：`pub struct BlockDevice {`
  - `os/src/fs/mod.rs:L20-L20`：trait BlockDevice
    代码片段：`pub trait BlockDevice: crate::drivers::block::BlockDevice {}`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度出现同名文件源码路径 `kernel/src/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! Kernel console output. //! //! Byte I/O is delegated to the active architecture backend`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`

## 相似点

- 与 2024 NPUcore-IMPACT!!! 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! Kernel console output. //! //! Byte I/O is delegated to the active architecture backend`
  - `kernel/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device drivers (M7). /// PCI ECAM enumeration for the loongarch64 'virt' board (virtio-pci).`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`//! VFS skeleton. Minimal Inode/File/FdTable, in-memory only (M5). use alloc::collections::BTreeMap;`
  - `kernel/src/fs/ext2.rs:L1-L5`：关键词命中
    代码片段：`//! ext2 read/write filesystem on a block device (our writable scratch, x1). //! //! A real on-disk filesystem: superblock, block/inode bitmaps, on-disk //! inodes with direct +...`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/arch/riscv64/trap.S:L1-L3`：关键词命中
    代码片段：`# Trap entry for both S-mode (kernel) and U-mode (user) traps. # # Convention:`
  - `kernel/src/arch/riscv64/trap.rs:L1-L3`：关键词命中
    代码片段：`//! S-mode trap dispatch (both kernel and user traps). use core::arch::global_asm;`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/mm/mod.rs:L5-L9`：关键词命中
    代码片段：`pub mod heap; pub mod memory_set; pub mod page_table; pub use address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum, PAGE_SIZE};`
  - `kernel/src/mm/heap.rs:L42-L46`：关键词命中
    代码片段：`#[global_allocator] static HEAP_ALLOCATOR: PreemptHeap = PreemptHeap(LockedHeap::empty()); pub fn init() {`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Tasks + cooperative scheduler (M5 part 2). //! //! Layout: each Task owns a kstack buffer with the per-task TrapFrame`
  - `kernel/src/main.rs:L35-L39`：关键词命中
    代码片段：`mod sync; mod syscall; mod task; mod vdso;`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/sync/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Locking primitives. //! //! 'Mutex' is a preempt-safe wrapper over 'spin::Mutex' (see 'spinlock'): while //! a lock is held it disables scheduler preemption, so a task holdi...`
  - `kernel/src/sync/futex.rs:L18-L22`：关键词命中
    代码片段：`use alloc::collections::{BTreeMap, VecDeque}; use alloc::sync::Arc; use core::sync::atomic::{AtomicI32, Ordering}; use crate::sync::Mutex; use spin::Lazy;`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/syscall/nr.rs:L1-L3`：关键词命中
    代码片段：`//! Linux generic riscv64 syscall numbers (selected subset). //! Source: include/uapi/asm-generic/unistd.h`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Syscall dispatch. pub mod keys;`
- 与 2024 NPUcore-IMPACT!!! 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 NPUcore-IMPACT!!! 的语言构成不同：待测作品为 {'build': 4296, 'markdown': 9372, 'toml': 83, 'asm': 544, 'rust': 266740, 'c': 418, 'json': 264, 'yaml': 104, 'text': 35}，历史样本为 {'json': 20, 'build': 338, 'markdown': 743, 'rust': 177629, 'asm': 10}。（置信度：medium）

## 可能创新点

- 待测作品包含 261 个历史样本 2024 NPUcore-IMPACT!!! 中未见的独特函数/类型定义（如 `write_raw_inode`, `extent_map`, `from_inode`, `read_dir`, `xattr_store_set`, `put_dirent`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/src/fs/ext2.rs:L629-L629`：fn write_raw_inode
    代码片段：`fn write_raw_inode(dev: &BlockDevice, inode_table: u32, ino: u32, di: &DiskInode) -> Result<()> {`
  - `kernel/src/fs/ext4.rs:L195-L195`：fn extent_map
    代码片段：`fn extent_map(&self, ino: &Inode4) -> core::result::Result<Vec<(u32, u64, u32)>, &'static str> {`

## 附录：核验摘要

- 关键结论数：25
- 含证据关键结论数：25（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
