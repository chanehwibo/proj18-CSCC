# 2026 T202610487999858 比较报告

- 对比历史仓库：2025 啊对的对的,嗷不对不对
- 生成时间：2026-07-02T09:03:15.645079+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 啊对的对的,嗷不对不对（来源：赛事历史作品）：画像相似度 score=9.99；同属 ucore-variant 风格; 语言构成相似度 1.00; OS 维度重合度 1.00; 代码规模接近度 1.00

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 啊对的对的,嗷不对不对 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mem/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/driver/bio.c:L23-L27`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include "fs/buf.h" #include "dev/virtio.h" struct {`
  - `kernel/mem/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/driver/bio.c:L23-L27`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include "fs/buf.h" #include "dev/virtio.h" struct {`
- 与 2025 啊对的对的,嗷不对不对 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/vfs/fs.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include <defs.h>`
  - `kernel/fs/vfs/ops.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/ops.h" #include <fs/fcntl.h>`
  - `kernel/fs/vfs/fs.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include <defs.h>`
  - `kernel/fs/vfs/ops.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/ops.h" #include <fs/fcntl.h>`
- 与 2025 啊对的对的,嗷不对不对 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sys/plic.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "proc/plic.h" //`
  - `kernel/trap/riscv/trap.c:L8-L12`：关键词命中
    代码片段：`#include "mem/mem.h" #include "sbi.h" #include "proc/plic.h" #include "dev/virtio.h" #include "lib/string.h"`
  - `kernel/sys/plic.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "proc/plic.h" //`
  - `kernel/trap/riscv/trap.c:L8-L12`：关键词命中
    代码片段：`#include "mem/mem.h" #include "sbi.h" #include "proc/plic.h" #include "dev/virtio.h"`
- 与 2025 啊对的对的,嗷不对不对 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mem/vm.c:L8-L12`：关键词命中
    代码片段：`#include "defs.h" #include "fs/vfs/fs.h" #include "mem/kalloc.h" #include "lib/string.h" #include "dev/virtio.h"`
  - `kernel/mem/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "mem/kalloc.h" #include <mem/slab.h>`
  - `kernel/mem/vm.c:L8-L12`：关键词命中
    代码片段：`#include "defs.h" #include "fs/vfs/fs.h" #include "mem/kalloc.h" #include "lib/string.h" #include "dev/virtio.h"`
  - `kernel/mem/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "mem/kalloc.h" #include <mem/slab.h>`
- 与 2025 啊对的对的,嗷不对不对 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/exec.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h" #include "lib/elf.h"`
  - `kernel/proc/pipe.c:L4-L8`：关键词命中
    代码片段：`#include "param.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "lock/sleeplock.h" #include "fs/fcntl.h"`
  - `kernel/proc/exec.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h" #include "lib/elf.h"`
  - `kernel/proc/pipe.c:L4-L8`：关键词命中
    代码片段：`#include "param.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "lock/sleeplock.h" #include "fs/fcntl.h"`
- 与 2025 啊对的对的,嗷不对不对 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "lock/semaphore.h" #include "defs.h"`
  - `kernel/mem/uart.c:L7-L11`：关键词命中
    代码片段：`#include "mem/memlayout.h" #include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h"`
  - `kernel/proc/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "lock/semaphore.h" #include "defs.h"`
  - `kernel/mem/uart.c:L7-L11`：关键词命中
    代码片段：`#include "mem/memlayout.h" #include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h"`
- 与 2025 啊对的对的,嗷不对不对 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sys/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "lock/spinlock.h" #include "proc/proc.h" #include "sys/syscall.h" #include "defs.h" #include "lib/string.h"`
  - `kernel/trap/riscv/trap.c:L188-L192`：关键词命中
    代码片段：`exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/sys/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "lock/spinlock.h" #include "proc/proc.h" #include "sys/syscall.h" #include "defs.h" #include "lib/string.h"`
  - `kernel/trap/riscv/trap.c:L127-L131`：关键词命中
    代码片段：`exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 啊对的对的,嗷不对不对 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：bus, char, device, function, int, pci_scan_device, unsigned, val。（置信度：high）
  证据：
  - `kernel/driver/loongarch/pci.c:L101-L105`：fn pci_scan_device
    代码片段：`void pci_scan_device(unsigned char bus, unsigned char device, unsigned char function) { unsigned int val; // pr_info("read config a\n");`
  - `kernel/driver/loongarch/pci.c:L101-L105`：fn pci_scan_device
    代码片段：`void pci_scan_device(unsigned char bus, unsigned char device, unsigned char function) { unsigned int val; // pr_info("read config a\n");`
- 与 2025 啊对的对的,嗷不对不对 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：bus, dev, function, if, int, pci_device_t, pci_get_device_by_bus, pci_max_bus。（置信度：high）
  证据：
  - `kernel/driver/loongarch/pci.c:L227-L231`：fn pci_get_device_by_bus
    代码片段：`/*通过主线号，设备号，功能号寻找设备信息*/ pci_device_t* pci_get_device_by_bus(unsigned int bus, unsigned int dev,unsigned int function){ if (bus>PCI_MAX_BUS|| dev>PCI_MAX_DEV || function>PCI_MAX_...`
  - `kernel/driver/loongarch/pci.c:L227-L231`：fn pci_get_device_by_bus
    代码片段：`/*通过主线号，设备号，功能号寻找设备信息*/ pci_device_t* pci_get_device_by_bus(unsigned int bus, unsigned int dev,unsigned int function){ if (bus>PCI_MAX_BUS|| dev>PCI_MAX_DEV || function>PCI_MAX_...`
- 与 2025 啊对的对的,嗷不对不对 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：for, fs_table_lock, i, init_fs_table, initlock, int, spinlock, struct。（置信度：high）
  证据：
  - `kernel/fs/vfs/fs.c:L26-L30`：fn init_fs_table
    代码片段：`struct spinlock fs_table_lock; void init_fs_table(void) { initlock(&fs_table_lock, "fs_table_lock"); for (int i = 0; i < VFS_MAX_FS; i++) {`
  - `kernel/fs/vfs/fs.c:L26-L30`：fn init_fs_table
    代码片段：`struct spinlock fs_table_lock; void init_fs_table(void) { initlock(&fs_table_lock, "fs_table_lock"); for (int i = 0; i < VFS_MAX_FS; i++) {`
- 与 2025 啊对的对的,嗷不对不对 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：acquire, char, const, dev, filesystem_t, fs, fs_init, fs_t。（置信度：high）
  证据：
  - `kernel/fs/vfs/fs.c:L34-L38`：fn fs_init
    代码片段：`} void fs_init(filesystem_t *fs, int dev, fs_t fs_type, const char *path) { acquire(&fs_table_lock); fs_table[fs_type] = fs;`
  - `kernel/fs/vfs/fs.c:L34-L38`：fn fs_init
    代码片段：`} void fs_init(filesystem_t *fs, int dev, fs_t fs_type, const char *path) { acquire(&fs_table_lock); fs_table[fs_type] = fs;`
- 与 2025 啊对的对的,嗷不对不对 在“中断与异常”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：allocation, cause, cow, int, lazy, pagefault_handler, todo, uint64。（置信度：high）
  证据：
  - `kernel/trap/loongarch/trap.c:L102-L106`：fn pagefault_handler
    代码片段：`} int pagefault_handler(uint64 va, uint64 cause) { /* *TODO: 支持lazy allocation和cow`
  - `kernel/trap/loongarch/trap.c:L46-L50`：fn pagefault_handler
    代码片段：`} int pagefault_handler(uint64 va, uint64 cause) { /* *TODO: 支持lazy allocation和cow`
- 与 2025 啊对的对的,嗷不对不对 在“内存管理”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：bus, dev, func, int, pages, pci_map, void。（置信度：high）
  证据：
  - `kernel/mem/vm.c:L206-L210`：fn pci_map
    代码片段：`} void pci_map(int bus, int dev, int func, void *pages) { #ifdef LOONGARCH`
  - `kernel/mem/vm.c:L206-L210`：fn pci_map
    代码片段：`} void pci_map(int bus, int dev, int func, void *pages) { #ifdef LOONGARCH`
- 宏名重合：与 2025 啊对的对的,嗷不对不对 在“文件系统”维度发现 4 个同名定义：Log, NELEM, CLONE_VM, CLONE_FS。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/debug.h:L47-L47`：macro Log
    代码片段：`#define Log(format, ...) printf("\33[1;34m[%s,%d,%s] " format "\33[0m\n", __FILE__, __LINE__, __func__, ##__VA_ARGS__)`
  - `include/debug.h:L47-L47`：macro Log
    代码片段：`#define Log(format, ...) printf("\33[1;34m[%s,%d,%s] " format "\33[0m\n", __FILE__, __LINE__, __func__, ##__VA_ARGS__)`
  - `include/defs.h:L151-L151`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x)/sizeof((x)[0]))`
  - `include/defs.h:L143-L143`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x)/sizeof((x)[0]))`
- 宏名重合：与 2025 啊对的对的,嗷不对不对 在“中断与异常”维度发现 4 个同名定义：LOONGARCH_CSR_ECFG, LOONGARCH_CSR_ESTAT, LOONGARCH_CSR_ERAISE, LOONGARCH_CSR_EENTRY。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/asm.h:L49-L49`：macro LOONGARCH_CSR_ECFG
    代码片段：`#define LOONGARCH_CSR_ECFG          0x4	    /* Exception config 例外配置 */`
  - `include/asm.h:L49-L49`：macro LOONGARCH_CSR_ECFG
    代码片段：`#define LOONGARCH_CSR_ECFG          0x4	    /* Exception config 例外配置 */`
  - `include/asm.h:L50-L50`：macro LOONGARCH_CSR_ESTAT
    代码片段：`#define LOONGARCH_CSR_ESTAT         0x5	    /* Exception status 例外状态 */`
  - `include/asm.h:L50-L50`：macro LOONGARCH_CSR_ESTAT
    代码片段：`#define LOONGARCH_CSR_ESTAT         0x5	    /* Exception status 例外状态 */`
- 宏名重合：与 2025 啊对的对的,嗷不对不对 在“内存管理”维度发现 3 个同名定义：PTE, VMA, CLONE_VM。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/debug.h:L48-L48`：macro PTE
    代码片段：`#define PTE(format, ...) printf(ANSI_FMT(format, ANSI_FG_GREEN), ##__VA_ARGS__)`
  - `include/debug.h:L48-L48`：macro PTE
    代码片段：`#define PTE(format, ...) printf(ANSI_FMT(format, ANSI_FG_GREEN), ##__VA_ARGS__)`
  - `include/debug.h:L49-L49`：macro VMA
    代码片段：`#define VMA(format, ...) printf(ANSI_FMT(format, ANSI_FG_GREEN), ##__VA_ARGS__)`
  - `include/debug.h:L49-L49`：macro VMA
    代码片段：`#define VMA(format, ...) printf(ANSI_FMT(format, ANSI_FG_GREEN), ##__VA_ARGS__)`
- 文件路径重合：与 2025 啊对的对的,嗷不对不对 在“设备驱动”维度出现同路径源码路径 `kernel/mem/uart.c` / `kernel/mem/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/mem/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/mem/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
- 文件路径重合：与 2025 啊对的对的,嗷不对不对 在“设备驱动”维度出现同路径源码路径 `kernel/driver/bio.c` / `kernel/driver/bio.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/driver/bio.c:L23-L27`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include "fs/buf.h" #include "dev/virtio.h" struct {`
  - `kernel/driver/bio.c:L23-L27`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include "fs/buf.h" #include "dev/virtio.h" struct {`
- 文件路径重合：与 2025 啊对的对的,嗷不对不对 在“文件系统”维度出现同路径源码路径 `kernel/fs/vfs/fs.c` / `kernel/fs/vfs/fs.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/vfs/fs.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include <defs.h>`
  - `kernel/fs/vfs/fs.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include <defs.h>`
- 文件路径重合：与 2025 啊对的对的,嗷不对不对 在“文件系统”维度出现同路径源码路径 `kernel/fs/vfs/ops.c` / `kernel/fs/vfs/ops.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs/vfs/ops.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/ops.h" #include <fs/fcntl.h>`
  - `kernel/fs/vfs/ops.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/ops.h" #include <fs/fcntl.h>`
- 文件路径重合：与 2025 啊对的对的,嗷不对不对 在“中断与异常”维度出现同路径源码路径 `kernel/sys/plic.c` / `kernel/sys/plic.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/sys/plic.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "proc/plic.h" //`
  - `kernel/sys/plic.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "proc/plic.h" //`
- 文件路径重合：与 2025 啊对的对的,嗷不对不对 在“中断与异常”维度出现同路径源码路径 `kernel/trap/riscv/trap.c` / `kernel/trap/riscv/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/trap/riscv/trap.c:L8-L12`：关键词命中
    代码片段：`#include "mem/mem.h" #include "sbi.h" #include "proc/plic.h" #include "dev/virtio.h" #include "lib/string.h"`
  - `kernel/trap/riscv/trap.c:L8-L12`：关键词命中
    代码片段：`#include "mem/mem.h" #include "sbi.h" #include "proc/plic.h" #include "dev/virtio.h"`

## 相似点

- 与 2025 啊对的对的,嗷不对不对 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 啊对的对的,嗷不对不对 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mem/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/driver/bio.c:L23-L27`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include "fs/buf.h" #include "dev/virtio.h" struct {`
- 与 2025 啊对的对的,嗷不对不对 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/vfs/fs.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include <defs.h>`
  - `kernel/fs/vfs/ops.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/ops.h" #include <fs/fcntl.h>`
- 与 2025 啊对的对的,嗷不对不对 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sys/plic.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "proc/plic.h" //`
  - `kernel/trap/riscv/trap.c:L8-L12`：关键词命中
    代码片段：`#include "mem/mem.h" #include "sbi.h" #include "proc/plic.h" #include "dev/virtio.h" #include "lib/string.h"`
- 与 2025 啊对的对的,嗷不对不对 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mem/vm.c:L8-L12`：关键词命中
    代码片段：`#include "defs.h" #include "fs/vfs/fs.h" #include "mem/kalloc.h" #include "lib/string.h" #include "dev/virtio.h"`
  - `kernel/mem/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "mem/kalloc.h" #include <mem/slab.h>`
- 与 2025 啊对的对的,嗷不对不对 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/exec.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h" #include "lib/elf.h"`
  - `kernel/proc/pipe.c:L4-L8`：关键词命中
    代码片段：`#include "param.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "lock/sleeplock.h" #include "fs/fcntl.h"`
- 与 2025 啊对的对的,嗷不对不对 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "lock/semaphore.h" #include "defs.h"`
  - `kernel/mem/uart.c:L7-L11`：关键词命中
    代码片段：`#include "mem/memlayout.h" #include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h"`
- 与 2025 啊对的对的,嗷不对不对 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sys/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "lock/spinlock.h" #include "proc/proc.h" #include "sys/syscall.h" #include "defs.h" #include "lib/string.h"`
  - `kernel/trap/riscv/trap.c:L188-L192`：关键词命中
    代码片段：`exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
- 与 2025 啊对的对的,嗷不对不对 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 啊对的对的,嗷不对不对 的语言构成不同：待测作品为 {'build': 549, 'text': 18, 'markdown': 954, 'c': 282443, 'cpp': 36728, 'asm': 864}，历史样本为 {'json': 20, 'build': 479, 'markdown': 1008, 'c': 280934, 'asm': 1166, 'text': 9, 'cpp': 36728}。（置信度：medium）

## 可能创新点

- 待测作品包含 90 个历史样本 2025 啊对的对的,嗷不对不对 中未见的独特函数/类型定义（如 `ext4_bg_set_inode_table_first_block`, `ext4_bmap_is_bit_clr`, `ext4_bmap_bit_clr`, `ext4_dir_en_set_entry_len`, `ext4_dir_en_set_inode`, `ext4_bg_set_free_blocks_count`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `include/fs/ext4/lwext4/ext4_block_group.h:L90-L90`：fn ext4_bg_set_inode_table_first_block
    代码片段：`static inline void ext4_bg_set_inode_table_first_block(struct ext4_bgroup *bg, struct ext4_sblock *s, uint64_t blk) {`
  - `include/fs/ext4/lwext4/ext4_bitmap.h:L35-L35`：fn ext4_bmap_is_bit_clr
    代码片段：`static inline bool ext4_bmap_is_bit_clr(uint8_t *bmap, uint32_t bit) { return !ext4_bmap_is_bit_set(bmap, bit); }`

## 附录：核验摘要

- 关键结论数：30
- 含证据关键结论数：30（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
