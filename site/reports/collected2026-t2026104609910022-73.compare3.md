# 2026 T2026104609910022 比较报告

- 对比历史仓库：rCore-Tutorial-v3
- 生成时间：2026-07-02T09:02:19.502289+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- rCore-Tutorial-v3（来源：教学基线）：画像相似度 score=9.55；同属 rcore-variant 风格; 语言构成相似度 0.98; OS 维度重合度 1.00; 代码规模接近度 0.60

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 rCore-Tutorial-v3 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/drivers/block.rs:L20-L24`：关键词命中
    代码片段：`#[cfg(feature = "arch-riscv64")] mod virtio_mmio { use super::{BlockProbe, BLOCK_SIZE}; use core::mem::size_of;`
  - `src-la/drivers/block.rs:L20-L24`：关键词命中
    代码片段：`#[cfg(feature = "arch-riscv64")] mod virtio_mmio { use super::{BlockProbe, BLOCK_SIZE}; use core::mem::size_of;`
  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
  - `os/src/drivers/bus/mod.rs:L1-L1`：关键词命中
    代码片段：`pub mod virtio;`
- 与 rCore-Tutorial-v3 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`use crate::println; pub mod fd_table; pub mod vfs; pub mod ext4; pub mod manager;`
  - `src/fs/ext4.rs:L1-L3`：关键词命中
    代码片段：`//! EXT4 file system layer ? inode lookup, file read use core::cell::Cell;`
  - `os/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`mod inode; mod pipe; mod stdio;`
  - `os/src/fs/inode.rs:L6-L10`：关键词命中
    代码片段：`use alloc::vec::Vec; use bitflags::*; use easy_fs::{EasyFileSystem, Inode}; use lazy_static::*;`
- 与 rCore-Tutorial-v3 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/trap/mod.rs:L5-L9`：关键词命中
    代码片段：`current_task, current_trap_cx, do_wake_expired, suspend_current_and_run_next, }; use crate::timer::set_next_trigger; pub use context::{GeneralRegs, TrapContext}; mod context;`
  - `src/trap/context.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 上下文 — 保存/恢复用户态寄存器 /// 通用寄存器（RISC-V 64）`
  - `os/src/timer.rs:L63-L67`：关键词命中
    代码片段：`let current_ms = get_time_ms(); TIMERS.exclusive_session(|timers| { while let Some(timer) = timers.peek() { if timer.expire_ms <= current_ms { wakeup_task(Arc::clone(&timer.task));`
  - `os/src/trap/mod.rs:L8-L12`：关键词命中
    代码片段：`suspend_current_and_run_next, }; use crate::timer::{check_timer, set_next_trigger}; use core::arch::{asm, global_asm}; use riscv::register::{`
- 与 rCore-Tutorial-v3 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`use crate::{print, println}; mod address; mod heap_allocator; mod frame_allocator; mod page_table;`
  - `src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use crate::mm::address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum, VPNRange}; use crate::mm::page_table::{PTEFlags, PageTableEntry, PageTable}; use crate::mm::frame_allocato...`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{FrameTracker, frame_alloc}; use super::{PTEFlags, PageTable, PageTableEntry}; use super::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum};`
- 与 rCore-Tutorial-v3 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`pub mod pid; pub mod processor; pub mod task; use crate::timer::get_time;`
  - `src/task/pid.rs:L14-L18`：关键词命中
    代码片段：`impl PidAllocator { pub fn new() -> Self { // Linux userspace reserves pid 0 for the scheduler/idle context. Self { current: 1, recycled: Vec::new() } }`
  - `os/src/task/mod.rs:L7-L11`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
  - `os/src/task/switch.S:L13-L17`：关键词命中
    代码片段：`#     next_task_cx_ptr: *const TaskContext # ) # save kernel stack of current task sd sp, 8(a0) # save ra & s0~s11 of current execution`
- 与 rCore-Tutorial-v3 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/task/mod.rs:L10-L14`：关键词命中
    代码片段：`use crate::config::CLOCK_FREQ; use lazy_static::*; use spin::Mutex; const PER_TASK_TIMEOUT: usize = 10 * CLOCK_FREQ;`
  - `src/task/pid.rs:L3-L7`：关键词命中
    代码片段：`use crate::config::{KERNEL_HEAP_SIZE, PAGE_SIZE}; use alloc::vec::Vec; use spin::Mutex; use lazy_static::*;`
  - `os/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`mod condvar; mod mutex; mod semaphore;`
  - `os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub trait Mutex: Sync + Send { fn lock(&self); fn unlock(&self);`
- 与 rCore-Tutorial-v3 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/syscall/mod.rs:L6-L10`：关键词命中
    代码片段：`use errno::*; // Linux RISC-V 64 syscall IDs（只列出我们需要的） pub const SYS_WRITE: usize = 64; pub const SYS_EVENTFD2: usize = 19;`
  - `src/trap/context.rs:L57-L61`：关键词命中
    代码片段：`/// 保留字段（汇编未使用，仅用于对齐） pub reserved: usize,          // 32*8 /// 发生 ecall 前的原始 a0（syscall 重启时需要） pub origin_a0: usize,         // 33*8 /// 陷入时的 sstatus（SPP/SPIE 等）`
  - `os/src/syscall/mod.rs:L50-L54`：关键词命中
    代码片段：`use thread::*; pub fn syscall(syscall_id: usize, args: [usize; 3]) -> isize { match syscall_id { SYSCALL_DUP => sys_dup(args[0]),`
  - `user/src/syscall.rs:L34-L38`：关键词命中
    代码片段：`const SYSCALL_KEY_PRESSED: usize = 3001; fn syscall(id: usize, args: [usize; 3]) -> isize { let mut ret: isize; unsafe {`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 rCore-Tutorial-v3 在“系统调用”维度发现片段级代码相似度 0.84 （token=0.75, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, init, pub, set_kernel_trap_entry。（置信度：high）
  证据：
  - `src/trap/mod.rs:L22-L26`：fn init
    代码片段：`} pub fn init() { // 内核模式 stvec 直接指向 trap_from_kernel set_kernel_trap_entry();`
  - `os/src/trap/mod.rs:L18-L22`：fn init
    代码片段：`global_asm!(include_str!("trap.S")); pub fn init() { set_kernel_trap_entry(); }`
- 与 rCore-Tutorial-v3 在“系统调用”维度发现片段级代码相似度 0.80 （token=0.69, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：__alltraps, extern, fn。（置信度：high）
  证据：
  - `src/trap/mod.rs:L18-L22`：fn __alltraps
    代码片段：`extern "C" { pub fn __alltraps(); pub fn __restore(); }`
  - `os/src/trap/mod.rs:L22-L26`：fn set_kernel_trap_entry
    代码片段：`} fn set_kernel_trap_entry() { unsafe extern "C" { unsafe fn __alltraps();`
- 函数/符号名重合：与 rCore-Tutorial-v3 在“文件系统”维度发现 4 个同名定义：new, close, dup, read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src-la/fs/ext4.rs:L28-L28`：fn new
    代码片段：`pub fn new() -> Result<Self, ()> {`
  - `os/src/fs/inode.rs:L23-L23`：fn new
    代码片段：`pub fn new(readable: bool, writable: bool, inode: Arc<Inode>) -> Self {`
  - `src-la/fs/fd_table.rs:L84-L84`：fn close
    代码片段：`pub fn close(&mut self, fd: usize) -> Result<FileDescriptor, ()> {`
  - `user/src/file.rs:L19-L19`：fn close
    代码片段：`pub fn close(fd: usize) -> isize {`
- 函数/符号名重合：与 rCore-Tutorial-v3 在“内存管理”维度发现 4 个同名定义：page_offset, floor, ceil, new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src-la/mm/address.rs:L22-L22`：fn page_offset
    代码片段：`pub fn page_offset(&self) -> usize { self.0 & (PAGE_SIZE - 1) }`
  - `os/src/mm/address.rs:L110-L110`：fn page_offset
    代码片段：`pub fn page_offset(&self) -> usize {`
  - `src-la/mm/address.rs:L23-L23`：fn floor
    代码片段：`pub fn floor(&self) -> PhysPageNum { PhysPageNum(self.0 / PAGE_SIZE) }`
  - `os/src/mm/address.rs:L100-L100`：fn floor
    代码片段：`pub fn floor(&self) -> VirtPageNum {`
- 与 rCore-Tutorial-v3 在“内存管理”维度发现片段级代码相似度 0.76 （token=0.62, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：frame_alloc, frametracker, pagetable, pagetableentry, physaddr, physpagenum, pteflags, use。（置信度：medium）
  证据：
  - `src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use crate::mm::address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum, VPNRange}; use crate::mm::page_table::{PTEFlags, PageTableEntry, PageTable}; use crate::mm::frame_allocato...`
  - `os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{FrameTracker, frame_alloc}; use super::{PTEFlags, PageTable, PageTableEntry}; use super::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum};`
- 与 rCore-Tutorial-v3 在“内存管理”维度发现片段级代码相似度 0.68 （token=0.58, structure=0.86）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：alloc, frame_alloc, frametracker, physaddr, physpagenum, use, vec, virtaddr。（置信度：medium）
  证据：
  - `src/mm/page_table.rs:L1-L5`：关键词命中
    代码片段：`use bitflags::*; use crate::mm::address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum}; use crate::mm::frame_allocator::{frame_alloc, frame_dealloc, FrameTracker}; use alloc::v...`
  - `os/src/mm/page_table.rs:L1-L3`：关键词命中
    代码片段：`use super::{FrameTracker, PhysAddr, PhysPageNum, StepByOne, VirtAddr, VirtPageNum, frame_alloc}; use alloc::string::String; use alloc::vec;`
- 与 rCore-Tutorial-v3 在“调度与任务管理”维度发现片段级代码相似度 0.67 （token=0.60, structure=0.80）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：current, fn, impl, new, pub, self。（置信度：medium）
  证据：
  - `src/task/pid.rs:L14-L18`：关键词命中
    代码片段：`impl PidAllocator { pub fn new() -> Self { // Linux userspace reserves pid 0 for the scheduler/idle context. Self { current: 1, recycled: Vec::new() } }`
  - `os/src/task/id.rs:L15-L19`：fn new
    代码片段：`impl RecycleAllocator { pub fn new() -> Self { RecycleAllocator { current: 0,`
- 函数/符号名重合：与 rCore-Tutorial-v3 在“系统调用”维度发现 4 个同名定义：sys_write, sys_read, sys_close, sys_dup。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src-la/syscall/fs.rs:L27-L27`：fn sys_write
    代码片段：`pub fn sys_write(fd: usize, buf: usize, count: usize) -> isize {`
  - `user/src/syscall.rs:L89-L89`：fn sys_write
    代码片段：`pub fn sys_write(fd: usize, buffer: &[u8]) -> isize {`
  - `src-la/syscall/fs.rs:L127-L127`：fn sys_read
    代码片段：`pub fn sys_read(fd: usize, buf: usize, count: usize) -> isize {`
  - `user/src/syscall.rs:L82-L82`：fn sys_read
    代码片段：`pub fn sys_read(fd: usize, buffer: &mut [u8]) -> isize {`
- 函数/符号名重合：与 rCore-Tutorial-v3 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src-la/console.rs:L7-L7`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {   // 创建write_str函数,输出字符串s中的每个字符`
  - `user/src/console.rs:L11-L11`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 结构体/类型重合：与 rCore-Tutorial-v3 在“文件系统”维度发现 3 个同名定义：Pipe, OpenFlags, File。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src-la/fs/manager.rs:L81-L81`：struct Pipe
    代码片段：`struct Pipe {`
  - `os/src/fs/pipe.rs:L8-L8`：struct Pipe
    代码片段：`pub struct Pipe {`
  - `src-la/fs/vfs.rs:L2-L2`：struct OpenFlags
    代码片段：`pub struct OpenFlags(pub usize);`
  - `user/src/file.rs:L4-L4`：struct OpenFlags
    代码片段：`pub struct OpenFlags: u32 {`
- 结构体/类型重合：与 rCore-Tutorial-v3 在“内存管理”维度发现 4 个同名定义：PhysAddr, VirtAddr, PhysPageNum, VirtPageNum。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src-la/mm/address.rs:L5-L5`：struct PhysAddr
    代码片段：`pub struct PhysAddr(pub usize);`
  - `os/src/mm/address.rs:L13-L13`：struct PhysAddr
    代码片段：`pub struct PhysAddr(pub usize);`
  - `src-la/mm/address.rs:L8-L8`：struct VirtAddr
    代码片段：`pub struct VirtAddr(pub usize);`
  - `os/src/mm/address.rs:L17-L17`：struct VirtAddr
    代码片段：`pub struct VirtAddr(pub usize);`
- 函数/符号名重合：与 rCore-Tutorial-v3 在“调度与任务管理”维度发现 1 个同名定义：sys_yield。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src-la/syscall/process.rs:L89-L89`：fn sys_yield
    代码片段：`pub fn sys_yield() -> isize {`
  - `user/src/syscall.rs:L102-L102`：fn sys_yield
    代码片段：`pub fn sys_yield() -> isize {`
- 结构体/类型重合：与 rCore-Tutorial-v3 在“设备驱动”维度发现 1 个同名定义：Stdout。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src-la/console.rs:L4-L4`：struct Stdout
    代码片段：`struct Stdout;                              // 创建结构体Stdout,作为标准输出的实现`
  - `user/src/console.rs:L8-L8`：struct Stdout
    代码片段：`struct Stdout;`
- 文件路径重合：与 rCore-Tutorial-v3 在“设备驱动”维度出现同名文件源码路径 `src/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print {                        // 定义print宏,接受一个格式字符串和可选的参数 ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?));  // 格式...`
  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
- 文件路径重合：与 rCore-Tutorial-v3 在“内存管理”维度出现同名文件源码路径 `src/mm/memory_set.rs` / `os/src/mm/memory_set.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use crate::mm::address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum, VPNRange}; use crate::mm::page_table::{PTEFlags, PageTableEntry, PageTable}; use crate::mm::frame_allocato...`
  - `os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{FrameTracker, frame_alloc}; use super::{PTEFlags, PageTable, PageTableEntry}; use super::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum};`
- 文件路径重合：与 rCore-Tutorial-v3 在“内存管理”维度出现同名文件源码路径 `src/mm/page_table.rs` / `os/src/mm/page_table.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/mm/page_table.rs:L1-L5`：关键词命中
    代码片段：`use bitflags::*; use crate::mm::address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum}; use crate::mm::frame_allocator::{frame_alloc, frame_dealloc, FrameTracker}; use alloc::v...`
  - `os/src/mm/page_table.rs:L1-L3`：关键词命中
    代码片段：`use super::{FrameTracker, PhysAddr, PhysPageNum, StepByOne, VirtAddr, VirtPageNum, frame_alloc}; use alloc::string::String; use alloc::vec;`
- 文件路径重合：与 rCore-Tutorial-v3 在“系统调用”维度出现同名文件源码路径 `src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/syscall/fs.rs:L25-L29`：fn sys_write
    代码片段：`/// represented by fd 1/2 there, so this keeps regular files, pipes and stdout /// on one ABI path. pub fn sys_write(fd: usize, buf: usize, count: usize) -> isize { let token =...`
  - `os/src/syscall/fs.rs:L4-L8`：fn sys_write
    代码片段：`use alloc::sync::Arc; pub fn sys_write(fd: usize, buf: *const u8, len: usize) -> isize { let token = current_user_token(); let process = current_process();`

## 相似点

- 与 rCore-Tutorial-v3 同属 rcore-variant 风格。（置信度：medium）
- 与 rCore-Tutorial-v3 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/drivers/block.rs:L20-L24`：关键词命中
    代码片段：`#[cfg(feature = "arch-riscv64")] mod virtio_mmio { use super::{BlockProbe, BLOCK_SIZE}; use core::mem::size_of;`
  - `src-la/drivers/block.rs:L20-L24`：关键词命中
    代码片段：`#[cfg(feature = "arch-riscv64")] mod virtio_mmio { use super::{BlockProbe, BLOCK_SIZE}; use core::mem::size_of;`
- 与 rCore-Tutorial-v3 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`use crate::println; pub mod fd_table; pub mod vfs; pub mod ext4; pub mod manager;`
  - `src/fs/ext4.rs:L1-L3`：关键词命中
    代码片段：`//! EXT4 file system layer ? inode lookup, file read use core::cell::Cell;`
- 与 rCore-Tutorial-v3 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/trap/mod.rs:L5-L9`：关键词命中
    代码片段：`current_task, current_trap_cx, do_wake_expired, suspend_current_and_run_next, }; use crate::timer::set_next_trigger; pub use context::{GeneralRegs, TrapContext}; mod context;`
  - `src/trap/context.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 上下文 — 保存/恢复用户态寄存器 /// 通用寄存器（RISC-V 64）`
- 与 rCore-Tutorial-v3 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`use crate::{print, println}; mod address; mod heap_allocator; mod frame_allocator; mod page_table;`
  - `src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use crate::mm::address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum, VPNRange}; use crate::mm::page_table::{PTEFlags, PageTableEntry, PageTable}; use crate::mm::frame_allocato...`
- 与 rCore-Tutorial-v3 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`pub mod pid; pub mod processor; pub mod task; use crate::timer::get_time;`
  - `src/task/pid.rs:L14-L18`：关键词命中
    代码片段：`impl PidAllocator { pub fn new() -> Self { // Linux userspace reserves pid 0 for the scheduler/idle context. Self { current: 1, recycled: Vec::new() } }`
- 与 rCore-Tutorial-v3 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/task/mod.rs:L10-L14`：关键词命中
    代码片段：`use crate::config::CLOCK_FREQ; use lazy_static::*; use spin::Mutex; const PER_TASK_TIMEOUT: usize = 10 * CLOCK_FREQ;`
  - `src/task/pid.rs:L3-L7`：关键词命中
    代码片段：`use crate::config::{KERNEL_HEAP_SIZE, PAGE_SIZE}; use alloc::vec::Vec; use spin::Mutex; use lazy_static::*;`
- 与 rCore-Tutorial-v3 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/syscall/mod.rs:L6-L10`：关键词命中
    代码片段：`use errno::*; // Linux RISC-V 64 syscall IDs（只列出我们需要的） pub const SYS_WRITE: usize = 64; pub const SYS_EVENTFD2: usize = 19;`
  - `src/trap/context.rs:L57-L61`：关键词命中
    代码片段：`/// 保留字段（汇编未使用，仅用于对齐） pub reserved: usize,          // 32*8 /// 发生 ecall 前的原始 a0（syscall 重启时需要） pub origin_a0: usize,         // 33*8 /// 陷入时的 sstatus（SPP/SPIE 等）`
- 与 rCore-Tutorial-v3 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 rCore-Tutorial-v3 的语言构成不同：待测作品为 {'build': 462, 'markdown': 235, 'toml': 32, 'text': 44, 'rust': 22107, 'asm': 360, 'c': 26}，历史样本为 {'json': 39, 'markdown': 314, 'build': 269, 'toml': 20, 'rust': 11081, 'asm': 150}。（置信度：medium）

## 可能创新点

- 待测作品包含 332 个历史样本 rCore-Tutorial-v3 中未见的独特函数/类型定义（如 `install_handle_with_descriptor`, `sys_mount`, `sys_linkat`, `sys_splice`, `socket_pair`, `sys_fchownat`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `src-la/fs/manager.rs:L1911-L1911`：fn install_handle_with_descriptor
    代码片段：`fn install_handle_with_descriptor(&mut self, handle: FileHandle, mut descriptor: FileDescriptor) -> Result<usize, isize> {`
  - `src-la/syscall/fs.rs:L808-L808`：fn sys_mount
    代码片段：`pub fn sys_mount(_special: *const u8, _dir: *const u8, _fstype: *const u8, _flags: usize, _data: *const u8) -> isize { 0 }`

## 附录：核验摘要

- 关键结论数：32
- 含证据关键结论数：32（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
