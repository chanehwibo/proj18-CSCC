# 2026 T2026104869910188 比较报告

- 对比历史仓库：2024 赤道会师
- 生成时间：2026-07-02T05:01:08.700759+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 赤道会师（来源：赛事历史作品）：画像相似度 score=9.40；同属 ucore-variant 风格; 语言构成相似度 0.97; OS 维度重合度 1.00; 代码规模接近度 0.45

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 赤道会师 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 与 2024 赤道会师 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/file.c:L3-L7`：关键词命中
    代码片段：`// // filestat, fileclose may need vfs file operations? #include "types.h"`
  - `kernel/file.h:L1-L4`：关键词命中
    代码片段：`#include "kernel/xv6_fs.h" #include "kernel/vfs/vfs.h" #define PIPESIZE 512`
  - `kernel/fs.c:L67-L71`：关键词命中
    代码片段：`{ char name_buf[255]; const struct ext4_direntry *dentry = NULL; ext4_dir *d = dir_open(path); if (d == NULL) {`
  - `kernel/file.c:L107-L111`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { // printf("[fileread] file: %p, off: %p, type: %d, addr: %p, n: %d\n", f->file, f->off, f->...`
- 与 2024 赤道会师 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L34-L38`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
  - `kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
- 与 2024 赤道会师 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/vm.c:L11-L15`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/page.h:L37-L41`：关键词命中
    代码片段：`// PFN is KERNBASE-relative so that mem_map[0] = page at KERNBASE (0x80000000). // This keeps all indices within [0, (PHYSTOP-KERNBASE)/PGSIZE). #define PFN_DOWN(pa)     (((uint...`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`
  - `kernel/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/kalloc.h" #include "include/string.h" #include "include/printf.h"`
- 与 2024 赤道会师 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "file.h"`
  - `kernel/proc.h:L42-L46`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `kernel/proc.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/proc.h" #include "include/intr.h" #include "include/kalloc.h"`
  - `kernel/include/proc.h:L36-L40`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2024 赤道会师 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "xv6_fs.h"`
  - `kernel/bio.c:L17-L21`：关键词命中
    代码片段：`#include "types.h" #include "param.h" #include "spinlock.h" #include "sleeplock.h" #include "arch.h"`
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`#include "include/types.h" #include "include/riscv.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/buf.h"`
  - `kernel/bio.c:L18-L22`：关键词命中
    代码片段：`#include "include/types.h" #include "include/param.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/riscv.h"`
- 与 2024 赤道会师 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap.c:L85-L89`：关键词命中
    代码片段：`// Save init's registers for epc corruption recovery if(p->pid == 1) { p->saved_epc = p->trapframe->epc + 4;  // instruction after ecall p->saved_ra = p->trapframe->ra; p->saved...`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "errno.h"`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "include/spinlock.h" #include "include/proc.h" #include "include/syscall.h" #include "include/sysinfo.h" #include "include/kalloc.h"`
  - `kernel/include/syscall.h:L10-L14`：关键词命中
    代码片段：`int argaddr(int n, uint64 *ip); int argstr(int n, char *buf, int max); void syscall(void); #endif`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 赤道会师 在“文件系统”维度发现片段级代码相似度 0.88 （token=0.81, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：enum, fd_device, fd_none, fd_pipe, file, int, ref, struct。（置信度：high）
  证据：
  - `kernel/file.h:L13-L17`：struct file
    代码片段：`}; struct file { enum { FD_NONE, FD_PIPE, FD_INODE, FD_DEVICE, FD_SOCKET } type; int ref; // reference count`
  - `kernel/include/file.h:L4-L8`：struct file
    代码片段：`#include "stat.h" struct file { enum { FD_NONE, FD_PIPE, FD_ENTRY, FD_DEVICE } type; int ref; // reference count`
- 与 2024 赤道会师 在“调度与任务管理”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：context, cpu, int, noff, proc, struct。（置信度：high）
  证据：
  - `kernel/proc.h:L42-L46`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `kernel/include/proc.h:L36-L40`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2024 赤道会师 在“调度与任务管理”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：context, ra, sp, struct, uint64。（置信度：high）
  证据：
  - `kernel/proc.h:L6-L10`：struct context
    代码片段：`// Saved registers for kernel context switches. struct context { uint64 ra; uint64 sp;`
  - `kernel/include/proc.h:L15-L19`：struct context
    代码片段：`// Saved registers for kernel context switches. struct context { uint64 ra; uint64 sp;`
- 与 2024 赤道会师 在“系统调用”维度发现片段级代码相似度 0.66 （token=0.55, structure=0.88）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：a0, a7, asm, register, volatile。（置信度：medium）
  证据：
  - `kernel/arch/riscv/arch.h:L330-L334`：关键词命中
    代码片段：`register uint64 a6 asm("a6") = 0; register uint64 a7 asm("a7") = 0x54494D45; asm volatile("ecall" : "+r"(a0) : "r"(a6), "r"(a7)`
  - `kernel/include/sbi.h:L26-L30`：关键词命中
    代码片段：`register uintptr_t a3 asm ("a3") = (uintptr_t)(arg3);	\ register uintptr_t a7 asm ("a7") = (uintptr_t)(which);	\ asm volatile ("ecall"					\ : "+r" (a0)				\ : "r" (a1), "r" (a2...`
- 宏名重合：与 2024 赤道会师 在“设备驱动”维度发现 3 个同名定义：VIRTIO0, BACKSPACE, CONSOLE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/arch/loongarch/memlayout.h:L35-L35`：macro VIRTIO0
    代码片段：`#define VIRTIO0 0x10001000`
  - `kernel/include/memlayout.h:L49-L49`：macro VIRTIO0
    代码片段：`#define VIRTIO0                 0x10001000`
  - `kernel/console.c:L25-L25`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100  // erase the last output character`
  - `kernel/console.c:L24-L24`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100`
- 宏名重合：与 2024 赤道会师 在“文件系统”维度发现 3 个同名定义：NELEM, NOFILE, CONSOLE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/defs.h:L335-L335`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x)/sizeof((x)[0]))`
  - `kernel/include/defs.h:L180-L180`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x)/sizeof((x)[0]))`
  - `kernel/file.h:L33-L33`：macro NOFILE
    代码片段：`#define NOFILE 16  // max open files per process`
  - `kernel/include/param.h:L6-L6`：macro NOFILE
    代码片段：`#define NOFILE      128  // open files per process`
- 宏名重合：与 2024 赤道会师 在“中断与异常”维度发现 4 个同名定义：PLIC, TRAPFRAME, PLIC_PRIORITY, PLIC_PENDING。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/arch/loongarch/memlayout.h:L51-L51`：macro PLIC
    代码片段：`#define PLIC 0L`
  - `kernel/include/memlayout.h:L57-L57`：macro PLIC
    代码片段：`#define PLIC                    0x0c000000L`
  - `kernel/arch/loongarch/memlayout.h:L92-L92`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME  0x80045000UL`
  - `kernel/include/memlayout.h:L127-L127`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME               (TRAMPOLINE - PGSIZE)`
- 函数/符号名重合：与 2024 赤道会师 在“调度与任务管理”维度发现 1 个同名定义：swtch。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/arch/loongarch/swtch.S:L4-L4`：fn swtch
    代码片段：`swtch:`
  - `kernel/swtch.S:L9-L9`：fn swtch
    代码片段：`swtch:`
- 宏名重合：与 2024 赤道会师 在“内存管理”维度发现 2 个同名定义：TRAPFRAME, TRAMPOLINE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/arch/loongarch/memlayout.h:L92-L92`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME  0x80045000UL`
  - `kernel/include/memlayout.h:L127-L127`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME               (TRAMPOLINE - PGSIZE)`
  - `kernel/arch/riscv/memlayout.h:L66-L66`：macro TRAMPOLINE
    代码片段：`#define TRAMPOLINE (MAXVA - PGSIZE)`
  - `kernel/include/memlayout.h:L109-L109`：macro TRAMPOLINE
    代码片段：`#define TRAMPOLINE              (MAXVA - PGSIZE)`
- 宏名重合：与 2024 赤道会师 在“调度与任务管理”维度发现 1 个同名定义：NOFILE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/file.h:L33-L33`：macro NOFILE
    代码片段：`#define NOFILE 16  // max open files per process`
  - `kernel/include/param.h:L6-L6`：macro NOFILE
    代码片段：`#define NOFILE      128  // open files per process`
- 宏名重合：与 2024 赤道会师 在“系统调用”维度发现 1 个同名定义：TRAPFRAME。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/arch/loongarch/memlayout.h:L92-L92`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME  0x80045000UL`
  - `kernel/include/memlayout.h:L127-L127`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME               (TRAMPOLINE - PGSIZE)`
- 文件路径重合：与 2024 赤道会师 在“设备驱动”维度出现同路径源码路径 `kernel/uart.c` / `kernel/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
- 文件路径重合：与 2024 赤道会师 在“文件系统”维度出现同路径源码路径 `kernel/file.c` / `kernel/file.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/file.c:L3-L7`：关键词命中
    代码片段：`// // filestat, fileclose may need vfs file operations? #include "types.h"`
  - `kernel/file.c:L107-L111`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { // printf("[fileread] file: %p, off: %p, type: %d, addr: %p, n: %d\n", f->file, f->off, f->...`
- 结构体/类型重合：与 2024 赤道会师 在“文件系统”维度发现 2 个同名定义：file, devsw。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/file.h:L15-L15`：struct file
    代码片段：`struct file {`
  - `kernel/include/file.h:L6-L6`：struct file
    代码片段：`struct file {`
  - `kernel/file.h:L53-L53`：struct devsw
    代码片段：`struct devsw {`
  - `kernel/include/file.h:L44-L44`：struct devsw
    代码片段：`struct devsw {`
- 文件路径重合：与 2024 赤道会师 在“中断与异常”维度出现同路径源码路径 `kernel/plic.c` / `kernel/plic.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
- 文件路径重合：与 2024 赤道会师 在“中断与异常”维度出现同路径源码路径 `kernel/trap.c` / `kernel/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/trap.c:L34-L38`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
- 文件路径重合：与 2024 赤道会师 在“内存管理”维度出现同路径源码路径 `kernel/vm.c` / `kernel/vm.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/vm.c:L11-L15`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`
- 文件路径重合：与 2024 赤道会师 在“调度与任务管理”维度出现同路径源码路径 `kernel/proc.c` / `kernel/proc.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "file.h"`
  - `kernel/proc.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/proc.h" #include "include/intr.h" #include "include/kalloc.h"`

## 相似点

- 与 2024 赤道会师 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 赤道会师 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
- 与 2024 赤道会师 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/file.c:L3-L7`：关键词命中
    代码片段：`// // filestat, fileclose may need vfs file operations? #include "types.h"`
  - `kernel/file.h:L1-L4`：关键词命中
    代码片段：`#include "kernel/xv6_fs.h" #include "kernel/vfs/vfs.h" #define PIPESIZE 512`
- 与 2024 赤道会师 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L34-L38`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
- 与 2024 赤道会师 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/vm.c:L11-L15`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/page.h:L37-L41`：关键词命中
    代码片段：`// PFN is KERNBASE-relative so that mem_map[0] = page at KERNBASE (0x80000000). // This keeps all indices within [0, (PHYSTOP-KERNBASE)/PGSIZE). #define PFN_DOWN(pa)     (((uint...`
- 与 2024 赤道会师 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "arch.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "file.h"`
  - `kernel/proc.h:L42-L46`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2024 赤道会师 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "xv6_fs.h"`
  - `kernel/bio.c:L17-L21`：关键词命中
    代码片段：`#include "types.h" #include "param.h" #include "spinlock.h" #include "sleeplock.h" #include "arch.h"`
- 与 2024 赤道会师 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap.c:L85-L89`：关键词命中
    代码片段：`// Save init's registers for epc corruption recovery if(p->pid == 1) { p->saved_epc = p->trapframe->epc + 4;  // instruction after ecall p->saved_ra = p->trapframe->ra; p->saved...`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "errno.h"`
- 与 2024 赤道会师 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 赤道会师 的语言构成不同：待测作品为 {'yaml': 62, 'build': 413, 'markdown': 5535, 'c': 184838, 'asm': 1214, 'make': 256, 'text': 13}，历史样本为 {'json': 20, 'build': 241, 'markdown': 2834, 'c': 54062, 'asm': 639}。（置信度：medium）

## 可能创新点

- 待测作品包含 93 个历史样本 2024 赤道会师 中未见的独特函数/类型定义（如 `ext4_bg_set_inode_table_first_block`, `ext4_xattr_get`, `ext4_bmap_is_bit_clr`, `ext4_bmap_bit_clr`, `ext4_dir_en_set_entry_len`, `ext4_dir_en_set_inode`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/fs-ext4/lwext4/include/ext4_block_group.h:L90-L90`：fn ext4_bg_set_inode_table_first_block
    代码片段：`static inline void ext4_bg_set_inode_table_first_block(struct ext4_bgroup *bg, struct ext4_sblock *s, uint64_t blk) {`
  - `kernel/fs-ext4/ext4_xattr_stub.c:L20-L20`：fn ext4_xattr_get
    代码片段：`int ext4_xattr_get(void *ref, uint32_t inode_no, const char *name, void *buf, size_t buf_size, size_t *data_size) {`
- 待测作品代码规模（192,331 LOC）显著大于历史样本 2024 赤道会师（57,796 LOC），可能包含更多功能模块或更完整的实现，但也可能包含非内核代码，需人工确认。（置信度：low）

## 附录：核验摘要

- 关键结论数：34
- 含证据关键结论数：33（97.1%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
