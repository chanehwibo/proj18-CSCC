# 2026 T2026100069910698 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026100069910698-3954`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：52
- 代码/文本行数：15168
- 主要语言：c 14326 LOC, asm 351 LOC, markdown 311 LOC, build 160 LOC, json 20 LOC

## 总览

2026 T2026100069910698 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 c: 14326 LOC, asm: 351 LOC, markdown: 311 LOC, build: 160 LOC。仓库包含 52 个已扫描文件、约 15168 行可分析文本，当前抽取到 716 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（90/100）
- 已确认 OS 维度：6/7；高置信维度：6/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 70/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 9 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 未确认 | unconfirmed | 0 |
| 中断与异常 | 已确认 | high | 5 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`CROSS_COMPILE ?= riscv64-unknown-elf- CC := $(CROSS_COMPILE)gcc LD := $(CROSS_COMPILE)ld`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct UserSigInfo, struct UserRegs, struct UserFpState, struct UserSigContext, struct UserStack。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/kernel/task.c` | L4-L8 | 关键词命中 |
| `src/kernel/fs.c` | L3-L7 | 关键词命中 |
| `src/kernel/main.c` | L7-L11 | 关键词命中 |
| `src/kernel/task.c` | L204-L208 | struct UserSigInfo |
| `src/kernel/task.c` | L214-L218 | struct UserRegs |
| `src/kernel/task.c` | L249-L253 | struct UserFpState |

### 关键代码片段

  - `src/kernel/task.c:L4-L8`：关键词命中
    代码片段：`#include <riscv.h> #include <string.h> #include <task.h> #define CLONE_VM             0x00000100UL`
  - `src/kernel/fs.c:L3-L7`：关键词命中
    代码片段：`#include <fs.h> #include <string.h> #include <task.h> #define FS_EXT4_ID_FLAG 0x40000000`
  - `src/kernel/main.c:L7-L11`：关键词命中
    代码片段：`#include <printk.h> #include <selftest.h> #include <task.h> #include <trap.h> #include <uart.h>`
  - `src/kernel/task.c:L204-L208`：struct UserSigInfo
    代码片段：`} struct UserSigInfo { int signo; int err;`

### 相关符号

`struct UserSigInfo` at `src/kernel/task.c:L204`、`struct UserRegs` at `src/kernel/task.c:L214`、`struct UserFpState` at `src/kernel/task.c:L249`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：struct PageInfo, macro PTE_V, macro PTE_R, macro PTE_W, macro PTE_X。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/kernel/vm.c` | L40-L44 | 关键词命中 |
| `src/kernel/elf.c` | L155-L159 | 关键词命中 |
| `src/kernel/task.c` | L150-L154 | 关键词命中 |
| `src/kernel/vm.c` | L12-L16 | struct PageInfo |
| `src/include/vm.h` | L7-L11 | macro PTE_V |
| `src/include/vm.h` | L8-L12 | macro PTE_R |

### 关键代码片段

  - `src/kernel/vm.c:L40-L44`：关键词命中
    代码片段：`} static int pte_is_leaf(pte_t pte) { return (pte & (PTE_R | PTE_W | PTE_X)) != 0;`
  - `src/kernel/elf.c:L155-L159`：关键词命中
    代码片段：`uintptr_t seg_end; uintptr_t page_va; u64 perm = PTE_U | PTE_R; if (ph->p_type != PT_LOAD) {`
  - `src/kernel/task.c:L150-L154`：关键词命中
    代码片段：`} pa = vm_translate(task->pagetable, (uintptr_t)user_ptr); if (pa == 0) { return;`
  - `src/kernel/vm.c:L12-L16`：struct PageInfo
    代码片段：`#define VM_NO_FREE_PAGE (-1) struct PageInfo { u16 ref; int next;`

### 相关符号

`struct PageInfo` at `src/kernel/vm.c:L12`、`macro PTE_V` at `src/include/vm.h:L7`、`macro PTE_R` at `src/include/vm.h:L8`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：struct EventFdState, struct SocketState, struct EpollEvent, struct EpollWatch, struct EpollState。（置信度：medium）
  - 静态识别到 3 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/kernel/syscall.c` | L8-L12 | 关键词命中 |
| `src/include/riscv.h` | L82-L86 | 关键词命中 |
| `src/user/init.S` | L8-L12 | 关键词命中 |
| `src/kernel/syscall.c` | L145-L149 | struct EventFdState |
| `src/kernel/syscall.c` | L152-L156 | struct SocketState |
| `src/kernel/syscall.c` | L176-L180 | struct EpollEvent |

### 关键代码片段

  - `src/kernel/syscall.c:L8-L12`：关键词命中
    代码片段：`#include <riscv.h> #include <string.h> #include <syscall.h> #include <task.h> #include <uart.h>`
  - `src/include/riscv.h:L82-L86`：关键词命中
    代码片段：`register u64 a7 __asm__("a7") = 0; __asm__ volatile ("ecall" : "+r"(a0) : "r"(a7)`
  - `src/user/init.S:L8-L12`：关键词命中
    代码片段：`li a2, 16 li a7, 64 ecall li a0, 0`
  - `src/kernel/syscall.c:L145-L149`：struct EventFdState
    代码片段：`static struct Pipe pipe_table[TASK_MAX_PIPE]; struct EventFdState { int used; int ref;`

### 相关符号

`struct EventFdState` at `src/kernel/syscall.c:L145`、`struct SocketState` at `src/kernel/syscall.c:L152`、`struct EpollEvent` at `src/kernel/syscall.c:L176`、`macro SYSCALL_ENTRY` at `src/kernel/syscall.c:L5565`、`macro OSK_SYSCALL_H` at `src/include/syscall.h:L1`、`struct SyscallDesc` at `src/include/syscall.h:L165`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct MemFile, struct FsStat, struct FsDirent。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/kernel/fs.c` | L46-L50 | 关键词命中 |
| `src/kernel/ext4.c` | L215-L219 | 关键词命中 |
| `src/include/ext4.h` | L11-L15 | 关键词命中 |
| `src/kernel/fs.c` | L19-L23 | struct MemFile |
| `src/include/fs.h` | L19-L23 | struct FsStat |
| `src/include/fs.h` | L31-L35 | struct FsDirent |

### 关键代码片段

  - `src/kernel/fs.c:L46-L50`：关键词命中
    代码片段：`} static int fs_ext4_id(u32 inode) { return FS_EXT4_ID_FLAG | (int)(inode & FS_EXT4_ID_MASK);`
  - `src/kernel/ext4.c:L215-L219`：关键词命中
    代码片段：`} static u64 inode_size_bytes(const u8 *inode) { return rd64_lohi(inode + 4, inode + 108);`
  - `src/include/ext4.h:L11-L15`：关键词命中
    代码片段：`struct Ext4Dirent { u32 inode; int is_dir; char name[64];`
  - `src/kernel/fs.c:L19-L23`：struct MemFile
    代码片段：`extern unsigned char _binary_build_user_test_echo_elf_end[]; struct MemFile { int used; int deleted;`

### 相关符号

`struct MemFile` at `src/kernel/fs.c:L19`、`struct FsStat` at `src/include/fs.h:L19`、`struct FsDirent` at `src/include/fs.h:L31`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：未确认该维度存在可追溯实现线索。（综合置信度：unconfirmed）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：当前未在核心源码路径中确认 同步机制 的实现证据。

### 证据表

| 证据 | 说明 |
| --- | --- |
| 未确认 | 当前未找到可引用的源码证据 |

### 复核建议

- 建议人工补查目录命名不典型的源码文件，或在后续版本中扩展该维度关键词。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：struct TrapFrame, fn trap_vector。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/kernel/main.c` | L8-L12 | 关键词命中 |
| `src/kernel/syscall.c` | L219-L223 | 关键词命中 |
| `src/kernel/selftest.c` | L6-L10 | 关键词命中 |
| `src/include/trap.h` | L4-L8 | struct TrapFrame |
| `src/arch/riscv64/trap.S` | L39-L43 | fn trap_vector |

### 关键代码片段

  - `src/kernel/main.c:L8-L12`：关键词命中
    代码片段：`#include <selftest.h> #include <task.h> #include <trap.h> #include <uart.h> #include <vm.h>`
  - `src/kernel/syscall.c:L219-L223`：关键词命中
    代码片段：`static int user_range_ok(uintptr_t ptr, usize size, int write); static int epoll_has_ready(struct Task *task, int epoll_id); static void timerfd_update(struct TimerFdState *time...`
  - `src/kernel/selftest.c:L6-L10`：关键词命中
    代码片段：`#include <syscall.h> #include <task.h> #include <trap.h> #include <user.h>`
  - `src/include/trap.h:L4-L8`：struct TrapFrame
    代码片段：`#include <types.h> struct TrapFrame { u64 ra; u64 sp;`

### 相关符号

`struct TrapFrame` at `src/include/trap.h:L4`、`fn trap_vector` at `src/arch/riscv64/trap.S:L39`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：macro VIRTIO_MMIO_MAGIC_VALUE, macro VIRTIO_MMIO_VERSION, macro VIRTIO_MMIO_DEVICE_ID, macro VIRTIO_MMIO_DEVICE_FEATURES, macro VIRTIO_MMIO_DRIVER_FEATURES。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/drivers/uart.c` | L1-L5 | 关键词命中 |
| `src/drivers/virtio_blk.c` | L5-L9 | 关键词命中 |
| `src/drivers/virtio_net.c` | L5-L9 | 关键词命中 |
| `src/drivers/virtio_blk.c` | L5-L9 | macro VIRTIO_MMIO_MAGIC_VALUE |
| `src/drivers/virtio_blk.c` | L6-L10 | macro VIRTIO_MMIO_VERSION |
| `src/drivers/virtio_blk.c` | L7-L11 | macro VIRTIO_MMIO_DEVICE_ID |

### 关键代码片段

  - `src/drivers/uart.c:L1-L5`：关键词命中
    代码片段：`#include <memlayout.h> #include <types.h> #include <uart.h> #define UART_THR 0x00`
  - `src/drivers/virtio_blk.c:L5-L9`：关键词命中
    代码片段：`#include <types.h> #define VIRTIO_MMIO_MAGIC_VALUE        0x000 #define VIRTIO_MMIO_VERSION            0x004 #define VIRTIO_MMIO_DEVICE_ID          0x008`
  - `src/drivers/virtio_net.c:L5-L9`：关键词命中
    代码片段：`#include <types.h> #define VIRTIO_MMIO_MAGIC_VALUE        0x000 #define VIRTIO_MMIO_VERSION            0x004 #define VIRTIO_MMIO_DEVICE_ID          0x008`
  - `src/drivers/virtio_blk.c:L5-L9`：macro VIRTIO_MMIO_MAGIC_VALUE
    代码片段：`#include <types.h> #define VIRTIO_MMIO_MAGIC_VALUE        0x000 #define VIRTIO_MMIO_VERSION            0x004 #define VIRTIO_MMIO_DEVICE_ID          0x008`

### 相关符号

`macro VIRTIO_MMIO_MAGIC_VALUE` at `src/drivers/virtio_blk.c:L5`、`macro VIRTIO_MMIO_VERSION` at `src/drivers/virtio_blk.c:L6`、`macro VIRTIO_MMIO_DEVICE_ID` at `src/drivers/virtio_blk.c:L7`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：14
- 含证据关键结论数：14（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：1
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
