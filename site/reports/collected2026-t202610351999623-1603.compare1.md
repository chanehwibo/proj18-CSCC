# 2026 T202610351999623 比较报告

- 对比历史仓库：2025 只编译了一次
- 生成时间：2026-07-01T21:00:25.477408+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 只编译了一次（来源：赛事历史作品）：画像相似度 score=9.38；同属 ucore-variant 风格; 语言构成相似度 0.88; OS 维度重合度 1.00; 代码规模接近度 0.61

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 只编译了一次 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/tty/driver.rs:L1-L3`：关键词命中
    代码片段：`/// tty/driver.rs — TTY 硬件后端管理 /// /// 管理 TTY 后端 (UART 等) 的注册和底层 I/O。`
  - `kernel/src/drivers/mod.rs:L2-L6`：关键词命中
    代码片段：`/// /// 子模块: ///   virtio_blk.rs — Virtio MMIO 块设备驱动 (QEMU virt 平台) pub mod virtio_blk;`
  - `os/src/console.rs:L74-L78`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`mod block; pub mod serial; pub use block::block_dev::BlockDevice;`
- 与 2025 只编译了一次 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod procfs; pub mod ramfs; mod vfs; // ─── 常量 ─────────────────────────────────────────────────`
  - `kernel/src/fs/vfs.rs:L1-L4`：关键词命中
    代码片段：`use super::{ devfs, ext4, procfs, ramfs, FsOps, FsType, GlobalFile, Inode, InodeType, MountEntry, MAX_FILES, MAX_INODES, MAX_MOUNTS, MOUNT_DEVFS, MOUNT_EXT4, MOUNT_PROCFS, MOUNT...`
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`mod fat32; mod file; pub(crate) mod inode; mod pipe; mod stdio;`
  - `os/src/fs/file.rs:L1-L3`：关键词命中
    代码片段：`use crate::fs::inode::{FatType, OSInode}; use crate::mm::UserBuffer; use alloc::string::String;`
- 与 2025 只编译了一次 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/hal/irq.rs:L4-L8`：关键词命中
    代码片段：`#[inline] pub fn disable() -> bool { crate::arch::riscv64::trap::interrupt::disable() }`
  - `kernel/src/hal/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 上下文抽象。 /// 用户态 trap 帧。`
  - `os/src/timer.rs:L72-L76`：关键词命中
    代码片段：`let current_ms = get_time_ms(); TIMERS.exclusive_session(|timers| { while let Some(timer) = timers.peek() { if timer.expire_ms <= current_ms { wakeup_task(Arc::clone(&timer.task));`
  - `os/src/hal/arch/riscv/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! 陷阱（Trap）处理模块。 //! //! 本模块负责处理所有 CPU 异常（Exceptions）和中断（Interrupts）。`
- 与 2025 只编译了一次 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/mm/fault.rs:L23-L27`：关键词命中
    代码片段：`let vpn = VirtPageNum::from(fault_addr / PAGE_SIZE); // 如果已经有映射, 可能是 COW 写保护, 否则属于权限异常。 // COW 页即使经过 mprotect 调整，也必须先私有化，不能让共享帧 // 重新以可写方式暴露给父子进程。`
  - `kernel/src/mm/frame.rs:L247-L251`：关键词命中
    代码片段：`} /// 查询某物理帧的引用计数（用于 mprotect COW 隔离） pub fn frame_ref_count(ppn: PhysPageNum) -> u16 { let (_, idx) = ppn_to_index(ppn);`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/address.rs:L1-L3`：关键词命中
    代码片段：`//! 虚拟/物理地址与页号模块（SV39） //! //! # Overview`
- 与 2025 只编译了一次 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/sched/fd.rs:L9-L13`：关键词命中
    代码片段：`use crate::hal::irq; use crate::sched::task::{FdKind, MAX_FDS}; // ─── FdCallbacks: fd 资源管理回调 ────────────────────────`
  - `kernel/src/sched/mod.rs:L4-L8`：关键词命中
    代码片段：`/// 子模块职责: /// - pid.rs     : PID 位图分配器 + PidHandle RAII 自动回收 /// - task.rs    : 进程控制块 (TCB) 定义 + 内核栈分配/释放 /// - manager.rs : 全局任务管理器 + 时间片轮转调度器 /// - exec.rs    : exec 路径 (ELF...`
  - `os/src/task/mod.rs:L13-L17`：关键词命中
    代码片段：`//! - 所有对 PCB/TCB 内部的可变访问通过 'UPIntrFreeCell' 或 'UPIntrRefMut' 独占访问，避免数据竞争 //! - 用户资源（ustack、trap_cx、tid）在主线程退出前被正确释放，防止内存泄漏 //! - 'schedule' 必须提供有效 'TaskContext' 指针，否则会导致上下文切换错误...`
  - `os/src/task/task.rs:L44-L48`：关键词命中
    代码片段：`use crate::mm::{MapPermission, PhysPageNum, VirtAddr}; use crate::sync::{UPIntrFreeCell, UPIntrRefMut}; use crate::task::context::TaskContext; use crate::task::process::ProcessC...`
- 与 2025 只编译了一次 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/klog.rs:L10-L14`：关键词命中
    代码片段：`///   3 = Debug  — 开发调试 ///   4 = Trace  — 热路径追踪 use core::sync::atomic::{AtomicU8, Ordering}; // ─── 日志级别常量 ────────────────────────────────────────`
  - `kernel/src/arch/riscv64/uart.rs:L10-L14`：关键词命中
    代码片段：`//! 注意: 使用了硬编码 QEMU UART 基址址, 本应由 DTB/SBI 提供 use core::sync::atomic::{AtomicUsize, Ordering}; /// UART 基地址 (QEMU virt 平台)`
  - `os/src/sync/mod.rs:L7-L11`：关键词命中
    代码片段：`//! //! 模块内部按功能拆分为多个子模块： //! - 'mutex'：互斥锁抽象及其具体实现（自旋 / 阻塞） //! - 'semaphore'：计数型信号量 //! - 'condvar'：条件变量`
  - `os/src/sync/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! # 互斥锁（Mutex）同步原语模块 //! //! ## Overview`
- 与 2025 只编译了一次 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/syscall/io.rs:L1-L4`：关键词命中
    代码片段：`use super::check::{validate_user_buffer, validate_user_buffer_write}; /// syscall/io.rs — I/O 类系统调用 /// /// 所有文件I/O通过VFS层分发:`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`/// syscall/ — 系统调用框架 /// /// 微内核用户态 ↔ 内核的唯一合法通道。`
  - `os/src/syscall/mod.rs:L41-L45`：关键词命中
    代码片段：`pub use process::*; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> isize { match syscall_id { //SYSCALL_OPEN => sys_openat(args[0] as *const u8, args[1] as u32),`
  - `os/src/hal/arch/riscv/sbi.rs:L5-L9`：关键词命中
    代码片段：`//! //! # Design //! - 所有 SBI 调用通过 'ecall' 指令触发陷入 S 模式执行。 //! - 'sbi_call' 函数是通用封装，将函数号和参数传递给 SBI。 //! - 上层函数（如 'set_timer'、'console_putchar'）直接调用 'sbi_call'，简化内核接口。`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2025 只编译了一次 在“内存管理”维度发现 4 个同名定义：indexes, from, page_offset, aligned。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/arch/riscv64/mmu/paging.rs:L30-L30`：fn indexes
    代码片段：`fn indexes(&self) -> [usize; MAX_PAGE_LEVELS] {`
  - `os/src/mm/address.rs:L209-L209`：fn indexes
    代码片段：`pub fn indexes<const T: usize>(&self) -> [usize; T] {`
  - `kernel/src/mm/address.rs:L34-L34`：fn from
    代码片段：`fn from(v: usize) -> Self { Self(v) }`
  - `os/src/mm/address.rs:L90-L90`：fn from
    代码片段：`fn from(v: usize) -> Self {`
- 与 2025 只编译了一次 在“系统调用”维度发现片段级代码相似度 0.65 （token=0.50, structure=0.92）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：args, fn, isize, let, usize。（置信度：medium）
  证据：
  - `kernel/src/syscall/io.rs:L17-L21`：fn sys_write
    代码片段：`/// sys_write(fd, buf, len) → 写入的字节数 / 负错误码 pub fn sys_write(args: [usize; 6]) -> isize { let fd = args[0]; let buf = args[1];`
  - `user/src/syscall.rs:L25-L29`：关键词命中
    代码片段：`#[cfg(feature = "riscv")] fn syscall(id: usize, args: [usize; 6]) -> isize { let mut ret: isize; unsafe {`
- 函数/符号名重合：与 2025 只编译了一次 在“文件系统”维度发现 1 个同名定义：resolve_path。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/fs/path.rs:L37-L37`：fn resolve_path
    代码片段：`pub fn resolve_path(cwd_inode: u16, path: &[u8]) -> Option<u16> {`
  - `os/src/fs/inode.rs:L376-L376`：fn resolve_path
    代码片段：`pub fn resolve_path(relative: &str, base: &str) -> String {`
- 结构体/类型重合：与 2025 只编译了一次 在“内存管理”维度发现 4 个同名定义：PhysAddr, VirtAddr, PhysPageNum, VirtPageNum。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/mm/address.rs:L17-L17`：struct PhysAddr
    代码片段：`pub struct PhysAddr(pub usize);         // 物理地址`
  - `os/src/mm/address.rs:L46-L46`：struct PhysAddr
    代码片段：`pub struct PhysAddr(pub usize);`
  - `kernel/src/mm/address.rs:L20-L20`：struct VirtAddr
    代码片段：`pub struct VirtAddr(pub usize);     // 虚拟地址`
  - `os/src/mm/address.rs:L51-L51`：struct VirtAddr
    代码片段：`pub struct VirtAddr(pub usize);`
- 结构体/类型重合：与 2025 只编译了一次 在“文件系统”维度发现 2 个同名定义：Stat, LinuxDirent64。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L126-L126`：struct Stat
    代码片段：`pub struct Stat {`
  - `os/src/fs/file.rs:L27-L27`：struct Stat
    代码片段：`pub struct Stat {`
  - `kernel/src/fs/mod.rs:L179-L179`：struct LinuxDirent64
    代码片段：`pub struct LinuxDirent64 {`
  - `os/src/fs/file.rs:L76-L76`：struct LinuxDirent64
    代码片段：`pub struct LinuxDirent64 {`
- 文件路径重合：与 2025 只编译了一次 在“设备驱动”维度出现同名文件源码路径 `kernel/src/hal/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/hal/console.rs:L4-L8`：关键词命中
    代码片段：`#[inline] pub fn putc(c: u8) { crate::arch::riscv64::uart::uart_putc(c); }`
  - `os/src/console.rs:L74-L78`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
- 文件路径重合：与 2025 只编译了一次 在“内存管理”维度出现同名文件源码路径 `kernel/src/mm/address.rs` / `os/src/mm/address.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/mm/address.rs:L10-L14`：关键词命中
    代码片段：`pub const PAGE_SIZE_BITS: usize = 12; // 页面大小的位数（2^12 = 4096） // 页表最大级数（RISC-V Sv39 支持 3 级页表） pub const MAX_PAGE_LEVELS: usize = 5; // 目前没有超过该级数的页表设计`
  - `os/src/mm/address.rs:L1-L3`：关键词命中
    代码片段：`//! 虚拟/物理地址与页号模块（SV39） //! //! # Overview`

## 相似点

- 与 2025 只编译了一次 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 只编译了一次 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/tty/driver.rs:L1-L3`：关键词命中
    代码片段：`/// tty/driver.rs — TTY 硬件后端管理 /// /// 管理 TTY 后端 (UART 等) 的注册和底层 I/O。`
  - `kernel/src/drivers/mod.rs:L2-L6`：关键词命中
    代码片段：`/// /// 子模块: ///   virtio_blk.rs — Virtio MMIO 块设备驱动 (QEMU virt 平台) pub mod virtio_blk;`
- 与 2025 只编译了一次 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod procfs; pub mod ramfs; mod vfs; // ─── 常量 ─────────────────────────────────────────────────`
  - `kernel/src/fs/vfs.rs:L1-L4`：关键词命中
    代码片段：`use super::{ devfs, ext4, procfs, ramfs, FsOps, FsType, GlobalFile, Inode, InodeType, MountEntry, MAX_FILES, MAX_INODES, MAX_MOUNTS, MOUNT_DEVFS, MOUNT_EXT4, MOUNT_PROCFS, MOUNT...`
- 与 2025 只编译了一次 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/hal/irq.rs:L4-L8`：关键词命中
    代码片段：`#[inline] pub fn disable() -> bool { crate::arch::riscv64::trap::interrupt::disable() }`
  - `kernel/src/hal/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 上下文抽象。 /// 用户态 trap 帧。`
- 与 2025 只编译了一次 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/mm/fault.rs:L23-L27`：关键词命中
    代码片段：`let vpn = VirtPageNum::from(fault_addr / PAGE_SIZE); // 如果已经有映射, 可能是 COW 写保护, 否则属于权限异常。 // COW 页即使经过 mprotect 调整，也必须先私有化，不能让共享帧 // 重新以可写方式暴露给父子进程。`
  - `kernel/src/mm/frame.rs:L247-L251`：关键词命中
    代码片段：`} /// 查询某物理帧的引用计数（用于 mprotect COW 隔离） pub fn frame_ref_count(ppn: PhysPageNum) -> u16 { let (_, idx) = ppn_to_index(ppn);`
- 与 2025 只编译了一次 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/sched/fd.rs:L9-L13`：关键词命中
    代码片段：`use crate::hal::irq; use crate::sched::task::{FdKind, MAX_FDS}; // ─── FdCallbacks: fd 资源管理回调 ────────────────────────`
  - `kernel/src/sched/mod.rs:L4-L8`：关键词命中
    代码片段：`/// 子模块职责: /// - pid.rs     : PID 位图分配器 + PidHandle RAII 自动回收 /// - task.rs    : 进程控制块 (TCB) 定义 + 内核栈分配/释放 /// - manager.rs : 全局任务管理器 + 时间片轮转调度器 /// - exec.rs    : exec 路径 (ELF...`
- 与 2025 只编译了一次 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/klog.rs:L10-L14`：关键词命中
    代码片段：`///   3 = Debug  — 开发调试 ///   4 = Trace  — 热路径追踪 use core::sync::atomic::{AtomicU8, Ordering}; // ─── 日志级别常量 ────────────────────────────────────────`
  - `kernel/src/arch/riscv64/uart.rs:L10-L14`：关键词命中
    代码片段：`//! 注意: 使用了硬编码 QEMU UART 基址址, 本应由 DTB/SBI 提供 use core::sync::atomic::{AtomicUsize, Ordering}; /// UART 基地址 (QEMU virt 平台)`
- 与 2025 只编译了一次 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/syscall/io.rs:L1-L4`：关键词命中
    代码片段：`use super::check::{validate_user_buffer, validate_user_buffer_write}; /// syscall/io.rs — I/O 类系统调用 /// /// 所有文件I/O通过VFS层分发:`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`/// syscall/ — 系统调用框架 /// /// 微内核用户态 ↔ 内核的唯一合法通道。`
- 与 2025 只编译了一次 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 只编译了一次 的语言构成不同：待测作品为 {'build': 508, 'markdown': 11117, 'toml': 37, 'rust': 25750, 'asm': 350, 'c': 22}，历史样本为 {'json': 20, 'build': 367, 'markdown': 4892, 'toml': 117, 'rust': 12330, 'text': 6, 'c': 1833, 'asm': 366, 'make': 116}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：21
- 含证据关键结论数：21（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
