# 2026 T2026105749910208 比较报告

- 对比历史仓库：2024 NPUcore-无所畏惧
- 生成时间：2026-07-02T05:02:45.025291+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 NPUcore-无所畏惧（来源：赛事历史作品）：画像相似度 score=8.76；同属 rcore-variant 风格; 语言构成相似度 0.57; OS 维度重合度 1.00; 代码规模接近度 0.61

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 NPUcore-无所畏惧 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console.rs:L23-L27`：关键词命中
    代码片段：`macro_rules! print { ($($arg:tt)*) => ({ $crate::console::_print(format_args!($($arg)*)); }); }`
  - `os/src/drivers/hal.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO 驱动所用的 ['virtio_drivers::Hal'] 实现。 //! //! LoongArch 在 PG=1 + DMW 模式下，裸物理地址不可直接解引用：`
  - `os/src/console.rs:L26-L30`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2024 NPUcore-无所畏惧 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/fd.rs:L12-L16`：关键词命中
    代码片段：`use crate::fs::block_dev; use crate::fs::ext4_vol; use crate::fs::vfs::VfsNodeKind; use crate::fs::{MemFileBacking, MemNodeMetadata}; use crate::task::wait_queue::WaitKey;`
  - `os/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod fd; pub mod vfat; pub mod vfs; #[allow(unused_imports)]`
  - `os/src/fs/mod.rs:L101-L105`：关键词命中
    代码片段：`} pub fn ls(&self) -> Result<(), isize> { let inode = self.file.get_dirtree_node(); let inode = match inode { Some(inode) => inode,`
  - `os/src/fs/cache.rs:L177-L181`：关键词命中
    代码片段：`/// Every time kernel tried to alloc this pagecache this number will increase 1(at most 1) /// Every time out of memory occurred this number will decrease 1(at least 0) /// When...`
- 与 2024 NPUcore-无所畏惧 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/timer.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use lazy_static::lazy_static; use polyhal::timer::current_time; use spin::Mutex;`
  - `os/src/trap/mod.rs:L2-L6`：关键词命中
    代码片段：`use lazy_static::lazy_static; use polyhal_trap::trap::TrapType; use polyhal_trap::trapframe::{TrapFrame, TrapFrameArgs}; use spin::Mutex;`
  - `os/src/timer.rs:L18-L22`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/arch/rv64/trap/mod.rs:L10-L14`：关键词命中
    代码片段：`}; pub use context::{MachineContext, TrapContext, UserContext}; pub type TrapImpl = riscv::register::scause::Trap; pub type InterruptImpl = riscv::register::scause::Interrupt; p...`
- 与 2024 NPUcore-无所畏惧 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod elf_loader; pub mod frame_allocator; pub mod heap_allocator; pub mod map_area;`
  - `os/src/syscall/mm.rs:L2-L6`：关键词命中
    代码片段：`use crate::config::{PAGE_SIZE, USER_HEAP_START, USER_STACK_TOP}; use crate::fs::{ext4_vol, fd::FileDescriptor}; use crate::mm::frame_allocator::{self, FrameTracker}; use crate::...`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `os/src/mm/map_area.rs:L1-L3`：关键词命中
    代码片段：`use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2024 NPUcore-无所畏惧 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`pub mod pid; pub mod processor; pub mod task; pub mod wait_queue;`
  - `os/src/task/task.rs:L10-L14`：关键词命中
    代码片段：`}; use crate::mm::memory_set::MemorySet; use crate::task::context::TaskContext; use crate::task::pid::Pid; use crate::utils::error::SysErrNo;`
  - `os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `os/src/task/task.rs:L378-L382`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 与 2024 NPUcore-无所畏惧 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/main.rs:L166-L170`：关键词命中
    代码片段：`// Guard against recursive calls use core::sync::atomic::{AtomicBool, Ordering}; static INIT_GUARD: AtomicBool = AtomicBool::new(false); if INIT_GUARD.swap(true, Ordering::SeqCs...`
  - `os/src/fs/fd.rs:L6-L10`：关键词命中
    代码片段：`use alloc::sync::Arc; use alloc::vec::Vec; use core::sync::atomic::{AtomicUsize, Ordering}; use lazy_static::lazy_static; use spin::Mutex;`
  - `os/src/fs/mod.rs:L29-L33`：关键词命中
    代码片段：`}; use lazy_static::*; use spin::Mutex; lazy_static! {`
  - `os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::drivers::BLOCK_DEVICE;`
- 与 2024 NPUcore-无所畏惧 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L872-L876`：关键词命中
    代码片段：`} Err(SysErrNo::EPIPE) if total == 0 => { crate::syscall::signal::send_sigpipe_to_current(); return Err(SysErrNo::EPIPE); }`
  - `os/src/syscall/mm.rs:L464-L468`：关键词命中
    代码片段：`) -> SyscallRet { log::info!( "[syscall] mmap(addr={:#x}, len={:#x}, prot={:#x}, flags={:#x}, fd={}, off={:#x})", addr, length,`
  - `os/src/syscall/fs.rs:L541-L545`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `os/src/syscall/mod.rs:L216-L220`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 NPUcore-无所畏惧 在“设备驱动”维度发现片段级代码相似度 0.84 （token=0.75, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：for, impl, struct, write。（置信度：high）
  证据：
  - `os/src/console.rs:L3-L7`：struct Stdout
    代码片段：`/// 串口输出结构体 struct Stdout; impl Write for Stdout {`
  - `os/src/console.rs:L7-L11`：struct KernelOutput
    代码片段：`// won't require lock, but unlikely to cause problem struct KernelOutput; impl Write for KernelOutput {`
- 与 2024 NPUcore-无所畏惧 在“设备驱动”维度发现片段级代码相似度 0.82 （token=0.72, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：c, fmt, fn, for, impl, in, mut, result。（置信度：high）
  证据：
  - `os/src/console.rs:L6-L10`：fn write_str
    代码片段：`impl Write for Stdout { fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.bytes() { DebugConsole::putchar(c);`
  - `os/src/console.rs:L10-L14`：fn write_str
    代码片段：`impl Write for KernelOutput { fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.chars() { console_putchar(c as usize);`
- 与 2024 NPUcore-无所畏惧 在“同步机制”维度发现片段级代码相似度 0.74 （token=0.60, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：alloc, arc, mutex, spin, sync, use, vec。（置信度：medium）
  证据：
  - `os/src/fs/fd.rs:L6-L10`：关键词命中
    代码片段：`use alloc::sync::Arc; use alloc::vec::Vec; use core::sync::atomic::{AtomicUsize, Ordering}; use lazy_static::lazy_static; use spin::Mutex;`
  - `os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::drivers::BLOCK_DEVICE;`
- 文件路径重合：与 2024 NPUcore-无所畏惧 在“设备驱动”维度出现同路径源码路径 `os/src/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/console.rs:L23-L27`：关键词命中
    代码片段：`macro_rules! print { ($($arg:tt)*) => ({ $crate::console::_print(format_args!($($arg)*)); }); }`
  - `os/src/console.rs:L26-L30`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
- 文件路径重合：与 2024 NPUcore-无所畏惧 在“中断与异常”维度出现同路径源码路径 `os/src/timer.rs` / `os/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/timer.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use lazy_static::lazy_static; use polyhal::timer::current_time; use spin::Mutex;`
  - `os/src/timer.rs:L18-L22`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
- 文件路径重合：与 2024 NPUcore-无所畏惧 在“内存管理”维度出现同路径源码路径 `os/src/mm/map_area.rs` / `os/src/mm/map_area.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/mm/map_area.rs:L1-L3`：关键词命中
    代码片段：`use super::frame_allocator::FrameTracker; use crate::mm::page_table::PTEFlags; use crate::{config::PAGE_SIZE, fs::fd::FileDescriptor};`
  - `os/src/mm/map_area.rs:L1-L3`：关键词命中
    代码片段：`use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 文件路径重合：与 2024 NPUcore-无所畏惧 在“调度与任务管理”维度出现同路径源码路径 `os/src/task/task.rs` / `os/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/task/task.rs:L10-L14`：关键词命中
    代码片段：`}; use crate::mm::memory_set::MemorySet; use crate::task::context::TaskContext; use crate::task::pid::Pid; use crate::utils::error::SysErrNo;`
  - `os/src/task/task.rs:L378-L382`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 文件路径重合：与 2024 NPUcore-无所畏惧 在“系统调用”维度出现同路径源码路径 `os/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/syscall/fs.rs:L872-L876`：关键词命中
    代码片段：`} Err(SysErrNo::EPIPE) if total == 0 => { crate::syscall::signal::send_sigpipe_to_current(); return Err(SysErrNo::EPIPE); }`
  - `os/src/syscall/fs.rs:L541-L545`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`

## 相似点

- 与 2024 NPUcore-无所畏惧 同属 rcore-variant 风格。（置信度：medium）
- 与 2024 NPUcore-无所畏惧 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console.rs:L23-L27`：关键词命中
    代码片段：`macro_rules! print { ($($arg:tt)*) => ({ $crate::console::_print(format_args!($($arg)*)); }); }`
  - `os/src/drivers/hal.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO 驱动所用的 ['virtio_drivers::Hal'] 实现。 //! //! LoongArch 在 PG=1 + DMW 模式下，裸物理地址不可直接解引用：`
- 与 2024 NPUcore-无所畏惧 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/fd.rs:L12-L16`：关键词命中
    代码片段：`use crate::fs::block_dev; use crate::fs::ext4_vol; use crate::fs::vfs::VfsNodeKind; use crate::fs::{MemFileBacking, MemNodeMetadata}; use crate::task::wait_queue::WaitKey;`
  - `os/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod fd; pub mod vfat; pub mod vfs; #[allow(unused_imports)]`
- 与 2024 NPUcore-无所畏惧 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/timer.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use lazy_static::lazy_static; use polyhal::timer::current_time; use spin::Mutex;`
  - `os/src/trap/mod.rs:L2-L6`：关键词命中
    代码片段：`use lazy_static::lazy_static; use polyhal_trap::trap::TrapType; use polyhal_trap::trapframe::{TrapFrame, TrapFrameArgs}; use spin::Mutex;`
- 与 2024 NPUcore-无所畏惧 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod elf_loader; pub mod frame_allocator; pub mod heap_allocator; pub mod map_area;`
  - `os/src/syscall/mm.rs:L2-L6`：关键词命中
    代码片段：`use crate::config::{PAGE_SIZE, USER_HEAP_START, USER_STACK_TOP}; use crate::fs::{ext4_vol, fd::FileDescriptor}; use crate::mm::frame_allocator::{self, FrameTracker}; use crate::...`
- 与 2024 NPUcore-无所畏惧 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`pub mod pid; pub mod processor; pub mod task; pub mod wait_queue;`
  - `os/src/task/task.rs:L10-L14`：关键词命中
    代码片段：`}; use crate::mm::memory_set::MemorySet; use crate::task::context::TaskContext; use crate::task::pid::Pid; use crate::utils::error::SysErrNo;`
- 与 2024 NPUcore-无所畏惧 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/main.rs:L166-L170`：关键词命中
    代码片段：`// Guard against recursive calls use core::sync::atomic::{AtomicBool, Ordering}; static INIT_GUARD: AtomicBool = AtomicBool::new(false); if INIT_GUARD.swap(true, Ordering::SeqCs...`
  - `os/src/fs/fd.rs:L6-L10`：关键词命中
    代码片段：`use alloc::sync::Arc; use alloc::vec::Vec; use core::sync::atomic::{AtomicUsize, Ordering}; use lazy_static::lazy_static; use spin::Mutex;`
- 与 2024 NPUcore-无所畏惧 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L872-L876`：关键词命中
    代码片段：`} Err(SysErrNo::EPIPE) if total == 0 => { crate::syscall::signal::send_sigpipe_to_current(); return Err(SysErrNo::EPIPE); }`
  - `os/src/syscall/mm.rs:L464-L468`：关键词命中
    代码片段：`) -> SyscallRet { log::info!( "[syscall] mmap(addr={:#x}, len={:#x}, prot={:#x}, flags={:#x}, fd={}, off={:#x})", addr, length,`
- 与 2024 NPUcore-无所畏惧 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 NPUcore-无所畏惧 的语言构成不同：待测作品为 {'build': 7812, 'markdown': 18581, 'yaml': 3458, 'rust': 467557, 'json': 282429, 'asm': 560, 'c': 239, 'toml': 125, 'text': 472}，历史样本为 {'json': 73, 'build': 8311, 'markdown': 9080, 'c': 171501, 'toml': 50, 'text': 1905, 'rust': 225425, 'asm': 242}。（置信度：medium）

## 可能创新点

- 待测作品包含 381 个历史样本 2024 NPUcore-无所畏惧 中未见的独特函数/类型定义（如 `cached_pblock_for_clean_read`, `probe_first_virtio_disk_from_dt`, `allocate`, `clean_page_cache_frame`, `clean_page_cached_read`, `set_owner_path`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `os/src/fs/ext4_vol.rs:L873-L873`：fn cached_pblock_for_clean_read
    代码片段：`fn cached_pblock_for_clean_read(fs: &Ext4, ino: u32, lblock: u32, max_run_len: u32) -> Option<u64> {`
  - `os/src/drivers/virtio_mmio_blk.rs:L118-L118`：fn probe_first_virtio_disk_from_dt
    代码片段：`pub unsafe fn probe_first_virtio_disk_from_dt(dtb_ptr: usize) -> Option<Arc<dyn RawBlockDevice>> {`

## 附录：核验摘要

- 关键结论数：23
- 含证据关键结论数：23（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
