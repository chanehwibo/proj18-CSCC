# 2026 T2026105749911365 比较报告

- 对比历史仓库：2024 刀片超车
- 生成时间：2026-07-02T05:02:49.840927+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 刀片超车（来源：赛事历史作品）：画像相似度 score=8.91；同属 ucore-variant 风格; 语言构成相似度 0.47; OS 维度重合度 1.00; 代码规模接近度 0.98

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 刀片超车 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 与 2024 刀片超车 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs.h:L6-L10`：关键词命中
    代码片段：`// Disk layout: // [ boot block | super block | log | inode blocks | //                                          free bit map | data blocks] //`
  - `kernel/file.c:L216-L220`：关键词命中
    代码片段：`// Read from file f. // addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `kernel/include/file.h:L68-L72`：关键词命中
    代码片段：`struct file*    filedup(struct file*); void            fileinit(void); int             fileread(struct file*, uint64, int n); int             filestat(struct file*, uint64 addr)...`
- 与 2024 刀片超车 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L31-L35`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/trap.c:L1-L3`：关键词命中
    代码片段：`#include "include/trap.h" #include "include/console.h" #include "include/disk.h"`
  - `kernel/timer.c:L1-L3`：关键词命中
    代码片段：`// Timer Interrupt handler #include <string.h> #include "include/timer.h"`
- 与 2024 刀片超车 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/vm.c:L10-L14`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/kalloc.c:L36-L40`：关键词命中
    代码片段：`char *p; p = (char *)PGROUNDUP((uint64)pa_start); for (; p + PGSIZE <= (char *)pa_end; p += PGSIZE) kfree(p); }`
  - `kernel/vm.c:L1-L5`：关键词命中
    代码片段：`#include "include/vm.h" #include "include/elf.h" #include "include/kalloc.h" #include "include/memlayout.h" #include "include/param.h"`
  - `kernel/kalloc.c:L3-L7`：关键词命中
    代码片段：`// and pipe buffers. Allocates whole 4096-byte pages. #include "include/kalloc.h" #include "include/memlayout.h" #include "include/param.h"`
- 与 2024 刀片超车 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "riscv.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "stat.h"`
  - `kernel/proc.h:L21-L25`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;      // The process running on this cpu, or null. struct context context; // swtch() here to enter scheduler(). int noff;...`
  - `kernel/proc.c:L1-L3`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/fat32.h" #include "include/file.h"`
  - `kernel/include/proc.h:L8-L12`：关键词命中
    代码片段：`#include "file.h" #include "fat32.h" // #include "thread.h" #include "trap.h" #include "vma.h"`
- 与 2024 刀片超车 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs.c:L15-L19`：关键词命中
    代码片段：`#include "param.h" #include "stat.h" #include "spinlock.h" #include "proc.h" #include "sleeplock.h"`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "riscv.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "fs.h"`
  - `kernel/bio.c:L21-L25`：关键词命中
    代码片段：`// #include "include/sdcard.h" #include "include/sleeplock.h" #include "include/spinlock.h" #include "include/types.h"`
  - `kernel/exec.c:L10-L14`：关键词命中
    代码片段：`#include "include/loongarch.h" #include "include/sleeplock.h" #include "include/spinlock.h" #include "include/string.h" #include "include/types.h"`
- 与 2024 刀片超车 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap.c:L58-L62`：关键词命中
    代码片段：`kexit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h"`
  - `kernel/syscall.c:L1-L3`：关键词命中
    代码片段：`#include "include/syscall.h" #include "include/kalloc.h" #include "include/memlayout.h"`
  - `kernel/include/syscall.h:L10-L14`：关键词命中
    代码片段：`int argaddr(uint64 n, uint64 *ip); int argstr(uint64 n, char *buf, int max); void syscall(void); #endif`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 刀片超车 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：addr, f, file, fileread, int, n, r, struct。（置信度：high）
  证据：
  - `kernel/file.c:L112-L116`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `kernel/file.c:L216-L220`：关键词命中
    代码片段：`// Read from file f. // addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
- 函数/符号名重合：与 2024 刀片超车 在“设备驱动”维度发现 4 个同名定义：virtio_disk_init, virtio_disk_intr, virtio_disk_rw, alloc_desc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/virtio_disk.c:L22-L22`：fn virtio_disk_init
    代码片段：`void virtio_disk_init(void) {}`
  - `kernel/virtio_disk.c:L53-L53`：fn virtio_disk_init
    代码片段：`void virtio_disk_init(void) {`
  - `kernel/virtio_disk.c:L23-L23`：fn virtio_disk_intr
    代码片段：`void virtio_disk_intr() {}`
  - `kernel/virtio_disk.c:L245-L245`：fn virtio_disk_intr
    代码片段：`void virtio_disk_intr() {`
- 函数/符号名重合：与 2024 刀片超车 在“文件系统”维度发现 3 个同名定义：sys_mkdirat, sys_unlinkat, sys_statx。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/sysfile.c:L748-L748`：fn sys_mkdirat
    代码片段：`uint64 sys_mkdirat(void) {`
  - `kernel/sysfile.c:L102-L102`：fn sys_mkdirat
    代码片段：`uint64 sys_mkdirat(void) {`
  - `kernel/sysfile.c:L763-L763`：fn sys_unlinkat
    代码片段：`uint64 sys_unlinkat(void) {`
  - `kernel/sysfile.c:L660-L660`：fn sys_unlinkat
    代码片段：`uint64 sys_unlinkat(void) {`
- 函数/符号名重合：与 2024 刀片超车 在“调度与任务管理”维度发现 4 个同名定义：swtch, sys_brk, sys_exit_group, sys_mprotect。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/kernel.asm:L5405-L5405`：fn swtch
    代码片段：`swtch:`
  - `kernel/swtch.S:L8-L8`：fn swtch
    代码片段：`swtch:`
  - `kernel/sysproc.c:L131-L131`：fn sys_brk
    代码片段：`uint64 sys_brk(void) {`
  - `kernel/sysproc.c:L338-L338`：fn sys_brk
    代码片段：`uint64 sys_brk(void) {`
- 函数/符号名重合：与 2024 刀片超车 在“系统调用”维度发现 4 个同名定义：sys_mkdirat, sys_unlinkat, sys_nanosleep, sys_statx。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/sysfile.c:L748-L748`：fn sys_mkdirat
    代码片段：`uint64 sys_mkdirat(void) {`
  - `kernel/sysfile.c:L102-L102`：fn sys_mkdirat
    代码片段：`uint64 sys_mkdirat(void) {`
  - `kernel/sysfile.c:L763-L763`：fn sys_unlinkat
    代码片段：`uint64 sys_unlinkat(void) {`
  - `kernel/sysfile.c:L660-L660`：fn sys_unlinkat
    代码片段：`uint64 sys_unlinkat(void) {`
- 函数/符号名重合：与 2024 刀片超车 在“内存管理”维度发现 2 个同名定义：alloc_desc, alloc3_desc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/virtio_disk.c:L164-L164`：fn alloc_desc
    代码片段：`static int alloc_desc() {`
  - `kernel/virtio_disk.c:L120-L120`：fn alloc_desc
    代码片段：`static int alloc_desc() {`
  - `kernel/virtio_disk.c:L192-L192`：fn alloc3_desc
    代码片段：`static int alloc3_desc(int *idx) {`
  - `kernel/virtio_disk.c:L152-L152`：fn alloc3_desc
    代码片段：`static int alloc3_desc(int *idx) {`
- 宏名重合：与 2024 刀片超车 在“设备驱动”维度发现 1 个同名定义：BACKSPACE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/console.c:L25-L25`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100       // erase the last output character`
  - `kernel/console.c:L25-L25`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100`
- 宏名重合：与 2024 刀片超车 在“文件系统”维度发现 2 个同名定义：min, MAXFILE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/fs.c:L24-L24`：macro min
    代码片段：`#define min(a, b) ((a) < (b) ? (a) : (b))`
  - `kernel/file.c:L586-L586`：macro MIN
    代码片段：`#define MIN(a, b) ((a) < (b) ? (a) : (b))`
  - `kernel/fs.h:L129-L129`：macro MAXFILE
    代码片段：`#define MAXFILE   (NDIRECT + NINDIRECT)`
  - `user/usertests.c:L20-L20`：macro MAXFILE
    代码片段：`#define MAXFILE 512`
- 宏名重合：与 2024 刀片超车 在“同步机制”维度发现 1 个同名定义：min。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/fs.c:L24-L24`：macro min
    代码片段：`#define min(a, b) ((a) < (b) ? (a) : (b))`
  - `kernel/exec.c:L18-L18`：macro min
    代码片段：`#define min(x, y) ((x) < (y) ? (x) : (y))`
- 文件路径重合：与 2024 刀片超车 在“设备驱动”维度出现同路径源码路径 `kernel/uart.c` / `kernel/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
- 文件路径重合：与 2024 刀片超车 在“设备驱动”维度出现同路径源码路径 `kernel/console.c` / `kernel/console.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 文件路径重合：与 2024 刀片超车 在“文件系统”维度出现同路径源码路径 `kernel/file.c` / `kernel/file.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/file.c:L112-L116`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `kernel/file.c:L216-L220`：关键词命中
    代码片段：`// Read from file f. // addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
- 文件路径重合：与 2024 刀片超车 在“中断与异常”维度出现同路径源码路径 `kernel/trap.c` / `kernel/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/trap.c:L31-L35`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/trap.c:L1-L3`：关键词命中
    代码片段：`#include "include/trap.h" #include "include/console.h" #include "include/disk.h"`
- 文件路径重合：与 2024 刀片超车 在“内存管理”维度出现同路径源码路径 `kernel/vm.c` / `kernel/vm.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/vm.c:L10-L14`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/vm.c:L1-L5`：关键词命中
    代码片段：`#include "include/vm.h" #include "include/elf.h" #include "include/kalloc.h" #include "include/memlayout.h" #include "include/param.h"`
- 文件路径重合：与 2024 刀片超车 在“内存管理”维度出现同路径源码路径 `kernel/kalloc.c` / `kernel/kalloc.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/kalloc.c:L36-L40`：关键词命中
    代码片段：`char *p; p = (char *)PGROUNDUP((uint64)pa_start); for (; p + PGSIZE <= (char *)pa_end; p += PGSIZE) kfree(p); }`
  - `kernel/kalloc.c:L3-L7`：关键词命中
    代码片段：`// and pipe buffers. Allocates whole 4096-byte pages. #include "include/kalloc.h" #include "include/memlayout.h" #include "include/param.h"`
- 结构体/类型重合：与 2024 刀片超车 在“内存管理”维度发现 1 个同名定义：run。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/kalloc.c:L17-L17`：struct run
    代码片段：`struct run {`
  - `kernel/kalloc.c:L22-L22`：struct run
    代码片段：`struct run {`

## 相似点

- 与 2024 刀片超车 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 刀片超车 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
- 与 2024 刀片超车 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs.h:L6-L10`：关键词命中
    代码片段：`// Disk layout: // [ boot block | super block | log | inode blocks | //                                          free bit map | data blocks] //`
- 与 2024 刀片超车 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L31-L35`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
- 与 2024 刀片超车 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/vm.c:L10-L14`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/kalloc.c:L36-L40`：关键词命中
    代码片段：`char *p; p = (char *)PGROUNDUP((uint64)pa_start); for (; p + PGSIZE <= (char *)pa_end; p += PGSIZE) kfree(p); }`
- 与 2024 刀片超车 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "riscv.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "stat.h"`
  - `kernel/proc.h:L21-L25`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;      // The process running on this cpu, or null. struct context context; // swtch() here to enter scheduler(). int noff;...`
- 与 2024 刀片超车 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs.c:L15-L19`：关键词命中
    代码片段：`#include "param.h" #include "stat.h" #include "spinlock.h" #include "proc.h" #include "sleeplock.h"`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "riscv.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "fs.h"`
- 与 2024 刀片超车 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap.c:L58-L62`：关键词命中
    代码片段：`kexit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h"`
- 与 2024 刀片超车 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 刀片超车 的语言构成不同：待测作品为 {'build': 271, 'markdown': 430, 'c': 8495, 'asm': 16066}，历史样本为 {'json': 20, 'build': 233, 'text': 210, 'markdown': 1048, 'c': 20644, 'asm': 2563}。（置信度：medium）

## 可能创新点

- 待测作品包含 40 个历史样本 2024 刀片超车 中未见的独特函数/类型定义（如 `run_test_script`, `procstate`, `run_test_dir`, `le16_store`, `le16_to_host`, `run_one_test`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/contest.c:L161-L161`：fn run_test_script
    代码片段：`static void run_test_script(char *script_path, char *busybox_path, char *group_name) {`
  - `kernel/proc.h:L79-L79`：enum procstate
    代码片段：`enum procstate { UNUSED, USED, SLEEPING, RUNNABLE, RUNNING, ZOMBIE };`

## 附录：核验摘要

- 关键结论数：31
- 含证据关键结论数：31（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
