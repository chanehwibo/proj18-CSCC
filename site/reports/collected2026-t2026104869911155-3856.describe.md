# 2026 T2026104869911155 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026104869911155-3856`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：73
- 代码/文本行数：38931
- 主要语言：rust 30521 LOC, markdown 6994 LOC, c 1176 LOC, asm 192 LOC, build 48 LOC

## 总览

2026 T2026104869911155 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 30521 LOC, markdown: 6994 LOC, c: 1176 LOC, asm: 192 LOC。仓库包含 73 个已扫描文件、约 38931 行可分析文本，当前抽取到 1777 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 3 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, Makefile。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "osoldierboy" version = "0.1.0"`
  - `Makefile:L1-L3`：构建入口
    代码片段：`RUST_TOOLCHAIN ?= nightly-2025-05-20 RV_TARGET := riscv64gc-unknown-none-elf LA_TARGET := loongarch64-unknown-none-softfloat`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct UserTask, enum SyscallResult, enum ExitStatus, fn log_status, struct CloneTransition。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/task/mod.rs` | L171-L175 | 关键词命中 |
| `src/task/riscv64.rs` | L13-L17 | 关键词命中 |
| `src/fs/ext4.rs` | L47-L51 | 关键词命中 |
| `src/task/mod.rs` | L64-L68 | struct UserTask |
| `src/task/mod.rs` | L128-L132 | enum SyscallResult |
| `src/task/mod.rs` | L177-L181 | enum ExitStatus |

### 关键代码片段

  - `src/task/mod.rs:L171-L175`：关键词命中
    代码片段：`FutexRequeue(FutexRequeueRequest), Sleep(SleepRequest), Yield, Exit(isize), ExitGroup(isize),`
  - `src/task/riscv64.rs:L13-L17`：关键词命中
    代码片段：`mm::user::LoadedElf, println, task::{ AcceptWaitRequest, DatagramReadWaitRequest, DatagramWriteWaitRequest, EpollWaitRequest, ExitStatus, FanotifyPermissionRequest, FutexRequeue...`
  - `src/fs/ext4.rs:L47-L51`：关键词命中
    代码片段：`"waitpid", "write", "yield", ];`
  - `src/task/mod.rs:L64-L68`：struct UserTask
    代码片段：`pub mod riscv64; pub struct UserTask { pid: usize, visible_pid: usize,`

### 相关符号

`struct UserTask` at `src/task/mod.rs:L64`、`enum SyscallResult` at `src/task/mod.rs:L128`、`enum ExitStatus` at `src/task/mod.rs:L177`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn zero_bss, fn init, fn symbol_addr, struct KernelHeap, struct FreeBlock。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/mm/user.rs` | L53-L57 | 关键词命中 |
| `src/mm/frame.rs` | L6-L10 | 关键词命中 |
| `src/mm/paging.rs` | L8-L12 | 关键词命中 |
| `src/mm/mod.rs` | L15-L19 | fn zero_bss |
| `src/mm/mod.rs` | L25-L29 | fn init |
| `src/mm/mod.rs` | L54-L58 | fn symbol_addr |

### 关键代码片段

  - `src/mm/user.rs:L53-L57`：关键词命中
    代码片段：`struct UserAddressSpaceInner { page_table: Sv39PageTable, pages: Vec<UserPage>, reservations: Vec<Reservation>,`
  - `src/mm/frame.rs:L6-L10`：关键词命中
    代码片段：`}; static FRAME_ALLOCATOR: FrameAllocator = FrameAllocator::new(); const MAX_FREE_FRAMES: usize = 32768; static mut FREE_FRAMES: [usize; MAX_FREE_FRAMES] = [0; MAX_FREE_FRAMES];`
  - `src/mm/paging.rs:L8-L12`：关键词命中
    代码片段：`const MAX_PAGE_TABLE_FRAMES: usize = 128; pub const LARGE_PAGE_SIZE: usize = PAGE_SIZE * ENTRY_COUNT; const PTE_V: u64 = 1 << 0; const PTE_R: u64 = 1 << 1; const PTE_W: u64 = 1...`
  - `src/mm/mod.rs:L15-L19`：fn zero_bss
    代码片段：`} pub unsafe fn zero_bss() { let mut current = core::ptr::addr_of_mut!(__bss_start); let end = core::ptr::addr_of_mut!(__bss_end);`

### 相关符号

`fn zero_bss` at `src/mm/mod.rs:L15`、`fn init` at `src/mm/mod.rs:L25`、`fn symbol_addr` at `src/mm/mod.rs:L54`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn handle_syscall, fn sys_clone, fn sys_clone3, fn sys_pidfd_send_signal, fn sys_pidfd_open。（置信度：medium）
  - 静态识别到 205 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/mm/user.rs` | L779-L783 | 关键词命中 |
| `src/task/mod.rs` | L1220-L1224 | 关键词命中 |
| `src/task/riscv64.rs` | L145-L149 | 关键词命中 |
| `src/task/mod.rs` | L1220-L1224 | fn handle_syscall |
| `src/task/mod.rs` | L1544-L1548 | fn sys_clone |
| `src/task/mod.rs` | L1594-L1598 | fn sys_clone3 |

### 关键代码片段

  - `src/mm/user.rs:L779-L783`：关键词命中
    代码片段：`const TRAMPOLINE: [u8; 8] = [ 0x93, 0x08, 0xb0, 0x08, // li a7, 139 0x73, 0x00, 0x00, 0x00, // ecall ];`
  - `src/task/mod.rs:L1220-L1224`：关键词命中
    代码片段：`} pub fn handle_syscall(&mut self, number: usize, args: [usize; 6]) -> SyscallResult { if number == 221 { return self.sys_execve(args[0], args[1], args[2]);`
  - `src/task/riscv64.rs:L145-L149`：关键词命中
    代码片段：`let scheduler = unsafe { scheduler_mut() }; scheduler.save_current_frame(*frame); let satp = scheduler.handle_syscall(frame); if scheduler.is_done() { if scheduler.start_next_ro...`
  - `src/task/mod.rs:L1220-L1224`：fn handle_syscall
    代码片段：`} pub fn handle_syscall(&mut self, number: usize, args: [usize; 6]) -> SyscallResult { if number == 221 { return self.sys_execve(args[0], args[1], args[2]);`

### 相关符号

`fn handle_syscall` at `src/task/mod.rs:L1220`、`fn sys_clone` at `src/task/mod.rs:L1544`、`fn sys_clone3` at `src/task/mod.rs:L1594`、`enum SyscallResult` at `src/task/mod.rs:L128`、`fn handle_syscall` at `src/task/mod.rs:L1220`、`fn handle_syscall_return` at `src/task/mod.rs:L1357`、`fn sys_clone` at `src/task/mod.rs:L1544`、`fn sys_clone3` at `src/task/mod.rs:L1594`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct ScriptProgram, struct ScriptCandidate, fn candidate_path, fn script_cwd, fn smoke_test。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/fs/ext4.rs` | L251-L255 | 关键词命中 |
| `src/fs/file.rs` | L1319-L1323 | 关键词命中 |
| `src/task/mod.rs` | L1965-L1969 | 关键词命中 |
| `src/fs/ext4.rs` | L200-L204 | struct ScriptProgram |
| `src/fs/ext4.rs` | L205-L209 | struct ScriptCandidate |
| `src/fs/ext4.rs` | L231-L235 | fn candidate_path |

### 关键代码片段

  - `src/fs/ext4.rs:L251-L255`：关键词命中
    代码片段：`println!( "ext4: magic=0x{:04x} block_size={} inode_size={} blocks={} inodes={}", fs.superblock.magic, fs.superblock.block_size(), fs.superblock.inode_size,`
  - `src/fs/file.rs:L1319-L1323`：关键词命中
    代码片段：`)); } if let Some((inode, bytes)) = self.block_device_sysfs_bytes(&path) { return Ok(OpenFile::new( path,`
  - `src/task/mod.rs:L1965-L1969`：关键词命中
    代码片段：`pid, metadata.file_type == FileType::Directory, metadata.inode, ); }`
  - `src/fs/ext4.rs:L200-L204`：struct ScriptProgram
    代码片段：`#[derive(Clone)] pub struct ScriptProgram { pub script: String, pub busybox: String,`

### 相关符号

`struct ScriptProgram` at `src/fs/ext4.rs:L200`、`struct ScriptCandidate` at `src/fs/ext4.rs:L205`、`fn candidate_path` at `src/fs/ext4.rs:L231`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn with_lock_owner, fn new, fn lock, fn lock, fn write_user_flock。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/fs/file.rs` | L6-L10 | 关键词命中 |
| `src/mm/heap.rs` | L2-L6 | 关键词命中 |
| `src/mm/frame.rs` | L1-L3 | 关键词命中 |
| `src/fs/file.rs` | L3576-L3580 | fn with_lock_owner |
| `src/fs/file.rs` | L9888-L9892 | fn new |
| `src/mm/heap.rs` | L85-L89 | fn lock |

### 关键代码片段

  - `src/fs/file.rs:L6-L10`：关键词命中
    代码片段：`}; use core::cell::RefCell; use core::sync::atomic::{AtomicI32, Ordering}; use crate::{`
  - `src/mm/heap.rs:L2-L6`：关键词命中
    代码片段：`use core::alloc::{GlobalAlloc, Layout}; use core::ptr::null_mut; use core::sync::atomic::{AtomicBool, AtomicUsize, Ordering}; use crate::{mm::align_up, println};`
  - `src/mm/frame.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicBool, AtomicUsize, Ordering}; use crate::{`
  - `src/fs/file.rs:L3576-L3580`：fn with_lock_owner
    代码片段：`} fn with_lock_owner(mut lock: FileLock, owner: FileLockOwner) -> FileLock { lock.pid = lock_owner_id(owner); lock`

### 相关符号

`fn with_lock_owner` at `src/fs/file.rs:L3576`、`fn new` at `src/fs/file.rs:L9888`、`fn lock` at `src/mm/heap.rs:L85`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn encode_itimer。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/task/mod.rs` | L9129-L9133 | 关键词命中 |
| `src/task/riscv64.rs` | L193-L197 | 关键词命中 |
| `src/task/mod.rs` | L9587-L9591 | fn encode_itimer |

### 关键代码片段

  - `src/task/mod.rs:L9129-L9133`：关键词命中
    代码片段：`Ok(()) => 0, Err(error) => { println!("timer_create: copy timer id failed: {}", error.as_str()); -14 }`
  - `src/task/riscv64.rs:L193-L197`：关键词命中
    代码片段：`Err(error) => { println!( "trap: page fault handling failed scause={} sepc=0x{:x} stval=0x{:x}: {}", scause, frame.sepc,`
  - `src/task/mod.rs:L9587-L9591`：fn encode_itimer
    代码片段：`} fn encode_itimer(&self, timer: RealTimer, now_us: u64) -> [u8; 32] { let mut raw = [0u8; 32]; write_timeval(&mut raw, 0, timer.interval_us);`

### 相关符号

`fn encode_itimer` at `src/task/mod.rs:L9587`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：trait BlockDevice, fn read_sector, fn capacity_sectors, enum Error, fn smoke_test。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/drivers/block.rs` | L2-L6 | 关键词命中 |
| `src/drivers/block/virtio_mmio.rs` | L12-L16 | 关键词命中 |
| `src/mm/user.rs` | L705-L709 | 关键词命中 |
| `src/drivers/block.rs` | L6-L10 | trait BlockDevice |
| `src/drivers/block.rs` | L7-L11 | fn read_sector |
| `src/drivers/block.rs` | L8-L12 | fn capacity_sectors |

### 关键代码片段

  - `src/drivers/block.rs:L2-L6`：关键词命中
    代码片段：`#[cfg(target_arch = "riscv64")] mod virtio_mmio; pub const SECTOR_SIZE: usize = 512;`
  - `src/drivers/block/virtio_mmio.rs:L12-L16`：关键词命中
    代码片段：`}; const VIRTIO_MAGIC: u32 = 0x7472_6976; const VIRTIO_DEVICE_BLOCK: u32 = 2; const VIRTIO_VERSION_1_HIGH_BIT: u32 = 1;`
  - `src/mm/user.rs:L705-L709`：关键词命中
    代码片段：`fn map_kernel_identity(&mut self) -> Result<(), Error> { const UART_BASE: usize = 0x1000_0000; const VIRTIO_BASE: usize = 0x1000_1000; const VIRTIO_END: usize = 0x1000_9000; con...`
  - `src/drivers/block.rs:L6-L10`：trait BlockDevice
    代码片段：`pub const SECTOR_SIZE: usize = 512; pub trait BlockDevice { fn read_sector(&mut self, sector: u64, buffer: &mut [u8; SECTOR_SIZE]) -> Result<(), Error>; fn capacity_sectors(&sel...`

### 相关符号

`trait BlockDevice` at `src/drivers/block.rs:L6`、`fn read_sector` at `src/drivers/block.rs:L7`、`fn capacity_sectors` at `src/drivers/block.rs:L8`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
