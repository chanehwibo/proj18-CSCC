# 2026 T2026104869910278 比较报告

- 对比历史仓库：2022 caporustos
- 生成时间：2026-07-01T21:03:28.936386+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 caporustos（来源：赛事历史作品）：画像相似度 score=8.89；同属 rcore-variant 风格; 语言构成相似度 0.60; OS 维度重合度 1.00; 代码规模接近度 0.70

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 caporustos 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `r-core/os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console output for QEMU virt (SBI on RISC-V, UART on LoongArch). #[cfg(target_arch = "loongarch64")]`
  - `r-core/os/src/la_console.rs:L1-L3`：关键词命中
    代码片段：`//! LoongArch QEMU virt UART (16550). use crate::config::UART_BASE;`
  - `codes/os/src/console.rs:L21-L25`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
  - `codes/os/src/drivers/block/mod.rs:L4-L8`：关键词命中
    代码片段：`mod sleep; mod spi; mod virtio_blk; mod virtio_fsimg;`
- 与 2022 caporustos 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `r-core/os/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`//! File trait & inode(dir, file, pipe, stdin, stdout) mod file_cap;`
  - `r-core/os/src/fs/proc.rs:L23-L27`：关键词命中
    代码片段：`struct ProcEntry { name: String, inode: u64, entry_type: ProcEntryType, }`
  - `codes/os/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`mod pipe; pub mod stdio; mod inode; mod mount; pub mod finfo;`
  - `codes/os/src/fs/inode.rs:L36-L40`：关键词命中
    代码片段：`} // 此inode实际被当作文件 pub struct OSInode { readable: bool,`
- 与 2022 caporustos 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `r-core/os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! Timer-related functionality use core::cmp::Ordering;`
  - `r-core/os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality //! //! For rCore, we have a single trap entry point, namely '__alltraps'. At`
  - `codes/os/src/trap/mod.rs:L6-L10`：关键词命中
    代码片段：`scause::{ self, Trap, Exception, Interrupt,`
  - `codes/os/src/sbi.rs:L30-L34`：关键词命中
    代码片段：`} pub fn set_timer(timer: usize) { sbi_call(SBI_SET_TIMER, timer, 0, 0); }`
- 与 2022 caporustos 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `r-core/os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `r-core/os/src/mm/tlb.rs:L1-L5`：关键词命中
    代码片段：`//! LoongArch64 hardware TLB refill ('TLBRENTRY') and page-walk CSR setup. use super::{PageTable, VirtAddr, VirtPageNum}; use core::arch::{asm, global_asm};`
  - `codes/os/src/mm/mod.rs:L1-L3`：关键词命中
    代码片段：`mod heap_allocator; mod address; mod frame_allocator;`
  - `codes/os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{PageTable, PageTableEntry, PTEFlags}; use super::{VirtPageNum, VirtAddr, PhysPageNum, PhysAddr}; use super::{FrameTracker, frame_alloc, frame_add_ref, enquire_refcou...`
- 与 2022 caporustos 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `r-core/os/src/fs/proc.rs:L1-L3`：关键词命中
    代码片段：`//! /proc 伪文件系统 //! //! 轻量级 /proc 实现，在 syscall 层面拦截路径，不依赖磁盘数据。`
  - `r-core/os/src/task/id.rs:L1-L3`：关键词命中
    代码片段：`//! Allocator for pid, task user resource, kernel stack using a simple recycle strategy. use super::ProcessControlBlock;`
  - `codes/os/src/process/thread/mod.rs:L2-L5`：关键词命中
    代码片段：`pub(in crate::process) mod adt; pub(in crate::process) mod context; pub(in crate::process) mod scheduler; pub(in crate::process) mod manager;`
  - `codes/os/src/process/thread/core/tcb.rs:L4-L8`：关键词命中
    代码片段：`use crate::{process::{ process::core::pcb::ProcessControlBlock, thread::{adt::{tid::TidHandle, kernel_stack::KernelStack}, scheduler::info::SchedulePolicy}, thread::context::con...`
- 与 2022 caporustos 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `r-core/os/src/sync/up.rs:L36-L40`：关键词命中
    代码片段：`error!( "last_syscall_id={}", crate::syscall::LAST_SYSCALL_ID.load(core::sync::atomic::Ordering::Relaxed) ); panic!(`
  - `r-core/os/src/sync/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization and interior mutability primitives mod condvar; mod mutex; mod semaphore;`
  - `codes/os/src/main.rs:L96-L100`：关键词命中
    代码片段：`lazy_static! { static ref CORE2_FLAG: Arc<Mutex<Core2flag>> = Arc::new(Mutex::new( Core2flag{ is_in:false,`
  - `codes/os/src/fs/pipe.rs:L1-L5`：关键词命中
    代码片段：`use super::File; use alloc::sync::{Arc, Weak}; use spin::Mutex; use crate::mm::UserBuffer; use crate::process::suspend_current_and_run_next;`
- 与 2022 caporustos 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `r-core/os/src/syscall/fs.rs:L10-L14`：关键词命中
    代码片段：`KLOG, RLIMIT_NOFILE, S_ISGID, check_parent_setgid, block_current_and_run_next, current_process, current_user_token }; use crate::syscall::{analyse_path, TimeSpec, REALTIME_OFFSE...`
  - `r-core/os/src/syscall/mq.rs:L1-L3`：关键词命中
    代码片段：`//! POSIX 消息队列（mqueue）及 System V 消息队列 syscall 实现 pub use crate::fs::mqueue::MqAttr;`
  - `codes/os/src/syscall/fs.rs:L903-L907`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: isize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize{ if dirfd == AT...`
  - `codes/os/src/syscall/mod.rs:L79-L83`：关键词命中
    代码片段：`use num_derive::FromPrimitive; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> isize { gdb_print!(SYSCALL_ENABLE,"syscall-({}) arg0 = {}, arg1 = {}\n",syscall_id, args[0]...`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2022 caporustos 在“中断与异常”维度发现片段级代码相似度 0.88 （token=0.81, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clock_freq, fn, get_time, msec_per_sec, pub, usize。（置信度：high）
  证据：
  - `r-core/os/src/timer.rs:L40-L44`：fn get_time_ms
    代码片段：`/// Get the current time in milliseconds pub fn get_time_ms() -> usize { get_time() * MSEC_PER_SEC / CLOCK_FREQ }`
  - `codes/os/src/timer.rs:L31-L35`：fn get_time_ns
    代码片段：`} pub fn get_time_ns() -> usize { (get_time() / (CLOCK_FREQ / USEC_PER_SEC)) * MSEC_PER_SEC }`
- 与 2022 caporustos 在“中断与异常”维度发现片段级代码相似度 0.81 （token=0.71, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clock_freq, fn, get_time, pub, usize。（置信度：high）
  证据：
  - `r-core/os/src/timer.rs:L45-L49`：fn get_time_us
    代码片段：`/// get current time in microseconds pub fn get_time_us() -> usize { get_time() * MICRO_PER_SEC / CLOCK_FREQ }`
  - `codes/os/src/timer.rs:L31-L35`：fn get_time_ns
    代码片段：`} pub fn get_time_ns() -> usize { (get_time() / (CLOCK_FREQ / USEC_PER_SEC)) * MSEC_PER_SEC }`
- 与 2022 caporustos 在“内存管理”维度发现片段级代码相似度 0.95 （token=0.92, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, frame_allocator, heap_allocator, init, init_frame_allocator, init_heap, pub。（置信度：high）
  证据：
  - `r-core/os/src/mm/mod.rs:L68-L72`：fn init
    代码片段：`/// initiate heap allocator, frame allocator and kernel space pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`
  - `codes/os/src/mm/mod.rs:L29-L33`：fn init
    代码片段：`pub use heap_allocator::HEAP_ALLOCATOR; pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`
- 与 2022 caporustos 在“系统调用”维度发现片段级代码相似度 0.69 （token=0.60, structure=0.86）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, init, pub, set_kernel_trap_entry。（置信度：medium）
  证据：
  - `r-core/os/src/trap/mod.rs:L80-L84`：fn init
    代码片段：`/// Initialize trap handling pub fn init() { set_kernel_trap_entry(); }`
  - `codes/os/src/trap/mod.rs:L34-L38`：fn init
    代码片段：`core::arch::global_asm!(include_str!("trap_signal.S")); pub fn init() { set_kernel_trap_entry(); }`
- 文件路径重合：与 2022 caporustos 在“设备驱动”维度出现同名文件源码路径 `r-core/os/src/console.rs` / `codes/os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `r-core/os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console output for QEMU virt (SBI on RISC-V, UART on LoongArch). #[cfg(target_arch = "loongarch64")]`
  - `codes/os/src/console.rs:L21-L25`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
- 文件路径重合：与 2022 caporustos 在“文件系统”维度出现同名文件源码路径 `r-core/os/src/fs/inode.rs` / `codes/os/src/fs/inode.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `r-core/os/src/fs/inode.rs:L1-L3`：关键词命中
    代码片段：`//! 'Arc<Inode>' -> 'OSInodeInner': In order to open files concurrently //! we need to wrap 'Inode' into 'Arc', but 'Mutex' in 'Inode' prevents //! file systems from being acces...`
  - `codes/os/src/fs/inode.rs:L36-L40`：关键词命中
    代码片段：`} // 此inode实际被当作文件 pub struct OSInode { readable: bool,`
- 文件路径重合：与 2022 caporustos 在“中断与异常”维度出现同名文件源码路径 `r-core/os/src/timer.rs` / `codes/os/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `r-core/os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! Timer-related functionality use core::cmp::Ordering;`
  - `codes/os/src/timer.rs:L9-L13`：fn get_time
    代码片段：`pub const NSEC_PER_SEC: usize = 1000_000_000; pub fn get_time() -> usize { // let mut time: usize = 0; // unsafe{`
- 文件路径重合：与 2022 caporustos 在“系统调用”维度出现同名文件源码路径 `r-core/os/src/syscall/fs.rs` / `codes/os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `r-core/os/src/syscall/fs.rs:L10-L14`：关键词命中
    代码片段：`KLOG, RLIMIT_NOFILE, S_ISGID, check_parent_setgid, block_current_and_run_next, current_process, current_user_token }; use crate::syscall::{analyse_path, TimeSpec, REALTIME_OFFSE...`
  - `codes/os/src/syscall/fs.rs:L903-L907`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: isize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize{ if dirfd == AT...`

## 相似点

- 与 2022 caporustos 同属 rcore-variant 风格。（置信度：medium）
- 与 2022 caporustos 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `r-core/os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console output for QEMU virt (SBI on RISC-V, UART on LoongArch). #[cfg(target_arch = "loongarch64")]`
  - `r-core/os/src/la_console.rs:L1-L3`：关键词命中
    代码片段：`//! LoongArch QEMU virt UART (16550). use crate::config::UART_BASE;`
- 与 2022 caporustos 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `r-core/os/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`//! File trait & inode(dir, file, pipe, stdin, stdout) mod file_cap;`
  - `r-core/os/src/fs/proc.rs:L23-L27`：关键词命中
    代码片段：`struct ProcEntry { name: String, inode: u64, entry_type: ProcEntryType, }`
- 与 2022 caporustos 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `r-core/os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! Timer-related functionality use core::cmp::Ordering;`
  - `r-core/os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality //! //! For rCore, we have a single trap entry point, namely '__alltraps'. At`
- 与 2022 caporustos 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `r-core/os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `r-core/os/src/mm/tlb.rs:L1-L5`：关键词命中
    代码片段：`//! LoongArch64 hardware TLB refill ('TLBRENTRY') and page-walk CSR setup. use super::{PageTable, VirtAddr, VirtPageNum}; use core::arch::{asm, global_asm};`
- 与 2022 caporustos 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `r-core/os/src/fs/proc.rs:L1-L3`：关键词命中
    代码片段：`//! /proc 伪文件系统 //! //! 轻量级 /proc 实现，在 syscall 层面拦截路径，不依赖磁盘数据。`
  - `r-core/os/src/task/id.rs:L1-L3`：关键词命中
    代码片段：`//! Allocator for pid, task user resource, kernel stack using a simple recycle strategy. use super::ProcessControlBlock;`
- 与 2022 caporustos 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `r-core/os/src/sync/up.rs:L36-L40`：关键词命中
    代码片段：`error!( "last_syscall_id={}", crate::syscall::LAST_SYSCALL_ID.load(core::sync::atomic::Ordering::Relaxed) ); panic!(`
  - `r-core/os/src/sync/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization and interior mutability primitives mod condvar; mod mutex; mod semaphore;`
- 与 2022 caporustos 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `r-core/os/src/syscall/fs.rs:L10-L14`：关键词命中
    代码片段：`KLOG, RLIMIT_NOFILE, S_ISGID, check_parent_setgid, block_current_and_run_next, current_process, current_user_token }; use crate::syscall::{analyse_path, TimeSpec, REALTIME_OFFSE...`
  - `r-core/os/src/syscall/mq.rs:L1-L3`：关键词命中
    代码片段：`//! POSIX 消息队列（mqueue）及 System V 消息队列 syscall 实现 pub use crate::fs::mqueue::MqAttr;`
- 与 2022 caporustos 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 caporustos 的语言构成不同：待测作品为 {'build': 1365, 'markdown': 2367, 'text': 6852, 'json': 11, 'yaml': 87, 'c': 27815, 'make': 664, 'toml': 86, 'asm': 1358, 'rust': 50564}，历史样本为 {'json': 20, 'build': 685, 'markdown': 3328, 'asm': 148, 'text': 60, 'rust': 54342, 'yaml': 427, 'toml': 22}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：22
- 含证据关键结论数：22（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
