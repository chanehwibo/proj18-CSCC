# 2026 T2026102139911088 比较报告

- 对比历史仓库：2025 undefined
- 生成时间：2026-07-02T04:59:37.168884+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 undefined（来源：赛事历史作品）：画像相似度 score=9.54；同属 ucore-variant 风格; 语言构成相似度 0.85; OS 维度重合度 1.00; 代码规模接近度 0.84

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 undefined 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console/mod.rs:L10-L14`：关键词命中
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result { // Record raw bytes into the kernel log buffer for 'dmesg'. // Keep '\n' as-is (UART will translate to CRLF separately). crate:...`
  - `os/src/drivers/block/mod.rs:L1-L3`：关键词命中
    代码片段：`mod virtio_blk; pub use virtio_blk::VirtIOBlock;`
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2025 undefined 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L3-L7`：关键词命中
    代码片段：`//! 本模块是内核文件系统层的聚合入口：对外统一暴露 'File' trait 与各类文件实现， //! 负责伪文件系统（/proc、/sys、/dev、cgroup 等）的按路径分发，以及 poll/epoll //! 就绪等待队列等通用机制。真实数据落盘走 ext4（见 'inode' 子模块），其余多为内存态伪文件。 // ---- 各类文件实...`
  - `os/src/fs/inode.rs:L1-L3`：关键词命中
    代码片段：`//! Inode abstraction for ext4 filesystem use super::{File, POLLIN, POLLOUT};`
  - `src/fs/imp/tmp.rs:L50-L54`：关键词命中
    代码片段：`pub struct MemoryFs { inodes: Mutex<Slab<Arc<Inode>>>, root: Mutex<Option<DirEntry<RawMutex>>>, }`
  - `src/fs/dynamic/file.rs:L45-L49`：关键词命中
    代码片段：`#[inherit_methods(from = "self.node")] impl NodeOps<RawMutex> for SimpleFile { fn inode(&self) -> u64; fn metadata(&self) -> VfsResult<Metadata>; fn update_metadata(&self, updat...`
- 与 2025 undefined 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/trap/mod.rs:L1-L4`：关键词命中
    代码片段：`#[cfg(target_arch = "loongarch64")] pub use crate::arch::loongarch64::trap; #[cfg(target_arch = "riscv64")] pub use crate::arch::riscv64::trap;`
  - `os/src/arch/riscv64/trap/entry.rs:L3-L7`：关键词命中
    代码片段：`use riscv::register::sie; global_asm!(include_str!("trap.asm")); unsafe extern "C" {`
  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `arceos/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
- 与 2025 undefined 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `os/src/mm/address.rs:L8-L12`：关键词命中
    代码片段：`#[cfg(target_arch = "loongarch64")] const PA_WIDTH: usize = 48; // LoongArch page tables here are Sv39-style (3 levels), so only 39-bit VAs are valid. #[cfg(target_arch = "loong...`
  - `core/src/mm.rs:L24-L28`：关键词命中
    代码片段：`#[cfg(not(any(target_arch = "aarch64", target_arch = "loongarch64")))] { // ARMv8 (aarch64) and LoongArch64 use separate page tables for user space // (aarch64: TTBR0_EL1, Loong...`
  - `arceos/modules/axmm/src/backend/alloc.rs:L2-L6`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PhysAddr, VirtAddr};`
- 与 2025 undefined 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L4-L8`：关键词命中
    代码片段：`config::{KERNEL_STACK_SIZE, KERNEL_STACK_TOP, PAGE_SIZE, TRAP_CONTEXT_BASE, USER_STACK_SIZE}, mm::{KERNEL_SPACE, MapPermission, PhysPageNum, VirtAddr}, task::{lazy_static, proce...`
  - `os/src/task/mod.rs:L1-L5`：关键词命中
    代码片段：`#![allow(unused)] use core::{arch::asm, cell::RefCell, fmt::Display, task}; use crate::{`
  - `core/src/task.rs:L14-L18`：关键词命中
    代码片段：`use spin::Once; use undefined_process::process::Process; use undefined_process::thread::Thread; /// Task extended data for the monolithic kernel.`
  - `src/fs/imp/proc.rs:L631-L635`：关键词命中
    代码片段：`Seccomp:        2 Seccomp_filters:        1 Speculation_Store_Bypass:       thread vulnerable SpeculationIndirectBranch:      conditional enabled Cpus_allowed:   ffff`
- 与 2025 undefined 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/mutex.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{collections::vec_deque::VecDeque, sync::Arc}; use core::sync::atomic::{AtomicBool, Ordering}; use crate::task::{`
  - `os/src/syscall/mutex.rs:L1-L3`：关键词命中
    代码片段：`use crate::task::mutex::BlockMutex; use crate::task::mutex::Mutex;`
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`use core::{alloc::Layout, sync::atomic::Ordering}; use crate::ctypes::TimeStat;`
  - `src/fs/imp/tmp.rs:L6-L10`：关键词命中
    代码片段：`use crate::fs::dynamic::dynamic::dummy_stat_fs; use axsync::{Mutex, RawMutex}; use slab::Slab; use undefined_vfs::fs::{Filesystem, FilesystemOps, StatFs};`
- 与 2025 undefined 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/bpf/syscall.rs:L4-L8`：关键词命中
    代码片段：`fs::File, mm::{try_copy_from_user, try_copy_to_user}, syscall::error::{ SyscallError::{E2BIG, EACCES, EBADF, EFAULT, EINVAL, EMFILE, ENOENT, ENOMEM, ENOSYS}, err,`
  - `os/src/syscall/mod.rs:L241-L245`：关键词命中
    代码片段：`const SYSCALL_TIMERFD_GETTIME: usize = 87; const SYSCALL_STATX: usize = 291; // riscv64 Linux syscall numbers (match upstream): statfs=43, fstatfs=44. const SYSCALL_STATFS: usiz...`
  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_core::task::{time_stat_from_kernel_to_user, time_stat_from_user_to_kernel};`
  - `vendor/syscalls/src/syscall/arm.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %r5 // // %r7 is used for the syscall number. // // %r0 is reused for the syscall return value.`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 undefined 在“同步机制”维度发现片段级代码相似度 0.72 （token=0.57, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：alloc, atomic, core, crate, ordering, sync, use。（置信度：medium）
  证据：
  - `os/src/task/mutex.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{collections::vec_deque::VecDeque, sync::Arc}; use core::sync::atomic::{AtomicBool, Ordering}; use crate::task::{`
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`use core::{alloc::Layout, sync::atomic::Ordering}; use crate::ctypes::TimeStat;`
- 文件路径重合：与 2025 undefined 在“系统调用”维度出现同名文件源码路径 `os/src/bpf/syscall.rs` / `src/syscall.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/bpf/syscall.rs:L4-L8`：关键词命中
    代码片段：`fs::File, mm::{try_copy_from_user, try_copy_to_user}, syscall::error::{ SyscallError::{E2BIG, EACCES, EBADF, EFAULT, EINVAL, EMFILE, ENOENT, ENOMEM, ENOSYS}, err,`
  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_core::task::{time_stat_from_kernel_to_user, time_stat_from_user_to_kernel};`

## 相似点

- 与 2025 undefined 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 undefined 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console/mod.rs:L10-L14`：关键词命中
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result { // Record raw bytes into the kernel log buffer for 'dmesg'. // Keep '\n' as-is (UART will translate to CRLF separately). crate:...`
  - `os/src/drivers/block/mod.rs:L1-L3`：关键词命中
    代码片段：`mod virtio_blk; pub use virtio_blk::VirtIOBlock;`
- 与 2025 undefined 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L3-L7`：关键词命中
    代码片段：`//! 本模块是内核文件系统层的聚合入口：对外统一暴露 'File' trait 与各类文件实现， //! 负责伪文件系统（/proc、/sys、/dev、cgroup 等）的按路径分发，以及 poll/epoll //! 就绪等待队列等通用机制。真实数据落盘走 ext4（见 'inode' 子模块），其余多为内存态伪文件。 // ---- 各类文件实...`
  - `os/src/fs/inode.rs:L1-L3`：关键词命中
    代码片段：`//! Inode abstraction for ext4 filesystem use super::{File, POLLIN, POLLOUT};`
- 与 2025 undefined 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/trap/mod.rs:L1-L4`：关键词命中
    代码片段：`#[cfg(target_arch = "loongarch64")] pub use crate::arch::loongarch64::trap; #[cfg(target_arch = "riscv64")] pub use crate::arch::riscv64::trap;`
  - `os/src/arch/riscv64/trap/entry.rs:L3-L7`：关键词命中
    代码片段：`use riscv::register::sie; global_asm!(include_str!("trap.asm")); unsafe extern "C" {`
- 与 2025 undefined 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `os/src/mm/address.rs:L8-L12`：关键词命中
    代码片段：`#[cfg(target_arch = "loongarch64")] const PA_WIDTH: usize = 48; // LoongArch page tables here are Sv39-style (3 levels), so only 39-bit VAs are valid. #[cfg(target_arch = "loong...`
- 与 2025 undefined 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L4-L8`：关键词命中
    代码片段：`config::{KERNEL_STACK_SIZE, KERNEL_STACK_TOP, PAGE_SIZE, TRAP_CONTEXT_BASE, USER_STACK_SIZE}, mm::{KERNEL_SPACE, MapPermission, PhysPageNum, VirtAddr}, task::{lazy_static, proce...`
  - `os/src/task/mod.rs:L1-L5`：关键词命中
    代码片段：`#![allow(unused)] use core::{arch::asm, cell::RefCell, fmt::Display, task}; use crate::{`
- 与 2025 undefined 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/mutex.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{collections::vec_deque::VecDeque, sync::Arc}; use core::sync::atomic::{AtomicBool, Ordering}; use crate::task::{`
  - `os/src/syscall/mutex.rs:L1-L3`：关键词命中
    代码片段：`use crate::task::mutex::BlockMutex; use crate::task::mutex::Mutex;`
- 与 2025 undefined 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/bpf/syscall.rs:L4-L8`：关键词命中
    代码片段：`fs::File, mm::{try_copy_from_user, try_copy_to_user}, syscall::error::{ SyscallError::{E2BIG, EACCES, EBADF, EFAULT, EINVAL, EMFILE, ENOENT, ENOMEM, ENOSYS}, err,`
  - `os/src/syscall/mod.rs:L241-L245`：关键词命中
    代码片段：`const SYSCALL_TIMERFD_GETTIME: usize = 87; const SYSCALL_STATX: usize = 291; // riscv64 Linux syscall numbers (match upstream): statfs=43, fstatfs=44. const SYSCALL_STATFS: usiz...`
- 与 2025 undefined 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 undefined 的语言构成不同：待测作品为 {'json': 13143, 'build': 20712, 'markdown': 38574, 'toml': 11379, 'yaml': 83, 'text': 18307, 'rust': 1821857, 'c': 472, 'asm': 980}，历史样本为 {'json': 282064, 'build': 21492, 'markdown': 41236, 'text': 30233, 'toml': 10472, 'rust': 1882737, 'c': 68083, 'yaml': 72, 'make': 1961, 'asm': 983}。（置信度：medium）

## 可能创新点

- 待测作品包含 269 个历史样本 2025 undefined 中未见的独特函数/类型定义（如 `recv_buffer_available_bytes`, `recv`, `resource_attach_backing`, `set_bar_32`, `set_bar_64`, `virtio_device_type`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `vendor/virtio-drivers-pci/src/device/socket/connectionmanager.rs:L259-L259`：fn recv_buffer_available_bytes
    代码片段：`pub fn recv_buffer_available_bytes(&mut self, peer: VsockAddr, src_port: u32) -> Result<usize> {`
  - `vendor/virtio-drivers-pci/src/device/socket/connectionmanager.rs:L237-L237`：fn recv
    代码片段：`pub fn recv(&mut self, peer: VsockAddr, src_port: u32, buffer: &mut [u8]) -> Result<usize> {`

## 附录：核验摘要

- 关键结论数：17
- 含证据关键结论数：17（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
