# 2026 T2026105549911448 比较报告

- 对比历史仓库：2021 kCore
- 生成时间：2026-07-02T05:02:31.920719+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2021 kCore（来源：赛事历史作品）：画像相似度 score=9.64；同属 ucore-variant 风格; 语言构成相似度 0.99; OS 维度重合度 1.00; 代码规模接近度 0.66

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2021 kCore 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! Kernel console output. //! //! Byte I/O is delegated to the active architecture backend`
  - `kernel/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device drivers (M7). /// PCI ECAM enumeration for the loongarch64 'virt' board (virtio-pci).`
  - `os/src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
  - `os/src/drivers/block/mod.rs:L2-L6`：关键词命中
    代码片段：`#[cfg(feature = "board_qemu")] type BlockDeviceImpl = virtio_blk::VirtIOBlock; #[cfg(feature = "board_k210")]`
- 与 2021 kCore 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`//! VFS skeleton. Minimal Inode/File/FdTable, in-memory only (M5). use alloc::collections::BTreeMap;`
  - `kernel/src/fs/ext2.rs:L1-L5`：关键词命中
    代码片段：`//! ext2 read/write filesystem on a block device (our writable scratch, x1). //! //! A real on-disk filesystem: superblock, block/inode bitmaps, on-disk //! inodes with direct +...`
  - `os/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`mod pipe; mod stdio; mod inode; use crate::mem::UserBuffer;`
  - `os/src/fs/inode.rs:L4-L8`：关键词命中
    代码片段：`use kcore_fs::{ KcoreFileSystem, Inode, }; use crate::drivers::BLOCK_DEVICE;`
- 与 2021 kCore 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/arch/riscv64/trap.S:L1-L3`：关键词命中
    代码片段：`# Trap entry for both S-mode (kernel) and U-mode (user) traps. # # Convention:`
  - `kernel/src/arch/riscv64/trap.rs:L1-L3`：关键词命中
    代码片段：`//! S-mode trap dispatch (both kernel and user traps). use core::arch::global_asm;`
  - `os/src/trap/mod.rs:L3-L7`：关键词命中
    代码片段：`use riscv::register::{ mtvec::TrapMode, scause::{self, Exception, Interrupt, Trap}, sie, stval, stvec, };`
  - `os/src/sbi.rs:L38-L42`：关键词命中
    代码片段：`} pub fn set_timer(timer: usize) { sbi_call(SBI_SET_TIMER, timer, 0, 0); }`
- 与 2021 kCore 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/mm/mod.rs:L5-L9`：关键词命中
    代码片段：`pub mod heap; pub mod memory_set; pub mod page_table; pub use address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum, PAGE_SIZE};`
  - `kernel/src/mm/heap.rs:L42-L46`：关键词命中
    代码片段：`#[global_allocator] static HEAP_ALLOCATOR: PreemptHeap = PreemptHeap(LockedHeap::empty()); pub fn init() {`
  - `os/src/mem/page.rs:L13-L17`：关键词命中
    代码片段：`/// } /// ''' /// and The address of the multi-level page table /// will be stored in the satp register, you can /// get the page table like this`
  - `os/src/mem/page_table.rs:L10-L14`：关键词命中
    代码片段：`use core::fmt::{self, Debug, Formatter}; pub struct PageTable { pub root: PhyPageNum, pages: Vec<PhyPageWrapper>,`
- 与 2021 kCore 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Tasks + cooperative scheduler (M5 part 2). //! //! Layout: each Task owns a kstack buffer with the per-task TrapFrame`
  - `kernel/src/main.rs:L35-L39`：关键词命中
    代码片段：`mod sync; mod syscall; mod task; mod vdso;`
  - `os/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`mod processor; mod switch; mod task; use crate::loader::get_app_data_by_name;`
  - `os/src/task/task.rs:L60-L64`：关键词命中
    代码片段：`let kernel_stack = KernelStack::new(&pid_handle); let kernel_stack_top = kernel_stack.get_top(); // push a task context which goes to trap_return to the top of kernel stack let...`
- 与 2021 kCore 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/sync/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Locking primitives. //! //! 'Mutex' is a preempt-safe wrapper over 'spin::Mutex' (see 'spinlock'): while //! a lock is held it disables scheduler preemption, so a task holdi...`
  - `kernel/src/sync/futex.rs:L18-L22`：关键词命中
    代码片段：`use alloc::collections::{BTreeMap, VecDeque}; use alloc::sync::Arc; use core::sync::atomic::{AtomicI32, Ordering}; use crate::sync::Mutex; use spin::Lazy;`
  - `os/src/fs/pipe.rs:L3-L7`：关键词命中
    代码片段：`use super::File; use alloc::sync::{Arc, Weak}; use spin::Mutex; use crate::mem::{ UserBuffer,`
  - `os/src/fs/inode.rs:L11-L15`：关键词命中
    代码片段：`use bitflags::*; use alloc::vec::Vec; use spin::Mutex; use super::File; use crate::mem::UserBuffer;`
- 与 2021 kCore 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/syscall/nr.rs:L1-L3`：关键词命中
    代码片段：`//! Linux generic riscv64 syscall numbers (selected subset). //! Source: include/uapi/asm-generic/unistd.h`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Syscall dispatch. pub mod keys;`
  - `os/src/syscall/mod.rs:L18-L22`：关键词命中
    代码片段：`pub mod mem; pub fn syscall(syscall_id: usize, args: [usize; 3]) -> isize { match syscall_id { SYSCALL_READ => sys_read(args[0], args[1] as *const u8, args[2]),`
  - `os/src/sbi.rs:L15-L19`：关键词命中
    代码片段：`let mut ret; unsafe { llvm_asm!("ecall" : "={x10}" (ret) : "{x10}" (arg0), "{x11}" (arg1), "{x12}" (arg2), "{x17}" (which)`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2021 kCore 在“设备驱动”维度发现片段级代码相似度 0.91 （token=0.87, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：core, fmt, for, impl, self, struct, use, write。（置信度：high）
  证据：
  - `kernel/src/console.rs:L7-L11`：struct Console
    代码片段：`use core::fmt::{self, Write}; struct Console; impl Write for Console {`
  - `os/src/console.rs:L2-L6`：struct Stdout
    代码片段：`use core::fmt::{self, Write}; struct Stdout; impl Write for Stdout {`
- 与 2021 kCore 在“设备驱动”维度发现片段级代码相似度 0.80 （token=0.69, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fmt, fn, for, impl, in, mut, result, s。（置信度：high）
  证据：
  - `kernel/src/console.rs:L9-L13`：impl Write::for::Console
    代码片段：`struct Console; impl Write for Console { fn write_str(&mut self, s: &str) -> fmt::Result { for &b in s.as_bytes() {`
  - `os/src/console.rs:L5-L9`：fn write_str
    代码片段：`impl Write for Stdout { fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.chars() { console_putchar(c as usize);`
- 与 2021 kCore 在“内存管理”维度发现片段级代码相似度 0.73 （token=0.63, structure=0.90）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：global_allocator, heap_allocator, lockedheap, static。（置信度：medium）
  证据：
  - `kernel/src/mm/heap.rs:L42-L46`：关键词命中
    代码片段：`#[global_allocator] static HEAP_ALLOCATOR: PreemptHeap = PreemptHeap(LockedHeap::empty()); pub fn init() {`
  - `os/src/mem/heap_allocator.rs:L4-L8`：关键词命中
    代码片段：`#[global_allocator] static HEAP_ALLOCATOR: LockedHeap = LockedHeap::new(); // heap memory for kernel`
- 函数/符号名重合：与 2021 kCore 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L12-L12`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `user/src/console.rs:L11-L11`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 函数/符号名重合：与 2021 kCore 在“内存管理”维度发现 1 个同名定义：alloc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L514-L514`：fn alloc
    代码片段：`pub fn alloc(&self, file: Arc<File>, cloexec: bool) -> Result<i32> {`
  - `third_party/user/buddy_system_allocator/src/frame.rs:L76-L76`：fn alloc
    代码片段：`pub fn alloc(&mut self, count: usize) -> Option<usize> {`
- 文件路径重合：与 2021 kCore 在“设备驱动”维度出现同名文件源码路径 `kernel/src/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! Kernel console output. //! //! Byte I/O is delegated to the active architecture backend`
  - `os/src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`

## 相似点

- 与 2021 kCore 同属 ucore-variant 风格。（置信度：medium）
- 与 2021 kCore 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! Kernel console output. //! //! Byte I/O is delegated to the active architecture backend`
  - `kernel/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device drivers (M7). /// PCI ECAM enumeration for the loongarch64 'virt' board (virtio-pci).`
- 与 2021 kCore 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`//! VFS skeleton. Minimal Inode/File/FdTable, in-memory only (M5). use alloc::collections::BTreeMap;`
  - `kernel/src/fs/ext2.rs:L1-L5`：关键词命中
    代码片段：`//! ext2 read/write filesystem on a block device (our writable scratch, x1). //! //! A real on-disk filesystem: superblock, block/inode bitmaps, on-disk //! inodes with direct +...`
- 与 2021 kCore 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/arch/riscv64/trap.S:L1-L3`：关键词命中
    代码片段：`# Trap entry for both S-mode (kernel) and U-mode (user) traps. # # Convention:`
  - `kernel/src/arch/riscv64/trap.rs:L1-L3`：关键词命中
    代码片段：`//! S-mode trap dispatch (both kernel and user traps). use core::arch::global_asm;`
- 与 2021 kCore 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/mm/mod.rs:L5-L9`：关键词命中
    代码片段：`pub mod heap; pub mod memory_set; pub mod page_table; pub use address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum, PAGE_SIZE};`
  - `kernel/src/mm/heap.rs:L42-L46`：关键词命中
    代码片段：`#[global_allocator] static HEAP_ALLOCATOR: PreemptHeap = PreemptHeap(LockedHeap::empty()); pub fn init() {`
- 与 2021 kCore 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Tasks + cooperative scheduler (M5 part 2). //! //! Layout: each Task owns a kstack buffer with the per-task TrapFrame`
  - `kernel/src/main.rs:L35-L39`：关键词命中
    代码片段：`mod sync; mod syscall; mod task; mod vdso;`
- 与 2021 kCore 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/sync/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Locking primitives. //! //! 'Mutex' is a preempt-safe wrapper over 'spin::Mutex' (see 'spinlock'): while //! a lock is held it disables scheduler preemption, so a task holdi...`
  - `kernel/src/sync/futex.rs:L18-L22`：关键词命中
    代码片段：`use alloc::collections::{BTreeMap, VecDeque}; use alloc::sync::Arc; use core::sync::atomic::{AtomicI32, Ordering}; use crate::sync::Mutex; use spin::Lazy;`
- 与 2021 kCore 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/syscall/nr.rs:L1-L3`：关键词命中
    代码片段：`//! Linux generic riscv64 syscall numbers (selected subset). //! Source: include/uapi/asm-generic/unistd.h`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Syscall dispatch. pub mod keys;`
- 与 2021 kCore 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2021 kCore 的语言构成不同：待测作品为 {'build': 4296, 'markdown': 9372, 'toml': 83, 'asm': 544, 'rust': 266740, 'c': 418, 'json': 264, 'yaml': 104, 'text': 35}，历史样本为 {'json': 87, 'build': 3692, 'markdown': 16369, 'toml': 60, 'rust': 452565, 'yaml': 92, 'c': 48, 'asm': 572, 'text': 1871}。（置信度：medium）

## 可能创新点

- 待测作品包含 267 个历史样本 2021 kCore 中未见的独特函数/类型定义（如 `write_raw_inode`, `extent_map`, `from_inode`, `read_dir`, `xattr_store_set`, `put_dirent`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/src/fs/ext2.rs:L629-L629`：fn write_raw_inode
    代码片段：`fn write_raw_inode(dev: &BlockDevice, inode_table: u32, ino: u32, di: &DiskInode) -> Result<()> {`
  - `kernel/src/fs/ext4.rs:L195-L195`：fn extent_map
    代码片段：`fn extent_map(&self, ino: &Inode4) -> core::result::Result<Vec<(u32, u64, u32)>, &'static str> {`

## 附录：核验摘要

- 关键结论数：21
- 含证据关键结论数：21（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
