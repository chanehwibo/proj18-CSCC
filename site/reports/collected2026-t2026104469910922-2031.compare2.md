# 2026 T2026104469910922 比较报告

- 对比历史仓库：2025 OSakura
- 生成时间：2026-07-02T09:02:11.768632+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 OSakura（来源：赛事历史作品）：画像相似度 score=9.66；同属 ucore-variant 风格; 语言构成相似度 0.86; OS 维度重合度 1.00; 代码规模接近度 0.94

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 OSakura 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/driver/virtio_blk.c:L4-L8`：关键词命中
    代码片段：`#include "kernel/log.h" #include "kernel/types.h" #include "platform/virtio_mmio.h" #define LOG_MODULE "VIRT"`
  - `include/kernel/defs.h:L133-L137`：关键词命中
    代码片段：`void put_inode(struct vfs_inode *ip); // virtio_disk.c void virtio_disk_init(); void virtio_disk_rw(struct buf *buffer, int write);`
  - `OSakura-rv/kernel/dev/uart.c:L1-L3`：关键词命中
    代码片段：`// low-level driver routines for 16550a UART #include "memlayout.h" #include "dev/uart.h"`
  - `OSakura-rv/kernel/dev/console.c:L1-L4`：关键词命中
    代码片段：`#include <stdarg.h> #include "dev/console.h" #include "dev/uart.h" #include "mem/vmem.h"`
- 与 2025 OSakura 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/vfs.c:L4-L8`：关键词命中
    代码片段：`#include "kernel/log.h" #define LOG_MODULE " VFS" struct vfs_inode *vfs_root;`
  - `kernel/core/file.c:L34-L38`：关键词命中
    代码片段：`/* * resolve_open_path - Choose the VFS lookup/create start point for open/openat. * * Cases:`
  - `OSakura-rv/kernel/fs/ext4/ext4_dir.c:L11-L15`：关键词命中
    代码片段：`// pip是一个目录, 在这个目录里寻找名为filename的文件 // 找到了返回inode 没找到返回NULL // 这个函数是从磁盘里读入inode的唯一路径 // 注意: pip应当有效且上锁`
  - `OSakura-rv/kernel/fs/ext4/ext4_sys.c:L271-L275`：关键词命中
    代码片段：`} } /* 到此获得了一个上了锁的inode */ file = ext4_file_alloc();`
- 与 2025 OSakura 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/core/proc.c:L4-L8`：关键词命中
    代码片段：`#include "asm/mm.h" #include "asm/riscv.h" #include "asm/trap.h" #include "kernel/defs.h" #include "kernel/fcntl.h"`
  - `kernel/core/printf.c:L1-L4`：关键词命中
    代码片段：`#include "asm/riscv.h" #include "asm/trap.h" #include "driver/hal_console.h" #include "kernel/defs.h"`
  - `OSakura-rv/kernel/dev/plic.c:L1-L3`：关键词命中
    代码片段：`// Platform-level interrupt controller #include "riscv.h"`
  - `OSakura-rv/kernel/dev/timer.c:L2-L6`：关键词命中
    代码片段：`#include "sbi.h" #include "riscv.h" #include "dev/timer.h" #include "lib/print.h"`
- 与 2025 OSakura 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mm/mmap.c:L82-L86`：关键词命中
    代码片段：`struct Process *proc = get_proc(); for (uint64 faddr = used_addr; faddr < proc->stack_bottom; faddr += PGSIZE) { int found = 1;`
  - `kernel/mm/kalloc.c:L1-L3`：关键词命中
    代码片段：`#include "kernel/kalloc.h" #include "asm/machine.h" #include "asm/mm.h"`
  - `OSakura-rv/kernel/mem/kvm.c:L16-L20`：关键词命中
    代码片段：`// 申请L2内核页表空间 kernel_pagetable = (pgtbl_t)pmem_alloc_pages(1, true); // printf("kernel pagetable = %p\n",kernel_pagetable); assert(kernel_pagetable != NULL, "kvm.c->kvm_init: 1\...`
  - `OSakura-rv/kernel/mem/uvm.c:L41-L45`：关键词命中
    代码片段：`// 释放region资源, 同时释放占用的物理页 void uvm_region_free(pgtbl_t pagetable, vm_region_t* region) { uvm_unmappages(pagetable, region->start, region->npages, true);`
- 与 2025 OSakura 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/core/proc.c:L1-L3`：关键词命中
    代码片段：`#include "core/proc.h" #include "asm/cpu.h" #include "asm/defs.h"`
  - `kernel/fs/vfs.c:L1-L4`：关键词命中
    代码片段：`#include "kernel/defs.h" #include "core/proc.h" #include "kernel/fs.h" #include "kernel/log.h"`
  - `OSakura-rv/kernel/proc/cpu.c:L1-L3`：关键词命中
    代码片段：`#include "proc/cpu.h" #include "lock/lock.h" #include "riscv.h"`
  - `OSakura-rv/kernel/proc/exec.c:L1-L3`：关键词命中
    代码片段：`#include "proc/elf.h" #include "proc/cpu.h" #include "mem/vmem.h"`
- 与 2025 OSakura 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mm/mmap.c:L9-L13`：关键词命中
    代码片段：`#define PROT_WRITE 0x2 /* page can be written */ #define PROT_EXEC 0x4  /* page can be executed */ #define PROT_SEM 0x8   /* page may be used for atomic ops */ #define PROT_NONE...`
  - `kernel/core/file.c:L12-L16`：关键词命中
    代码片段：`extern struct vfs_inode *vfs_root; extern struct spinlock ftable_lock; extern struct file ftable[NFILE];`
  - `OSakura-rv/kernel/lock/spinlock.c:L1-L3`：关键词命中
    代码片段：`#include "lock/lock.h" #include "proc/cpu.h" #include "lib/print.h"`
  - `OSakura-rv/kernel/lock/sleeplock.c:L1-L4`：关键词命中
    代码片段：`#include "proc/cpu.h" #include "lock/lock.h" void sleeplock_init(sleeplock_t* lock, char* name)`
- 与 2025 OSakura 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/core/syscall.c:L1-L3`：关键词命中
    代码片段：`#include "kernel/syscall.h" #include "asm/defs.h" #include "core/proc.h"`
  - `include/kernel/syscall.h:L9-L13`：关键词命中
    代码片段：`#define ARG5 5 // RISC-V Linux syscall numbers used by musl/glibc test binaries. #define SYS_getcwd 17 #define SYS_pipe2 59`
  - `OSakura-rv/kernel/syscall/syscall.c:L2-L6`：关键词命中
    代码片段：`#include "proc/cpu.h"          // myproc #include "mem/vmem.h"          // uvm_copy #include "syscall/sysnum.h" #include "syscall/sysfile.h" #include "syscall/sysproc.h"`
  - `OSakura-rv/kernel/syscall/sysfile.c:L1-L3`：关键词命中
    代码片段：`#include "syscall/sysfile.h" #include "syscall/syscall.h" #include "fs/procfs.h"`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 OSakura 在“同步机制”维度发现片段级代码相似度 0.82 （token=0.73, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：char, locked, name, spinlock, struct。（置信度：high）
  证据：
  - `include/kernel/spinlock.h:L4-L8`：struct spinlock
    代码片段：`#include "kernel/types.h" struct spinlock { uint locked; char *name;`
  - `OSakura-rv/include/lock/lock.h:L4-L8`：关键词命中
    代码片段：`#include "common.h" typedef struct spinlock { int locked;  // 是否上锁 char* name;  // for debug`
- 宏名重合：与 2025 OSakura 在“设备驱动”维度发现 4 个同名定义：UART_IRQ, IER_RX_ENABLE, IER_TX_ENABLE, FCR_FIFO_ENABLE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `arch/riscv/include/platform/uart.h:L6-L6`：macro UART_IRQ
    代码片段：`#define UART_IRQ 0xa`
  - `OSakura-rv/include/memlayout.h:L33-L33`：macro UART_IRQ
    代码片段：`#define UART_IRQ   10`
  - `arch/riscv/include/platform/uart.h:L26-L26`：macro IER_RX_ENABLE
    代码片段：`#define IER_RX_ENABLE (1 << 0)`
  - `OSakura-rv/kernel/dev/uart.c:L11-L11`：macro IER_RX_ENABLE
    代码片段：`#define IER_RX_ENABLE   (1<<0)`
- 宏名重合：与 2025 OSakura 在“文件系统”维度发现 3 个同名定义：NOFILE, SYS_fstat, NFILE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `arch/riscv/include/core/proc.h:L17-L17`：macro NOFILE
    代码片段：`#define NOFILE 128`
  - `OSakura-rv/include/common.h:L45-L45`：macro NOFILE
    代码片段：`#define NOFILE 128                 // 单个进程同时打开的最大文件数量`
  - `include/kernel/syscall.h:L20-L20`：macro SYS_fstat
    代码片段：`#define SYS_fstat 80`
  - `OSakura-rv/user/include/syscall_num.h:L19-L19`：macro SYS_fstat
    代码片段：`#define SYS_fstat       80`
- 宏名重合：与 2025 OSakura 在“中断与异常”维度发现 4 个同名定义：MIE_MTIE, MSTATUS_MIE, SSTATUS_SIE, SSTATUS_SPIE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `arch/riscv/include/asm/trap.h:L6-L6`：macro MIE_MTIE
    代码片段：`#define MIE_MTIE (1UL << 7) // MTIE : Machine Timer Interrupt Enable`
  - `OSakura-rv/include/riscv.h:L91-L91`：macro MIE_MTIE
    代码片段：`#define MIE_MTIE (1L << 7)  // timer`
  - `arch/riscv/include/asm/trap.h:L13-L13`：macro MSTATUS_MIE
    代码片段：`#define MSTATUS_MIE (1UL << 3)`
  - `OSakura-rv/include/riscv.h:L18-L18`：macro MSTATUS_MIE
    代码片段：`#define MSTATUS_MIE (1L << 3)    // machine-mode interrupt enable.`
- 宏名重合：与 2025 OSakura 在“内存管理”维度发现 4 个同名定义：PTE_D, PTE_A, PTE_G, PTE_U。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `arch/riscv/include/asm/mm.h:L7-L7`：macro PTE_D
    代码片段：`#define PTE_D (1 << 7)`
  - `OSakura-rv/include/mem/vmem.h:L58-L58`：macro PTE_D
    代码片段：`#define PTE_D (1 << 7)`
  - `arch/riscv/include/asm/mm.h:L9-L9`：macro PTE_A
    代码片段：`#define PTE_A (1 << 6)`
  - `OSakura-rv/include/mem/vmem.h:L57-L57`：macro PTE_A
    代码片段：`#define PTE_A (1 << 6)`
- 宏名重合：与 2025 OSakura 在“系统调用”维度发现 4 个同名定义：SYS_getcwd, SYS_pipe2, SYS_dup, SYS_openat。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/kernel/syscall.h:L12-L12`：macro SYS_getcwd
    代码片段：`#define SYS_getcwd 17`
  - `OSakura-rv/user/include/syscall_num.h:L6-L6`：macro SYS_getcwd
    代码片段：`#define SYS_getcwd      17`
  - `include/kernel/syscall.h:L13-L13`：macro SYS_pipe2
    代码片段：`#define SYS_pipe2 59`
  - `OSakura-rv/user/include/syscall_num.h:L7-L7`：macro SYS_pipe2
    代码片段：`#define SYS_pipe2       59`
- 结构体/类型重合：与 2025 OSakura 在“设备驱动”维度发现 4 个同名定义：virtq_desc, virtq_avail, virtq_used_elem, virtq_used。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `arch/riscv/include/platform/virtio_mmio.h:L172-L172`：struct virtq_desc
    代码片段：`struct virtq_desc {`
  - `OSakura-rv/include/dev/virtio.h:L45-L45`：struct virtq_desc
    代码片段：`struct virtq_desc {`
  - `arch/riscv/include/platform/virtio_mmio.h:L183-L183`：struct virtq_avail
    代码片段：`struct virtq_avail {`
  - `OSakura-rv/include/dev/virtio.h:L55-L55`：struct virtq_avail
    代码片段：`struct virtq_avail {`
- 结构体/类型重合：与 2025 OSakura 在“调度与任务管理”维度发现 3 个同名定义：context, trapframe, cpu。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `arch/riscv/include/core/proc.h:L19-L19`：struct context
    代码片段：`struct context {`
  - `OSakura-rv/include/proc/proc.h:L55-L55`：struct context
    代码片段：`typedef struct context {`
  - `arch/riscv/include/core/proc.h:L38-L38`：struct trapframe
    代码片段：`struct trapframe {`
  - `OSakura-rv/include/proc/proc.h:L15-L15`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 宏名重合：与 2025 OSakura 在“调度与任务管理”维度发现 2 个同名定义：NPROC, SYS_sched_yield。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `arch/riscv/include/core/proc.h:L8-L8`：macro NPROC
    代码片段：`#define NPROC 64`
  - `OSakura-rv/include/common.h:L26-L26`：macro NPROC
    代码片段：`#define NPROC 64                  // 进程最大数量`
  - `include/kernel/syscall.h:L50-L50`：macro SYS_sched_yield
    代码片段：`#define SYS_sched_yield 124`
  - `OSakura-rv/user/include/syscall_num.h:L39-L39`：macro SYS_sched_yield
    代码片段：`#define SYS_sched_yield  124`
- 结构体/类型重合：与 2025 OSakura 在“文件系统”维度发现 1 个同名定义：pipe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/kernel/fs.h:L86-L86`：struct pipe
    代码片段：`struct pipe {`
  - `OSakura-rv/include/fs/fat32_pipe.h:L8-L8`：struct pipe
    代码片段：`typedef struct pipe {`
- 结构体/类型重合：与 2025 OSakura 在“中断与异常”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `arch/riscv/include/core/proc.h:L38-L38`：struct trapframe
    代码片段：`struct trapframe {`
  - `OSakura-rv/include/proc/proc.h:L15-L15`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 结构体/类型重合：与 2025 OSakura 在“内存管理”维度发现 1 个同名定义：trapframe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `arch/riscv/include/core/proc.h:L38-L38`：struct trapframe
    代码片段：`struct trapframe {`
  - `OSakura-rv/include/proc/proc.h:L15-L15`：struct trapframe
    代码片段：`typedef struct trapframe {`
- 结构体/类型重合：与 2025 OSakura 在“同步机制”维度发现 2 个同名定义：sleeplock, spinlock。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/kernel/sleeplock.h:L6-L6`：struct sleeplock
    代码片段：`struct sleeplock {`
  - `OSakura-rv/include/lock/lock.h:L12-L12`：struct sleeplock
    代码片段：`typedef struct sleeplock {`
  - `include/kernel/spinlock.h:L6-L6`：struct spinlock
    代码片段：`struct spinlock {`
  - `OSakura-rv/include/lock/lock.h:L6-L6`：struct spinlock
    代码片段：`typedef struct spinlock {`
- 文件路径重合：与 2025 OSakura 在“调度与任务管理”维度出现同名文件源码路径 `kernel/core/proc.c` / `OSakura-rv/kernel/proc/proc.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/core/proc.c:L1-L3`：关键词命中
    代码片段：`#include "core/proc.h" #include "asm/cpu.h" #include "asm/defs.h"`
  - `OSakura-rv/kernel/proc/proc.c:L2-L6`：关键词命中
    代码片段：`#include "lib/str.h" #include "lib/print.h" #include "proc/cpu.h" #include "proc/initcode.h" #include "mem/pmem.h"`
- 文件路径重合：与 2025 OSakura 在“系统调用”维度出现同名文件源码路径 `kernel/core/syscall.c` / `OSakura-rv/kernel/syscall/syscall.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/core/syscall.c:L1-L3`：关键词命中
    代码片段：`#include "kernel/syscall.h" #include "asm/defs.h" #include "core/proc.h"`
  - `OSakura-rv/kernel/syscall/syscall.c:L2-L6`：关键词命中
    代码片段：`#include "proc/cpu.h"          // myproc #include "mem/vmem.h"          // uvm_copy #include "syscall/sysnum.h" #include "syscall/sysfile.h" #include "syscall/sysproc.h"`

## 相似点

- 与 2025 OSakura 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 OSakura 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/driver/virtio_blk.c:L4-L8`：关键词命中
    代码片段：`#include "kernel/log.h" #include "kernel/types.h" #include "platform/virtio_mmio.h" #define LOG_MODULE "VIRT"`
  - `include/kernel/defs.h:L133-L137`：关键词命中
    代码片段：`void put_inode(struct vfs_inode *ip); // virtio_disk.c void virtio_disk_init(); void virtio_disk_rw(struct buf *buffer, int write);`
- 与 2025 OSakura 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/vfs.c:L4-L8`：关键词命中
    代码片段：`#include "kernel/log.h" #define LOG_MODULE " VFS" struct vfs_inode *vfs_root;`
  - `kernel/core/file.c:L34-L38`：关键词命中
    代码片段：`/* * resolve_open_path - Choose the VFS lookup/create start point for open/openat. * * Cases:`
- 与 2025 OSakura 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/core/proc.c:L4-L8`：关键词命中
    代码片段：`#include "asm/mm.h" #include "asm/riscv.h" #include "asm/trap.h" #include "kernel/defs.h" #include "kernel/fcntl.h"`
  - `kernel/core/printf.c:L1-L4`：关键词命中
    代码片段：`#include "asm/riscv.h" #include "asm/trap.h" #include "driver/hal_console.h" #include "kernel/defs.h"`
- 与 2025 OSakura 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mm/mmap.c:L82-L86`：关键词命中
    代码片段：`struct Process *proc = get_proc(); for (uint64 faddr = used_addr; faddr < proc->stack_bottom; faddr += PGSIZE) { int found = 1;`
  - `kernel/mm/kalloc.c:L1-L3`：关键词命中
    代码片段：`#include "kernel/kalloc.h" #include "asm/machine.h" #include "asm/mm.h"`
- 与 2025 OSakura 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/core/proc.c:L1-L3`：关键词命中
    代码片段：`#include "core/proc.h" #include "asm/cpu.h" #include "asm/defs.h"`
  - `kernel/fs/vfs.c:L1-L4`：关键词命中
    代码片段：`#include "kernel/defs.h" #include "core/proc.h" #include "kernel/fs.h" #include "kernel/log.h"`
- 与 2025 OSakura 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mm/mmap.c:L9-L13`：关键词命中
    代码片段：`#define PROT_WRITE 0x2 /* page can be written */ #define PROT_EXEC 0x4  /* page can be executed */ #define PROT_SEM 0x8   /* page may be used for atomic ops */ #define PROT_NONE...`
  - `kernel/core/file.c:L12-L16`：关键词命中
    代码片段：`extern struct vfs_inode *vfs_root; extern struct spinlock ftable_lock; extern struct file ftable[NFILE];`
- 与 2025 OSakura 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/core/syscall.c:L1-L3`：关键词命中
    代码片段：`#include "kernel/syscall.h" #include "asm/defs.h" #include "core/proc.h"`
  - `include/kernel/syscall.h:L9-L13`：关键词命中
    代码片段：`#define ARG5 5 // RISC-V Linux syscall numbers used by musl/glibc test binaries. #define SYS_getcwd 17 #define SYS_pipe2 59`
- 与 2025 OSakura 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 OSakura 的语言构成不同：待测作品为 {'build': 83, 'markdown': 2333, 'c': 16675, 'asm': 352, 'make': 431}，历史样本为 {'json': 20, 'build': 459, 'markdown': 15, 'make': 26, 'c': 20261, 'asm': 315}。（置信度：medium）

## 可能创新点

- 待测作品包含 40 个历史样本 2025 OSakura 中未见的独特函数/类型定义（如 `proc_state`, `file_type`, `virtio_blk_discard_write_zeroes`, `vm_operations_struct`, `virtio_blk_geometry`, `virtio_blk_topology`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `arch/riscv/include/core/proc.h:L104-L104`：enum proc_state
    代码片段：`enum proc_state { UNUSED, USED, RUNNABLE, RUNNING, SLEEPING, ZOMBIE };`
  - `include/kernel/fs.h:L95-L95`：enum file_type
    代码片段：`enum file_type { FILE_NONE, FILE_VFS_NODE, FILE_PIPE };`

## 附录：核验摘要

- 关键结论数：30
- 含证据关键结论数：30（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
