# 2026 T202610486999563 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t202610486999563-1502`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：844
- 代码/文本行数：251518
- 主要语言：rust 240449 LOC, markdown 6853 LOC, build 3579 LOC, text 285 LOC, json 161 LOC

## 总览

2026 T202610486999563 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 240449 LOC, markdown: 6853 LOC, build: 3579 LOC, text: 285 LOC。仓库包含 844 个已扫描文件、约 251518 行可分析文本，当前抽取到 17299 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, Makefile, crates/fs-ext4/Cargo.toml, crates/hal-api/Cargo.toml, crates/hal-loongarch64-virt/Cargo.toml。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] members = [ "tools/xtask",`
  - `Makefile:L1-L3`：构建入口
    代码片段：`.PHONY: all build check qemu test build-riscv build-loongarch qemu-riscv qemu-loongarch \ oscomp-riscv oscomp-loongarch oscomp-riscv-contest oscomp-loongarch-contest \ oscomp-ri...`
  - `crates/fs-ext4/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "fs-ext4" version.workspace = true`
  - `crates/hal-api/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "hal-api" version.workspace = true`
  - `crates/hal-loongarch64-virt/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "hal-loongarch64-virt" version.workspace = true`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn dispatch, enum ProcessState, struct Credentials, struct ProcessGroupState, struct SignalState。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `crates/syscall/src/task_domain.rs` | L12-L16 | 关键词命中 |
| `crates/proc/src/lib.rs` | L184-L188 | 关键词命中 |
| `crates/task/src/lib.rs` | L16-L20 | 关键词命中 |
| `crates/syscall/src/task_domain.rs` | L5-L9 | fn dispatch |
| `crates/proc/src/lib.rs` | L36-L40 | enum ProcessState |
| `crates/proc/src/lib.rs` | L44-L48 | struct Credentials |

### 关键代码片段

  - `crates/syscall/src/task_domain.rs:L12-L16`：关键词命中
    代码片段：`Some(match sysno { SYS_SET_TID_ADDRESS => ctx.dispatcher.sys_set_tid_address(args, ctx.procs), SYS_SCHED_YIELD => ctx.dispatcher.sys_sched_yield(ctx.scheduler), SYS_EXIT => ctx...`
  - `crates/proc/src/lib.rs:L184-L188`：关键词命中
    代码片段：`pub sigsuspend_saved_mask: Option<u64>, pub is_thread: bool, /// Set to true when a signal frame has been dispatched into the thread's /// trap frame (sepc redirected to signal...`
  - `crates/task/src/lib.rs:L16-L20`：关键词命中
    代码片段：`#[derive(Clone, Debug)] pub struct Task { pub id: usize, pub process_id: usize,`
  - `crates/syscall/src/task_domain.rs:L5-L9`：fn dispatch
    代码片段：`}; pub(crate) fn dispatch( ctx: &mut DispatchContext<'_>, sysno: usize,`

### 相关符号

`fn dispatch` at `crates/syscall/src/task_domain.rs:L5`、`enum ProcessState` at `crates/proc/src/lib.rs:L36`、`struct Credentials` at `crates/proc/src/lib.rs:L44`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn dispatch, struct MappingArea, struct FrameAllocator, fn from_regions, fn alloc_page。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `crates/mm/src/lib.rs` | L161-L165 | 关键词命中 |
| `vendor/virtio-drivers/src/lib.rs` | L2-L6 | 关键词命中 |
| `third_party/virtio-drivers/src/lib.rs` | L2-L6 | 关键词命中 |
| `crates/syscall/src/mm_domain.rs` | L4-L8 | fn dispatch |
| `crates/mm/src/lib.rs` | L58-L62 | struct MappingArea |
| `crates/mm/src/lib.rs` | L65-L69 | struct FrameAllocator |

### 关键代码片段

  - `crates/mm/src/lib.rs:L161-L165`：关键词命中
    代码片段：`program_break: usize, next_mapping_base: usize, page_table: Option<PageTableSpace>, dirty: bool, frame_allocator: Mutex<FrameAllocator>,`
  - `vendor/virtio-drivers/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM //! to interact with VirtIO devices provided by the VMM (such as QEMU or...`
  - `third_party/virtio-drivers/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM //! to interact with VirtIO devices provided by the VMM (such as QEMU or...`
  - `crates/syscall/src/mm_domain.rs:L4-L8`：fn dispatch
    代码片段：`}; pub(crate) fn dispatch( ctx: &mut DispatchContext<'_>, sysno: usize,`

### 相关符号

`fn dispatch` at `crates/syscall/src/mm_domain.rs:L4`、`struct MappingArea` at `crates/mm/src/lib.rs:L58`、`struct FrameAllocator` at `crates/mm/src/lib.rs:L65`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn dispatch, fn dispatch, fn dispatch, fn write_console_bytes, fn write_console_line。（置信度：medium）
  - 静态识别到 158 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `crates/syscall/src/lib.rs` | L537-L541 | 关键词命中 |
| `crates/user-init/src/lib.rs` | L33-L37 | 关键词命中 |
| `crates/hal-riscv64-virt/src/lib.rs` | L763-L767 | 关键词命中 |
| `crates/syscall/src/fs_domain.rs` | L11-L15 | fn dispatch |
| `crates/syscall/src/mm_domain.rs` | L4-L8 | fn dispatch |
| `crates/syscall/src/task_domain.rs` | L5-L9 | fn dispatch |

### 关键代码片段

  - `crates/syscall/src/lib.rs:L537-L541`：关键词命中
    代码片段：`} fn log_user_path_fault(process: &proc::Process, syscall: &str, path_ptr: usize) { log_always(&format!( "whuse-ltp:path-fault syscall={} tgid={} tid={} name={} cwd={} ptr={:#x}...`
  - `crates/user-init/src/lib.rs:L33-L37`：关键词命中
    代码片段：`li a2, init_msg_end - init_msg li a7, 64 ecall li a0, 0`
  - `crates/hal-riscv64-virt/src/lib.rs:L763-L767`：关键词命中
    代码片段：`unsafe { core::arch::asm!( "ecall", inlateout("a0") timer_ticks as usize => error, in("a6") SBI_FID_SET_TIMER,`
  - `crates/syscall/src/fs_domain.rs:L11-L15`：fn dispatch
    代码片段：`}; pub(crate) fn dispatch( ctx: &mut DispatchContext<'_>, sysno: usize,`

### 相关符号

`fn dispatch` at `crates/syscall/src/fs_domain.rs:L11`、`fn dispatch` at `crates/syscall/src/mm_domain.rs:L4`、`fn dispatch` at `crates/syscall/src/task_domain.rs:L5`、`fn syscall_trace_enabled` at `crates/syscall/src/lib.rs:L362`、`struct SyscallArgs` at `crates/syscall/src/lib.rs:L551`、`struct SyscallDispatcher` at `crates/syscall/src/lib.rs:L562`、`impl SyscallDispatcher` at `crates/syscall/src/lib.rs:L1023`、`fn sys_getcwd` at `crates/syscall/src/lib.rs:L1130`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：fn dispatch, fn write_console_bytes, fn write_console_line, enum NodeKind, struct FileStat。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `crates/syscall/src/fs_domain.rs` | L22-L26 | 关键词命中 |
| `crates/vfs/src/lib.rs` | L295-L299 | 关键词命中 |
| `crates/syscall/src/mm_domain.rs` | L12-L16 | 关键词命中 |
| `crates/syscall/src/fs_domain.rs` | L11-L15 | fn dispatch |
| `crates/vfs/src/lib.rs` | L57-L61 | fn write_console_bytes |
| `crates/vfs/src/lib.rs` | L64-L68 | fn write_console_line |

### 关键代码片段

  - `crates/syscall/src/fs_domain.rs:L22-L26`：关键词命中
    代码片段：`SYS_FCNTL => ctx .dispatcher .sys_fcntl(args, ctx.procs, ctx.scheduler, ctx.vfs), SYS_IOCTL => ctx.dispatcher.sys_ioctl(args, ctx.procs), SYS_FLOCK => ctx.dispatcher.sys_flock(a...`
  - `crates/vfs/src/lib.rs:L295-L299`：关键词命中
    代码片段：`pub fn new() -> Self { let root = Arc::new(Node::directory("/")); let mut vfs = Self { root, mounts: Vec::new(),`
  - `crates/syscall/src/mm_domain.rs:L12-L16`：关键词命中
    代码片段：`SYS_BRK => ctx.dispatcher.sys_brk(args, ctx.procs), SYS_MREMAP => ctx.dispatcher.sys_mremap(args, ctx.procs), SYS_MMAP => ctx.dispatcher.sys_mmap(args, ctx.procs, ctx.vfs), SYS_...`
  - `crates/syscall/src/fs_domain.rs:L11-L15`：fn dispatch
    代码片段：`}; pub(crate) fn dispatch( ctx: &mut DispatchContext<'_>, sysno: usize,`

### 相关符号

`fn dispatch` at `crates/syscall/src/fs_domain.rs:L11`、`fn write_console_bytes` at `crates/vfs/src/lib.rs:L57`、`fn write_console_line` at `crates/vfs/src/lib.rs:L64`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn wait_until_queue_notified, fn poll_queue_notified, fn wait_until_queue_notified, fn poll_queue_notified, struct Mutex。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `vendor/virtio-drivers/src/queue.rs` | L24-L28 | 关键词命中 |
| `third_party/virtio-drivers/src/queue.rs` | L24-L28 | 关键词命中 |
| `vendor/virtio-drivers/src/device/blk.rs` | L577-L581 | 关键词命中 |
| `vendor/virtio-drivers/src/transport/fake.rs` | L259-L263 | fn wait_until_queue_notified |
| `vendor/virtio-drivers/src/transport/fake.rs` | L269-L273 | fn poll_queue_notified |
| `third_party/virtio-drivers/src/transport/fake.rs` | L259-L263 | fn wait_until_queue_notified |

### 关键代码片段

  - `vendor/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
  - `third_party/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
  - `vendor/virtio-drivers/src/device/blk.rs:L577-L581`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec}; use core::mem::size_of; use std::{sync::Mutex, thread}; #[test]`
  - `vendor/virtio-drivers/src/transport/fake.rs:L259-L263`：fn wait_until_queue_notified
    代码片段：`/// Waits until the given queue is notified. pub fn wait_until_queue_notified(state: &Mutex<Self>, queue_index: u16) { while !Self::poll_queue_notified(state, queue_index) { thr...`

### 相关符号

`fn wait_until_queue_notified` at `vendor/virtio-drivers/src/transport/fake.rs:L259`、`fn poll_queue_notified` at `vendor/virtio-drivers/src/transport/fake.rs:L269`、`fn wait_until_queue_notified` at `third_party/virtio-drivers/src/transport/fake.rs:L259`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn enable_irq, fn disable_irq, fn ack_irq, fn enable_ptr, fn priority_ptr。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `vendor/virtio-drivers/src/device/blk.rs` | L89-L93 | 关键词命中 |
| `vendor/virtio-drivers/src/device/rng.rs` | L52-L56 | 关键词命中 |
| `vendor/virtio-drivers/src/device/input.rs` | L62-L66 | 关键词命中 |
| `crates/hal-api/src/lib.rs` | L111-L115 | fn enable_irq |
| `crates/hal-api/src/lib.rs` | L112-L116 | fn disable_irq |
| `crates/hal-api/src/lib.rs` | L113-L117 | fn ack_irq |

### 关键代码片段

  - `vendor/virtio-drivers/src/device/blk.rs:L89-L93`：关键词命中
    代码片段：`} /// Acknowledges a pending interrupt, if any. /// /// Returns true if there was an interrupt to acknowledge.`
  - `vendor/virtio-drivers/src/device/rng.rs:L52-L56`：关键词命中
    代码片段：`} /// Acknowledge interrupt. pub fn ack_interrupt(&mut self) -> InterruptStatus { self.transport.ack_interrupt()`
  - `vendor/virtio-drivers/src/device/input.rs:L62-L66`：关键词命中
    代码片段：`} /// Acknowledge interrupt and process events. pub fn ack_interrupt(&mut self) -> InterruptStatus { self.transport.ack_interrupt()`
  - `crates/hal-api/src/lib.rs:L111-L115`：fn enable_irq
    代码片段：`pub trait HalInterrupt: Send + Sync { fn name(&self) -> &'static str; fn enable_irq(&self, irq: usize); fn disable_irq(&self, irq: usize); fn ack_irq(&self, irq: usize);`

### 相关符号

`fn enable_irq` at `crates/hal-api/src/lib.rs:L111`、`fn disable_irq` at `crates/hal-api/src/lib.rs:L112`、`fn ack_irq` at `crates/hal-api/src/lib.rs:L113`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct Dma, fn new, fn paddr, fn vaddr, fn raw_slice。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `vendor/virtio-drivers/src/hal.rs` | L5-L9 | 关键词命中 |
| `vendor/virtio-drivers/src/lib.rs` | L1-L3 | 关键词命中 |
| `vendor/virtio-drivers/src/queue.rs` | L1-L3 | 关键词命中 |
| `vendor/virtio-drivers/src/hal.rs` | L10-L14 | struct Dma |
| `vendor/virtio-drivers/src/hal.rs` | L29-L33 | fn new |
| `vendor/virtio-drivers/src/hal.rs` | L43-L47 | fn paddr |

### 关键代码片段

  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = u64;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
  - `vendor/virtio-drivers/src/queue.rs:L1-L3`：关键词命中
    代码片段：`//! Support for virt queues, the main mechanism for data transport on VirtIO devices. //! //! Types from this module are used to implement VirtIO device drivers. If you just wan...`
  - `vendor/virtio-drivers/src/hal.rs:L10-L14`：struct Dma
    代码片段：`/// A region of contiguous physical memory used for DMA. #[derive(Debug)] pub struct Dma<H: Hal> { paddr: PhysAddr, vaddr: NonNull<u8>,`

### 相关符号

`struct Dma` at `vendor/virtio-drivers/src/hal.rs:L10`、`fn new` at `vendor/virtio-drivers/src/hal.rs:L29`、`fn paddr` at `vendor/virtio-drivers/src/hal.rs:L43`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
