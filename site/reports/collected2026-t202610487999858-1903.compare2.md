# 2026 T202610487999858 比较报告

- 对比历史仓库：2023 LostWakeup
- 生成时间：2026-07-02T05:02:21.789095+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2023 LostWakeup（来源：赛事历史作品）：画像相似度 score=9.27；同属 ucore-variant 风格; 语言构成相似度 0.92; OS 维度重合度 1.00; 代码规模接近度 0.43

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2023 LostWakeup 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mem/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/driver/bio.c:L23-L27`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include "fs/buf.h" #include "dev/virtio.h" struct {`
  - `src/kernel/main.c:L19-L23`：关键词命中
    代码片段：`void plicinit(void); void plicinithart(void); void virtio_disk_init(void); void binit(void); void fileinit(void);`
  - `src/driver/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 与 2023 LostWakeup 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/vfs/fs.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include <defs.h>`
  - `kernel/fs/vfs/ops.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/ops.h" #include <fs/fcntl.h>`
  - `src/fs/dev.c:L1-L5`：关键词命中
    代码片段：`#include "common.h" #include "kernel/trap.h" #include "fs/vfs/fs_macro.h" #include "proc/pcb_life.h" #include "memory/allocator.h"`
  - `src/fs/mpage.c:L4-L8`：关键词命中
    代码片段：`#include "lib/riscv.h" #include "lib/radix-tree.h" #include "fs/vfs/fs.h" #include "fs/bio.h" #include "fs/mpage.h"`
- 与 2023 LostWakeup 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sys/plic.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "proc/plic.h" //`
  - `kernel/trap/riscv/trap.c:L8-L12`：关键词命中
    代码片段：`#include "mem/mem.h" #include "sbi.h" #include "proc/plic.h" #include "dev/virtio.h" #include "lib/string.h"`
  - `src/kernel/trap.c:L1-L3`：关键词命中
    代码片段：`#include "kernel/plic.h" #include "kernel/trap.h" #include "kernel/cpu.h"`
  - `include/kernel/trap.h:L9-L13`：关键词命中
    代码片段：`struct file; // per-process data for the trap handling code in trampoline.S. // sits in a page by itself just under the trampoline page in the // user page table. not specially...`
- 与 2023 LostWakeup 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mem/vm.c:L8-L12`：关键词命中
    代码片段：`#include "defs.h" #include "fs/vfs/fs.h" #include "mem/kalloc.h" #include "lib/string.h" #include "dev/virtio.h"`
  - `kernel/mem/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "mem/kalloc.h" #include <mem/slab.h>`
  - `src/mm/mm.c:L7-L11`：关键词命中
    代码片段：`#include "memory/vma.h" /* allocate a mm_struct, with root pagetable and head_vma */ struct mm_struct *alloc_mm() { struct mm_struct *mm;`
  - `src/mm/vm.c:L17-L21`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
- 与 2023 LostWakeup 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/exec.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h" #include "lib/elf.h"`
  - `kernel/proc/pipe.c:L4-L8`：关键词命中
    代码片段：`#include "param.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "lock/sleeplock.h" #include "fs/fcntl.h"`
  - `src/mm/mm.c:L4-L8`：关键词命中
    代码片段：`#include "memory/allocator.h" #include "debug.h" #include "proc/pcb_mm.h" #include "memory/vma.h"`
  - `src/mm/vm.c:L7-L11`：关键词命中
    代码片段：`#include "debug.h" #include "memory/allocator.h" #include "proc/pcb_life.h" #include "proc/pcb_mm.h" #include "lib/list.h"`
- 与 2023 LostWakeup 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "lock/semaphore.h" #include "defs.h"`
  - `kernel/mem/uart.c:L7-L11`：关键词命中
    代码片段：`#include "mem/memlayout.h" #include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h"`
  - `src/mm/mm.c:L18-L22`：关键词命中
    代码片段：`INIT_LIST_HEAD(&mm->head_vma); // semaphore sema_init(&mm->mmap_sem, 1, "mm_semaphore");`
  - `src/fs/bio.c:L1-L5`：关键词命中
    代码片段：`#include "common.h" #include "param.h" #include "atomic/spinlock.h" #include "lib/riscv.h" #include "fs/bio.h"`
- 与 2023 LostWakeup 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sys/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "lock/spinlock.h" #include "proc/proc.h" #include "sys/syscall.h" #include "defs.h" #include "lib/string.h"`
  - `kernel/trap/riscv/trap.c:L188-L192`：关键词命中
    代码片段：`exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `src/kernel/syscall.c:L10-L14`：关键词命中
    代码片段：`#include "syscall_gen/syscall_num.h" #include "debug.h" #include "kernel/syscall.h" extern atomic_t pages_cnt;`
  - `include/kernel/syscall.h:L4-L8`：关键词命中
    代码片段：`struct file; // syscall.c uint64 argraw(int); int argint(int, int *);`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2023 LostWakeup 在“同步机制”维度发现片段级代码相似度 0.87 （token=0.80, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：char, int, name, s, sem, value, void。（置信度：high）
  证据：
  - `kernel/proc/semaphore.c:L3-L7`：fn sem_init
    代码片段：`#include "defs.h" void sem_init(sem *s, int value, char *name) { s->value = value; s->top = 0;`
  - `src/atomic/semaphore.c:L3-L7`：fn sema_init
    代码片段：`#include "atomic/semaphore.h" void sema_init(sem *S, int value, char *name) { S->value = value; S->wakeup = 0;`
- 与 2023 LostWakeup 在“同步机制”维度发现片段级代码相似度 0.86 （token=0.78, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：acquire, s, sem, value, void。（置信度：high）
  证据：
  - `kernel/proc/semaphore.c:L39-L43`：fn sem_v
    代码片段：`} void sem_v(sem *s) { acquire(&s->lock); s->value++;`
  - `src/atomic/semaphore.c:L22-L26`：fn sema_signal
    代码片段：`} void sema_signal(sem *S) { acquire(&S->sem_lock); S->value++;`
- 宏名重合：与 2023 LostWakeup 在“设备驱动”维度发现 3 个同名定义：ENXIO, ENODEV, ENOSPC。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/fs/ext4/lwext4/ext4_errno.h:L16-L16`：macro ENXIO
    代码片段：`#define ENXIO 6 /* No such device or address */`
  - `include/errno.h:L18-L18`：macro ENXIO
    代码片段：`#define ENXIO 6   /* No such device or address */`
  - `include/fs/ext4/lwext4/ext4_errno.h:L22-L22`：macro ENODEV
    代码片段：`#define ENODEV 19 /* No such device */`
  - `include/errno.h:L30-L30`：macro ENODEV
    代码片段：`#define ENODEV 19  /* No such device */`
- 宏名重合：与 2023 LostWakeup 在“文件系统”维度发现 4 个同名定义：ENOENT, EEXIST, EFBIG, EROFS。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/fs/ext4/lwext4/ext4_errno.h:L14-L14`：macro ENOENT
    代码片段：`#define ENOENT 2 /* No such file or directory */`
  - `include/errno.h:L10-L10`：macro ENOENT
    代码片段：`#define ENOENT 2  /* No such file or directory */`
  - `include/fs/ext4/lwext4/ext4_errno.h:L21-L21`：macro EEXIST
    代码片段：`#define EEXIST 17 /* File exists */`
  - `include/errno.h:L11-L11`：macro EEXIST
    代码片段：`#define EEXIST 17 /* File exists */`
- 宏名重合：与 2023 LostWakeup 在“内存管理”维度发现 3 个同名定义：PTE, VMA, ENOMEM。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/debug.h:L48-L48`：macro PTE
    代码片段：`#define PTE(format, ...) printf(ANSI_FMT(format, ANSI_FG_GREEN), ##__VA_ARGS__)`
  - `include/debug.h:L82-L82`：macro PTE
    代码片段：`#define PTE(format, ...) \`
  - `include/debug.h:L49-L49`：macro VMA
    代码片段：`#define VMA(format, ...) printf(ANSI_FMT(format, ANSI_FG_GREEN), ##__VA_ARGS__)`
  - `include/debug.h:L85-L85`：macro VMA
    代码片段：`#define VMA(format, ...) \`
- 宏名重合：与 2023 LostWakeup 在“系统调用”维度发现 1 个同名定义：EROFS。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/fs/ext4/lwext4/ext4_errno.h:L28-L28`：macro EROFS
    代码片段：`#define EROFS 30 /* Read-only file system */`
  - `include/errno.h:L40-L40`：macro EROFS
    代码片段：`#define EROFS 30  /* Read-only file system */`
- 文件路径重合：与 2023 LostWakeup 在“设备驱动”维度出现同名文件源码路径 `kernel/lib/console.c` / `src/driver/console.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/lib/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `src/driver/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 文件路径重合：与 2023 LostWakeup 在“文件系统”维度出现同名文件源码路径 `kernel/fs/vfs/ops.c` / `src/fs/vfs/ops.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/fs/vfs/ops.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/ops.h" #include <fs/fcntl.h>`
  - `src/fs/vfs/ops.c:L3-L7`：关键词命中
    代码片段：`#include "proc/pcb_life.h" #include "fs/fcntl.h" #include "fs/vfs/fs.h" #include "fs/vfs/ops.h" #include "fs/fat/fat32_file.h"`
- 文件路径重合：与 2023 LostWakeup 在“中断与异常”维度出现同名文件源码路径 `kernel/trap/riscv/trap.c` / `src/kernel/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/trap/riscv/trap.c:L8-L12`：关键词命中
    代码片段：`#include "mem/mem.h" #include "sbi.h" #include "proc/plic.h" #include "dev/virtio.h" #include "lib/string.h"`
  - `src/kernel/trap.c:L1-L3`：关键词命中
    代码片段：`#include "kernel/plic.h" #include "kernel/trap.h" #include "kernel/cpu.h"`
- 文件路径重合：与 2023 LostWakeup 在“中断与异常”维度出现同名文件源码路径 `kernel/trap/loongarch/trap.c` / `src/kernel/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/trap/loongarch/trap.c:L102-L106`：fn pagefault_handler
    代码片段：`} int pagefault_handler(uint64 va, uint64 cause) { /* *TODO: 支持lazy allocation和cow`
  - `src/kernel/trap.c:L1-L3`：关键词命中
    代码片段：`#include "kernel/plic.h" #include "kernel/trap.h" #include "kernel/cpu.h"`
- 文件路径重合：与 2023 LostWakeup 在“内存管理”维度出现同名文件源码路径 `kernel/mem/vm.c` / `src/mm/vm.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/mem/vm.c:L8-L12`：关键词命中
    代码片段：`#include "defs.h" #include "fs/vfs/fs.h" #include "mem/kalloc.h" #include "lib/string.h" #include "dev/virtio.h"`
  - `src/mm/vm.c:L17-L21`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
- 文件路径重合：与 2023 LostWakeup 在“同步机制”维度出现同名文件源码路径 `kernel/proc/semaphore.c` / `src/atomic/semaphore.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/proc/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "lock/semaphore.h" #include "defs.h"`
  - `src/atomic/semaphore.c:L3-L7`：fn sema_init
    代码片段：`#include "atomic/semaphore.h" void sema_init(sem *S, int value, char *name) { S->value = value; S->wakeup = 0;`

## 相似点

- 与 2023 LostWakeup 同属 ucore-variant 风格。（置信度：medium）
- 与 2023 LostWakeup 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mem/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/driver/bio.c:L23-L27`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include "fs/buf.h" #include "dev/virtio.h" struct {`
- 与 2023 LostWakeup 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/vfs/fs.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include <defs.h>`
  - `kernel/fs/vfs/ops.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/ops.h" #include <fs/fcntl.h>`
- 与 2023 LostWakeup 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sys/plic.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "proc/plic.h" //`
  - `kernel/trap/riscv/trap.c:L8-L12`：关键词命中
    代码片段：`#include "mem/mem.h" #include "sbi.h" #include "proc/plic.h" #include "dev/virtio.h" #include "lib/string.h"`
- 与 2023 LostWakeup 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mem/vm.c:L8-L12`：关键词命中
    代码片段：`#include "defs.h" #include "fs/vfs/fs.h" #include "mem/kalloc.h" #include "lib/string.h" #include "dev/virtio.h"`
  - `kernel/mem/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "mem/kalloc.h" #include <mem/slab.h>`
- 与 2023 LostWakeup 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/exec.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h" #include "lib/elf.h"`
  - `kernel/proc/pipe.c:L4-L8`：关键词命中
    代码片段：`#include "param.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "lock/sleeplock.h" #include "fs/fcntl.h"`
- 与 2023 LostWakeup 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "lock/semaphore.h" #include "defs.h"`
  - `kernel/mem/uart.c:L7-L11`：关键词命中
    代码片段：`#include "mem/memlayout.h" #include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h"`
- 与 2023 LostWakeup 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sys/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "lock/spinlock.h" #include "proc/proc.h" #include "sys/syscall.h" #include "defs.h" #include "lib/string.h"`
  - `kernel/trap/riscv/trap.c:L188-L192`：关键词命中
    代码片段：`exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
- 与 2023 LostWakeup 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2023 LostWakeup 的语言构成不同：待测作品为 {'build': 549, 'text': 18, 'markdown': 954, 'c': 282443, 'cpp': 36728, 'asm': 864}，历史样本为 {'json': 20, 'build': 13569, 'markdown': 7891, 'text': 17100, 'c': 1107103, 'make': 88, 'asm': 8391, 'cpp': 38149, 'yaml': 14}。（置信度：medium）

## 可能创新点

- 待测作品包含 90 个历史样本 2023 LostWakeup 中未见的独特函数/类型定义（如 `ext4_bg_set_inode_table_first_block`, `ext4_bmap_is_bit_clr`, `ext4_bmap_bit_clr`, `ext4_dir_en_set_entry_len`, `ext4_dir_en_set_inode`, `ext4_bg_set_free_blocks_count`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `include/fs/ext4/lwext4/ext4_block_group.h:L90-L90`：fn ext4_bg_set_inode_table_first_block
    代码片段：`static inline void ext4_bg_set_inode_table_first_block(struct ext4_bgroup *bg, struct ext4_sblock *s, uint64_t blk) {`
  - `include/fs/ext4/lwext4/ext4_bitmap.h:L35-L35`：fn ext4_bmap_is_bit_clr
    代码片段：`static inline bool ext4_bmap_is_bit_clr(uint8_t *bmap, uint32_t bit) { return !ext4_bmap_is_bit_set(bmap, bit); }`

## 附录：核验摘要

- 关键结论数：27
- 含证据关键结论数：27（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
