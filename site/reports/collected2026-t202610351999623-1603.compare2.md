# 2026 T202610351999623 比较报告

- 对比历史仓库：2024 NPUcore-重生之我是菜狗
- 生成时间：2026-07-02T09:02:01.604284+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 NPUcore-重生之我是菜狗（来源：赛事历史作品）：画像相似度 score=9.15；同属 ucore-variant 风格; 语言构成相似度 0.71; OS 维度重合度 1.00; 代码规模接近度 0.74

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 NPUcore-重生之我是菜狗 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/tty/driver.rs:L1-L3`：关键词命中
    代码片段：`/// tty/driver.rs — TTY 硬件后端管理 /// /// 管理 TTY 后端 (UART 等) 的注册和底层 I/O。`
  - `kernel/src/drivers/mod.rs:L2-L6`：关键词命中
    代码片段：`/// /// 子模块: ///   virtio_blk.rs — Virtio MMIO 块设备驱动 (QEMU virt 平台) pub mod virtio_blk;`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2024 NPUcore-重生之我是菜狗 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod procfs; pub mod ramfs; mod vfs; // ─── 常量 ─────────────────────────────────────────────────`
  - `kernel/src/fs/vfs.rs:L1-L4`：关键词命中
    代码片段：`use super::{ devfs, ext4, procfs, ramfs, FsOps, FsType, GlobalFile, Inode, InodeType, MountEntry, MAX_FILES, MAX_INODES, MAX_MOUNTS, MOUNT_DEVFS, MOUNT_EXT4, MOUNT_PROCFS, MOUNT...`
  - `os/src/fs/mod.rs:L111-L115`：关键词命中
    代码片段：`pub fn get_cwd(&self) -> Option<String> { let inode = self.file.get_dirtree_node(); let inode = match inode { Some(inode) => inode,`
  - `os/src/fs/cache.rs:L193-L197`：关键词命中
    代码片段：`/// Every time kernel tried to alloc this pagecache this number will increase 1(at most 1) /// Every time out of memory occurred this number will decrease 1(at least 0) /// When...`
- 与 2024 NPUcore-重生之我是菜狗 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/hal/irq.rs:L4-L8`：关键词命中
    代码片段：`#[inline] pub fn disable() -> bool { crate::arch::riscv64::trap::interrupt::disable() }`
  - `kernel/src/hal/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 上下文抽象。 /// 用户态 trap 帧。`
  - `os/src/timer.rs:L21-L25`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/arch/la64/trap/mod.rs:L3-L7`：关键词命中
    代码片段：`use self::context::GeneralRegs; use super::register::{self, Exception, Interrupt, Trap, ERA}; use super::{pre_start_init, MErrEntry}; use crate::arch::la64::laflex::LAFlexPageTa...`
- 与 2024 NPUcore-重生之我是菜狗 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/mm/fault.rs:L23-L27`：关键词命中
    代码片段：`let vpn = VirtPageNum::from(fault_addr / PAGE_SIZE); // 如果已经有映射, 可能是 COW 写保护, 否则属于权限异常。 // COW 页即使经过 mprotect 调整，也必须先私有化，不能让共享帧 // 重新以可写方式暴露给父子进程。`
  - `kernel/src/mm/frame.rs:L247-L251`：关键词命中
    代码片段：`} /// 查询某物理帧的引用计数（用于 mprotect COW 隔离） pub fn frame_ref_count(ppn: PhysPageNum) -> u16 { let (_, idx) = ppn_to_index(ppn);`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `os/src/mm/map_area.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::Debug; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2024 NPUcore-重生之我是菜狗 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/sched/fd.rs:L9-L13`：关键词命中
    代码片段：`use crate::hal::irq; use crate::sched::task::{FdKind, MAX_FDS}; // ─── FdCallbacks: fd 资源管理回调 ────────────────────────`
  - `kernel/src/sched/mod.rs:L4-L8`：关键词命中
    代码片段：`/// 子模块职责: /// - pid.rs     : PID 位图分配器 + PidHandle RAII 自动回收 /// - task.rs    : 进程控制块 (TCB) 定义 + 内核栈分配/释放 /// - manager.rs : 全局任务管理器 + 时间片轮转调度器 /// - exec.rs    : exec 路径 (ELF...`
  - `os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `os/src/task/task.rs:L387-L391`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 与 2024 NPUcore-重生之我是菜狗 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/klog.rs:L10-L14`：关键词命中
    代码片段：`///   3 = Debug  — 开发调试 ///   4 = Trace  — 热路径追踪 use core::sync::atomic::{AtomicU8, Ordering}; // ─── 日志级别常量 ────────────────────────────────────────`
  - `kernel/src/arch/riscv64/uart.rs:L10-L14`：关键词命中
    代码片段：`//! 注意: 使用了硬编码 QEMU UART 基址址, 本应由 DTB/SBI 提供 use core::sync::atomic::{AtomicUsize, Ordering}; /// UART 基地址 (QEMU virt 平台)`
  - `os/src/fs/mod.rs:L34-L38`：关键词命中
    代码片段：`}; use lazy_static::*; use spin::Mutex; lazy_static! {`
  - `os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::{arch::BLOCK_SZ, config::PAGE_SIZE, drivers::BLOCK_DEVICE};`
- 与 2024 NPUcore-重生之我是菜狗 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/syscall/io.rs:L1-L4`：关键词命中
    代码片段：`use super::check::{validate_user_buffer, validate_user_buffer_write}; /// syscall/io.rs — I/O 类系统调用 /// /// 所有文件I/O通过VFS层分发:`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`/// syscall/ — 系统调用框架 /// /// 微内核用户态 ↔ 内核的唯一合法通道。`
  - `os/src/syscall/fs.rs:L663-L667`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `os/src/syscall/mod.rs:L122-L126`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2024 NPUcore-重生之我是菜狗 在“内存管理”维度发现 4 个同名定义：indexes, from, page_offset, aligned。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/arch/riscv64/mmu/paging.rs:L30-L30`：fn indexes
    代码片段：`fn indexes(&self) -> [usize; MAX_PAGE_LEVELS] {`
  - `os/src/mm/address.rs:L161-L161`：fn indexes
    代码片段：`pub fn indexes<const T: usize>(&self) -> [usize; T] {`
  - `kernel/src/mm/address.rs:L34-L34`：fn from
    代码片段：`fn from(v: usize) -> Self { Self(v) }`
  - `os/src/mm/address.rs:L56-L56`：fn from
    代码片段：`fn from(v: usize) -> Self {`
- 函数/符号名重合：与 2024 NPUcore-重生之我是菜狗 在“文件系统”维度发现 1 个同名定义：read_block。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/fs/ext4/disk_io.rs:L64-L64`：fn read_block
    代码片段：`pub(super) unsafe fn read_block(block_no: u64, buf: &mut [u8]) -> bool {`
  - `os/src/fs/cache.rs:L90-L90`：fn read_block
    代码片段：`pub fn read_block(&mut self, block_id: usize, block_device: &Arc<dyn BlockDevice>) {`
- 结构体/类型重合：与 2024 NPUcore-重生之我是菜狗 在“内存管理”维度发现 4 个同名定义：PhysAddr, VirtAddr, PhysPageNum, VirtPageNum。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/mm/address.rs:L17-L17`：struct PhysAddr
    代码片段：`pub struct PhysAddr(pub usize);         // 物理地址`
  - `os/src/mm/address.rs:L7-L7`：struct PhysAddr
    代码片段：`pub struct PhysAddr(pub usize);`
  - `kernel/src/mm/address.rs:L20-L20`：struct VirtAddr
    代码片段：`pub struct VirtAddr(pub usize);     // 虚拟地址`
  - `os/src/mm/address.rs:L11-L11`：struct VirtAddr
    代码片段：`pub struct VirtAddr(pub usize);`
- 函数/符号名重合：与 2024 NPUcore-重生之我是菜狗 在“同步机制”维度发现 1 个同名定义：read_block。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/fs/ext4/disk_io.rs:L64-L64`：fn read_block
    代码片段：`pub(super) unsafe fn read_block(block_no: u64, buf: &mut [u8]) -> bool {`
  - `os/src/fs/cache.rs:L90-L90`：fn read_block
    代码片段：`pub fn read_block(&mut self, block_id: usize, block_device: &Arc<dyn BlockDevice>) {`
- 结构体/类型重合：与 2024 NPUcore-重生之我是菜狗 在“文件系统”维度发现 1 个同名定义：Stat。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L126-L126`：struct Stat
    代码片段：`pub struct Stat {`
  - `os/src/fs/layout.rs:L99-L99`：struct Stat
    代码片段：`pub struct Stat {`
- 文件路径重合：与 2024 NPUcore-重生之我是菜狗 在“设备驱动”维度出现同名文件源码路径 `kernel/src/hal/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/hal/console.rs:L4-L8`：关键词命中
    代码片段：`#[inline] pub fn putc(c: u8) { crate::arch::riscv64::uart::uart_putc(c); }`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`

## 相似点

- 与 2024 NPUcore-重生之我是菜狗 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 NPUcore-重生之我是菜狗 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/tty/driver.rs:L1-L3`：关键词命中
    代码片段：`/// tty/driver.rs — TTY 硬件后端管理 /// /// 管理 TTY 后端 (UART 等) 的注册和底层 I/O。`
  - `kernel/src/drivers/mod.rs:L2-L6`：关键词命中
    代码片段：`/// /// 子模块: ///   virtio_blk.rs — Virtio MMIO 块设备驱动 (QEMU virt 平台) pub mod virtio_blk;`
- 与 2024 NPUcore-重生之我是菜狗 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod procfs; pub mod ramfs; mod vfs; // ─── 常量 ─────────────────────────────────────────────────`
  - `kernel/src/fs/vfs.rs:L1-L4`：关键词命中
    代码片段：`use super::{ devfs, ext4, procfs, ramfs, FsOps, FsType, GlobalFile, Inode, InodeType, MountEntry, MAX_FILES, MAX_INODES, MAX_MOUNTS, MOUNT_DEVFS, MOUNT_EXT4, MOUNT_PROCFS, MOUNT...`
- 与 2024 NPUcore-重生之我是菜狗 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/hal/irq.rs:L4-L8`：关键词命中
    代码片段：`#[inline] pub fn disable() -> bool { crate::arch::riscv64::trap::interrupt::disable() }`
  - `kernel/src/hal/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 上下文抽象。 /// 用户态 trap 帧。`
- 与 2024 NPUcore-重生之我是菜狗 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/mm/fault.rs:L23-L27`：关键词命中
    代码片段：`let vpn = VirtPageNum::from(fault_addr / PAGE_SIZE); // 如果已经有映射, 可能是 COW 写保护, 否则属于权限异常。 // COW 页即使经过 mprotect 调整，也必须先私有化，不能让共享帧 // 重新以可写方式暴露给父子进程。`
  - `kernel/src/mm/frame.rs:L247-L251`：关键词命中
    代码片段：`} /// 查询某物理帧的引用计数（用于 mprotect COW 隔离） pub fn frame_ref_count(ppn: PhysPageNum) -> u16 { let (_, idx) = ppn_to_index(ppn);`
- 与 2024 NPUcore-重生之我是菜狗 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/sched/fd.rs:L9-L13`：关键词命中
    代码片段：`use crate::hal::irq; use crate::sched::task::{FdKind, MAX_FDS}; // ─── FdCallbacks: fd 资源管理回调 ────────────────────────`
  - `kernel/src/sched/mod.rs:L4-L8`：关键词命中
    代码片段：`/// 子模块职责: /// - pid.rs     : PID 位图分配器 + PidHandle RAII 自动回收 /// - task.rs    : 进程控制块 (TCB) 定义 + 内核栈分配/释放 /// - manager.rs : 全局任务管理器 + 时间片轮转调度器 /// - exec.rs    : exec 路径 (ELF...`
- 与 2024 NPUcore-重生之我是菜狗 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/klog.rs:L10-L14`：关键词命中
    代码片段：`///   3 = Debug  — 开发调试 ///   4 = Trace  — 热路径追踪 use core::sync::atomic::{AtomicU8, Ordering}; // ─── 日志级别常量 ────────────────────────────────────────`
  - `kernel/src/arch/riscv64/uart.rs:L10-L14`：关键词命中
    代码片段：`//! 注意: 使用了硬编码 QEMU UART 基址址, 本应由 DTB/SBI 提供 use core::sync::atomic::{AtomicUsize, Ordering}; /// UART 基地址 (QEMU virt 平台)`
- 与 2024 NPUcore-重生之我是菜狗 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/syscall/io.rs:L1-L4`：关键词命中
    代码片段：`use super::check::{validate_user_buffer, validate_user_buffer_write}; /// syscall/io.rs — I/O 类系统调用 /// /// 所有文件I/O通过VFS层分发:`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`/// syscall/ — 系统调用框架 /// /// 微内核用户态 ↔ 内核的唯一合法通道。`
- 与 2024 NPUcore-重生之我是菜狗 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 NPUcore-重生之我是菜狗 的语言构成不同：待测作品为 {'build': 508, 'markdown': 11117, 'toml': 37, 'rust': 25750, 'asm': 350, 'c': 22}，历史样本为 {'json': 20, 'build': 395, 'markdown': 53, 'rust': 25306, 'asm': 306, 'text': 60, 'c': 383}。（置信度：medium）

## 可能创新点

- 待测作品包含 382 个历史样本 2024 NPUcore-重生之我是菜狗 中未见的独特函数/类型定义（如 `block_io`, `handle_page_fault`, `get_or_alloc_inode_locked`, `mprotect_range`, `vfs_bind_file`, `ramfs_create`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/src/drivers/virtio_blk/io.rs:L121-L121`：fn block_io
    代码片段：`pub(super) unsafe fn block_io(type_: u32, sector: u64, data_ptr: *mut u8, data_len: usize) -> bool {`
  - `kernel/src/mm/fault.rs:L22-L22`：fn handle_page_fault
    代码片段：`pub fn handle_page_fault(addr_space: &mut AddressSpace, fault_addr: usize, is_write: bool) -> bool {`

## 附录：核验摘要

- 关键结论数：21
- 含证据关键结论数：21（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
