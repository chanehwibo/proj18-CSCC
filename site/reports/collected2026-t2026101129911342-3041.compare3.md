# 2026 T2026101129911342 比较报告

- 对比历史仓库：2025 Chronix
- 生成时间：2026-07-02T04:58:55.038706+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 Chronix（来源：赛事历史作品）：画像相似度 score=9.04；同属 ucore-variant 风格; 语言构成相似度 0.86; OS 维度重合度 1.00; 代码规模接近度 0.31

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 Chronix 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/virtio_blk.rs:L1-L3`：关键词命中
    代码片段：`//! 最小 virtio-blk 驱动：探测 + 读扇区。split virtqueue 三环放在一个连续页对齐结构里 //! （desc@0 / avail@256 / used@4096）。恒等映射下内核 VA = PA，故 ring/缓冲地址即设备 //! 可用的物理地址，无需翻译或弹跳缓冲。`
  - `kernel/src/ext4.rs:L1-L4`：关键词命中
    代码片段：`//! 最小 ext4 只读：超级块 + inode(extent, depth 0) + 根目录项 + 按名读文件。 //! 仅支持 4096 块大小（赛事与本地镜像默认）。块经 virtio-blk 读取。 use crate::virtio_blk; use alloc::string::String;`
  - `os/src/drivers/mod.rs:L2-L5`：关键词命中
    代码片段：`pub mod dma; pub mod net; pub mod serial; pub use block::BLOCK_DEVICE;`
  - `os/src/drivers/net/mod.rs:L1-L4`：关键词命中
    代码片段：`#[allow(unused)] pub mod virtio_net; pub mod loopback; use core::{mem, ptr::NonNull};`
- 与 2025 Chronix 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/ext4.rs:L1-L3`：关键词命中
    代码片段：`//! 最小 ext4 只读：超级块 + inode(extent, depth 0) + 根目录项 + 按名读文件。 //! 仅支持 4096 块大小（赛事与本地镜像默认）。块经 virtio-blk 读取。 use crate::virtio_blk;`
  - `os/src/fs/fs.rs:L4-L8`：关键词命中
    代码片段：`use alloc::sync::{Arc, Weak}; use crate::{fs::{tmpfs::{dentry::TmpDentry, inode::{InodeContent, TmpInode, TmpSysInode}}, vfs::{dentry::{global_find_dentry, global_update_dentry,...`
  - `os/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`//! file system module: offer the file system interface //! define the file trait //! impl File for OSInode in 'inode.rs' //! impl Stdin and Stdout in 'stdio.rs' #![allow(missin...`
- 与 2025 Chronix 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/main.rs:L30-L34`：关键词命中
    代码片段：`khal::init_trap(); khal::test_breakpoint(); khal::kprintln!("[KunikOS] trap round-trip ok"); khal::init_paging();`
  - `kernel/src/syscall.rs:L1-L4`：关键词命中
    代码片段：`//! 系统调用分发（架构无关，语义对齐 Linux）。 //! khal 的 trap 入口解出 (号, 参数) 后调用 kunikos_syscall，返回值写回 a0。 use khal::console_putchar;`
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality //! //! For rCore, we have a single trap entry point, namely '__alltraps'. At`
  - `os/src/timer/ffi.rs:L1-L4`：关键词命中
    代码片段：`use alloc::collections::btree_map::Values; use crate::timer::get_current_time_ns; use super::{get_current_time_ms, NSEC_PER_SEC};`
- 与 2025 Chronix 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L5-L9`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate 的大文件 + ELF 加载） const MIN: usize = 16; // 块粒度，满足 align<=16 的全部内核分配`
  - `khal/src/riscv64.rs:L117-L121`：关键词命中
    代码片段：`#[repr(C, align(4096))] struct PageTable([u64; 512]); static mut ROOT: PageTable = PageTable([0; 512]);`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `os/src/mm/user.rs:L2-L6`：关键词命中
    代码片段：`use alloc::sync::Arc; use hal::{addr::{VirtAddr, VirtAddrHal}, constant::{Constant, ConstantsHal}, pagetable::MapPerm, println}; use crate::{mm::vm::UserVmPagesLocker, processor...`
- 与 2025 Chronix 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs.rs:L260-L264`：关键词命中
    代码片段：`} // 合成 /proc、/sys 等只读伪文件（LTP tst 框架启动要读）。 /// /proc/<pid>/X 归一为 /proc/self/X（数字 pid 段映射到当前进程视图）。 fn normalize_proc(name: &str) -> Option<String> {`
  - `kernel/src/main.rs:L144-L148`：关键词命中
    代码片段：`"waitpid", "write", "yield", ]; khal::kprintln!("#### OS COMP TEST GROUP START basic ####");`
  - `os/src/task/fs.rs:L1-L3`：关键词命中
    代码片段：`//! file system support for Task use alloc::{sync::Arc, vec::Vec};`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Task management implementation //! //! Everything about task management, like starting and switching tasks is`
- 与 2025 Chronix 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L3-L7`：关键词命中
    代码片段：`use core::alloc::{GlobalAlloc, Layout}; use core::cell::UnsafeCell; use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate...`
  - `kernel/src/virtio_blk.rs:L5-L9`：关键词命中
    代码片段：`//! 传输层按架构分流：rv64 走 virtio-mmio（0x10001000 槽位扫描）；la64 走 virtio-pci //! （ECAM 枚举 + BAR4 现代能力）。队列布局与 'read()' 主体两架构共用，仅 init/notify 不同。 use core::sync::atomic::{Ordering, fence};...`
  - `os/src/sync/up.rs:L2-L6`：关键词命中
    代码片段：`use core::cell::{UnsafeCell, RefMut,RefCell}; use core::sync::atomic::{AtomicBool, Ordering}; use core::ops::{Deref, DerefMut}; use log::info;`
  - `os/src/sync/mod.rs:L6-L10`：关键词命中
    代码片段：`extern crate alloc; /// safe cell type for uniprocessor systems pub mod mutex; pub mod lazy;`
- 与 2025 Chronix 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `khal/src/riscv64.rs:L20-L24`：关键词命中
    代码片段：`let ret; unsafe { asm!("ecall", inlateout("a0") arg0 => ret, in("a7") eid, options(nostack)); } ret`
  - `os/src/syscall/fd.rs:L1-L5`：关键词命中
    代码片段：`use alloc::sync::Arc; use crate::{fs::{tmpfs::{dentry::TmpDentry, inode::{EmptyFile, TmpSysInode}}, vfs::inode::InodeMode, OpenFlags}, syscall::{SysError, SysResult, SyscallId},...`
  - `os/src/syscall/fs.rs:L17-L21`：关键词命中
    代码片段：`use crate::processor::processor::{current_processor,current_task,current_user_token}; /// syscall: write pub async fn sys_write(fd: usize, buf: usize, len: usize) -> SysResult {...`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2025 Chronix 在“内存管理”维度发现 3 个同名定义：alloc, dealloc, sys_mmap。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/heap.rs:L86-L86`：fn alloc
    代码片段：`unsafe fn alloc(&self, layout: Layout) -> *mut u8 {`
  - `os/src/devices/net.rs:L124-L124`：fn alloc
    代码片段：`pub fn alloc(self: &Arc<Self>) -> Option<NetBuf> {`
  - `kernel/src/heap.rs:L127-L127`：fn dealloc
    代码片段：`unsafe fn dealloc(&self, ptr: *mut u8, layout: Layout) {`
  - `os/src/devices/net.rs:L148-L148`：fn dealloc
    代码片段：`pub fn dealloc(&self, idx: usize) {`
- 函数/符号名重合：与 2025 Chronix 在“系统调用”维度发现 4 个同名定义：sys_clone, sys_execve, sys_mmap, sys_write。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/syscall.rs:L158-L158`：fn sys_clone
    代码片段：`fn sys_clone(flags: usize, new_sp: usize, tls: usize, ctid: usize) -> isize {`
  - `user/src/syscall.rs:L129-L129`：fn sys_clone
    代码片段：`pub fn sys_clone(flags: usize, stack: usize, tls: usize) -> isize {`
  - `kernel/src/syscall.rs:L311-L311`：fn sys_execve
    代码片段：`fn sys_execve(path_ptr: usize) -> isize {`
  - `user/src/syscall.rs:L143-L143`：fn sys_execve
    代码片段：`pub fn sys_execve(path: *const u8, argv: usize, envp: usize) -> isize {`
- 函数/符号名重合：与 2025 Chronix 在“设备驱动”维度发现 2 个同名定义：read_block, read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/ext4.rs:L33-L33`：fn read_block
    代码片段：`fn read_block(blk: u64, buf: &mut [u8]) -> bool {`
  - `os/src/devices/buffer_cache.rs:L79-L79`：fn read_block
    代码片段：`pub fn read_block(&self, block_id: usize, buf: &mut [u8]) {`
  - `kernel/src/virtio_blk.rs:L95-L95`：fn read
    代码片段：`pub fn read(sector: u64, buf: &mut [u8]) -> bool {`
  - `os/src/devices/mod.rs:L197-L197`：fn read
    代码片段：`async fn read(&self, buf: &mut [u8]) -> usize;`
- 函数/符号名重合：与 2025 Chronix 在“文件系统”维度发现 1 个同名定义：read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/fs.rs:L391-L391`：fn read
    代码片段：`pub fn read(fd: usize, buf: usize, len: usize) -> isize {`
  - `os/src/fs/pipe.rs:L83-L83`：fn read
    代码片段：`async fn read(&self, buf: &mut [u8]) -> usize {`
- 函数/符号名重合：与 2025 Chronix 在“同步机制”维度发现 2 个同名定义：read_block, read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/ext4.rs:L33-L33`：fn read_block
    代码片段：`fn read_block(blk: u64, buf: &mut [u8]) -> bool {`
  - `os/src/devices/buffer_cache.rs:L79-L79`：fn read_block
    代码片段：`pub fn read_block(&self, block_id: usize, buf: &mut [u8]) {`
  - `kernel/src/virtio_blk.rs:L95-L95`：fn read
    代码片段：`pub fn read(sector: u64, buf: &mut [u8]) -> bool {`
  - `os/src/devices/mod.rs:L197-L197`：fn read
    代码片段：`async fn read(&self, buf: &mut [u8]) -> usize;`
- 文件路径重合：与 2025 Chronix 在“文件系统”维度出现同名文件源码路径 `kernel/src/fs.rs` / `os/src/fs/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/fs.rs:L5-L9`：struct Node
    代码片段：`use alloc::vec::Vec; struct Node { name: String, data: Vec<u8>,`
  - `os/src/fs/fs.rs:L4-L8`：关键词命中
    代码片段：`use alloc::sync::{Arc, Weak}; use crate::{fs::{tmpfs::{dentry::TmpDentry, inode::{InodeContent, TmpInode, TmpSysInode}}, vfs::{dentry::{global_find_dentry, global_update_dentry,...`
- 文件路径重合：与 2025 Chronix 在“调度与任务管理”维度出现同名文件源码路径 `kernel/src/fs.rs` / `os/src/task/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/fs.rs:L260-L264`：关键词命中
    代码片段：`} // 合成 /proc、/sys 等只读伪文件（LTP tst 框架启动要读）。 /// /proc/<pid>/X 归一为 /proc/self/X（数字 pid 段映射到当前进程视图）。 fn normalize_proc(name: &str) -> Option<String> {`
  - `os/src/task/fs.rs:L1-L3`：关键词命中
    代码片段：`//! file system support for Task use alloc::{sync::Arc, vec::Vec};`

## 相似点

- 与 2025 Chronix 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 Chronix 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/virtio_blk.rs:L1-L3`：关键词命中
    代码片段：`//! 最小 virtio-blk 驱动：探测 + 读扇区。split virtqueue 三环放在一个连续页对齐结构里 //! （desc@0 / avail@256 / used@4096）。恒等映射下内核 VA = PA，故 ring/缓冲地址即设备 //! 可用的物理地址，无需翻译或弹跳缓冲。`
  - `kernel/src/ext4.rs:L1-L4`：关键词命中
    代码片段：`//! 最小 ext4 只读：超级块 + inode(extent, depth 0) + 根目录项 + 按名读文件。 //! 仅支持 4096 块大小（赛事与本地镜像默认）。块经 virtio-blk 读取。 use crate::virtio_blk; use alloc::string::String;`
- 与 2025 Chronix 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/ext4.rs:L1-L3`：关键词命中
    代码片段：`//! 最小 ext4 只读：超级块 + inode(extent, depth 0) + 根目录项 + 按名读文件。 //! 仅支持 4096 块大小（赛事与本地镜像默认）。块经 virtio-blk 读取。 use crate::virtio_blk;`
  - `os/src/fs/fs.rs:L4-L8`：关键词命中
    代码片段：`use alloc::sync::{Arc, Weak}; use crate::{fs::{tmpfs::{dentry::TmpDentry, inode::{InodeContent, TmpInode, TmpSysInode}}, vfs::{dentry::{global_find_dentry, global_update_dentry,...`
- 与 2025 Chronix 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/main.rs:L30-L34`：关键词命中
    代码片段：`khal::init_trap(); khal::test_breakpoint(); khal::kprintln!("[KunikOS] trap round-trip ok"); khal::init_paging();`
  - `kernel/src/syscall.rs:L1-L4`：关键词命中
    代码片段：`//! 系统调用分发（架构无关，语义对齐 Linux）。 //! khal 的 trap 入口解出 (号, 参数) 后调用 kunikos_syscall，返回值写回 a0。 use khal::console_putchar;`
- 与 2025 Chronix 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L5-L9`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate 的大文件 + ELF 加载） const MIN: usize = 16; // 块粒度，满足 align<=16 的全部内核分配`
  - `khal/src/riscv64.rs:L117-L121`：关键词命中
    代码片段：`#[repr(C, align(4096))] struct PageTable([u64; 512]); static mut ROOT: PageTable = PageTable([0; 512]);`
- 与 2025 Chronix 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs.rs:L260-L264`：关键词命中
    代码片段：`} // 合成 /proc、/sys 等只读伪文件（LTP tst 框架启动要读）。 /// /proc/<pid>/X 归一为 /proc/self/X（数字 pid 段映射到当前进程视图）。 fn normalize_proc(name: &str) -> Option<String> {`
  - `kernel/src/main.rs:L144-L148`：关键词命中
    代码片段：`"waitpid", "write", "yield", ]; khal::kprintln!("#### OS COMP TEST GROUP START basic ####");`
- 与 2025 Chronix 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L3-L7`：关键词命中
    代码片段：`use core::alloc::{GlobalAlloc, Layout}; use core::cell::UnsafeCell; use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate...`
  - `kernel/src/virtio_blk.rs:L5-L9`：关键词命中
    代码片段：`//! 传输层按架构分流：rv64 走 virtio-mmio（0x10001000 槽位扫描）；la64 走 virtio-pci //! （ECAM 枚举 + BAR4 现代能力）。队列布局与 'read()' 主体两架构共用，仅 init/notify 不同。 use core::sync::atomic::{Ordering, fence};...`
- 与 2025 Chronix 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `khal/src/riscv64.rs:L20-L24`：关键词命中
    代码片段：`let ret; unsafe { asm!("ecall", inlateout("a0") arg0 => ret, in("a7") eid, options(nostack)); } ret`
  - `os/src/syscall/fd.rs:L1-L5`：关键词命中
    代码片段：`use alloc::sync::Arc; use crate::{fs::{tmpfs::{dentry::TmpDentry, inode::{EmptyFile, TmpSysInode}}, vfs::inode::InodeMode, OpenFlags}, syscall::{SysError, SysResult, SyscallId},...`
- 与 2025 Chronix 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 Chronix 的语言构成不同：待测作品为 {'build': 72, 'markdown': 868, 'rust': 4095, 'toml': 8, 'c': 6}，历史样本为 {'json': 20, 'build': 353, 'markdown': 2055, 'toml': 139, 'make': 405, 'rust': 43626, 'asm': 381}。（置信度：medium）

## 可能创新点

- 待测作品包含 150 个历史样本 2025 Chronix 中未见的独特函数/类型定义（如 `__resume_user`, `__la_enter_user`, `spawn_child`, `pwrite`, `sys_rt_sigprocmask`, `__enter_user`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `khal/src/riscv64.rs:L562-L562`：fn __resume_user
    代码片段：`fn __resume_user(ctx: *mut KernelContext, tf: *const TrapFrame, kstack_top: usize) -> usize;`
  - `khal/src/loongarch64.rs:L446-L446`：fn __la_enter_user
    代码片段：`fn __la_enter_user(entry: usize, user_sp: usize, kstack_top: usize) -> !;`

## 附录：核验摘要

- 关键结论数：22
- 含证据关键结论数：22（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
