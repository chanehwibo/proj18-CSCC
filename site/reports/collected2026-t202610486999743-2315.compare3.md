# 2026 T202610486999743 比较报告

- 对比历史仓库：2025 rustflyer
- 生成时间：2026-07-01T21:07:35.988753+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 rustflyer（来源：赛事历史作品）：画像相似度 score=9.13；同属 rcore-variant 风格; 语言构成相似度 0.70; OS 维度重合度 1.00; 代码规模接近度 0.73

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 rustflyer 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console driver, for text output use core::fmt::{self, Write}; use hal::firmware::Firmware;`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! block device driver pub(crate) mod block;`
  - `kernel/src/osdriver/mod.rs:L4-L8`：关键词命中
    代码片段：`use alloc::{boxed::Box, sync::Arc, vec::Vec}; use driver::{ CHAR_DEVICE, DeviceType, block::{dw_mshc::MMC, virtblk::VirtBlkDevice},`
- 与 2025 rustflyer 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`//! File trait & inode(dir, file, pipe, stdin, stdout) mod epoll;`
  - `os/src/fs/ext4.rs:L1732-L1736`：关键词命中
    代码片段：`} /// Set extended attribute on an EXT4 inode path. pub(crate) fn ext4_setxattr(path: &str, name: &str, value: &[u8]) -> Result<(), i32> { let p = normalize(path);`
  - `lib/ext4/src/fs.rs:L1-L5`：关键词命中
    代码片段：`use alloc::{ffi::CString, sync::Arc}; use config::{inode::InodeType, vfs::MountFlags}; use systype::error::SysResult; use vfs::{`
  - `lib/fat32/src/fs.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{boxed::Box, sync::Arc}; use config::vfs::MountFlags; use driver::BlockDevice; use systype::error::SysResult;`
- 与 2025 rustflyer 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! Timer-related functionality use hal::constant::Constant;`
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality //! //! For rCore, we have a single trap entry point, namely '__alltraps'. At`
  - `kernel/src/trap/mod.rs:L5-L9`：关键词命中
    代码片段：`pub mod trap_syscall; use arch::trap::enable_interrupt; pub use trap_handler::trap_handler;`
  - `lib/arch/src/trap/mod.rs:L3-L7`：关键词命中
    代码片段：`define_arch_mods!(); /// An abstract representation of the trap mode. pub enum TrapMode { /// Traps into a specific address.`
- 与 2025 rustflyer 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/mm.rs:L3-L7`：关键词命中
    代码片段：`use super::kernel_token; use super::memory_set::{ProcSmapEntry, UserVmaInfo}; use super::MemorySet; use super::PageFaultResult; use super::PhysPageNum;`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `kernel/src/vm/mod.rs:L9-L13`：关键词命中
    代码片段：`pub mod iomap; mod page_table; pub use page_table::switch_to_kernel_page_table;`
  - `lib/config/src/mm.rs:L95-L99`：关键词命中
    代码片段：`/// Width of page offset pub const PAGE_OFFSET_WIDTH: usize = 12; /// Width of a physical address in Sv39 pub const PA_WIDTH_SV39: usize = 56; /// Width of a virtual address in...`
- 与 2025 rustflyer 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L1-L3`：关键词命中
    代码片段：`//! Task pid implementation. //! //! Assign PID to the process here.`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Core task types and ready-queue primitives. mod id;`
  - `kernel/src/task/mod.rs:L9-L13`：关键词命中
    代码片段：`pub mod sig_members; pub mod signal; pub mod task; pub mod taskf; pub mod threadgroup;`
  - `kernel/src/task/tid.rs:L26-L30`：关键词命中
    代码片段：`/// 'tid_alloc()' can look for an unused id from ['INIT_PROC_ID'] /// and allocate it for a new thread. pub fn tid_alloc() -> TidHandle { match TID_ALLOCATOR.lock().alloc() {`
- 与 2025 rustflyer 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/sync/spin.rs:L4-L8`：关键词命中
    代码片段：`use core::hint::spin_loop; use core::ops::{Deref, DerefMut}; use core::sync::atomic::{AtomicBool, Ordering}; /// A small SMP-safe spin mutex for short kernel critical sections.`
  - `os/src/fs/mod.rs:L12-L16`：关键词命中
    代码片段：`use alloc::collections::{BTreeMap, BTreeSet}; use core::any::Any; use core::sync::atomic::{AtomicU64, Ordering}; use lazy_static::lazy_static;`
  - `kernel/src/vm/shm.rs:L3-L7`：关键词命中
    代码片段：`use config::mm::{MMAP_END, MMAP_START, PAGE_SIZE}; use mm::address::VirtAddr; use mutex::ShareMutex; use shm::SharedMemory; use systype::{`
  - `kernel/src/logging.rs:L1-L5`：关键词命中
    代码片段：`use core::{ fmt, sync::atomic::{AtomicBool, AtomicUsize, Ordering}, };`
- 与 2025 rustflyer 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L35-L39`：关键词命中
    代码片段：`use crate::signal::{send_signal_to_task, SIGPIPE, SIGXFSZ}; use crate::sync::{SpinMutex, UPSafeCell}; use crate::syscall::ipc::{ ipc_namespace_eq, mqueue_mount_entries, mqueue_m...`
  - `os/src/syscall/ipc.rs:L11-L15`：关键词命中
    代码片段：`current_task_has_deliverable_signal, send_signal_info_to_task, SigInfo, SIGRTMAX, }; use crate::syscall::fs::fd_to_file; use crate::syscall::{clock_gettime_us, TimeSpec}; use al...`
  - `kernel/src/syscall/fs.rs:L241-L245`：关键词命中
    代码片段：`///   files such as stdout. When user calls 'printf' in user space without 'close(STDOUT)' + 'dup', the 'fd' is STDOUT ///   by default. /// - This is a 'async' syscall, which m...`
  - `kernel/src/syscall/mod.rs:L37-L41`：关键词命中
    代码片段：`use user::*; use crate::syscall::time::{sys_adjtimex, sys_clock_adjtime, sys_clock_getres, sys_clock_settime}; pub async fn syscall(syscall_no: usize, args: [usize; 6]) -> usize {`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 rustflyer 在“同步机制”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, inner, new, pub, refcell, self, t, unsafe。（置信度：high）
  证据：
  - `os/src/sync/up.rs:L20-L24`：fn new
    代码片段：`/// User is responsible to guarantee that inner struct is only used in /// uniprocessor. pub unsafe fn new(value: T) -> Self { Self { inner: RefCell::new(value),`
  - `lib/mutex/src/up.rs:L23-L27`：fn new
    代码片段：`/// # Safety /// The caller must ensure that the value is not accessed concurrently. pub unsafe fn new(value: T) -> Self { Self { inner: RefCell::new(value),`
- 与 2025 rustflyer 在“文件系统”维度发现片段级代码相似度 0.60 （token=0.50, structure=0.80）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, impl, new, self。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L32-L36`：fn new
    代码片段：`impl FlockState { fn new() -> Self { Self { shared: BTreeSet::new(),`
  - `lib/ext4/src/fs.rs:L19-L23`：fn new
    代码片段：`impl ExtFsType { pub fn new() -> Arc<Self> { Arc::new(Self { meta: FileSystemTypeMeta::new("ext4"),`
- 与 2025 rustflyer 在“同步机制”维度发现片段级代码相似度 0.75 （token=0.61, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：atomic, atomicbool, core, ordering, sync, use。（置信度：medium）
  证据：
  - `os/src/sync/spin.rs:L4-L8`：关键词命中
    代码片段：`use core::hint::spin_loop; use core::ops::{Deref, DerefMut}; use core::sync::atomic::{AtomicBool, Ordering}; /// A small SMP-safe spin mutex for short kernel critical sections.`
  - `kernel/src/logging.rs:L1-L5`：关键词命中
    代码片段：`use core::{ fmt, sync::atomic::{AtomicBool, AtomicUsize, Ordering}, };`
- 函数/符号名重合：与 2025 rustflyer 在“设备驱动”维度发现 2 个同名定义：write_str, new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/console.rs:L8-L8`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `user/src/console.rs:L11-L11`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `os/src/drivers/block/virtio_blk.rs:L110-L110`：fn new
    代码片段：`pub(crate) fn new() -> Self {`
  - `lib/vfs/src/fanotify/fs/superblock.rs:L29-L29`：fn new
    代码片段：`pub fn new(device: Option<Arc<dyn BlockDevice>>, fs_type: Arc<dyn FileSystemType>) -> Self {`
- 函数/符号名重合：与 2025 rustflyer 在“同步机制”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/drivers/block/virtio_blk.rs:L110-L110`：fn new
    代码片段：`pub(crate) fn new() -> Self {`
  - `lib/vfs/src/fstype.rs:L22-L22`：fn new
    代码片段：`pub fn new(name: &str) -> Self {`
- 结构体/类型重合：与 2025 rustflyer 在“设备驱动”维度发现 1 个同名定义：Stdout。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/console.rs:L5-L5`：struct Stdout
    代码片段：`struct Stdout;`
  - `user/src/console.rs:L8-L8`：struct Stdout
    代码片段：`struct Stdout;`
- 结构体/类型重合：与 2025 rustflyer 在“中断与异常”维度发现 1 个同名定义：Timer。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `hal/src/component/timer/mod.rs:L6-L6`：struct Timer
    代码片段：`pub struct Timer;`
  - `lib/timer/src/timer.rs:L16-L16`：struct Timer
    代码片段：`pub struct Timer {`
- 文件路径重合：与 2025 rustflyer 在“内存管理”维度出现同名文件源码路径 `os/src/mm/mm.rs` / `lib/config/src/mm.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/mm/mm.rs:L3-L7`：关键词命中
    代码片段：`use super::kernel_token; use super::memory_set::{ProcSmapEntry, UserVmaInfo}; use super::MemorySet; use super::PageFaultResult; use super::PhysPageNum;`
  - `lib/config/src/mm.rs:L95-L99`：关键词命中
    代码片段：`/// Width of page offset pub const PAGE_OFFSET_WIDTH: usize = 12; /// Width of a physical address in Sv39 pub const PA_WIDTH_SV39: usize = 56; /// Width of a virtual address in...`
- 文件路径重合：与 2025 rustflyer 在“同步机制”维度出现同名文件源码路径 `os/src/sync/up.rs` / `lib/mutex/src/up.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/sync/up.rs:L10-L14`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>,`
  - `lib/mutex/src/up.rs:L11-L15`：struct UPSafeCell
    代码片段：`/// 'exclusive_access'. #[derive(Debug)] pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>,`
- 文件路径重合：与 2025 rustflyer 在“系统调用”维度出现同名文件源码路径 `os/src/syscall/fs.rs` / `kernel/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L35-L39`：关键词命中
    代码片段：`use crate::signal::{send_signal_to_task, SIGPIPE, SIGXFSZ}; use crate::sync::{SpinMutex, UPSafeCell}; use crate::syscall::ipc::{ ipc_namespace_eq, mqueue_mount_entries, mqueue_m...`
  - `kernel/src/syscall/fs.rs:L241-L245`：关键词命中
    代码片段：`///   files such as stdout. When user calls 'printf' in user space without 'close(STDOUT)' + 'dup', the 'fd' is STDOUT ///   by default. /// - This is a 'async' syscall, which m...`

## 相似点

- 与 2025 rustflyer 同属 rcore-variant 风格。（置信度：medium）
- 与 2025 rustflyer 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console driver, for text output use core::fmt::{self, Write}; use hal::firmware::Firmware;`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! block device driver pub(crate) mod block;`
- 与 2025 rustflyer 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`//! File trait & inode(dir, file, pipe, stdin, stdout) mod epoll;`
  - `os/src/fs/ext4.rs:L1732-L1736`：关键词命中
    代码片段：`} /// Set extended attribute on an EXT4 inode path. pub(crate) fn ext4_setxattr(path: &str, name: &str, value: &[u8]) -> Result<(), i32> { let p = normalize(path);`
- 与 2025 rustflyer 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! Timer-related functionality use hal::constant::Constant;`
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality //! //! For rCore, we have a single trap entry point, namely '__alltraps'. At`
- 与 2025 rustflyer 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/mm.rs:L3-L7`：关键词命中
    代码片段：`use super::kernel_token; use super::memory_set::{ProcSmapEntry, UserVmaInfo}; use super::MemorySet; use super::PageFaultResult; use super::PhysPageNum;`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
- 与 2025 rustflyer 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/id.rs:L1-L3`：关键词命中
    代码片段：`//! Task pid implementation. //! //! Assign PID to the process here.`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Core task types and ready-queue primitives. mod id;`
- 与 2025 rustflyer 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/sync/spin.rs:L4-L8`：关键词命中
    代码片段：`use core::hint::spin_loop; use core::ops::{Deref, DerefMut}; use core::sync::atomic::{AtomicBool, Ordering}; /// A small SMP-safe spin mutex for short kernel critical sections.`
  - `os/src/fs/mod.rs:L12-L16`：关键词命中
    代码片段：`use alloc::collections::{BTreeMap, BTreeSet}; use core::any::Any; use core::sync::atomic::{AtomicU64, Ordering}; use lazy_static::lazy_static;`
- 与 2025 rustflyer 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L35-L39`：关键词命中
    代码片段：`use crate::signal::{send_signal_to_task, SIGPIPE, SIGXFSZ}; use crate::sync::{SpinMutex, UPSafeCell}; use crate::syscall::ipc::{ ipc_namespace_eq, mqueue_mount_entries, mqueue_m...`
  - `os/src/syscall/ipc.rs:L11-L15`：关键词命中
    代码片段：`current_task_has_deliverable_signal, send_signal_info_to_task, SigInfo, SIGRTMAX, }; use crate::syscall::fs::fd_to_file; use crate::syscall::{clock_gettime_us, TimeSpec}; use al...`
- 与 2025 rustflyer 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 rustflyer 的语言构成不同：待测作品为 {'yaml': 48, 'markdown': 7562, 'build': 1095, 'text': 11912, 'rust': 69053, 'asm': 505, 'toml': 23, 'c': 28135, 'json': 11, 'make': 664}，历史样本为 {'json': 20, 'build': 1018, 'text': 17440, 'markdown': 271, 'toml': 83, 'rust': 62889, 'asm': 429, 'c': 159}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：24
- 含证据关键结论数：24（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
