# 2026 T202610486999572 比较报告

- 对比历史仓库：2025 StarryX
- 生成时间：2026-07-02T05:01:44.541922+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 StarryX（来源：赛事历史作品）：画像相似度 score=9.93；同属 ucore-variant 风格; 语言构成相似度 0.99; OS 维度重合度 1.00; 代码规模接近度 0.95

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 StarryX 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/starry-next/vendor/dw_apb_uart/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! Definitions for snps,dw-apb-uart serial driver. //! Uart snps,dw-apb-uart driver in Rust for BST A1000b FADA board. #![no_std]`
  - `kernel/starry-next/vendor/x86/src/apic/ioapic.rs:L29-L33`：关键词命中
    代码片段：`} pub struct IoApic { reg: *mut u32, data: *mut u32,`
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2025 StarryX 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/starry-next/vendor/fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `kernel/starry-next/vendor/fatfs/src/file.rs:L9-L13`：关键词命中
    代码片段：`const MAX_FILE_SIZE: u32 = core::u32::MAX; /// A FAT filesystem file object used for reading and writing data. /// /// This struct is created by the 'open_file' or 'create_file'...`
  - `xapi/src/fs/io.rs:L172-L176`：关键词命中
    代码片段：`with_fs(AT_FDCWD, path, |fs| { PAGE_CACHE_MANAGER .get_cache(fs.write_file(path)?.access(FileFlags::WRITE)?.inode()) .map(|inode| inode.evict_from_pos(length as _)) .unwrap_or(O...`
  - `xapi/src/fs/ctl.rs:L13-L17`：关键词命中
    代码片段：`fd::{Directory, File, get_file_like}, file::FileLike, vfs::VirtDevice, with_fs, with_location, },`
- 与 2025 StarryX 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/starry-next/vendor/x86/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Shared interrupt description and set-up code. //! See the 'bits*::irq' modules for arch-specific portions.`
  - `kernel/starry-next/vendor/timer_list/src/lib.rs:L15-L19`：关键词命中
    代码片段：`/// A trait that all timed events must implement. pub trait TimerEvent { /// Callback function that will be called when the timer expires. fn callback(self, now: TimeValue); }`
  - `xapi/src/fs/fd/timer.rs:L9-L13`：关键词命中
    代码片段：`use xutils::ctypes::{__kernel_clockid_t, TFD_CLOEXEC, sys::itimerspec}; /// Create a new timer file descriptor /// /// # Arguments`
  - `xcore/src/fs/fd/timer.rs:L16-L20`：关键词命中
    代码片段：`use crate::{fs::file::FileLike, task::have_signals}; /// Internal timer state #[derive(Debug, Clone)] struct TimerState {`
- 与 2025 StarryX 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/starry-next/src/mm.rs:L186-L190`：关键词命中
    代码片段：`entry: VirtAddr, user_sp: VirtAddr, heap_bottom: VirtAddr, user_tp: usize, auxv: &[AuxvEntry],`
  - `kernel/starry-next/src/syscall_imp/mm/brk.rs:L39-L43`：关键词命中
    代码片段：`let current_task = current(); let mut return_val: isize = current_task.task_ext().get_heap_top() as isize; let heap_bottom = current_task.task_ext().get_heap_bottom() as usize;...`
  - `xapi/src/mm/brk.rs:L10-L14`：关键词命中
    代码片段：`with_xprocess(|xprocess| { let mut return_val: isize = xprocess.get_heap_top() as isize; let heap_bottom = xprocess.get_heap_bottom(); if addr != 0 && addr >= heap_bottom && add...`
  - `xcore/src/mm/init.rs:L194-L198`：关键词命中
    代码片段：`uspace.write(user_sp, stack_data.as_slice(), PageSize::Size4K)?; let heap_start = VirtAddr::from_usize(crate::config::USER_HEAP_BASE); let heap_size = crate::config::USER_HEAP_S...`
- 与 2025 StarryX 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/starry-next/src/task.rs:L113-L117`：关键词命中
    代码片段：`} fn task_is_live(task: &AxTaskRef) -> bool { task.state() != axtask::TaskState::Exited }`
  - `kernel/starry-next/vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`
  - `xapi/src/task/ctl.rs:L7-L11`：关键词命中
    代码片段：`}; use xcore::task::{get_process, with_uspace}; fn validate_cap_header(header: &mut __user_cap_header_struct) -> LinuxResult<()> {`
  - `xapi/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod exit; mod futex; mod schedule; mod signal; mod thread;`
- 与 2025 StarryX 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/starry-next/vendor/spin/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! Locks that have the same behaviour as a mutex. //! //! The ['Mutex'] in the root of the crate, can be configured using the 'ticket_mutex' feature.`
  - `kernel/starry-next/vendor/lock_api/src/lib.rs:L6-L10`：关键词命中
    代码片段：`// copied, modified, or distributed except according to those terms. //! This library provides type-safe and fully-featured ['Mutex'] and ['RwLock'] //! types which wrap a simpl...`
  - `xapi/src/fs/fd_ops.rs:L4-L8`：关键词命中
    代码片段：`use axfs_ng::{FileFlags, OpenResult}; use axfs_ng_vfs::NodePermission; use axsync::{Mutex, RawMutex}; use xcore::{`
  - `xapi/src/task/exit.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::Ordering; use axtask::{TaskExtRef, current};`
- 与 2025 StarryX 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/starry-next/src/syscall_imp/mod.rs:L8-L12`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{register_trap_handler, SYSCALL}, }; use axsync::Mutex;`
  - `kernel/starry-next/src/syscall_imp/fs/ctl.rs:L1693-L1697`：关键词命中
    代码片段：`} // Directory buffer for getdents64 syscall struct DirBuffer<'a> { buf: &'a mut [u8],`
  - `src/syscall.rs:L4-L8`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use syscalls::Sysno;`
  - `vendor/syscalls/src/syscall/arm.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %r5 // // %r7 is used for the syscall number. // // %r0 is reused for the syscall return value.`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 StarryX 在“系统调用”维度发现片段级代码相似度 0.84 （token=0.75, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：arch, axhal, register_trap_handler, syscall, trap, trapframe, use。（置信度：high）
  证据：
  - `kernel/starry-next/src/syscall_imp/mod.rs:L8-L12`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{register_trap_handler, SYSCALL}, }; use axsync::Mutex;`
  - `src/syscall.rs:L4-L8`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use syscalls::Sysno;`
- 函数/符号名重合：与 2025 StarryX 在“文件系统”维度发现 4 个同名定义：read_at, write_at, new, add_to_fd_table。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/fd_ops.rs:L78-L78`：fn read_at
    代码片段：`fn read_at(&self, _offset: u64, _buf: &mut [u8]) -> LinuxResult<usize> {`
  - `xmodules/xvma/src/lib.rs:L17-L17`：fn read_at
    代码片段：`fn read_at(&self, buf: &mut [u8], offset: u64) -> LinuxResult<usize>;`
  - `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/fd_ops.rs:L81-L81`：fn write_at
    代码片段：`fn write_at(&self, _offset: u64, _buf: &[u8]) -> LinuxResult<usize> {`
  - `xmodules/xcache/src/lib.rs:L36-L36`：fn write_at
    代码片段：`fn write_at(&self, buf: &[u8], offset: u64) -> LinuxResult<usize>;`
- 函数/符号名重合：与 2025 StarryX 在“同步机制”维度发现 3 个同名定义：is_nonblocking, into_any, set_nonblocking。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/eventfd.rs:L41-L41`：fn is_nonblocking
    代码片段：`fn is_nonblocking(&self) -> bool {`
  - `xcore/src/fs/file.rs:L22-L22`：fn is_nonblocking
    代码片段：`fn is_nonblocking(&self) -> bool {`
  - `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/eventfd.rs:L147-L147`：fn into_any
    代码片段：`fn into_any(self: Arc<Self>) -> Arc<dyn core::any::Any + Send + Sync> {`
  - `xcore/src/fs/file.rs:L19-L19`：fn into_any
    代码片段：`fn into_any(self: Arc<Self>) -> Arc<dyn Any + Send + Sync>;`
- 函数/符号名重合：与 2025 StarryX 在“调度与任务管理”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/fs.rs:L563-L563`：fn new
    代码片段：`fn new(kind: ProcNetSysctlKind) -> Self {`
  - `xmodules/xsignal/src/api/process.rs:L51-L51`：fn new
    代码片段：`pub fn new(actions: Arc<Mutex<M, SignalActions>>, default_restorer: usize) -> Self {`
- 函数/符号名重合：与 2025 StarryX 在“系统调用”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/fs.rs:L563-L563`：fn new
    代码片段：`fn new(kind: ProcNetSysctlKind) -> Self {`
  - `xmodules/xsignal/src/arch/aarch64.rs:L34-L34`：fn new
    代码片段：`pub fn new(tf: &TrapFrame) -> Self {`
- 结构体/类型重合：与 2025 StarryX 在“文件系统”维度发现 1 个同名定义：FileLike。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/fd_ops.rs:L75-L75`：trait FileLike
    代码片段：`pub trait FileLike: Send + Sync {`
  - `xcore/src/fs/file.rs:L15-L15`：trait FileLike
    代码片段：`pub trait FileLike: Send + Sync {`
- 结构体/类型重合：与 2025 StarryX 在“同步机制”维度发现 1 个同名定义：FileLike。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/fd_ops.rs:L75-L75`：trait FileLike
    代码片段：`pub trait FileLike: Send + Sync {`
  - `xcore/src/fs/file.rs:L15-L15`：trait FileLike
    代码片段：`pub trait FileLike: Send + Sync {`
- 文件路径重合：与 2025 StarryX 在“文件系统”维度出现同名文件源码路径 `kernel/starry-next/src/syscall_imp/fs/ctl.rs` / `xapi/src/fs/ctl.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/starry-next/src/syscall_imp/fs/ctl.rs:L694-L698`：关键词命中
    代码片段：`.map_err(|_| LinuxError::EIO)?; if read < sector.len() { warn!("mount fat source={source}: short boot sector read len={read}"); return Err(LinuxError::EINVAL); }`
  - `xapi/src/fs/ctl.rs:L13-L17`：关键词命中
    代码片段：`fd::{Directory, File, get_file_like}, file::FileLike, vfs::VirtDevice, with_fs, with_location, },`
- 文件路径重合：与 2025 StarryX 在“中断与异常”维度出现同名文件源码路径 `kernel/starry-next/vendor/x86/src/irq.rs` / `arceos/modules/axhal/src/irq.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/starry-next/vendor/x86/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Shared interrupt description and set-up code. //! See the 'bits*::irq' modules for arch-specific portions.`
  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use alloc::{collections::btree_map::BTreeMap, vec::Vec};`
- 文件路径重合：与 2025 StarryX 在“内存管理”维度出现同名文件源码路径 `kernel/starry-next/src/mm.rs` / `src/mm.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/starry-next/src/mm.rs:L186-L190`：关键词命中
    代码片段：`entry: VirtAddr, user_sp: VirtAddr, heap_bottom: VirtAddr, user_tp: usize, auxv: &[AuxvEntry],`
  - `src/mm.rs:L14-L18`：fn _mm_trace
    代码片段：`use xuspace::is_accessing_user_memory; fn _mm_trace(vaddr: VirtAddr, len: usize) { let xtask = XTaskExt::from_task(&current()); let xprocess = xtask.xprocess();`
- 文件路径重合：与 2025 StarryX 在“内存管理”维度出现同名文件源码路径 `kernel/starry-next/src/syscall_imp/mm/brk.rs` / `xapi/src/mm/brk.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/starry-next/src/syscall_imp/mm/brk.rs:L39-L43`：关键词命中
    代码片段：`let current_task = current(); let mut return_val: isize = current_task.task_ext().get_heap_top() as isize; let heap_bottom = current_task.task_ext().get_heap_bottom() as usize;...`
  - `xapi/src/mm/brk.rs:L10-L14`：关键词命中
    代码片段：`with_xprocess(|xprocess| { let mut return_val: isize = xprocess.get_heap_top() as isize; let heap_bottom = xprocess.get_heap_bottom(); if addr != 0 && addr >= heap_bottom && add...`

## 相似点

- 与 2025 StarryX 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 StarryX 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/starry-next/vendor/dw_apb_uart/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! Definitions for snps,dw-apb-uart serial driver. //! Uart snps,dw-apb-uart driver in Rust for BST A1000b FADA board. #![no_std]`
  - `kernel/starry-next/vendor/x86/src/apic/ioapic.rs:L29-L33`：关键词命中
    代码片段：`} pub struct IoApic { reg: *mut u32, data: *mut u32,`
- 与 2025 StarryX 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/starry-next/vendor/fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `kernel/starry-next/vendor/fatfs/src/file.rs:L9-L13`：关键词命中
    代码片段：`const MAX_FILE_SIZE: u32 = core::u32::MAX; /// A FAT filesystem file object used for reading and writing data. /// /// This struct is created by the 'open_file' or 'create_file'...`
- 与 2025 StarryX 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/starry-next/vendor/x86/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Shared interrupt description and set-up code. //! See the 'bits*::irq' modules for arch-specific portions.`
  - `kernel/starry-next/vendor/timer_list/src/lib.rs:L15-L19`：关键词命中
    代码片段：`/// A trait that all timed events must implement. pub trait TimerEvent { /// Callback function that will be called when the timer expires. fn callback(self, now: TimeValue); }`
- 与 2025 StarryX 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/starry-next/src/mm.rs:L186-L190`：关键词命中
    代码片段：`entry: VirtAddr, user_sp: VirtAddr, heap_bottom: VirtAddr, user_tp: usize, auxv: &[AuxvEntry],`
  - `kernel/starry-next/src/syscall_imp/mm/brk.rs:L39-L43`：关键词命中
    代码片段：`let current_task = current(); let mut return_val: isize = current_task.task_ext().get_heap_top() as isize; let heap_bottom = current_task.task_ext().get_heap_bottom() as usize;...`
- 与 2025 StarryX 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/starry-next/src/task.rs:L113-L117`：关键词命中
    代码片段：`} fn task_is_live(task: &AxTaskRef) -> bool { task.state() != axtask::TaskState::Exited }`
  - `kernel/starry-next/vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`
- 与 2025 StarryX 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/starry-next/vendor/spin/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! Locks that have the same behaviour as a mutex. //! //! The ['Mutex'] in the root of the crate, can be configured using the 'ticket_mutex' feature.`
  - `kernel/starry-next/vendor/lock_api/src/lib.rs:L6-L10`：关键词命中
    代码片段：`// copied, modified, or distributed except according to those terms. //! This library provides type-safe and fully-featured ['Mutex'] and ['RwLock'] //! types which wrap a simpl...`
- 与 2025 StarryX 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/starry-next/src/syscall_imp/mod.rs:L8-L12`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{register_trap_handler, SYSCALL}, }; use axsync::Mutex;`
  - `kernel/starry-next/src/syscall_imp/fs/ctl.rs:L1693-L1697`：关键词命中
    代码片段：`} // Directory buffer for getdents64 syscall struct DirBuffer<'a> { buf: &'a mut [u8],`
- 与 2025 StarryX 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 StarryX 的语言构成不同：待测作品为 {'toml': 10869, 'build': 20701, 'markdown': 36243, 'c': 42244, 'asm': 1098, 'text': 6339, 'rust': 1784054, 'make': 1242, 'json': 282046, 'yaml': 36}，历史样本为 {'json': 282042, 'build': 20984, 'markdown': 48819, 'toml': 10448, 'rust': 1899616, 'text': 6288, 'c': 38244, 'yaml': 36, 'make': 1269, 'asm': 829}。（置信度：medium）

## 可能创新点

- 待测作品包含 258 个历史样本 2025 StarryX 中未见的独特函数/类型定义（如 `get_path_times`, `ax_udp_peek_from`, `ax_udp_recv_from`, `ax_udp_send_to`, `ax_wait_queue_wait`, `open_named_tmpfile`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/starry-next/arceos/api/arceos_posix_api/src/imp/fs.rs:L312-L312`：fn get_path_times
    代码片段：`pub fn get_path_times(path: &str, is_dir: bool) -> (ctypes::timespec, ctypes::timespec, ctypes::timespec) {`
  - `kernel/starry-next/arceos/api/arceos_api/src/lib.rs:L330-L330`：fn ax_udp_peek_from
    代码片段：`pub fn ax_udp_peek_from(socket: &AxUdpSocketHandle, buf: &mut [u8]) -> AxResult<(usize, SocketAddr)>;`

## 附录：核验摘要

- 关键结论数：26
- 含证据关键结论数：26（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
