# 2026 T202610346999652 比较报告

- 对比历史仓库：2025 BlueStar
- 生成时间：2026-07-02T05:00:39.301417+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 BlueStar（来源：赛事历史作品）：画像相似度 score=9.38；同属 ucore-variant 风格; 语言构成相似度 0.83; OS 维度重合度 1.00; 代码规模接近度 0.72

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 BlueStar 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `third_party/rust-patches/axdriver/src/drivers.rs:L9-L13`：关键词命中
    代码片段：`pub use super::dummy::*; use crate::AxDeviceEnum; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `third_party/rust-patches/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `kernel/src/console.rs:L8-L12`：关键词命中
    代码片段：`#[macro_export] /// Kernel print function,it can output to serial macro_rules! kprint { ($fmt: literal $(, $($arg: tt)+)?) => {`
  - `kernel/src/driver/mod.rs:L1-L2`：关键词命中
    代码片段：`mod virtio_blk; pub use self::virtio_blk::*;`
- 与 2025 BlueStar 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/file/fs.rs:L171-L175`：关键词命中
    代码片段：`Kstat { dev: metadata.device, ino: metadata.inode, mode, nlink: metadata.nlink as _,`
  - `kernel/src/file/flock.rs:L12-L16`：关键词命中
    代码片段：`use starry_process::Pid; /// Inode identity: (device, inode number). pub(crate) type InodeId = (u64, u64); type FlockOwner = u64;`
  - `kernel/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`pub mod vfs; pub mod fs_backend; pub mod component;`
  - `kernel/src/fs/vfs/api.rs:L5-L9`：关键词命中
    代码片段：`use log::error; use crate::task::{TASK_MANAER, TASK_MANAGER_INIT}; use crate::fs::vfs::{File, KStat, MountFs, OpenFlags, ROOTFS,  VfsFsError, VfsStat}; use alloc::format; use al...`
- 与 2025 BlueStar 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/task/timer.rs:L110-L114`：关键词命中
    代码片段：`/// The action to take when an alarm fires. enum AlarmAction { /// Interrupt a task and poll an itimer if the queued generation is current. PollITimer { task: WeakAxTaskRef,`
  - `third_party/rust-patches/axcpu/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use memory_addr::VirtAddr;`
  - `kernel/src/trap/mod.rs:L4-L8`：关键词命中
    代码片段：`use crate::{config::*, task::TASK_MANAER, time::set_next_time_interupt}; use log::{debug, error, }; use riscv::register::{scause::{self, Exception, Trap},  sstatus::{self, SPP,...`
  - `kernel/src/time/timer.rs:L27-L31`：关键词命中
    代码片段：`///设置下一次时钟中断(不带中断检查，太耗时间，所有耗时操作其实都不应该出现在这里)，mtimecmp使用原始tick计数 pub fn set_next_time_interupt(){ //需要考虑调用误差，即使错过也没事，只是提前触发中断(mtime < mtimecmp)`
- 与 2025 BlueStar 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/mm/stats.rs:L48-L52`：关键词命中
    代码片段：`cached_bytes: usages.get(UsageKind::PageCache), mapped_bytes: usages.get(UsageKind::VirtMem), page_table_bytes: usages.get(UsageKind::PageTable), } }`
  - `kernel/src/mm/access.rs:L105-L109`：关键词命中
    代码片段：`// TODO: this is inefficient, but we have to do this instead of // querying the page table since the page might has not been // allocated yet. let curr = current();`
  - `kernel/src/memory/mod.rs:L2-L6`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod memset;`
  - `kernel/src/memory/memset.rs:L6-L10`：关键词命中
    代码片段：`use log::{debug, error, trace}; use core::arch::asm; use riscv::register::satp; use crate::fs::vfs::File; use crate::task::{TASK_MANAER};`
- 与 2025 BlueStar 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. mod accounting;`
  - `kernel/src/task/ops.rs:L86-L90`：关键词命中
    代码片段：`} /// Cleanup expired entries in the task tables. /// /// This function is intended to be used during memory leak analysis to remove`
  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`mod task; mod process; use crate::fs::vfs::{OpenFlags, vfs_open};`
  - `kernel/src/task/task.rs:L19-L23`：关键词命中
    代码片段：`use crate::memory::*; use crate::sbi::shutdown; use crate::task::Signal; use crate::task::file_loader; use log::debug;`
- 与 2025 BlueStar 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/syscall/sync/membarrier.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{Ordering, compiler_fence}; use axerrno::{AxError, AxResult};`
  - `kernel/src/time.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicI64, Ordering}; use axerrno::{AxError, AxResult};`
  - `kernel/src/sync/up.rs:L19-L23`：关键词命中
    代码片段：`#[track_caller] pub fn lock(&self)->core::cell::RefMut<'_,T>{ match self.inner.try_borrow_mut() { Ok(g) => g,`
  - `kernel/vendor/spin/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! Locks that have the same behaviour as a mutex. //! //! The ['Mutex'] in the root of the crate, can be configured using the 'ticket_mutex' feature.`
- 与 2025 BlueStar 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/syscall/mod.rs:L114-L118`：关键词命中
    代码片段：`/// Fast path for trivial getter syscalls that only read from the current /// thread and return a scalar: no user copy, no restart, no blocking, no /// timer accounting. Handlin...`
  - `kernel/src/syscall/fs/io.rs:L197-L201`：关键词命中
    代码片段：`pub fn sys_dummy_fd(sysno: Sysno) -> AxResult<isize> { warn!("Unimplemented fd syscall: {sysno}"); Err(AxError::Unsupported) }`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`mod syscall; use log::{error}; use crate::memory::VirAddr;`
  - `kernel/src/syscall/syscall.rs:L1272-L1276`：关键词命中
    代码片段：`/// 返回：成功返回映射起始地址；失败返回 -1 /// /// 参数说明（Linux riscv64 ABI，用户态用 ecall 传参）： /// 'addr'  : 映射起始虚拟地址（用户 hint）。若带 'MAP_FIXED' 则必须使用该地址。 /// 'len'   : 映射长度（字节）。内核按页对齐到覆盖该区间。`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2025 BlueStar 在“设备驱动”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/axnet-ng/src/device/ethernet.rs:L39-L39`：fn new
    代码片段：`pub fn new(name: String, inner: AxNetDevice, ip: Ipv4Cidr) -> Self {`
  - `user/src/console.rs:L33-L33`：fn new
    代码片段：`pub fn new(len:usize)->Self{`
- 函数/符号名重合：与 2025 BlueStar 在“同步机制”维度发现 1 个同名定义：lock。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/axnet-ng/src/state.rs:L49-L49`：fn lock
    代码片段：`pub fn lock(&self, expect: State) -> Result<StateGuard<'_>, State> {`
  - `user/vendor/spin-0.7.1/src/mutex.rs:L172-L172`：fn lock
    代码片段：`pub fn lock(&self) -> MutexGuard<T> {`
- 文件路径重合：与 2025 BlueStar 在“中断与异常”维度出现同名文件源码路径 `kernel/src/task/timer.rs` / `kernel/src/time/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/task/timer.rs:L110-L114`：关键词命中
    代码片段：`/// The action to take when an alarm fires. enum AlarmAction { /// Interrupt a task and poll an itimer if the queued generation is current. PollITimer { task: WeakAxTaskRef,`
  - `kernel/src/time/timer.rs:L27-L31`：关键词命中
    代码片段：`///设置下一次时钟中断(不带中断检查，太耗时间，所有耗时操作其实都不应该出现在这里)，mtimecmp使用原始tick计数 pub fn set_next_time_interupt(){ //需要考虑调用误差，即使错过也没事，只是提前触发中断(mtime < mtimecmp)`

## 相似点

- 与 2025 BlueStar 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 BlueStar 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `third_party/rust-patches/axdriver/src/drivers.rs:L9-L13`：关键词命中
    代码片段：`pub use super::dummy::*; use crate::AxDeviceEnum; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `third_party/rust-patches/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
- 与 2025 BlueStar 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/file/fs.rs:L171-L175`：关键词命中
    代码片段：`Kstat { dev: metadata.device, ino: metadata.inode, mode, nlink: metadata.nlink as _,`
  - `kernel/src/file/flock.rs:L12-L16`：关键词命中
    代码片段：`use starry_process::Pid; /// Inode identity: (device, inode number). pub(crate) type InodeId = (u64, u64); type FlockOwner = u64;`
- 与 2025 BlueStar 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/task/timer.rs:L110-L114`：关键词命中
    代码片段：`/// The action to take when an alarm fires. enum AlarmAction { /// Interrupt a task and poll an itimer if the queued generation is current. PollITimer { task: WeakAxTaskRef,`
  - `third_party/rust-patches/axcpu/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use memory_addr::VirtAddr;`
- 与 2025 BlueStar 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/mm/stats.rs:L48-L52`：关键词命中
    代码片段：`cached_bytes: usages.get(UsageKind::PageCache), mapped_bytes: usages.get(UsageKind::VirtMem), page_table_bytes: usages.get(UsageKind::PageTable), } }`
  - `kernel/src/mm/access.rs:L105-L109`：关键词命中
    代码片段：`// TODO: this is inefficient, but we have to do this instead of // querying the page table since the page might has not been // allocated yet. let curr = current();`
- 与 2025 BlueStar 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. mod accounting;`
  - `kernel/src/task/ops.rs:L86-L90`：关键词命中
    代码片段：`} /// Cleanup expired entries in the task tables. /// /// This function is intended to be used during memory leak analysis to remove`
- 与 2025 BlueStar 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/syscall/sync/membarrier.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{Ordering, compiler_fence}; use axerrno::{AxError, AxResult};`
  - `kernel/src/time.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicI64, Ordering}; use axerrno::{AxError, AxResult};`
- 与 2025 BlueStar 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/syscall/mod.rs:L114-L118`：关键词命中
    代码片段：`/// Fast path for trivial getter syscalls that only read from the current /// thread and return a scalar: no user copy, no restart, no blocking, no /// timer accounting. Handlin...`
  - `kernel/src/syscall/fs/io.rs:L197-L201`：关键词命中
    代码片段：`pub fn sys_dummy_fd(sysno: Sysno) -> AxResult<isize> { warn!("Unimplemented fd syscall: {sysno}"); Err(AxError::Unsupported) }`
- 与 2025 BlueStar 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 BlueStar 的语言构成不同：待测作品为 {'build': 4608, 'markdown': 2579, 'rust': 167003, 'yaml': 67, 'text': 1083, 'make': 1158, 'toml': 513, 'c': 31276, 'asm': 1023, 'json': 63}，历史样本为 {'json': 53, 'build': 2684, 'markdown': 10844, 'toml': 9593, 'c': 336, 'rust': 279800, 'asm': 641, 'text': 5383, 'yaml': 51}。（置信度：medium）

## 可能创新点

- 待测作品包含 225 个历史样本 2025 BlueStar 中未见的独特函数/类型定义（如 `receive`, `snoop_tcp_packet`, `handle_vsock_event`, `set_filter`, `try_send`, `on_data_received`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `crates/axnet-ng/src/router.rs:L248-L248`：fn receive
    代码片段：`fn receive(&mut self, _timestamp: Instant) -> Option<(Self::RxToken<'_>, Self::TxToken<'_>)> {`
  - `crates/axnet-ng/src/router.rs:L194-L194`：fn snoop_tcp_packet
    代码片段：`fn snoop_tcp_packet(buf: &[u8], sockets: &mut SocketSet<'_>, listen_table: &ListenTable) {`

## 附录：核验摘要

- 关键结论数：18
- 含证据关键结论数：18（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
