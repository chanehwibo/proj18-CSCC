# 2026 T2026101129911342 比较报告

- 对比历史仓库：uCore OS Lab
- 生成时间：2026-07-02T04:58:54.904877+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- uCore OS Lab（来源：教学基线）：画像相似度 score=9.28；同属 ucore-variant 风格; 语言构成相似度 0.86; OS 维度重合度 1.00; 代码规模接近度 0.57

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 uCore OS Lab 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/virtio_blk.rs:L1-L3`：关键词命中
    代码片段：`//! 最小 virtio-blk 驱动：探测 + 读扇区。split virtqueue 三环放在一个连续页对齐结构里 //! （desc@0 / avail@256 / used@4096）。恒等映射下内核 VA = PA，故 ring/缓冲地址即设备 //! 可用的物理地址，无需翻译或弹跳缓冲。`
  - `kernel/src/ext4.rs:L1-L4`：关键词命中
    代码片段：`//! 最小 ext4 只读：超级块 + inode(extent, depth 0) + 根目录项 + 按名读文件。 //! 仅支持 4096 块大小（赛事与本地镜像默认）。块经 virtio-blk 读取。 use crate::virtio_blk; use alloc::string::String;`
  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
  - `os/src/drivers/mod.rs:L8-L12`：关键词命中
    代码片段：`pub use block::BLOCK_DEVICE; pub use bus::*; pub use chardev::UART; pub use gpu::*; pub use input::*;`
- 与 uCore OS Lab 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/ext4.rs:L1-L3`：关键词命中
    代码片段：`//! 最小 ext4 只读：超级块 + inode(extent, depth 0) + 根目录项 + 按名读文件。 //! 仅支持 4096 块大小（赛事与本地镜像默认）。块经 virtio-blk 读取。 use crate::virtio_blk;`
  - `os/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`mod inode; mod pipe; mod stdio;`
  - `os/src/fs/inode.rs:L6-L10`：关键词命中
    代码片段：`use alloc::vec::Vec; use bitflags::*; use easy_fs::{EasyFileSystem, Inode}; use lazy_static::*;`
- 与 uCore OS Lab 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/main.rs:L30-L34`：关键词命中
    代码片段：`khal::init_trap(); khal::test_breakpoint(); khal::kprintln!("[KunikOS] trap round-trip ok"); khal::init_paging();`
  - `kernel/src/syscall.rs:L1-L4`：关键词命中
    代码片段：`//! 系统调用分发（架构无关，语义对齐 Linux）。 //! khal 的 trap 入口解出 (号, 参数) 后调用 kunikos_syscall，返回值写回 a0。 use khal::console_putchar;`
  - `os/src/timer.rs:L63-L67`：关键词命中
    代码片段：`let current_ms = get_time_ms(); TIMERS.exclusive_session(|timers| { while let Some(timer) = timers.peek() { if timer.expire_ms <= current_ms { wakeup_task(Arc::clone(&timer.task));`
  - `os/src/trap/mod.rs:L7-L11`：关键词命中
    代码片段：`current_user_token, exit_current_and_run_next, suspend_current_and_run_next, SignalFlags, }; use crate::timer::{check_timer, set_next_trigger}; use core::arch::{asm, global_asm}...`
- 与 uCore OS Lab 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L5-L9`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate 的大文件 + ELF 加载） const MIN: usize = 16; // 块粒度，满足 align<=16 的全部内核分配`
  - `khal/src/riscv64.rs:L117-L121`：关键词命中
    代码片段：`#[repr(C, align(4096))] struct PageTable([u64; 512]); static mut ROOT: PageTable = PageTable([0; 512]);`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/memory_set.rs:L1-L3`：关键词命中
    代码片段：`use super::{frame_alloc, FrameTracker}; use super::{PTEFlags, PageTable, PageTableEntry}; use super::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum};`
- 与 uCore OS Lab 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs.rs:L260-L264`：关键词命中
    代码片段：`} // 合成 /proc、/sys 等只读伪文件（LTP tst 框架启动要读）。 /// /proc/<pid>/X 归一为 /proc/self/X（数字 pid 段映射到当前进程视图）。 fn normalize_proc(name: &str) -> Option<String> {`
  - `kernel/src/main.rs:L144-L148`：关键词命中
    代码片段：`"waitpid", "write", "yield", ]; khal::kprintln!("#### OS COMP TEST GROUP START basic ####");`
  - `os/src/task/mod.rs:L7-L11`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
  - `os/src/task/switch.S:L13-L17`：关键词命中
    代码片段：`#     next_task_cx_ptr: *const TaskContext # ) # save kernel stack of current task sd sp, 8(a0) # save ra & s0~s11 of current execution`
- 与 uCore OS Lab 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L3-L7`：关键词命中
    代码片段：`use core::alloc::{GlobalAlloc, Layout}; use core::cell::UnsafeCell; use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate...`
  - `kernel/src/virtio_blk.rs:L5-L9`：关键词命中
    代码片段：`//! 传输层按架构分流：rv64 走 virtio-mmio（0x10001000 槽位扫描）；la64 走 virtio-pci //! （ECAM 枚举 + BAR4 现代能力）。队列布局与 'read()' 主体两架构共用，仅 init/notify 不同。 use core::sync::atomic::{Ordering, fence};...`
  - `os/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`mod condvar; mod mutex; mod semaphore;`
  - `os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub trait Mutex: Sync + Send { fn lock(&self); fn unlock(&self);`
- 与 uCore OS Lab 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `khal/src/riscv64.rs:L20-L24`：关键词命中
    代码片段：`let ret; unsafe { asm!("ecall", inlateout("a0") arg0 => ret, in("a7") eid, options(nostack)); } ret`
  - `os/src/syscall/mod.rs:L45-L49`：关键词命中
    代码片段：`use thread::*; pub fn syscall(syscall_id: usize, args: [usize; 3]) -> isize { match syscall_id { SYSCALL_DUP => sys_dup(args[0]),`
  - `os/src/sbi.rs:L19-L23`：关键词命中
    代码片段：`core::arch::asm!( "li x16, 0", "ecall", inlateout("x10") arg0 => ret, in("x11") arg1,`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 uCore OS Lab 在“文件系统”维度发现 3 个同名定义：close, read, dup。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/fs.rs:L382-L382`：fn close
    代码片段：`pub fn close(fd: usize) -> isize {`
  - `user/src/file.rs:L19-L19`：fn close
    代码片段：`pub fn close(fd: usize) -> isize {`
  - `kernel/src/fs.rs:L391-L391`：fn read
    代码片段：`pub fn read(fd: usize, buf: usize, len: usize) -> isize {`
  - `user/src/file.rs:L25-L25`：fn read
    代码片段：`pub fn read(fd: usize, buf: &mut [u8]) -> isize {`
- 函数/符号名重合：与 uCore OS Lab 在“系统调用”维度发现 4 个同名定义：sys_write, sbi_call, console_putchar, shutdown。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/syscall.rs:L581-L581`：fn sys_write
    代码片段：`fn sys_write(fd: usize, buf: usize, len: usize) -> isize {`
  - `user/src/syscall.rs:L70-L70`：fn sys_write
    代码片段：`pub fn sys_write(fd: usize, buffer: &[u8]) -> isize {`
  - `khal/src/riscv64.rs:L19-L19`：fn sbi_call
    代码片段：`fn sbi_call(eid: usize, arg0: usize) -> usize {`
  - `os/src/sbi.rs:L16-L16`：fn sbi_call
    代码片段：`fn sbi_call(which: usize, arg0: usize, arg1: usize, arg2: usize) -> usize {`
- 函数/符号名重合：与 uCore OS Lab 在“设备驱动”维度发现 1 个同名定义：console_putchar。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `khal/src/loongarch64.rs:L20-L20`：fn console_putchar
    代码片段：`pub fn console_putchar(c: u8) {`
  - `os/src/sbi.rs:L35-L35`：fn console_putchar
    代码片段：`pub fn console_putchar(c: usize) {`
- 函数/符号名重合：与 uCore OS Lab 在“中断与异常”维度发现 1 个同名定义：set_timer。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `khal/src/riscv64.rs:L255-L255`：fn set_timer
    代码片段：`fn set_timer(future: u64) {`
  - `os/src/sbi.rs:L31-L31`：fn set_timer
    代码片段：`pub fn set_timer(timer: usize) {`
- 函数/符号名重合：与 uCore OS Lab 在“内存管理”维度发现 2 个同名定义：alloc, dealloc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L86-L86`：fn alloc
    代码片段：`unsafe fn alloc(&self, layout: Layout) -> *mut u8 {`
  - `os/src/mm/frame_allocator.rs:L37-L37`：fn alloc
    代码片段：`fn alloc(&mut self) -> Option<PhysPageNum>;`
  - `kernel/src/heap.rs:L127-L127`：fn dealloc
    代码片段：`unsafe fn dealloc(&self, ptr: *mut u8, layout: Layout) {`
  - `os/src/mm/frame_allocator.rs:L38-L38`：fn dealloc
    代码片段：`fn dealloc(&mut self, ppn: PhysPageNum);`
- 文件路径重合：与 uCore OS Lab 在“文件系统”维度出现同名文件源码路径 `kernel/src/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/fs.rs:L5-L9`：struct Node
    代码片段：`use alloc::vec::Vec; struct Node { name: String, data: Vec<u8>,`
  - `os/src/syscall/fs.rs:L48-L52`：关键词命中
    代码片段：`let token = current_user_token(); let path = translated_str(token, path); if let Some(inode) = open_file(path.as_str(), OpenFlags::from_bits(flags).unwrap()) { let mut inner = p...`
- 文件路径重合：与 uCore OS Lab 在“系统调用”维度出现同名文件源码路径 `kernel/src/syscall.rs` / `user/src/syscall.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/syscall.rs:L143-L147`：fn reset_pids
    代码片段：`/// 每个程序启动前重置进程态（与 fs::reset 同步调用）。 pub fn reset_pids() { unsafe { NEXT_PID = 3;`
  - `user/src/syscall.rs:L31-L35`：关键词命中
    代码片段：`const SYSCALL_KEY_PRESSED: usize = 3001; fn syscall(id: usize, args: [usize; 3]) -> isize { let mut ret: isize; unsafe {`

## 相似点

- 与 uCore OS Lab 同属 ucore-variant 风格。（置信度：medium）
- 与 uCore OS Lab 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/virtio_blk.rs:L1-L3`：关键词命中
    代码片段：`//! 最小 virtio-blk 驱动：探测 + 读扇区。split virtqueue 三环放在一个连续页对齐结构里 //! （desc@0 / avail@256 / used@4096）。恒等映射下内核 VA = PA，故 ring/缓冲地址即设备 //! 可用的物理地址，无需翻译或弹跳缓冲。`
  - `kernel/src/ext4.rs:L1-L4`：关键词命中
    代码片段：`//! 最小 ext4 只读：超级块 + inode(extent, depth 0) + 根目录项 + 按名读文件。 //! 仅支持 4096 块大小（赛事与本地镜像默认）。块经 virtio-blk 读取。 use crate::virtio_blk; use alloc::string::String;`
- 与 uCore OS Lab 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/ext4.rs:L1-L3`：关键词命中
    代码片段：`//! 最小 ext4 只读：超级块 + inode(extent, depth 0) + 根目录项 + 按名读文件。 //! 仅支持 4096 块大小（赛事与本地镜像默认）。块经 virtio-blk 读取。 use crate::virtio_blk;`
  - `os/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`mod inode; mod pipe; mod stdio;`
- 与 uCore OS Lab 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/main.rs:L30-L34`：关键词命中
    代码片段：`khal::init_trap(); khal::test_breakpoint(); khal::kprintln!("[KunikOS] trap round-trip ok"); khal::init_paging();`
  - `kernel/src/syscall.rs:L1-L4`：关键词命中
    代码片段：`//! 系统调用分发（架构无关，语义对齐 Linux）。 //! khal 的 trap 入口解出 (号, 参数) 后调用 kunikos_syscall，返回值写回 a0。 use khal::console_putchar;`
- 与 uCore OS Lab 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L5-L9`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate 的大文件 + ELF 加载） const MIN: usize = 16; // 块粒度，满足 align<=16 的全部内核分配`
  - `khal/src/riscv64.rs:L117-L121`：关键词命中
    代码片段：`#[repr(C, align(4096))] struct PageTable([u64; 512]); static mut ROOT: PageTable = PageTable([0; 512]);`
- 与 uCore OS Lab 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs.rs:L260-L264`：关键词命中
    代码片段：`} // 合成 /proc、/sys 等只读伪文件（LTP tst 框架启动要读）。 /// /proc/<pid>/X 归一为 /proc/self/X（数字 pid 段映射到当前进程视图）。 fn normalize_proc(name: &str) -> Option<String> {`
  - `kernel/src/main.rs:L144-L148`：关键词命中
    代码片段：`"waitpid", "write", "yield", ]; khal::kprintln!("#### OS COMP TEST GROUP START basic ####");`
- 与 uCore OS Lab 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L3-L7`：关键词命中
    代码片段：`use core::alloc::{GlobalAlloc, Layout}; use core::cell::UnsafeCell; use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate...`
  - `kernel/src/virtio_blk.rs:L5-L9`：关键词命中
    代码片段：`//! 传输层按架构分流：rv64 走 virtio-mmio（0x10001000 槽位扫描）；la64 走 virtio-pci //! （ECAM 枚举 + BAR4 现代能力）。队列布局与 'read()' 主体两架构共用，仅 init/notify 不同。 use core::sync::atomic::{Ordering, fence};...`
- 与 uCore OS Lab 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `khal/src/riscv64.rs:L20-L24`：关键词命中
    代码片段：`let ret; unsafe { asm!("ecall", inlateout("a0") arg0 => ret, in("a7") eid, options(nostack)); } ret`
  - `os/src/syscall/mod.rs:L45-L49`：关键词命中
    代码片段：`use thread::*; pub fn syscall(syscall_id: usize, args: [usize; 3]) -> isize { match syscall_id { SYSCALL_DUP => sys_dup(args[0]),`
- 与 uCore OS Lab 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 uCore OS Lab 的语言构成不同：待测作品为 {'build': 72, 'markdown': 868, 'rust': 4095, 'toml': 8, 'c': 6}，历史样本为 {'json': 18, 'markdown': 314, 'build': 261, 'toml': 5, 'rust': 9932, 'asm': 150}。（置信度：medium）

## 可能创新点

- 待测作品包含 160 个历史样本 uCore OS Lab 中未见的独特函数/类型定义（如 `__resume_user`, `sys_clone`, `__la_enter_user`, `spawn_child`, `pwrite`, `sys_rt_sigprocmask`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `khal/src/riscv64.rs:L562-L562`：fn __resume_user
    代码片段：`fn __resume_user(ctx: *mut KernelContext, tf: *const TrapFrame, kstack_top: usize) -> usize;`
  - `kernel/src/syscall.rs:L158-L158`：fn sys_clone
    代码片段：`fn sys_clone(flags: usize, new_sp: usize, tls: usize, ctid: usize) -> isize {`

## 附录：核验摘要

- 关键结论数：22
- 含证据关键结论数：22（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
