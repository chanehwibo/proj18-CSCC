# 2026 T202610486999803 比较报告

- 对比历史仓库：2023 AVX
- 生成时间：2026-07-02T09:02:59.620982+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2023 AVX（来源：赛事历史作品）：画像相似度 score=9.38；同属 ucore-variant 风格; 语言构成相似度 0.94; OS 维度重合度 1.00; 代码规模接近度 0.49

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2023 AVX 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/drivers/char/uart.c:L1-L3`：关键词命中
    代码片段：`#include "drivers/char/uart.h" #include "drivers/core/driver_core.h" #include "drivers/core/driver_class.h"`
  - `kernel/drivers/block/loop.c:L54-L58`：关键词命中
    代码片段：`for (int i = 0; i < MAX_LOOP_DEVS; i++) { memset(&g_loop[i], 0, sizeof(g_loop[i])); /* LOCK_ORDER: initialize each per-device loop lock at boot. */ spin_init(&g_loop[i].lock); g...`
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 与 2023 AVX 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/vfs.c:L1-L4`：关键词命中
    代码片段：`/* * A20OS — Virtual Filesystem (VFS) * * Provides a unified fd-based interface over:`
  - `kernel/fs/ext4.c:L1-L5`：关键词命中
    代码片段：`#include "fs/ext4.h" #include "fs/file.h" #include "fs/vfs.h" #include "fs/block_cache.h" #include "mm/mm.h"`
  - `kernel/file.c:L206-L210`：关键词命中
    代码片段：`// Read from file f. // addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `kernel/include/file.h:L68-L72`：关键词命中
    代码片段：`struct file*    filedup(struct file*); void            fileinit(void); int             fileread(struct file*, uint64, int n); int             filestat(struct file*, uint64 addr)...`
- 与 2023 AVX 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/core/trap.c:L1-L3`：关键词命中
    代码片段：`#include "core/trap.h" #include "proc/proc.h" #include "proc/signal.h"`
  - `kernel/include/core/trap.h:L7-L11`：关键词命中
    代码片段：`/* * Architecture-specific types and symbols are now in arch/trap.h, * pulled in via arch.h (included from defs.h): *   - trap_context_t, task_context_t`
  - `kernel/plic.c:L1-L4`：关键词命中
    代码片段：`#include "include/plic.h" #include "include/memlayout.h" #include "include/param.h"`
  - `kernel/trap.c:L1-L3`：关键词命中
    代码片段：`#include "include/trap.h" #include "include/console.h" #include "include/disk.h"`
- 与 2023 AVX 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mm/mm.c:L11-L15`：关键词命中
    代码片段：`#include "proc/proc.h" static inline int pte_user_readable(uint64_t pte) { return arch_pte_is_leaf(pte) && (pte & PTE_U) && (pte & PTE_R); }`
  - `kernel/mm/vm.c:L20-L24`：关键词命中
    代码片段：`*   total_vm/rss/locked_vm while holding the address-space write exclusion *   defined by MM_LOCK_MODEL. * - Page-table write paths live in COW/demand faults, file/VMO faults, *...`
  - `kernel/vm.c:L1-L5`：关键词命中
    代码片段：`#include "include/vm.h" #include "include/elf.h" #include "include/kalloc.h" #include "include/memlayout.h" #include "include/param.h"`
  - `kernel/kalloc.c:L3-L7`：关键词命中
    代码片段：`// and pipe buffers. Allocates whole 4096-byte pages. #include "include/kalloc.h" #include "include/memlayout.h" #include "include/param.h"`
- 与 2023 AVX 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/pid.c:L1-L3`：关键词命中
    代码片段：`#include "proc/proc.h" #include "proc/proc_internal.h" #include "core/lock.h"`
  - `kernel/proc/exec.c:L16-L20`：关键词命中
    代码片段：`*/ #include "proc/proc.h" #include "proc/proc_internal.h" #include "proc/signal.h"`
  - `kernel/proc.c:L1-L4`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/fat32.h" #include "include/file.h"`
  - `kernel/thread.c:L1-L3`：关键词命中
    代码片段：`#include "include/thread.h" #include "include/kalloc.h" #include "include/memlayout.h"`
- 与 2023 AVX 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/core/sync.c:L9-L13`：关键词命中
    代码片段：`if (!q) return; spin_init(&q->lock); spin_set_debug(&q->lock, "wait_queue", q); q->head = NULL;`
  - `kernel/include/core/lock.h:L11-L15`：关键词命中
    代码片段：`/* * Global lock order contract (outermost -> innermost). * For the full driver-private lock contracts see docs/driver-lock-order.md. *`
  - `kernel/bio.c:L21-L25`：关键词命中
    代码片段：`#include "include/sdcard.h" #include "include/sleeplock.h" #include "include/spinlock.h" #include "include/types.h"`
  - `kernel/sem.c:L2-L6`：关键词命中
    代码片段：`#include "include/printf.h" #include "include/proc.h" #include "include/spinlock.h" #include "include/timer.h" #include "include/types.h"`
- 与 2023 AVX 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/syscall/syscall.c:L1-L4`：关键词命中
    代码片段：`/* * A20OS syscall dispatcher. * * Keep ABI dispatch here; concrete syscall implementations live in sys_*.c.`
  - `kernel/abi/linux/sys_signal.c:L99-L103`：关键词命中
    代码片段：`int64_t sys_sigreturn(trap_context_t *ctx) { /* * Linux syscall 139 is rt_sigreturn on RISC-V, LoongArch and AArch64. * Restoring from task->sig_saved_ctx is not sufficient: it...`
  - `kernel/syscall.c:L1-L4`：关键词命中
    代码片段：`#include "include/syscall.h" #include "include/kalloc.h" #include "include/memlayout.h"`
  - `kernel/include/syscall.h:L10-L14`：关键词命中
    代码片段：`int argaddr(int n, uint64 *ip); int argstr(int n, char *buf, int max); void syscall(void); #endif`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2023 AVX 在“文件系统”维度发现 4 个同名定义：sys_read, sys_write, sys_writev, sys_readv。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/abi/linux/sys_fs.c:L156-L156`：fn sys_read
    代码片段：`int64_t sys_read(int fd, char *buf, size_t count) {`
  - `kernel/sysfile.c:L157-L157`：fn sys_read
    代码片段：`uint64 sys_read(void) {`
  - `kernel/abi/linux/sys_fs.c:L174-L174`：fn sys_write
    代码片段：`int64_t sys_write(int fd, const char *buf, size_t count) {`
  - `kernel/sysfile.c:L180-L180`：fn sys_write
    代码片段：`uint64 sys_write(void) {`
- 函数/符号名重合：与 2023 AVX 在“调度与任务管理”维度发现 4 个同名定义：sys_exit, sys_exit_group, sys_getpid, sys_getppid。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/abi/linux/sys_proc.c:L64-L64`：fn sys_exit
    代码片段：`int64_t sys_exit(int code) {`
  - `kernel/sysproc.c:L179-L179`：fn sys_exit
    代码片段：`uint64 sys_exit(void) {`
  - `kernel/abi/linux/sys_proc.c:L69-L69`：fn sys_exit_group
    代码片段：`int64_t sys_exit_group(int code) {`
  - `kernel/sysproc.c:L427-L427`：fn sys_exit_group
    代码片段：`uint64 sys_exit_group(void) { return 0; }`
- 函数/符号名重合：与 2023 AVX 在“同步机制”维度发现 4 个同名定义：sys_sync, sys_fsync, sys_clock_gettime, sys_clock_getres。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/abi/linux/sys_fs.c:L470-L470`：fn sys_sync
    代码片段：`int64_t sys_sync(void) {`
  - `kernel/sysfile.c:L1325-L1325`：fn sys_sync
    代码片段：`uint64 sys_sync(void) {`
  - `kernel/abi/linux/sys_fs.c:L474-L474`：fn sys_fsync
    代码片段：`int64_t sys_fsync(int fd) {`
  - `kernel/sysfile.c:L1330-L1330`：fn sys_fsync
    代码片段：`uint64 sys_fsync(void) { return 0; }`
- 函数/符号名重合：与 2023 AVX 在“系统调用”维度发现 4 个同名定义：sys_read, sys_write, sys_writev, sys_readv。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/abi/linux/sys_fs.c:L156-L156`：fn sys_read
    代码片段：`int64_t sys_read(int fd, char *buf, size_t count) {`
  - `kernel/sysfile.c:L157-L157`：fn sys_read
    代码片段：`uint64 sys_read(void) {`
  - `kernel/abi/linux/sys_fs.c:L174-L174`：fn sys_write
    代码片段：`int64_t sys_write(int fd, const char *buf, size_t count) {`
  - `kernel/sysfile.c:L180-L180`：fn sys_write
    代码片段：`uint64 sys_write(void) {`
- 函数/符号名重合：与 2023 AVX 在“内存管理”维度发现 1 个同名定义：sys_mmap。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/abi/linux/sys_mm.c:L24-L24`：fn sys_mmap
    代码片段：`int64_t sys_mmap(uint64_t addr, size_t len, int prot, int flags, int fd, long off) {`
  - `kernel/sysfile.c:L1061-L1061`：fn sys_mmap
    代码片段：`uint64 sys_mmap() {`
- 文件路径重合：与 2023 AVX 在“设备驱动”维度出现同名文件源码路径 `kernel/drivers/char/uart.c` / `kernel/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/drivers/char/uart.c:L1-L3`：关键词命中
    代码片段：`#include "drivers/char/uart.h" #include "drivers/core/driver_core.h" #include "drivers/core/driver_class.h"`
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
- 文件路径重合：与 2023 AVX 在“中断与异常”维度出现同名文件源码路径 `kernel/core/trap.c` / `kernel/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/core/trap.c:L1-L3`：关键词命中
    代码片段：`#include "core/trap.h" #include "proc/proc.h" #include "proc/signal.h"`
  - `kernel/trap.c:L1-L3`：关键词命中
    代码片段：`#include "include/trap.h" #include "include/console.h" #include "include/disk.h"`
- 文件路径重合：与 2023 AVX 在“内存管理”维度出现同名文件源码路径 `kernel/mm/vm.c` / `kernel/vm.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/mm/vm.c:L20-L24`：关键词命中
    代码片段：`*   total_vm/rss/locked_vm while holding the address-space write exclusion *   defined by MM_LOCK_MODEL. * - Page-table write paths live in COW/demand faults, file/VMO faults, *...`
  - `kernel/vm.c:L1-L5`：关键词命中
    代码片段：`#include "include/vm.h" #include "include/elf.h" #include "include/kalloc.h" #include "include/memlayout.h" #include "include/param.h"`
- 文件路径重合：与 2023 AVX 在“调度与任务管理”维度出现同名文件源码路径 `kernel/proc/proc.c` / `kernel/proc.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/proc/proc.c:L185-L189`：fn proc_make_ready
    代码片段：`} void proc_make_ready(task_t *t) { if (!t || t->state == PROC_UNUSED || t->state == PROC_ZOMBIE) return;`
  - `kernel/proc.c:L1-L4`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/fat32.h" #include "include/file.h"`
- 文件路径重合：与 2023 AVX 在“系统调用”维度出现同名文件源码路径 `kernel/syscall/syscall.c` / `kernel/syscall.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/syscall/syscall.c:L1-L4`：关键词命中
    代码片段：`/* * A20OS syscall dispatcher. * * Keep ABI dispatch here; concrete syscall implementations live in sys_*.c.`
  - `kernel/syscall.c:L1-L4`：关键词命中
    代码片段：`#include "include/syscall.h" #include "include/kalloc.h" #include "include/memlayout.h"`
- 文件路径重合：与 2023 AVX 在“系统调用”维度出现同名文件源码路径 `kernel/core/trap.c` / `kernel/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/core/trap.c:L14-L18`：fn fetch_user_insn
    代码片段：`#include "core/klog.h" static int fetch_user_insn(task_t *task, uint64_t va, uint32_t *insn_out) { if (!task || !task->mm || !task->mm->pgdir || !insn_out) return 0;`
  - `kernel/trap.c:L34-L38`：fn trapinithart
    代码片段：`// set up to take exceptions and traps while in the kernel. void trapinithart(void) { w_stvec((uint64)kernelvec); w_sstatus(r_sstatus() | SSTATUS_SIE);`

## 相似点

- 与 2023 AVX 同属 ucore-variant 风格。（置信度：medium）
- 与 2023 AVX 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/drivers/char/uart.c:L1-L3`：关键词命中
    代码片段：`#include "drivers/char/uart.h" #include "drivers/core/driver_core.h" #include "drivers/core/driver_class.h"`
  - `kernel/drivers/block/loop.c:L54-L58`：关键词命中
    代码片段：`for (int i = 0; i < MAX_LOOP_DEVS; i++) { memset(&g_loop[i], 0, sizeof(g_loop[i])); /* LOCK_ORDER: initialize each per-device loop lock at boot. */ spin_init(&g_loop[i].lock); g...`
- 与 2023 AVX 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/vfs.c:L1-L4`：关键词命中
    代码片段：`/* * A20OS — Virtual Filesystem (VFS) * * Provides a unified fd-based interface over:`
  - `kernel/fs/ext4.c:L1-L5`：关键词命中
    代码片段：`#include "fs/ext4.h" #include "fs/file.h" #include "fs/vfs.h" #include "fs/block_cache.h" #include "mm/mm.h"`
- 与 2023 AVX 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/core/trap.c:L1-L3`：关键词命中
    代码片段：`#include "core/trap.h" #include "proc/proc.h" #include "proc/signal.h"`
  - `kernel/include/core/trap.h:L7-L11`：关键词命中
    代码片段：`/* * Architecture-specific types and symbols are now in arch/trap.h, * pulled in via arch.h (included from defs.h): *   - trap_context_t, task_context_t`
- 与 2023 AVX 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mm/mm.c:L11-L15`：关键词命中
    代码片段：`#include "proc/proc.h" static inline int pte_user_readable(uint64_t pte) { return arch_pte_is_leaf(pte) && (pte & PTE_U) && (pte & PTE_R); }`
  - `kernel/mm/vm.c:L20-L24`：关键词命中
    代码片段：`*   total_vm/rss/locked_vm while holding the address-space write exclusion *   defined by MM_LOCK_MODEL. * - Page-table write paths live in COW/demand faults, file/VMO faults, *...`
- 与 2023 AVX 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/pid.c:L1-L3`：关键词命中
    代码片段：`#include "proc/proc.h" #include "proc/proc_internal.h" #include "core/lock.h"`
  - `kernel/proc/exec.c:L16-L20`：关键词命中
    代码片段：`*/ #include "proc/proc.h" #include "proc/proc_internal.h" #include "proc/signal.h"`
- 与 2023 AVX 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/core/sync.c:L9-L13`：关键词命中
    代码片段：`if (!q) return; spin_init(&q->lock); spin_set_debug(&q->lock, "wait_queue", q); q->head = NULL;`
  - `kernel/include/core/lock.h:L11-L15`：关键词命中
    代码片段：`/* * Global lock order contract (outermost -> innermost). * For the full driver-private lock contracts see docs/driver-lock-order.md. *`
- 与 2023 AVX 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/syscall/syscall.c:L1-L4`：关键词命中
    代码片段：`/* * A20OS syscall dispatcher. * * Keep ABI dispatch here; concrete syscall implementations live in sys_*.c.`
  - `kernel/abi/linux/sys_signal.c:L99-L103`：关键词命中
    代码片段：`int64_t sys_sigreturn(trap_context_t *ctx) { /* * Linux syscall 139 is rt_sigreturn on RISC-V, LoongArch and AArch64. * Restoring from task->sig_saved_ctx is not sufficient: it...`
- 与 2023 AVX 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2023 AVX 的语言构成不同：待测作品为 {'build': 2900, 'markdown': 15529, 'c': 347925, 'asm': 11000, 'make': 534, 'text': 235}，历史样本为 {'json': 20, 'build': 353, 'markdown': 1027, 'asm': 1663, 'c': 132010, 'make': 90, 'text': 2}。（置信度：medium）

## 可能创新点

- 待测作品包含 183 个历史样本 2023 AVX 中未见的独特函数/类型定义（如 `sys_mremap`, `arch_trap_ctx_get_kernel_stack`, `sys_sigtimedwait`, `arch_pci_config_write32`, `arch_trap_ctx_set_kernel_stack`, `cred_update_uid_caps`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/abi/linux/sys_mm.c:L274-L274`：fn sys_mremap
    代码片段：`int64_t sys_mremap(uint64_t old_addr, size_t old_size, size_t new_size, int flags, uint64_t new_addr) {`
  - `kernel/arch/aarch64/include/trap_frame.h:L152-L152`：fn arch_trap_ctx_get_kernel_stack
    代码片段：`static inline uint64_t arch_trap_ctx_get_kernel_stack(const trap_context_t *ctx, uint64_t fallback) {`

## 附录：核验摘要

- 关键结论数：26
- 含证据关键结论数：26（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
