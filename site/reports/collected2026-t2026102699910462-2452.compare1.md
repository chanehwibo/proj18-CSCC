# 2026 T2026102699910462 比较报告

- 对比历史仓库：2025 NexusOS
- 生成时间：2026-07-02T05:00:01.136309+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 NexusOS（来源：赛事历史作品）：画像相似度 score=9.31；同属 rcore-variant 风格; 语言构成相似度 0.94; OS 维度重合度 1.00; 代码规模接近度 0.43

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 NexusOS 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/virtio.rs:L6-L10`：关键词命中
    代码片段：`use myos_mm::{PAGE_SIZE, PageAllocation, PhysAddr, VirtAddr}; use virtio_drivers::{ BufferDirection, Hal, device::blk::{SECTOR_SIZE, VirtIOBlk},`
  - `kernel/src/console.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt; use myos_runtime::console::ByteConsole; /// 将当前架构的早期控制台适配到公共格式化设施。`
  - `ostd/src/drivers/mod.rs:L1-L5`：关键词命中
    代码片段：`// SPDX-License-Identifier: MPL-2.0 //! Device drivers collection. pub mod virtio;`
  - `ostd/src/drivers/virtio/hal.rs:L1-L4`：关键词命中
    代码片段：`// SPDX-License-Identifier: MPL-2.0 //! VirtIO‑drivers ↔ OSTD (RISC‑V) HAL ‑‑ *no‑IOMMU* version. use alloc::{collections::BTreeMap};`
- 与 2025 NexusOS 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L11-L15`：关键词命中
    代码片段：`use crate::process::Process; const ROOT_LOCK: LockClass = LockClass::new("vfs.root", LockRank::Vfs, 0); const TREE_LOCK: LockClass = LockClass::new("vfs.tree", LockRank::Vfs, 0)...`
  - `kernel/src/rng.rs:L10-L14`：关键词命中
    代码片段：`use crate::lockdep::{LockClass, LockRank}; const RNG_LOCK: LockClass = LockClass::new("rng.pool", LockRank::Vfs, 5); const CHACHA_KEY_SIZE: usize = 32;`
  - `kernel/src/syscall/fs.rs:L10-L14`：关键词命中
    代码片段：`}; use tracing::trace; use vfs::{self, get_path_resolver, impls::pipe::{PipeReader, PipeWriter}, FileMode, FileOpen, PathBuf, PathSlice, SFileHandle, VnodeType}; use vfs::{SeekF...`
  - `kernel/comps/vfs/src/lib.rs:L7-L11`：关键词命中
    代码片段：`extern crate alloc; // NexusOS 虚拟文件系统 (VFS) 核心组件。 // // 本 crate 定义了 VFS 的核心抽象，包括路径操作、文件系统、Vnode（虚拟节点）`
- 与 2025 NexusOS 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/irq.rs:L2-L6`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug, Eq, PartialEq)] pub enum InterruptSource { Timer, Software, External,`
  - `kernel/src/trap.rs:L4-L8`：关键词命中
    代码片段：`pub fn initialize() { crate::arch::trap::initialize(); crate::println!("trap subsystem:");`
  - `ostd/src/trap/irq.rs:L4-L8`：关键词命中
    代码片段：`use smallvec::SmallVec; #[cfg(target_arch = "riscv64")] use crate::arch::riscv::plic; use crate::{`
  - `ostd/src/trap/mod.rs:L1-L5`：关键词命中
    代码片段：`// SPDX-License-Identifier: MPL-2.0 //! Handles trap across kernel and user space. mod handler;`
- 与 2025 NexusOS 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/vm.rs:L19-L23`：关键词命中
    代码片段：`static KERNEL_PAGE_TABLE: IrqSpinLock<Option<RuntimePageTable>> = IrqSpinLock::new_with_class( None, LockClass::new("kernel_page_table", LockRank::PageTable, 1), );`
  - `kernel/src/memory.rs:L236-L240`：关键词命中
    代码片段：`pub struct EarlyMemoryState { frame_allocator: EarlyFrameAllocator<MEMORY_MAP_CAPACITY>, boot_page_table: crate::arch::memory::paging::BootPageTable,`
  - `ostd/src/mm/io.rs:L377-L381`：关键词命中
    代码片段：`/// When the operating range is in kernel space, the memory within that range /// is guaranteed to be valid, and the corresponding memory reads are infallible. /// When the oper...`
  - `ostd/src/mm/mod.rs:L11-L15`：关键词命中
    代码片段：`pub(crate) mod dma; pub mod frame; pub(crate) mod heap_allocator; mod io; pub(crate) mod kspace;`
- 与 2025 NexusOS 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L47-L51`：关键词命中
    代码片段：`type ContextSwitch = ( *mut crate::arch::task::Context, *const crate::arch::task::Context, );`
  - `kernel/src/task/stack.rs:L3-L7`：关键词命中
    代码片段：`const KERNEL_STACK_SIZE: usize = 64 * 1024; const KERNEL_STACK_ALIGNMENT: usize = PAGE_SIZE; // M6-B r3: architecture-owned fresh-task bootstrap reserve. // // The architecture...`
  - `kernel/src/thread.rs:L22-L26`：关键词命中
    代码片段：`use syscall_numbers::native::sys_call_name; use vfs::{PathBuf, PathSlice}; use core::{future::Future, ops::ControlFlow, pin::Pin, str, task::{Context, Poll}}; use exception::{ha...`
  - `ostd/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`pub(crate) mod atomic_mode; mod preempt; pub mod scheduler; mod utils;`
- 与 2025 NexusOS 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/irq_lock.rs:L2-L6`：关键词命中
    代码片段：`marker::PhantomData, ops::{Deref, DerefMut}, sync::atomic::{AtomicUsize, Ordering}, };`
  - `vendor/rust-src/library/alloc/src/sync.rs:L5-L9`：关键词命中
    代码片段：`//! See the ['Arc<T>'][Arc] documentation for more details. //! //! **Note**: This module is only available on platforms that support atomic //! loads and stores of pointers. Th...`
  - `ostd/src/sync/mod.rs:L9-L13`：关键词命中
    代码片段：`mod rcu; pub use ::spin::Mutex as SpinMutex; pub use maitake::sync::*;`
  - `ostd/src/sync/guard.rs:L6-L10`：关键词命中
    代码片段：`}; /// A guardian that denotes the guard behavior for holding a lock. pub trait Guardian { /// The guard type for holding a spin lock or a write lock.`
- 与 2025 NexusOS 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/trap.rs:L84-L88`：关键词命中
    代码片段：`match frame.cause_code() { BREAKPOINT => handle_breakpoint(frame), USER_ECALL if frame.previous_mode_was_user() => crate::user::handle_syscall(frame), INSTRUCTION_PAGE_FAULT =>...`
  - `kernel/src/syscall.rs:L1-L3`：关键词命中
    代码片段：`//! Linux asm-generic 64-bit syscall ABI shared by both SudoOS ports. //! //! This module owns ABI facts only. Syscall policy remains in the dispatcher so`
  - `kernel/src/syscall.rs:L17-L21`：关键词命中
    代码片段：`use tracing::warn; use crate::syscall::fs::{do_chdir, do_close, do_copy_file_range, do_dup, do_dup3, do_fstat, do_ftruncate, do_getcwd, do_getdents64, do_linkat, do_lseek, do_mk...`
  - `kernel/hello.S:L9-L13`：关键词命中
    代码片段：`la      a2, message_end sub     a2, a2, a1              # 计算消息长度 ecall                           # 系统调用 li      a7, 93                  # 退出系统调用号 li      a0, 0...`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 NexusOS 在“中断与异常”维度发现片段级代码相似度 0.86 （token=0.79, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, debug, derive, enum, eq, external, partialeq。（置信度：high）
  证据：
  - `kernel/src/irq.rs:L2-L6`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug, Eq, PartialEq)] pub enum InterruptSource { Timer, Software, External,`
  - `ostd/src/trap/irq.rs:L27-L31`：enum SourceKind
    代码片段：`#[derive(Clone, Copy, Debug, PartialEq, Eq)] enum SourceKind { External, Software,`
- 与 2025 NexusOS 在“中断与异常”维度发现片段级代码相似度 0.78 （token=0.67, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, debug, derive, enum, eq, partialeq, software。（置信度：high）
  证据：
  - `kernel/src/irq.rs:L1-L5`：enum InterruptSource
    代码片段：`#[allow(dead_code)] #[derive(Clone, Copy, Debug, Eq, PartialEq)] pub enum InterruptSource { Timer, Software,`
  - `ostd/src/trap/irq.rs:L27-L31`：enum SourceKind
    代码片段：`#[derive(Clone, Copy, Debug, PartialEq, Eq)] enum SourceKind { External, Software,`
- 文件路径重合：与 2025 NexusOS 在“内存管理”维度出现同路径源码路径 `kernel/src/vm.rs` / `kernel/src/vm.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/src/vm.rs:L19-L23`：关键词命中
    代码片段：`static KERNEL_PAGE_TABLE: IrqSpinLock<Option<RuntimePageTable>> = IrqSpinLock::new_with_class( None, LockClass::new("kernel_page_table", LockRank::PageTable, 1), );`
  - `kernel/src/vm.rs:L42-L46`：struct ProcessVm
    代码片段：`// The process user space virtual memory pub struct ProcessVm { root_vmar: Vmar<Full>, heap: Heap,`
- 文件路径重合：与 2025 NexusOS 在“系统调用”维度出现同路径源码路径 `kernel/src/syscall.rs` / `kernel/src/syscall.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/src/syscall.rs:L1-L3`：关键词命中
    代码片段：`//! Linux asm-generic 64-bit syscall ABI shared by both SudoOS ports. //! //! This module owns ABI facts only. Syscall policy remains in the dispatcher so`
  - `kernel/src/syscall.rs:L17-L21`：关键词命中
    代码片段：`use tracing::warn; use crate::syscall::fs::{do_chdir, do_close, do_copy_file_range, do_dup, do_dup3, do_fstat, do_ftruncate, do_getcwd, do_getdents64, do_linkat, do_lseek, do_mk...`
- 文件路径重合：与 2025 NexusOS 在“中断与异常”维度出现同名文件源码路径 `kernel/src/irq.rs` / `ostd/src/trap/irq.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/irq.rs:L2-L6`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug, Eq, PartialEq)] pub enum InterruptSource { Timer, Software, External,`
  - `ostd/src/trap/irq.rs:L4-L8`：关键词命中
    代码片段：`use smallvec::SmallVec; #[cfg(target_arch = "riscv64")] use crate::arch::riscv::plic; use crate::{`
- 文件路径重合：与 2025 NexusOS 在“系统调用”维度出现同名文件源码路径 `kernel/src/trap.rs` / `vendor/axhal/src/trap.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/trap.rs:L84-L88`：关键词命中
    代码片段：`match frame.cause_code() { BREAKPOINT => handle_breakpoint(frame), USER_ECALL if frame.previous_mode_was_user() => crate::user::handle_syscall(frame), INSTRUCTION_PAGE_FAULT =>...`
  - `vendor/axhal/src/trap.rs:L51-L55`：fn handle_syscall
    代码片段：`/// Call the external syscall handler. #[cfg(feature = "uspace")] pub(crate) fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize { SYSCALL[0](tf, syscall_num) }`

## 相似点

- 与 2025 NexusOS 同属 rcore-variant 风格。（置信度：medium）
- 与 2025 NexusOS 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/virtio.rs:L6-L10`：关键词命中
    代码片段：`use myos_mm::{PAGE_SIZE, PageAllocation, PhysAddr, VirtAddr}; use virtio_drivers::{ BufferDirection, Hal, device::blk::{SECTOR_SIZE, VirtIOBlk},`
  - `kernel/src/console.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt; use myos_runtime::console::ByteConsole; /// 将当前架构的早期控制台适配到公共格式化设施。`
- 与 2025 NexusOS 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L11-L15`：关键词命中
    代码片段：`use crate::process::Process; const ROOT_LOCK: LockClass = LockClass::new("vfs.root", LockRank::Vfs, 0); const TREE_LOCK: LockClass = LockClass::new("vfs.tree", LockRank::Vfs, 0)...`
  - `kernel/src/rng.rs:L10-L14`：关键词命中
    代码片段：`use crate::lockdep::{LockClass, LockRank}; const RNG_LOCK: LockClass = LockClass::new("rng.pool", LockRank::Vfs, 5); const CHACHA_KEY_SIZE: usize = 32;`
- 与 2025 NexusOS 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/irq.rs:L2-L6`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug, Eq, PartialEq)] pub enum InterruptSource { Timer, Software, External,`
  - `kernel/src/trap.rs:L4-L8`：关键词命中
    代码片段：`pub fn initialize() { crate::arch::trap::initialize(); crate::println!("trap subsystem:");`
- 与 2025 NexusOS 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/vm.rs:L19-L23`：关键词命中
    代码片段：`static KERNEL_PAGE_TABLE: IrqSpinLock<Option<RuntimePageTable>> = IrqSpinLock::new_with_class( None, LockClass::new("kernel_page_table", LockRank::PageTable, 1), );`
  - `kernel/src/memory.rs:L236-L240`：关键词命中
    代码片段：`pub struct EarlyMemoryState { frame_allocator: EarlyFrameAllocator<MEMORY_MAP_CAPACITY>, boot_page_table: crate::arch::memory::paging::BootPageTable,`
- 与 2025 NexusOS 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L47-L51`：关键词命中
    代码片段：`type ContextSwitch = ( *mut crate::arch::task::Context, *const crate::arch::task::Context, );`
  - `kernel/src/task/stack.rs:L3-L7`：关键词命中
    代码片段：`const KERNEL_STACK_SIZE: usize = 64 * 1024; const KERNEL_STACK_ALIGNMENT: usize = PAGE_SIZE; // M6-B r3: architecture-owned fresh-task bootstrap reserve. // // The architecture...`
- 与 2025 NexusOS 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/irq_lock.rs:L2-L6`：关键词命中
    代码片段：`marker::PhantomData, ops::{Deref, DerefMut}, sync::atomic::{AtomicUsize, Ordering}, };`
  - `vendor/rust-src/library/alloc/src/sync.rs:L5-L9`：关键词命中
    代码片段：`//! See the ['Arc<T>'][Arc] documentation for more details. //! //! **Note**: This module is only available on platforms that support atomic //! loads and stores of pointers. Th...`
- 与 2025 NexusOS 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/trap.rs:L84-L88`：关键词命中
    代码片段：`match frame.cause_code() { BREAKPOINT => handle_breakpoint(frame), USER_ECALL if frame.previous_mode_was_user() => crate::user::handle_syscall(frame), INSTRUCTION_PAGE_FAULT =>...`
  - `kernel/src/syscall.rs:L1-L3`：关键词命中
    代码片段：`//! Linux asm-generic 64-bit syscall ABI shared by both SudoOS ports. //! //! This module owns ABI facts only. Syscall policy remains in the dispatcher so`
- 与 2025 NexusOS 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 NexusOS 的语言构成不同：待测作品为 {'markdown': 23954, 'build': 11383, 'asm': 6300, 'rust': 1585288, 'toml': 198, 'json': 69, 'c': 44494, 'text': 10457, 'yaml': 79, 'make': 664}，历史样本为 {'json': 282272, 'yaml': 1409, 'toml': 10114, 'build': 48447, 'markdown': 110170, 'asm': 6884, 'text': 112991, 'rust': 5618211, 'c': 13184, 'cpp': 67, 'make': 33}。（置信度：medium）

## 可能创新点

- 待测作品包含 306 个历史样本 2025 NexusOS 中未见的独特函数/类型定义（如 `mmio_virtual_address`, `ram_virtual_address`, `switch_user_address_space`, `call2`, `hart_start`, `write_entry`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `arch/riscv64/src/memory/phys_access.rs:L44-L44`：fn mmio_virtual_address
    代码片段：`pub fn mmio_virtual_address(physical: PhysAddr, size: usize) -> Result<VirtAddr, PhysAccessError> {`
  - `arch/riscv64/src/memory/phys_access.rs:L17-L17`：fn ram_virtual_address
    代码片段：`pub fn ram_virtual_address(physical: PhysAddr, size: usize) -> Result<VirtAddr, PhysAccessError> {`

## 附录：核验摘要

- 关键结论数：21
- 含证据关键结论数：21（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
