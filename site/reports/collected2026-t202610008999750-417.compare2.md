# 2026 T202610008999750 比较报告

- 对比历史仓库：2024 海底小纵队
- 生成时间：2026-07-02T09:00:39.631051+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 海底小纵队（来源：赛事历史作品）：画像相似度 score=9.14；同属 ucore-variant 风格; 语言构成相似度 0.81; OS 维度重合度 1.00; 代码规模接近度 0.52

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 海底小纵队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `vendor/arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2024 海底小纵队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/cargo/fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `vendor/arceos/modules/axfs/src/fs/fatfs.rs:L34-L38`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
  - `vendor/fatfs/src/fs.rs:L21-L25`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `kernel/src/main.rs:L190-L194`：关键词命中
    代码片段：`.expect("can't create tmp dir"); // Initialize the Dentry node. // dentry::dentry_init(rootfs); // FileItem::fs_open("/bin", OpenFlags::O_DIRECTORY)`
- 与 2024 海底小纵队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `vendor/arceos/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
  - `kernel/src/main.rs:L64-L68`：关键词命中
    代码片段：`#[polyhal::arch_interrupt] /// Handle kernel interrupt fn kernel_interrupt(cx_ref: &mut TrapFrame, trap_type: TrapType) { match trap_type {`
  - `kernel/src/tasks/mod.rs:L132-L136`：关键词命中
    代码片段：`#[derive(Debug, Clone, Copy, Default)] pub struct ProcessTimer { pub timer: ITimerVal, pub next: TimeVal, pub last: TimeVal,`
- 与 2024 海底小纵队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `core/src/mm.rs:L26-L30`：关键词命中
    代码片段：`pub fn copy_from_kernel(aspace: &mut AddrSpace) -> AxResult { if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use se...`
  - `vendor/arceos/modules/axmm/src/backend/alloc.rs:L2-L6`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PhysAddr, VirtAddr};`
  - `kernel/src/syscall/mm.rs:L4-L8`：关键词命中
    代码片段：`use polyhal::PAGE_SIZE; use polyhal::USER_VADDR_END; use frame_allocator::ceil_div; use log::debug;`
  - `kernel/src/main.rs:L39-L43`：关键词命中
    代码片段：`use devices::{self, get_int_device}; use executor::current_task; use frame_allocator::{self, frame_alloc_persist, frame_unalloc}; use hal; use tasks::UserTask;`
- 与 2024 海底小纵队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. use core::{`
  - `src/mm.rs:L40-L44`：关键词命中
    代码片段：`"{} ({:?}): segmentation fault at {:#x}, exit!", curr.id_name(), curr.task_ext().thread, vaddr );`
  - `vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`
  - `kernel/src/tasks/elf.rs:L14-L18`：关键词命中
    代码片段：`use crate::tasks::memset::MemType; use super::task::UserTask; pub trait ElfExtra {`
- 与 2024 海底小纵队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `core/src/task.rs:L4-L8`：关键词命中
    代码片段：`alloc::Layout, cell::RefCell, sync::atomic::{AtomicUsize, Ordering}, time::Duration, };`
  - `vendor/arceos/modules/axfs/src/root.rs:L7-L11`：关键词命中
    代码片段：`use axfs_vfs::{VfsNodeAttr, VfsNodeOps, VfsNodeRef, VfsNodeType, VfsOps, VfsResult}; use axns::{ResArc, def_resource}; use axsync::Mutex; use lazyinit::LazyInit; use spin::RwLock;`
  - `kernel/src/main.rs:L30-L34`：关键词命中
    代码片段：`mod user; use core::sync::atomic::{AtomicBool, Ordering}; use polyhal::addr::{PhysPage, VirtPage};`
  - `kernel/src/socket.rs:L5-L9`：关键词命中
    代码片段：`use fs::INodeInterface; use lose_net_stack::net_trait::SocketInterface; use sync::Mutex; use vfscore::{Metadata, PollEvent, VfsResult};`
- 与 2024 海底小纵队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_api::*;`
  - `vendor/cargo/syscalls/src/syscall/arm.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %r5 // // %r7 is used for the syscall number. // // %r0 is reused for the syscall return value.`
  - `kernel/src/syscall/fd.rs:L14-L18`：关键词命中
    代码片段：`use crate::epoll::{EpollEvent, EpollFile}; use crate::syscall::consts::{from_vfs, FcntlCmd, IoVec, AT_CWD}; use crate::syscall::func::timespc_now; use crate::syscall::time::curr...`
  - `kernel/src/syscall/mm.rs:L7-L11`：关键词命中
    代码片段：`use log::debug; use crate::syscall::consts::from_vfs; use crate::syscall::consts::MSyncFlags; use crate::syscall::consts::MapFlags;`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 海底小纵队 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：crate, defaulttimeprovider, time, timeprovider, use。（置信度：high）
  证据：
  - `vendor/cargo/fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `vendor/fatfs/src/fs.rs:L21-L25`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
- 与 2024 海底小纵队 在“文件系统”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, debug, derive, enum, eq, fat12, fattype。（置信度：high）
  证据：
  - `vendor/cargo/fatfs/src/fs.rs:L28-L32`：enum FatType
    代码片段：`/// 'FatType' values are based on the size of File Allocation Table entry. #[derive(Copy, Clone, Eq, PartialEq, Debug)] pub enum FatType { /// 12 bits per FAT entry Fat12,`
  - `vendor/fatfs/src/fs.rs:L29-L33`：enum FatType
    代码片段：`/// 'FatType' values are based on the size of File Allocation Table entry. #[derive(Copy, Clone, Eq, PartialEq, Debug)] pub enum FatType { /// 12 bits per FAT entry Fat12,`
- 与 2024 海底小纵队 在“同步机制”维度发现片段级代码相似度 0.71 （token=0.56, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：atomic, core, ordering, sync, use。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axsync/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! A naïve sleeping mutex. use core::sync::atomic::{AtomicU64, Ordering};`
  - `kernel/src/main.rs:L30-L34`：关键词命中
    代码片段：`mod user; use core::sync::atomic::{AtomicBool, Ordering}; use polyhal::addr::{PhysPage, VirtPage};`
- 函数/符号名重合：与 2024 海底小纵队 在“设备驱动”维度发现 1 个同名定义：ident。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `vendor/cargo/syscalls/syscalls-gen/src/tables.rs:L38-L38`：fn ident
    代码片段：`fn ident(&self) -> Cow<str> {`
  - `vendor/zerocopy-derive/src/lib.rs:L647-L647`：fn ident
    代码片段：`fn ident(&self) -> Ident {`
- 文件路径重合：与 2024 海底小纵队 在“文件系统”维度出现同名文件源码路径 `vendor/cargo/fatfs/src/fs.rs` / `vendor/fatfs/src/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `vendor/cargo/fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `vendor/fatfs/src/fs.rs:L21-L25`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
- 文件路径重合：与 2024 海底小纵队 在“中断与异常”维度出现同名文件源码路径 `vendor/arceos/modules/axhal/src/irq.rs` / `vendor/x86/src/irq.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `vendor/x86/src/irq.rs:L9-L13`：struct InterruptDescription
    代码片段：`/// x86 Exception description (see also Intel Vol. 3a Chapter 6). #[derive(Debug)] pub struct InterruptDescription { pub vector: u8, pub mnemonic: &'static str,`
- 文件路径重合：与 2024 海底小纵队 在“内存管理”维度出现同名文件源码路径 `core/src/mm.rs` / `kernel/src/syscall/mm.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `core/src/mm.rs:L26-L30`：关键词命中
    代码片段：`pub fn copy_from_kernel(aspace: &mut AddrSpace) -> AxResult { if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use se...`
  - `kernel/src/syscall/mm.rs:L4-L8`：关键词命中
    代码片段：`use polyhal::PAGE_SIZE; use polyhal::USER_VADDR_END; use frame_allocator::ceil_div; use log::debug;`
- 文件路径重合：与 2024 海底小纵队 在“内存管理”维度出现同名文件源码路径 `src/mm.rs` / `kernel/src/syscall/mm.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/mm.rs:L11-L15`：fn handle_page_fault
    代码片段：`#[register_trap_handler(PAGE_FAULT)] fn handle_page_fault(vaddr: VirtAddr, access_flags: MappingFlags, is_user: bool) -> bool { // 可恢复缺页是用户地址空间按需映射的一部分，避免污染测评串口输出。 debug!(`
  - `kernel/src/syscall/mm.rs:L4-L8`：关键词命中
    代码片段：`use polyhal::PAGE_SIZE; use polyhal::USER_VADDR_END; use frame_allocator::ceil_div; use log::debug;`
- 文件路径重合：与 2024 海底小纵队 在“调度与任务管理”维度出现同名文件源码路径 `core/src/task.rs` / `vendor/x86/src/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. use core::{`
  - `vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`

## 相似点

- 与 2024 海底小纵队 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 海底小纵队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `vendor/arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 与 2024 海底小纵队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/cargo/fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `vendor/arceos/modules/axfs/src/fs/fatfs.rs:L34-L38`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
- 与 2024 海底小纵队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `vendor/arceos/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
- 与 2024 海底小纵队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `core/src/mm.rs:L26-L30`：关键词命中
    代码片段：`pub fn copy_from_kernel(aspace: &mut AddrSpace) -> AxResult { if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use se...`
  - `vendor/arceos/modules/axmm/src/backend/alloc.rs:L2-L6`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PhysAddr, VirtAddr};`
- 与 2024 海底小纵队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. use core::{`
  - `src/mm.rs:L40-L44`：关键词命中
    代码片段：`"{} ({:?}): segmentation fault at {:#x}, exit!", curr.id_name(), curr.task_ext().thread, vaddr );`
- 与 2024 海底小纵队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `core/src/task.rs:L4-L8`：关键词命中
    代码片段：`alloc::Layout, cell::RefCell, sync::atomic::{AtomicUsize, Ordering}, time::Duration, };`
  - `vendor/arceos/modules/axfs/src/root.rs:L7-L11`：关键词命中
    代码片段：`use axfs_vfs::{VfsNodeAttr, VfsNodeOps, VfsNodeRef, VfsNodeType, VfsOps, VfsResult}; use axns::{ResArc, def_resource}; use axsync::Mutex; use lazyinit::LazyInit; use spin::RwLock;`
- 与 2024 海底小纵队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_api::*;`
  - `vendor/cargo/syscalls/src/syscall/arm.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %r5 // // %r7 is used for the syscall number. // // %r0 is reused for the syscall return value.`
- 与 2024 海底小纵队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 海底小纵队 的语言构成不同：待测作品为 {'yaml': 14, 'json': 53, 'build': 3856, 'markdown': 4696, 'toml': 1063, 'rust': 120477, 'text': 38, 'c': 39653, 'make': 1316, 'asm': 983}，历史样本为 {'json': 123, 'yaml': 68, 'build': 6380, 'markdown': 20083, 'toml': 259, 'asm': 359, 'text': 28, 'rust': 379391, 'c': 27758, 'make': 664}。（置信度：medium）

## 可能创新点

- 待测作品包含 163 个历史样本 2024 海底小纵队 中未见的独特函数/类型定义（如 `accept_ipv4_mapped_ipv6_part`, `accept_number`, `handle_page_fault`, `accept_mac_joined_with`, `syscall`, `name_and_description`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `vendor/cargo/smoltcp/src/parsers.rs:L151-L151`：fn accept_ipv4_mapped_ipv6_part
    代码片段：`fn accept_ipv4_mapped_ipv6_part(&mut self, parts: &mut [u16], idx: &mut usize) -> Result<()> {`
  - `vendor/cargo/smoltcp/src/parsers.rs:L109-L109`：fn accept_number
    代码片段：`fn accept_number(&mut self, max_digits: usize, max_value: u32, hex: bool) -> Result<u32> {`

## 附录：核验摘要

- 关键结论数：24
- 含证据关键结论数：24（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
