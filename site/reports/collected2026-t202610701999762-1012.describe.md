# 2026 T202610701999762 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t202610701999762-1012`
- 风格：rcore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：389
- 代码/文本行数：337155
- 主要语言：markdown 301273 LOC, rust 32745 LOC, toml 2615 LOC, build 482 LOC, make 28 LOC

## 总览

2026 T202610701999762 是一个 rcore-variant 风格的小型操作系统相关仓库，主要语言统计为 markdown: 301273 LOC, rust: 32745 LOC, toml: 2615 LOC, build: 482 LOC。仓库包含 389 个已扫描文件、约 337155 行可分析文本，当前抽取到 2193 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 10 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, Makefile, crate/ext4_rs/Cargo.toml, kernel/Cargo.toml, kernel/Makefile。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] members = [ "kernel",`
  - `Makefile:L1-L3`：构建入口
    代码片段：`MODE ?= release MEM ?= 1G SMP ?= 1`
  - `crate/ext4_rs/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "ext4_rs" version = "1.3.3"`
  - `kernel/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "kernel" version = "0.1.0"`
  - `kernel/Makefile:L1-L3`：构建入口
    代码片段：`ARCH ?= riscv64 MODE ?= release MEM ?= 1G`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct ThreadContext, fn set_saved, fn init_kernel, fn init_user, fn init_fork。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/arch/thread.rs` | L7-L11 | 关键词命中 |
| `kernel/src/l1/20_task/task_mm_signal/api.rs` | L6-L10 | 关键词命中 |
| `kernel/src/l1/50_bridge/proc_export/need.rs` | L3-L6 | 关键词命中 |
| `kernel/src/arch/thread.rs` | L4-L8 | struct ThreadContext |
| `kernel/src/arch/thread.rs` | L35-L39 | fn set_saved |
| `kernel/src/arch/thread.rs` | L42-L46 | fn init_kernel |

### 关键代码片段

  - `kernel/src/arch/thread.rs:L7-L11`：关键词命中
    代码片段：`/// Kernel stack pointer. Kept at offset 0 for arch switch assembly. pub sp: usize, /// Kernel thread pointer / CPU-local pointer. Kept at offset 1. pub tp: usize, /// Callee-sa...`
  - `kernel/src/l1/20_task/task_mm_signal/api.rs:L6-L10`：关键词命中
    代码片段：`pub mod pid; pub mod signal; pub mod task; pub mod user_memory;`
  - `kernel/src/l1/50_bridge/proc_export/need.rs:L3-L6`：关键词命中
    代码片段：`#![allow(unused_imports)] pub use crate::l1::s20_task::task_mm_signal::api::task as task; pub use crate::l1::s31_vfs_cache::page_cache::api::file as file;`
  - `kernel/src/arch/thread.rs:L4-L8`：struct ThreadContext
    代码片段：`#[repr(C)] #[derive(Debug, Default, Clone, Copy, PartialEq, Eq)] pub struct ThreadContext { /// Kernel stack pointer. Kept at offset 0 for arch switch assembly. pub sp: usize,`

### 相关符号

`struct ThreadContext` at `kernel/src/arch/thread.rs:L4`、`fn set_saved` at `kernel/src/arch/thread.rs:L35`、`fn init_kernel` at `kernel/src/arch/thread.rs:L42`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：struct PhysAddr, struct VirtAddr, struct AddressLayout, enum PageTableKind, struct PageTableToken。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/arch/vm.rs` | L15-L19 | 关键词命中 |
| `kernel/src/l0/30_page/page_map/api.rs` | L8-L12 | 关键词命中 |
| `kernel/src/l1/20_task/task_mm_signal/api/memory.rs` | L51-L55 | 关键词命中 |
| `kernel/src/arch/vm.rs` | L1-L4 | struct PhysAddr |
| `kernel/src/arch/vm.rs` | L3-L7 | struct VirtAddr |
| `kernel/src/arch/vm.rs` | L6-L10 | struct AddressLayout |

### 关键代码片段

  - `kernel/src/arch/vm.rs:L15-L19`：关键词命中
    代码片段：`pub enum PageTableKind { Generic, Sv39, LoongArch64ThreeLevel, }`
  - `kernel/src/l0/30_page/page_map/api.rs:L8-L12`：关键词命中
    代码片段：`pub use crate::arch::PageFlags; /// PageTable 结构体，属于该能力对外暴露的稳定接口。 #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct PageTable {`
  - `kernel/src/l1/20_task/task_mm_signal/api/memory.rs:L51-L55`：关键词命中
    代码片段：`struct Space { table: map::PageTable, mappings: Vec<Mapping>, brk: usize,`
  - `kernel/src/arch/vm.rs:L1-L4`：struct PhysAddr
    代码片段：`#[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)] pub struct PhysAddr(pub usize); #[derive(Debug, Clone, Copy, PartialEq, Eq, PartialOrd, Ord)]`

### 相关符号

`struct PhysAddr` at `kernel/src/arch/vm.rs:L1`、`struct VirtAddr` at `kernel/src/arch/vm.rs:L3`、`struct AddressLayout` at `kernel/src/arch/vm.rs:L6`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：enum TrapKind, enum AccessKind, struct TrapInfo, struct TrapFrame, fn from_user。（置信度：medium）
  - 静态识别到 13 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/l2/40_table/syscall_table/api.rs` | L41-L45 | 关键词命中 |
| `kernel/src/l2/50_syscall_entry/syscall_entry/api.rs` | L59-L63 | 关键词命中 |
| `kernel/src/arch/trap.rs` | L1-L4 | enum TrapKind |
| `kernel/src/arch/trap.rs` | L11-L15 | enum AccessKind |
| `kernel/src/arch/trap.rs` | L18-L22 | struct TrapInfo |
| `kernel/src/arch/imp/generic.rs` | L218-L222 | fn syscall_number |

### 关键代码片段

  - `kernel/src/l2/40_table/syscall_table/api.rs:L41-L45`：关键词命中
    代码片段：`pub fn dispatch(args: Args) -> isize { match number::decode(args.number) { number::Syscall::Getcwd => file::getcwd(args.get(0), args.get(1)), number::Syscall::Dup => file::dup(a...`
  - `kernel/src/l2/50_syscall_entry/syscall_entry/api.rs:L59-L63`：关键词命中
    代码片段：`pub fn handle_trap(tf: &mut super::TrapFrame) { let number = need::syscall_raw::number(tf); let syscall = need::number::decode(number); let args = [ need::syscall_raw::arg(tf, 0),`
  - `kernel/src/arch/trap.rs:L1-L4`：enum TrapKind
    代码片段：`#[derive(Debug, Clone, Copy, PartialEq, Eq)] pub enum TrapKind { Unknown, Interrupt,`
  - `kernel/src/arch/trap.rs:L11-L15`：enum AccessKind
    代码片段：`#[derive(Debug, Clone, Copy, PartialEq, Eq)] pub enum AccessKind { Read, Write,`

### 相关符号

`enum TrapKind` at `kernel/src/arch/trap.rs:L1`、`enum AccessKind` at `kernel/src/arch/trap.rs:L11`、`struct TrapInfo` at `kernel/src/arch/trap.rs:L18`、`fn syscall_number` at `kernel/src/arch/imp/generic.rs:L218`、`fn syscall_arg` at `kernel/src/arch/imp/generic.rs:L222`、`fn syscall_set_result` at `kernel/src/arch/imp/generic.rs:L226`、`fn syscall_advance_pc` at `kernel/src/arch/imp/generic.rs:L231`、`fn syscall_number` at `kernel/src/arch/imp/riscv64.rs:L674`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct OpenFlags, enum SeekWhence, fn access, fn can_read, fn can_write。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/l1/30_vfs/vfs_fd/api.rs` | L106-L110 | 关键词命中 |
| `kernel/src/l1/30_vfs/vfs_fd/devfs.rs` | L49-L53 | 关键词命中 |
| `kernel/src/l1/30_vfs/vfs_fd/procfs.rs` | L113-L117 | 关键词命中 |
| `kernel/src/l1/30_vfs/vfs_fd/api.rs` | L16-L20 | struct OpenFlags |
| `kernel/src/l1/30_vfs/vfs_fd/api.rs` | L20-L24 | enum SeekWhence |
| `kernel/src/l1/30_vfs/vfs_fd/api.rs` | L57-L61 | fn access |

### 关键代码片段

  - `kernel/src/l1/30_vfs/vfs_fd/api.rs:L106-L110`：关键词命中
    代码片段：`#[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct Metadata { pub inode: u64, pub kind: NodeKind, pub mode: u16,`
  - `kernel/src/l1/30_vfs/vfs_fd/devfs.rs:L49-L53`：关键词命中
    代码片段：`display, }, inode: file_inode(kind) as u32, kind: NodeKind::CharDevice, })`
  - `kernel/src/l1/30_vfs/vfs_fd/procfs.rs:L113-L117`：关键词命中
    代码片段：`} let display = super::normalize_path(path).ok()?; let (inode, kind) = match node_normalized(&display)? { Node::Dir(kind) => (dir_inode(kind), NodeKind::Directory), Node::File(k...`
  - `kernel/src/l1/30_vfs/vfs_fd/api.rs:L16-L20`：struct OpenFlags
    代码片段：`/// 打开文件时的行为标志位集合（位掩码），封装读写、创建、截断、追加等语义。 #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct OpenFlags(u32); /// 'lseek' 使用的偏移基准。`

### 相关符号

`struct OpenFlags` at `kernel/src/l1/30_vfs/vfs_fd/api.rs:L16`、`enum SeekWhence` at `kernel/src/l1/30_vfs/vfs_fd/api.rs:L20`、`fn access` at `kernel/src/l1/30_vfs/vfs_fd/api.rs:L57`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct Mutex, impl Mutex<T>, fn lock, fn lock, struct Semaphore。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/arch/boot.rs` | L1-L3 | 关键词命中 |
| `kernel/src/arch/trap.rs` | L113-L117 | 关键词命中 |
| `kernel/src/arch/barrier.rs` | L16-L19 | 关键词命中 |
| `kernel/src/l0/70_run/wait_work/api.rs` | L4-L8 | struct Mutex |
| `kernel/src/l0/70_run/wait_work/api.rs` | L11-L15 | impl Mutex<T> |
| `kernel/src/l0/40_heap/dynamic_log/api.rs` | L151-L155 | fn lock |

### 关键代码片段

  - `kernel/src/arch/boot.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicUsize, Ordering}; #[derive(Debug, Clone, Copy, PartialEq, Eq)]`
  - `kernel/src/arch/trap.rs:L113-L117`：关键词命中
    代码片段：`true } use core::sync::atomic::{AtomicUsize, Ordering}; pub type PageFaultHandler = fn(usize, Option<AccessKind>) -> bool;`
  - `kernel/src/arch/barrier.rs:L16-L19`：关键词命中
    代码片段：`#[inline] pub fn compiler() { core::sync::atomic::compiler_fence(core::sync::atomic::Ordering::SeqCst); }`
  - `kernel/src/l0/70_run/wait_work/api.rs:L4-L8`：struct Mutex
    代码片段：`/// Mutex 结构体，属于该能力对外暴露的稳定接口。 pub struct Mutex<T> { locked: AtomicBool, value: UnsafeCell<T>,`

### 相关符号

`struct Mutex` at `kernel/src/l0/70_run/wait_work/api.rs:L4`、`impl Mutex<T>` at `kernel/src/l0/70_run/wait_work/api.rs:L11`、`fn lock` at `kernel/src/l0/40_heap/dynamic_log/api.rs:L151`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn enable, fn disable, fn is_enabled, fn save, fn restore。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/arch/trap.rs` | L2-L6 | 关键词命中 |
| `kernel/src/l0/50_trap/trap_core/api.rs` | L1-L3 | 关键词命中 |
| `kernel/src/l0/50_trap/trap_core/body.rs` | L1-L4 | 关键词命中 |
| `kernel/src/arch/irq.rs` | L4-L8 | fn enable |
| `kernel/src/arch/irq.rs` | L9-L13 | fn disable |
| `kernel/src/arch/irq.rs` | L14-L18 | fn is_enabled |

### 关键代码片段

  - `kernel/src/arch/trap.rs:L2-L6`：关键词命中
    代码片段：`pub enum TrapKind { Unknown, Interrupt, Timer, PageFault,`
  - `kernel/src/l0/50_trap/trap_core/api.rs:L1-L3`：关键词命中
    代码片段：`pub mod trap { pub type TrapKind = crate::arch::trap::TrapKind; pub type TrapInfo = crate::arch::trap::TrapInfo;`
  - `kernel/src/l0/50_trap/trap_core/body.rs:L1-L4`：关键词命中
    代码片段：`pub fn init() -> crate::init::InitResult { crate::arch::trap::init(); Ok(()) }`
  - `kernel/src/arch/irq.rs:L4-L8`：fn enable
    代码片段：`#[inline] pub fn enable() { super::platform::irq_enable(); }`

### 相关符号

`fn enable` at `kernel/src/arch/irq.rs:L4`、`fn disable` at `kernel/src/arch/irq.rs:L9`、`fn is_enabled` at `kernel/src/arch/irq.rs:L14`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：fn put_byte, fn read_byte, fn write_bytes, fn write_fmt, struct EarlyConsole。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/l0/80_device/device_core/body.rs` | L1-L3 | 关键词命中 |
| `kernel/src/l0/10_early_output/early_console/api.rs` | L5-L9 | 关键词命中 |
| `kernel/src/l0/80_device/device_core/virtio_pci_blk.rs` | L24-L28 | 关键词命中 |
| `kernel/src/arch/console.rs` | L1-L5 | fn put_byte |
| `kernel/src/arch/console.rs` | L5-L9 | fn read_byte |
| `kernel/src/arch/console.rs` | L9-L13 | fn write_bytes |

### 关键代码片段

  - `kernel/src/l0/80_device/device_core/body.rs:L1-L3`：关键词命中
    代码片段：`#[path = "virtio_mmio_blk.rs"] mod virtio_mmio_blk; #[cfg(target_arch = "loongarch64")]`
  - `kernel/src/l0/10_early_output/early_console/api.rs:L5-L9`：关键词命中
    代码片段：`/// - 'bytes'：要输出或填充的字节缓冲区。 pub fn bytes(bytes: &[u8]) { crate::arch::console::write_bytes(bytes); }`
  - `kernel/src/l0/80_device/device_core/virtio_pci_blk.rs:L24-L28`：关键词命中
    代码片段：`const PCI_COMMAND_BUS_MASTER: u16 = 1 << 2; const VIRTIO_PCI_CAP_COMMON_CFG: u8 = 1; const VIRTIO_PCI_CAP_NOTIFY_CFG: u8 = 2; const VIRTIO_PCI_CAP_ISR_CFG: u8 = 3;`
  - `kernel/src/arch/console.rs:L1-L5`：fn put_byte
    代码片段：`use core::fmt::{self, Write}; pub fn put_byte(byte: u8) { super::platform::console_putchar(byte); }`

### 相关符号

`fn put_byte` at `kernel/src/arch/console.rs:L1`、`fn read_byte` at `kernel/src/arch/console.rs:L5`、`fn write_bytes` at `kernel/src/arch/console.rs:L9`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
