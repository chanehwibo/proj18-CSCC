# 2026 T2026100069910661 比较报告

- 对比历史仓库：2023 终结者-FIVE
- 生成时间：2026-07-02T09:00:22.829552+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2023 终结者-FIVE（来源：赛事历史作品）：画像相似度 score=5.49；同属 ucore-variant 风格; 语言构成相似度 0.41; OS 维度重合度 0.33; 代码规模接近度 0.34

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2023 终结者-FIVE 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/init.c:L52-L56`：关键词命中
    代码片段：`// 进程调度 // schedule(0); halt(); }`
  - `sched.c:L2-L6`：关键词命中
    代码片段：`/* defined in entry.S */ extern void switch_to(struct context *next); #define MAX_TASKS 10`
  - `os.h:L22-L26`：关键词命中
    代码片段：`extern void page_free(void *p); /* task management */ struct context { /* ignore x0 */`
- 与 2023 终结者-FIVE 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `include/purchar.h:L3-L7`：关键词命中
    代码片段：`register uint64_t a0 asm("a0") = ch;  // 字符 register uint64_t a7 asm("a7") = 0x1; // SBI_CONSOLE_PUTCHAR asm volatile("ecall"                  // 发起SBI调用 : "+r"(a0) : "r"(a7)`
  - `trap.c:L5-L9`：关键词命中
    代码片段：`extern void timer_handler(void); extern void schedule(void); extern void do_syscall(struct context *cxt); void trap_init()`
  - `syscall.c:L1-L4`：关键词命中
    代码片段：`#include "os.h" #include "syscall.h" int sys_gethid(unsigned int *ptr_hid)`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2023 终结者-FIVE 在“系统调用”维度发现片段级代码相似度 0.75 （token=0.61, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：a0, a7, asm, register, volatile。（置信度：medium）
  证据：
  - `include/purchar.h:L3-L7`：关键词命中
    代码片段：`register uint64_t a0 asm("a0") = ch;  // 字符 register uint64_t a7 asm("a7") = 0x1; // SBI_CONSOLE_PUTCHAR asm volatile("ecall"                  // 发起SBI调用 : "+r"(a0) : "r"(a7)`
  - `timer.c:L29-L33`：关键词命中
    代码片段：`register reg_t a7 asm("a7") = (reg_t)0x00UL; register reg_t a0 asm("a0") = (reg_t)stime_value; asm volatile("ecall" :"+r"(a0) :"r"(a7)`

## 相似点

- 与 2023 终结者-FIVE 同属 ucore-variant 风格。（置信度：medium）
- 与 2023 终结者-FIVE 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/init.c:L52-L56`：关键词命中
    代码片段：`// 进程调度 // schedule(0); halt(); }`
  - `sched.c:L2-L6`：关键词命中
    代码片段：`/* defined in entry.S */ extern void switch_to(struct context *next); #define MAX_TASKS 10`
- 与 2023 终结者-FIVE 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `include/purchar.h:L3-L7`：关键词命中
    代码片段：`register uint64_t a0 asm("a0") = ch;  // 字符 register uint64_t a7 asm("a7") = 0x1; // SBI_CONSOLE_PUTCHAR asm volatile("ecall"                  // 发起SBI调用 : "+r"(a0) : "r"(a7)`
  - `trap.c:L5-L9`：关键词命中
    代码片段：`extern void timer_handler(void); extern void schedule(void); extern void do_syscall(struct context *cxt); void trap_init()`
- 与 2023 终结者-FIVE 在 scheduler, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2023 终结者-FIVE 的语言构成不同：待测作品为 {'json': 20, 'make': 11, 'build': 69, 'markdown': 92, 'c': 66, 'asm': 15}，历史样本为 {'json': 20, 'make': 14, 'asm': 286, 'c': 1425, 'build': 87, 'markdown': 92}。（置信度：medium）

## 可能创新点

- 待测作品包含 3 个历史样本 2023 终结者-FIVE 中未见的独特函数/类型定义（如 `mips_init`, `putchar`, `halt`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/init.c:L7-L7`：fn mips_init
    代码片段：`void mips_init(u_int argc, char **argv, char **penv, u_int ram_low_size) {`
  - `include/purchar.h:L2-L2`：fn putchar
    代码片段：`int64_t putchar(char ch) {`

## 附录：核验摘要

- 关键结论数：6
- 含证据关键结论数：6（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
