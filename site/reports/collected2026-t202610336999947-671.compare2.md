# 2026 T202610336999947 比较报告

- 对比历史仓库：2022 图漏图森破
- 生成时间：2026-07-02T05:00:32.095375+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 图漏图森破（来源：赛事历史作品）：画像相似度 score=9.31；同属 ucore-variant 风格; 语言构成相似度 0.91; OS 维度重合度 1.00; 代码规模接近度 0.48

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 图漏图森破 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `arceos/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `kernel/driver/Sd.c:L1-L5`：关键词命中
    代码片段：`#include <Platform.h> #include <Spi.h> #include <Uart.h> #include <Driver.h> #include <MemoryConfig.h>`
  - `kernel/driver/Print.c:L1-L3`：关键词命中
    代码片段：`#include <Driver.h> #include <Riscv.h> #include <Spinlock.h>`
- 与 2022 图漏图森破 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `arceos/modules/axfs/src/fs/fatfs.rs:L32-L36`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
  - `testsuits-for-oskernel/busybox/libbb/copy_file.c:L182-L186`：关键词命中
    代码片段：`} } /* remember (dev,inode) of each created dir. * NULL: name is not remembered */ add_to_ino_dev_hashtable(&dest_stat, NULL);`
  - `kernel/fs/Fat.c:L1-L5`：关键词命中
    代码片段：`#include <Defs.h> #include "Bio.h" #include "Fat.h" #include "Sleeplock.h" #include "Spinlock.h"`
  - `kernel/fs/File.c:L101-L105`：关键词命中
    代码片段：`// Read from file f. // addr is a user virtual address. int fileread(struct File* f, bool isUser, u64 addr, int n) { int r = 0;`
- 与 2022 图漏图森破 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use axcpu::trap::{IRQ, register_trap_handler};`
  - `testsuits-for-oskernel/iperf/src/timer.c:L31-L35`：关键词命中
    代码片段：`#include <stdlib.h> #include "timer.h" #include "iperf_time.h"`
  - `kernel/trap/Exec.c:L9-L13`：关键词命中
    代码片段：`#include <Sysarg.h> #include <Debug.h> #include <Trap.h> #include <Sysfile.h> #include <uapi/linux/Auxvec.h>`
  - `kernel/trap/Trap.c:L1-L5`：关键词命中
    代码片段：`#include <Riscv.h> #include <Driver.h> #include <Timer.h> #include <Trap.h> #include <Process.h>`
- 与 2022 图漏图森破 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `arceos/modules/axmm/src/backend/alloc.rs:L1-L5`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PAGE_SIZE_4K, PageIter4K,...`
  - `arceos/modules/axmm/src/lib.rs:L78-L82`：关键词命中
    代码片段：`let mut aspace = AddrSpace::new_empty(base, size)?; if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use separate pag...`
  - `kernel/memory/Page.c:L18-L22`：关键词命中
    代码片段：`} int pageRemove(u64 *pgdir, u64 va) { u64 *pte; u64 pa = pageLookup(pgdir, va, &pte);`
  - `kernel/memory/MemoryInit.c:L37-L41`：关键词命中
    代码片段：`static void virtualMemory() { u64 va, pa; pageInsert(kernelPageDirectory, UART_V, UART, PTE_READ | PTE_WRITE | PTE_ACCESSED | PTE_DIRTY); va = CLINT_V, pa = CLINT; for (u64 i =...`
- 与 2022 图漏图森破 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/lmbench_src/src/lib_sched.c:L50-L54`：关键词命中
    代码片段：`if (!sched || strcasecmp(sched, "DEFAULT") == 0) { /* do nothing.  Allow scheduler to control placement */ return 0; } else if (strcasecmp(sched, "SINGLE") == 0) {`
  - `testsuits-for-oskernel/ltp-full-20240524/lib/tst_thread_state.c:L18-L22`：关键词命中
    代码片段：`unsigned int msecs = 0; snprintf(proc_path, sizeof(proc_path), "/proc/self/task/%i/stat", tid); for (;;) {`
  - `kernel/yield/Thread.c:L1-L3`：关键词命中
    代码片段：`#include <Thread.h> #include <Riscv.h> #include <MemoryConfig.h>`
  - `kernel/fs/Fat.c:L12-L16`：关键词命中
    代码片段：`#include <FileSystem.h> #include <Sysfile.h> #include <Thread.h> #include <Riscv.h>`
- 与 2022 图漏图森破 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/busybox/miscutils/flash_lock_unlock.c:L30-L34`：关键词命中
    代码片段：`//usage:       "MTD_DEVICE OFFSET SECTORS" //usage:#define flash_lock_full_usage "\n\n" //usage:       "Lock part or all of an MTD device. If SECTORS is -1, then all sectors\n"...`
  - `testsuits-for-oskernel/ltp-full-20240524/include/tst_fuzzy_sync.h:L155-L159`：关键词命中
    代码片段：`float max_dev_ratio; /** Internal; Atomic counter used by fzsync_pair_wait() */ int a_cntr; /** Internal; Atomic counter used by fzsync_pair_wait() */`
  - `kernel/lock/Spinlock.c:L1-L3`：关键词命中
    代码片段：`#include "Spinlock.h" #include "Hart.h" #include "Interrupt.h"`
  - `kernel/lock/Sleeplock.c:L3-L7`：关键词命中
    代码片段：`#include "Sleeplock.h" #include "Process.h" #include "Spinlock.h" #include "Driver.h" #include <Debug.h>`
- 与 2022 图漏图森破 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/UnixBench/src/syscall.c:L1-L5`：关键词命中
    代码片段：`/******************************************************************************* *  The BYTE UNIX Benchmarks - Release 3 *          Module: syscall.c   SID: 3.3 5/15/91 19:30:21...`
  - `testsuits-for-oskernel/lmbench_src/src/lat_syscall.c:L138-L142`：关键词命中
    代码片段：`benchmp(NULL, do_getppid, NULL, 0, parallel, warmup, repetitions, &state); micro("Simple syscall", get_n()); } else if (!strcmp("write", av[optind])) { state.fd = open("/dev/nul...`
  - `kernel/trap/Syscall.c:L1-L3`：关键词命中
    代码片段：`#include <Syscall.h> #include <Driver.h> #include <Process.h>`
  - `kernel/system/SignalTrampoline.S:L4-L6`：关键词命中
    代码片段：`signalTrampoline: li a7, 139 ecall`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 结构体/类型重合：与 2022 图漏图森破 在“文件系统”维度发现 1 个同名定义：File。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `arceos/api/arceos_posix_api/src/imp/fs.rs:L12-L12`：struct File
    代码片段：`pub struct File {`
  - `include/File.h:L21-L21`：struct File
    代码片段：`typedef struct File {`
- 文件路径重合：与 2022 图漏图森破 在“文件系统”维度出现同名文件源码路径 `testsuits-for-oskernel/busybox/util-linux/volume_id/fat.c` / `kernel/fs/Fat.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/busybox/util-linux/volume_id/fat.c:L19-L23`：关键词命中
    代码片段：`*/ //config:config FEATURE_VOLUMEID_FAT //config:	bool "fat filesystem" //config:	default y //config:	depends on VOLUMEID`
  - `kernel/fs/Fat.c:L1-L5`：关键词命中
    代码片段：`#include <Defs.h> #include "Bio.h" #include "Fat.h" #include "Sleeplock.h" #include "Spinlock.h"`
- 文件路径重合：与 2022 图漏图森破 在“中断与异常”维度出现同名文件源码路径 `testsuits-for-oskernel/iperf/src/timer.c` / `kernel/trap/Timer.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/iperf/src/timer.c:L31-L35`：关键词命中
    代码片段：`#include <stdlib.h> #include "timer.h" #include "iperf_time.h"`
  - `kernel/trap/Timer.c:L1-L4`：关键词命中
    代码片段：`#include <Driver.h> #include <Timer.h> #include <Process.h> #include <Riscv.h>`
- 文件路径重合：与 2022 图漏图森破 在“系统调用”维度出现同名文件源码路径 `testsuits-for-oskernel/UnixBench/src/syscall.c` / `kernel/trap/Syscall.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/UnixBench/src/syscall.c:L1-L5`：关键词命中
    代码片段：`/******************************************************************************* *  The BYTE UNIX Benchmarks - Release 3 *          Module: syscall.c   SID: 3.3 5/15/91 19:30:21...`
  - `kernel/trap/Syscall.c:L1-L3`：关键词命中
    代码片段：`#include <Syscall.h> #include <Driver.h> #include <Process.h>`

## 相似点

- 与 2022 图漏图森破 同属 ucore-variant 风格。（置信度：medium）
- 与 2022 图漏图森破 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `arceos/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 与 2022 图漏图森破 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `arceos/modules/axfs/src/fs/fatfs.rs:L32-L36`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
  - `testsuits-for-oskernel/busybox/libbb/copy_file.c:L182-L186`：关键词命中
    代码片段：`} } /* remember (dev,inode) of each created dir. * NULL: name is not remembered */ add_to_ino_dev_hashtable(&dest_stat, NULL);`
- 与 2022 图漏图森破 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use axcpu::trap::{IRQ, register_trap_handler};`
  - `testsuits-for-oskernel/iperf/src/timer.c:L31-L35`：关键词命中
    代码片段：`#include <stdlib.h> #include "timer.h" #include "iperf_time.h"`
- 与 2022 图漏图森破 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `arceos/modules/axmm/src/backend/alloc.rs:L1-L5`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PAGE_SIZE_4K, PageIter4K,...`
  - `arceos/modules/axmm/src/lib.rs:L78-L82`：关键词命中
    代码片段：`let mut aspace = AddrSpace::new_empty(base, size)?; if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use separate pag...`
- 与 2022 图漏图森破 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/lmbench_src/src/lib_sched.c:L50-L54`：关键词命中
    代码片段：`if (!sched || strcasecmp(sched, "DEFAULT") == 0) { /* do nothing.  Allow scheduler to control placement */ return 0; } else if (strcasecmp(sched, "SINGLE") == 0) {`
  - `testsuits-for-oskernel/ltp-full-20240524/lib/tst_thread_state.c:L18-L22`：关键词命中
    代码片段：`unsigned int msecs = 0; snprintf(proc_path, sizeof(proc_path), "/proc/self/task/%i/stat", tid); for (;;) {`
- 与 2022 图漏图森破 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/busybox/miscutils/flash_lock_unlock.c:L30-L34`：关键词命中
    代码片段：`//usage:       "MTD_DEVICE OFFSET SECTORS" //usage:#define flash_lock_full_usage "\n\n" //usage:       "Lock part or all of an MTD device. If SECTORS is -1, then all sectors\n"...`
  - `testsuits-for-oskernel/ltp-full-20240524/include/tst_fuzzy_sync.h:L155-L159`：关键词命中
    代码片段：`float max_dev_ratio; /** Internal; Atomic counter used by fzsync_pair_wait() */ int a_cntr; /** Internal; Atomic counter used by fzsync_pair_wait() */`
- 与 2022 图漏图森破 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/UnixBench/src/syscall.c:L1-L5`：关键词命中
    代码片段：`/******************************************************************************* *  The BYTE UNIX Benchmarks - Release 3 *          Module: syscall.c   SID: 3.3 5/15/91 19:30:21...`
  - `testsuits-for-oskernel/lmbench_src/src/lat_syscall.c:L138-L142`：关键词命中
    代码片段：`benchmp(NULL, do_getppid, NULL, 0, parallel, warmup, repetitions, &state); micro("Simple syscall", get_n()); } else if (!strcmp("write", av[optind])) { state.fd = open("/dev/nul...`
- 与 2022 图漏图森破 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 图漏图森破 的语言构成不同：待测作品为 {'build': 25533, 'markdown': 4799, 'rust': 20321, 'c': 1237469, 'toml': 137, 'make': 1752, 'text': 24768, 'asm': 1355, 'cpp': 1434, 'yaml': 32, 'json': 54}，历史样本为 {'json': 20, 'make': 28, 'build': 326, 'markdown': 3993, 'c': 405628, 'asm': 408, 'cpp': 36715, 'text': 55}。（置信度：medium）

## 可能创新点

- 待测作品包含 301 个历史样本 2022 图漏图森破 中未见的独特函数/类型定义（如 `ax_udp_peek_from`, `ax_udp_recv_from`, `ax_udp_send_to`, `ax_wait_queue_wait`, `sys_writev`, `sys_nanosleep`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `arceos/api/arceos_api/src/lib.rs:L333-L333`：fn ax_udp_peek_from
    代码片段：`pub fn ax_udp_peek_from(socket: &AxUdpSocketHandle, buf: &mut [u8]) -> AxResult<(usize, SocketAddr)>;`
  - `arceos/api/arceos_api/src/lib.rs:L330-L330`：fn ax_udp_recv_from
    代码片段：`pub fn ax_udp_recv_from(socket: &AxUdpSocketHandle, buf: &mut [u8]) -> AxResult<(usize, SocketAddr)>;`

## 附录：核验摘要

- 关键结论数：19
- 含证据关键结论数：19（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
