# 2026 T2026105749910208 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026105749910208-1758`
- 风格：rcore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：2222
- 代码/文本行数：781233
- 主要语言：rust 467557 LOC, json 282429 LOC, markdown 18581 LOC, build 7812 LOC, yaml 3458 LOC

## 总览

2026 T2026105749910208 是一个 rcore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 467557 LOC, json: 282429 LOC, markdown: 18581 LOC, build: 7812 LOC。仓库包含 2222 个已扫描文件、约 781233 行可分析文本，当前抽取到 32561 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, Makefile, offline_crate_sources/buddy_system_allocator/Cargo.toml, offline_crate_sources/sbi-spec/Cargo.toml, os/Cargo.toml。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] members = ["os"] resolver = "2"`
  - `Makefile:L1-L3`：构建入口
    代码片段：`# OS contest 2026 - Makefile ARCH ?= riscv64`
  - `offline_crate_sources/buddy_system_allocator/Cargo.toml:L1-L3`：构建入口
    代码片段：`# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO # # When uploading crates to the registry Cargo will automatically`
  - `offline_crate_sources/sbi-spec/Cargo.toml:L1-L3`：构建入口
    代码片段：`# THIS FILE IS AUTOMATICALLY GENERATED BY CARGO # # When uploading crates to the registry Cargo will automatically`
  - `os/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "wll_OS" version = "0.1.0"`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct UserProgramSpec, fn default, fn marker, struct Credentials, fn root。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/task/mod.rs` | L4-L8 | 关键词命中 |
| `os/src/task/task.rs` | L10-L14 | 关键词命中 |
| `os/src/task/context.rs` | L50-L54 | 关键词命中 |
| `os/src/task/mod.rs` | L60-L64 | struct UserProgramSpec |
| `os/src/task/mod.rs` | L76-L80 | fn default |
| `os/src/task/mod.rs` | L93-L97 | fn marker |

### 关键代码片段

  - `os/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`pub mod pid; pub mod processor; pub mod task; pub mod wait_queue;`
  - `os/src/task/task.rs:L10-L14`：关键词命中
    代码片段：`}; use crate::mm::memory_set::MemorySet; use crate::task::context::TaskContext; use crate::task::pid::Pid; use crate::utils::error::SysErrNo;`
  - `os/src/task/context.rs:L50-L54`：关键词命中
    代码片段：`#[naked] #[no_mangle] pub unsafe extern "C" fn switch_to( _current_task_ctx: *mut TaskContext, _next_task_ctx: *const TaskContext,`
  - `os/src/task/mod.rs:L60-L64`：struct UserProgramSpec
    代码片段：`/// argv, envp, cwd, and output marker name. Used by the test harness to run /// basic test binaries with correct paths and working directories. pub struct UserProgramSpec { ///...`

### 相关符号

`struct UserProgramSpec` at `os/src/task/mod.rs:L60`、`fn default` at `os/src/task/mod.rs:L76`、`fn marker` at `os/src/task/mod.rs:L93`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：struct KernelPageAlloc, fn alloc, fn dealloc, fn init, struct SharedMemorySegment。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/mm/mod.rs` | L1-L4 | 关键词命中 |
| `os/src/syscall/mm.rs` | L2-L6 | 关键词命中 |
| `os/src/mm/map_area.rs` | L1-L3 | 关键词命中 |
| `os/src/mm/mod.rs` | L10-L14 | struct KernelPageAlloc |
| `os/src/mm/mod.rs` | L13-L17 | fn alloc |
| `os/src/mm/mod.rs` | L24-L28 | fn dealloc |

### 关键代码片段

  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod elf_loader; pub mod frame_allocator; pub mod heap_allocator; pub mod map_area;`
  - `os/src/syscall/mm.rs:L2-L6`：关键词命中
    代码片段：`use crate::config::{PAGE_SIZE, USER_HEAP_START, USER_STACK_TOP}; use crate::fs::{ext4_vol, fd::FileDescriptor}; use crate::mm::frame_allocator::{self, FrameTracker}; use crate::...`
  - `os/src/mm/map_area.rs:L1-L3`：关键词命中
    代码片段：`use super::frame_allocator::FrameTracker; use crate::mm::page_table::PTEFlags; use crate::{config::PAGE_SIZE, fs::fd::FileDescriptor};`
  - `os/src/mm/mod.rs:L10-L14`：struct KernelPageAlloc
    代码片段：`/// 内核页分配器实现 pub struct KernelPageAlloc; impl PageAlloc for KernelPageAlloc {`

### 相关符号

`struct KernelPageAlloc` at `os/src/mm/mod.rs:L10`、`fn alloc` at `os/src/mm/mod.rs:L13`、`fn dealloc` at `os/src/mm/mod.rs:L24`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn enter_foreground_driver, fn leave_foreground_driver, fn foreground_driver_active, fn signal_syscall_parked, fn syscall_parked。（置信度：medium）
  - 静态识别到 192 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/syscall/fs.rs` | L872-L876 | 关键词命中 |
| `os/src/syscall/mm.rs` | L464-L468 | 关键词命中 |
| `os/src/syscall/mod.rs` | L200-L204 | 关键词命中 |
| `os/src/trap/mod.rs` | L30-L34 | fn enter_foreground_driver |
| `os/src/trap/mod.rs` | L35-L39 | fn leave_foreground_driver |
| `os/src/trap/mod.rs` | L40-L44 | fn foreground_driver_active |

### 关键代码片段

  - `os/src/syscall/fs.rs:L872-L876`：关键词命中
    代码片段：`} Err(SysErrNo::EPIPE) if total == 0 => { crate::syscall::signal::send_sigpipe_to_current(); return Err(SysErrNo::EPIPE); }`
  - `os/src/syscall/mm.rs:L464-L468`：关键词命中
    代码片段：`) -> SyscallRet { log::info!( "[syscall] mmap(addr={:#x}, len={:#x}, prot={:#x}, flags={:#x}, fd={}, off={:#x})", addr, length,`
  - `os/src/syscall/mod.rs:L200-L204`：关键词命中
    代码片段：`pub const SYSCALL_GETRANDOM: usize = 278; /// Old SYS_open = 1024, used by some basic test binaries via syscall(SYS_open, ...). /// Maps to openat(AT_FDCWD, path, flags, mode)....`
  - `os/src/trap/mod.rs:L30-L34`：fn enter_foreground_driver
    代码片段：`} pub fn enter_foreground_driver() { *FOREGROUND_DRIVER_ACTIVE.lock() = true; crate::timer::set_next_foreground_trigger();`

### 相关符号

`fn enter_foreground_driver` at `os/src/trap/mod.rs:L30`、`fn leave_foreground_driver` at `os/src/trap/mod.rs:L35`、`fn foreground_driver_active` at `os/src/trap/mod.rs:L40`、`fn signal_syscall_parked` at `os/src/trap/mod.rs:L44`、`fn syscall_parked` at `os/src/trap/mod.rs:L48`、`fn take_syscall_parked` at `os/src/trap/mod.rs:L52`、`fn handle_syscall` at `os/src/trap/mod.rs:L313`、`fn sys_openat` at `os/src/syscall/fs.rs:L2116`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct OpenFileDescriptionOwner, fn id, fn lease, fn set_lease, fn drop。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/fs/fd.rs` | L12-L16 | 关键词命中 |
| `os/src/fs/mod.rs` | L6-L10 | 关键词命中 |
| `os/src/fs/vfs.rs` | L1-L3 | 关键词命中 |
| `os/src/fs/fd.rs` | L44-L48 | struct OpenFileDescriptionOwner |
| `os/src/fs/fd.rs` | L50-L54 | fn id |
| `os/src/fs/fd.rs` | L54-L58 | fn lease |

### 关键代码片段

  - `os/src/fs/fd.rs:L12-L16`：关键词命中
    代码片段：`use crate::fs::block_dev; use crate::fs::ext4_vol; use crate::fs::vfs::VfsNodeKind; use crate::fs::{MemFileBacking, MemNodeMetadata}; use crate::task::wait_queue::WaitKey;`
  - `os/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod fd; pub mod vfat; pub mod vfs; #[allow(unused_imports)]`
  - `os/src/fs/vfs.rs:L1-L3`：关键词命中
    代码片段：`//! 最薄 VFS：统一 MemFS + ext4 后端的查找与变更入口；'mount'/'umount2' 语义见 'syscall::fs'。 use alloc::collections::{BTreeMap, BTreeSet};`
  - `os/src/fs/fd.rs:L44-L48`：struct OpenFileDescriptionOwner
    代码片段：`#[derive(Debug)] pub struct OpenFileDescriptionOwner { id: usize, lease: Mutex<i16>,`

### 相关符号

`struct OpenFileDescriptionOwner` at `os/src/fs/fd.rs:L44`、`fn id` at `os/src/fs/fd.rs:L50`、`fn lease` at `os/src/fs/fd.rs:L54`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn pipe_wait_key, fn wake_pipe_readers, fn wake_pipe_writers, fn eventfd_wait_key, fn wake_eventfd_readers。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/main.rs` | L166-L170 | 关键词命中 |
| `os/src/fs/fd.rs` | L6-L10 | 关键词命中 |
| `os/src/timer.rs` | L3-L7 | 关键词命中 |
| `os/src/fs/fd.rs` | L82-L86 | fn pipe_wait_key |
| `os/src/fs/fd.rs` | L86-L90 | fn wake_pipe_readers |
| `os/src/fs/fd.rs` | L90-L94 | fn wake_pipe_writers |

### 关键代码片段

  - `os/src/main.rs:L166-L170`：关键词命中
    代码片段：`// Guard against recursive calls use core::sync::atomic::{AtomicBool, Ordering}; static INIT_GUARD: AtomicBool = AtomicBool::new(false); if INIT_GUARD.swap(true, Ordering::SeqCs...`
  - `os/src/fs/fd.rs:L6-L10`：关键词命中
    代码片段：`use alloc::sync::Arc; use alloc::vec::Vec; use core::sync::atomic::{AtomicUsize, Ordering}; use lazy_static::lazy_static; use spin::Mutex;`
  - `os/src/timer.rs:L3-L7`：关键词命中
    代码片段：`use lazy_static::lazy_static; use polyhal::timer::current_time; use spin::Mutex; use crate::task::wait_queue::{BlockReason, WaitOutcome, WaitQueue};`
  - `os/src/fs/fd.rs:L82-L86`：fn pipe_wait_key
    代码片段：`} fn pipe_wait_key(state: &Arc<Mutex<PipeState>>, event: usize) -> WaitKey { WaitKey::new(Arc::as_ptr(state) as usize, event) }`

### 相关符号

`fn pipe_wait_key` at `os/src/fs/fd.rs:L82`、`fn wake_pipe_readers` at `os/src/fs/fd.rs:L86`、`fn wake_pipe_writers` at `os/src/fs/fd.rs:L90`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：struct TimerWaiter, fn get_time, fn get_time_us, fn next_timer_delay_us, fn program_next_timer。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/timer.rs` | L2-L6 | 关键词命中 |
| `os/src/trap/mod.rs` | L2-L6 | 关键词命中 |
| `os/src/trap/interrupts.rs` | L1-L3 | 关键词命中 |
| `os/src/timer.rs` | L20-L24 | struct TimerWaiter |
| `os/src/timer.rs` | L32-L36 | fn get_time |
| `os/src/timer.rs` | L37-L41 | fn get_time_us |

### 关键代码片段

  - `os/src/timer.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use lazy_static::lazy_static; use polyhal::timer::current_time; use spin::Mutex;`
  - `os/src/trap/mod.rs:L2-L6`：关键词命中
    代码片段：`use lazy_static::lazy_static; use polyhal_trap::trap::TrapType; use polyhal_trap::trapframe::{TrapFrame, TrapFrameArgs}; use spin::Mutex;`
  - `os/src/trap/interrupts.rs:L1-L3`：关键词命中
    代码片段：`use polyhal_trap::trap::TrapType; /// 中断处理辅助函数 use polyhal_trap::trapframe::TrapFrame;`
  - `os/src/timer.rs:L20-L24`：struct TimerWaiter
    代码片段：`pub const FOREGROUND_TIME_SLICE_MS: u64 = 50; struct TimerWaiter { deadline_us: usize, task: Arc<TaskControlBlock>,`

### 相关符号

`struct TimerWaiter` at `os/src/timer.rs:L20`、`fn get_time` at `os/src/timer.rs:L32`、`fn get_time_us` at `os/src/timer.rs:L37`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct Stdout, fn write_str, fn putchar, fn getchar, fn _print。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/console.rs` | L23-L27 | 关键词命中 |
| `os/src/drivers/hal.rs` | L1-L3 | 关键词命中 |
| `os/src/drivers/mod.rs` | L2-L6 | 关键词命中 |
| `os/src/console.rs` | L3-L7 | struct Stdout |
| `os/src/console.rs` | L6-L10 | fn write_str |
| `os/src/console.rs` | L15-L19 | fn putchar |

### 关键代码片段

  - `os/src/console.rs:L23-L27`：关键词命中
    代码片段：`macro_rules! print { ($($arg:tt)*) => ({ $crate::console::_print(format_args!($($arg)*)); }); }`
  - `os/src/drivers/hal.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO 驱动所用的 ['virtio_drivers::Hal'] 实现。 //! //! LoongArch 在 PG=1 + DMW 模式下，裸物理地址不可直接解引用：`
  - `os/src/drivers/mod.rs:L2-L6`：关键词命中
    代码片段：`//! 平台相关驱动入口。 //! //! - riscv64:  VirtIO-MMIO（设备树枚举） //! - loongarch64: VirtIO-PCI（ECAM 枚举）`
  - `os/src/console.rs:L3-L7`：struct Stdout
    代码片段：`/// 串口输出结构体 struct Stdout; impl Write for Stdout {`

### 相关符号

`struct Stdout` at `os/src/console.rs:L3`、`fn write_str` at `os/src/console.rs:L6`、`fn putchar` at `os/src/console.rs:L15`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
