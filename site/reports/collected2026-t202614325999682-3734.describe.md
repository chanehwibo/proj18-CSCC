# 2026 T202614325999682 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t202614325999682-3734`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：743
- 代码/文本行数：127845
- 主要语言：cpp 59391 LOC, c 51040 LOC, markdown 11959 LOC, asm 3654 LOC, make 1151 LOC

## 总览

2026 T202614325999682 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 cpp: 59391 LOC, c: 51040 LOC, markdown: 11959 LOC, asm: 3654 LOC。仓库包含 743 个已扫描文件、约 127845 行可分析文本，当前抽取到 6282 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 9 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, kernel/Makefile, libs/basecpp/Makefile, libs/kmod/Makefile, libs/linuxss-libc/Makefile。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`global-env := ./script/env/global.mk config-json := $(firstword $(wildcard config.json) script/config.default.json) config-arch := $(shell python3 -c "import json; from pathlib...`
  - `kernel/Makefile:L1-L3`：构建入口
    代码片段：`global-env ?= ./script/env/global.mk include $(global-env) include $(path-script)/build/component.mk`
  - `libs/basecpp/Makefile:L1-L3`：构建入口
    代码片段：`global-env ?= ./script/env/global.mk include $(global-env) include $(path-script)/build/component.mk`
  - `libs/kmod/Makefile:L1-L3`：构建入口
    代码片段：`global-env ?= ./script/env/global.mk include $(global-env) include $(path-script)/build/component.mk`
  - `libs/linuxss-libc/Makefile:L1-L3`：构建入口
    代码片段：`global-env ?= ./script/env/global.mk include $(global-env) include $(path-script)/build/component.mk`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct LoadPrm, struct TaskSpec, struct BootstrapRecordData, struct ProgramHeaderInfo, struct LoadedElfMeta。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/exe/task.h` | L1-L4 | 关键词命中 |
| `kernel/task/task.h` | L1-L4 | 关键词命中 |
| `kernel/task/wait.h` | L2-L6 | 关键词命中 |
| `kernel/exe/task.h` | L30-L34 | struct LoadPrm |
| `kernel/exe/task.h` | L46-L50 | struct TaskSpec |
| `kernel/exe/task.h` | L47-L51 | struct BootstrapRecordData |

### 关键代码片段

  - `kernel/exe/task.h:L1-L4`：关键词命中
    代码片段：`/** * @file task.h * @author theflysong (song_of_the_fly@163.com) * @brief 进程相关结构`
  - `kernel/task/task.h:L1-L4`：关键词命中
    代码片段：`/** * @file task.h * @author theflysong (song_of_the_fly@163.com) * @brief 进程与线程`
  - `kernel/task/wait.h:L2-L6`：关键词命中
    代码片段：`* @file wait.h * @author theflysong (song_of_the_fly@163.com) * @brief thread waiting reasons * @version alpha-1.0.0 * @date 2026-05-14`
  - `kernel/exe/task.h:L30-L34`：struct LoadPrm
    代码片段：`* */ struct LoadPrm { // 进程文件的能力索引 CapIdx image_file_cap;`

### 相关符号

`struct LoadPrm` at `kernel/exe/task.h:L30`、`struct TaskSpec` at `kernel/exe/task.h:L46`、`struct BootstrapRecordData` at `kernel/exe/task.h:L47`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：class LinearGrowAllocator, class SimpleKOP, fn free, struct PhyPage, class MemoryObject。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/object/memory.h` | L25-L29 | 关键词命中 |
| `kernel/main.cpp` | L15-L19 | 关键词命中 |
| `kernel/mem/gfp.h` | L21-L25 | 关键词命中 |
| `kernel/mem/alloc_def.h` | L40-L44 | class LinearGrowAllocator |
| `kernel/mem/alloc_def.h` | L69-L73 | class SimpleKOP |
| `kernel/mem/alloc_def.h` | L76-L80 | fn free |

### 关键代码片段

  - `kernel/object/memory.h:L25-L29`：关键词命中
    代码片段：`* * addr 是物理页起始地址, refcount 仅用于标记该页是否仍处于 * 当前 payload 视角下的 COW 共享状态. */ struct PhyPage {`
  - `kernel/main.cpp:L15-L19`：关键词命中
    代码片段：`#include <arch/riscv64/device/fdt_helper.h> #include <arch/riscv64/device/platform.h> #include <arch/riscv64/mem/sv39.h> #elif defined(__ARCH_loongarch64__) #include <arch/loong...`
  - `kernel/mem/gfp.h:L21-L25`：关键词命中
    代码片段：`* * RawGFPImpl 只负责实际页框的分配与释放, 不维护页框共享状态. * GFP 在此基础上叠加引用计数, 用于支持 fork 后的 COW 页面共享. */ using RawGFPImpl = BuddyAllocator;`
  - `kernel/mem/alloc_def.h:L40-L44`：class LinearGrowAllocator
    代码片段：`}; class LinearGrowAllocator { private: static constexpr size_t SIZE = 0x400000;  // 2 MB`

### 相关符号

`class LinearGrowAllocator` at `kernel/mem/alloc_def.h:L40`、`class SimpleKOP` at `kernel/mem/alloc_def.h:L69`、`fn free` at `kernel/mem/alloc_def.h:L76`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：struct PipeCreateRet, struct ForkCaps, struct StartupArguments, struct ArgPack, struct RetPack。（置信度：medium）
  - 静态识别到 3 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/syscall/cap.h` | L13-L17 | 关键词命中 |
| `kernel/syscall/vfs.h` | L14-L18 | 关键词命中 |
| `kernel/syscall/pipe.h` | L7-L11 | 关键词命中 |
| `kernel/syscall/pipe.h` | L12-L16 | struct PipeCreateRet |
| `kernel/syscall/task.h` | L23-L27 | struct ForkCaps |
| `kernel/syscall/task.h` | L28-L32 | struct StartupArguments |

### 关键代码片段

  - `kernel/syscall/cap.h:L13-L17`：关键词命中
    代码片段：`#include <sustcore/capability.h> #include <syscall/uaccess.h> namespace syscall {`
  - `kernel/syscall/vfs.h:L14-L18`：关键词命中
    代码片段：`#include <sustcore/attr.h> #include <sustcore/files.h> #include <syscall/uaccess.h> #include <vfs/ops.h>`
  - `kernel/syscall/pipe.h:L7-L11`：关键词命中
    代码片段：`#include <sustcore/capability.h> #include <syscall/uaccess.h> #include <cstddef>`
  - `kernel/syscall/pipe.h:L12-L16`：struct PipeCreateRet
    代码片段：`namespace syscall { struct PipeCreateRet { CapIdx read_cap; CapIdx write_cap;`

### 相关符号

`struct PipeCreateRet` at `kernel/syscall/pipe.h:L12`、`struct ForkCaps` at `kernel/syscall/task.h:L23`、`struct StartupArguments` at `kernel/syscall/task.h:L28`、`struct SyscallInfo` at `kernel/task/task_struct.h:L80`、`macro SYSCALL_BASE` at `include/sustcore/syscall.h:L12`、`fn syscall_to_string` at `include/sustcore/syscall_str.h:L16`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：class IFile, class IDirectory, class IMetadata, class IDentry, class IINode。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/vfs/ops.h` | L2-L6 | 关键词命中 |
| `kernel/vfs/vfs.h` | L1-L4 | 关键词命中 |
| `kernel/vfs/ext4.h` | L14-L18 | 关键词命中 |
| `kernel/vfs/ops.h` | L38-L42 | class IFile |
| `kernel/vfs/ops.h` | L39-L43 | class IDirectory |
| `kernel/vfs/ops.h` | L40-L44 | class IMetadata |

### 关键代码片段

  - `kernel/vfs/ops.h:L2-L6`：关键词命中
    代码片段：`* @file ops.h * @author theflysong (song_of_the_fly@163.com) * @brief VFS操作 * @version alpha-1.0.0 * @date 2026-02-03`
  - `kernel/vfs/vfs.h:L1-L4`：关键词命中
    代码片段：`/** * @file vfs.h * @author theflysong (song_of_the_fly@163.com) * @brief Virtual File System`
  - `kernel/vfs/ext4.h:L14-L18`：关键词命中
    代码片段：`#include <bio/buffer.h> #include <sus/types.h> #include <vfs/ops.h> #include <cstddef>`
  - `kernel/vfs/ops.h:L38-L42`：class IFile
    代码片段：`}; class IFile; class IDirectory; class IMetadata;`

### 相关符号

`class IFile` at `kernel/vfs/ops.h:L38`、`class IDirectory` at `kernel/vfs/ops.h:L39`、`class IMetadata` at `kernel/vfs/ops.h:L40`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：class MutexObject, fn lock, fn __raw_spin_lock, fn __raw_spin_unlock, fn __raw_spin_lock。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/object/mutex.h` | L1-L4 | 关键词命中 |
| `kernel/object/mutex.cpp` | L1-L4 | 关键词命中 |
| `kernel/mem/gfp.h` | L15-L19 | 关键词命中 |
| `kernel/object/mutex.h` | L21-L25 | class MutexObject |
| `kernel/spinlock.h` | L29-L33 | fn lock |
| `kernel/arch/riscv64/spinlock.h` | L19-L23 | fn __raw_spin_lock |

### 关键代码片段

  - `kernel/object/mutex.h:L1-L4`：关键词命中
    代码片段：`/** * @file mutex.h * @author theflysong (song_of_the_fly@163.com) * @brief 互斥锁对象`
  - `kernel/object/mutex.cpp:L1-L4`：关键词命中
    代码片段：`/** * @file mutex.cpp * @author theflysong (song_of_the_fly@163.com) * @brief 互斥锁对象`
  - `kernel/mem/gfp.h:L15-L19`：关键词命中
    代码片段：`#include <mem/gfp_def.h> #include <sustcore/addr.h> #include <atomic> /**`
  - `kernel/object/mutex.h:L21-L25`：class MutexObject
    代码片段：`}; class MutexObject : public CapObj<MutexPayload> { public: explicit MutexObject(util::nonnull<Capability *> cap)`

### 相关符号

`class MutexObject` at `kernel/object/mutex.h:L21`、`fn lock` at `kernel/spinlock.h:L29`、`fn __raw_spin_lock` at `kernel/arch/riscv64/spinlock.h:L19`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn isr_entry, fn new_utask_trampoline, fn __switch_to, class Plic, struct Context。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/driver/int/plic.h` | L1-L4 | 关键词命中 |
| `kernel/driver/int/clint.h` | L1-L4 | 关键词命中 |
| `kernel/arch/riscv64/int/trap.S` | L1-L4 | 关键词命中 |
| `kernel/arch/riscv64/int/trap.S` | L27-L31 | fn isr_entry |
| `kernel/arch/riscv64/int/trap.S` | L182-L186 | fn new_utask_trampoline |
| `kernel/arch/riscv64/int/trap.S` | L188-L192 | fn __switch_to |

### 关键代码片段

  - `kernel/driver/int/plic.h:L1-L4`：关键词命中
    代码片段：`/** * @file plic.h * @author theflysong (song_of_the_fly@163.com) * @brief RISC-V PLIC 兼容转发头`
  - `kernel/driver/int/clint.h:L1-L4`：关键词命中
    代码片段：`/** * @file clint.h * @author theflysong (song_of_the_fly@163.com) * @brief RISC-V CLINT 兼容转发头`
  - `kernel/arch/riscv64/int/trap.S:L1-L4`：关键词命中
    代码片段：`/** * @file trap.S * @author theflysong (song_of_the_fly@163.com) * @brief 异常处理程序入口, 出口汇编代码`
  - `kernel/arch/riscv64/int/trap.S:L27-L31`：fn isr_entry
    代码片段：`/* ISR入口 */ .balign 4 isr_entry: save_context: /* 第1步: 切换到内核栈空间 */`

### 相关符号

`fn isr_entry` at `kernel/arch/riscv64/int/trap.S:L27`、`fn new_utask_trampoline` at `kernel/arch/riscv64/int/trap.S:L182`、`fn __switch_to` at `kernel/arch/riscv64/int/trap.S:L188`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct RegisteredBlockDevice, class BlkManager, fn init, fn initialized, struct CacheInfo。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/bio/blk.h` | L1-L4 | 关键词命中 |
| `kernel/bio/blk.cpp` | L1-L4 | 关键词命中 |
| `kernel/driver/base.h` | L13-L17 | 关键词命中 |
| `kernel/bio/blk.h` | L24-L28 | struct RegisteredBlockDevice |
| `kernel/bio/blk.h` | L31-L35 | class BlkManager |
| `kernel/bio/blk.h` | L48-L52 | fn init |

### 关键代码片段

  - `kernel/bio/blk.h:L1-L4`：关键词命中
    代码片段：`/** * @file blk.h * @author theflysong (song_of_the_fly@163.com) * @brief 块设备接口`
  - `kernel/bio/blk.cpp:L1-L4`：关键词命中
    代码片段：`/** * @file blk.cpp * @author theflysong (song_of_the_fly@163.com) * @brief 块设备管理器实现`
  - `kernel/driver/base.h:L13-L17`：关键词命中
    代码片段：`#include <cap/cholder.h> #include <device/device.h> #include <device/resource.h>`
  - `kernel/bio/blk.h:L24-L28`：struct RegisteredBlockDevice
    代码片段：`namespace blk { struct RegisteredBlockDevice { IBlockDeviceOps *device = nullptr; util::owner<BufferCache *> cache;`

### 相关符号

`struct RegisteredBlockDevice` at `kernel/bio/blk.h:L24`、`class BlkManager` at `kernel/bio/blk.h:L31`、`fn init` at `kernel/bio/blk.h:L48`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
