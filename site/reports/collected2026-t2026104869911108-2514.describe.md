# 2026 T2026104869911108 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026104869911108-2514`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：700
- 代码/文本行数：157000
- 主要语言：rust 115957 LOC, c 29425 LOC, markdown 5088 LOC, build 3073 LOC, make 1282 LOC

## 总览

2026 T2026104869911108 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 115957 LOC, c: 29425 LOC, markdown: 5088 LOC, build: 3073 LOC。仓库包含 700 个已扫描文件、约 157000 行可分析文本，当前抽取到 8162 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, Makefile, api/Cargo.toml, apps/junior/Makefile, apps/libc/Makefile。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] resolver = "2" members = ["api", "core", "crates/axprocess", "crates/axsignal"]`
  - `Makefile:L1-L3`：构建入口
    代码片段：`AX_ROOT ?= $(PWD)/arceos AX_TESTCASE ?= nimbos ARCH ?= x86_64`
  - `api/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "starry-api" version.workspace = true`
  - `apps/junior/Makefile:L1-L3`：构建入口
    代码片段：`ARCH ?= riscv64 TESTCASE_URL := https://github.com/Starry-OS/testcases/releases/download/v0.3/$(ARCH)_syscall_testcases.tar.gz`
  - `apps/libc/Makefile:L1-L3`：构建入口
    代码片段：`# Build testcases for rust and c programs ARCH ?= x86_64`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn new_user_task, struct TaskExt, fn new, fn time_stat_from_kernel_to_user, fn time_stat_from_user_to_kernel。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `core/src/task.rs` | L1-L3 | 关键词命中 |
| `src/mm.rs` | L30-L34 | 关键词命中 |
| `core/src/mm.rs` | L373-L377 | 关键词命中 |
| `core/src/task.rs` | L35-L39 | fn new_user_task |
| `core/src/task.rs` | L62-L66 | struct TaskExt |
| `core/src/task.rs` | L71-L75 | fn new |

### 关键代码片段

  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. use core::{`
  - `src/mm.rs:L30-L34`：关键词命中
    代码片段：`"{} ({:?}): segmentation fault at {:#x}, exit!", curr.id_name(), curr.task_ext().thread, vaddr );`
  - `core/src/mm.rs:L373-L377`：关键词命中
    代码片段：`} /// Check if the current thread is accessing user memory. pub fn is_accessing_user_memory() -> bool { ACCESSING_USER_MEM.read_current()`
  - `core/src/task.rs:L35-L39`：fn new_user_task
    代码片段：`/// Create a new user task. pub fn new_user_task( name: &str, uctx: UspaceContext,`

### 相关符号

`fn new_user_task` at `core/src/task.rs:L35`、`struct TaskExt` at `core/src/task.rs:L62`、`fn new` at `core/src/task.rs:L71`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn handle_page_fault, fn new_user_aspace_empty, fn copy_from_kernel, fn map_trampoline, fn map_elf。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `core/src/mm.rs` | L26-L30 | 关键词命中 |
| `arceos/modules/axmm/src/backend/alloc.rs` | L2-L6 | 关键词命中 |
| `core/src/task.rs` | L213-L217 | 关键词命中 |
| `src/mm.rs` | L10-L14 | fn handle_page_fault |
| `core/src/mm.rs` | L15-L19 | fn new_user_aspace_empty |
| `core/src/mm.rs` | L24-L28 | fn copy_from_kernel |

### 关键代码片段

  - `core/src/mm.rs:L26-L30`：关键词命中
    代码片段：`pub fn copy_from_kernel(aspace: &mut AddrSpace) -> AxResult { if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use se...`
  - `arceos/modules/axmm/src/backend/alloc.rs:L2-L6`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PhysAddr, VirtAddr};`
  - `core/src/task.rs:L213-L217`：关键词命中
    代码片段：`pub ns: AxNamespace, /// The user heap bottom heap_bottom: AtomicUsize, /// The user heap top heap_top: AtomicUsize,`
  - `src/mm.rs:L10-L14`：fn handle_page_fault
    代码片段：`#[register_trap_handler(PAGE_FAULT)] fn handle_page_fault(vaddr: VirtAddr, access_flags: MappingFlags, is_user: bool) -> bool { debug!( "Page fault at {:#x}, access_flags: {:#x?}",`

### 相关符号

`fn handle_page_fault` at `src/mm.rs:L10`、`fn new_user_aspace_empty` at `core/src/mm.rs:L15`、`fn copy_from_kernel` at `core/src/mm.rs:L24`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn handle_syscall, fn post_trap_callback, fn handle_syscall, fn syscall0, fn syscall1。（置信度：medium）
  - 静态识别到 325 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/syscall.rs` | L2-L6 | 关键词命中 |
| `vendor/syscalls/src/syscall/arm.rs` | L7-L11 | 关键词命中 |
| `vendor/syscalls/src/syscall/x86.rs` | L7-L11 | 关键词命中 |
| `src/syscall.rs` | L9-L13 | fn handle_syscall |
| `arceos/modules/axhal/src/trap.rs` | L43-L47 | fn post_trap_callback |
| `arceos/modules/axhal/src/trap.rs` | L51-L55 | fn handle_syscall |

### 关键代码片段

  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_api::*;`
  - `vendor/syscalls/src/syscall/arm.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %r5 // // %r7 is used for the syscall number. // // %r0 is reused for the syscall return value.`
  - `vendor/syscalls/src/syscall/x86.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %ebp // // eax is used for both the syscall number and the syscall return value. // // No other registers are clobbered. syscalls can also modify memory. With the`
  - `src/syscall.rs:L9-L13`：fn handle_syscall
    代码片段：`#[register_trap_handler(SYSCALL)] fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize { let sysno = Sysno::from(syscall_num as u32); info!("Syscall {}", sysno);`

### 相关符号

`fn handle_syscall` at `src/syscall.rs:L9`、`fn post_trap_callback` at `arceos/modules/axhal/src/trap.rs:L43`、`fn handle_syscall` at `arceos/modules/axhal/src/trap.rs:L51`、`fn handle_syscall` at `src/syscall.rs:L9`、`fn handle_syscall` at `arceos/modules/axhal/src/trap.rs:L51`、`fn syscall0` at `vendor/syscalls/src/syscall/arm.rs:L21`、`fn syscall1` at `vendor/syscalls/src/syscall/arm.rs:L39`、`fn syscall2` at `vendor/syscalls/src/syscall/arm.rs:L57`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：enum FatType, fn from_clusters, fn bits_per_fat_entry, fn min_clusters, fn max_clusters。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `vendor/rust-fatfs/src/fs.rs` | L20-L24 | 关键词命中 |
| `arceos/modules/axfs/src/fs/fatfs.rs` | L34-L38 | 关键词命中 |
| `arceos/modules/axfs/src/lib.rs` | L5-L9 | 关键词命中 |
| `vendor/rust-fatfs/src/fs.rs` | L28-L32 | enum FatType |
| `vendor/rust-fatfs/src/fs.rs` | L42-L46 | fn from_clusters |
| `vendor/rust-fatfs/src/fs.rs` | L52-L56 | fn bits_per_fat_entry |

### 关键代码片段

  - `vendor/rust-fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `arceos/modules/axfs/src/fs/fatfs.rs:L34-L38`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
  - `arceos/modules/axfs/src/lib.rs:L5-L9`：关键词命中
    代码片段：`//! # Cargo Features //! //! - 'fatfs': Use [FAT] as the main filesystem and mount it on '/'. This feature //!   is **enabled** by default. //! - 'devfs': Mount ['axfs_devfs::De...`
  - `vendor/rust-fatfs/src/fs.rs:L28-L32`：enum FatType
    代码片段：`/// 'FatType' values are based on the size of File Allocation Table entry. #[derive(Copy, Clone, Eq, PartialEq, Debug)] pub enum FatType { /// 12 bits per FAT entry Fat12,`

### 相关符号

`enum FatType` at `vendor/rust-fatfs/src/fs.rs:L28`、`fn from_clusters` at `vendor/rust-fatfs/src/fs.rs:L42`、`fn bits_per_fat_entry` at `vendor/rust-fatfs/src/fs.rs:L52`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn copy_inner, struct RawMutex, fn lock, fn try_lock, fn unlock。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `core/src/task.rs` | L4-L8 | 关键词命中 |
| `arceos/modules/axfs/src/root.rs` | L7-L11 | 关键词命中 |
| `arceos/modules/axsync/src/mutex.rs` | L1-L3 | 关键词命中 |
| `arceos/modules/axfs/src/root.rs` | L24-L28 | fn copy_inner |
| `arceos/modules/axsync/src/mutex.rs` | L10-L14 | struct RawMutex |
| `arceos/modules/axsync/src/mutex.rs` | L31-L35 | fn lock |

### 关键代码片段

  - `core/src/task.rs:L4-L8`：关键词命中
    代码片段：`alloc::Layout, cell::RefCell, sync::atomic::{AtomicUsize, Ordering}, time::Duration, };`
  - `arceos/modules/axfs/src/root.rs:L7-L11`：关键词命中
    代码片段：`use axfs_vfs::{VfsNodeAttr, VfsNodeOps, VfsNodeRef, VfsNodeType, VfsOps, VfsResult}; use axns::{ResArc, def_resource}; use axsync::Mutex; use lazyinit::LazyInit; use spin::RwLock;`
  - `arceos/modules/axsync/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! A naïve sleeping mutex. use core::sync::atomic::{AtomicU64, Ordering};`
  - `arceos/modules/axfs/src/root.rs:L24-L28`：fn copy_inner
    代码片段：`impl CURRENT_DIR_PATH { /// Return a copy of the inner path. pub fn copy_inner(&self) -> Mutex<String> { Mutex::new(self.lock().clone()) }`

### 相关符号

`fn copy_inner` at `arceos/modules/axfs/src/root.rs:L24`、`struct RawMutex` at `arceos/modules/axsync/src/mutex.rs:L10`、`fn lock` at `arceos/modules/axsync/src/mutex.rs:L31`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn dispatch_irq_common, fn register_handler_common, fn handler_irq, fn post_trap_callback, fn handle_syscall。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `arceos/modules/axhal/src/irq.rs` | L1-L3 | 关键词命中 |
| `arceos/modules/axhal/src/trap.rs` | L1-L3 | 关键词命中 |
| `arceos/modules/axhal/src/arch/riscv/trap.S` | L53-L57 | 关键词命中 |
| `arceos/modules/axhal/src/irq.rs` | L15-L19 | fn dispatch_irq_common |
| `arceos/modules/axhal/src/irq.rs` | L27-L31 | fn register_handler_common |
| `arceos/modules/axhal/src/irq.rs` | L37-L41 | fn handler_irq |

### 关键代码片段

  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `arceos/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
  - `arceos/modules/axhal/src/arch/riscv/trap.S:L53-L57`：关键词命中
    代码片段：`.global trap_vector_base trap_vector_base: // sscratch == 0: trap from S mode // sscratch != 0: trap from U mode csrrw   sp, sscratch, sp            // swap sscratch and sp`
  - `arceos/modules/axhal/src/irq.rs:L15-L19`：fn dispatch_irq_common
    代码片段：`/// Platform-independent IRQ dispatching. #[allow(dead_code)] pub(crate) fn dispatch_irq_common(irq_num: usize) { trace!("IRQ {}", irq_num); if !IRQ_HANDLER_TABLE.handle(irq_num) {`

### 相关符号

`fn dispatch_irq_common` at `arceos/modules/axhal/src/irq.rs:L15`、`fn register_handler_common` at `arceos/modules/axhal/src/irq.rs:L27`、`fn handler_irq` at `arceos/modules/axhal/src/irq.rs:L37`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：trait VirtIoDevMeta, fn try_new, struct VirtIoNet, struct VirtIoBlk, struct VirtIoGpu。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `arceos/modules/axdriver/src/virtio.rs` | L19-L23 | 关键词命中 |
| `arceos/modules/axdriver/src/drivers.rs` | L6-L10 | 关键词命中 |
| `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs` | L6-L10 | 关键词命中 |
| `arceos/modules/axdriver/src/virtio.rs` | L20-L24 | trait VirtIoDevMeta |
| `arceos/modules/axdriver/src/virtio.rs` | L26-L30 | fn try_new |
| `arceos/modules/axdriver/src/virtio.rs` | L31-L35 | struct VirtIoNet |

### 关键代码片段

  - `arceos/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `arceos/modules/axhal/src/platform/riscv64_qemu_virt/console.rs:L6-L10`：关键词命中
    代码片段：`const MAX_RW_SIZE: usize = 256; /// Writes a byte to the console. pub fn putchar(c: u8) { sbi_rt::console_write_byte(c);`
  - `arceos/modules/axdriver/src/virtio.rs:L20-L24`：trait VirtIoDevMeta
    代码片段：`/// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`

### 相关符号

`trait VirtIoDevMeta` at `arceos/modules/axdriver/src/virtio.rs:L20`、`fn try_new` at `arceos/modules/axdriver/src/virtio.rs:L26`、`struct VirtIoNet` at `arceos/modules/axdriver/src/virtio.rs:L31`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
