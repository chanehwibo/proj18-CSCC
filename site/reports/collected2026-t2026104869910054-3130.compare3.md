# 2026 T2026104869910054 比较报告

- 对比历史仓库：2023 Runable
- 生成时间：2026-07-02T05:01:06.429595+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2023 Runable（来源：赛事历史作品）：画像相似度 score=9.84；同属 rcore-variant 风格; 语言构成相似度 0.97; OS 维度重合度 1.00; 代码规模接近度 0.91

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2023 Runable 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/driver/src/bus.rs:L1-L5`：关键词命中
    代码片段：`//! Bus abstraction layer. //! //! Currently only MMIO bus is supported (used by VirtIO legacy MMIO and //! NS16550A UART on RISC-V platforms). PCI is a stub for future expansio...`
  - `crates/driver/src/trait.rs:L1-L3`：关键词命中
    代码片段：`//! Device trait definitions for the GoodOS driver subsystem. //! //! Defines the core traits that all device drivers must implement:`
  - `os/src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/block/mod.rs:L1-L3`：关键词命中
    代码片段：`mod virtio_blk; mod block_device;`
- 与 2023 Runable 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/fs/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! GoodOS 文件系统层 (VFS)。 //! //! 提供统一的文件系统接口，支持多种文件系统实现：`
  - `crates/fs/src/file.rs:L4-L8`：关键词命中
    代码片段：`//! 减少各文件系统实现的重复代码。 use crate::vfs::traits::File; use crate::vfs::FsResult;`
  - `os/src/fs/mod.rs:L1-L4`：关键词命中
    代码片段：`#![allow(unused)] mod inode; mod pipe; mod stdio;`
  - `os/src/fatfs/fs.rs:L76-L80`：关键词命中
    代码片段：`let cluster = { let mut fat = self.fat_slice(); match table_alloc_cluster(&mut fat, prev_cluster, hint, self.total_clusters) { Ok(x) => x,`
- 与 2023 Runable 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/kernel/src/trap_handler.rs:L1-L3`：关键词命中
    代码片段：`// Trap 分发器：时钟、系统调用、缺页。HAL 解码后回调本模块进行子系统分发。 use crate::log;`
  - `crates/hal/src/rv64/trap.S:L1-L3`：关键词命中
    代码片段：`# RISC-V trap assembly with an xv6/148-style trampoline. # # TrapContext layout (50 * 8 = 400 bytes):`
  - `os/src/timer.rs:L88-L92`：关键词命中
    代码片段：`let current_ms = get_time_ms(); let mut timers = TIMERS.exclusive_access(); while let Some(timer) = timers.peek() { if timer.expire_ms <= current_ms { add_task(Arc::clone(&timer...`
  - `os/src/trap/mod.rs:L9-L13`：关键词命中
    代码片段：`}; use self::page_fault::{handle_page_fault}; use crate::timer::{check_timer, set_next_trigger}; use core::arch::{asm, global_asm}; use riscv::register::{`
- 与 2023 Runable 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/mm/src/cow.rs:L1-L5`：关键词命中
    代码片段：`// Copy-on-write — stub. // // COW lets multiple processes share read-only physical pages.  When a process // writes to a COW page a page fault fires; the handler allocates a pr...`
  - `crates/mm/src/lib.rs:L2-L6`：关键词命中
    代码片段：`// // 提供地址空间管理、物理页分配、页表操作、用户态/内核态拷贝和 ELF 加载。 // 具体地址常量由 'platform' crate 提供；架构相关操作（TLB 刷新、satp 写入） // 通过 cfg 守卫区分 RISC-V / LoongArch。 //`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/mmap.rs:L1-L3`：关键词命中
    代码片段：`use super::{frame_alloc, FrameTracker, MapPermission, translated_byte_buffer, UserBuffer}; use super::{PTEFlags, PageTable}; use super::{VirtAddr, VirtPageNum};`
- 与 2023 Runable 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/task/src/task.rs:L1-L3`：关键词命中
    代码片段：`// Task —— simple task status type. use crate::task_id::Pid;`
  - `crates/task/src/task_id.rs:L1-L5`：关键词命中
    代码片段：`// Pid/Tid 全局分配器。 // // Pid/Tid share one Linux-style task-id namespace. use core::sync::atomic::{AtomicUsize, Ordering};`
  - `os/src/task/id.rs:L123-L127`：关键词命中
    代码片段：`pub ustack_top: usize,              // user stack top pub ustack_bottom: usize,          // user stack bottom // manager all the stacks(thread0 may have many stack frame, but ot...`
  - `os/src/task/mod.rs:L7-L11`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
- 与 2023 Runable 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/task/src/task_id.rs:L3-L7`：关键词命中
    代码片段：`// Pid/Tid share one Linux-style task-id namespace. use core::sync::atomic::{AtomicUsize, Ordering}; /// 进程 ID。`
  - `crates/mm/src/lib.rs:L20-L24`：关键词命中
    代码片段：`extern crate alloc; use core::sync::atomic::{AtomicUsize, Ordering}; mod aspace;`
  - `os/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`mod condvar; mod mutex; mod semaphore;`
  - `os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub trait Mutex: Sync + Send { fn lock(&self); fn unlock(&self);`
- 与 2023 Runable 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/syscall/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! Provides the 'syscall_instrument!' macro and the central dispatch function. //! All syscall implementations live in 'imp/' submodules, categorised by domain. #![no_std]`
  - `crates/syscall/src/consts.rs:L1-L5`：关键词命中
    代码片段：`// System call number constants. // // These match the Linux asm-generic syscall ABI used by RISC-V and LoongArch. pub const SYS_LSEEK: usize = 62;`
  - `os/src/syscall/mod.rs:L53-L57`：关键词命中
    代码片段：`use sys_info::*; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> isize { match syscall_id { SYSCALL_CHDIR => sys_chdir(args[0] as *const u8),`
  - `user/src/syscall.rs:L34-L38`：关键词命中
    代码片段：`// 目前系统调用参数除了id外最多6个 fn syscall(id: usize, args: [usize; 6]) -> isize { let mut ret: isize; unsafe {`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2023 Runable 在“文件系统”维度发现 4 个同名定义：new, name, drop, read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `crates/fs/src/block_cache.rs:L77-L77`：fn new
    代码片段：`pub fn new(device: &'a dyn BlockDevice) -> Self {`
  - `os/src/fatfs/dir_entry.rs:L109-L109`：fn new
    代码片段：`pub fn new(name: [u8; SFN_SIZE], attrs: DirAttr) -> Self {`
  - `crates/fs/src/block_cache.rs:L136-L136`：fn name
    代码片段：`fn name(&self) -> &'static str {`
  - `os/src/fatfs/dir_entry.rs:L129-L129`：fn name
    代码片段：`pub fn name(&self) -> &[u8; SFN_SIZE] {`
- 函数/符号名重合：与 2023 Runable 在“设备驱动”维度发现 1 个同名定义：getchar。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/driver/src/impls/char/ns16550a.rs:L82-L82`：fn getchar
    代码片段：`fn getchar(&self) -> Option<u8> {`
  - `user/src/console.rs:L35-L35`：fn getchar
    代码片段：`pub fn getchar() -> u8 {`
- 函数/符号名重合：与 2023 Runable 在“内存管理”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/driver/src/impls/net/virtio_net.rs:L8-L8`：fn new
    代码片段：`pub fn new(mmio: MmioInfo) -> Self { Self { mmio, mac: [0; 6] } }`
  - `user/src/bin/user_shell.rs:L30-L30`：fn new
    代码片段：`pub fn new(command: &str) -> Self {`
- 函数/符号名重合：与 2023 Runable 在“同步机制”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/driver/src/impls/block/ramdisk.rs:L20-L20`：fn new
    代码片段：`pub fn new(name: &'static str, block_size: usize, num_blocks: usize) -> Self {`
  - `user/src/bin/barrier_condvar.rs:L25-L25`：fn new
    代码片段：`pub fn new() -> Self {`
- 结构体/类型重合：与 2023 Runable 在“文件系统”维度发现 2 个同名定义：Inode, DirEntry。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/fs/src/impls/ext4/inode.rs:L23-L23`：struct Inode
    代码片段：`pub struct Inode {`
  - `os/src/fatfs/file.rs:L191-L191`：enum Inode
    代码片段：`pub enum Inode {`
  - `crates/fs/src/impls/ext4/inode.rs:L104-L104`：struct DirEntry
    代码片段：`pub struct DirEntry {`
  - `os/src/fatfs/dir_entry.rs:L350-L350`：struct DirEntry
    代码片段：`pub struct DirEntry {`
- 文件路径重合：与 2023 Runable 在“系统调用”维度出现同名文件源码路径 `crates/syscall/src/imp/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `crates/syscall/src/imp/fs.rs:L726-L730`：fn sys_write
    代码片段：`#[inline(always)] pub fn sys_write(fd: usize, buf_ptr: usize, len: usize) -> isize { if len == 0 { return 0;`
  - `os/src/syscall/fs.rs:L9-L13`：fn sys_getcwd
    代码片段：`pub fn sys_getcwd(buf: *const u8, size: usize) -> isize{ let token = current_user_token(); let process = current_process();`

## 相似点

- 与 2023 Runable 同属 rcore-variant 风格。（置信度：medium）
- 与 2023 Runable 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/driver/src/bus.rs:L1-L5`：关键词命中
    代码片段：`//! Bus abstraction layer. //! //! Currently only MMIO bus is supported (used by VirtIO legacy MMIO and //! NS16550A UART on RISC-V platforms). PCI is a stub for future expansio...`
  - `crates/driver/src/trait.rs:L1-L3`：关键词命中
    代码片段：`//! Device trait definitions for the GoodOS driver subsystem. //! //! Defines the core traits that all device drivers must implement:`
- 与 2023 Runable 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/fs/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! GoodOS 文件系统层 (VFS)。 //! //! 提供统一的文件系统接口，支持多种文件系统实现：`
  - `crates/fs/src/file.rs:L4-L8`：关键词命中
    代码片段：`//! 减少各文件系统实现的重复代码。 use crate::vfs::traits::File; use crate::vfs::FsResult;`
- 与 2023 Runable 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/kernel/src/trap_handler.rs:L1-L3`：关键词命中
    代码片段：`// Trap 分发器：时钟、系统调用、缺页。HAL 解码后回调本模块进行子系统分发。 use crate::log;`
  - `crates/hal/src/rv64/trap.S:L1-L3`：关键词命中
    代码片段：`# RISC-V trap assembly with an xv6/148-style trampoline. # # TrapContext layout (50 * 8 = 400 bytes):`
- 与 2023 Runable 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/mm/src/cow.rs:L1-L5`：关键词命中
    代码片段：`// Copy-on-write — stub. // // COW lets multiple processes share read-only physical pages.  When a process // writes to a COW page a page fault fires; the handler allocates a pr...`
  - `crates/mm/src/lib.rs:L2-L6`：关键词命中
    代码片段：`// // 提供地址空间管理、物理页分配、页表操作、用户态/内核态拷贝和 ELF 加载。 // 具体地址常量由 'platform' crate 提供；架构相关操作（TLB 刷新、satp 写入） // 通过 cfg 守卫区分 RISC-V / LoongArch。 //`
- 与 2023 Runable 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/task/src/task.rs:L1-L3`：关键词命中
    代码片段：`// Task —— simple task status type. use crate::task_id::Pid;`
  - `crates/task/src/task_id.rs:L1-L5`：关键词命中
    代码片段：`// Pid/Tid 全局分配器。 // // Pid/Tid share one Linux-style task-id namespace. use core::sync::atomic::{AtomicUsize, Ordering};`
- 与 2023 Runable 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/task/src/task_id.rs:L3-L7`：关键词命中
    代码片段：`// Pid/Tid share one Linux-style task-id namespace. use core::sync::atomic::{AtomicUsize, Ordering}; /// 进程 ID。`
  - `crates/mm/src/lib.rs:L20-L24`：关键词命中
    代码片段：`extern crate alloc; use core::sync::atomic::{AtomicUsize, Ordering}; mod aspace;`
- 与 2023 Runable 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/syscall/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! Provides the 'syscall_instrument!' macro and the central dispatch function. //! All syscall implementations live in 'imp/' submodules, categorised by domain. #![no_std]`
  - `crates/syscall/src/consts.rs:L1-L5`：关键词命中
    代码片段：`// System call number constants. // // These match the Linux asm-generic syscall ABI used by RISC-V and LoongArch. pub const SYS_LSEEK: usize = 62;`
- 与 2023 Runable 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2023 Runable 的语言构成不同：待测作品为 {'toml': 44, 'build': 693, 'markdown': 681, 'rust': 46328, 'asm': 618}，历史样本为 {'json': 20, 'build': 593, 'markdown': 1658, 'toml': 7, 'text': 283, 'rust': 40526, 'asm': 569, 'yaml': 79, 'c': 48}。（置信度：medium）

## 可能创新点

- 待测作品包含 295 个历史样本 2023 Runable 中未见的独特函数/类型定义（如 `raw_read_cache_block`, `raw_write_cache_block`, `raw_read_cache_blocks`, `read_bytes_from`, `get_scratch_block_device`, `lookup_child_ino`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `crates/fs/src/impls/ext4/super_block.rs:L462-L462`：fn raw_read_cache_block
    代码片段：`fn raw_read_cache_block(dev: &'static dyn BlockDevice, base: usize, data: &mut [u8; EXT4_CACHE_BLOCK_SIZE]) -> Result<(), ()> {`
  - `crates/fs/src/impls/ext4/super_block.rs:L498-L498`：fn raw_write_cache_block
    代码片段：`fn raw_write_cache_block(dev: &'static dyn BlockDevice, base: usize, data: &[u8; EXT4_CACHE_BLOCK_SIZE]) -> Result<(), ()> {`

## 附录：核验摘要

- 关键结论数：21
- 含证据关键结论数：21（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
