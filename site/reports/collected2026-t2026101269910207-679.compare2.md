# 2026 T2026101269910207 比较报告

- 对比历史仓库：2024 ChCore
- 生成时间：2026-07-02T04:59:10.377353+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 ChCore（来源：赛事历史作品）：画像相似度 score=9.69；同属 ucore-variant 风格; 语言构成相似度 0.95; OS 维度重合度 1.00; 代码规模接近度 0.79

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 ChCore 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/pm/pm.c:L8-L12`：关键词命中
    代码片段：`* 2012-06-02     Bernard      the first version * 2018-08-02     Tanek        split run and sleep modes, support custom mode * 2019-04-28     Zero-Free    improve PM mode and de...`
  - `rt-thread/components/drivers/ofw/io.c:L268-L272`：关键词命中
    代码片段：`*          ranges = <0x0 0xe0000000 0x00100000>; * *          serial@4600 { *              device_type = "serial"; *              reg = <0x4600 0x100>;`
  - `kernel/include/io/uart.h:L14-L18`：关键词命中
    代码片段：`#define IO_UART_H /* Use mini-uart or PL011 */ #define USE_mini_uart 0`
  - `kernel/arch/riscv64/drivers/uart/uart.c:L11-L15`：关键词命中
    代码片段：`*/ #include <io/uart.h> #include <arch/machine/sbi.h>`
- 与 2024 ChCore 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/pci/procfs.c:L67-L71`：关键词命中
    代码片段：`size_t res = count; rt_ubase_t pm_flags; struct proc_dentry *dentry = file->vnode->data; struct rt_pci_device *pdev = dentry->data;`
  - `rt-thread/components/dfs/dfs_v2/src/dfs_fs.c:L271-L275`：关键词命中
    代码片段：`/* it's the root file system */ /* the mount point dentry is the same as root dentry. */ /* Create root filesystem mount point */ DLOG(msg, "dfs", "mnt", DLOG_MSG, "mnt_parent =...`
  - `ltp/testcases/kernel/syscalls/sync_file_range/sync_file_range02.c:L104-L108`：关键词命中
    代码片段：`/* * Fat does not support sparse files, we have to pre-fill the file so * that the zero-filled start of the file has been written to disk * before the test starts.`
  - `ltp/testcases/kernel/syscalls/dup2/dup204.c:L7-L11`：关键词命中
    代码片段：`* [Description] * * Test whether the inode number are the same for both file descriptors. */`
- 与 2024 ChCore 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/ofw/irq.c:L27-L31`：关键词命中
    代码片段：`int interrupt_cells = -RT_EEMPTY; rt_ofw_prop_read_u32(np, "#interrupt-cells", (rt_uint32_t *)&interrupt_cells); return interrupt_cells;`
  - `rt-thread/components/drivers/pci/irq.c:L11-L15`：关键词命中
    代码片段：`#include <rtthread.h> #define DBG_TAG "pci.irq" #define DBG_LVL DBG_INFO #include <rtdbg.h>`
  - `kernel/irq/ipi.c:L12-L16`：关键词命中
    代码片段：`#include <arch/sync.h> #include <irq/ipi.h> #include <machine.h> #include <common/kprint.h>`
  - `kernel/irq/timer.c:L11-L15`：关键词命中
    代码片段：`*/ #include <irq/irq.h> #include <irq/timer.h> #include <sched/sched.h>`
- 与 2024 ChCore 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/mm/mm_anon.c:L6-L10`：关键词命中
    代码片段：`* Change Logs: * Date           Author       Notes * 2023-08-19     Shell        Support PRIVATE mapping and COW */`
  - `rt-thread/components/mm/mm_page.c:L1248-L1252`：关键词命中
    代码片段：`pages_alloc_handler = _early_pages_alloc; /* doing the page table bushiness */ if (rt_aspace_load_page(&rt_kernel_space, (void *)init_mpr_align_start, init_mpr_npage)) {`
  - `kernel/mm/kmalloc.c:L176-L180`：关键词命中
    代码片段：`} void kfree(void *ptr) { _kfree(ptr, true);`
  - `kernel/mm/vmspace.c:L64-L68`：关键词命中
    代码片段：`static void free_cow_private_page(struct cow_private_page *record) { kfree(record->page); kfree(record); }`
- 与 2024 ChCore 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/pm/pm.c:L1-L4`：关键词命中
    代码片段：`/* * Copyright (c) 2006-2024 RT-Thread Development Team * * SPDX-License-Identifier: Apache-2.0`
  - `rt-thread/components/drivers/ofw/io.c:L1-L4`：关键词命中
    代码片段：`/* * Copyright (c) 2006-2022, RT-Thread Development Team * * SPDX-License-Identifier: Apache-2.0`
  - `kernel/sched/sched.c:L18-L22`：关键词命中
    代码片段：`#include <common/util.h> /* Scheduler global data */ struct thread *current_threads[PLAT_CPU_NUM]; struct thread idle_threads[PLAT_CPU_NUM];`
  - `kernel/object/thread.c:L20-L24`：关键词命中
    代码片段：`#include <mm/mm.h> #include <mm/uaccess.h> #include <object/thread.h> #include <object/recycle.h> #ifdef CHCORE_SIGNAL`
- 与 2024 ChCore 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/dma/dma.c:L24-L28`：关键词命中
    代码片段：`if (rt_thread_self()) { rt_mutex_take(&ctrl->mutex, RT_WAITING_FOREVER); } }`
  - `rt-thread/components/drivers/led/led.c:L165-L169`：关键词命中
    代码片段：`led->sysdata = RT_NULL; rt_spin_lock_init(&led->spinlock); if (!led->ops->set_period && led->ops->set_state)`
  - `kernel/include/common/lock.h:L16-L20`：关键词命中
    代码片段：`#include <common/types.h> /* Simple/Compact ticket/rwlock impl */ struct lock {`
  - `kernel/arch/riscv64/sync/rwlock.c:L14-L18`：关键词命中
    代码片段：`#include <common/errno.h> #include <common/macro.h> #include <common/lock.h> #include <arch/sync.h>`
- 与 2024 ChCore 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `rt-thread/components/lwp/lwp_syscall.c:L7-L11`：关键词命中
    代码片段：`* Date           Author       Notes * 2018-06-10     Bernard      first version * 2021-02-03     lizhirui     add limit condition for network syscall and add 64-bit arch support...`
  - `rt-thread/components/lwp/syscall_generic.h:L7-L11`：关键词命中
    代码片段：`* Date           Author       Notes * 2022-11-10     RT-Thread    The first version * 2023-03-13     WangXiaoyao  syscall metadata as structure */ #ifndef __SYSCALL_DATA_H__`
  - `unixbench/src/syscall.c:L1-L5`：关键词命中
    代码片段：`/******************************************************************************* *  The BYTE UNIX Benchmarks - Release 3 *          Module: syscall.c   SID: 3.3 5/15/91 19:30:21...`
  - `kernel/syscall/syscall.c:L36-L40`：关键词命中
    代码片段：`#include <irq/irq.h> #include <common/poweroff.h> #include <syscall/opentrustee.h> #ifdef CHCORE_OPENTRUSTEE #include <arch/trustzone/smc.h>`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 ChCore 在“系统调用”维度发现片段级代码相似度 0.64 （token=0.53, structure=0.86）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：long, struct, unsigned, void。（置信度：medium）
  证据：
  - `rt-thread/components/lwp/lwp_syscall.c:L6402-L6406`：struct k_sigaction
    代码片段：`} struct k_sigaction { void (*handler)(int); unsigned long flags;`
  - `kernel/object/signal.c:L315-L319`：fn sys_rt_sigreturn
    代码片段：`unsigned long sys_rt_sigreturn(void) { struct rt_sigframe *frame;`
- 文件路径重合：与 2024 ChCore 在“中断与异常”维度出现同名文件源码路径 `rt-thread/components/drivers/ofw/irq.c` / `kernel/object/irq.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/ofw/irq.c:L27-L31`：关键词命中
    代码片段：`int interrupt_cells = -RT_EEMPTY; rt_ofw_prop_read_u32(np, "#interrupt-cells", (rt_uint32_t *)&interrupt_cells); return interrupt_cells;`
  - `kernel/object/irq.c:L13-L17`：关键词命中
    代码片段：`#include <arch/sync.h> #include <ipc/notification.h> #include <irq/irq.h> #include <object/irq.h> #include <common/errno.h>`
- 文件路径重合：与 2024 ChCore 在“中断与异常”维度出现同名文件源码路径 `rt-thread/components/drivers/pci/irq.c` / `kernel/object/irq.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/pci/irq.c:L11-L15`：关键词命中
    代码片段：`#include <rtthread.h> #define DBG_TAG "pci.irq" #define DBG_LVL DBG_INFO #include <rtdbg.h>`
  - `kernel/object/irq.c:L13-L17`：关键词命中
    代码片段：`#include <arch/sync.h> #include <ipc/notification.h> #include <irq/irq.h> #include <object/irq.h> #include <common/errno.h>`

## 相似点

- 与 2024 ChCore 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 ChCore 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/pm/pm.c:L8-L12`：关键词命中
    代码片段：`* 2012-06-02     Bernard      the first version * 2018-08-02     Tanek        split run and sleep modes, support custom mode * 2019-04-28     Zero-Free    improve PM mode and de...`
  - `rt-thread/components/drivers/ofw/io.c:L268-L272`：关键词命中
    代码片段：`*          ranges = <0x0 0xe0000000 0x00100000>; * *          serial@4600 { *              device_type = "serial"; *              reg = <0x4600 0x100>;`
- 与 2024 ChCore 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/pci/procfs.c:L67-L71`：关键词命中
    代码片段：`size_t res = count; rt_ubase_t pm_flags; struct proc_dentry *dentry = file->vnode->data; struct rt_pci_device *pdev = dentry->data;`
  - `rt-thread/components/dfs/dfs_v2/src/dfs_fs.c:L271-L275`：关键词命中
    代码片段：`/* it's the root file system */ /* the mount point dentry is the same as root dentry. */ /* Create root filesystem mount point */ DLOG(msg, "dfs", "mnt", DLOG_MSG, "mnt_parent =...`
- 与 2024 ChCore 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/ofw/irq.c:L27-L31`：关键词命中
    代码片段：`int interrupt_cells = -RT_EEMPTY; rt_ofw_prop_read_u32(np, "#interrupt-cells", (rt_uint32_t *)&interrupt_cells); return interrupt_cells;`
  - `rt-thread/components/drivers/pci/irq.c:L11-L15`：关键词命中
    代码片段：`#include <rtthread.h> #define DBG_TAG "pci.irq" #define DBG_LVL DBG_INFO #include <rtdbg.h>`
- 与 2024 ChCore 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/mm/mm_anon.c:L6-L10`：关键词命中
    代码片段：`* Change Logs: * Date           Author       Notes * 2023-08-19     Shell        Support PRIVATE mapping and COW */`
  - `rt-thread/components/mm/mm_page.c:L1248-L1252`：关键词命中
    代码片段：`pages_alloc_handler = _early_pages_alloc; /* doing the page table bushiness */ if (rt_aspace_load_page(&rt_kernel_space, (void *)init_mpr_align_start, init_mpr_npage)) {`
- 与 2024 ChCore 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/pm/pm.c:L1-L4`：关键词命中
    代码片段：`/* * Copyright (c) 2006-2024 RT-Thread Development Team * * SPDX-License-Identifier: Apache-2.0`
  - `rt-thread/components/drivers/ofw/io.c:L1-L4`：关键词命中
    代码片段：`/* * Copyright (c) 2006-2022, RT-Thread Development Team * * SPDX-License-Identifier: Apache-2.0`
- 与 2024 ChCore 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/drivers/dma/dma.c:L24-L28`：关键词命中
    代码片段：`if (rt_thread_self()) { rt_mutex_take(&ctrl->mutex, RT_WAITING_FOREVER); } }`
  - `rt-thread/components/drivers/led/led.c:L165-L169`：关键词命中
    代码片段：`led->sysdata = RT_NULL; rt_spin_lock_init(&led->spinlock); if (!led->ops->set_period && led->ops->set_state)`
- 与 2024 ChCore 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `rt-thread/components/lwp/lwp_syscall.c:L7-L11`：关键词命中
    代码片段：`* Date           Author       Notes * 2018-06-10     Bernard      first version * 2021-02-03     lizhirui     add limit condition for network syscall and add 64-bit arch support...`
  - `rt-thread/components/lwp/syscall_generic.h:L7-L11`：关键词命中
    代码片段：`* Date           Author       Notes * 2022-11-10     RT-Thread    The first version * 2023-03-13     WangXiaoyao  syscall metadata as structure */ #ifndef __SYSCALL_DATA_H__`
- 与 2024 ChCore 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 ChCore 的语言构成不同：待测作品为 {'json': 362, 'markdown': 13785, 'build': 318, 'c': 860328, 'asm': 33452, 'text': 5182, 'yaml': 3094, 'rust': 3462, 'cpp': 1671}，历史样本为 {'json': 35, 'text': 15034, 'build': 11544, 'make': 1866, 'markdown': 6258, 'c': 1165153, 'yaml': 32, 'asm': 6070, 'cpp': 1510}。（置信度：medium）

## 可能创新点

- 待测作品包含 12 个历史样本 2024 ChCore 中未见的独特函数/类型定义（如 `rt_interrupt_context`, `lwp_siginfo_ext`, `lwp_sigqueue`, `lwp_siginfo`, `loongarch_user_after_exit`, `lwp_thread_signal`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `rt-thread/include/rtdef.h:L755-L755`：struct rt_interrupt_context
    代码片段：`typedef struct rt_interrupt_context {`
  - `rt-thread/include/rtdef.h:L801-L801`：struct lwp_siginfo_ext
    代码片段：`typedef struct lwp_siginfo_ext {`

## 附录：核验摘要

- 关键结论数：18
- 含证据关键结论数：18（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
