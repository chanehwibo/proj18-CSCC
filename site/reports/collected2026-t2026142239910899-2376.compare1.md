# 2026 T2026142239910899 比较报告

- 对比历史仓库：2021 TeamNameNotFound
- 生成时间：2026-07-02T09:04:02.685467+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2021 TeamNameNotFound（来源：赛事历史作品）：画像相似度 score=8.07；同属 ucore-variant 风格; 语言构成相似度 0.71; OS 维度重合度 0.83; 代码规模接近度 0.32

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2021 TeamNameNotFound 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L3`：关键词命中
    代码片段：`#include "uart.h" static inline uint8_t uart_read_reg(uint32_t offset) { return *(volatile uint8_t *)(UART_BASE + offset); } static inline void uart_write_reg(uint32_t offset, u...`
  - `kernel/driver/clock.c:L2-L6`：fn set_next_timer
    代码片段：`#include "sbi.h" void set_next_timer() { uint64 temp; csrr(time, temp);`
  - `kernel/driver/clock.c:L9-L13`：fn clock_init
    代码片段：`} void clock_init() { uint64 temp; csrr(sie, temp);`
- 与 2021 TeamNameNotFound 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap.c:L1-L3`：关键词命中
    代码片段：`#include "trap.h" #include "uart.h" #include "sched.h"`
  - `kernel/main.c:L2-L6`：关键词命中
    代码片段：`#include "uart.h" #include "mem.h" #include "trap.h" #include "sched.h" #include "string.h"`
  - `kernel/trap/trap.c:L1-L3`：关键词命中
    代码片段：`#include "trap.h" #include "types.h" #include "riscv.h" // csrr()`
  - `kernel/trap/trap.h:L8-L12`：关键词命中
    代码片段：`// kernel stack of this app uint64 kstack; // pc after trap uint64 epc; uint64 ra;`
- 与 2021 TeamNameNotFound 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mem.c:L4-L8`：关键词命中
    代码片段：`static struct page *pages = NULL; static uint64_t num_pages = 0, free_pages = 0; static char *heap_ptr = NULL; void mem_init(void) { uint64_t kernel_end = (uint64_t)_kernel_end;`
  - `include/mem.h:L13-L16`：关键词命中
    代码片段：`uint64_t get_total_pages(void); void *kmalloc(size_t size); void kfree(void *ptr); #endif`
  - `kernel/mem/pmm.c:L1-L3`：关键词命中
    代码片段：`#include "pmm.h" #include "list.h" #include "types.h"// ROUNDUP`
  - `kernel/mem/pmm.h:L12-L16`：关键词命中
    代码片段：`void free_a_page(uint64 start); void pmm_init(); uint64 kalloc(uint64 size); void kfree(uint64 start, uint64 size);`
- 与 2021 TeamNameNotFound 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sched.c:L4-L8`：关键词命中
    代码片段：`#include "trap.h" #include "string.h" static struct proc proc_table[PROC_MAX_NUM]; static struct proc *current = NULL; static uint32_t next_pid = 1;`
  - `kernel/main.c:L7-L11`：关键词命中
    代码片段：`static void test_process_a(void) { uart_puts("[PROC-A] Hello from process A!\n"); for (int i = 0; i < 3; i++) { uart_puts("[PROC-A] iter "); uart_int(i+1); uart_puts("/3\n"); }...`
  - `kernel/proc/proc.c:L1-L3`：关键词命中
    代码片段：`#include "proc.h" #include "panic.h" #include "page.h"`
  - `kernel/proc/proc.h:L12-L16`：关键词命中
    代码片段：`// return address uint64 ra; // proc stack pointer uint64 sp; uint64 s0;`
- 与 2021 TeamNameNotFound 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap.c:L13-L17`：关键词命中
    代码片段：`static void handle_exception(uint64_t cause, uint64_t sepc, uint64_t stval) { switch (cause) { case EXC_ECALL_U: case EXC_ECALL_S: uart_puts("[TRAP] Ecall from "); uart_puts(cau...`
  - `kernel/trap/syscall.c:L28-L32`：关键词命中
    代码片段：`} /* @brief: syscall dispatcher */ void syscall(struct trapframe *tf_ptr) {`
  - `kernel/libs/sbi.c:L13-L17`：关键词命中
    代码片段：`"mv a1, %[arg1]\n" "mv a2, %[arg2]\n" "ecall\n" "mv %[ret], a7" : [ret] "=r" (ret)`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2021 TeamNameNotFound 在“中断与异常”维度发现 2 个同名定义：set_next_timer, trap_init。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/sched.c:L16-L16`：fn set_next_timer
    代码片段：`void set_next_timer(void) { *(volatile uint64_t *)CLINT_MTIMECMP = read_time() + TIMER_FREQ / TICKS_PER_SEC; }`
  - `kernel/driver/clock.c:L4-L4`：fn set_next_timer
    代码片段：`void set_next_timer() {`
  - `kernel/trap.c:L4-L4`：fn trap_init
    代码片段：`void trap_init(void) { uart_puts("[TRAP] Trap handler initialized\n"); }`
  - `kernel/trap/trap.c:L9-L9`：fn trap_init
    代码片段：`void trap_init() {`
- 函数/符号名重合：与 2021 TeamNameNotFound 在“内存管理”维度发现 1 个同名定义：kfree。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/mem.c:L41-L41`：fn kfree
    代码片段：`void kfree(void *ptr) { (void)ptr; }`
  - `kernel/mem/pmm.c:L120-L120`：fn kfree
    代码片段：`void kfree(uint64 start, uint64 size) {`
- 函数/符号名重合：与 2021 TeamNameNotFound 在“调度与任务管理”维度发现 1 个同名定义：proc_exit。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/sched.c:L51-L51`：fn proc_exit
    代码片段：`void proc_exit(void) {`
  - `kernel/proc/proc.c:L366-L366`：fn proc_exit
    代码片段：`void proc_exit(int ec) {`
- 函数/符号名重合：与 2021 TeamNameNotFound 在“系统调用”维度发现 1 个同名定义：trap_init。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/trap.c:L4-L4`：fn trap_init
    代码片段：`void trap_init(void) { uart_puts("[TRAP] Trap handler initialized\n"); }`
  - `kernel/trap/trap.c:L9-L9`：fn trap_init
    代码片段：`void trap_init() {`
- 结构体/类型重合：与 2021 TeamNameNotFound 在“调度与任务管理”维度发现 2 个同名定义：context, proc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/sched.h:L8-L8`：struct context
    代码片段：`struct context {`
  - `kernel/proc/proc.h:L11-L11`：struct context
    代码片段：`struct context {`
  - `include/sched.h:L13-L13`：struct proc
    代码片段：`struct proc {`
  - `kernel/proc/proc.h:L35-L35`：struct proc
    代码片段：`struct proc {`
- 文件路径重合：与 2021 TeamNameNotFound 在“中断与异常”维度出现同名文件源码路径 `kernel/trap.c` / `kernel/trap/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/trap.c:L1-L3`：关键词命中
    代码片段：`#include "trap.h" #include "uart.h" #include "sched.h"`
  - `kernel/trap/trap.c:L1-L3`：关键词命中
    代码片段：`#include "trap.h" #include "types.h" #include "riscv.h" // csrr()`
- 文件路径重合：与 2021 TeamNameNotFound 在“系统调用”维度出现同名文件源码路径 `kernel/trap.c` / `kernel/trap/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/trap.c:L13-L17`：关键词命中
    代码片段：`static void handle_exception(uint64_t cause, uint64_t sepc, uint64_t stval) { switch (cause) { case EXC_ECALL_U: case EXC_ECALL_S: uart_puts("[TRAP] Ecall from "); uart_puts(cau...`
  - `kernel/trap/trap.c:L7-L11`：fn trap_init
    代码片段：`#include "stdio.h" // puts() void trap_init() { uint64 temp; csrw(stvec, trap_entry);`

## 相似点

- 与 2021 TeamNameNotFound 同属 ucore-variant 风格。（置信度：medium）
- 与 2021 TeamNameNotFound 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L3`：关键词命中
    代码片段：`#include "uart.h" static inline uint8_t uart_read_reg(uint32_t offset) { return *(volatile uint8_t *)(UART_BASE + offset); } static inline void uart_write_reg(uint32_t offset, u...`
  - `kernel/driver/clock.c:L2-L6`：fn set_next_timer
    代码片段：`#include "sbi.h" void set_next_timer() { uint64 temp; csrr(time, temp);`
- 与 2021 TeamNameNotFound 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap.c:L1-L3`：关键词命中
    代码片段：`#include "trap.h" #include "uart.h" #include "sched.h"`
  - `kernel/main.c:L2-L6`：关键词命中
    代码片段：`#include "uart.h" #include "mem.h" #include "trap.h" #include "sched.h" #include "string.h"`
- 与 2021 TeamNameNotFound 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mem.c:L4-L8`：关键词命中
    代码片段：`static struct page *pages = NULL; static uint64_t num_pages = 0, free_pages = 0; static char *heap_ptr = NULL; void mem_init(void) { uint64_t kernel_end = (uint64_t)_kernel_end;`
  - `include/mem.h:L13-L16`：关键词命中
    代码片段：`uint64_t get_total_pages(void); void *kmalloc(size_t size); void kfree(void *ptr); #endif`
- 与 2021 TeamNameNotFound 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sched.c:L4-L8`：关键词命中
    代码片段：`#include "trap.h" #include "string.h" static struct proc proc_table[PROC_MAX_NUM]; static struct proc *current = NULL; static uint32_t next_pid = 1;`
  - `kernel/main.c:L7-L11`：关键词命中
    代码片段：`static void test_process_a(void) { uart_puts("[PROC-A] Hello from process A!\n"); for (int i = 0; i < 3; i++) { uart_puts("[PROC-A] iter "); uart_int(i+1); uart_puts("/3\n"); }...`
- 与 2021 TeamNameNotFound 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap.c:L13-L17`：关键词命中
    代码片段：`static void handle_exception(uint64_t cause, uint64_t sepc, uint64_t stval) { switch (cause) { case EXC_ECALL_U: case EXC_ECALL_S: uart_puts("[TRAP] Ecall from "); uart_puts(cau...`
  - `kernel/trap/syscall.c:L28-L32`：关键词命中
    代码片段：`} /* @brief: syscall dispatcher */ void syscall(struct trapframe *tf_ptr) {`
- 与 2021 TeamNameNotFound 在 driver, interrupt, memory, scheduler, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2021 TeamNameNotFound 的语言构成不同：待测作品为 {'build': 25, 'markdown': 20, 'asm': 97, 'c': 334}，历史样本为 {'json': 20, 'build': 122, 'markdown': 1316, 'c': 2397, 'asm': 154, 'text': 3}。（置信度：medium）

## 可能创新点

- 待测作品包含 27 个历史样本 2021 TeamNameNotFound 中未见的独特函数/类型定义（如 `uart_write_reg`, `disable_interrupt`, `enable_interrupt`, `uart_putc`, `uart_read_reg`, `uart_getc`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/uart.c:L3-L3`：fn uart_write_reg
    代码片段：`static inline void uart_write_reg(uint32_t offset, uint8_t value) { *(volatile uint8_t *)(UART_BASE + offset) = value; }`
  - `include/trap.h:L21-L21`：fn disable_interrupt
    代码片段：`static inline void disable_interrupt(void) { __asm__ volatile ("csrci sstatus, %0" : : "i"(SSTATUS_SIE)); }`

## 附录：核验摘要

- 关键结论数：18
- 含证据关键结论数：18（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
