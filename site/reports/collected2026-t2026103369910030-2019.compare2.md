# 2026 T2026103369910030 比较报告

- 对比历史仓库：2023 BiteTheDisk
- 生成时间：2026-07-01T20:57:39.755136+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2023 BiteTheDisk（来源：赛事历史作品）：画像相似度 score=9.38；同属 rcore-variant 风格; 语言构成相似度 0.96; OS 维度重合度 1.00; 代码规模接近度 0.46

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2023 BiteTheDisk 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernelx/vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `kernelx/vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
  - `kernel/src/console.rs:L36-L40`：关键词命中
    代码片段：`macro_rules! print { ($fmt:literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); }; }`
  - `kernel/src/drivers/mod.rs:L25-L29`：关键词命中
    代码片段：`}); /// Initialize platform specific device drivers. pub fn init() { #[cfg(feature = "fu740")]`
- 与 2023 BiteTheDisk 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernelx/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`pub mod file; mod init; pub mod inode; pub mod vfs;`
  - `kernelx/src/fs/init.rs:L1-L4`：关键词命中
    代码片段：`use crate::fs::filesystem::MountOptions; use crate::fs::{Mode, Owner, devfs, vfs}; use crate::kernel::config; use crate::kernel::errno::{Errno, SysResult};`
  - `crates/nix/src/fs.rs:L89-L93`：关键词命中
    代码片段：`#[derive(Debug)] pub struct Dirent { d_ino: usize,             // inode number d_off: isize,             // offset from 0 to next dirent d_reclen: u16,            // length of t...`
  - `kernel/src/fs/page.rs:L29-L33`：关键词命中
    代码片段：`/// Data block state pub data_states: [DataState; PAGE_SIZE / BLOCK_SIZE], /// Inode that this page related to inode: Weak<VirtFile>, }`
- 与 2023 BiteTheDisk 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernelx/src/kernel/trap.rs:L2-L6`：关键词命中
    代码片段：`use crate::driver; use crate::kernel::errno::Errno; use crate::kernel::event::timer; use crate::kernel::ipc::{KSiFields, SiCode, signum}; use crate::kernel::mm::MemAccessType;`
  - `kernelx/src/kernel/event/timer/mod.rs:L1-L4`：关键词命中
    代码片段：`mod event; mod timer; pub use timer::*;`
  - `kernel/src/timer.rs:L48-L52`：关键词命中
    代码片段：`} /// Setup the next time interrupt time. pub fn set_next_trigger() { set_timer((get_time() + CLOCK_FREQ / TIME_SLICE) as u64);`
  - `kernel/src/trap/mod.rs:L3-L7`：关键词命中
    代码片段：`pub use context::*; pub use handler::*; use riscv::register::scause::{Interrupt, Trap}; use crate::consts::TRAMPOLINE;`
- 与 2023 BiteTheDisk 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernelx/src/kernel/mm/page.rs:L27-L31`：关键词命中
    代码片段：`fn alloc(&mut self) -> Option<usize> { let layout = Layout::from_size_align(arch::PGSIZE, arch::PGSIZE).unwrap(); let addr = self.allocator.alloc_aligned(layout)?; ALLOCATED_PAG...`
  - `kernelx/src/kernel/mm/ubuf.rs:L74-L78`：关键词命中
    代码片段：`}; let length = core::cmp::min(remaining, arch::PGSIZE - (kaddr % arch::PGSIZE)); self.offset += length;`
  - `kernel/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod kernel_heap_allocator; mod kvmm;`
  - `kernel/src/mm/kvmm.rs:L6-L10`：关键词命中
    代码片段：`mm::{ vm_area::{VmArea, VmAreaType}, MapPermission, MapType, MemorySet, }, };`
- 与 2023 BiteTheDisk 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernelx/src/kernel/task/def.rs:L4-L8`：关键词命中
    代码片段：`pub files: bool, pub vm: bool, pub thread: bool, pub parent: bool, pub vfork: bool,`
  - `kernelx/src/kernel/task/tcb.rs:L17-L21`：关键词命中
    代码片段：`use crate::kernel::mm::maparea::{AuxKey, Auxv}; use crate::kernel::mm::{AddrSpace, elf}; use crate::kernel::scheduler::{KernelStack, Task, Tid, WakeupFailure, current}; use crat...`
  - `kernel/src/task/mod.rs:L7-L11`：关键词命中
    代码片段：`mod signal; mod switch; mod task; pub use context::*; pub use id::*;`
  - `kernel/src/task/task.rs:L66-L70`：关键词命中
    代码片段：`pub parent: Option<Weak<TaskControlBlock>>, // child process and thread collection pub children: Vec<Arc<TaskControlBlock>>,`
- 与 2023 BiteTheDisk 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernelx/src/fs/vfs/vfs.rs:L9-L13`：关键词命中
    代码片段：`use crate::fs::perm::{Perm, PermFlags}; use crate::kernel::errno::{Errno, SysResult}; use crate::klib::{InitedCell, SleepLock, SpinLock}; use super::dentry::{Dentry, Mount};`
  - `kernelx/src/kernel/main.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; use alloc::collections::BTreeMap;`
  - `kernel/vendor/spin/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! Locks that have the same behaviour as a mutex. //! //! The ['Mutex'] in the root of the crate, can be configured using the 'ticket_mutex' feature.`
  - `kernel/vendor/lock_api/src/lib.rs:L6-L10`：关键词命中
    代码片段：`// copied, modified, or distributed except according to those terms. //! This library provides type-safe and fully-featured 'Mutex' and 'RwLock' //! types which wrap a simple ra...`
- 与 2023 BiteTheDisk 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernelx/src/kernel/syscall/fs.rs:L25-L29`：关键词命中
    代码片段：`use crate::kernel::scheduler::current::{copy_from_user, copy_to_user}; use crate::kernel::scheduler::*; use crate::kernel::syscall::uptr::{UArray, UBuffer, UPtr, UString, UserPo...`
  - `kernelx/src/kernel/syscall/mm.rs:L10-L14`：关键词命中
    代码片段：`use crate::kernel::mm::{AddrSpace, MapPerm}; use crate::kernel::scheduler::*; use crate::kernel::syscall::SyscallRet; use crate::kernel::task;`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Syscall Implementation //! //! About syscall id in RISC-V: https://marcin.juszkiewicz.com.pl/download/tables/syscalls.html`
  - `kernel/src/trap/trampoline.S:L139-L142`：关键词命中
    代码片段：`.align 2 user_sigreturn: li	a7, 139 # syscall SIGRETURN ecall`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2023 BiteTheDisk 在“内存管理”维度发现片段级代码相似度 0.78 （token=0.67, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：bitflags, clone, const, copy, debug, derive, pub, struct。（置信度：high）
  证据：
  - `kernelx/src/kernel/mm/mod.rs:L24-L28`：struct MapPerm
    代码片段：`bitflags! { #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct MapPerm: u8 { const R = 1 << 0; const W = 1 << 1;`
  - `crates/nix/src/mm.rs:L4-L8`：struct MmapProts
    代码片段：`bitflags! { #[derive(Clone, Copy, Debug)] pub struct MmapProts: usize { const PROT_NONE = 0;  // Unaccessible. Used for guard pages. const PROT_READ = 1 << 0;`
- 与 2023 BiteTheDisk 在“内存管理”维度发现片段级代码相似度 0.72 （token=0.62, structure=0.92）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：bitflags, clone, const, copy, debug, derive, pub, struct。（置信度：medium）
  证据：
  - `kernelx/src/kernel/mm/mod.rs:L24-L28`：struct MapPerm
    代码片段：`bitflags! { #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub struct MapPerm: u8 { const R = 1 << 0; const W = 1 << 1;`
  - `crates/nix/src/mm.rs:L16-L20`：struct MmapFlags
    代码片段：`bitflags! { #[derive(Clone, Copy, Debug)] pub struct MmapFlags: usize { /// File mapping. Used for file initialization.(for compatibility, can be ignored) const MAP_FILE = 0;`
- 文件路径重合：与 2023 BiteTheDisk 在“调度与任务管理”维度出现同名文件源码路径 `kernelx/src/kernel/kthread/task.rs` / `kernel/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernelx/src/kernel/kthread/task.rs:L5-L9`：关键词命中
    代码片段：`use crate::arch::KernelContext; use crate::kernel::event::Event; use crate::kernel::scheduler::{self, KernelStack, Task, Tid, WakeupFailure, current, tid}; use crate::kernel::ta...`
  - `kernel/src/task/task.rs:L66-L70`：关键词命中
    代码片段：`pub parent: Option<Weak<TaskControlBlock>>, // child process and thread collection pub children: Vec<Arc<TaskControlBlock>>,`

## 相似点

- 与 2023 BiteTheDisk 同属 rcore-variant 风格。（置信度：medium）
- 与 2023 BiteTheDisk 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernelx/vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `kernelx/vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2023 BiteTheDisk 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernelx/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`pub mod file; mod init; pub mod inode; pub mod vfs;`
  - `kernelx/src/fs/init.rs:L1-L4`：关键词命中
    代码片段：`use crate::fs::filesystem::MountOptions; use crate::fs::{Mode, Owner, devfs, vfs}; use crate::kernel::config; use crate::kernel::errno::{Errno, SysResult};`
- 与 2023 BiteTheDisk 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernelx/src/kernel/trap.rs:L2-L6`：关键词命中
    代码片段：`use crate::driver; use crate::kernel::errno::Errno; use crate::kernel::event::timer; use crate::kernel::ipc::{KSiFields, SiCode, signum}; use crate::kernel::mm::MemAccessType;`
  - `kernelx/src/kernel/event/timer/mod.rs:L1-L4`：关键词命中
    代码片段：`mod event; mod timer; pub use timer::*;`
- 与 2023 BiteTheDisk 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernelx/src/kernel/mm/page.rs:L27-L31`：关键词命中
    代码片段：`fn alloc(&mut self) -> Option<usize> { let layout = Layout::from_size_align(arch::PGSIZE, arch::PGSIZE).unwrap(); let addr = self.allocator.alloc_aligned(layout)?; ALLOCATED_PAG...`
  - `kernelx/src/kernel/mm/ubuf.rs:L74-L78`：关键词命中
    代码片段：`}; let length = core::cmp::min(remaining, arch::PGSIZE - (kaddr % arch::PGSIZE)); self.offset += length;`
- 与 2023 BiteTheDisk 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernelx/src/kernel/task/def.rs:L4-L8`：关键词命中
    代码片段：`pub files: bool, pub vm: bool, pub thread: bool, pub parent: bool, pub vfork: bool,`
  - `kernelx/src/kernel/task/tcb.rs:L17-L21`：关键词命中
    代码片段：`use crate::kernel::mm::maparea::{AuxKey, Auxv}; use crate::kernel::mm::{AddrSpace, elf}; use crate::kernel::scheduler::{KernelStack, Task, Tid, WakeupFailure, current}; use crat...`
- 与 2023 BiteTheDisk 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernelx/src/fs/vfs/vfs.rs:L9-L13`：关键词命中
    代码片段：`use crate::fs::perm::{Perm, PermFlags}; use crate::kernel::errno::{Errno, SysResult}; use crate::klib::{InitedCell, SleepLock, SpinLock}; use super::dentry::{Dentry, Mount};`
  - `kernelx/src/kernel/main.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; use alloc::collections::BTreeMap;`
- 与 2023 BiteTheDisk 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernelx/src/kernel/syscall/fs.rs:L25-L29`：关键词命中
    代码片段：`use crate::kernel::scheduler::current::{copy_from_user, copy_to_user}; use crate::kernel::scheduler::*; use crate::kernel::syscall::uptr::{UArray, UBuffer, UPtr, UString, UserPo...`
  - `kernelx/src/kernel/syscall/mm.rs:L10-L14`：关键词命中
    代码片段：`use crate::kernel::mm::{AddrSpace, MapPerm}; use crate::kernel::scheduler::*; use crate::kernel::syscall::SyscallRet; use crate::kernel::task;`
- 与 2023 BiteTheDisk 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2023 BiteTheDisk 的语言构成不同：待测作品为 {'build': 11325, 'markdown': 38449, 'toml': 6889, 'make': 1197, 'c': 47299, 'yaml': 36, 'asm': 4651, 'rust': 1375226, 'cpp': 2943, 'json': 88, 'text': 5913}，历史样本为 {'json': 83, 'build': 3797, 'markdown': 14373, 'toml': 8819, 'rust': 428744, 'asm': 519, 'c': 48, 'yaml': 427}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：17
- 含证据关键结论数：17（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
