# 2026 T202610486999563 比较报告

- 对比历史仓库：2024 NPUcore-IMPACT!!!
- 生成时间：2026-07-02T09:02:45.099175+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 NPUcore-IMPACT!!!（来源：赛事历史作品）：画像相似度 score=9.67；同属 ucore-variant 风格; 语言构成相似度 0.96; OS 维度重合度 1.00; 代码规模接近度 0.75

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = u64;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/syscall/src/fs_domain.rs:L22-L26`：关键词命中
    代码片段：`SYS_FCNTL => ctx .dispatcher .sys_fcntl(args, ctx.procs, ctx.scheduler, ctx.vfs), SYS_IOCTL => ctx.dispatcher.sys_ioctl(args, ctx.procs), SYS_FLOCK => ctx.dispatcher.sys_flock(a...`
  - `crates/vfs/src/lib.rs:L295-L299`：关键词命中
    代码片段：`pub fn new() -> Self { let root = Arc::new(Node::directory("/")); let mut vfs = Self { root, mounts: Vec::new(),`
  - `os/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod file_trait; mod filesystem; // mod inode; mod layout; pub mod poll;`
  - `os/src/fs/cache.rs:L193-L197`：关键词命中
    代码片段：`/// Every time kernel tried to alloc this pagecache this number will increase 1(at most 1) /// Every time out of memory occurred this number will decrease 1(at least 0) /// When...`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/device/blk.rs:L89-L93`：关键词命中
    代码片段：`} /// Acknowledges a pending interrupt, if any. /// /// Returns true if there was an interrupt to acknowledge.`
  - `vendor/virtio-drivers/src/device/rng.rs:L52-L56`：关键词命中
    代码片段：`} /// Acknowledge interrupt. pub fn ack_interrupt(&mut self) -> InterruptStatus { self.transport.ack_interrupt()`
  - `os/src/timer.rs:L18-L22`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/main.rs:L36-L40`：关键词命中
    代码片段：`mod syscall; mod task; mod timer; // use crate::arch::config::DISK_IMAGE_BASE;`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/mm/src/lib.rs:L161-L165`：关键词命中
    代码片段：`program_break: usize, next_mapping_base: usize, page_table: Option<PageTableSpace>, dirty: bool, frame_allocator: Mutex<FrameAllocator>,`
  - `vendor/virtio-drivers/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM //! to interact with VirtIO devices provided by the VMM (such as QEMU or...`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `os/src/mm/map_area.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::Debug; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/syscall/src/task_domain.rs:L12-L16`：关键词命中
    代码片段：`Some(match sysno { SYS_SET_TID_ADDRESS => ctx.dispatcher.sys_set_tid_address(args, ctx.procs), SYS_SCHED_YIELD => ctx.dispatcher.sys_sched_yield(ctx.scheduler), SYS_EXIT => ctx...`
  - `crates/proc/src/lib.rs:L184-L188`：关键词命中
    代码片段：`pub sigsuspend_saved_mask: Option<u64>, pub is_thread: bool, /// Set to true when a signal frame has been dispatched into the thread's /// trap frame (sepc redirected to signal...`
  - `os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `os/src/task/task.rs:L380-L384`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
  - `third_party/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
  - `os/src/fs/mod.rs:L35-L39`：关键词命中
    代码片段：`use lazy_static::*; use log::warn; use spin::Mutex; lazy_static! {`
  - `os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::{arch::BLOCK_SZ, config::PAGE_SIZE, drivers::BLOCK_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/syscall/src/lib.rs:L537-L541`：关键词命中
    代码片段：`} fn log_user_path_fault(process: &proc::Process, syscall: &str, path_ptr: usize) { log_always(&format!( "whuse-ltp:path-fault syscall={} tgid={} tid={} name={} cwd={} ptr={:#x}...`
  - `crates/user-init/src/lib.rs:L33-L37`：关键词命中
    代码片段：`li a2, init_msg_end - init_msg li a7, 64 ecall li a0, 0`
  - `os/src/syscall/fs.rs:L548-L552`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `os/src/syscall/mod.rs:L115-L119`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现 3 个同名定义：is_dir, read, new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `crates/fs-ext4/src/lib.rs:L83-L83`：fn is_dir
    代码片段：`pub fn is_dir(&self, path: &str) -> Result<bool, i32> {`
  - `os/src/fs/file_trait.rs:L28-L28`：fn is_dir
    代码片段：`fn is_dir(&self) -> bool {`
  - `crates/fs-ext4/src/lib.rs:L92-L92`：fn read
    代码片段：`pub fn read(&self, path: &str) -> Result<Vec<u8>, i32> {`
  - `os/src/fs/cache.rs:L15-L15`：fn read
    代码片段：`fn read<T, V>(&self, offset: usize, f: impl FnOnce(&T) -> V) -> V;`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“内存管理”维度发现 4 个同名定义：alloc, dealloc, alloc_page, new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `crates/hal-virtio/src/lib.rs:L93-L93`：fn alloc
    代码片段：`pub fn alloc(&self, pages: usize) -> Option<(u64, NonNull<u8>)> {`
  - `os/src/mm/frame_allocator.rs:L46-L46`：fn alloc
    代码片段：`fn alloc(&mut self) -> Option<FrameTracker>;`
  - `crates/hal-virtio/src/lib.rs:L124-L124`：fn dealloc
    代码片段：`pub fn dealloc(&self, paddr: u64, pages: usize) -> i32 {`
  - `os/src/mm/frame_allocator.rs:L48-L48`：fn dealloc
    代码片段：`fn dealloc(&mut self, ppn: PhysPageNum);`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“同步机制”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/fs-ext4/src/lib.rs:L235-L235`：fn new
    代码片段：`fn new(device: &'static dyn HalBlockDevice) -> Self {`
  - `os/src/fs/cache.rs:L81-L81`：fn new
    代码片段：`pub fn new(buffer_ptr: *mut [u8; BUFFER_SIZE]) -> Self {`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度发现 1 个同名定义：Timespec。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/hal-api/src/lib.rs:L17-L17`：struct Timespec
    代码片段：`pub struct Timespec {`
  - `os/src/timer.rs:L47-L47`：struct TimeSpec
    代码片段：`pub struct TimeSpec {`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“内存管理”维度发现 1 个同名定义：FrameAllocator。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/mm/src/lib.rs:L67-L67`：struct FrameAllocator
    代码片段：`pub struct FrameAllocator {`
  - `os/src/mm/frame_allocator.rs:L44-L44`：trait FrameAllocator
    代码片段：`trait FrameAllocator {`

## 相似点

- 与 2024 NPUcore-IMPACT!!! 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = u64;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/syscall/src/fs_domain.rs:L22-L26`：关键词命中
    代码片段：`SYS_FCNTL => ctx .dispatcher .sys_fcntl(args, ctx.procs, ctx.scheduler, ctx.vfs), SYS_IOCTL => ctx.dispatcher.sys_ioctl(args, ctx.procs), SYS_FLOCK => ctx.dispatcher.sys_flock(a...`
  - `crates/vfs/src/lib.rs:L295-L299`：关键词命中
    代码片段：`pub fn new() -> Self { let root = Arc::new(Node::directory("/")); let mut vfs = Self { root, mounts: Vec::new(),`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/device/blk.rs:L89-L93`：关键词命中
    代码片段：`} /// Acknowledges a pending interrupt, if any. /// /// Returns true if there was an interrupt to acknowledge.`
  - `vendor/virtio-drivers/src/device/rng.rs:L52-L56`：关键词命中
    代码片段：`} /// Acknowledge interrupt. pub fn ack_interrupt(&mut self) -> InterruptStatus { self.transport.ack_interrupt()`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/mm/src/lib.rs:L161-L165`：关键词命中
    代码片段：`program_break: usize, next_mapping_base: usize, page_table: Option<PageTableSpace>, dirty: bool, frame_allocator: Mutex<FrameAllocator>,`
  - `vendor/virtio-drivers/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM //! to interact with VirtIO devices provided by the VMM (such as QEMU or...`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/syscall/src/task_domain.rs:L12-L16`：关键词命中
    代码片段：`Some(match sysno { SYS_SET_TID_ADDRESS => ctx.dispatcher.sys_set_tid_address(args, ctx.procs), SYS_SCHED_YIELD => ctx.dispatcher.sys_sched_yield(ctx.scheduler), SYS_EXIT => ctx...`
  - `crates/proc/src/lib.rs:L184-L188`：关键词命中
    代码片段：`pub sigsuspend_saved_mask: Option<u64>, pub is_thread: bool, /// Set to true when a signal frame has been dispatched into the thread's /// trap frame (sepc redirected to signal...`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
  - `third_party/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/syscall/src/lib.rs:L537-L541`：关键词命中
    代码片段：`} fn log_user_path_fault(process: &proc::Process, syscall: &str, path_ptr: usize) { log_always(&format!( "whuse-ltp:path-fault syscall={} tgid={} tid={} name={} cwd={} ptr={:#x}...`
  - `crates/user-init/src/lib.rs:L33-L37`：关键词命中
    代码片段：`li a2, init_msg_end - init_msg li a7, 64 ecall li a0, 0`
- 与 2024 NPUcore-IMPACT!!! 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 NPUcore-IMPACT!!! 的语言构成不同：待测作品为 {'toml': 76, 'markdown': 6853, 'build': 3579, 'rust': 240449, 'asm': 81, 'json': 161, 'text': 285, 'yaml': 34}，历史样本为 {'json': 20, 'build': 338, 'markdown': 743, 'rust': 177629, 'asm': 10}。（置信度：medium）

## 可能创新点

- 待测作品包含 261 个历史样本 2024 NPUcore-IMPACT!!! 中未见的独特函数/类型定义（如 `process_needs_forced_preempt`, `log_block_probe_span`, `share`, `should_dispatch_pending_signals_after_syscall`, `map_anonymous_at`, `install_host_range`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `crates/kernel-core/src/lib_riscv.inc.rs:L3213-L3213`：fn process_needs_forced_preempt
    代码片段：`fn process_needs_forced_preempt(name: &str, now: u64, iozone_busybox_window_until_ns: u64) -> bool {`
  - `crates/kernel-core/src/lib_riscv.inc.rs:L3272-L3272`：fn log_block_probe_span
    代码片段：`fn log_block_probe_span(device: &'static dyn hal_api::HalBlockDevice, start: usize, count: usize) {`

## 附录：核验摘要

- 关键结论数：20
- 含证据关键结论数：20（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
