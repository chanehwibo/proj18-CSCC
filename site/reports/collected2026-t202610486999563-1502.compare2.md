# 2026 T202610486999563 比较报告

- 对比历史仓库：2024 海底小纵队
- 生成时间：2026-07-02T05:01:33.749713+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 海底小纵队（来源：赛事历史作品）：画像相似度 score=9.47；同属 ucore-variant 风格; 语言构成相似度 0.91; OS 维度重合度 1.00; 代码规模接近度 0.65

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 海底小纵队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = u64;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2024 海底小纵队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/syscall/src/fs_domain.rs:L22-L26`：关键词命中
    代码片段：`SYS_FCNTL => ctx .dispatcher .sys_fcntl(args, ctx.procs, ctx.scheduler, ctx.vfs), SYS_IOCTL => ctx.dispatcher.sys_ioctl(args, ctx.procs), SYS_FLOCK => ctx.dispatcher.sys_flock(a...`
  - `crates/vfs/src/lib.rs:L295-L299`：关键词命中
    代码片段：`pub fn new() -> Self { let root = Arc::new(Node::directory("/")); let mut vfs = Self { root, mounts: Vec::new(),`
  - `vendor/fatfs/src/fs.rs:L21-L25`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `kernel/src/main.rs:L190-L194`：关键词命中
    代码片段：`.expect("can't create tmp dir"); // Initialize the Dentry node. // dentry::dentry_init(rootfs); // FileItem::fs_open("/bin", OpenFlags::O_DIRECTORY)`
- 与 2024 海底小纵队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/device/blk.rs:L89-L93`：关键词命中
    代码片段：`} /// Acknowledges a pending interrupt, if any. /// /// Returns true if there was an interrupt to acknowledge.`
  - `vendor/virtio-drivers/src/device/rng.rs:L52-L56`：关键词命中
    代码片段：`} /// Acknowledge interrupt. pub fn ack_interrupt(&mut self) -> InterruptStatus { self.transport.ack_interrupt()`
  - `kernel/src/main.rs:L64-L68`：关键词命中
    代码片段：`#[polyhal::arch_interrupt] /// Handle kernel interrupt fn kernel_interrupt(cx_ref: &mut TrapFrame, trap_type: TrapType) { match trap_type {`
  - `kernel/src/tasks/mod.rs:L132-L136`：关键词命中
    代码片段：`#[derive(Debug, Clone, Copy, Default)] pub struct ProcessTimer { pub timer: ITimerVal, pub next: TimeVal, pub last: TimeVal,`
- 与 2024 海底小纵队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/mm/src/lib.rs:L161-L165`：关键词命中
    代码片段：`program_break: usize, next_mapping_base: usize, page_table: Option<PageTableSpace>, dirty: bool, frame_allocator: Mutex<FrameAllocator>,`
  - `vendor/virtio-drivers/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM //! to interact with VirtIO devices provided by the VMM (such as QEMU or...`
  - `kernel/src/syscall/mm.rs:L4-L8`：关键词命中
    代码片段：`use polyhal::PAGE_SIZE; use polyhal::USER_VADDR_END; use frame_allocator::ceil_div; use log::debug;`
  - `kernel/src/main.rs:L39-L43`：关键词命中
    代码片段：`use devices::{self, get_int_device}; use executor::current_task; use frame_allocator::{self, frame_alloc_persist, frame_unalloc}; use hal; use tasks::UserTask;`
- 与 2024 海底小纵队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/syscall/src/task_domain.rs:L12-L16`：关键词命中
    代码片段：`Some(match sysno { SYS_SET_TID_ADDRESS => ctx.dispatcher.sys_set_tid_address(args, ctx.procs), SYS_SCHED_YIELD => ctx.dispatcher.sys_sched_yield(ctx.scheduler), SYS_EXIT => ctx...`
  - `crates/proc/src/lib.rs:L184-L188`：关键词命中
    代码片段：`pub sigsuspend_saved_mask: Option<u64>, pub is_thread: bool, /// Set to true when a signal frame has been dispatched into the thread's /// trap frame (sepc redirected to signal...`
  - `vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`
  - `kernel/src/tasks/elf.rs:L14-L18`：关键词命中
    代码片段：`use crate::tasks::memset::MemType; use super::task::UserTask; pub trait ElfExtra {`
- 与 2024 海底小纵队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
  - `third_party/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
  - `kernel/src/main.rs:L30-L34`：关键词命中
    代码片段：`mod user; use core::sync::atomic::{AtomicBool, Ordering}; use polyhal::addr::{PhysPage, VirtPage};`
  - `kernel/src/socket.rs:L5-L9`：关键词命中
    代码片段：`use fs::INodeInterface; use lose_net_stack::net_trait::SocketInterface; use sync::Mutex; use vfscore::{Metadata, PollEvent, VfsResult};`
- 与 2024 海底小纵队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/syscall/src/lib.rs:L537-L541`：关键词命中
    代码片段：`} fn log_user_path_fault(process: &proc::Process, syscall: &str, path_ptr: usize) { log_always(&format!( "whuse-ltp:path-fault syscall={} tgid={} tid={} name={} cwd={} ptr={:#x}...`
  - `crates/user-init/src/lib.rs:L33-L37`：关键词命中
    代码片段：`li a2, init_msg_end - init_msg li a7, 64 ecall li a0, 0`
  - `kernel/src/syscall/fd.rs:L14-L18`：关键词命中
    代码片段：`use crate::epoll::{EpollEvent, EpollFile}; use crate::syscall::consts::{from_vfs, FcntlCmd, IoVec, AT_CWD}; use crate::syscall::func::timespc_now; use crate::syscall::time::curr...`
  - `kernel/src/syscall/mm.rs:L7-L11`：关键词命中
    代码片段：`use log::debug; use crate::syscall::consts::from_vfs; use crate::syscall::consts::MSyncFlags; use crate::syscall::consts::MapFlags;`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 海底小纵队 在“设备驱动”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：bufferdirection, direction, dma_alloc, fn, h, if, let, new。（置信度：high）
  证据：
  - `vendor/virtio-drivers/src/hal.rs:L29-L33`：fn new
    代码片段：`/// /// The pages will be zeroed. pub fn new(pages: usize, direction: BufferDirection) -> Result<Self> { let (paddr, vaddr) = H::dma_alloc(pages, direction); if paddr == 0 {`
  - `vendor/virtio-drivers/src/hal.rs:L22-L26`：fn new
    代码片段：`/// /// The pages will be zeroed. pub fn new(pages: usize, direction: BufferDirection) -> Result<Self> { let (paddr, vaddr) = H::dma_alloc(pages, direction); if paddr == 0 {`
- 与 2024 海底小纵队 在“设备驱动”维度发现片段级代码相似度 0.94 （token=0.91, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：debug, derive, dma, h, hal, nonnull, paddr, pub。（置信度：high）
  证据：
  - `vendor/virtio-drivers/src/hal.rs:L10-L14`：struct Dma
    代码片段：`/// A region of contiguous physical memory used for DMA. #[derive(Debug)] pub struct Dma<H: Hal> { paddr: PhysAddr, vaddr: NonNull<u8>,`
  - `vendor/virtio-drivers/src/hal.rs:L10-L14`：struct Dma
    代码片段：`/// A region of contiguous physical memory used for DMA. #[derive(Debug)] pub struct Dma<H: Hal> { paddr: usize, vaddr: NonNull<u8>,`
- 文件路径重合：与 2024 海底小纵队 在“设备驱动”维度出现同路径源码路径 `vendor/virtio-drivers/src/hal.rs` / `vendor/virtio-drivers/src/hal.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = u64;`
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
- 文件路径重合：与 2024 海底小纵队 在“设备驱动”维度出现同路径源码路径 `vendor/virtio-drivers/src/queue.rs` / `vendor/virtio-drivers/src/queue.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `vendor/virtio-drivers/src/queue.rs:L1-L3`：关键词命中
    代码片段：`//! Support for virt queues, the main mechanism for data transport on VirtIO devices. //! //! Types from this module are used to implement VirtIO device drivers. If you just wan...`
  - `vendor/virtio-drivers/src/queue.rs:L17-L21`：关键词命中
    代码片段：`use zerocopy::{AsBytes, FromBytes, FromZeroes}; /// The mechanism for bulk data transport on virtio devices. /// /// Each device can have zero or more virtqueues.`

## 相似点

- 与 2024 海底小纵队 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 海底小纵队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = u64;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2024 海底小纵队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/syscall/src/fs_domain.rs:L22-L26`：关键词命中
    代码片段：`SYS_FCNTL => ctx .dispatcher .sys_fcntl(args, ctx.procs, ctx.scheduler, ctx.vfs), SYS_IOCTL => ctx.dispatcher.sys_ioctl(args, ctx.procs), SYS_FLOCK => ctx.dispatcher.sys_flock(a...`
  - `crates/vfs/src/lib.rs:L295-L299`：关键词命中
    代码片段：`pub fn new() -> Self { let root = Arc::new(Node::directory("/")); let mut vfs = Self { root, mounts: Vec::new(),`
- 与 2024 海底小纵队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/device/blk.rs:L89-L93`：关键词命中
    代码片段：`} /// Acknowledges a pending interrupt, if any. /// /// Returns true if there was an interrupt to acknowledge.`
  - `vendor/virtio-drivers/src/device/rng.rs:L52-L56`：关键词命中
    代码片段：`} /// Acknowledge interrupt. pub fn ack_interrupt(&mut self) -> InterruptStatus { self.transport.ack_interrupt()`
- 与 2024 海底小纵队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/mm/src/lib.rs:L161-L165`：关键词命中
    代码片段：`program_break: usize, next_mapping_base: usize, page_table: Option<PageTableSpace>, dirty: bool, frame_allocator: Mutex<FrameAllocator>,`
  - `vendor/virtio-drivers/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM //! to interact with VirtIO devices provided by the VMM (such as QEMU or...`
- 与 2024 海底小纵队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/syscall/src/task_domain.rs:L12-L16`：关键词命中
    代码片段：`Some(match sysno { SYS_SET_TID_ADDRESS => ctx.dispatcher.sys_set_tid_address(args, ctx.procs), SYS_SCHED_YIELD => ctx.dispatcher.sys_sched_yield(ctx.scheduler), SYS_EXIT => ctx...`
  - `crates/proc/src/lib.rs:L184-L188`：关键词命中
    代码片段：`pub sigsuspend_saved_mask: Option<u64>, pub is_thread: bool, /// Set to true when a signal frame has been dispatched into the thread's /// trap frame (sepc redirected to signal...`
- 与 2024 海底小纵队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
  - `third_party/virtio-drivers/src/queue.rs:L24-L28`：关键词命中
    代码片段：`use core::ptr; use core::ptr::NonNull; use core::sync::atomic::{AtomicU16, Ordering, fence}; use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout};`
- 与 2024 海底小纵队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/syscall/src/lib.rs:L537-L541`：关键词命中
    代码片段：`} fn log_user_path_fault(process: &proc::Process, syscall: &str, path_ptr: usize) { log_always(&format!( "whuse-ltp:path-fault syscall={} tgid={} tid={} name={} cwd={} ptr={:#x}...`
  - `crates/user-init/src/lib.rs:L33-L37`：关键词命中
    代码片段：`li a2, init_msg_end - init_msg li a7, 64 ecall li a0, 0`
- 与 2024 海底小纵队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 海底小纵队 的语言构成不同：待测作品为 {'toml': 76, 'markdown': 6853, 'build': 3579, 'rust': 240449, 'asm': 81, 'json': 161, 'text': 285, 'yaml': 34}，历史样本为 {'json': 123, 'yaml': 68, 'build': 6380, 'markdown': 20083, 'toml': 259, 'asm': 359, 'text': 28, 'rust': 379391, 'c': 27758, 'make': 664}。（置信度：medium）

## 可能创新点

- 待测作品包含 272 个历史样本 2024 海底小纵队 中未见的独特函数/类型定义（如 `process_needs_forced_preempt`, `log_block_probe_span`, `share`, `should_dispatch_pending_signals_after_syscall`, `map_anonymous_at`, `install_host_range`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `crates/kernel-core/src/lib_riscv.inc.rs:L3213-L3213`：fn process_needs_forced_preempt
    代码片段：`fn process_needs_forced_preempt(name: &str, now: u64, iozone_busybox_window_until_ns: u64) -> bool {`
  - `crates/kernel-core/src/lib_riscv.inc.rs:L3272-L3272`：fn log_block_probe_span
    代码片段：`fn log_block_probe_span(device: &'static dyn hal_api::HalBlockDevice, start: usize, count: usize) {`

## 附录：核验摘要

- 关键结论数：19
- 含证据关键结论数：19（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
