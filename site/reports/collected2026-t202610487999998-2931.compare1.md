# 2026 T202610487999998 比较报告

- 对比历史仓库：2025 哈基玛丽
- 生成时间：2026-07-01T21:13:00.488711+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 哈基玛丽（来源：赛事历史作品）：画像相似度 score=9.50；同属 ucore-variant 风格; 语言构成相似度 0.89; OS 维度重合度 1.00; 代码规模接近度 0.71

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 哈基玛丽 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/common/virtio.c:L5-L9`：关键词命中
    代码片段：`#ifdef ARCH_RISCV64 #define VIRTIO_BASE 0x10001000UL #define VIRTIO_MAGIC 0x000 #define VIRTIO_VERSION 0x004`
  - `kernel/proc/user.c:L16-L20`：关键词命中
    代码片段：`#define USER_HEAP_LIMIT 0x000000004f000000UL #define KERNEL_MAP_END 0x88000000UL #define VIRTIO_MMIO_START 0x10000000UL #define VIRTIO_MMIO_END 0x10002000UL #define MMAP_BASE 0x...`
  - `kernel/drivers/virtio.c:L1-L3`：关键词命中
    代码片段：`#include "virtio.h" #include "printf.h"`
  - `include/virtio.h:L7-L11`：关键词命中
    代码片段：`// 寄存器偏移 #define VIRTIO_MMIO_MAGIC_VALUE 0x000 #define VIRTIO_MMIO_VERSION 0x004 #define VIRTIO_MMIO_DEVICE_ID 0x008`
- 与 2025 哈基玛丽 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/ext4.c:L104-L108`：关键词命中
    代码片段：`struct ext4_dir_entry_2 { uint32_t inode; uint16_t rec_len; uint8_t name_len;`
  - `kernel/common/main.c:L104-L108`：关键词命中
    代码片段：`} else if (str_eq(name, "fstat")) { kputs("fstat ret: 0\n"); kputs("fstat: dev: 0, inode: 1, mode: 32768, nlink: 1, size: 49, atime: 0, mtime: 0, ctime: 0\n"); } else if (str_eq...`
  - `kernel/fs/fat32.c:L92-L96`：关键词命中
    代码片段：`void print_fat32sb() { printf("--- FAT32 SuperBlock ---\n"); printf("Bytes per Sector: %d\n", sb.bytes_per_sector); printf("Sector per Cluster: %d\n", sb.sector_per_cluster);`
  - `include/ustruct.h:L29-L33`：关键词命中
    代码片段：`{ uint64 st_dev; uint64 st_ino;   // 在 FAT32 中常用起始簇号作为 inode 号 uint32 st_mode;  // 文件类型和权限 uint32 st_nlink; // 连结数`
- 与 2025 哈基玛丽 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/user.c:L4300-L4304`：关键词命中
    代码片段：`} kprintf("[trap] scause=%x stval=%x sepc=%x\n", scause, stval, tf->sepc); return 1; }`
  - `kernel/common/virtio.c:L112-L116`：关键词命中
    代码片段：`static void virtio_ack_used(void) { g_last_used++; uint32_t isr = mmio_read(VIRTIO_INTERRUPT_STATUS); mmio_write(VIRTIO_INTERRUPT_ACK, isr & 3); }`
  - `kernel/proc/trap.c:L1-L3`：关键词命中
    代码片段：`#include "trap.h" #include "printf.h" #include "riscv.h"`
  - `kernel/proc/trap_entry.S:L81-L85`：关键词命中
    代码片段：`store_all_registers # come back to save a0 register before entering trap handling in trapframe csrr t0, sscratch sd t0, 72(a0)`
- 与 2025 哈基玛丽 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/user.c:L29-L33`：关键词命中
    代码片段：`#define PROT_EXEC_FLAG 0x4UL #define PTE_V (1UL << 0) #define PTE_R (1UL << 1) #define PTE_W (1UL << 2)`
  - `kernel/arch/riscv64/trap.S:L5-L9`：关键词命中
    代码片段：`user_enter: # a0 = user satp, a1 = entry, a2 = user sp addi    sp, sp, -104 sd      ra, 0(sp)`
  - `kernel/mem/pmm.c:L1-L3`：关键词命中
    代码片段：`#include "pmm.h" #include "memlayout.h" #include "printf.h"`
  - `kernel/mem/vmm.c:L1-L3`：关键词命中
    代码片段：`#include "vmm.h" #include "fat32.h" #include "memlayout.h"`
- 与 2025 哈基玛丽 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/user.c:L898-L902`：关键词命中
    代码片段：`str_eq(path, "/tmp") || str_eq(path, "tmp") || str_eq(path, "/var") || str_eq(path, "/var/tmp") || str_eq(path, "/dev") || str_eq(path, "/etc") || str_eq(path, "/proc") || str_e...`
  - `kernel/fs/ext4.c:L634-L638`：关键词命中
    代码片段：`"gettimeofday", "mkdir_", "mmap", "mount", "munmap", "openat", "open", "pipe", "read", "sleep", "times", "umount", "uname", "unlink", "wait", "waitpid", "write", "yield", }; str...`
  - `kernel/proc/switch.S:L1-L3`：关键词命中
    代码片段：`.globl swtch swtch: sd ra, 0(a0)`
  - `kernel/proc/process.c:L6-L10`：关键词命中
    代码片段：`#include "pmm.h" #include "printf.h" #include "scheduler.h" #include "string.h" #include "virtio.h"`
- 与 2025 哈基玛丽 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/arch/riscv64/arch.c:L15-L19`：关键词命中
    代码片段：`register long a6 asm("a6") = fid; register long a7 asm("a7") = eid; asm volatile ("ecall" : "+r"(a0) : "r"(a1), "r"(a2), "r"(a6), "r"(a7)`
  - `kernel/syscall/sbi.c:L9-L13`：关键词命中
    代码片段：`register long a7 asm("a7") = ext; asm volatile("ecall" : "+r"(a0), "+r"(a1) : "r"(a6), "r"(a7) : "memory"); ret.error = a0; ret.value = a1;`
  - `kernel/syscall/syscall.c:L1-L3`：关键词命中
    代码片段：`#include "syscall.h" #include "file.h" #include "pipe.h"`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 哈基玛丽 在“系统调用”维度发现片段级代码相似度 0.73 （token=0.65, structure=0.88）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：a0, a1, a6, a7, asm, long, register, volatile。（置信度：medium）
  证据：
  - `kernel/arch/riscv64/arch.c:L15-L19`：关键词命中
    代码片段：`register long a6 asm("a6") = fid; register long a7 asm("a7") = eid; asm volatile ("ecall" : "+r"(a0) : "r"(a1), "r"(a2), "r"(a6), "r"(a7)`
  - `kernel/syscall/sbi.c:L9-L13`：关键词命中
    代码片段：`register long a7 asm("a7") = ext; asm volatile("ecall" : "+r"(a0), "+r"(a1) : "r"(a6), "r"(a7) : "memory"); ret.error = a0; ret.value = a1;`
- 宏名重合：与 2025 哈基玛丽 在“内存管理”维度发现 4 个同名定义：SYS_MMAP, USER_STACK_TOP, PTE_V, PTE_R。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/include/syscall.h:L153-L153`：macro SYS_MMAP
    代码片段：`#define SYS_MMAP 222`
  - `include/syscall.h:L41-L41`：macro SYS_mmap
    代码片段：`#define SYS_mmap 222`
  - `kernel/proc/user.c:L14-L14`：macro USER_STACK_TOP
    代码片段：`#define USER_STACK_TOP 0x0000003fffff0000UL`
  - `include/memlayout.h:L12-L12`：macro USER_STACK_TOP
    代码片段：`#define USER_STACK_TOP 0x80000000 - PGSIZE`
- 宏名重合：与 2025 哈基玛丽 在“系统调用”维度发现 4 个同名定义：SYS_GETCWD, SYS_DUP, SYS_DUP3, SYS_MKDIRAT。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/include/syscall.h:L4-L4`：macro SYS_GETCWD
    代码片段：`#define SYS_GETCWD 17`
  - `include/syscall.h:L8-L8`：macro SYS_getcwd
    代码片段：`#define SYS_getcwd 17`
  - `kernel/include/syscall.h:L9-L9`：macro SYS_DUP
    代码片段：`#define SYS_DUP 23`
  - `include/syscall.h:L9-L9`：macro SYS_dup
    代码片段：`#define SYS_dup 23`
- 宏名重合：与 2025 哈基玛丽 在“设备驱动”维度发现 1 个同名定义：QUEUE_SIZE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/common/virtio.c:L27-L27`：macro QUEUE_SIZE
    代码片段：`#define QUEUE_SIZE 256`
  - `include/virtio.h:L40-L40`：macro QUEUE_SIZE
    代码片段：`#define QUEUE_SIZE 8`
- 宏名重合：与 2025 哈基玛丽 在“文件系统”维度发现 1 个同名定义：SYS_FSTAT。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/include/syscall.h:L54-L54`：macro SYS_FSTAT
    代码片段：`#define SYS_FSTAT 80`
  - `include/syscall.h:L22-L22`：macro SYS_fstat
    代码片段：`#define SYS_fstat 80`
- 宏名重合：与 2025 哈基玛丽 在“调度与任务管理”维度发现 1 个同名定义：SYS_SCHED_YIELD。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/include/syscall.h:L88-L88`：macro SYS_SCHED_YIELD
    代码片段：`#define SYS_SCHED_YIELD 124`
  - `include/syscall.h:L26-L26`：macro SYS_sched_yield
    代码片段：`#define SYS_sched_yield 124`
- 文件路径重合：与 2025 哈基玛丽 在“设备驱动”维度出现同名文件源码路径 `kernel/common/virtio.c` / `kernel/drivers/virtio.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/common/virtio.c:L5-L9`：关键词命中
    代码片段：`#ifdef ARCH_RISCV64 #define VIRTIO_BASE 0x10001000UL #define VIRTIO_MAGIC 0x000 #define VIRTIO_VERSION 0x004`
  - `kernel/drivers/virtio.c:L1-L3`：关键词命中
    代码片段：`#include "virtio.h" #include "printf.h"`
- 文件路径重合：与 2025 哈基玛丽 在“系统调用”维度出现同名文件源码路径 `kernel/include/syscall.h` / `include/syscall.h`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/include/syscall.h:L2-L6`：macro SYS_GETCWD
    代码片段：`#define OSKERNEL2026_SYSCALL_H #define SYS_GETCWD 17 #define SYS_EVENTFD2 19 #define SYS_EPOLL_CREATE1 20`
  - `include/syscall.h:L41-L45`：关键词命中
    代码片段：`#define SYS_mmap 222 long do_syscall(long a0, long a1, long a2, long a3, long a4, long a5, long a6, long a7);`

## 相似点

- 与 2025 哈基玛丽 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 哈基玛丽 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/common/virtio.c:L5-L9`：关键词命中
    代码片段：`#ifdef ARCH_RISCV64 #define VIRTIO_BASE 0x10001000UL #define VIRTIO_MAGIC 0x000 #define VIRTIO_VERSION 0x004`
  - `kernel/proc/user.c:L16-L20`：关键词命中
    代码片段：`#define USER_HEAP_LIMIT 0x000000004f000000UL #define KERNEL_MAP_END 0x88000000UL #define VIRTIO_MMIO_START 0x10000000UL #define VIRTIO_MMIO_END 0x10002000UL #define MMAP_BASE 0x...`
- 与 2025 哈基玛丽 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/ext4.c:L104-L108`：关键词命中
    代码片段：`struct ext4_dir_entry_2 { uint32_t inode; uint16_t rec_len; uint8_t name_len;`
  - `kernel/common/main.c:L104-L108`：关键词命中
    代码片段：`} else if (str_eq(name, "fstat")) { kputs("fstat ret: 0\n"); kputs("fstat: dev: 0, inode: 1, mode: 32768, nlink: 1, size: 49, atime: 0, mtime: 0, ctime: 0\n"); } else if (str_eq...`
- 与 2025 哈基玛丽 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/user.c:L4300-L4304`：关键词命中
    代码片段：`} kprintf("[trap] scause=%x stval=%x sepc=%x\n", scause, stval, tf->sepc); return 1; }`
  - `kernel/common/virtio.c:L112-L116`：关键词命中
    代码片段：`static void virtio_ack_used(void) { g_last_used++; uint32_t isr = mmio_read(VIRTIO_INTERRUPT_STATUS); mmio_write(VIRTIO_INTERRUPT_ACK, isr & 3); }`
- 与 2025 哈基玛丽 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/user.c:L29-L33`：关键词命中
    代码片段：`#define PROT_EXEC_FLAG 0x4UL #define PTE_V (1UL << 0) #define PTE_R (1UL << 1) #define PTE_W (1UL << 2)`
  - `kernel/arch/riscv64/trap.S:L5-L9`：关键词命中
    代码片段：`user_enter: # a0 = user satp, a1 = entry, a2 = user sp addi    sp, sp, -104 sd      ra, 0(sp)`
- 与 2025 哈基玛丽 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/user.c:L898-L902`：关键词命中
    代码片段：`str_eq(path, "/tmp") || str_eq(path, "tmp") || str_eq(path, "/var") || str_eq(path, "/var/tmp") || str_eq(path, "/dev") || str_eq(path, "/etc") || str_eq(path, "/proc") || str_e...`
  - `kernel/fs/ext4.c:L634-L638`：关键词命中
    代码片段：`"gettimeofday", "mkdir_", "mmap", "mount", "munmap", "openat", "open", "pipe", "read", "sleep", "times", "umount", "uname", "unlink", "wait", "waitpid", "write", "yield", }; str...`
- 与 2025 哈基玛丽 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/arch/riscv64/arch.c:L15-L19`：关键词命中
    代码片段：`register long a6 asm("a6") = fid; register long a7 asm("a7") = eid; asm volatile ("ecall" : "+r"(a0) : "r"(a1), "r"(a2), "r"(a6), "r"(a7)`
  - `kernel/syscall/sbi.c:L9-L13`：关键词命中
    代码片段：`register long a7 asm("a7") = ext; asm volatile("ecall" : "+r"(a0), "+r"(a1) : "r"(a6), "r"(a7) : "memory"); ret.error = a0; ret.value = a1;`
- 与 2025 哈基玛丽 在 driver, filesystem, interrupt, memory, scheduler, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 哈基玛丽 的语言构成不同：待测作品为 {'build': 146, 'markdown': 92, 'c': 7592, 'asm': 281}，历史样本为 {'json': 20, 'build': 69, 'markdown': 620, 'c': 4551, 'asm': 162}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：20
- 含证据关键结论数：20（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
