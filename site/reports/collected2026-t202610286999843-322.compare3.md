# 2026 T202610286999843 比较报告

- 对比历史仓库：2025 静春山
- 生成时间：2026-07-01T20:57:35.976547+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 静春山（来源：赛事历史作品）：画像相似度 score=8.53；同属 ucore-variant 风格; 语言构成相似度 0.39; OS 维度重合度 1.00; 代码规模接近度 0.75

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 静春山 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `kernel/drive/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/fs/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 与 2025 静春山 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs.h:L8-L12`：关键词命中
    代码片段：`// Disk layout: // [ boot block | super block | log | inode blocks | //                                          free bit map | data blocks] //`
  - `kernel/fs/bio.c:L19-L23`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include <fs/vfs/fs.h> #include <fs/buf.h>`
  - `kernel/fs/pipe.c:L6-L10`：关键词命中
    代码片段：`#include "proc/proc.h" #include "fs/fcntl.h" #include "fs/vfs/file.h" #define PIPESIZE 512`
- 与 2025 静春山 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L32-L36`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/trap/la/apic.c:L7-L11`：关键词命中
    代码片段：`#include <dev/pci/pci.h> // // the loongarch 7A1000 I/O Interrupt Controller Registers. //`
  - `kernel/trap/la/trap.c:L13-L17`：关键词命中
    代码片段：`#include "mem.h" struct spinlock interrupt; struct spinlock tickslock; uint ticks;`
- 与 2025 静春山 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/vm.c:L10-L14`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/vm-la.c:L27-L31`：关键词命中
    代码片段：`pagetable_t kpgtbl; kpgtbl = (pagetable_t) kalloc(); memset(kpgtbl, 0, PGSIZE); proc_mapstacks(kpgtbl);`
  - `kernel/mm/vm.c:L12-L16`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/mm/buddy.c:L242-L246`：关键词命中
    代码片段：`// Also ensure it fits within our static buffer size int level = 0; uint64 size = PGSIZE;             // Start with page size int max_level = MAX_BUDDY_LEVEL;  // Allow managing...`
- 与 2025 静春山 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc.c:L8-L12`：关键词命中
    代码片段：`#endif #include "spinlock.h" #include "proc.h" #include "defs.h"`
  - `kernel/proc.h:L46-L50`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `kernel/proc/exec.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/lock.h"  // 假设锁的定义在这里 #include "proc/proc.h" #include "defs.h" #include "lib/elf.h"`
  - `kernel/proc/proc.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/lock.h" #include "proc/proc.h" #include "fs/vfs/fs.h"`
- 与 2025 静春山 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs.c:L15-L19`：关键词命中
    代码片段：`#include "param.h" #include "stat.h" #include "spinlock.h" #include "proc.h" #include "sleeplock.h"`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "fs.h"`
  - `kernel/lock/spinlock.c:L4-L8`：关键词命中
    代码片段：`#include "param.h" #include "memlayout.h" #include <lock/lock.h> #include "platform.h" #include <proc/proc.h>`
  - `kernel/lock/sleeplock.c:L6-L10`：关键词命中
    代码片段：`#include "param.h" #include "memlayout.h" #include <lock/lock.h> #include <proc/proc.h>`
- 与 2025 静春山 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap.c:L59-L63`：关键词命中
    代码片段：`kexit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h"`
  - `kernel/trap/la/trap.c:L130-L134`：关键词命中
    代码片段：`if (p->killed) exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->era += 4;`
  - `kernel/trap/rv/trap.c:L117-L121`：关键词命中
    代码片段：`} // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 静春山 在“系统调用”维度发现片段级代码相似度 0.58 （token=0.57, structure=0.60）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：epc, p, trapframe。（置信度：medium）
  证据：
  - `kernel/trap.c:L59-L63`：关键词命中
    代码片段：`kexit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/trap/rv/trap.c:L117-L121`：关键词命中
    代码片段：`} // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
- 宏名重合：与 2025 静春山 在“设备驱动”维度发现 4 个同名定义：UART0, UART0_IRQ, VIRTIO0, VIRTIO0_IRQ。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/memlayout.h:L22-L22`：macro UART0
    代码片段：`#define UART0 (0x1fe001e0UL | DMWIN_MASK)`
  - `include/memlayout.h:L25-L25`：macro UART0
    代码片段：`#define UART0 0x10000000L`
  - `kernel/memlayout.h:L23-L23`：macro UART0_IRQ
    代码片段：`#define UART0_IRQ 2`
  - `include/memlayout.h:L27-L27`：macro UART0_IRQ
    代码片段：`#define UART0_IRQ 32`
- 宏名重合：与 2025 静春山 在“文件系统”维度发现 4 个同名定义：MAP_FILE, NELEM, NOFILE, NFILE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `codex-basic-syscall-bundle/references/headers/stddef.h:L156-L156`：macro MAP_FILE
    代码片段：`#define MAP_FILE 0`
  - `include/mem.h:L10-L10`：macro MAP_FILE
    代码片段：`#define MAP_FILE 0`
  - `kernel/defs.h:L240-L240`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x)/sizeof((x)[0]))`
  - `include/defs.h:L226-L226`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x) / sizeof((x)[0]))`
- 宏名重合：与 2025 静春山 在“中断与异常”维度发现 4 个同名定义：LOONGARCH_IOCSR_EXTIOI_ISR_BASE, UART0_IRQ, VIRTIO0_IRQ, PLIC。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/loongarch.h:L91-L91`：macro LOONGARCH_IOCSR_EXTIOI_ISR_BASE
    代码片段：`#define LOONGARCH_IOCSR_EXTIOI_ISR_BASE		0x1800`
  - `include/platform.h:L304-L304`：macro LOONGARCH_IOCSR_EXTIOI_ISR_BASE
    代码片段：`#define LOONGARCH_IOCSR_EXTIOI_ISR_BASE 0x1800`
  - `kernel/memlayout.h:L23-L23`：macro UART0_IRQ
    代码片段：`#define UART0_IRQ 2`
  - `include/memlayout.h:L27-L27`：macro UART0_IRQ
    代码片段：`#define UART0_IRQ 32`
- 宏名重合：与 2025 静春山 在“内存管理”维度发现 4 个同名定义：PTEWIDTH, PGSIZE, PGROUNDUP, PGROUNDDOWN。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/loongarch.h:L355-L355`：macro PTEWIDTH
    代码片段：`#define PTEWIDTH  0U`
  - `include/platform.h:L459-L459`：macro PTEWIDTH
    代码片段：`#define PTEWIDTH 0U`
  - `kernel/loongarch.h:L360-L360`：macro PGSIZE
    代码片段：`#define PGSIZE 4096`
  - `include/platform.h:L258-L258`：macro PGSIZE
    代码片段：`#define PGSIZE 4096  // bytes per page`
- 宏名重合：与 2025 静春山 在“同步机制”维度发现 3 个同名定义：MAXOPBLOCKS, NBUF, FSSIZE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/param.h:L11-L11`：macro MAXOPBLOCKS
    代码片段：`#define MAXOPBLOCKS  10  // max # of blocks any FS op writes`
  - `include/param.h:L12-L12`：macro MAXOPBLOCKS
    代码片段：`#define MAXOPBLOCKS 20             // max # of blocks any FS op writes`
  - `kernel/param.h:L13-L13`：macro NBUF
    代码片段：`#define NBUF         (MAXOPBLOCKS*3)  // size of disk block cache`
  - `include/param.h:L14-L14`：macro NBUF
    代码片段：`#define NBUF (MAXOPBLOCKS * 3)     // size of disk block cache`
- 宏名重合：与 2025 静春山 在“系统调用”维度发现 4 个同名定义：TRAPFRAME, NFILE, ROOTDEV, FSSIZE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/memlayout.h:L112-L112`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME (TRAMPOLINE - PGSIZE)`
  - `include/memlayout.h:L64-L64`：macro TRAPFRAME
    代码片段：`#define TRAPFRAME (SIG_TRAMPOLINE - PGSIZE)`
  - `kernel/param.h:L4-L4`：macro NFILE
    代码片段：`#define NFILE       256  // open files per system`
  - `include/param.h:L6-L6`：macro NFILE
    代码片段：`#define NFILE 100                  // open files per system`
- 文件路径重合：与 2025 静春山 在“设备驱动”维度出现同名文件源码路径 `kernel/uart.c` / `kernel/drive/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/drive/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
- 文件路径重合：与 2025 静春山 在“设备驱动”维度出现同名文件源码路径 `kernel/console.c` / `kernel/fs/console.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/fs/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 文件路径重合：与 2025 静春山 在“文件系统”维度出现同名文件源码路径 `kernel/fs.c` / `kernel/fs/vfs/fs.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs/vfs/fs.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include <defs.h>`
- 文件路径重合：与 2025 静春山 在“中断与异常”维度出现同名文件源码路径 `kernel/trap.c` / `kernel/trap/la/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/trap.c:L32-L36`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/trap/la/trap.c:L13-L17`：关键词命中
    代码片段：`#include "mem.h" struct spinlock interrupt; struct spinlock tickslock; uint ticks;`
- 文件路径重合：与 2025 静春山 在“中断与异常”维度出现同名文件源码路径 `kernel/trap.c` / `kernel/trap/rv/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/trap.c:L32-L36`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/trap/rv/trap.c:L9-L13`：关键词命中
    代码片段：`#include <mem.h> struct spinlock interrupt; struct spinlock tickslock; uint ticks;`
- 文件路径重合：与 2025 静春山 在“内存管理”维度出现同名文件源码路径 `kernel/vm.c` / `kernel/mm/vm.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/vm.c:L10-L14`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/mm/vm.c:L12-L16`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`

## 相似点

- 与 2025 静春山 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 静春山 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
- 与 2025 静春山 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs.h:L8-L12`：关键词命中
    代码片段：`// Disk layout: // [ boot block | super block | log | inode blocks | //                                          free bit map | data blocks] //`
- 与 2025 静春山 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L32-L36`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
- 与 2025 静春山 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/vm.c:L10-L14`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/vm-la.c:L27-L31`：关键词命中
    代码片段：`pagetable_t kpgtbl; kpgtbl = (pagetable_t) kalloc(); memset(kpgtbl, 0, PGSIZE); proc_mapstacks(kpgtbl);`
- 与 2025 静春山 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc.c:L8-L12`：关键词命中
    代码片段：`#endif #include "spinlock.h" #include "proc.h" #include "defs.h"`
  - `kernel/proc.h:L46-L50`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2025 静春山 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs.c:L15-L19`：关键词命中
    代码片段：`#include "param.h" #include "stat.h" #include "spinlock.h" #include "proc.h" #include "sleeplock.h"`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "arch.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "fs.h"`
- 与 2025 静春山 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap.c:L59-L63`：关键词命中
    代码片段：`kexit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h"`
- 与 2025 静春山 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 静春山 的语言构成不同：待测作品为 {'markdown': 15110, 'build': 636, 'c': 21332, 'asm': 49345}，历史样本为 {'json': 22, 'markdown': 7411, 'text': 1439, 'build': 891, 'c': 51250, 'asm': 924}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：27
- 含证据关键结论数：27（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
