# 2026 T202610487999858 比较报告

- 对比历史仓库：2022 图漏图森破
- 生成时间：2026-07-01T21:12:59.148205+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 图漏图森破（来源：赛事历史作品）：画像相似度 score=9.68；同属 ucore-variant 风格; 语言构成相似度 0.97; OS 维度重合度 1.00; 代码规模接近度 0.75

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 图漏图森破 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mem/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/driver/bio.c:L23-L27`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include "fs/buf.h" #include "dev/virtio.h" struct {`
  - `kernel/driver/Sd.c:L1-L5`：关键词命中
    代码片段：`#include <Platform.h> #include <Spi.h> #include <Uart.h> #include <Driver.h> #include <MemoryConfig.h>`
  - `kernel/driver/Print.c:L1-L3`：关键词命中
    代码片段：`#include <Driver.h> #include <Riscv.h> #include <Spinlock.h>`
- 与 2022 图漏图森破 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/vfs/fs.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include <defs.h>`
  - `kernel/fs/vfs/ops.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/ops.h" #include <fs/fcntl.h>`
  - `kernel/fs/Fat.c:L1-L5`：关键词命中
    代码片段：`#include <Defs.h> #include "Bio.h" #include "Fat.h" #include "Sleeplock.h" #include "Spinlock.h"`
  - `kernel/fs/File.c:L101-L105`：关键词命中
    代码片段：`// Read from file f. // addr is a user virtual address. int fileread(struct File* f, bool isUser, u64 addr, int n) { int r = 0;`
- 与 2022 图漏图森破 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sys/plic.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "proc/plic.h" //`
  - `kernel/trap/riscv/trap.c:L8-L12`：关键词命中
    代码片段：`#include "mem/mem.h" #include "sbi.h" #include "proc/plic.h" #include "dev/virtio.h" #include "lib/string.h"`
  - `kernel/trap/Exec.c:L9-L13`：关键词命中
    代码片段：`#include <Sysarg.h> #include <Debug.h> #include <Trap.h> #include <Sysfile.h> #include <uapi/linux/Auxvec.h>`
  - `kernel/trap/Trap.c:L1-L5`：关键词命中
    代码片段：`#include <Riscv.h> #include <Driver.h> #include <Timer.h> #include <Trap.h> #include <Process.h>`
- 与 2022 图漏图森破 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/mem/vm.c:L8-L12`：关键词命中
    代码片段：`#include "defs.h" #include "fs/vfs/fs.h" #include "mem/kalloc.h" #include "lib/string.h" #include "dev/virtio.h"`
  - `kernel/mem/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "mem/kalloc.h" #include <mem/slab.h>`
  - `kernel/memory/Page.c:L18-L22`：关键词命中
    代码片段：`} int pageRemove(u64 *pgdir, u64 va) { u64 *pte; u64 pa = pageLookup(pgdir, va, &pte);`
  - `kernel/memory/MemoryInit.c:L37-L41`：关键词命中
    代码片段：`static void virtualMemory() { u64 va, pa; pageInsert(kernelPageDirectory, UART_V, UART, PTE_READ | PTE_WRITE | PTE_ACCESSED | PTE_DIRTY); va = CLINT_V, pa = CLINT; for (u64 i =...`
- 与 2022 图漏图森破 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/exec.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h" #include "lib/elf.h"`
  - `kernel/proc/pipe.c:L4-L8`：关键词命中
    代码片段：`#include "param.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "lock/sleeplock.h" #include "fs/fcntl.h"`
  - `kernel/yield/Thread.c:L1-L3`：关键词命中
    代码片段：`#include <Thread.h> #include <Riscv.h> #include <MemoryConfig.h>`
  - `kernel/fs/Fat.c:L12-L16`：关键词命中
    代码片段：`#include <FileSystem.h> #include <Sysfile.h> #include <Thread.h> #include <Riscv.h>`
- 与 2022 图漏图森破 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "lock/semaphore.h" #include "defs.h"`
  - `kernel/mem/uart.c:L7-L11`：关键词命中
    代码片段：`#include "mem/memlayout.h" #include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h"`
  - `kernel/lock/Spinlock.c:L1-L3`：关键词命中
    代码片段：`#include "Spinlock.h" #include "Hart.h" #include "Interrupt.h"`
  - `kernel/lock/Sleeplock.c:L3-L7`：关键词命中
    代码片段：`#include "Sleeplock.h" #include "Process.h" #include "Spinlock.h" #include "Driver.h" #include <Debug.h>`
- 与 2022 图漏图森破 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sys/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "lock/spinlock.h" #include "proc/proc.h" #include "sys/syscall.h" #include "defs.h" #include "lib/string.h"`
  - `kernel/trap/riscv/trap.c:L188-L192`：关键词命中
    代码片段：`exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/trap/Syscall.c:L1-L3`：关键词命中
    代码片段：`#include <Syscall.h> #include <Driver.h> #include <Process.h>`
  - `kernel/system/SignalTrampoline.S:L4-L6`：关键词命中
    代码片段：`signalTrampoline: li a7, 139 ecall`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2022 图漏图森破 在“同步机制”维度发现片段级代码相似度 0.69 （token=0.52, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：char, name, void。（置信度：medium）
  证据：
  - `kernel/proc/semaphore.c:L3-L7`：fn sem_init
    代码片段：`#include "defs.h" void sem_init(sem *s, int value, char *name) { s->value = value; s->top = 0;`
  - `kernel/lock/Spinlock.c:L4-L8`：fn initLock
    代码片段：`#include "Driver.h" void initLock(struct Spinlock* lock, char* name) { lock->name = name; lock->locked = 0;`
- 宏名重合：与 2022 图漏图森破 在“设备驱动”维度发现 3 个同名定义：ENXIO, ENODEV, ENOSPC。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/fs/ext4/lwext4/ext4_errno.h:L16-L16`：macro ENXIO
    代码片段：`#define ENXIO 6 /* No such device or address */`
  - `include/Error.h:L20-L20`：macro ENXIO
    代码片段：`#define	ENXIO		 6	/* No such device or address */`
  - `include/fs/ext4/lwext4/ext4_errno.h:L22-L22`：macro ENODEV
    代码片段：`#define ENODEV 19 /* No such device */`
  - `include/Error.h:L33-L33`：macro ENODEV
    代码片段：`#define	ENODEV		19	/* No such device */`
- 宏名重合：与 2022 图漏图森破 在“文件系统”维度发现 4 个同名定义：CLONE_FS, CLONE_FILES, ENOENT, EEXIST。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `include/defs.h:L162-L162`：macro CLONE_FS
    代码片段：`#define CLONE_FS     0x00000200  // 共享文件系统信息`
  - `include/Process.h:L77-L77`：macro CLONE_FS
    代码片段：`#define CLONE_FS	0x00000200`
  - `include/defs.h:L163-L163`：macro CLONE_FILES
    代码片段：`#define CLONE_FILES  0x00000400  // 共享文件描述符表`
  - `include/Process.h:L78-L78`：macro CLONE_FILES
    代码片段：`#define CLONE_FILES	0x00000400`
- 宏名重合：与 2022 图漏图森破 在“内存管理”维度发现 2 个同名定义：CLONE_VM, ENOMEM。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/defs.h:L161-L161`：macro CLONE_VM
    代码片段：`#define CLONE_VM     0x00000100  // 共享内存空间`
  - `include/Process.h:L76-L76`：macro CLONE_VM
    代码片段：`#define CLONE_VM	0x00000100`
  - `include/fs/ext4/lwext4/ext4_errno.h:L18-L18`：macro ENOMEM
    代码片段：`#define ENOMEM 12 /* Out of memory */`
  - `include/Error.h:L26-L26`：macro ENOMEM
    代码片段：`#define	ENOMEM		12	/* Out of memory */`
- 宏名重合：与 2022 图漏图森破 在“调度与任务管理”维度发现 1 个同名定义：CLONE_THREAD。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/defs.h:L166-L166`：macro CLONE_THREAD
    代码片段：`#define CLONE_THREAD 0x00010000`
  - `include/Process.h:L84-L84`：macro CLONE_THREAD
    代码片段：`#define CLONE_THREAD	0x00010000`
- 宏名重合：与 2022 图漏图森破 在“系统调用”维度发现 1 个同名定义：EROFS。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `include/fs/ext4/lwext4/ext4_errno.h:L28-L28`：macro EROFS
    代码片段：`#define EROFS 30 /* Read-only file system */`
  - `include/Error.h:L44-L44`：macro EROFS
    代码片段：`#define	EROFS		30	/* Read-only file system */`
- 文件路径重合：与 2022 图漏图森破 在“设备驱动”维度出现同名文件源码路径 `kernel/lib/console.c` / `kernel/driver/Console.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/lib/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/driver/Console.c:L1-L4`：关键词命中
    代码片段：`#include <Type.h> #include <Driver.h> #include <Uart.h> #include <Spinlock.h>`
- 文件路径重合：与 2022 图漏图森破 在“文件系统”维度出现同名文件源码路径 `kernel/fs/vfs/file.c` / `kernel/fs/File.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/fs/vfs/file.c:L5-L9`：关键词命中
    代码片段：`#include "lock/sleeplock.h" #include "fs/stat.h" #include "fs/vfs/fs.h" #include "fs/vfs/file.h"`
  - `kernel/fs/File.c:L101-L105`：关键词命中
    代码片段：`// Read from file f. // addr is a user virtual address. int fileread(struct File* f, bool isUser, u64 addr, int n) { int r = 0;`
- 文件路径重合：与 2022 图漏图森破 在“中断与异常”维度出现同名文件源码路径 `kernel/trap/riscv/trap.c` / `kernel/trap/Trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/trap/riscv/trap.c:L8-L12`：关键词命中
    代码片段：`#include "mem/mem.h" #include "sbi.h" #include "proc/plic.h" #include "dev/virtio.h" #include "lib/string.h"`
  - `kernel/trap/Trap.c:L1-L5`：关键词命中
    代码片段：`#include <Riscv.h> #include <Driver.h> #include <Timer.h> #include <Trap.h> #include <Process.h>`
- 文件路径重合：与 2022 图漏图森破 在“中断与异常”维度出现同名文件源码路径 `kernel/trap/loongarch/trap.c` / `kernel/trap/Trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/trap/loongarch/trap.c:L102-L106`：fn pagefault_handler
    代码片段：`} int pagefault_handler(uint64 va, uint64 cause) { /* *TODO: 支持lazy allocation和cow`
  - `kernel/trap/Trap.c:L1-L5`：关键词命中
    代码片段：`#include <Riscv.h> #include <Driver.h> #include <Timer.h> #include <Trap.h> #include <Process.h>`
- 文件路径重合：与 2022 图漏图森破 在“系统调用”维度出现同名文件源码路径 `kernel/sys/syscall.c` / `kernel/trap/Syscall.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/sys/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "lock/spinlock.h" #include "proc/proc.h" #include "sys/syscall.h" #include "defs.h" #include "lib/string.h"`
  - `kernel/trap/Syscall.c:L1-L3`：关键词命中
    代码片段：`#include <Syscall.h> #include <Driver.h> #include <Process.h>`

## 相似点

- 与 2022 图漏图森破 同属 ucore-variant 风格。（置信度：medium）
- 与 2022 图漏图森破 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mem/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/driver/bio.c:L23-L27`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include "fs/buf.h" #include "dev/virtio.h" struct {`
- 与 2022 图漏图森破 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/vfs/fs.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include <defs.h>`
  - `kernel/fs/vfs/ops.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/ops.h" #include <fs/fcntl.h>`
- 与 2022 图漏图森破 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sys/plic.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "proc/plic.h" //`
  - `kernel/trap/riscv/trap.c:L8-L12`：关键词命中
    代码片段：`#include "mem/mem.h" #include "sbi.h" #include "proc/plic.h" #include "dev/virtio.h" #include "lib/string.h"`
- 与 2022 图漏图森破 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/mem/vm.c:L8-L12`：关键词命中
    代码片段：`#include "defs.h" #include "fs/vfs/fs.h" #include "mem/kalloc.h" #include "lib/string.h" #include "dev/virtio.h"`
  - `kernel/mem/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "mem/kalloc.h" #include <mem/slab.h>`
- 与 2022 图漏图森破 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/exec.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h" #include "lib/elf.h"`
  - `kernel/proc/pipe.c:L4-L8`：关键词命中
    代码片段：`#include "param.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "lock/sleeplock.h" #include "fs/fcntl.h"`
- 与 2022 图漏图森破 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "lock/semaphore.h" #include "defs.h"`
  - `kernel/mem/uart.c:L7-L11`：关键词命中
    代码片段：`#include "mem/memlayout.h" #include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h"`
- 与 2022 图漏图森破 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sys/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "lock/spinlock.h" #include "proc/proc.h" #include "sys/syscall.h" #include "defs.h" #include "lib/string.h"`
  - `kernel/trap/riscv/trap.c:L188-L192`：关键词命中
    代码片段：`exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
- 与 2022 图漏图森破 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 图漏图森破 的语言构成不同：待测作品为 {'build': 549, 'text': 18, 'markdown': 954, 'c': 282443, 'cpp': 36728, 'asm': 864}，历史样本为 {'json': 20, 'make': 28, 'build': 326, 'markdown': 3993, 'c': 405628, 'asm': 408, 'cpp': 36715, 'text': 55}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：25
- 含证据关键结论数：25（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
