# 2026 T2026104889910567 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026104889910567-3996`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：176
- 代码/文本行数：18606
- 主要语言：c 17312 LOC, asm 709 LOC, markdown 303 LOC, build 240 LOC, make 42 LOC

## 总览

2026 T2026104889910567 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 c: 17312 LOC, asm: 709 LOC, markdown: 303 LOC, build: 240 LOC。仓库包含 176 个已扫描文件、约 18606 行可分析文本，当前抽取到 1129 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, src/arch/loongarch64/Makefile, src/arch/riscv64/Makefile, src/async/Makefile, src/boot/Makefile。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`# 工具链设置 TOOL_PREFIX = riscv64-unknown-elf- CC = $(TOOL_PREFIX)gcc`
  - `src/arch/loongarch64/Makefile:L1-L3`：构建入口
    代码片段：`MODULE_PATH := arch/loongarch64 SRCS := entry.S trap.S switch.S idle.S kthread_entry.S mmu.c SUBMODULES :=`
  - `src/arch/riscv64/Makefile:L1-L3`：构建入口
    代码片段：`MODULE_PATH := arch/riscv64 SRCS := entry.S switch.S idle.S kthread_entry.S kernelvec.S trampoline.S SUBMODULES :=`
  - `src/async/Makefile:L1-L3`：构建入口
    代码片段：`MODULE_PATH := async SRCS := SUBMODULES :=`
  - `src/boot/Makefile:L1-L3`：构建入口
    代码片段：`MODULE_PATH := boot SRCS := main.c entry.S SUBMODULES :=`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn idle_loop, fn is_runq_empty, fn context_switch, fn context_switch_to, struct elf_info。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/task/proc.c` | L1-L3 | 关键词命中 |
| `src/task/sched.c` | L1-L3 | 关键词命中 |
| `src/task/switch.S` | L1-L5 | 关键词命中 |
| `src/task/idle.S` | L1-L4 | fn idle_loop |
| `src/task/sched.c` | L18-L22 | fn is_runq_empty |
| `src/task/switch.S` | L7-L11 | fn context_switch |

### 关键代码片段

  - `src/task/proc.c:L1-L3`：关键词命中
    代码片段：`#include <task/proc.h> #include <task/sched.h> #include <config.h>`
  - `src/task/sched.c:L1-L3`：关键词命中
    代码片段：`#include <task/sched.h> #include <misc/list.h> #include <misc/log.h>`
  - `src/task/switch.S:L1-L5`：关键词命中
    代码片段：`# Context switch # #   void context_switch(struct context *old, struct context *new); # # Save current registers in old. Load from new.`
  - `src/task/idle.S:L1-L4`：fn idle_loop
    代码片段：`.globl idle_loop idle_loop: nop call sched_yield`

### 相关符号

`fn idle_loop` at `src/task/idle.S:L1`、`fn is_runq_empty` at `src/task/sched.c:L18`、`fn context_switch` at `src/task/switch.S:L7`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：struct slab, fn slab_empty, struct mem_block, fn tf_overlap_check, fn buddy_nesting_enter。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/mm/pm.c` | L4-L8 | 关键词命中 |
| `src/mm/vm.c` | L1-L4 | 关键词命中 |
| `src/mm/vma.c` | L1-L4 | 关键词命中 |
| `src/mm/slab.c` | L21-L25 | struct slab |
| `src/mm/slab.c` | L39-L43 | fn slab_empty |
| `src/mm/buddy.c` | L23-L27 | struct mem_block |

### 关键代码片段

  - `src/mm/pm.c:L4-L8`：关键词命中
    代码片段：`#include <misc/list.h> #include <mm/early.h> #include <mm/kalloc.h> #include <mm/buddy.h> #include <misc/log.h>`
  - `src/mm/vm.c:L1-L4`：关键词命中
    代码片段：`#include <misc/log.h> #include <mm/pagetable.h> #include <mm/vm.h> #include <mm/kalloc.h>`
  - `src/mm/vma.c:L1-L4`：关键词命中
    代码片段：`#include <misc/errno.h> #include <mm/kalloc.h> #include <mm/pagetable.h> #include <mm/vm.h>`
  - `src/mm/slab.c:L21-L25`：struct slab
    代码片段：`#define slab_log(fmt, ...) dummy(0, ##__VA_ARGS__) #endif struct slab { struct list_head list; struct kmem_cache *kmem;`

### 相关符号

`struct slab` at `src/mm/slab.c:L21`、`fn slab_empty` at `src/mm/slab.c:L39`、`struct mem_block` at `src/mm/buddy.c:L23`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn sys_mount, fn sys_umount2, struct elf_info, fn kernelvec, fn kernelret。（置信度：medium）
  - 静态识别到 36 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/syscall/ipc.c` | L18-L22 | 关键词命中 |
| `src/syscall/file.c` | L17-L21 | 关键词命中 |
| `src/syscall/mmap.c` | L1-L3 | 关键词命中 |
| `src/syscall/file.c` | L359-L363 | fn sys_mount |
| `src/syscall/file.c` | L360-L364 | fn sys_umount2 |
| `src/syscall/proc.c` | L191-L195 | struct elf_info |

### 关键代码片段

  - `src/syscall/ipc.c:L18-L22`：关键词命中
    代码片段：`*   恢复保存的 saved_tf 到 trapframe，清除 in_signal_handler 标志 */ #include <syscall/syscall.h> #include <task/proc.h> #include <task/sched.h>`
  - `src/syscall/file.c:L17-L21`：关键词命中
    代码片段：`*   fdnum 仅提取 fd 号做基本校验，再手动 fd_free + vfs_close */ #include <syscall/syscall.h> #include <hal/uart.h> #include <task/proc.h>`
  - `src/syscall/mmap.c:L1-L3`：关键词命中
    代码片段：`#include <syscall/syscall.h> #include <task/proc.h> #include <task/sched.h>`
  - `src/syscall/file.c:L359-L363`：fn sys_mount
    代码片段：`} int64_t sys_mount(void) { return 0; } int64_t sys_umount2(void) { return 0; }`

### 相关符号

`fn sys_mount` at `src/syscall/file.c:L359`、`fn sys_umount2` at `src/syscall/file.c:L360`、`struct elf_info` at `src/syscall/proc.c:L191`、`fn sys_mount` at `src/syscall/file.c:L359`、`fn sys_umount2` at `src/syscall/file.c:L360`、`macro NR_SYSCALLS` at `include/syscall/syscall.h:L66`、`macro sys_write` at `user/shell.c:L25`、`macro sys_read` at `user/shell.c:L26`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：fn vfs_close, struct ramfs_node, struct ramfs_sb_info, struct ramfs_file, struct dentry_match_arg。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/fs/vfs.c` | L1-L3 | 关键词命中 |
| `src/fs/file.c` | L1-L4 | 关键词命中 |
| `src/fs/inode.c` | L1-L3 | 关键词命中 |
| `src/fs/vfs.c` | L332-L336 | fn vfs_close |
| `src/fs/ramfs.c` | L10-L14 | struct ramfs_node |
| `src/fs/ramfs.c` | L21-L25 | struct ramfs_sb_info |

### 关键代码片段

  - `src/fs/vfs.c:L1-L3`：关键词命中
    代码片段：`#include <fs/vfs.h> #include <misc/log.h> #include <misc/string.h>`
  - `src/fs/file.c:L1-L4`：关键词命中
    代码片段：`#include <fs/file.h> #include <fs/inode.h> #include <fs/dentry.h> #include <misc/log.h>`
  - `src/fs/inode.c:L1-L3`：关键词命中
    代码片段：`#include <fs/inode.h> #include <fs/super.h> #include <misc/log.h>`
  - `src/fs/vfs.c:L332-L336`：fn vfs_close
    代码片段：`} int vfs_close(struct file *file) { return file_close(file); } ssize_t vfs_read(struct file *file, void *buf, size_t count)`

### 相关符号

`fn vfs_close` at `src/fs/vfs.c:L332`、`struct ramfs_node` at `src/fs/ramfs.c:L10`、`struct ramfs_sb_info` at `src/fs/ramfs.c:L21`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn mb, fn rmb, fn wmb。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/sync/spinlock.c` | L1-L3 | 关键词命中 |
| `src/fs/vfs.c` | L5-L9 | 关键词命中 |
| `src/fs/file.c` | L7-L11 | 关键词命中 |
| `include/sync/barrier.h` | L7-L11 | fn mb |
| `include/sync/barrier.h` | L10-L14 | fn rmb |
| `include/sync/barrier.h` | L13-L17 | fn wmb |

### 关键代码片段

  - `src/sync/spinlock.c:L1-L3`：关键词命中
    代码片段：`#include <sync/spinlock.h> #include <trap/trap.h> #include <arch/cpu.h>`
  - `src/fs/vfs.c:L5-L9`：关键词命中
    代码片段：`#include <misc/complier.h> #include <mm/kalloc.h> #include <sync/spinlock.h> static struct {`
  - `src/fs/file.c:L7-L11`：关键词命中
    代码片段：`#include <mm/slab.h> #include <mm/kalloc.h> #include <sync/spinlock.h> static struct kmem_cache file_cache;`
  - `include/sync/barrier.h:L7-L11`：fn mb
    代码片段：`/* 内存屏障（全屏障） */ static inline void mb(void) { arch_mb(); } /* 读内存屏障 */`

### 相关符号

`fn mb` at `include/sync/barrier.h:L7`、`fn rmb` at `include/sync/barrier.h:L10`、`fn wmb` at `include/sync/barrier.h:L13`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn kernelvec, fn kernelret, fn trampoline, fn uservec, fn userret。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/trap/trap.c` | L1-L3 | 关键词命中 |
| `src/trap/trampoline.S` | L16-L20 | 关键词命中 |
| `src/task/sched.c` | L2-L6 | 关键词命中 |
| `src/trap/kernelvec.S` | L8-L12 | fn kernelvec |
| `src/trap/kernelvec.S` | L59-L63 | fn kernelret |
| `src/trap/trampoline.S` | L11-L15 | fn trampoline |

### 关键代码片段

  - `src/trap/trap.c:L1-L3`：关键词命中
    代码片段：`#include <trap/trap.h> #include <task/proc.h> #include <task/sched.h>`
  - `src/trap/trampoline.S:L16-L20`：关键词命中
    代码片段：`uservec: # # trap.c sets stvec to point here, so # traps from user space start here, # in supervisor mode, but with a`
  - `src/task/sched.c:L2-L6`：关键词命中
    代码片段：`#include <misc/list.h> #include <misc/log.h> #include <trap/trap.h> #include <mm/kalloc.h>`
  - `src/trap/kernelvec.S:L8-L12`：fn kernelvec
    代码片段：`.align 4 kernelvec: # 1. 读取关键 CSR（必须最先做，避免 sepc 被覆盖） csrr t0, sepc`

### 相关符号

`fn kernelvec` at `src/trap/kernelvec.S:L8`、`fn kernelret` at `src/trap/kernelvec.S:L59`、`fn trampoline` at `src/trap/trampoline.S:L11`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：macro BLK_MAX_DEVICES, macro BLK_MAJOR_VIRTIO, macro BLK_MAJOR_SD, struct blk_operations, struct block_device。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/fs/uartdev.c` | L1-L3 | 关键词命中 |
| `src/hal/blk.c` | L1-L3 | 关键词命中 |
| `src/hal/uart.c` | L1-L3 | 关键词命中 |
| `src/hal/blk.c` | L4-L8 | macro BLK_MAX_DEVICES |
| `include/hal/blk.h` | L5-L9 | macro BLK_MAJOR_VIRTIO |
| `include/hal/blk.h` | L6-L10 | macro BLK_MAJOR_SD |

### 关键代码片段

  - `src/fs/uartdev.c:L1-L3`：关键词命中
    代码片段：`/* UART character device — provides stdin/stdout/stderr backing via VFS. * * The file's f_inode is NULL so file_read()/file_write() skip the VFS layer`
  - `src/hal/blk.c:L1-L3`：关键词命中
    代码片段：`#include <hal/blk.h> #include <misc/log.h> #include <misc/string.h>`
  - `src/hal/uart.c:L1-L3`：关键词命中
    代码片段：`#include <hal/uart.h> #include <hal/sbi.h> #include <hal/timer.h>`
  - `src/hal/blk.c:L4-L8`：macro BLK_MAX_DEVICES
    代码片段：`#include <mm/kalloc.h> #define BLK_MAX_DEVICES 16 static struct {`

### 相关符号

`macro BLK_MAX_DEVICES` at `src/hal/blk.c:L4`、`macro BLK_MAJOR_VIRTIO` at `include/hal/blk.h:L5`、`macro BLK_MAJOR_SD` at `include/hal/blk.h:L6`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
