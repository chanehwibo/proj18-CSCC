# 2026 T2026102569910192 比较报告

- 对比历史仓库：2025 undefined
- 生成时间：2026-07-02T09:01:25.754238+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 undefined（来源：赛事历史作品）：画像相似度 score=9.46；同属 ucore-variant 风格; 语言构成相似度 0.91; OS 维度重合度 1.00; 代码规模接近度 0.63

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 undefined 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `SWTC/kernel/src/driver/mod.rs:L12-L16`：关键词命中
    代码片段：`use self::{ fu740::{sdcard::SDCard, uart::UART, IntrSource}, plic::{initplic, PLIC}, qemu::virtio_blk::VirtIOBlock,`
  - `SWTC/kernel/src/driver/plic.rs:L4-L8`：关键词命中
    代码片段：`use core::{ops::BitAnd, panic}; use crate::{driver::Mutex, println, sync::mutex::SpinNoIrqLock, utils::error::GeneralRet}; pub struct PLIC { base_addr: usize,`
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2025 undefined 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `SWTC-la/xapi/src/fs/io.rs:L167-L171`：关键词命中
    代码片段：`with_fs(AT_FDCWD, path, |fs| { PAGE_CACHE_MANAGER .get_cache(fs.write_file(path)?.access(FileFlags::WRITE)?.inode()) .map(|inode| inode.evict_from_pos(length as _)) .unwrap_or(O...`
  - `SWTC/kernel/src/fs/ffi.rs:L2-L6`：关键词命中
    代码片段：`use super::fat32::SECTOR_SIZE; use super::{Inode, InodeMode}; use crate::timer::current_time_duration; use crate::timer::ffi::TimeSpec;`
  - `src/fs/imp/tmp.rs:L50-L54`：关键词命中
    代码片段：`pub struct MemoryFs { inodes: Mutex<Slab<Arc<Inode>>>, root: Mutex<Option<DirEntry<RawMutex>>>, }`
  - `src/fs/dynamic/file.rs:L45-L49`：关键词命中
    代码片段：`#[inherit_methods(from = "self.node")] impl NodeOps<RawMutex> for SimpleFile { fn inode(&self) -> u64; fn metadata(&self) -> VfsResult<Metadata>; fn update_metadata(&self, updat...`
- 与 2025 undefined 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `SWTC/kernel/src/trap/ctx.rs:L5-L9`：关键词命中
    代码片段：`use riscv::register::sstatus::{self, Sstatus, FS, SPP}; /// Trap context structure containing sstatus, sepc and registers #[derive(Clone, Copy, Debug)] #[repr(C)]`
  - `SWTC/kernel/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality mod ctx; /// Kernel trap handler`
  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `arceos/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
- 与 2025 undefined 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `SWTC/kernel/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `SWTC/kernel/src/mm/shm.rs:L7-L11`：关键词命中
    代码片段：`use crate::{ config::mm::{PAGE_SIZE, PAGE_SIZE_BITS}, mm::{memory_space::vm_area::VmAreaType, page_table::PTEFlags, MapPermission, PageBuilder}, processor::current_process, stac...`
  - `core/src/mm.rs:L24-L28`：关键词命中
    代码片段：`#[cfg(not(any(target_arch = "aarch64", target_arch = "loongarch64")))] { // ARMv8 (aarch64) and LoongArch64 use separate page tables for user space // (aarch64: TTBR0_EL1, Loong...`
  - `arceos/modules/axmm/src/backend/alloc.rs:L2-L6`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PhysAddr, VirtAddr};`
- 与 2025 undefined 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `SWTC-la/xapi/src/task/ctl.rs:L7-L11`：关键词命中
    代码片段：`}; use xcore::task::{get_process, with_uspace}; fn validate_cap_header(header: &mut __user_cap_header_struct) -> LinuxResult<()> {`
  - `SWTC-la/xapi/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod exit; mod futex; mod schedule; mod signal; mod thread;`
  - `core/src/task.rs:L14-L18`：关键词命中
    代码片段：`use spin::Once; use undefined_process::process::Process; use undefined_process::thread::Thread; /// Task extended data for the monolithic kernel.`
  - `src/fs/imp/proc.rs:L631-L635`：关键词命中
    代码片段：`Seccomp:        2 Seccomp_filters:        1 Speculation_Store_Bypass:       thread vulnerable SpeculationIndirectBranch:      conditional enabled Cpus_allowed:   ffff`
- 与 2025 undefined 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `SWTC/kernel/src/sync/mod.rs:L8-L12`：关键词命中
    代码片段：`mod mailbox; /// Different kinds of mutex pub mod mutex; pub use mailbox::Event;`
  - `SWTC/kernel/src/sync/futex.rs:L146-L150`：关键词命中
    代码片段：`/// The reason why we use Arc here is that we may need to change the /// addr this thread is waiting for(i.e. futex queue) through another thread. /// The reason why we use Unsa...`
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`use core::{alloc::Layout, sync::atomic::Ordering}; use crate::ctypes::TimeStat;`
  - `src/fs/imp/tmp.rs:L6-L10`：关键词命中
    代码片段：`use crate::fs::dynamic::dynamic::dummy_stat_fs; use axsync::{Mutex, RawMutex}; use slab::Slab; use undefined_vfs::fs::{Filesystem, FilesystemOps, StatFs};`
- 与 2025 undefined 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `SWTC-la/src/syscall.rs:L4-L8`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use syscalls::Sysno;`
  - `SWTC/kernel/src/syscall/fs.rs:L34-L38`：关键词命中
    代码片段：`use crate::stack_trace; use crate::sync::Event; use crate::syscall::PollEvents; use crate::timer::io_multiplex::{IOMultiplexFormat, IOMultiplexFuture, RawFdSetRWE}; use crate::t...`
  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_core::task::{time_stat_from_kernel_to_user, time_stat_from_user_to_kernel};`
  - `vendor/syscalls/src/syscall/arm.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %r5 // // %r7 is used for the syscall number. // // %r0 is reused for the syscall return value.`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 undefined 在“系统调用”维度发现片段级代码相似度 0.83 （token=0.78, structure=0.93）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, handle_syscall, isize, let, mut, new, register_trap_handler, syscall。（置信度：high）
  证据：
  - `SWTC-la/src/syscall.rs:L632-L636`：fn handle_syscall
    代码片段：`#[register_trap_handler(SYSCALL)] fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize { let sysno = Sysno::new(syscall_num); trace!("Syscall {:?}", sysno);`
  - `src/syscall.rs:L27-L31`：fn handle_syscall
    代码片段：`#[register_trap_handler(SYSCALL)] fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize { let sysno = Sysno::new(syscall_num as _); if sysno.is_none() {`
- 与 2025 undefined 在“内存管理”维度发现片段级代码相似度 0.77 （token=0.75, structure=0.82）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：access_flags, bool, fn, handle_page_fault, is_user, mappingflags, page_fault, register_trap_handler。（置信度：medium）
  证据：
  - `SWTC-la/src/mm.rs:L12-L16`：fn handle_page_fault
    代码片段：`#[register_trap_handler(PAGE_FAULT)] fn handle_page_fault(vaddr: VirtAddr, access_flags: MappingFlags, is_user: bool) -> bool { // warn!( //     "Page fault at {:#x}, access_fla...`
  - `src/mm.rs:L12-L16`：fn handle_page_fault
    代码片段：`#[register_trap_handler(PAGE_FAULT)] fn handle_page_fault(vaddr: VirtAddr, access_flags: MappingFlags, is_user: bool) -> bool { if vaddr.as_usize() > PHYS_VIRT_OFFSET { error!(`
- 函数/符号名重合：与 2025 undefined 在“文件系统”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `SWTC-la/xmodules/xvma/src/lib.rs:L44-L44`：fn new
    代码片段：`pub fn new(range: VirtAddrRange, file: F, offset: isize, align: PageSize) -> Self {`
  - `vendor/bindgen/clang.rs:L2036-L2036`：fn new
    代码片段：`pub(crate) fn new(name: &str, contents: &str) -> UnsavedFile {`
- 函数/符号名重合：与 2025 undefined 在“中断与异常”维度发现 1 个同名定义：handle_syscall。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `SWTC-la/src/syscall.rs:L634-L634`：fn handle_syscall
    代码片段：`fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize {`
  - `src/syscall.rs:L29-L29`：fn handle_syscall
    代码片段：`fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize {`
- 函数/符号名重合：与 2025 undefined 在“内存管理”维度发现 2 个同名定义：handle_page_fault, handle_syscall。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `SWTC-la/src/mm.rs:L14-L14`：fn handle_page_fault
    代码片段：`fn handle_page_fault(vaddr: VirtAddr, access_flags: MappingFlags, is_user: bool) -> bool {`
  - `src/mm.rs:L14-L14`：fn handle_page_fault
    代码片段：`fn handle_page_fault(vaddr: VirtAddr, access_flags: MappingFlags, is_user: bool) -> bool {`
  - `SWTC-la/src/syscall.rs:L634-L634`：fn handle_syscall
    代码片段：`fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize {`
  - `src/syscall.rs:L29-L29`：fn handle_syscall
    代码片段：`fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize {`
- 函数/符号名重合：与 2025 undefined 在“系统调用”维度发现 1 个同名定义：handle_syscall。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `SWTC-la/src/syscall.rs:L634-L634`：fn handle_syscall
    代码片段：`fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize {`
  - `src/syscall.rs:L29-L29`：fn handle_syscall
    代码片段：`fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize {`
- 文件路径重合：与 2025 undefined 在“中断与异常”维度出现同名文件源码路径 `SWTC/kernel/src/trap/trap.S` / `arceos/modules/axhal/src/arch/riscv/trap.S`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `SWTC/kernel/src/trap/trap.S:L63-L67`：关键词命中
    代码片段：`# switch to user space # let sscratch store the trap context's address csrw sscratch, a0 # save kernel callee-saved regs`
  - `arceos/modules/axhal/src/arch/riscv/trap.S:L53-L57`：关键词命中
    代码片段：`.global trap_vector_base trap_vector_base: // sscratch == 0: trap from S mode // sscratch != 0: trap from U mode csrrw   sp, sscratch, sp            // swap sscratch and sp`
- 文件路径重合：与 2025 undefined 在“内存管理”维度出现同名文件源码路径 `SWTC-la/src/mm.rs` / `core/src/mm.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `SWTC-la/src/mm.rs:L12-L16`：fn handle_page_fault
    代码片段：`#[register_trap_handler(PAGE_FAULT)] fn handle_page_fault(vaddr: VirtAddr, access_flags: MappingFlags, is_user: bool) -> bool { // warn!( //     "Page fault at {:#x}, access_fla...`
  - `core/src/mm.rs:L24-L28`：关键词命中
    代码片段：`#[cfg(not(any(target_arch = "aarch64", target_arch = "loongarch64")))] { // ARMv8 (aarch64) and LoongArch64 use separate page tables for user space // (aarch64: TTBR0_EL1, Loong...`
- 文件路径重合：与 2025 undefined 在“内存管理”维度出现同名文件源码路径 `SWTC-la/src/mm.rs` / `src/mm.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `SWTC-la/src/mm.rs:L12-L16`：fn handle_page_fault
    代码片段：`#[register_trap_handler(PAGE_FAULT)] fn handle_page_fault(vaddr: VirtAddr, access_flags: MappingFlags, is_user: bool) -> bool { // warn!( //     "Page fault at {:#x}, access_fla...`
  - `src/mm.rs:L12-L16`：fn handle_page_fault
    代码片段：`#[register_trap_handler(PAGE_FAULT)] fn handle_page_fault(vaddr: VirtAddr, access_flags: MappingFlags, is_user: bool) -> bool { if vaddr.as_usize() > PHYS_VIRT_OFFSET { error!(`
- 文件路径重合：与 2025 undefined 在“系统调用”维度出现同名文件源码路径 `SWTC-la/src/syscall.rs` / `src/syscall.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `SWTC-la/src/syscall.rs:L4-L8`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use syscalls::Sysno;`
  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_core::task::{time_stat_from_kernel_to_user, time_stat_from_user_to_kernel};`

## 相似点

- 与 2025 undefined 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 undefined 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `SWTC/kernel/src/driver/mod.rs:L12-L16`：关键词命中
    代码片段：`use self::{ fu740::{sdcard::SDCard, uart::UART, IntrSource}, plic::{initplic, PLIC}, qemu::virtio_blk::VirtIOBlock,`
  - `SWTC/kernel/src/driver/plic.rs:L4-L8`：关键词命中
    代码片段：`use core::{ops::BitAnd, panic}; use crate::{driver::Mutex, println, sync::mutex::SpinNoIrqLock, utils::error::GeneralRet}; pub struct PLIC { base_addr: usize,`
- 与 2025 undefined 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `SWTC-la/xapi/src/fs/io.rs:L167-L171`：关键词命中
    代码片段：`with_fs(AT_FDCWD, path, |fs| { PAGE_CACHE_MANAGER .get_cache(fs.write_file(path)?.access(FileFlags::WRITE)?.inode()) .map(|inode| inode.evict_from_pos(length as _)) .unwrap_or(O...`
  - `SWTC/kernel/src/fs/ffi.rs:L2-L6`：关键词命中
    代码片段：`use super::fat32::SECTOR_SIZE; use super::{Inode, InodeMode}; use crate::timer::current_time_duration; use crate::timer::ffi::TimeSpec;`
- 与 2025 undefined 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `SWTC/kernel/src/trap/ctx.rs:L5-L9`：关键词命中
    代码片段：`use riscv::register::sstatus::{self, Sstatus, FS, SPP}; /// Trap context structure containing sstatus, sepc and registers #[derive(Clone, Copy, Debug)] #[repr(C)]`
  - `SWTC/kernel/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality mod ctx; /// Kernel trap handler`
- 与 2025 undefined 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `SWTC/kernel/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `SWTC/kernel/src/mm/shm.rs:L7-L11`：关键词命中
    代码片段：`use crate::{ config::mm::{PAGE_SIZE, PAGE_SIZE_BITS}, mm::{memory_space::vm_area::VmAreaType, page_table::PTEFlags, MapPermission, PageBuilder}, processor::current_process, stac...`
- 与 2025 undefined 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `SWTC-la/xapi/src/task/ctl.rs:L7-L11`：关键词命中
    代码片段：`}; use xcore::task::{get_process, with_uspace}; fn validate_cap_header(header: &mut __user_cap_header_struct) -> LinuxResult<()> {`
  - `SWTC-la/xapi/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod exit; mod futex; mod schedule; mod signal; mod thread;`
- 与 2025 undefined 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `SWTC/kernel/src/sync/mod.rs:L8-L12`：关键词命中
    代码片段：`mod mailbox; /// Different kinds of mutex pub mod mutex; pub use mailbox::Event;`
  - `SWTC/kernel/src/sync/futex.rs:L146-L150`：关键词命中
    代码片段：`/// The reason why we use Arc here is that we may need to change the /// addr this thread is waiting for(i.e. futex queue) through another thread. /// The reason why we use Unsa...`
- 与 2025 undefined 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `SWTC-la/src/syscall.rs:L4-L8`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use syscalls::Sysno;`
  - `SWTC/kernel/src/syscall/fs.rs:L34-L38`：关键词命中
    代码片段：`use crate::stack_trace; use crate::sync::Event; use crate::syscall::PollEvents; use crate::timer::io_multiplex::{IOMultiplexFormat, IOMultiplexFuture, RawFdSetRWE}; use crate::t...`
- 与 2025 undefined 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 undefined 的语言构成不同：待测作品为 {'json': 294862, 'build': 41930, 'markdown': 86964, 'toml': 7554, 'rust': 3726200, 'c': 28156, 'text': 8331, 'cpp': 67, 'make': 984, 'asm': 1006, 'yaml': 318}，历史样本为 {'json': 282064, 'build': 21492, 'markdown': 41236, 'text': 30233, 'toml': 10472, 'rust': 1882737, 'c': 68083, 'yaml': 72, 'make': 1961, 'asm': 983}。（置信度：medium）

## 可能创新点

- 待测作品包含 280 个历史样本 2025 undefined 中未见的独特函数/类型定义（如 `get_as_slice`, `populate_region`, `read_str_array`, `get_as_null_terminated`, `from_fd`, `notify_event`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `SWTC-la/xmodules/xuspace/src/ptr.rs:L92-L92`：fn get_as_slice
    代码片段：`fn get_as_slice<A: UserSpaceAccess>(self, uspace: &A, len: usize) -> LinuxResult<&'static [T]>;`
  - `SWTC-la/xmodules/xuspace/src/uspace.rs:L45-L45`：fn populate_region
    代码片段：`fn populate_region(&self, range: VirtAddrRange, access_flags: MappingFlags) -> LinuxResult<()>;`

## 附录：核验摘要

- 关键结论数：25
- 含证据关键结论数：25（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
