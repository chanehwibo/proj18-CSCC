# 2026 T202610486999929 比较报告

- 对比历史仓库：2021 kCore
- 生成时间：2026-07-01T21:09:07.530172+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2021 kCore（来源：赛事历史作品）：画像相似度 score=9.68；同属 ucore-variant 风格; 语言构成相似度 0.98; OS 维度重合度 1.00; 代码规模接近度 0.71

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2021 kCore 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/fs/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console vnode backing file descriptors 0 (stdin), 1 (stdout), 2 (stderr). //! //! Reads blocks on the UART until a byte arrives. Writes output each byte`
  - `crates/scarlet/src/util/console.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::{self, Write}; struct Console; impl Write for Console {`
  - `os/src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
  - `os/src/drivers/block/mod.rs:L2-L6`：关键词命中
    代码片段：`#[cfg(feature = "board_qemu")] type BlockDeviceImpl = virtio_blk::VirtIOBlock; #[cfg(feature = "board_k210")]`
- 与 2021 kCore 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/fs/fat.rs:L1-L1`：关键词命中
    代码片段：`// FAT filesystem Vnode integration — TODO`
  - `crates/scarlet/src/fs/ext4.rs:L35-L39`：关键词命中
    代码片段：`#[derive(Clone)] struct CachedDirEntry { inode: u32, mode: u16, }`
  - `os/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`mod pipe; mod stdio; mod inode; use crate::mem::UserBuffer;`
  - `os/src/fs/inode.rs:L4-L8`：关键词命中
    代码片段：`use kcore_fs::{ KcoreFileSystem, Inode, }; use crate::drivers::BLOCK_DEVICE;`
- 与 2021 kCore 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap infrastructure: initialization, local exception handlers, and //! re-exports of public types.`
  - `crates/scarlet/src/irq/mod.rs:L1-L3`：关键词命中
    代码片段：`//! IRQ subsystem: IrqChip trait, per-IRQ descriptors, domain registration. //! //! Provides a Linux-style IRQ subsystem with hierarchical domains,`
  - `os/src/trap/mod.rs:L3-L7`：关键词命中
    代码片段：`use riscv::register::{ mtvec::TrapMode, scause::{self, Exception, Interrupt, Trap}, sie, stval, stvec, };`
  - `os/src/sbi.rs:L38-L42`：关键词命中
    代码片段：`} pub fn set_timer(timer: usize) { sbi_call(SBI_SET_TIMER, timer, 0, 0); }`
- 与 2021 kCore 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/mm/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Memory management: physical frames, kernel heap, page tables, and //! address-space mapping helpers.`
  - `crates/scarlet/src/mm/heap.rs:L39-L43`：关键词命中
    代码片段：`static HEAP: SyncHeap = SyncHeap(SpinMutex::new(KernelHeap::new())); static HEAP_READY: AtomicBool = AtomicBool::new(false); /// The global allocator instance.`
  - `os/src/mem/page.rs:L13-L17`：关键词命中
    代码片段：`/// } /// ''' /// and The address of the multi-level page table /// will be stored in the satp register, you can /// get the page table like this`
  - `os/src/mem/page_table.rs:L10-L14`：关键词命中
    代码片段：`use core::fmt::{self, Debug, Formatter}; pub struct PageTable { pub root: PhyPageNum, pages: Vec<PhyPageWrapper>,`
- 与 2021 kCore 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/syscall/sched.rs:L1-L3`：关键词命中
    代码片段：`//! Scheduler and process-lifecycle syscalls: clone, execve, waitid, wait4, //! set_tid_address, rseq, futex, set_robust_list, get_robust_list.`
  - `crates/scarlet/src/fs/procfs/task_dir.rs:L99-L103`：关键词命中
    代码片段：`// --------------------------------------------------------------------------- // ProcFdEntry — hybrid vnode for /proc/<pid>/fd/<n> entries. // Delegates I/O to the backing open...`
  - `os/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`mod processor; mod switch; mod task; use crate::loader::get_app_data_by_name;`
  - `os/src/task/task.rs:L60-L64`：关键词命中
    代码片段：`let kernel_stack = KernelStack::new(&pid_handle); let kernel_stack_top = kernel_stack.get_top(); // push a task context which goes to trap_return to the top of kernel stack let...`
- 与 2021 kCore 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/sync/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! Provides ticket-based spinlocks with IRQ-safe variants, //! replacing the external 'spin::Mutex' crate. pub mod tlb_shootdown;`
  - `crates/scarlet/src/sync/tlb_shootdown.rs:L3-L7`：关键词命中
    代码片段：`use crate::arch; use crate::smp::{self, HartMask, SmpError}; use core::sync::atomic::{AtomicUsize, Ordering}; const MAX_HARTS: usize = 8;`
  - `os/src/fs/pipe.rs:L3-L7`：关键词命中
    代码片段：`use super::File; use alloc::sync::{Arc, Weak}; use spin::Mutex; use crate::mem::{ UserBuffer,`
  - `os/src/fs/inode.rs:L11-L15`：关键词命中
    代码片段：`use bitflags::*; use alloc::vec::Vec; use spin::Mutex; use super::File; use crate::mem::UserBuffer;`
- 与 2021 kCore 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/syscall/fd.rs:L2-L6`：关键词命中
    代码片段：`//! stat, getdents, getcwd, chdir, dup, dup3, fcntl, ioctl. use crate::arch::shared::syscall::SyscallRequest; use crate::fs; use crate::process;`
  - `crates/scarlet/src/syscall/io.rs:L8-L12`：关键词命中
    代码片段：`//! blocking console reads don't stall unrelated process operations. use crate::arch::shared::syscall::SyscallRequest; use crate::fs::FsError; use crate::fs::file_table::OpenFile;`
  - `os/src/syscall/mod.rs:L18-L22`：关键词命中
    代码片段：`pub mod mem; pub fn syscall(syscall_id: usize, args: [usize; 3]) -> isize { match syscall_id { SYSCALL_READ => sys_read(args[0], args[1] as *const u8, args[2]),`
  - `os/src/sbi.rs:L15-L19`：关键词命中
    代码片段：`let mut ret; unsafe { llvm_asm!("ecall" : "={x10}" (ret) : "{x10}" (arg0), "{x11}" (arg1), "{x12}" (arg2), "{x17}" (which)`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2021 kCore 在“设备驱动”维度发现片段级代码相似度 0.91 （token=0.87, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：core, fmt, for, impl, self, struct, use, write。（置信度：high）
  证据：
  - `crates/scarlet/src/util/console.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::{self, Write}; struct Console; impl Write for Console {`
  - `os/src/console.rs:L2-L6`：struct Stdout
    代码片段：`use core::fmt::{self, Write}; struct Stdout; impl Write for Stdout {`
- 函数/符号名重合：与 2021 kCore 在“系统调用”维度发现 2 个同名定义：sys_write, sys_read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/sakuya/tests/basic-lseek/main.c:L14-L14`：fn sys_write
    代码片段：`static long sys_write(long fd, const void *buf, long n) {`
  - `user/src/syscall.rs:L31-L31`：fn sys_write
    代码片段：`pub fn sys_write(fd: usize, buffer: &[u8]) -> isize {`
  - `crates/sakuya/tests/basic-lseek/main.c:L19-L19`：fn sys_read
    代码片段：`static long sys_read(long fd, void *buf, long n) {`
  - `user/src/syscall.rs:L24-L24`：fn sys_read
    代码片段：`pub fn sys_read(fd: usize, buffer: &mut [u8]) -> isize {`
- 文件路径重合：与 2021 kCore 在“设备驱动”维度出现同名文件源码路径 `crates/scarlet/src/fs/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `crates/scarlet/src/fs/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console vnode backing file descriptors 0 (stdin), 1 (stdout), 2 (stderr). //! //! Reads blocks on the UART until a byte arrives. Writes output each byte`
  - `os/src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
- 文件路径重合：与 2021 kCore 在“设备驱动”维度出现同名文件源码路径 `crates/scarlet/src/util/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `crates/scarlet/src/util/console.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::{self, Write}; struct Console; impl Write for Console {`
  - `os/src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`

## 相似点

- 与 2021 kCore 同属 ucore-variant 风格。（置信度：medium）
- 与 2021 kCore 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/fs/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console vnode backing file descriptors 0 (stdin), 1 (stdout), 2 (stderr). //! //! Reads blocks on the UART until a byte arrives. Writes output each byte`
  - `crates/scarlet/src/util/console.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::{self, Write}; struct Console; impl Write for Console {`
- 与 2021 kCore 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/fs/fat.rs:L1-L1`：关键词命中
    代码片段：`// FAT filesystem Vnode integration — TODO`
  - `crates/scarlet/src/fs/ext4.rs:L35-L39`：关键词命中
    代码片段：`#[derive(Clone)] struct CachedDirEntry { inode: u32, mode: u16, }`
- 与 2021 kCore 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap infrastructure: initialization, local exception handlers, and //! re-exports of public types.`
  - `crates/scarlet/src/irq/mod.rs:L1-L3`：关键词命中
    代码片段：`//! IRQ subsystem: IrqChip trait, per-IRQ descriptors, domain registration. //! //! Provides a Linux-style IRQ subsystem with hierarchical domains,`
- 与 2021 kCore 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/mm/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Memory management: physical frames, kernel heap, page tables, and //! address-space mapping helpers.`
  - `crates/scarlet/src/mm/heap.rs:L39-L43`：关键词命中
    代码片段：`static HEAP: SyncHeap = SyncHeap(SpinMutex::new(KernelHeap::new())); static HEAP_READY: AtomicBool = AtomicBool::new(false); /// The global allocator instance.`
- 与 2021 kCore 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/syscall/sched.rs:L1-L3`：关键词命中
    代码片段：`//! Scheduler and process-lifecycle syscalls: clone, execve, waitid, wait4, //! set_tid_address, rseq, futex, set_robust_list, get_robust_list.`
  - `crates/scarlet/src/fs/procfs/task_dir.rs:L99-L103`：关键词命中
    代码片段：`// --------------------------------------------------------------------------- // ProcFdEntry — hybrid vnode for /proc/<pid>/fd/<n> entries. // Delegates I/O to the backing open...`
- 与 2021 kCore 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/sync/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! Provides ticket-based spinlocks with IRQ-safe variants, //! replacing the external 'spin::Mutex' crate. pub mod tlb_shootdown;`
  - `crates/scarlet/src/sync/tlb_shootdown.rs:L3-L7`：关键词命中
    代码片段：`use crate::arch; use crate::smp::{self, HartMask, SmpError}; use core::sync::atomic::{AtomicUsize, Ordering}; const MAX_HARTS: usize = 8;`
- 与 2021 kCore 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/syscall/fd.rs:L2-L6`：关键词命中
    代码片段：`//! stat, getdents, getcwd, chdir, dup, dup3, fcntl, ioctl. use crate::arch::shared::syscall::SyscallRequest; use crate::fs; use crate::process;`
  - `crates/scarlet/src/syscall/io.rs:L8-L12`：关键词命中
    代码片段：`//! blocking console reads don't stall unrelated process operations. use crate::arch::shared::syscall::SyscallRequest; use crate::fs::FsError; use crate::fs::file_table::OpenFile;`
- 与 2021 kCore 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2021 kCore 的语言构成不同：待测作品为 {'toml': 262, 'json': 178, 'markdown': 7088, 'build': 2929, 'rust': 302446, 'c': 3163, 'yaml': 50}，历史样本为 {'json': 87, 'build': 3692, 'markdown': 16369, 'toml': 60, 'rust': 452565, 'yaml': 92, 'c': 48, 'asm': 572, 'text': 1871}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：18
- 含证据关键结论数：18（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
