# 2026 T202610486999845 比较报告

- 对比历史仓库：2025 Chronix
- 生成时间：2026-07-02T05:02:02.163559+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 Chronix（来源：赛事历史作品）：画像相似度 score=9.46；同属 ucore-variant 风格; 语言构成相似度 0.94; OS 维度重合度 1.00; 代码规模接近度 0.58

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 Chronix 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use crate::sbi::console_putchar;`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device-driver entry points used by the current contest kernel. //! //! Drivers expose small, capability-oriented APIs to the rest of the kernel so`
  - `os/src/drivers/mod.rs:L2-L5`：关键词命中
    代码片段：`pub mod dma; pub mod net; pub mod serial; pub use block::BLOCK_DEVICE;`
  - `os/src/drivers/net/mod.rs:L1-L4`：关键词命中
    代码片段：`#[allow(unused)] pub mod virtio_net; pub mod loopback; use core::{mem, ptr::NonNull};`
- 与 2025 Chronix 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/ext4.rs:L23-L27`：关键词命中
    代码片段：`const MAX_BLOCK_SIZE: usize = 4096; /// EXT4 root inode number. pub(crate) const ROOT_INODE: u32 = 2;`
  - `os-la/src/fs/ext4.rs:L23-L27`：关键词命中
    代码片段：`const MAX_BLOCK_SIZE: usize = 4096; /// EXT4 root inode number. pub(crate) const ROOT_INODE: u32 = 2;`
  - `os/src/fs/fs.rs:L4-L8`：关键词命中
    代码片段：`use alloc::sync::{Arc, Weak}; use crate::{fs::{tmpfs::{dentry::TmpDentry, inode::{InodeContent, TmpInode, TmpSysInode}}, vfs::{dentry::{global_find_dentry, global_update_dentry,...`
  - `os/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`//! file system module: offer the file system interface //! define the file trait //! impl File for OSInode in 'inode.rs' //! impl Stdin and Stdout in 'stdio.rs' #![allow(missin...`
- 与 2025 Chronix 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V trap handling for user-mode contest programs. mod context;`
  - `os/src/trap/trap.S:L41-L45`：关键词命中
    代码片段：`__restore: # case1: start running app by __restore # case2: back to U after handling trap mv sp, a0 # now sp->kernel stack(after allocated), sscratch->user stack`
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling functionality //! //! For rCore, we have a single trap entry point, namely '__alltraps'. At`
  - `os/src/timer/ffi.rs:L1-L4`：关键词命中
    代码片段：`use alloc::collections::btree_map::Values; use crate::timer::get_current_time_ns; use super::{get_current_time_ms, NSEC_PER_SEC};`
- 与 2025 Chronix 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/arch.rs:L62-L66`：关键词命中
    代码片段：`} /// Write 'satp'. #[inline] pub unsafe fn write_satp(bits: usize) {`
  - `os/src/task/user.rs:L15-L19`：关键词命中
    代码片段：`const SATP_MODE_SV39: usize = 8usize << 60; const PTE_V: u64 = 1 << 0; const PTE_R: u64 = 1 << 1; const PTE_W: u64 = 1 << 2;`
  - `os/src/mm/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Memory management implementation //! //! SV39 page-based virtual-memory architecture for RV64 systems, and //! everything about memory management, like frame allocator, page...`
  - `os/src/mm/user.rs:L2-L6`：关键词命中
    代码片段：`use alloc::sync::Arc; use hal::{addr::{VirtAddr, VirtAddrHal}, constant::{Constant, ConstantsHal}, pagetable::MapPerm, println}; use crate::{mm::vm::UserVmPagesLocker, processor...`
- 与 2025 Chronix 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User-task runtime services. //! //! This layer groups address-space setup, user-stack construction, and the`
  - `os/src/task/user.rs:L1-L5`：关键词命中
    代码片段：`//! Minimal user address space and enter/return path for contest ELFs. //! //! The 'task' layer owns user-memory setup and the supervisor-to-user handoff, //! keeping execution...`
  - `os/src/task/fs.rs:L1-L3`：关键词命中
    代码片段：`//! file system support for Task use alloc::{sync::Arc, vec::Vec};`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Task management implementation //! //! Everything about task management, like starting and switching tasks is`
- 与 2025 Chronix 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/user.rs:L9-L13`：关键词命中
    代码片段：`use core::arch::{asm, global_asm}; use core::mem::size_of; use core::sync::atomic::{Ordering, compiler_fence}; const PAGE_SIZE: usize = 4096;`
  - `os/src/syscall/fs.rs:L1387-L1391`：关键词命中
    代码片段：`used: bool, counter: u64, semaphore: bool, }`
  - `os/src/sync/up.rs:L2-L6`：关键词命中
    代码片段：`use core::cell::{UnsafeCell, RefMut,RefCell}; use core::sync::atomic::{AtomicBool, Ordering}; use core::ops::{Deref, DerefMut}; use log::info;`
  - `os/src/sync/mod.rs:L6-L10`：关键词命中
    代码片段：`extern crate alloc; /// safe cell type for uniprocessor systems pub mod mutex; pub mod lazy;`
- 与 2025 Chronix 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File, directory, and fd-table syscall subset. //! //! The basic tests run one user program at a time, so a compact global runtime`
  - `os/src/syscall/mod.rs:L369-L373`：关键词命中
    代码片段：`}; /// Result of handling one syscall. pub enum SyscallResult { /// Return this value to user register 'a0'.`
  - `os/src/syscall/fd.rs:L1-L5`：关键词命中
    代码片段：`use alloc::sync::Arc; use crate::{fs::{tmpfs::{dentry::TmpDentry, inode::{EmptyFile, TmpSysInode}}, vfs::inode::InodeMode, OpenFlags}, syscall::{SysError, SysResult, SyscallId},...`
  - `os/src/syscall/fs.rs:L17-L21`：关键词命中
    代码片段：`use crate::processor::processor::{current_processor,current_task,current_user_token}; /// syscall: write pub async fn sys_write(fd: usize, buf: usize, len: usize) -> SysResult {...`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 Chronix 在“同步机制”维度发现片段级代码相似度 0.89 （token=0.83, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：inner, pub, struct, t, upsafecell。（置信度：high）
  证据：
  - `os/src/sync/up.rs:L12-L16`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>, // RefCell 是 Rust 标准库提供的一种智能指针类型，用...`
  - `os/src/sync/up.rs:L13-L17`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: UnsafeCell<T>,`
- 与 2025 Chronix 在“同步机制”维度发现片段级代码相似度 0.89 （token=0.82, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, inner, new, pub, self, t, value。（置信度：high）
  证据：
  - `os/src/sync/up.rs:L23-L27`：fn new
    代码片段：`/// uniprocessor. /// 创建一个新的 UPSafeCell 实例 pub unsafe fn new(value: T) -> Self { Self { inner: RefCell::new(value),`
  - `os/src/sync/up.rs:L23-L27`：fn new
    代码片段：`/// User is responsible to guarantee that inner struct is only used in /// uniprocessor. pub fn new(value: T) -> Self { Self { inner: UnsafeCell::new(value),`
- 函数/符号名重合：与 2025 Chronix 在“系统调用”维度发现 4 个同名定义：sys_openat, sys_read, sys_write, sys_close。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `os-la/src/syscall/fs.rs:L2295-L2295`：fn sys_openat
    代码片段：`pub fn sys_openat(dirfd: usize, path_ptr: usize, flags: usize, mode: usize) -> isize {`
  - `user/src/syscall.rs:L82-L82`：fn sys_openat
    代码片段：`pub fn sys_openat(dirfd: isize, path: &str, flags: u32) -> isize {`
  - `os-la/src/syscall/fs.rs:L3085-L3085`：fn sys_read
    代码片段：`pub fn sys_read(fd: usize, buf: usize, len: usize) -> isize {`
  - `user/src/syscall.rs:L94-L94`：fn sys_read
    代码片段：`pub fn sys_read(fd: usize, buffer: &mut [u8]) -> isize {`
- 函数/符号名重合：与 2025 Chronix 在“设备驱动”维度发现 2 个同名定义：write_str, new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os-la/src/console.rs:L9-L9`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `user/src/console.rs:L11-L11`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `os-la/src/drivers/virtio.rs:L171-L171`：fn new
    代码片段：`pub(crate) fn new() -> Result<Self, &'static str> {`
  - `os/src/devices/buffer_cache.rs:L18-L18`：fn new
    代码片段：`pub fn new() -> Self {`
- 函数/符号名重合：与 2025 Chronix 在“同步机制”维度发现 2 个同名定义：read_block, new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os-la/src/fs/ext4.rs:L310-L310`：fn read_block
    代码片段：`fn read_block(&mut self, block_num: u64, buf: &mut [u8]) -> Result<(), &'static str> {`
  - `os/src/devices/buffer_cache.rs:L79-L79`：fn read_block
    代码片段：`pub fn read_block(&self, block_id: usize, buf: &mut [u8]) {`
  - `os-la/src/sync/up.rs:L25-L25`：fn new
    代码片段：`pub unsafe fn new(value: T) -> Self {`
  - `os/src/fs/pipe.rs:L49-L49`：fn new
    代码片段：`fn new(pipe: Arc<SpinNoIrqLock<Pipe>>, is_reader: bool) -> Arc<Self> {`
- 结构体/类型重合：与 2025 Chronix 在“设备驱动”维度发现 1 个同名定义：Stdout。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os-la/src/console.rs:L6-L6`：struct Stdout
    代码片段：`struct Stdout;`
  - `user/src/console.rs:L8-L8`：struct Stdout
    代码片段：`struct Stdout;`
- 结构体/类型重合：与 2025 Chronix 在“文件系统”维度发现 1 个同名定义：Pipe。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os-la/src/syscall/fs.rs:L1055-L1055`：struct Pipe
    代码片段：`struct Pipe {`
  - `os/src/fs/pipe.rs:L18-L18`：struct Pipe
    代码片段：`pub struct Pipe {`
- 文件路径重合：与 2025 Chronix 在“同步机制”维度出现同路径源码路径 `os/src/sync/up.rs` / `os/src/sync/up.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/sync/up.rs:L12-L16`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>, // RefCell 是 Rust 标准库提供的一种智能指针类型，用...`
  - `os/src/sync/up.rs:L2-L6`：关键词命中
    代码片段：`use core::cell::{UnsafeCell, RefMut,RefCell}; use core::sync::atomic::{AtomicBool, Ordering}; use core::ops::{Deref, DerefMut}; use log::info;`
- 文件路径重合：与 2025 Chronix 在“系统调用”维度出现同路径源码路径 `os/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File, directory, and fd-table syscall subset. //! //! The basic tests run one user program at a time, so a compact global runtime`
  - `os/src/syscall/fs.rs:L17-L21`：关键词命中
    代码片段：`use crate::processor::processor::{current_processor,current_task,current_user_token}; /// syscall: write pub async fn sys_write(fd: usize, buf: usize, len: usize) -> SysResult {...`
- 文件路径重合：与 2025 Chronix 在“文件系统”维度出现同名文件源码路径 `os/src/syscall/fs.rs` / `os/src/fs/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L798-L802`：关键词命中
    代码片段：`struct FdEntry { kind: u8, inode: u32, mem_index: usize, shared_offset: usize,`
  - `os/src/fs/fs.rs:L4-L8`：关键词命中
    代码片段：`use alloc::sync::{Arc, Weak}; use crate::{fs::{tmpfs::{dentry::TmpDentry, inode::{InodeContent, TmpInode, TmpSysInode}}, vfs::{dentry::{global_find_dentry, global_update_dentry,...`
- 文件路径重合：与 2025 Chronix 在“内存管理”维度出现同名文件源码路径 `os/src/task/user.rs` / `os/src/mm/user.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/task/user.rs:L15-L19`：关键词命中
    代码片段：`const SATP_MODE_SV39: usize = 8usize << 60; const PTE_V: u64 = 1 << 0; const PTE_R: u64 = 1 << 1; const PTE_W: u64 = 1 << 2;`
  - `os/src/mm/user.rs:L2-L6`：关键词命中
    代码片段：`use alloc::sync::Arc; use hal::{addr::{VirtAddr, VirtAddrHal}, constant::{Constant, ConstantsHal}, pagetable::MapPerm, println}; use crate::{mm::vm::UserVmPagesLocker, processor...`
- 文件路径重合：与 2025 Chronix 在“内存管理”维度出现同名文件源码路径 `os-la/src/task/user.rs` / `os/src/mm/user.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os-la/src/task/user.rs:L136-L140`：struct PageTable
    代码片段：`#[repr(align(4096))] #[derive(Clone, Copy)] struct PageTable { entries: [u64; 512], }`
  - `os/src/mm/user.rs:L2-L6`：关键词命中
    代码片段：`use alloc::sync::Arc; use hal::{addr::{VirtAddr, VirtAddrHal}, constant::{Constant, ConstantsHal}, pagetable::MapPerm, println}; use crate::{mm::vm::UserVmPagesLocker, processor...`
- 文件路径重合：与 2025 Chronix 在“系统调用”维度出现同名文件源码路径 `os-la/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os-la/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File, directory, and fd-table syscall subset. //! //! The basic tests run one user program at a time, so a compact global runtime`
  - `os/src/syscall/fs.rs:L17-L21`：关键词命中
    代码片段：`use crate::processor::processor::{current_processor,current_task,current_user_token}; /// syscall: write pub async fn sys_write(fd: usize, buf: usize, len: usize) -> SysResult {...`

## 相似点

- 与 2025 Chronix 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 Chronix 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use crate::sbi::console_putchar;`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Device-driver entry points used by the current contest kernel. //! //! Drivers expose small, capability-oriented APIs to the rest of the kernel so`
- 与 2025 Chronix 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/ext4.rs:L23-L27`：关键词命中
    代码片段：`const MAX_BLOCK_SIZE: usize = 4096; /// EXT4 root inode number. pub(crate) const ROOT_INODE: u32 = 2;`
  - `os-la/src/fs/ext4.rs:L23-L27`：关键词命中
    代码片段：`const MAX_BLOCK_SIZE: usize = 4096; /// EXT4 root inode number. pub(crate) const ROOT_INODE: u32 = 2;`
- 与 2025 Chronix 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V trap handling for user-mode contest programs. mod context;`
  - `os/src/trap/trap.S:L41-L45`：关键词命中
    代码片段：`__restore: # case1: start running app by __restore # case2: back to U after handling trap mv sp, a0 # now sp->kernel stack(after allocated), sscratch->user stack`
- 与 2025 Chronix 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/arch.rs:L62-L66`：关键词命中
    代码片段：`} /// Write 'satp'. #[inline] pub unsafe fn write_satp(bits: usize) {`
  - `os/src/task/user.rs:L15-L19`：关键词命中
    代码片段：`const SATP_MODE_SV39: usize = 8usize << 60; const PTE_V: u64 = 1 << 0; const PTE_R: u64 = 1 << 1; const PTE_W: u64 = 1 << 2;`
- 与 2025 Chronix 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! User-task runtime services. //! //! This layer groups address-space setup, user-stack construction, and the`
  - `os/src/task/user.rs:L1-L5`：关键词命中
    代码片段：`//! Minimal user address space and enter/return path for contest ELFs. //! //! The 'task' layer owns user-memory setup and the supervisor-to-user handoff, //! keeping execution...`
- 与 2025 Chronix 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/user.rs:L9-L13`：关键词命中
    代码片段：`use core::arch::{asm, global_asm}; use core::mem::size_of; use core::sync::atomic::{Ordering, compiler_fence}; const PAGE_SIZE: usize = 4096;`
  - `os/src/syscall/fs.rs:L1387-L1391`：关键词命中
    代码片段：`used: bool, counter: u64, semaphore: bool, }`
- 与 2025 Chronix 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File, directory, and fd-table syscall subset. //! //! The basic tests run one user program at a time, so a compact global runtime`
  - `os/src/syscall/mod.rs:L369-L373`：关键词命中
    代码片段：`}; /// Result of handling one syscall. pub enum SyscallResult { /// Return this value to user register 'a0'.`
- 与 2025 Chronix 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 Chronix 的语言构成不同：待测作品为 {'json': 21, 'markdown': 205, 'build': 593, 'toml': 23, 'rust': 96864, 'asm': 221}，历史样本为 {'json': 20, 'build': 353, 'markdown': 2055, 'toml': 139, 'make': 405, 'rust': 43626, 'asm': 381}。（置信度：medium）

## 可能创新点

- 待测作品包含 437 个历史样本 2025 Chronix 中未见的独特函数/类型定义（如 `load_vaddr_range`, `emit_netperf_case`, `iperf_case_profile`, `ltp_aiodio_storage_compat_bridge_case`, `lua_case_exists`, `open_fifo_mem_file`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `os-la/src/loader/elf.rs:L776-L776`：fn load_vaddr_range
    代码片段：`fn load_vaddr_range(program_headers: &[u8], program_header_count: usize) -> Option<(usize, usize)> {`
  - `os-la/src/platform/contest/mod.rs:L11134-L11134`：fn emit_netperf_case
    代码片段：`fn emit_netperf_case(name: &str, mode: NetperfMode, msg_size: &str, reply_size: &str, rate: &str) {`

## 附录：核验摘要

- 关键结论数：28
- 含证据关键结论数：28（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
