# 2026 T202610574999903 比较报告

- 对比历史仓库：2025 BlueStar
- 生成时间：2026-07-02T05:02:57.956739+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 BlueStar（来源：赛事历史作品）：画像相似度 score=9.67；同属 ucore-variant 风格; 语言构成相似度 0.94; OS 维度重合度 1.00; 代码规模接近度 0.79

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 BlueStar 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/console.rs:L1-L3`：关键词命中
    代码片段：`// my-os/src/console.rs //封装为print！和println！宏 use crate::sbi::console_putchar;`
  - `kernel/src/console.rs:L8-L12`：关键词命中
    代码片段：`#[macro_export] /// Kernel print function,it can output to serial macro_rules! kprint { ($fmt: literal $(, $($arg: tt)+)?) => {`
  - `kernel/src/driver/mod.rs:L1-L2`：关键词命中
    代码片段：`mod virtio_blk; pub use self::virtio_blk::*;`
- 与 2025 BlueStar 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/cc/src/flags.rs:L327-L331`：关键词命中
    代码片段：`// to compile the c/c++ libraries because llvm linker plugin/lld uses thin LTO by default. // And for thin LTO in linker plugin to work, the archive also has to be compiled usin...`
  - `vendor/futures-task/src/future_obj.rs:L140-L144`：关键词命中
    代码片段：`/// implementor must provide. pub unsafe trait UnsafeFutureObj<'a, T>: 'a { /// Convert an owned instance into a (conceptually owned) fat pointer. /// /// # Safety`
  - `kernel/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`pub mod vfs; pub mod fs_backend; pub mod component;`
  - `kernel/src/fs/vfs/api.rs:L5-L9`：关键词命中
    代码片段：`use log::error; use crate::task::{TASK_MANAER, TASK_MANAGER_INIT}; use crate::fs::vfs::{File, KStat, MountFs, OpenFlags, ROOTFS,  VfsFsError, VfsStat}; use alloc::format; use al...`
- 与 2025 BlueStar 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/trap/mod.rs:L7-L11`：关键词命中
    代码片段：`global_asm!(include_str!("../entry.asm")); /// 初始化 Trap 处理 pub fn init() { extern "C" {`
  - `src/trap/context.rs:L8-L12`：关键词命中
    代码片段：`/// Supervisor Status Register pub sstatus: usize, /// Supervisor Exception Program Counter pub sepc: usize, /// 内核页表地址（暂未使用）`
  - `kernel/src/trap/mod.rs:L4-L8`：关键词命中
    代码片段：`use crate::{config::*, task::TASK_MANAER, time::set_next_time_interupt}; use log::{debug, error, }; use riscv::register::{scause::{self, Exception, Trap},  sstatus::{self, SPP,...`
  - `kernel/src/time/timer.rs:L27-L31`：关键词命中
    代码片段：`///设置下一次时钟中断(不带中断检查，太耗时间，所有耗时操作其实都不应该出现在这里)，mtimecmp使用原始tick计数 pub fn set_next_time_interupt(){ //需要考虑调用误差，即使错过也没事，只是提前触发中断(mtime < mtimecmp)`
- 与 2025 BlueStar 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/buddy_system_allocator/benches/memory_allocator_benchmark.rs:L144-L148`：关键词命中
    代码片段：`/// TODO: reduce memory use const KERNEL_HEAP_SIZE: usize = 128 * 1024 * 1024; const HEAP_BLOCK: usize = KERNEL_HEAP_SIZE / MACHINE_ALIGN; static mut HEAP: [usize; HEAP_BLOCK] =...`
  - `vendor/cc/src/lib.rs:L241-L245`：关键词命中
    代码片段：`#![warn(clippy::doc_markdown)] use std::borrow::Cow; use std::collections::HashMap; use std::env;`
  - `kernel/src/memory/mod.rs:L2-L6`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod memset;`
  - `kernel/src/memory/memset.rs:L6-L10`：关键词命中
    代码片段：`use log::{debug, error, trace}; use core::arch::asm; use riscv::register::satp; use crate::fs::vfs::File; use crate::task::{TASK_MANAER};`
- 与 2025 BlueStar 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/futures-core/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Task notification. #[macro_use]`
  - `vendor/futures-util/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`//! //! - ['Spawn'], a trait for spawning new tasks. //! - ['Context'], a context of an asynchronous task, //!   including a handle for waking up the task. //! - ['Waker'], a ha...`
  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`mod task; mod process; use crate::fs::vfs::{OpenFlags, vfs_open};`
  - `kernel/src/task/task.rs:L19-L23`：关键词命中
    代码片段：`use crate::memory::*; use crate::sbi::shutdown; use crate::task::Signal; use crate::task::file_loader; use log::debug;`
- 与 2025 BlueStar 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/futures-util/src/task/mod.rs:L33-L37`：关键词命中
    代码片段：`#[cfg_attr( target_os = "none", cfg(any(target_has_atomic = "ptr", feature = "portable-atomic")) )] pub use futures_core::task::__internal::AtomicWaker;`
  - `vendor/futures-core/src/task/__internal/mod.rs:L1-L3`：关键词命中
    代码片段：`#[cfg_attr(target_os = "none", cfg(any(target_has_atomic = "ptr", feature = "portable-atomic")))] mod atomic_waker; #[cfg_attr(`
  - `kernel/src/sync/up.rs:L19-L23`：关键词命中
    代码片段：`#[track_caller] pub fn lock(&self)->core::cell::RefMut<'_,T>{ match self.inner.try_borrow_mut() { Ok(g) => g,`
  - `kernel/vendor/spin/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! Locks that have the same behaviour as a mutex. //! //! The ['Mutex'] in the root of the crate, can be configured using the 'ticket_mutex' feature.`
- 与 2025 BlueStar 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/syscall/mod.rs:L12-L16`：关键词命中
    代码片段：`/// 系统调用分发 pub fn syscall(id: usize, args: [usize; 3]) -> isize { match id { SYS_WRITE => sys_write(args[0], args[1] as *const u8, args[2]),`
  - `vendor/sbi-rt/src/binary.rs:L8-L12`：关键词命中
    代码片段：`unsafe { core::arch::asm!( "ecall", in("a7") eid, in("a6") fid,`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`mod syscall; use log::{error}; use crate::memory::VirAddr;`
  - `kernel/src/syscall/syscall.rs:L1272-L1276`：关键词命中
    代码片段：`/// 返回：成功返回映射起始地址；失败返回 -1 /// /// 参数说明（Linux riscv64 ABI，用户态用 ecall 传参）： /// 'addr'  : 映射起始虚拟地址（用户 hint）。若带 'MAP_FIXED' 则必须使用该地址。 /// 'len'   : 映射长度（字节）。内核按页对齐到覆盖该区间。`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 BlueStar 在“中断与异常”维度发现片段级代码相似度 0.93 （token=0.89, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：c, pub, repr, struct, trapcontext, usize, x。（置信度：high）
  证据：
  - `src/trap/context.rs:L3-L7`：struct TrapContext
    代码片段：`/// 用户态和内核态之间切换时需要保存的 CPU 状态 #[repr(C)] pub struct TrapContext { /// 通用寄存器 x0-x31（x0 硬连线为 0，占位） pub x: [usize; 32],`
  - `kernel/src/trap/mod.rs:L17-L21`：struct TrapContext
    代码片段：`#[repr(C)] #[repr(align(8))]  // 确保 8 字节对齐 pub struct TrapContext{ ///32个寄存器完全保存 pub x:[usize;32],`
- 与 2025 BlueStar 在“同步机制”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：inner, innermutex, mutex, pub, r, sized, spin, struct。（置信度：high）
  证据：
  - `vendor/spin/src/mutex.rs:L111-L115`：struct Mutex
    代码片段：`/// # } /// ''' pub struct Mutex<T: ?Sized, R = Spin> { inner: InnerMutex<T, R>, }`
  - `kernel/vendor/spin/src/mutex.rs:L111-L115`：struct Mutex
    代码片段：`/// # } /// ''' pub struct Mutex<T: ?Sized, R = Spin> { inner: InnerMutex<T, R>, }`
- 与 2025 BlueStar 在“设备驱动”维度发现片段级代码相似度 0.69 （token=0.58, structure=0.89）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：arguments, fmt, fn, pub。（置信度：medium）
  证据：
  - `src/console.rs:L15-L19`：fn print
    代码片段：`} pub fn print(args: fmt::Arguments) { Stdout.write_fmt(args).unwrap(); }`
  - `kernel/src/console.rs:L3-L7`：fn kprint
    代码片段：`/// 内核打印函数,直接输出到当前任务缓冲区？？，不对吧。 pub fn kprint(fmt: fmt::Arguments) { driver_print(fmt); }`
- 与 2025 BlueStar 在“同步机制”维度发现片段级代码相似度 0.61 （token=0.52, structure=0.78）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：inner, pub, sized, struct, t。（置信度：medium）
  证据：
  - `vendor/spin/src/mutex.rs:L125-L129`：struct MutexGuard
    代码片段：`/// ['TicketMutexGuard']: ./struct.TicketMutexGuard.html /// ['SpinMutexGuard']: ./struct.SpinMutexGuard.html pub struct MutexGuard<'a, T: 'a + ?Sized> { inner: InnerMutexGuard<...`
  - `kernel/vendor/spin/src/mutex.rs:L111-L115`：struct Mutex
    代码片段：`/// # } /// ''' pub struct Mutex<T: ?Sized, R = Spin> { inner: InnerMutex<T, R>, }`
- 函数/符号名重合：与 2025 BlueStar 在“系统调用”维度发现 4 个同名定义：sys_write, sys_read, sys_exit, sys_getpid。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src/syscall/fs.rs:L4-L4`：fn sys_write
    代码片段：`pub fn sys_write(fd: usize, buf: *const u8, len: usize) -> isize {`
  - `user/src/syscall.rs:L137-L137`：fn sys_write
    代码片段：`pub fn sys_write(fd:usize,buffer_ptr:usize,buffer_len:usize)->isize{`
  - `src/syscall/fs.rs:L17-L17`：fn sys_read
    代码片段：`pub fn sys_read(_fd: usize, _buf: *mut u8, _len: usize) -> isize {`
  - `user/src/syscall.rs:L133-L133`：fn sys_read
    代码片段：`pub fn sys_read(fd:usize,buffer_ptr:usize,buffer_len:usize)->isize{`
- 与 2025 BlueStar 在“系统调用”维度发现片段级代码相似度 0.77 （token=0.68, structure=0.93）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, isize, match, pub, sys_write, usize。（置信度：medium）
  证据：
  - `src/syscall/mod.rs:L12-L16`：关键词命中
    代码片段：`/// 系统调用分发 pub fn syscall(id: usize, args: [usize; 3]) -> isize { match id { SYS_WRITE => sys_write(args[0], args[1] as *const u8, args[2]),`
  - `kernel/src/syscall/mod.rs:L40-L44`：fn syscall_handler
    代码片段：`///args:接受1个usize参数 ///返回值：通过 x10 (a0) 寄存器返回给用户态 pub fn syscall_handler(id:usize,arg:[usize;6]) -> isize { match id { SYS_WRITE => sys_write(arg[0], arg[1], arg[2]),`
- 函数/符号名重合：与 2025 BlueStar 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/console.rs:L9-L9`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `user/src/console.rs:L44-L44`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> core::fmt::Result {`
- 函数/符号名重合：与 2025 BlueStar 在“调度与任务管理”维度发现 1 个同名定义：sys_yield。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/syscall/process.rs:L12-L12`：fn sys_yield
    代码片段：`pub fn sys_yield() -> isize {`
  - `user/src/syscall.rs:L338-L338`：fn sys_yield
    代码片段：`pub fn sys_yield(){`
- 函数/符号名重合：与 2025 BlueStar 在“同步机制”维度发现 1 个同名定义：deref。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `vendor/buddy_system_allocator/src/frame.rs:L203-L203`：fn deref
    代码片段：`fn deref(&self) -> &Mutex<FrameAllocator<ORDER>> {`
  - `user/vendor/spin-0.7.1/src/mutex.rs:L281-L281`：fn deref
    代码片段：`fn deref(&self) -> &T {`
- 结构体/类型重合：与 2025 BlueStar 在“设备驱动”维度发现 1 个同名定义：Stdout。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/console.rs:L6-L6`：struct Stdout
    代码片段：`struct Stdout;`
  - `user/src/console.rs:L12-L12`：struct STDOUT
    代码片段：`struct STDOUT;`
- 文件路径重合：与 2025 BlueStar 在“设备驱动”维度出现同名文件源码路径 `src/console.rs` / `kernel/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/console.rs:L1-L3`：关键词命中
    代码片段：`// my-os/src/console.rs //封装为print！和println！宏 use crate::sbi::console_putchar;`
  - `kernel/src/console.rs:L8-L12`：关键词命中
    代码片段：`#[macro_export] /// Kernel print function,it can output to serial macro_rules! kprint { ($fmt: literal $(, $($arg: tt)+)?) => {`
- 文件路径重合：与 2025 BlueStar 在“同步机制”维度出现同名文件源码路径 `vendor/spin/src/mutex.rs` / `kernel/vendor/spin/src/mutex.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `vendor/spin/src/mutex.rs:L111-L115`：struct Mutex
    代码片段：`/// # } /// ''' pub struct Mutex<T: ?Sized, R = Spin> { inner: InnerMutex<T, R>, }`
  - `kernel/vendor/spin/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! Locks that have the same behaviour as a mutex. //! //! The ['Mutex'] in the root of the crate, can be configured using the 'ticket_mutex' feature.`

## 相似点

- 与 2025 BlueStar 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 BlueStar 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/console.rs:L1-L3`：关键词命中
    代码片段：`// my-os/src/console.rs //封装为print！和println！宏 use crate::sbi::console_putchar;`
  - `kernel/src/console.rs:L8-L12`：关键词命中
    代码片段：`#[macro_export] /// Kernel print function,it can output to serial macro_rules! kprint { ($fmt: literal $(, $($arg: tt)+)?) => {`
- 与 2025 BlueStar 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/cc/src/flags.rs:L327-L331`：关键词命中
    代码片段：`// to compile the c/c++ libraries because llvm linker plugin/lld uses thin LTO by default. // And for thin LTO in linker plugin to work, the archive also has to be compiled usin...`
  - `vendor/futures-task/src/future_obj.rs:L140-L144`：关键词命中
    代码片段：`/// implementor must provide. pub unsafe trait UnsafeFutureObj<'a, T>: 'a { /// Convert an owned instance into a (conceptually owned) fat pointer. /// /// # Safety`
- 与 2025 BlueStar 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/trap/mod.rs:L7-L11`：关键词命中
    代码片段：`global_asm!(include_str!("../entry.asm")); /// 初始化 Trap 处理 pub fn init() { extern "C" {`
  - `src/trap/context.rs:L8-L12`：关键词命中
    代码片段：`/// Supervisor Status Register pub sstatus: usize, /// Supervisor Exception Program Counter pub sepc: usize, /// 内核页表地址（暂未使用）`
- 与 2025 BlueStar 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/buddy_system_allocator/benches/memory_allocator_benchmark.rs:L144-L148`：关键词命中
    代码片段：`/// TODO: reduce memory use const KERNEL_HEAP_SIZE: usize = 128 * 1024 * 1024; const HEAP_BLOCK: usize = KERNEL_HEAP_SIZE / MACHINE_ALIGN; static mut HEAP: [usize; HEAP_BLOCK] =...`
  - `vendor/cc/src/lib.rs:L241-L245`：关键词命中
    代码片段：`#![warn(clippy::doc_markdown)] use std::borrow::Cow; use std::collections::HashMap; use std::env;`
- 与 2025 BlueStar 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/futures-core/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Task notification. #[macro_use]`
  - `vendor/futures-util/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`//! //! - ['Spawn'], a trait for spawning new tasks. //! - ['Context'], a context of an asynchronous task, //!   including a handle for waking up the task. //! - ['Waker'], a ha...`
- 与 2025 BlueStar 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/futures-util/src/task/mod.rs:L33-L37`：关键词命中
    代码片段：`#[cfg_attr( target_os = "none", cfg(any(target_has_atomic = "ptr", feature = "portable-atomic")) )] pub use futures_core::task::__internal::AtomicWaker;`
  - `vendor/futures-core/src/task/__internal/mod.rs:L1-L3`：关键词命中
    代码片段：`#[cfg_attr(target_os = "none", cfg(any(target_has_atomic = "ptr", feature = "portable-atomic")))] mod atomic_waker; #[cfg_attr(`
- 与 2025 BlueStar 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/syscall/mod.rs:L12-L16`：关键词命中
    代码片段：`/// 系统调用分发 pub fn syscall(id: usize, args: [usize; 3]) -> isize { match id { SYS_WRITE => sys_write(args[0], args[1] as *const u8, args[2]),`
  - `vendor/sbi-rt/src/binary.rs:L8-L12`：关键词命中
    代码片段：`unsafe { core::arch::asm!( "ecall", in("a7") eid, in("a6") fid,`
- 与 2025 BlueStar 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 BlueStar 的语言构成不同：待测作品为 {'markdown': 11121, 'build': 4763, 'toml': 59, 'rust': 387000, 'asm': 185, 'json': 1697, 'c': 15, 'text': 244, 'cpp': 67}，历史样本为 {'json': 53, 'build': 2684, 'markdown': 10844, 'toml': 9593, 'c': 336, 'rust': 279800, 'asm': 641, 'text': 5383, 'yaml': 51}。（置信度：medium）

## 可能创新点

- 待测作品包含 276 个历史样本 2025 BlueStar 中未见的独特函数/类型定义（如 `property_callback`, `from_iter_in`, `from_utf16_in`, `dealloc_array`, `LockedFrameAllocator`, `mutil_thread_random_size`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `vendor/android_system_properties/src/lib.rs:L47-L47`：fn property_callback
    代码片段：`unsafe fn property_callback(payload: *mut String, _name: *const c_char, value: *const c_char, _serial: u32) {`
  - `vendor/bumpalo/src/collections/string.rs:L712-L712`：fn from_iter_in
    代码片段：`pub fn from_iter_in<I: IntoIterator<Item = char>>(iter: I, bump: &'bump Bump) -> String<'bump> {`

## 附录：核验摘要

- 关键结论数：27
- 含证据关键结论数：27（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
