# 2026 T202610486999929 比较报告

- 对比历史仓库：2024 NPUcore-IMPACT!!!
- 生成时间：2026-07-02T09:03:05.506326+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 NPUcore-IMPACT!!!（来源：赛事历史作品）：画像相似度 score=9.56；同属 ucore-variant 风格; 语言构成相似度 0.96; OS 维度重合度 1.00; 代码规模接近度 0.64

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/fs/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console vnode backing file descriptors 0 (stdin), 1 (stdout), 2 (stderr). //! //! Reads blocks on the UART until a byte arrives. Writes output each byte`
  - `crates/scarlet/src/util/console.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::{self, Write}; struct Console; impl Write for Console {`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod block; pub mod serial; pub use block::BLOCK_DEVICE;`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/fs/fat.rs:L1-L1`：关键词命中
    代码片段：`// FAT filesystem Vnode integration — TODO`
  - `crates/scarlet/src/fs/ext4.rs:L35-L39`：关键词命中
    代码片段：`#[derive(Clone)] struct CachedDirEntry { inode: u32, mode: u16, }`
  - `os/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod file_trait; mod filesystem; // mod inode; mod layout; pub mod poll;`
  - `os/src/fs/cache.rs:L193-L197`：关键词命中
    代码片段：`/// Every time kernel tried to alloc this pagecache this number will increase 1(at most 1) /// Every time out of memory occurred this number will decrease 1(at least 0) /// When...`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap infrastructure: initialization, local exception handlers, and //! re-exports of public types.`
  - `crates/scarlet/src/irq/mod.rs:L1-L3`：关键词命中
    代码片段：`//! IRQ subsystem: IrqChip trait, per-IRQ descriptors, domain registration. //! //! Provides a Linux-style IRQ subsystem with hierarchical domains,`
  - `os/src/timer.rs:L18-L22`：关键词命中
    代码片段：`pub fn get_time_sec() -> usize { let i = get_time() / (get_clock_freq()); //log::info!("[timer.rs] get_time(): {},sec: {}", get_time(), i); i }`
  - `os/src/main.rs:L36-L40`：关键词命中
    代码片段：`mod syscall; mod task; mod timer; // use crate::arch::config::DISK_IMAGE_BASE;`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/mm/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Memory management: physical frames, kernel heap, page tables, and //! address-space mapping helpers.`
  - `crates/scarlet/src/mm/heap.rs:L39-L43`：关键词命中
    代码片段：`static HEAP: SyncHeap = SyncHeap(SpinMutex::new(KernelHeap::new())); static HEAP_READY: AtomicBool = AtomicBool::new(false); /// The global allocator instance.`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod map_area;`
  - `os/src/mm/map_area.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::Debug; use super::page_table::PageTable; #[cfg(feature = "zram")] use super::zram::{ZramTracker, ZRAM_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/syscall/sched.rs:L1-L3`：关键词命中
    代码片段：`//! Scheduler and process-lifecycle syscalls: clone, execve, waitid, wait4, //! set_tid_address, rseq, futex, set_robust_list, get_robust_list.`
  - `crates/scarlet/src/fs/procfs/task_dir.rs:L99-L103`：关键词命中
    代码片段：`// --------------------------------------------------------------------------- // ProcFdEntry — hybrid vnode for /proc/<pid>/fd/<n> entries. // Delegates I/O to the backing open...`
  - `os/src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`mod processor; pub mod signal; mod task; pub mod threads;`
  - `os/src/task/task.rs:L380-L384`：关键词命中
    代码片段：`// destory all other threads manager .ready_queue .retain(|task| (*task).tgid != (*self).tgid); manager`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/sync/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! Provides ticket-based spinlocks with IRQ-safe variants, //! replacing the external 'spin::Mutex' crate. pub mod tlb_shootdown;`
  - `crates/scarlet/src/sync/tlb_shootdown.rs:L3-L7`：关键词命中
    代码片段：`use crate::arch; use crate::smp::{self, HartMask, SmpError}; use core::sync::atomic::{AtomicUsize, Ordering}; const MAX_HARTS: usize = 8;`
  - `os/src/fs/mod.rs:L35-L39`：关键词命中
    代码片段：`use lazy_static::*; use log::warn; use spin::Mutex; lazy_static! {`
  - `os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::{arch::BLOCK_SZ, config::PAGE_SIZE, drivers::BLOCK_DEVICE};`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/scarlet/src/syscall/fd.rs:L2-L6`：关键词命中
    代码片段：`//! stat, getdents, getcwd, chdir, dup, dup3, fcntl, ioctl. use crate::arch::shared::syscall::SyscallRequest; use crate::fs; use crate::process;`
  - `crates/scarlet/src/syscall/io.rs:L8-L12`：关键词命中
    代码片段：`//! blocking console reads don't stall unrelated process operations. use crate::arch::shared::syscall::SyscallRequest; use crate::fs::FsError; use crate::fs::file_table::OpenFile;`
  - `os/src/syscall/fs.rs:L548-L552`：关键词命中
    代码片段：`} // This syscall is not complete at all, only /read proc/self/exe pub fn sys_readlinkat(dirfd: usize, pathname: *const u8, buf: *mut u8, bufsiz: usize) -> isize { let task = cu...`
  - `os/src/syscall/mod.rs:L115-L119`：关键词命中
    代码片段：`use crate::{ fs::poll::FdSet, syscall::errno::Errno, task::Rusage, timer::{ITimerVal, TimeSpec, Times},`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度发现片段级代码相似度 0.74 （token=0.60, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, init, pub。（置信度：medium）
  证据：
  - `crates/scarlet/src/mm/mod.rs:L33-L37`：fn heap_init
    代码片段：`} pub fn heap_init() { heap::init(); }`
  - `os/src/mm/mod.rs:L26-L30`：fn init
    代码片段：`}; pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度发现片段级代码相似度 0.68 （token=0.50, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：alloc, arc, mutex, spin, sync, use, vec。（置信度：medium）
  证据：
  - `crates/scarlet/src/fs/mod.rs:L16-L20`：关键词命中
    代码片段：`use alloc::sync::Arc; use alloc::vec::Vec; use core::sync::atomic::{AtomicBool, AtomicPtr, Ordering}; use spin::Mutex;`
  - `os/src/fs/swap.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use spin::Mutex; use crate::{arch::BLOCK_SZ, config::PAGE_SIZE, drivers::BLOCK_DEVICE};`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/scarlet/ext4_rs_fixed/src/ext4_defs/direntry.rs:L66-L66`：fn new
    代码片段：`pub fn new(dentry: Ext4DirEntry) -> Self {`
  - `os/src/fs/cache.rs:L81-L81`：fn new
    代码片段：`pub fn new(buffer_ptr: *mut [u8; BUFFER_SIZE]) -> Self {`
- 函数/符号名重合：与 2024 NPUcore-IMPACT!!! 在“系统调用”维度发现 2 个同名定义：sys_write, sys_read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/sakuya/tests/basic-lseek/main.c:L14-L14`：fn sys_write
    代码片段：`static long sys_write(long fd, const void *buf, long n) {`
  - `user/src/syscall.rs:L103-L103`：fn sys_write
    代码片段：`pub fn sys_write(fd: usize, buffer: &[u8]) -> isize {`
  - `crates/sakuya/tests/basic-lseek/main.c:L19-L19`：fn sys_read
    代码片段：`static long sys_read(long fd, void *buf, long n) {`
  - `user/src/syscall.rs:L96-L96`：fn sys_read
    代码片段：`pub fn sys_read(fd: usize, buffer: &mut [u8]) -> isize {`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度发现 1 个同名定义：BlockDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/scarlet/ext4_rs_fixed/src/ext4_defs/block.rs:L3-L3`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync + Any {`
  - `os/src/fs/mod.rs:L20-L20`：trait BlockDevice
    代码片段：`pub trait BlockDevice: crate::drivers::block::BlockDevice {}`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“文件系统”维度发现 1 个同名定义：BlockDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/scarlet/ext4_rs_fixed/src/ext4_defs/block.rs:L3-L3`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync + Any {`
  - `os/src/fs/mod.rs:L20-L20`：trait BlockDevice
    代码片段：`pub trait BlockDevice: crate::drivers::block::BlockDevice {}`
- 结构体/类型重合：与 2024 NPUcore-IMPACT!!! 在“同步机制”维度发现 1 个同名定义：BlockDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/scarlet/ext4_rs_fixed/src/ext4_defs/block.rs:L3-L3`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Send + Sync + Any {`
  - `os/src/fs/mod.rs:L20-L20`：trait BlockDevice
    代码片段：`pub trait BlockDevice: crate::drivers::block::BlockDevice {}`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度出现同名文件源码路径 `crates/scarlet/src/fs/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `crates/scarlet/src/fs/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console vnode backing file descriptors 0 (stdin), 1 (stdout), 2 (stderr). //! //! Reads blocks on the UART until a byte arrives. Writes output each byte`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
- 文件路径重合：与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度出现同名文件源码路径 `crates/scarlet/src/util/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `crates/scarlet/src/util/console.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::{self, Write}; struct Console; impl Write for Console {`
  - `os/src/console.rs:L34-L38`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`

## 相似点

- 与 2024 NPUcore-IMPACT!!! 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 NPUcore-IMPACT!!! 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/fs/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console vnode backing file descriptors 0 (stdin), 1 (stdout), 2 (stderr). //! //! Reads blocks on the UART until a byte arrives. Writes output each byte`
  - `crates/scarlet/src/util/console.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::{self, Write}; struct Console; impl Write for Console {`
- 与 2024 NPUcore-IMPACT!!! 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/fs/fat.rs:L1-L1`：关键词命中
    代码片段：`// FAT filesystem Vnode integration — TODO`
  - `crates/scarlet/src/fs/ext4.rs:L35-L39`：关键词命中
    代码片段：`#[derive(Clone)] struct CachedDirEntry { inode: u32, mode: u16, }`
- 与 2024 NPUcore-IMPACT!!! 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Trap infrastructure: initialization, local exception handlers, and //! re-exports of public types.`
  - `crates/scarlet/src/irq/mod.rs:L1-L3`：关键词命中
    代码片段：`//! IRQ subsystem: IrqChip trait, per-IRQ descriptors, domain registration. //! //! Provides a Linux-style IRQ subsystem with hierarchical domains,`
- 与 2024 NPUcore-IMPACT!!! 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/mm/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Memory management: physical frames, kernel heap, page tables, and //! address-space mapping helpers.`
  - `crates/scarlet/src/mm/heap.rs:L39-L43`：关键词命中
    代码片段：`static HEAP: SyncHeap = SyncHeap(SpinMutex::new(KernelHeap::new())); static HEAP_READY: AtomicBool = AtomicBool::new(false); /// The global allocator instance.`
- 与 2024 NPUcore-IMPACT!!! 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/syscall/sched.rs:L1-L3`：关键词命中
    代码片段：`//! Scheduler and process-lifecycle syscalls: clone, execve, waitid, wait4, //! set_tid_address, rseq, futex, set_robust_list, get_robust_list.`
  - `crates/scarlet/src/fs/procfs/task_dir.rs:L99-L103`：关键词命中
    代码片段：`// --------------------------------------------------------------------------- // ProcFdEntry — hybrid vnode for /proc/<pid>/fd/<n> entries. // Delegates I/O to the backing open...`
- 与 2024 NPUcore-IMPACT!!! 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/sync/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! Provides ticket-based spinlocks with IRQ-safe variants, //! replacing the external 'spin::Mutex' crate. pub mod tlb_shootdown;`
  - `crates/scarlet/src/sync/tlb_shootdown.rs:L3-L7`：关键词命中
    代码片段：`use crate::arch; use crate::smp::{self, HartMask, SmpError}; use core::sync::atomic::{AtomicUsize, Ordering}; const MAX_HARTS: usize = 8;`
- 与 2024 NPUcore-IMPACT!!! 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/scarlet/src/syscall/fd.rs:L2-L6`：关键词命中
    代码片段：`//! stat, getdents, getcwd, chdir, dup, dup3, fcntl, ioctl. use crate::arch::shared::syscall::SyscallRequest; use crate::fs; use crate::process;`
  - `crates/scarlet/src/syscall/io.rs:L8-L12`：关键词命中
    代码片段：`//! blocking console reads don't stall unrelated process operations. use crate::arch::shared::syscall::SyscallRequest; use crate::fs::FsError; use crate::fs::file_table::OpenFile;`
- 与 2024 NPUcore-IMPACT!!! 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 NPUcore-IMPACT!!! 的语言构成不同：待测作品为 {'toml': 262, 'json': 178, 'markdown': 7088, 'build': 2929, 'rust': 302446, 'c': 3163, 'yaml': 50}，历史样本为 {'json': 20, 'build': 338, 'markdown': 743, 'rust': 177629, 'asm': 10}。（置信度：medium）

## 可能创新点

- 待测作品包含 252 个历史样本 2024 NPUcore-IMPACT!!! 中未见的独特函数/类型定义（如 `sys_mount`, `sys_renameat2`, `skip_node`, `read_string`, `get_block_group_checksum`, `set_block_group_balloc_bitmap_csum`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `crates/sakuya/tests/fs-mount/main.c:L17-L17`：fn sys_mount
    代码片段：`static long sys_mount(const char *source, const char *target, const char *fstype, long flags, const void *data) {`
  - `crates/sakuya/tests/fs-rename/main.c:L18-L18`：fn sys_renameat2
    代码片段：`static long sys_renameat2(long odir, const char *opath, long ndir, const char *npath, long flags) {`

## 附录：核验摘要

- 关键结论数：24
- 含证据关键结论数：24（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
