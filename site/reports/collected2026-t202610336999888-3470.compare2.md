# 2026 T202610336999888 比较报告

- 对比历史仓库：2025 BlueStar
- 生成时间：2026-07-01T20:57:49.874872+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 BlueStar（来源：赛事历史作品）：画像相似度 score=9.42；同属 ucore-variant 风格; 语言构成相似度 0.91; OS 维度重合度 1.00; 代码规模接近度 0.60

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 BlueStar 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/drivers/axdriver/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! [ArceOS](https://github.com/arceos-org/arceos) device drivers. //! //! # Usage`
  - `kernel/drivers/axdisplay/src/lib.rs:L21-L25`：关键词命中
    代码片段：`info!("Initialize graphics subsystem..."); let dev = display_devs.take_one().expect("No graphics device found!"); info!("  use graphics device 0: {:?}", dev.device_name()); MAIN...`
  - `kernel/src/console.rs:L8-L12`：关键词命中
    代码片段：`#[macro_export] /// Kernel print function,it can output to serial macro_rules! kprint { ($fmt: literal $(, $($arg: tt)+)?) => {`
  - `kernel/src/driver/mod.rs:L1-L2`：关键词命中
    代码片段：`mod virtio_blk; pub use self::virtio_blk::*;`
- 与 2025 BlueStar 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/axfs/src/lib.rs:L5-L9`：关键词命中
    代码片段：`//! # Cargo Features //! //! - 'fatfs': Use [FAT] as the main filesystem and mount it on '/'. This feature //!   is **enabled** by default. //! - 'ext4fs': Use [ext4] as the mai...`
  - `kernel/fs/axfs/src/root.rs:L376-L380`：关键词命中
    代码片段：`#[derive(Debug, Clone, Copy, Eq, PartialEq)] enum RootFileSystemKind { Fat, Ext4, Unknown,`
  - `kernel/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`pub mod vfs; pub mod fs_backend; pub mod component;`
  - `kernel/src/fs/vfs/api.rs:L5-L9`：关键词命中
    代码片段：`use log::error; use crate::task::{TASK_MANAER, TASK_MANAGER_INIT}; use crate::fs::vfs::{File, KStat, MountFs, OpenFlags, ROOTFS,  VfsFsError, VfsStat}; use alloc::format; use al...`
- 与 2025 BlueStar 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/axcpu/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. #[cfg(feature = "uspace")]`
  - `kernel/arch/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use axcpu::trap::{IRQ, register_trap_handler};`
  - `kernel/src/trap/mod.rs:L4-L8`：关键词命中
    代码片段：`use crate::{config::*, task::TASK_MANAER, time::set_next_time_interupt}; use log::{debug, error, }; use riscv::register::{scause::{self, Exception, Trap},  sstatus::{self, SPP,...`
  - `kernel/src/time/timer.rs:L27-L31`：关键词命中
    代码片段：`///设置下一次时钟中断(不带中断检查，太耗时间，所有耗时操作其实都不应该出现在这里)，mtimecmp使用原始tick计数 pub fn set_next_time_interupt(){ //需要考虑调用误差，即使错过也没事，只是提前触发中断(mtime < mtimecmp)`
- 与 2025 BlueStar 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/memory/axmm/src/lib.rs:L78-L82`：关键词命中
    代码片段：`let mut aspace = AddrSpace::new_empty(base, size)?; if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use separate pag...`
  - `kernel/memory/axalloc/src/lib.rs:L203-L207`：关键词命中
    代码片段：`let init_heap_size = MIN_HEAP_SIZE; self.palloc.lock().init(start_vaddr, size); let heap_ptr = self .alloc_pages(init_heap_size / PAGE_SIZE, PAGE_SIZE) .unwrap();`
  - `kernel/src/memory/mod.rs:L2-L6`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod memset;`
  - `kernel/src/memory/memset.rs:L6-L10`：关键词命中
    代码片段：`use log::{debug, error, trace}; use core::arch::asm; use riscv::register::satp; use crate::fs::vfs::File; use crate::task::{TASK_MANAER};`
- 与 2025 BlueStar 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/axfs/src/root.rs:L473-L477`：关键词命中
    代码片段：`#[cfg(feature = "procfs")] root_dir // should not fail .mount("/proc", mounts::procfs().unwrap()) .expect("fail to mount procfs at /proc");`
  - `kernel/arch/axhal/src/lib.rs:L21-L25`：关键词命中
    代码片段：`//! - 'paging': Enable page table manipulation. //! - 'irq': Enable interrupt handling support. //! - 'tls': Enable kernel space thread-local storage support. //! - 'rtc': Enabl...`
  - `kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`mod task; mod process; use crate::fs::vfs::{OpenFlags, vfs_open};`
  - `kernel/src/task/task.rs:L19-L23`：关键词命中
    代码片段：`use crate::memory::*; use crate::sbi::shutdown; use crate::task::Signal; use crate::task::file_loader; use log::debug;`
- 与 2025 BlueStar 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sync/axsync/src/lib.rs:L3-L7`：关键词命中
    代码片段：`//! Currently supported primitives: //! //! - ['Mutex']: A mutual exclusion primitive. //! - mod ['spin']: spinlocks imported from the ['kspin'] crate. //!`
  - `kernel/sync/axsync/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! A naïve sleeping mutex. use core::sync::atomic::{AtomicU64, Ordering};`
  - `kernel/src/sync/up.rs:L19-L23`：关键词命中
    代码片段：`#[track_caller] pub fn lock(&self)->core::cell::RefMut<'_,T>{ match self.inner.try_borrow_mut() { Ok(g) => g,`
  - `kernel/vendor/spin/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! Locks that have the same behaviour as a mutex. //! //! The ['Mutex'] in the root of the crate, can be configured using the 'ticket_mutex' feature.`
- 与 2025 BlueStar 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `vendor/axcpu/src/riscv/trap.rs:L48-L52`：关键词命中
    代码片段：`#[cfg(feature = "uspace")] Trap::Exception(E::UserEnvCall) => { tf.regs.a0 = crate::trap::handle_syscall(tf, tf.regs.a7) as usize; tf.sepc += 4; }`
  - `vendor/axcpu/src/aarch64/trap.rs:L99-L103`：关键词命中
    代码片段：`#[cfg(feature = "uspace")] Some(ESR_EL1::EC::Value::SVC64) => { tf.r[0] = crate::trap::handle_syscall(tf, tf.r[8] as usize) as u64; } Some(ESR_EL1::EC::Value::InstrAbortLowerEL)...`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`mod syscall; use log::{error}; use crate::memory::VirAddr;`
  - `kernel/src/syscall/syscall.rs:L1272-L1276`：关键词命中
    代码片段：`/// 返回：成功返回映射起始地址；失败返回 -1 /// /// 参数说明（Linux riscv64 ABI，用户态用 ecall 传参）： /// 'addr'  : 映射起始虚拟地址（用户 hint）。若带 'MAP_FIXED' 则必须使用该地址。 /// 'len'   : 映射长度（字节）。内核按页对齐到覆盖该区间。`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2025 BlueStar 在“同步机制”维度发现 3 个同名定义：lock, try_lock, unlock。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `api/arceos_posix_api/src/imp/pthread/mutex.rs:L69-L69`：fn lock
    代码片段：`fn lock(&self) -> LinuxResult {`
  - `user/vendor/spin-0.7.1/src/mutex.rs:L172-L172`：fn lock
    代码片段：`pub fn lock(&self) -> MutexGuard<T> {`
  - `api/arceos_posix_api/src/imp/pthread/mutex.rs:L96-L96`：fn try_lock
    代码片段：`fn try_lock(&self) -> LinuxResult {`
  - `user/vendor/spin-0.7.1/src/mutex.rs:L205-L205`：fn try_lock
    代码片段：`pub fn try_lock(&self) -> Option<MutexGuard<T>> {`
- 函数/符号名重合：与 2025 BlueStar 在“系统调用”维度发现 4 个同名定义：sys_close, sys_dup, sys_dup2, sys_open。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `api/arceos_posix_api/src/imp/fd_ops.rs:L102-L102`：fn sys_close
    代码片段：`pub fn sys_close(fd: c_int) -> c_int {`
  - `user/src/syscall.rs:L275-L275`：fn sys_close
    代码片段：`pub fn sys_close(fd: usize) -> isize {`
  - `api/arceos_posix_api/src/imp/fd_ops.rs:L149-L149`：fn sys_dup
    代码片段：`pub fn sys_dup(old_fd: c_int) -> c_int {`
  - `user/src/syscall.rs:L141-L141`：fn sys_dup
    代码片段：`pub fn sys_dup(oldfd: usize) -> isize {`
- 函数/符号名重合：与 2025 BlueStar 在“文件系统”维度发现 2 个同名定义：sys_lseek, sys_fstat。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `api/arceos_posix_api/src/imp/fs.rs:L307-L307`：fn sys_lseek
    代码片段：`pub fn sys_lseek(fd: c_int, offset: ctypes::off_t, whence: c_int) -> ctypes::off_t {`
  - `user/src/syscall.rs:L279-L279`：fn sys_lseek
    代码片段：`pub fn sys_lseek(fd: usize, offset: isize, whence: usize) -> isize {`
  - `api/arceos_posix_api/src/imp/fs.rs:L346-L346`：fn sys_fstat
    代码片段：`pub unsafe fn sys_fstat(fd: c_int, buf: *mut ctypes::stat) -> c_int {`
  - `user/src/syscall.rs:L267-L267`：fn sys_fstat
    代码片段：`pub fn sys_fstat(fd: usize, stat_buf: *mut KStat) -> isize {`
- 文件路径重合：与 2025 BlueStar 在“调度与任务管理”维度出现同名文件源码路径 `api/arceos_api/src/imp/task.rs` / `kernel/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `api/arceos_api/src/imp/task.rs:L1-L3`：fn ax_sleep_until
    代码片段：`pub fn ax_sleep_until(deadline: crate::time::AxTimeValue) { #[cfg(feature = "multitask")] axtask::sleep_until(deadline);`
  - `kernel/src/task/task.rs:L19-L23`：关键词命中
    代码片段：`use crate::memory::*; use crate::sbi::shutdown; use crate::task::Signal; use crate::task::file_loader; use log::debug;`
- 文件路径重合：与 2025 BlueStar 在“同步机制”维度出现同名文件源码路径 `kernel/sync/axsync/src/mutex.rs` / `kernel/vendor/spin/src/mutex.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/sync/axsync/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! A naïve sleeping mutex. use core::sync::atomic::{AtomicU64, Ordering};`
  - `kernel/vendor/spin/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! Locks that have the same behaviour as a mutex. //! //! The ['Mutex'] in the root of the crate, can be configured using the 'ticket_mutex' feature.`
- 文件路径重合：与 2025 BlueStar 在“系统调用”维度出现同名文件源码路径 `vendor/axcpu/src/x86_64/syscall.rs` / `kernel/src/syscall/syscall.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `vendor/axcpu/src/x86_64/syscall.rs:L11-L15`：关键词命中
    代码片段：`core::arch::global_asm!( include_str!("syscall.S"), tss_rsp0_offset = const core::mem::offset_of!(TaskStateSegment, privilege_stack_table), );`
  - `kernel/src/syscall/syscall.rs:L1272-L1276`：关键词命中
    代码片段：`/// 返回：成功返回映射起始地址；失败返回 -1 /// /// 参数说明（Linux riscv64 ABI，用户态用 ecall 传参）： /// 'addr'  : 映射起始虚拟地址（用户 hint）。若带 'MAP_FIXED' 则必须使用该地址。 /// 'len'   : 映射长度（字节）。内核按页对齐到覆盖该区间。`

## 相似点

- 与 2025 BlueStar 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 BlueStar 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/drivers/axdriver/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! [ArceOS](https://github.com/arceos-org/arceos) device drivers. //! //! # Usage`
  - `kernel/drivers/axdisplay/src/lib.rs:L21-L25`：关键词命中
    代码片段：`info!("Initialize graphics subsystem..."); let dev = display_devs.take_one().expect("No graphics device found!"); info!("  use graphics device 0: {:?}", dev.device_name()); MAIN...`
- 与 2025 BlueStar 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/axfs/src/lib.rs:L5-L9`：关键词命中
    代码片段：`//! # Cargo Features //! //! - 'fatfs': Use [FAT] as the main filesystem and mount it on '/'. This feature //!   is **enabled** by default. //! - 'ext4fs': Use [ext4] as the mai...`
  - `kernel/fs/axfs/src/root.rs:L376-L380`：关键词命中
    代码片段：`#[derive(Debug, Clone, Copy, Eq, PartialEq)] enum RootFileSystemKind { Fat, Ext4, Unknown,`
- 与 2025 BlueStar 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/axcpu/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. #[cfg(feature = "uspace")]`
  - `kernel/arch/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use axcpu::trap::{IRQ, register_trap_handler};`
- 与 2025 BlueStar 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/memory/axmm/src/lib.rs:L78-L82`：关键词命中
    代码片段：`let mut aspace = AddrSpace::new_empty(base, size)?; if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use separate pag...`
  - `kernel/memory/axalloc/src/lib.rs:L203-L207`：关键词命中
    代码片段：`let init_heap_size = MIN_HEAP_SIZE; self.palloc.lock().init(start_vaddr, size); let heap_ptr = self .alloc_pages(init_heap_size / PAGE_SIZE, PAGE_SIZE) .unwrap();`
- 与 2025 BlueStar 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/axfs/src/root.rs:L473-L477`：关键词命中
    代码片段：`#[cfg(feature = "procfs")] root_dir // should not fail .mount("/proc", mounts::procfs().unwrap()) .expect("fail to mount procfs at /proc");`
  - `kernel/arch/axhal/src/lib.rs:L21-L25`：关键词命中
    代码片段：`//! - 'paging': Enable page table manipulation. //! - 'irq': Enable interrupt handling support. //! - 'tls': Enable kernel space thread-local storage support. //! - 'rtc': Enabl...`
- 与 2025 BlueStar 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sync/axsync/src/lib.rs:L3-L7`：关键词命中
    代码片段：`//! Currently supported primitives: //! //! - ['Mutex']: A mutual exclusion primitive. //! - mod ['spin']: spinlocks imported from the ['kspin'] crate. //!`
  - `kernel/sync/axsync/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! A naïve sleeping mutex. use core::sync::atomic::{AtomicU64, Ordering};`
- 与 2025 BlueStar 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `vendor/axcpu/src/riscv/trap.rs:L48-L52`：关键词命中
    代码片段：`#[cfg(feature = "uspace")] Trap::Exception(E::UserEnvCall) => { tf.regs.a0 = crate::trap::handle_syscall(tf, tf.regs.a7) as usize; tf.sepc += 4; }`
  - `vendor/axcpu/src/aarch64/trap.rs:L99-L103`：关键词命中
    代码片段：`#[cfg(feature = "uspace")] Some(ESR_EL1::EC::Value::SVC64) => { tf.r[0] = crate::trap::handle_syscall(tf, tf.r[8] as usize) as u64; } Some(ESR_EL1::EC::Value::InstrAbortLowerEL)...`
- 与 2025 BlueStar 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 BlueStar 的语言构成不同：待测作品为 {'json': 26, 'build': 3004, 'markdown': 2972, 'rust': 137568, 'c': 10853, 'toml': 591, 'asm': 797, 'make': 657, 'text': 394}，历史样本为 {'json': 53, 'build': 2684, 'markdown': 10844, 'toml': 9593, 'c': 336, 'rust': 279800, 'asm': 641, 'text': 5383, 'yaml': 51}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：20
- 含证据关键结论数：20（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
