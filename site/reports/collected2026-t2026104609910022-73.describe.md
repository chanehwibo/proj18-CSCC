# 2026 T2026104609910022 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026104609910022-73`
- 风格：rcore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：93
- 代码/文本行数：23266
- 主要语言：rust 22107 LOC, build 462 LOC, asm 360 LOC, markdown 235 LOC, text 44 LOC

## 总览

2026 T2026104609910022 是一个 rcore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 22107 LOC, build: 462 LOC, asm: 360 LOC, markdown: 235 LOC。仓库包含 93 个已扫描文件、约 23266 行可分析文本，当前抽取到 1478 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 3 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, Makefile, src-la/Cargo.toml, src-la/linker.ld, src/linker.ld。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "os" version = "0.1.0"`
  - `Makefile:L1-L3`：构建入口
    代码片段：`# HPU - OS Kernel Makefile # 比赛评测要求: make all 生成 kernel-rv 和 kernel-la (ELF格式) # ============================================================`
  - `src-la/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "os-la" version = "0.1.0"`
  - `src-la/linker.ld:L1-L3`：构建入口
    代码片段：`OUTPUT_ARCH(loongarch) ENTRY(_start)`
  - `src/linker.ld:L1-L3`：构建入口
    代码片段：`/* 链接脚本：控制内核映像在内存中的布局 */ OUTPUT_ARCH(riscv)          /* 指定目标架构为 RISC-V */ ENTRY(_start)              /* 程序入口点符号，启动汇编代码从这里开始执行 */`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn aligned_elf_image, fn load_elf, fn check_per_task_timeout, fn suspend_current_and_run_next, fn block_current_and_run_next。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/task/mod.rs` | L5-L9 | 关键词命中 |
| `src/task/pid.rs` | L14-L18 | 关键词命中 |
| `src/task/task.rs` | L2-L6 | 关键词命中 |
| `src/task/elf.rs` | L6-L10 | fn aligned_elf_image |
| `src/task/elf.rs` | L25-L29 | fn load_elf |
| `src/task/mod.rs` | L20-L24 | fn check_per_task_timeout |

### 关键代码片段

  - `src/task/mod.rs:L5-L9`：关键词命中
    代码片段：`pub mod pid; pub mod processor; pub mod task; use crate::timer::get_time;`
  - `src/task/pid.rs:L14-L18`：关键词命中
    代码片段：`impl PidAllocator { pub fn new() -> Self { // Linux userspace reserves pid 0 for the scheduler/idle context. Self { current: 1, recycled: Vec::new() } }`
  - `src/task/task.rs:L2-L6`：关键词命中
    代码片段：`use crate::mm::{virt_to_phys, MemorySet, KERNEL_SPACE}; use crate::println; use crate::task::context::TaskContext; use crate::task::pid::{trap_cx_bottom_from_tid, KernelStack, P...`
  - `src/task/elf.rs:L6-L10`：fn aligned_elf_image
    代码片段：`use xmas_elf::ElfFile; fn aligned_elf_image(elf_data: &[u8]) -> Vec<usize> { let word_size = size_of::<usize>(); let word_count = (elf_data.len() + word_size - 1) / word_size;`

### 相关符号

`fn aligned_elf_image` at `src/task/elf.rs:L6`、`fn load_elf` at `src/task/elf.rs:L25`、`fn check_per_task_timeout` at `src/task/mod.rs:L20`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn init, fn ekernel, fn info, struct PhysAddr, struct VirtAddr。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/mm/mod.rs` | L2-L6 | 关键词命中 |
| `src/mm/memory_set.rs` | L1-L4 | 关键词命中 |
| `src/mm/page_table.rs` | L1-L5 | 关键词命中 |
| `src/mm/mod.rs` | L16-L20 | fn init |
| `src/mm/mod.rs` | L20-L24 | fn ekernel |
| `src/mm/mod.rs` | L37-L41 | fn info |

### 关键代码片段

  - `src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`use crate::{print, println}; mod address; mod heap_allocator; mod frame_allocator; mod page_table;`
  - `src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use crate::mm::address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum, VPNRange}; use crate::mm::page_table::{PTEFlags, PageTableEntry, PageTable}; use crate::mm::frame_allocato...`
  - `src/mm/page_table.rs:L1-L5`：关键词命中
    代码片段：`use bitflags::*; use crate::mm::address::{PhysAddr, PhysPageNum, VirtAddr, VirtPageNum}; use crate::mm::frame_allocator::{frame_alloc, frame_dealloc, FrameTracker}; use alloc::v...`
  - `src/mm/mod.rs:L16-L20`：fn init
    代码片段：`pub use memory_set::{MapPermission, KERNEL_SPACE}; pub fn init() { heap_allocator::init_heap(); let kernel_end = unsafe {`

### 相关符号

`fn init` at `src/mm/mod.rs:L16`、`fn ekernel` at `src/mm/mod.rs:L20`、`fn info` at `src/mm/mod.rs:L37`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn __alltraps, fn __restore, fn init, fn set_kernel_trap_entry, fn set_user_trap_entry。（置信度：medium）
  - 静态识别到 284 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/syscall/mod.rs` | L6-L10 | 关键词命中 |
| `src/trap/context.rs` | L57-L61 | 关键词命中 |
| `src/syscall/errno.rs` | L2-L6 | 关键词命中 |
| `src/trap/mod.rs` | L18-L22 | fn __alltraps |
| `src/trap/mod.rs` | L19-L23 | fn __restore |
| `src/trap/mod.rs` | L22-L26 | fn init |

### 关键代码片段

  - `src/syscall/mod.rs:L6-L10`：关键词命中
    代码片段：`use errno::*; // Linux RISC-V 64 syscall IDs（只列出我们需要的） pub const SYS_WRITE: usize = 64; pub const SYS_EVENTFD2: usize = 19;`
  - `src/trap/context.rs:L57-L61`：关键词命中
    代码片段：`/// 保留字段（汇编未使用，仅用于对齐） pub reserved: usize,          // 32*8 /// 发生 ecall 前的原始 a0（syscall 重启时需要） pub origin_a0: usize,         // 33*8 /// 陷入时的 sstatus（SPP/SPIE 等）`
  - `src/syscall/errno.rs:L2-L6`：关键词命中
    代码片段：`pub const SUCCESS: isize = 0; // Linux returns errors from the syscall ABI as negative errno values.  The // userspace supplied by the contest relies on this convention ('ret <...`
  - `src/trap/mod.rs:L18-L22`：fn __alltraps
    代码片段：`extern "C" { pub fn __alltraps(); pub fn __restore(); }`

### 相关符号

`fn __alltraps` at `src/trap/mod.rs:L18`、`fn __restore` at `src/trap/mod.rs:L19`、`fn init` at `src/trap/mod.rs:L22`、`fn sys_write` at `src/syscall/fs.rs:L25`、`fn sys_writev` at `src/syscall/fs.rs:L42`、`fn sys_readv` at `src/syscall/fs.rs:L65`、`fn sys_openat` at `src/syscall/fs.rs:L103`、`fn sys_read` at `src/syscall/fs.rs:L125`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：fn init, fn self_test, struct OpenFlags, trait File, fn readable。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/fs/mod.rs` | L1-L5 | 关键词命中 |
| `src/fs/ext4.rs` | L1-L3 | 关键词命中 |
| `src/fs/manager.rs` | L38-L42 | 关键词命中 |
| `src/fs/mod.rs` | L8-L12 | fn init |
| `src/fs/mod.rs` | L13-L17 | fn self_test |
| `src/fs/vfs.rs` | L1-L4 | struct OpenFlags |

### 关键代码片段

  - `src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`use crate::println; pub mod fd_table; pub mod vfs; pub mod ext4; pub mod manager;`
  - `src/fs/ext4.rs:L1-L3`：关键词命中
    代码片段：`//! EXT4 file system layer ? inode lookup, file read use core::cell::Cell;`
  - `src/fs/manager.rs:L38-L42`：关键词命中
    代码片段：`pub struct Ext4File { pub fs: Ext4Fs, pub inode: Ext4Inode, pub offset: Cell<usize>, }`
  - `src/fs/mod.rs:L8-L12`：fn init
    代码片段：`pub use manager::FS_MANAGER; pub fn init() { crate::println!("fs: ext4 filesystem init..."); manager::init();`

### 相关符号

`fn init` at `src/fs/mod.rs:L8`、`fn self_test` at `src/fs/mod.rs:L13`、`struct OpenFlags` at `src/fs/vfs.rs:L1`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/task/mod.rs` | L10-L14 | 关键词命中 |
| `src/task/pid.rs` | L3-L7 | 关键词命中 |
| `src/task/task.rs` | L9-L13 | 关键词命中 |

### 关键代码片段

  - `src/task/mod.rs:L10-L14`：关键词命中
    代码片段：`use crate::config::CLOCK_FREQ; use lazy_static::*; use spin::Mutex; const PER_TASK_TIMEOUT: usize = 10 * CLOCK_FREQ;`
  - `src/task/pid.rs:L3-L7`：关键词命中
    代码片段：`use crate::config::{KERNEL_HEAP_SIZE, PAGE_SIZE}; use alloc::vec::Vec; use spin::Mutex; use lazy_static::*;`
  - `src/task/task.rs:L9-L13`：关键词命中
    代码片段：`use core::fmt::{self, Debug, Formatter}; use riscv::register::satp; use spin::Mutex; #[derive(PartialEq, Clone, Copy, Debug)]`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn __alltraps, fn __restore, fn init, fn set_kernel_trap_entry, fn set_user_trap_entry。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/trap/mod.rs` | L5-L9 | 关键词命中 |
| `src/trap/context.rs` | L1-L3 | 关键词命中 |
| `src/task/mod.rs` | L7-L11 | 关键词命中 |
| `src/trap/mod.rs` | L18-L22 | fn __alltraps |
| `src/trap/mod.rs` | L19-L23 | fn __restore |
| `src/trap/mod.rs` | L22-L26 | fn init |

### 关键代码片段

  - `src/trap/mod.rs:L5-L9`：关键词命中
    代码片段：`current_task, current_trap_cx, do_wake_expired, suspend_current_and_run_next, }; use crate::timer::set_next_trigger; pub use context::{GeneralRegs, TrapContext}; mod context;`
  - `src/trap/context.rs:L1-L3`：关键词命中
    代码片段：`//! Trap 上下文 — 保存/恢复用户态寄存器 /// 通用寄存器（RISC-V 64）`
  - `src/task/mod.rs:L7-L11`：关键词命中
    代码片段：`pub mod task; use crate::timer::get_time; use crate::config::CLOCK_FREQ; use lazy_static::*;`
  - `src/trap/mod.rs:L18-L22`：fn __alltraps
    代码片段：`extern "C" { pub fn __alltraps(); pub fn __restore(); }`

### 相关符号

`fn __alltraps` at `src/trap/mod.rs:L18`、`fn __restore` at `src/trap/mod.rs:L19`、`fn init` at `src/trap/mod.rs:L22`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：fn init, fn self_test, trait BlockDevice, fn read_block, fn write_block。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/drivers/block.rs` | L20-L24 | 关键词命中 |
| `src-la/drivers/block.rs` | L20-L24 | 关键词命中 |
| `src/console.rs` | L20-L24 | 关键词命中 |
| `src/drivers/mod.rs` | L5-L9 | fn init |
| `src/drivers/mod.rs` | L11-L15 | fn self_test |
| `src/drivers/block.rs` | L3-L7 | trait BlockDevice |

### 关键代码片段

  - `src/drivers/block.rs:L20-L24`：关键词命中
    代码片段：`#[cfg(feature = "arch-riscv64")] mod virtio_mmio { use super::{BlockProbe, BLOCK_SIZE}; use core::mem::size_of;`
  - `src-la/drivers/block.rs:L20-L24`：关键词命中
    代码片段：`#[cfg(feature = "arch-riscv64")] mod virtio_mmio { use super::{BlockProbe, BLOCK_SIZE}; use core::mem::size_of;`
  - `src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print {                        // 定义print宏,接受一个格式字符串和可选的参数 ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?));  // 格式...`
  - `src/drivers/mod.rs:L5-L9`：fn init
    代码片段：`pub use block::{BlockDevice, BLOCK_SIZE}; pub fn init() { println!("drivers: block/net interfaces declared"); let ok = block::init_block_device();`

### 相关符号

`fn init` at `src/drivers/mod.rs:L5`、`fn self_test` at `src/drivers/mod.rs:L11`、`trait BlockDevice` at `src/drivers/block.rs:L3`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：15
- 含证据关键结论数：15（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
