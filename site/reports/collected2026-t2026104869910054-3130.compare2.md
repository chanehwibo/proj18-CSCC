# 2026 T2026104869910054 比较报告

- 对比历史仓库：2025 sudo_pacman_Syu
- 生成时间：2026-07-02T05:01:06.467490+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 sudo_pacman_Syu（来源：赛事历史作品）：画像相似度 score=9.82；同属 rcore-variant 风格; 语言构成相似度 0.98; OS 维度重合度 1.00; 代码规模接近度 0.87

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 sudo_pacman_Syu 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/driver/src/bus.rs:L1-L5`：关键词命中
    代码片段：`//! Bus abstraction layer. //! //! Currently only MMIO bus is supported (used by VirtIO legacy MMIO and //! NS16550A UART on RISC-V platforms). PCI is a stub for future expansio...`
  - `crates/driver/src/trait.rs:L1-L3`：关键词命中
    代码片段：`//! Device trait definitions for the GoodOS driver subsystem. //! //! Defines the core traits that all device drivers must implement:`
  - `src/kernel/console.rs:L2-L6`：关键词命中
    代码片段：`use alloc::sync::Arc; static CONSOLE: Spin<Option<Arc<Terminal>>> = Spin::new(None); pub fn set_console(terminal: Arc<Terminal>) -> KResult<()> {`
  - `src/kernel/pcie/driver.rs:L1-L4`：关键词命中
    代码片段：`use super::{ device::{PCIDevice, PCIE_DEVICES}, error::PciError, };`
- 与 2025 sudo_pacman_Syu 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/fs/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! GoodOS 文件系统层 (VFS)。 //! //! 提供统一的文件系统接口，支持多种文件系统实现：`
  - `crates/fs/src/file.rs:L4-L8`：关键词命中
    代码片段：`//! 减少各文件系统实现的重复代码。 use crate::vfs::traits::File; use crate::vfs::FsResult;`
  - `src/fs/shm.rs:L7-L11`：关键词命中
    代码片段：`use crate::{ fs::tmpfs::{DirectoryInode, FileInode, TmpFs}, kernel::{constants::ENOSPC, vfs::inode::Mode}, prelude::KResult, };`
  - `src/fs/ext4.rs:L4-L8`：关键词命中
    代码片段：`use crate::kernel::task::block_on; use crate::kernel::timer::Ticks; use crate::kernel::vfs::inode::{AtomicMode, Mode}; use crate::{ io::{Buffer, ByteBuffer, Stream},`
- 与 2025 sudo_pacman_Syu 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/kernel/src/trap_handler.rs:L1-L3`：关键词命中
    代码片段：`// Trap 分发器：时钟、系统调用、缺页。HAL 解码后回调本模块进行子系统分发。 use crate::log;`
  - `crates/hal/src/rv64/trap.S:L1-L3`：关键词命中
    代码片段：`# RISC-V trap assembly with an xv6/148-style trampoline. # # TrapContext layout (50 * 8 = 400 bytes):`
  - `src/kernel/timer.rs:L170-L174`：关键词命中
    代码片段：`pub fn timer_interrupt() { if CPU::local().cpuid() != 0 { // Only the BSP should handle the timer interrupt. return; }`
  - `src/kernel/interrupt.rs:L1-L5`：关键词命中
    代码片段：`use super::mem::handle_kernel_page_fault; use super::task::block_on; use super::timer::timer_interrupt; use crate::kernel::constants::EINVAL; use crate::prelude::*;`
- 与 2025 sudo_pacman_Syu 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/mm/src/cow.rs:L1-L5`：关键词命中
    代码片段：`// Copy-on-write — stub. // // COW lets multiple processes share read-only physical pages.  When a process // writes to a COW page a page fault fires; the handler allocates a pr...`
  - `crates/mm/src/lib.rs:L2-L6`：关键词命中
    代码片段：`// // 提供地址空间管理、物理页分配、页表操作、用户态/内核态拷贝和 ELF 加载。 // 具体地址常量由 'platform' crate 提供；架构相关操作（TLB 刷新、satp 写入） // 通过 cfg 守卫区分 RISC-V / LoongArch。 //`
  - `src/kernel/mem/mm_area.rs:L8-L12`：关键词命中
    代码片段：`use core::cmp; use eonix_mm::address::{AddrOps as _, VAddr, VRange}; use eonix_mm::page_table::{PageAttribute, RawAttribute, PTE}; use eonix_mm::paging::{PAGE_SIZE, PFN};`
  - `src/kernel/mem/mm_list.rs:L17-L21`：关键词命中
    代码片段：`}; use eonix_mm::address::PAddr; use eonix_mm::page_table::PageAttribute; use eonix_mm::paging::PFN; use eonix_mm::{`
- 与 2025 sudo_pacman_Syu 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/task/src/task.rs:L1-L3`：关键词命中
    代码片段：`// Task —— simple task status type. use crate::task_id::Pid;`
  - `crates/task/src/task_id.rs:L1-L5`：关键词命中
    代码片段：`// Pid/Tid 全局分配器。 // // Pid/Tid share one Linux-style task-id namespace. use core::sync::atomic::{AtomicUsize, Ordering};`
  - `src/kernel/task.rs:L8-L12`：关键词命中
    代码片段：`mod session; mod signal; mod thread; pub use clone::{do_clone, CloneArgs, CloneFlags};`
  - `src/kernel/task/clone.rs:L2-L6`：关键词命中
    代码片段：`kernel::{ syscall::{procops::parse_user_tls, UserMut}, task::{alloc_pid, ProcessBuilder, ProcessList, Thread, ThreadBuilder}, user::UserPointerMut, },`
- 与 2025 sudo_pacman_Syu 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/task/src/task_id.rs:L3-L7`：关键词命中
    代码片段：`// Pid/Tid share one Linux-style task-id namespace. use core::sync::atomic::{AtomicUsize, Ordering}; /// 进程 ID。`
  - `crates/mm/src/lib.rs:L20-L24`：关键词命中
    代码片段：`extern crate alloc; use core::sync::atomic::{AtomicUsize, Ordering}; mod aspace;`
  - `src/sync.rs:L1-L4`：关键词命中
    代码片段：`mod arcswap; mod condvar; pub use arcswap::ArcSwap;`
  - `src/sync/arcswap.rs:L3-L7`：关键词命中
    代码片段：`fmt::{self, Debug, Formatter}, ptr::NonNull, sync::atomic::{AtomicPtr, Ordering}, }; use pointers::BorrowedArc;`
- 与 2025 sudo_pacman_Syu 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `crates/syscall/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! Provides the 'syscall_instrument!' macro and the central dispatch function. //! All syscall implementations live in 'imp/' submodules, categorised by domain. #![no_std]`
  - `crates/syscall/src/consts.rs:L1-L5`：关键词命中
    代码片段：`// System call number constants. // // These match the Linux asm-generic syscall ABI used by RISC-V and LoongArch. pub const SYS_LSEEK: usize = 62;`
  - `src/kernel/syscall/net.rs:L6-L10`：关键词命中
    代码片段：`use crate::io::IntoStream; use crate::kernel::constants::{EAFNOSUPPORT, EBADF, EINVAL, ENOTSOCK}; use crate::kernel::syscall::file_rw::IoVec; use crate::kernel::syscall::User; u...`
  - `src/kernel/syscall/file_rw.rs:L4-L8`：关键词命中
    代码片段：`EBADF, EFAULT, EINVAL, ENOENT, ENOTDIR, SEEK_CUR, SEEK_END, SEEK_SET, }; use crate::kernel::syscall::UserMut; use crate::kernel::task::Thread; use crate::kernel::timer::sleep;`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2025 sudo_pacman_Syu 在“设备驱动”维度发现 3 个同名定义：new, probe, register。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `crates/driver/src/impls/block/ramdisk.rs:L20-L20`：fn new
    代码片段：`pub fn new(name: &'static str, block_size: usize, num_blocks: usize) -> Self {`
  - `src/driver/e1000e.rs:L88-L88`：fn new
    代码片段：`fn new(base: PAddr) -> Self {`
  - `crates/driver/src/impls/block/virtio_blk.rs:L150-L150`：fn probe
    代码片段：`pub fn probe(base: usize) -> Option<usize> {`
  - `src/driver/goldfish_rtc.rs:L34-L34`：fn probe
    代码片段：`pub fn probe() {`
- 函数/符号名重合：与 2025 sudo_pacman_Syu 在“文件系统”维度发现 4 个同名定义：new, read_block, write_block, get。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `crates/fs/src/block_cache.rs:L77-L77`：fn new
    代码片段：`pub fn new(device: &'a dyn BlockDevice) -> Self {`
  - `src/fs/ext4.rs:L41-L41`：fn new
    代码片段：`pub fn new(device: Arc<BlockDevice>) -> Self {`
  - `crates/fs/src/block_cache.rs:L156-L156`：fn read_block
    代码片段：`fn read_block(&self, block_id: u64, buf: &mut [u8]) -> DriverResult<()> {`
  - `src/fs/ext4.rs:L47-L47`：fn read_block
    代码片段：`fn read_block(&self, block_id: PBlockId) -> Block {`
- 函数/符号名重合：与 2025 sudo_pacman_Syu 在“同步机制”维度发现 3 个同名定义：new, read_block, write_block。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `crates/driver/src/impls/block/ramdisk.rs:L20-L20`：fn new
    代码片段：`pub fn new(name: &'static str, block_size: usize, num_blocks: usize) -> Self {`
  - `src/fs/ext4.rs:L41-L41`：fn new
    代码片段：`pub fn new(device: Arc<BlockDevice>) -> Self {`
  - `crates/driver/src/impls/block/ramdisk.rs:L64-L64`：fn read_block
    代码片段：`fn read_block(&self, block_id: u64, buf: &mut [u8]) -> DriverResult<()> {`
  - `src/fs/ext4.rs:L47-L47`：fn read_block
    代码片段：`fn read_block(&self, block_id: PBlockId) -> Block {`
- 函数/符号名重合：与 2025 sudo_pacman_Syu 在“内存管理”维度发现 1 个同名定义：read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/fs/src/impls/ext4/adapter.rs:L1365-L1365`：fn read
    代码片段：`pub fn read(&self, handle: &FileHandle, buf: &mut [u8], offset: usize) -> FsResult<usize> {`
  - `src/fs/procfs.rs:L68-L68`：fn read
    代码片段：`fn read(&self, buffer: &mut PageBuffer) -> KResult<usize> {`
- 结构体/类型重合：与 2025 sudo_pacman_Syu 在“设备驱动”维度发现 2 个同名定义：BlockDevice, CharDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/driver/src/trait.rs:L72-L72`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Device {`
  - `src/kernel/block.rs:L51-L51`：struct BlockDevice
    代码片段：`pub struct BlockDevice {`
  - `crates/driver/src/trait.rs:L123-L123`：trait CharDevice
    代码片段：`pub trait CharDevice: Device {`
  - `src/kernel/chardev.rs:L34-L34`：struct CharDevice
    代码片段：`pub struct CharDevice {`
- 结构体/类型重合：与 2025 sudo_pacman_Syu 在“文件系统”维度发现 1 个同名定义：Ext4Fs。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/fs/src/impls/ext4/adapter.rs:L352-L352`：struct Ext4Fs
    代码片段：`pub struct Ext4Fs {`
  - `src/fs/ext4.rs:L70-L70`：struct Ext4Fs
    代码片段：`struct Ext4Fs {`
- 结构体/类型重合：与 2025 sudo_pacman_Syu 在“同步机制”维度发现 1 个同名定义：BlockDevice。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `crates/driver/src/trait.rs:L72-L72`：trait BlockDevice
    代码片段：`pub trait BlockDevice: Device {`
  - `src/kernel/block.rs:L51-L51`：struct BlockDevice
    代码片段：`pub struct BlockDevice {`
- 文件路径重合：与 2025 sudo_pacman_Syu 在“调度与任务管理”维度出现同名文件源码路径 `crates/task/src/task.rs` / `src/kernel/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `crates/task/src/task.rs:L1-L3`：关键词命中
    代码片段：`// Task —— simple task status type. use crate::task_id::Pid;`
  - `src/kernel/task.rs:L8-L12`：关键词命中
    代码片段：`mod session; mod signal; mod thread; pub use clone::{do_clone, CloneArgs, CloneFlags};`

## 相似点

- 与 2025 sudo_pacman_Syu 同属 rcore-variant 风格。（置信度：medium）
- 与 2025 sudo_pacman_Syu 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/driver/src/bus.rs:L1-L5`：关键词命中
    代码片段：`//! Bus abstraction layer. //! //! Currently only MMIO bus is supported (used by VirtIO legacy MMIO and //! NS16550A UART on RISC-V platforms). PCI is a stub for future expansio...`
  - `crates/driver/src/trait.rs:L1-L3`：关键词命中
    代码片段：`//! Device trait definitions for the GoodOS driver subsystem. //! //! Defines the core traits that all device drivers must implement:`
- 与 2025 sudo_pacman_Syu 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/fs/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! GoodOS 文件系统层 (VFS)。 //! //! 提供统一的文件系统接口，支持多种文件系统实现：`
  - `crates/fs/src/file.rs:L4-L8`：关键词命中
    代码片段：`//! 减少各文件系统实现的重复代码。 use crate::vfs::traits::File; use crate::vfs::FsResult;`
- 与 2025 sudo_pacman_Syu 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/kernel/src/trap_handler.rs:L1-L3`：关键词命中
    代码片段：`// Trap 分发器：时钟、系统调用、缺页。HAL 解码后回调本模块进行子系统分发。 use crate::log;`
  - `crates/hal/src/rv64/trap.S:L1-L3`：关键词命中
    代码片段：`# RISC-V trap assembly with an xv6/148-style trampoline. # # TrapContext layout (50 * 8 = 400 bytes):`
- 与 2025 sudo_pacman_Syu 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/mm/src/cow.rs:L1-L5`：关键词命中
    代码片段：`// Copy-on-write — stub. // // COW lets multiple processes share read-only physical pages.  When a process // writes to a COW page a page fault fires; the handler allocates a pr...`
  - `crates/mm/src/lib.rs:L2-L6`：关键词命中
    代码片段：`// // 提供地址空间管理、物理页分配、页表操作、用户态/内核态拷贝和 ELF 加载。 // 具体地址常量由 'platform' crate 提供；架构相关操作（TLB 刷新、satp 写入） // 通过 cfg 守卫区分 RISC-V / LoongArch。 //`
- 与 2025 sudo_pacman_Syu 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/task/src/task.rs:L1-L3`：关键词命中
    代码片段：`// Task —— simple task status type. use crate::task_id::Pid;`
  - `crates/task/src/task_id.rs:L1-L5`：关键词命中
    代码片段：`// Pid/Tid 全局分配器。 // // Pid/Tid share one Linux-style task-id namespace. use core::sync::atomic::{AtomicUsize, Ordering};`
- 与 2025 sudo_pacman_Syu 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/task/src/task_id.rs:L3-L7`：关键词命中
    代码片段：`// Pid/Tid share one Linux-style task-id namespace. use core::sync::atomic::{AtomicUsize, Ordering}; /// 进程 ID。`
  - `crates/mm/src/lib.rs:L20-L24`：关键词命中
    代码片段：`extern crate alloc; use core::sync::atomic::{AtomicUsize, Ordering}; mod aspace;`
- 与 2025 sudo_pacman_Syu 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `crates/syscall/src/lib.rs:L2-L6`：关键词命中
    代码片段：`//! //! Provides the 'syscall_instrument!' macro and the central dispatch function. //! All syscall implementations live in 'imp/' submodules, categorised by domain. #![no_std]`
  - `crates/syscall/src/consts.rs:L1-L5`：关键词命中
    代码片段：`// System call number constants. // // These match the Linux asm-generic syscall ABI used by RISC-V and LoongArch. pub const SYS_LSEEK: usize = 62;`
- 与 2025 sudo_pacman_Syu 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 sudo_pacman_Syu 的语言构成不同：待测作品为 {'toml': 44, 'build': 693, 'markdown': 681, 'rust': 46328, 'asm': 618}，历史样本为 {'json': 36, 'toml': 86, 'build': 455, 'markdown': 1434, 'text': 40, 'rust': 39368}。（置信度：medium）

## 可能创新点

- 待测作品包含 292 个历史样本 2025 sudo_pacman_Syu 中未见的独特函数/类型定义（如 `raw_read_cache_block`, `raw_write_cache_block`, `raw_read_cache_blocks`, `read_bytes_from`, `get_scratch_block_device`, `lookup_child_ino`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `crates/fs/src/impls/ext4/super_block.rs:L462-L462`：fn raw_read_cache_block
    代码片段：`fn raw_read_cache_block(dev: &'static dyn BlockDevice, base: usize, data: &mut [u8; EXT4_CACHE_BLOCK_SIZE]) -> Result<(), ()> {`
  - `crates/fs/src/impls/ext4/super_block.rs:L498-L498`：fn raw_write_cache_block
    代码片段：`fn raw_write_cache_block(dev: &'static dyn BlockDevice, base: usize, data: &[u8; EXT4_CACHE_BLOCK_SIZE]) -> Result<(), ()> {`

## 附录：核验摘要

- 关键结论数：23
- 含证据关键结论数：23（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
