# 2026 T2026101129911342 比较报告

- 对比历史仓库：2022 啊队队队
- 生成时间：2026-07-02T04:58:54.926164+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 啊队队队（来源：赛事历史作品）：画像相似度 score=9.24；同属 ucore-variant 风格; 语言构成相似度 0.88; OS 维度重合度 1.00; 代码规模接近度 0.47

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 啊队队队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/virtio_blk.rs:L1-L3`：关键词命中
    代码片段：`//! 最小 virtio-blk 驱动：探测 + 读扇区。split virtqueue 三环放在一个连续页对齐结构里 //! （desc@0 / avail@256 / used@4096）。恒等映射下内核 VA = PA，故 ring/缓冲地址即设备 //! 可用的物理地址，无需翻译或弹跳缓冲。`
  - `kernel/src/ext4.rs:L1-L4`：关键词命中
    代码片段：`//! 最小 ext4 只读：超级块 + inode(extent, depth 0) + 根目录项 + 按名读文件。 //! 仅支持 4096 块大小（赛事与本地镜像默认）。块经 virtio-blk 读取。 use crate::virtio_blk; use alloc::string::String;`
  - `kernel/src/console.rs:L8-L12`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
  - `kernel/src/device/mod.rs:L9-L13`：关键词命中
    代码片段：`use fatfs::LossyOemCpConverter; use fatfs::NullTimeProvider; use virtio_drivers::VirtIOBlk; use virtio_drivers::VirtIOHeader; use crate::sync::mutex::Mutex;`
- 与 2022 啊队队队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/ext4.rs:L1-L3`：关键词命中
    代码片段：`//! 最小 ext4 只读：超级块 + inode(extent, depth 0) + 根目录项 + 按名读文件。 //! 仅支持 4096 块大小（赛事与本地镜像默认）。块经 virtio-blk 读取。 use crate::virtio_blk;`
  - `fatfs/src/fs.rs:L22-L26`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `kernel/src/fs/file.rs:L11-L15`：关键词命中
    代码片段：`use crate::memory::page_table::{PageMappingManager, PTEFlags}; use super::filetree::INode; #[allow(unused)]`
- 与 2022 啊队队队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/main.rs:L30-L34`：关键词命中
    代码片段：`khal::init_trap(); khal::test_breakpoint(); khal::kprintln!("[KunikOS] trap round-trip ok"); khal::init_paging();`
  - `kernel/src/syscall.rs:L1-L4`：关键词命中
    代码片段：`//! 系统调用分发（架构无关，语义对齐 Linux）。 //! khal 的 trap 入口解出 (号, 参数) 后调用 kunikos_syscall，返回值写回 a0。 use khal::console_putchar;`
  - `kernel/src/interrupt/mod.rs:L1-L3`：关键词命中
    代码片段：`pub mod timer; use core::arch::global_asm;`
  - `kernel/src/main.rs:L13-L17`：关键词命中
    代码片段：`mod console; mod device; pub mod interrupt; mod memory; mod fs;`
- 与 2022 啊队队队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L5-L9`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate 的大文件 + ELF 加载） const MIN: usize = 16; // 块粒度，满足 align<=16 的全部内核分配`
  - `khal/src/riscv64.rs:L117-L121`：关键词命中
    代码片段：`#[repr(C, align(4096))] struct PageTable([u64; 512]); static mut ROOT: PageTable = PageTable([0; 512]);`
  - `kernel/src/memory/mod.rs:L4-L8`：关键词命中
    代码片段：`pub mod page; pub mod addr; pub mod page_table; pub mod mem_map; pub mod mem_set;`
  - `kernel/src/memory/heap.rs:L3-L7`：关键词命中
    代码片段：`// 堆大小 const HEAP_SIZE: usize = 0x0008_0000; // 堆空间`
- 与 2022 啊队队队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs.rs:L260-L264`：关键词命中
    代码片段：`} // 合成 /proc、/sys 等只读伪文件（LTP tst 框架启动要读）。 /// /proc/<pid>/X 归一为 /proc/self/X（数字 pid 段映射到当前进程视图）。 fn normalize_proc(name: &str) -> Option<String> {`
  - `kernel/src/main.rs:L144-L148`：关键词命中
    代码片段：`"waitpid", "write", "yield", ]; khal::kprintln!("#### OS COMP TEST GROUP START basic ####");`
  - `kernel/src/task/mod.rs:L14-L18`：关键词命中
    代码片段：`use crate::memory::page::alloc_more; use crate::runtime_err::RuntimeError; use crate::task::process::Process; use crate::task::task_scheduler::start_tasks; use crate::memory::pa...`
  - `kernel/src/task/task.rs:L4-L8`：关键词命中
    代码片段：`use crate::memory::addr::UserAddr; use crate::interrupt::Context; use crate::task::task_scheduler::kill_task; use super::process::Process;`
- 与 2022 啊队队队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L3-L7`：关键词命中
    代码片段：`use core::alloc::{GlobalAlloc, Layout}; use core::cell::UnsafeCell; use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate...`
  - `kernel/src/virtio_blk.rs:L5-L9`：关键词命中
    代码片段：`//! 传输层按架构分流：rv64 走 virtio-mmio（0x10001000 槽位扫描）；la64 走 virtio-pci //! （ECAM 枚举 + BAR4 现代能力）。队列布局与 'read()' 主体两架构共用，仅 init/notify 不同。 use core::sync::atomic::{Ordering, fence};...`
  - `kernel/src/sync/mod.rs:L1-L1`：关键词命中
    代码片段：`pub mod mutex;`
  - `kernel/src/sync/mutex.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; use core::cell::UnsafeCell; use core::marker::Sync;`
- 与 2022 啊队队队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `khal/src/riscv64.rs:L20-L24`：关键词命中
    代码片段：`let ret; unsafe { asm!("ecall", inlateout("a0") arg0 => ret, in("a7") eid, options(nostack)); } ret`
  - `kernel/src/sbi.rs:L19-L23`：关键词命中
    代码片段：`let mut ret; unsafe { asm!("ecall", in("a7") which, inlateout("a0") arg0 as i32 => ret,`
  - `device/src/sbi.rs:L19-L23`：关键词命中
    代码片段：`let mut ret; unsafe { asm!("ecall", in("a7") which, inlateout("a0") arg0 as i32 => ret,`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2022 啊队队队 在“设备驱动”维度发现 3 个同名定义：read_block, read, console_putchar。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/ext4.rs:L33-L33`：fn read_block
    代码片段：`fn read_block(blk: u64, buf: &mut [u8]) -> bool {`
  - `kernel/src/device/block.rs:L8-L8`：fn read_block
    代码片段：`fn read_block(&mut self, sector_offset: usize, buf: &mut [u8]) {`
  - `kernel/src/virtio_blk.rs:L95-L95`：fn read
    代码片段：`pub fn read(sector: u64, buf: &mut [u8]) -> bool {`
  - `kernel/src/device/mod.rs:L133-L133`：fn read
    代码片段：`fn read(&mut self, buf: &mut [u8]) -> Result<usize, RuntimeError> {`
- 函数/符号名重合：与 2022 啊队队队 在“文件系统”维度发现 4 个同名定义：read_block, mount, mkdir, read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/ext4.rs:L33-L33`：fn read_block
    代码片段：`fn read_block(blk: u64, buf: &mut [u8]) -> bool {`
  - `kernel/src/device/block.rs:L8-L8`：fn read_block
    代码片段：`fn read_block(&mut self, sector_offset: usize, buf: &mut [u8]) {`
  - `kernel/src/ext4.rs:L38-L38`：fn mount
    代码片段：`pub fn mount() -> Option<Ext4> {`
  - `kernel/src/fs/partition.rs:L5-L5`：fn mount
    代码片段：`fn mount(&self, prefix: &str);                                              // 获取文件树`
- 函数/符号名重合：与 2022 啊队队队 在“内存管理”维度发现 3 个同名定义：alloc, dealloc, sys_mmap。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/heap.rs:L86-L86`：fn alloc
    代码片段：`unsafe fn alloc(&self, layout: Layout) -> *mut u8 {`
  - `kernel/src/memory/page.rs:L49-L49`：fn alloc
    代码片段：`pub fn alloc(&mut self) -> Result<PhysPageNum, RuntimeError> {`
  - `kernel/src/heap.rs:L127-L127`：fn dealloc
    代码片段：`unsafe fn dealloc(&self, ptr: *mut u8, layout: Layout) {`
  - `kernel/src/memory/page.rs:L63-L63`：fn dealloc
    代码片段：`pub fn dealloc(&mut self, page: PhysPageNum) {`
- 与 2022 啊队队队 在“同步机制”维度发现片段级代码相似度 0.58 （token=0.54, structure=0.67）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：atomic, atomicbool, cell, core, ordering, sync, unsafecell, use。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L3-L7`：关键词命中
    代码片段：`use core::alloc::{GlobalAlloc, Layout}; use core::cell::UnsafeCell; use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate...`
  - `kernel/src/sync/mutex.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; use core::cell::UnsafeCell; use core::marker::Sync;`
- 函数/符号名重合：与 2022 啊队队队 在“系统调用”维度发现 4 个同名定义：sys_nanosleep, sys_mmap, sys_fcntl, sys_gettimeofday。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/src/syscall.rs:L321-L321`：fn sys_nanosleep
    代码片段：`fn sys_nanosleep(req: usize) -> isize {`
  - `kernel/src/sys_call/time.rs:L11-L11`：fn sys_nanosleep
    代码片段：`pub fn sys_nanosleep(&self, req_ptr: UserAddr<TimeSpec>, _rem_ptr: VirtAddr) -> Result<(), RuntimeError> {`
  - `kernel/src/syscall.rs:L338-L338`：fn sys_mmap
    代码片段：`fn sys_mmap(len: usize, flags: usize, fd: usize) -> isize {`
  - `kernel/src/sys_call/mm.rs:L37-L37`：fn sys_mmap
    代码片段：`pub fn sys_mmap(&self, start: usize, len: usize, _prot: usize,`
- 与 2022 啊队队队 在“系统调用”维度发现片段级代码相似度 0.73 （token=0.65, structure=0.89）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：arg0, asm, in, inlateout, let, ret, unsafe。（置信度：medium）
  证据：
  - `khal/src/riscv64.rs:L20-L24`：关键词命中
    代码片段：`let ret; unsafe { asm!("ecall", inlateout("a0") arg0 => ret, in("a7") eid, options(nostack)); } ret`
  - `device/src/sbi.rs:L19-L23`：关键词命中
    代码片段：`let mut ret; unsafe { asm!("ecall", in("a7") which, inlateout("a0") arg0 as i32 => ret,`
- 函数/符号名重合：与 2022 啊队队队 在“中断与异常”维度发现 1 个同名定义：set_timer。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `khal/src/riscv64.rs:L255-L255`：fn set_timer
    代码片段：`fn set_timer(future: u64) {`
  - `kernel/src/sbi.rs:L31-L31`：fn set_timer
    代码片段：`pub fn set_timer(time: usize) {`
- 函数/符号名重合：与 2022 啊队队队 在“调度与任务管理”维度发现 2 个同名定义：read, lseek。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/src/fs.rs:L391-L391`：fn read
    代码片段：`pub fn read(fd: usize, buf: usize, len: usize) -> isize {`
  - `kernel/src/task/fd_table.rs:L47-L47`：fn read
    代码片段：`pub fn read(&mut self, buf: &mut [u8]) -> usize {`
  - `kernel/src/fs.rs:L508-L508`：fn lseek
    代码片段：`pub fn lseek(fd: usize, offset: isize, whence: usize) -> isize {`
  - `kernel/src/task/fd_table.rs:L63-L63`：fn lseek
    代码片段：`pub fn lseek(&mut self, offset: usize, whence: usize) -> usize {`
- 文件路径重合：与 2022 啊队队队 在“文件系统”维度出现同名文件源码路径 `kernel/src/fs.rs` / `fatfs/src/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/fs.rs:L5-L9`：struct Node
    代码片段：`use alloc::vec::Vec; struct Node { name: String, data: Vec<u8>,`
  - `fatfs/src/fs.rs:L22-L26`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
- 文件路径重合：与 2022 啊队队队 在“内存管理”维度出现同名文件源码路径 `kernel/src/heap.rs` / `kernel/src/memory/heap.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L5-L9`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate 的大文件 + ELF 加载） const MIN: usize = 16; // 块粒度，满足 align<=16 的全部内核分配`
  - `kernel/src/memory/heap.rs:L3-L7`：关键词命中
    代码片段：`// 堆大小 const HEAP_SIZE: usize = 0x0008_0000; // 堆空间`

## 相似点

- 与 2022 啊队队队 同属 ucore-variant 风格。（置信度：medium）
- 与 2022 啊队队队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/virtio_blk.rs:L1-L3`：关键词命中
    代码片段：`//! 最小 virtio-blk 驱动：探测 + 读扇区。split virtqueue 三环放在一个连续页对齐结构里 //! （desc@0 / avail@256 / used@4096）。恒等映射下内核 VA = PA，故 ring/缓冲地址即设备 //! 可用的物理地址，无需翻译或弹跳缓冲。`
  - `kernel/src/ext4.rs:L1-L4`：关键词命中
    代码片段：`//! 最小 ext4 只读：超级块 + inode(extent, depth 0) + 根目录项 + 按名读文件。 //! 仅支持 4096 块大小（赛事与本地镜像默认）。块经 virtio-blk 读取。 use crate::virtio_blk; use alloc::string::String;`
- 与 2022 啊队队队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/ext4.rs:L1-L3`：关键词命中
    代码片段：`//! 最小 ext4 只读：超级块 + inode(extent, depth 0) + 根目录项 + 按名读文件。 //! 仅支持 4096 块大小（赛事与本地镜像默认）。块经 virtio-blk 读取。 use crate::virtio_blk;`
  - `fatfs/src/fs.rs:L22-L26`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
- 与 2022 啊队队队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/main.rs:L30-L34`：关键词命中
    代码片段：`khal::init_trap(); khal::test_breakpoint(); khal::kprintln!("[KunikOS] trap round-trip ok"); khal::init_paging();`
  - `kernel/src/syscall.rs:L1-L4`：关键词命中
    代码片段：`//! 系统调用分发（架构无关，语义对齐 Linux）。 //! khal 的 trap 入口解出 (号, 参数) 后调用 kunikos_syscall，返回值写回 a0。 use khal::console_putchar;`
- 与 2022 啊队队队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L5-L9`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate 的大文件 + ELF 加载） const MIN: usize = 16; // 块粒度，满足 align<=16 的全部内核分配`
  - `khal/src/riscv64.rs:L117-L121`：关键词命中
    代码片段：`#[repr(C, align(4096))] struct PageTable([u64; 512]); static mut ROOT: PageTable = PageTable([0; 512]);`
- 与 2022 啊队队队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs.rs:L260-L264`：关键词命中
    代码片段：`} // 合成 /proc、/sys 等只读伪文件（LTP tst 框架启动要读）。 /// /proc/<pid>/X 归一为 /proc/self/X（数字 pid 段映射到当前进程视图）。 fn normalize_proc(name: &str) -> Option<String> {`
  - `kernel/src/main.rs:L144-L148`：关键词命中
    代码片段：`"waitpid", "write", "yield", ]; khal::kprintln!("#### OS COMP TEST GROUP START basic ####");`
- 与 2022 啊队队队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/heap.rs:L3-L7`：关键词命中
    代码片段：`use core::alloc::{GlobalAlloc, Layout}; use core::cell::UnsafeCell; use core::sync::atomic::{AtomicBool, Ordering}; const HEAP_SIZE: usize = 0x400_0000; // 64 MiB（容纳测试 ftruncate...`
  - `kernel/src/virtio_blk.rs:L5-L9`：关键词命中
    代码片段：`//! 传输层按架构分流：rv64 走 virtio-mmio（0x10001000 槽位扫描）；la64 走 virtio-pci //! （ECAM 枚举 + BAR4 现代能力）。队列布局与 'read()' 主体两架构共用，仅 init/notify 不同。 use core::sync::atomic::{Ordering, fence};...`
- 与 2022 啊队队队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `khal/src/riscv64.rs:L20-L24`：关键词命中
    代码片段：`let ret; unsafe { asm!("ecall", inlateout("a0") arg0 => ret, in("a7") eid, options(nostack)); } ret`
  - `kernel/src/sbi.rs:L19-L23`：关键词命中
    代码片段：`let mut ret; unsafe { asm!("ecall", in("a7") which, inlateout("a0") arg0 as i32 => ret,`
- 与 2022 啊队队队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 啊队队队 的语言构成不同：待测作品为 {'build': 72, 'markdown': 868, 'rust': 4095, 'toml': 8, 'c': 6}，历史样本为 {'yaml': 19, 'json': 20, 'build': 91, 'markdown': 995, 'text': 19, 'toml': 2, 'rust': 13878, 'asm': 227}。（置信度：medium）

## 可能创新点

- 待测作品包含 153 个历史样本 2022 啊队队队 中未见的独特函数/类型定义（如 `__resume_user`, `sys_clone`, `__la_enter_user`, `spawn_child`, `pwrite`, `sys_rt_sigprocmask`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `khal/src/riscv64.rs:L562-L562`：fn __resume_user
    代码片段：`fn __resume_user(ctx: *mut KernelContext, tf: *const TrapFrame, kstack_top: usize) -> usize;`
  - `kernel/src/syscall.rs:L158-L158`：fn sys_clone
    代码片段：`fn sys_clone(flags: usize, new_sp: usize, tls: usize, ctid: usize) -> isize {`

## 附录：核验摘要

- 关键结论数：25
- 含证据关键结论数：25（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
