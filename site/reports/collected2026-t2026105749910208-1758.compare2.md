# 2026 T2026105749910208 比较报告

- 对比历史仓库：2023 BiteTheDisk
- 生成时间：2026-07-02T09:03:28.968499+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2023 BiteTheDisk（来源：赛事历史作品）：画像相似度 score=8.92；同属 rcore-variant 风格; 语言构成相似度 0.63; OS 维度重合度 1.00; 代码规模接近度 0.65

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2023 BiteTheDisk 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console.rs:L23-L27`：关键词命中
    代码片段：`macro_rules! print { ($($arg:tt)*) => ({ $crate::console::_print(format_args!($($arg)*)); }); }`
  - `os/src/drivers/hal.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO 驱动所用的 ['virtio_drivers::Hal'] 实现。 //! //! LoongArch 在 PG=1 + DMW 模式下，裸物理地址不可直接解引用：`
  - `kernel/src/console.rs:L36-L40`：关键词命中
    代码片段：`macro_rules! print { ($fmt:literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); }; }`
  - `kernel/src/drivers/mod.rs:L25-L29`：关键词命中
    代码片段：`}); /// Initialize platform specific device drivers. pub fn init() { #[cfg(feature = "fu740")]`
- 与 2023 BiteTheDisk 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/fd.rs:L12-L16`：关键词命中
    代码片段：`use crate::fs::block_dev; use crate::fs::ext4_vol; use crate::fs::vfs::VfsNodeKind; use crate::fs::{MemFileBacking, MemNodeMetadata}; use crate::task::wait_queue::WaitKey;`
  - `os/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod fd; pub mod vfat; pub mod vfs; #[allow(unused_imports)]`
  - `crates/nix/src/fs.rs:L89-L93`：关键词命中
    代码片段：`#[derive(Debug)] pub struct Dirent { d_ino: usize,             // inode number d_off: isize,             // offset from 0 to next dirent d_reclen: u16,            // length of t...`
  - `kernel/src/fs/page.rs:L29-L33`：关键词命中
    代码片段：`/// Data block state pub data_states: [DataState; PAGE_SIZE / BLOCK_SIZE], /// Inode that this page related to inode: Weak<VirtFile>, }`
- 与 2023 BiteTheDisk 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/timer.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use lazy_static::lazy_static; use polyhal::timer::current_time; use spin::Mutex;`
  - `os/src/trap/mod.rs:L2-L6`：关键词命中
    代码片段：`use lazy_static::lazy_static; use polyhal_trap::trap::TrapType; use polyhal_trap::trapframe::{TrapFrame, TrapFrameArgs}; use spin::Mutex;`
  - `kernel/src/timer.rs:L48-L52`：关键词命中
    代码片段：`} /// Setup the next time interrupt time. pub fn set_next_trigger() { set_timer((get_time() + CLOCK_FREQ / TIME_SLICE) as u64);`
  - `kernel/src/trap/mod.rs:L3-L7`：关键词命中
    代码片段：`pub use context::*; pub use handler::*; use riscv::register::scause::{Interrupt, Trap}; use crate::consts::TRAMPOLINE;`
- 与 2023 BiteTheDisk 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod elf_loader; pub mod frame_allocator; pub mod heap_allocator; pub mod map_area;`
  - `os/src/syscall/mm.rs:L2-L6`：关键词命中
    代码片段：`use crate::config::{PAGE_SIZE, USER_HEAP_START, USER_STACK_TOP}; use crate::fs::{ext4_vol, fd::FileDescriptor}; use crate::mm::frame_allocator::{self, FrameTracker}; use crate::...`
  - `kernel/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod kernel_heap_allocator; mod kvmm;`
  - `kernel/src/mm/kvmm.rs:L6-L10`：关键词命中
    代码片段：`mm::{ vm_area::{VmArea, VmAreaType}, MapPermission, MapType, MemorySet, }, };`
- 与 2023 BiteTheDisk 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`pub mod pid; pub mod processor; pub mod task; pub mod wait_queue;`
  - `os/src/task/task.rs:L10-L14`：关键词命中
    代码片段：`}; use crate::mm::memory_set::MemorySet; use crate::task::context::TaskContext; use crate::task::pid::Pid; use crate::utils::error::SysErrNo;`
  - `kernel/src/task/mod.rs:L7-L11`：关键词命中
    代码片段：`mod signal; mod switch; mod task; pub use context::*; pub use id::*;`
  - `kernel/src/task/task.rs:L66-L70`：关键词命中
    代码片段：`pub parent: Option<Weak<TaskControlBlock>>, // child process and thread collection pub children: Vec<Arc<TaskControlBlock>>,`
- 与 2023 BiteTheDisk 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/main.rs:L166-L170`：关键词命中
    代码片段：`// Guard against recursive calls use core::sync::atomic::{AtomicBool, Ordering}; static INIT_GUARD: AtomicBool = AtomicBool::new(false); if INIT_GUARD.swap(true, Ordering::SeqCs...`
  - `os/src/fs/fd.rs:L6-L10`：关键词命中
    代码片段：`use alloc::sync::Arc; use alloc::vec::Vec; use core::sync::atomic::{AtomicUsize, Ordering}; use lazy_static::lazy_static; use spin::Mutex;`
  - `kernel/vendor/spin/src/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! Locks that have the same behaviour as a mutex. //! //! The ['Mutex'] in the root of the crate, can be configured using the 'ticket_mutex' feature.`
  - `kernel/vendor/lock_api/src/lib.rs:L6-L10`：关键词命中
    代码片段：`// copied, modified, or distributed except according to those terms. //! This library provides type-safe and fully-featured 'Mutex' and 'RwLock' //! types which wrap a simple ra...`
- 与 2023 BiteTheDisk 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L872-L876`：关键词命中
    代码片段：`} Err(SysErrNo::EPIPE) if total == 0 => { crate::syscall::signal::send_sigpipe_to_current(); return Err(SysErrNo::EPIPE); }`
  - `os/src/syscall/mm.rs:L464-L468`：关键词命中
    代码片段：`) -> SyscallRet { log::info!( "[syscall] mmap(addr={:#x}, len={:#x}, prot={:#x}, flags={:#x}, fd={}, off={:#x})", addr, length,`
  - `kernel/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Syscall Implementation //! //! About syscall id in RISC-V: https://marcin.juszkiewicz.com.pl/download/tables/syscalls.html`
  - `kernel/src/trap/trampoline.S:L139-L142`：关键词命中
    代码片段：`.align 2 user_sigreturn: li	a7, 139 # syscall SIGRETURN ecall`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2023 BiteTheDisk 在“设备驱动”维度发现片段级代码相似度 0.85 （token=0.77, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：c, fmt, fn, for, impl, in, mut, result。（置信度：high）
  证据：
  - `os/src/console.rs:L6-L10`：fn write_str
    代码片段：`impl Write for Stdout { fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.bytes() { DebugConsole::putchar(c);`
  - `kernel/src/console.rs:L17-L21`：fn write_str
    代码片段：`impl Write for Stdout { fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.chars() { console_putchar(c as i32);`
- 与 2023 BiteTheDisk 在“设备驱动”维度发现片段级代码相似度 0.74 （token=0.73, structure=0.77）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：arg, console, crate, format_args, macro_rules, print, tt。（置信度：medium）
  证据：
  - `os/src/console.rs:L23-L27`：关键词命中
    代码片段：`macro_rules! print { ($($arg:tt)*) => ({ $crate::console::_print(format_args!($($arg)*)); }); }`
  - `kernel/src/console.rs:L36-L40`：关键词命中
    代码片段：`macro_rules! print { ($fmt:literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); }; }`
- 与 2023 BiteTheDisk 在“中断与异常”维度发现片段级代码相似度 0.63 （token=0.56, structure=0.75）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, get_time, pub, usize。（置信度：medium）
  证据：
  - `os/src/timer.rs:L32-L36`：fn get_time
    代码片段：`/// 获取当前时间戳（毫秒） pub fn get_time() -> usize { current_time().as_millis() as usize }`
  - `kernel/src/timer.rs:L19-L23`：fn get_time
    代码片段：`/// /// - 'mtime': 统计处理器自上电以来经过了多少个内置时钟的时钟周期,64bit pub fn get_time() -> usize { time::read() }`
- 与 2023 BiteTheDisk 在“中断与异常”维度发现片段级代码相似度 0.61 （token=0.53, structure=0.75）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, get_time, pub, usize。（置信度：medium）
  证据：
  - `os/src/timer.rs:L32-L36`：fn get_time
    代码片段：`/// 获取当前时间戳（毫秒） pub fn get_time() -> usize { current_time().as_millis() as usize }`
  - `kernel/src/timer.rs:L24-L28`：fn get_time_ms
    代码片段：`/// 获取CPU上电时间(单位: ms) pub fn get_time_ms() -> usize { get_time() / (CLOCK_FREQ / MSEC_PER_SEC) }`
- 函数/符号名重合：与 2023 BiteTheDisk 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/console.rs:L8-L8`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `kernel/src/console.rs:L19-L19`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 结构体/类型重合：与 2023 BiteTheDisk 在“设备驱动”维度发现 1 个同名定义：Stdout。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/console.rs:L5-L5`：struct Stdout
    代码片段：`struct Stdout;`
  - `kernel/src/console.rs:L7-L7`：struct Stdout
    代码片段：`struct Stdout;`
- 文件路径重合：与 2023 BiteTheDisk 在“设备驱动”维度出现同名文件源码路径 `os/src/console.rs` / `kernel/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/console.rs:L23-L27`：关键词命中
    代码片段：`macro_rules! print { ($($arg:tt)*) => ({ $crate::console::_print(format_args!($($arg)*)); }); }`
  - `kernel/src/console.rs:L36-L40`：关键词命中
    代码片段：`macro_rules! print { ($fmt:literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); }; }`
- 文件路径重合：与 2023 BiteTheDisk 在“中断与异常”维度出现同名文件源码路径 `os/src/timer.rs` / `kernel/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/timer.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use lazy_static::lazy_static; use polyhal::timer::current_time; use spin::Mutex;`
  - `kernel/src/timer.rs:L48-L52`：关键词命中
    代码片段：`} /// Setup the next time interrupt time. pub fn set_next_trigger() { set_timer((get_time() + CLOCK_FREQ / TIME_SLICE) as u64);`
- 文件路径重合：与 2023 BiteTheDisk 在“内存管理”维度出现同名文件源码路径 `os/src/syscall/mm.rs` / `crates/nix/src/mm.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/syscall/mm.rs:L2-L6`：关键词命中
    代码片段：`use crate::config::{PAGE_SIZE, USER_HEAP_START, USER_STACK_TOP}; use crate::fs::{ext4_vol, fd::FileDescriptor}; use crate::mm::frame_allocator::{self, FrameTracker}; use crate::...`
  - `crates/nix/src/mm.rs:L4-L8`：struct MmapProts
    代码片段：`bitflags! { #[derive(Clone, Copy, Debug)] pub struct MmapProts: usize { const PROT_NONE = 0;  // Unaccessible. Used for guard pages. const PROT_READ = 1 << 0;`
- 文件路径重合：与 2023 BiteTheDisk 在“调度与任务管理”维度出现同名文件源码路径 `os/src/task/task.rs` / `kernel/src/task/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/task/task.rs:L10-L14`：关键词命中
    代码片段：`}; use crate::mm::memory_set::MemorySet; use crate::task::context::TaskContext; use crate::task::pid::Pid; use crate::utils::error::SysErrNo;`
  - `kernel/src/task/task.rs:L66-L70`：关键词命中
    代码片段：`pub parent: Option<Weak<TaskControlBlock>>, // child process and thread collection pub children: Vec<Arc<TaskControlBlock>>,`

## 相似点

- 与 2023 BiteTheDisk 同属 rcore-variant 风格。（置信度：medium）
- 与 2023 BiteTheDisk 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console.rs:L23-L27`：关键词命中
    代码片段：`macro_rules! print { ($($arg:tt)*) => ({ $crate::console::_print(format_args!($($arg)*)); }); }`
  - `os/src/drivers/hal.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO 驱动所用的 ['virtio_drivers::Hal'] 实现。 //! //! LoongArch 在 PG=1 + DMW 模式下，裸物理地址不可直接解引用：`
- 与 2023 BiteTheDisk 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/fd.rs:L12-L16`：关键词命中
    代码片段：`use crate::fs::block_dev; use crate::fs::ext4_vol; use crate::fs::vfs::VfsNodeKind; use crate::fs::{MemFileBacking, MemNodeMetadata}; use crate::task::wait_queue::WaitKey;`
  - `os/src/fs/mod.rs:L6-L10`：关键词命中
    代码片段：`pub mod fd; pub mod vfat; pub mod vfs; #[allow(unused_imports)]`
- 与 2023 BiteTheDisk 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/timer.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use lazy_static::lazy_static; use polyhal::timer::current_time; use spin::Mutex;`
  - `os/src/trap/mod.rs:L2-L6`：关键词命中
    代码片段：`use lazy_static::lazy_static; use polyhal_trap::trap::TrapType; use polyhal_trap::trapframe::{TrapFrame, TrapFrameArgs}; use spin::Mutex;`
- 与 2023 BiteTheDisk 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod elf_loader; pub mod frame_allocator; pub mod heap_allocator; pub mod map_area;`
  - `os/src/syscall/mm.rs:L2-L6`：关键词命中
    代码片段：`use crate::config::{PAGE_SIZE, USER_HEAP_START, USER_STACK_TOP}; use crate::fs::{ext4_vol, fd::FileDescriptor}; use crate::mm::frame_allocator::{self, FrameTracker}; use crate::...`
- 与 2023 BiteTheDisk 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`pub mod pid; pub mod processor; pub mod task; pub mod wait_queue;`
  - `os/src/task/task.rs:L10-L14`：关键词命中
    代码片段：`}; use crate::mm::memory_set::MemorySet; use crate::task::context::TaskContext; use crate::task::pid::Pid; use crate::utils::error::SysErrNo;`
- 与 2023 BiteTheDisk 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/main.rs:L166-L170`：关键词命中
    代码片段：`// Guard against recursive calls use core::sync::atomic::{AtomicBool, Ordering}; static INIT_GUARD: AtomicBool = AtomicBool::new(false); if INIT_GUARD.swap(true, Ordering::SeqCs...`
  - `os/src/fs/fd.rs:L6-L10`：关键词命中
    代码片段：`use alloc::sync::Arc; use alloc::vec::Vec; use core::sync::atomic::{AtomicUsize, Ordering}; use lazy_static::lazy_static; use spin::Mutex;`
- 与 2023 BiteTheDisk 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L872-L876`：关键词命中
    代码片段：`} Err(SysErrNo::EPIPE) if total == 0 => { crate::syscall::signal::send_sigpipe_to_current(); return Err(SysErrNo::EPIPE); }`
  - `os/src/syscall/mm.rs:L464-L468`：关键词命中
    代码片段：`) -> SyscallRet { log::info!( "[syscall] mmap(addr={:#x}, len={:#x}, prot={:#x}, flags={:#x}, fd={}, off={:#x})", addr, length,`
- 与 2023 BiteTheDisk 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2023 BiteTheDisk 的语言构成不同：待测作品为 {'build': 7812, 'markdown': 18581, 'yaml': 3458, 'rust': 467557, 'json': 282429, 'asm': 560, 'c': 239, 'toml': 125, 'text': 472}，历史样本为 {'json': 83, 'build': 3797, 'markdown': 14373, 'toml': 8819, 'rust': 428744, 'asm': 519, 'c': 48, 'yaml': 427}。（置信度：medium）

## 可能创新点

- 待测作品包含 375 个历史样本 2023 BiteTheDisk 中未见的独特函数/类型定义（如 `cached_pblock_for_clean_read`, `probe_first_virtio_disk_from_dt`, `allocate`, `clean_page_cache_frame`, `clean_page_cached_read`, `set_owner_path`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `os/src/fs/ext4_vol.rs:L873-L873`：fn cached_pblock_for_clean_read
    代码片段：`fn cached_pblock_for_clean_read(fs: &Ext4, ino: u32, lblock: u32, max_run_len: u32) -> Option<u64> {`
  - `os/src/drivers/virtio_mmio_blk.rs:L118-L118`：fn probe_first_virtio_disk_from_dt
    代码片段：`pub unsafe fn probe_first_virtio_disk_from_dt(dtb_ptr: usize) -> Option<Arc<dyn RawBlockDevice>> {`

## 附录：核验摘要

- 关键结论数：25
- 含证据关键结论数：25（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
