# 2026 T202610183999672 比较报告

- 对比历史仓库：2025 如有名字队
- 生成时间：2026-07-02T04:59:32.781731+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 如有名字队（来源：赛事历史作品）：画像相似度 score=9.44；同属 rcore-variant 风格; 语言构成相似度 0.72; OS 维度重合度 1.00; 代码规模接近度 0.99

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 如有名字队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/console.rs:L5-L9`：关键词命中
    代码片段：`use log::{self, Level, LevelFilter, Log, Metadata, Record}; // la64: console_putchar 直接写 UART，无 SBI 序列化保护，因此需要 irq-save // 临界区确保整条 print 输出原子。rv64 虽 SBI ecall 有单字符原子性，但全局 // 关中断...`
  - `os/src/drivers/mod.rs:L1-L5`：关键词命中
    代码片段：`pub mod block; pub mod net; pub mod serial; pub use block::BLOCK_DEVICES;`
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use core::fmt::{self, Write};`
  - `os/src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`pub mod device; use device::*; pub mod disk;`
- 与 2025 如有名字队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L23-L27`：关键词命中
    代码片段：`// Xein add this pub mod dirent; // file_descriptor module removed — migrated to vfs::File mod inode; mod timestamp;`
  - `os/src/fs/poll.rs:L3-L7`：关键词命中
    代码片段：`use crate::{ config::PAGE_SIZE, fs::vfs::{event::EPollEvent, FdTable, File, PollWaitQueue}, mm::{UserPtr, UserSlice}, net::config::NET_INTERFACE,`
  - `os/src/fs/mod.rs:L3-L7`：关键词命中
    代码片段：`pub mod fsidx; mod fstruct; mod inode; mod mount; mod pipe;`
  - `os/src/fs/pipe.rs:L172-L176`：关键词命中
    代码片段：`pub fn splice_from_pipe( pipe: Arc<dyn File>, inode: Arc<OSInode>, offset: *const isize, len: usize,`
- 与 2025 如有名字队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/hal/arch/riscv/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V trap 分发和返回路径。 //! //! 处理 syscall、缺页、设备中断、timer interrupt，并在返回用户态前完成信号`
  - `os/src/hal/arch/riscv/trap/context.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V trap context 和用户信号上下文布局。 //! //! 该文件定义从 trap 入口保存的通用寄存器、状态寄存器和 signal frame 需要的`
  - `os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! Timer module using the 'Time' abstraction layer. use polyhal::time::Time;`
  - `os/src/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Kernel trap and interrupt handling pub mod interrupts; use crate::config::TIMER_IRQ;`
- 与 2025 如有名字队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L7-L11`：关键词命中
    代码片段：`//! # TLB //! //! 任何修改 PTE 的路径必须立即刷新对应 TLB，或明确使用 //! '*_no_flush' 批量接口并在批量结束后调用 'flush_tlb()'。 //!`
  - `os/src/mm/vma.rs:L2-L6`：关键词命中
    代码片段：`//! //! 'Vma' 保存地址区间、权限、mmap flag、文件后端以及页帧状态表，并提供单页映射、 //! 解除映射、fork 继承、CoW、范围伸缩和 OOM 回收等操作。 //! //! # Semantics`
  - `os/src/mm/mod.rs:L1-L3`：关键词命中
    代码片段：`mod frame_allocator; mod group; mod heap_allocator;`
  - `os/src/mm/shm.rs:L8-L12`：关键词命中
    代码片段：`}; use super::{frame_alloc, FrameTracker, MapPermission}; use lazy_static::*;`
- 与 2025 如有名字队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L28-L32`：关键词命中
    代码片段：`pub mod signal; mod sleep; mod task; pub mod threads;`
  - `os/src/task/pid.rs:L12-L16`：关键词命中
    代码片段：`use spin::Mutex; /// Matches the '/proc/sys/kernel/pid_max' value exposed by procfs. pub const DEFAULT_PID_MAX: usize = 32_768; const RESERVED_PID_REUSE_FLOOR: usize = 300;`
  - `os/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Task management implementation //! //! Everything about task management, like starting and switching tasks is`
  - `os/src/task/task.rs:L12-L16`：关键词命中
    代码片段：`use crate::syscall::CloneFlags; use crate::syscall::RLimit; use crate::task::aux_::{Aux, AuxType}; use crate::timer::TimeData; use crate::utils::{get_abs_path, is_abs_path, SysE...`
- 与 2025 如有名字队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/vfs/posix_lock.rs:L1-L3`：关键词命中
    代码片段：`//! Sharded POSIX record lock manager with blocking support. //! //! Uses 53 shards keyed by '(dev_id, inode_id)'.  Each shard maps a 'LockKey'`
  - `os/src/timer.rs:L432-L436`：关键词命中
    代码片段：`// ───────────────────────────────────────────────────────── use core::sync::atomic::{AtomicU64, Ordering as AtomicOrdering}; pub trait TimeSource {`
  - `os/src/fs/mod.rs:L21-L25`：关键词命中
    代码片段：`use alloc::sync::Arc; use lazy_static::lazy_static; use spin::Mutex; use usedfiles::*;`
  - `os/src/mm/shm.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{collections::BTreeMap, sync::Arc, vec::Vec}; use spin::Mutex; use crate::{`
- 与 2025 如有名字队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L9-L13`：关键词命中
    代码片段：`UserPtrMut, UserSlice, VirtAddr, }; use crate::syscall::utils::wait_io_core; use crate::task::{ current_task, current_user_token, find_process_by_pid, find_task_by_tid,`
  - `os/src/syscall/mod.rs:L17-L21`：关键词命中
    代码片段：`sys_timerfd_create, sys_timerfd_gettime, sys_timerfd_settime, TimerFdSpec, }; use crate::net::syscall::*; use core::convert::TryFrom; use flock::*;`
  - `os/src/syscall/fs.rs:L15-L19`：关键词命中
    代码片段：`}; use crate::signal::SignalFlags; use crate::syscall::option::{ fcntl_cmd_name, FdSet, F_DUPFD, F_DUPFD_CLOEXEC, F_GETFD, F_GETFL, F_SETFD, };`
  - `os/src/syscall/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Implementation of syscalls //! //! The single entry point to all system calls, ['syscall()'], is called //! whenever userspace wishes to perform a system call using the 'eca...`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 如有名字队 在“设备驱动”维度发现片段级代码相似度 0.79 （token=0.73, structure=0.90）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：args, arguments, fmt, fn, print, pub, unwrap, write_fmt。（置信度：high）
  证据：
  - `os/src/console.rs:L28-L32`：fn print
    代码片段：`} pub fn print(args: fmt::Arguments) { let irq_state = local_irq_save(); KernelOutput.write_fmt(args).unwrap();`
  - `os/src/console.rs:L15-L19`：fn print
    代码片段：`} pub fn print(args: fmt::Arguments) { Stdout.write_fmt(args).unwrap(); }`
- 与 2025 如有名字队 在“系统调用”维度发现片段级代码相似度 0.78 （token=0.67, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：current_task, fn, let, pub, task, unwrap, usize。（置信度：high）
  证据：
  - `os/src/syscall/fs.rs:L1203-L1207`：fn sys_getcwd
    代码片段：`} pub fn sys_getcwd(buf: usize, size: usize) -> isize { info!("[sys_getcwd] buf={:#x}, size={}", buf, size); let task = current_task().unwrap();`
  - `os/src/syscall/fs.rs:L66-L70`：fn sys_dup
    代码片段：`} pub fn sys_dup(fd: usize) -> SyscallRet { trace!("[sys_dup] called with fd={}", fd); let task = current_task().unwrap();`
- 与 2025 如有名字队 在“设备驱动”维度发现片段级代码相似度 0.75 （token=0.70, structure=0.85）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：c, fmt, fn, for, impl, in, mut, result。（置信度：medium）
  证据：
  - `os/src/console.rs:L11-L15`：fn write_str
    代码片段：`impl Write for KernelOutput { fn write_str(&mut self, s: &str) -> fmt::Result { let mut i = 0; for c in s.chars() {`
  - `os/src/console.rs:L7-L11`：fn write_str
    代码片段：`impl Write for Stdout { fn write_str(&mut self, s: &str) -> fmt::Result { for c in s.bytes() { DebugConsole::putchar(c);`
- 与 2025 如有名字队 在“调度与任务管理”维度发现片段级代码相似度 0.61 （token=0.53, structure=0.75）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：c, pub, repr, struct。（置信度：medium）
  证据：
  - `os/src/task/elf.rs:L72-L76`：struct AuxvEntry
    代码片段：`#[repr(C)] /// 写入用户栈的单个 auxv 条目。 pub struct AuxvEntry { auxv_type: AuxvType, auxv_val: usize,`
  - `os/src/task/context.rs:L3-L7`：关键词命中
    代码片段：`#[repr(C)] /// task context structure containing some registers pub struct TaskContext { /// return address ( e.g. __restore ) of __switch ASM function`
- 文件路径重合：与 2025 如有名字队 在“设备驱动”维度出现同路径源码路径 `os/src/console.rs` / `os/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/console.rs:L5-L9`：关键词命中
    代码片段：`use log::{self, Level, LevelFilter, Log, Metadata, Record}; // la64: console_putchar 直接写 UART，无 SBI 序列化保护，因此需要 irq-save // 临界区确保整条 print 输出原子。rv64 虽 SBI ecall 有单字符原子性，但全局 // 关中断...`
  - `os/src/console.rs:L1-L3`：关键词命中
    代码片段：`//! SBI console driver, for text output use core::fmt::{self, Write};`
- 文件路径重合：与 2025 如有名字队 在“中断与异常”维度出现同路径源码路径 `os/src/timer.rs` / `os/src/timer.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/timer.rs:L30-L34`：fn clock_freq
    代码片段：`/// Hardware counter frequency in Hz. #[inline(always)] pub fn clock_freq() -> u64 { get_clock_freq() as u64 }`
  - `os/src/timer.rs:L1-L3`：关键词命中
    代码片段：`//! Timer module using the 'Time' abstraction layer. use polyhal::time::Time;`
- 文件路径重合：与 2025 如有名字队 在“系统调用”维度出现同路径源码路径 `os/src/syscall/fs.rs` / `os/src/syscall/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `os/src/syscall/fs.rs:L9-L13`：关键词命中
    代码片段：`UserPtrMut, UserSlice, VirtAddr, }; use crate::syscall::utils::wait_io_core; use crate::task::{ current_task, current_user_token, find_process_by_pid, find_task_by_tid,`
  - `os/src/syscall/fs.rs:L15-L19`：关键词命中
    代码片段：`}; use crate::signal::SignalFlags; use crate::syscall::option::{ fcntl_cmd_name, FdSet, F_DUPFD, F_DUPFD_CLOEXEC, F_GETFD, F_GETFL, F_SETFD, };`

## 相似点

- 与 2025 如有名字队 同属 rcore-variant 风格。（置信度：medium）
- 与 2025 如有名字队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/console.rs:L5-L9`：关键词命中
    代码片段：`use log::{self, Level, LevelFilter, Log, Metadata, Record}; // la64: console_putchar 直接写 UART，无 SBI 序列化保护，因此需要 irq-save // 临界区确保整条 print 输出原子。rv64 虽 SBI ecall 有单字符原子性，但全局 // 关中断...`
  - `os/src/drivers/mod.rs:L1-L5`：关键词命中
    代码片段：`pub mod block; pub mod net; pub mod serial; pub use block::BLOCK_DEVICES;`
- 与 2025 如有名字队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/mod.rs:L23-L27`：关键词命中
    代码片段：`// Xein add this pub mod dirent; // file_descriptor module removed — migrated to vfs::File mod inode; mod timestamp;`
  - `os/src/fs/poll.rs:L3-L7`：关键词命中
    代码片段：`use crate::{ config::PAGE_SIZE, fs::vfs::{event::EPollEvent, FdTable, File, PollWaitQueue}, mm::{UserPtr, UserSlice}, net::config::NET_INTERFACE,`
- 与 2025 如有名字队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/hal/arch/riscv/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V trap 分发和返回路径。 //! //! 处理 syscall、缺页、设备中断、timer interrupt，并在返回用户态前完成信号`
  - `os/src/hal/arch/riscv/trap/context.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V trap context 和用户信号上下文布局。 //! //! 该文件定义从 trap 入口保存的通用寄存器、状态寄存器和 signal frame 需要的`
- 与 2025 如有名字队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm/mod.rs:L7-L11`：关键词命中
    代码片段：`//! # TLB //! //! 任何修改 PTE 的路径必须立即刷新对应 TLB，或明确使用 //! '*_no_flush' 批量接口并在批量结束后调用 'flush_tlb()'。 //!`
  - `os/src/mm/vma.rs:L2-L6`：关键词命中
    代码片段：`//! //! 'Vma' 保存地址区间、权限、mmap flag、文件后端以及页帧状态表，并提供单页映射、 //! 解除映射、fork 继承、CoW、范围伸缩和 OOM 回收等操作。 //! //! # Semantics`
- 与 2025 如有名字队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/task/mod.rs:L28-L32`：关键词命中
    代码片段：`pub mod signal; mod sleep; mod task; pub mod threads;`
  - `os/src/task/pid.rs:L12-L16`：关键词命中
    代码片段：`use spin::Mutex; /// Matches the '/proc/sys/kernel/pid_max' value exposed by procfs. pub const DEFAULT_PID_MAX: usize = 32_768; const RESERVED_PID_REUSE_FLOOR: usize = 300;`
- 与 2025 如有名字队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/vfs/posix_lock.rs:L1-L3`：关键词命中
    代码片段：`//! Sharded POSIX record lock manager with blocking support. //! //! Uses 53 shards keyed by '(dev_id, inode_id)'.  Each shard maps a 'LockKey'`
  - `os/src/timer.rs:L432-L436`：关键词命中
    代码片段：`// ───────────────────────────────────────────────────────── use core::sync::atomic::{AtomicU64, Ordering as AtomicOrdering}; pub trait TimeSource {`
- 与 2025 如有名字队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall/fs.rs:L9-L13`：关键词命中
    代码片段：`UserPtrMut, UserSlice, VirtAddr, }; use crate::syscall::utils::wait_io_core; use crate::task::{ current_task, current_user_token, find_process_by_pid, find_task_by_tid,`
  - `os/src/syscall/mod.rs:L17-L21`：关键词命中
    代码片段：`sys_timerfd_create, sys_timerfd_gettime, sys_timerfd_settime, TimerFdSpec, }; use crate::net::syscall::*; use core::convert::TryFrom; use flock::*;`
- 与 2025 如有名字队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 如有名字队 的语言构成不同：待测作品为 {'json': 372, 'markdown': 99153, 'yaml': 93, 'build': 12118, 'c': 409, 'toml': 9933, 'rust': 981778, 'asm': 1177, 'text': 1086, 'make': 369}，历史样本为 {'json': 281978, 'build': 9776, 'text': 482, 'markdown': 22278, 'toml': 9717, 'yaml': 87, 'c': 27884, 'asm': 928, 'rust': 762578, 'make': 664}。（置信度：medium）

## 可能创新点

- 待测作品包含 239 个历史样本 2025 如有名字队 中未见的独特函数/类型定义（如 `sys_linkat`, `version_and_date_from_rustc_verbose_version`, `sys_socketpair`, `socketpair`, `sys_timer_create`, `version_and_date_from_rustc_version`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `user/src/syscall.rs:L535-L535`：fn sys_linkat
    代码片段：`pub fn sys_linkat(olddirfd: isize, oldpath: &str, newdirfd: isize, newpath: &str, flags: u32) -> isize {`
  - `user/vendor/version_check/src/lib.rs:L138-L138`：fn version_and_date_from_rustc_verbose_version
    代码片段：`fn version_and_date_from_rustc_verbose_version(s: &str) -> (Option<String>, Option<String>) {`

## 附录：核验摘要

- 关键结论数：22
- 含证据关键结论数：22（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
