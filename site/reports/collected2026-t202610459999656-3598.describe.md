# 2026 T202610459999656 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t202610459999656-3598`
- 风格：rcore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：2185
- 代码/文本行数：732189
- 主要语言：rust 588336 LOC, c 119018 LOC, markdown 11526 LOC, build 6236 LOC, text 6096 LOC

## 总览

2026 T202610459999656 是一个 rcore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 588336 LOC, c: 119018 LOC, markdown: 11526 LOC, build: 6236 LOC。仓库包含 2185 个已扫描文件、约 732189 行可分析文本，当前抽取到 45232 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, os/Cargo.toml, os/build.rs, os/third_party/rsext4/Cargo.toml, os/third_party/rsext4/qemu_test/Makefile。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`.PHONY: all clean kernel-rv kernel-la prepare-assets-rv prepare-assets-la RV_TARGET := riscv64gc-unknown-none-elf`
  - `os/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "nexus-os" version = "0.1.0"`
  - `os/build.rs:L1-L3`：构建入口
    代码片段：`use std::{ env, fs, path::{Path, PathBuf},`
  - `os/third_party/rsext4/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "rsext4" version = "0.1.0"`
  - `os/third_party/rsext4/qemu_test/Makefile:L1-L3`：构建入口
    代码片段：`# Makefile for qemu test BE_TARGET := mips-unknown-linux-gnu BE_MODE := release`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：enum ThreadSyscallOutcome, struct ThreadSyscallSnapshot, struct PreemptSnapshot, struct TimeoutRunnableLookup, struct ThreadSyscallTrace。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/thread.rs` | L12-L16 | 关键词命中 |
| `os/src/hal/task.rs` | L14-L18 | 关键词命中 |
| `os/src/syscall/proc.rs` | L37-L41 | 关键词命中 |
| `os/src/thread.rs` | L46-L50 | enum ThreadSyscallOutcome |
| `os/src/thread.rs` | L57-L61 | struct ThreadSyscallSnapshot |
| `os/src/thread.rs` | L67-L71 | struct PreemptSnapshot |

### 关键代码片段

  - `os/src/thread.rs:L12-L16`：关键词命中
    代码片段：`use crate::{ fs::fs_context::{FsCloneFlags, FsContext}, hal::{task::TaskContext, HartId, HartMask}, mm::heap::HeapAllocError, process::Process,`
  - `os/src/hal/task.rs:L14-L18`：关键词命中
    代码片段：`/// - 可以通过 ['TaskContext::set_stack_pointer'] 与 ['TaskContext::set_instruction_pointer'] ///   初始化一个“首次运行”的内核线程。 pub use arch::task::TaskContext; /// 在两个任务上下文之间切换。`
  - `os/src/syscall/proc.rs:L37-L41`：关键词命中
    代码片段：`}, sync::{Once, RwLock, Waiter}, thread::clone::{CloneFlags, CSIGNAL}, thread::{ thread_local::{NanosleepRestartBlock, SignalAltStackSnapshot, SyscallRestartBlock},`
  - `os/src/thread.rs:L46-L50`：enum ThreadSyscallOutcome
    代码片段：`/// 最近一次 syscall 快照中的完成状态。 #[derive(Clone, Copy, Debug, PartialEq, Eq)] pub(crate) enum ThreadSyscallOutcome { InFlight, Ok(isize),`

### 相关符号

`enum ThreadSyscallOutcome` at `os/src/thread.rs:L46`、`struct ThreadSyscallSnapshot` at `os/src/thread.rs:L57`、`struct PreemptSnapshot` at `os/src/thread.rs:L67`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：trait VmIo, fn read, fn read_bytes, fn read_val, fn read_slice。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/mm.rs` | L11-L15 | 关键词命中 |
| `os/src/mm/init.rs` | L9-L13 | 关键词命中 |
| `os/src/mm/vmar.rs` | L81-L85 | 关键词命中 |
| `os/src/mm/io.rs` | L39-L43 | trait VmIo |
| `os/src/mm/io.rs` | L45-L49 | fn read |
| `os/src/mm/io.rs` | L48-L52 | fn read_bytes |

### 关键代码片段

  - `os/src/mm.rs:L11-L15`：关键词命中
    代码片段：`pub mod page_fault_handler; pub mod page_prop; pub mod page_table; pub mod reclaim; pub mod status;`
  - `os/src/mm/init.rs:L9-L13`：关键词命中
    代码片段：`meta::init as init_frame_metadata, }, mm::{kspace::init_kernel_page_table, page_table::boot_pt}, };`
  - `os/src/mm/vmar.rs:L81-L85`：关键词命中
    代码片段：`/// 将 VMAR 语义权限转换为可安装到页表的硬件权限位。 /// /// RISC-V 不接受 W-only 叶子 PTE；部分架构也允许把 'PROT_WRITE' 表达为 /// 硬件可读写页。VMAR 的语义权限仍记录在 'VmMarker'，这里仅保证落表权限可前进。 fn from(perms: VmPerms) -> PageFlags {`
  - `os/src/mm/io.rs:L39-L43`：trait VmIo
    代码片段：`/// /// 这些方法可能被多个线程并发执行。需要稳定结果或复合操作原子性时，调用方必须提供额外同步。 pub trait VmIo: Send + Sync { /// 从指定偏移读取数据到写游标。 ///`

### 相关符号

`trait VmIo` at `os/src/mm/io.rs:L39`、`fn read` at `os/src/mm/io.rs:L45`、`fn read_bytes` at `os/src/mm/io.rs:L48`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：struct KernelRelease, fn release_string, fn uname26_string, fn kernel_release, fn kernel_version。（置信度：medium）
  - 静态识别到 370 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/syscall.rs` | L163-L167 | 关键词命中 |
| `os/src/syscall/fs.rs` | L1-L3 | 关键词命中 |
| `os/src/syscall/abi.rs` | L69-L73 | 关键词命中 |
| `os/src/syscall.rs` | L51-L55 | struct KernelRelease |
| `os/src/syscall.rs` | L68-L72 | fn release_string |
| `os/src/syscall.rs` | L73-L77 | fn uname26_string |

### 关键代码片段

  - `os/src/syscall.rs:L163-L167`：关键词命中
    代码片段：`} /// 验证 message-header 坏地址在 syscall 边界返回 'EFAULT'。 #[cfg(feature = "ktest")] pub(crate) fn ktest_socket_message_badaddr_contract() -> crate::Result<()> {`
  - `os/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! syscall：文件系统相关分组 extern crate alloc;`
  - `os/src/syscall/abi.rs:L69-L73`：关键词命中
    代码片段：`} /// 向 syscall 表写入 RISC-V 专用的 syscall 条目。 /// /// 架构专用条目仍通过统一密集表分发；未实现条目的 handler 保持为 'None'。`
  - `os/src/syscall.rs:L51-L55`：struct KernelRelease
    代码片段：`/// 内核对用户态公布的 release 三元组。 #[derive(Clone, Copy)] struct KernelRelease { major: u16, minor: u16,`

### 相关符号

`struct KernelRelease` at `os/src/syscall.rs:L51`、`fn release_string` at `os/src/syscall.rs:L68`、`fn uname26_string` at `os/src/syscall.rs:L73`、`fn ktest_dev_null_syscall_fastpath` at `os/src/syscall.rs:L271`、`enum SyscallOutcome` at `os/src/syscall.rs:L758`、`enum SyscallControlFlow` at `os/src/syscall.rs:L824`、`struct SyscallContext` at `os/src/syscall.rs:L849`、`impl SyscallContext` at `os/src/syscall.rs:L856`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct Termio, struct Termios, fn termio, fn with_termio, fn is_canonical。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/fs/pty.rs` | L9-L13 | 关键词命中 |
| `os/src/fs/ext4.rs` | L38-L42 | 关键词命中 |
| `os/src/fs/path.rs` | L1-L3 | 关键词命中 |
| `os/src/fs/pty.rs` | L82-L86 | struct Termio |
| `os/src/fs/pty.rs` | L97-L101 | struct Termios |
| `os/src/fs/pty.rs` | L129-L133 | fn termio |

### 关键代码片段

  - `os/src/fs/pty.rs:L9-L13`：关键词命中
    代码片段：`fs::{ file_handle::{FileLike, PollEvents}, utils::{AccessMode, InodeMode, InodeType, Metadata, StatusFlags, SuperBlock, NAME_MAX}, }, hal::VirtAddr,`
  - `os/src/fs/ext4.rs:L38-L42`：关键词命中
    代码片段：`fs::utils::{ CreateNode, CreateOwner, DirSink, FileAttributeFlags, FileAttributes, FileSystem, FsFlags, Inode, InodeMode, InodeType, Metadata, StatxAttributes, StatxMetadata, Su...`
  - `os/src/fs/path.rs:L1-L3`：关键词命中
    代码片段：`//! 路径组件：'Mount/Dentry/Path' extern crate alloc;`
  - `os/src/fs/pty.rs:L82-L86`：struct Termio
    代码片段：`#[repr(C)] #[derive(Clone, Copy)] struct Termio { c_iflag: u16, c_oflag: u16,`

### 相关符号

`struct Termio` at `os/src/fs/pty.rs:L82`、`struct Termios` at `os/src/fs/pty.rs:L97`、`fn termio` at `os/src/fs/pty.rs:L129`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct RcuMonitor, fn new, fn after_grace_period, fn finish_grace_period, struct State。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/sync.rs` | L2-L6 | 关键词命中 |
| `os/src/sync/rcu.rs` | L7-L11 | 关键词命中 |
| `os/src/sync/once.rs` | L7-L11 | 关键词命中 |
| `os/src/sync/rcu.rs` | L17-L21 | struct RcuMonitor |
| `os/src/sync/rcu.rs` | L24-L28 | fn new |
| `os/src/sync/rcu.rs` | L32-L36 | fn after_grace_period |

### 关键代码片段

  - `os/src/sync.rs:L2-L6`：关键词命中
    代码片段：`pub mod guard; pub mod mutex; pub mod once; pub mod rcu;`
  - `os/src/sync/rcu.rs:L7-L11`：关键词命中
    代码片段：`mem::ManuallyDrop, ops::Deref, sync::atomic::{AtomicBool, Ordering}, };`
  - `os/src/sync/once.rs:L7-L11`：关键词命中
    代码片段：`hint::spin_loop, mem::MaybeUninit, sync::atomic::{AtomicU32, Ordering}, };`
  - `os/src/sync/rcu.rs:L17-L21`：struct RcuMonitor
    代码片段：`type Callbacks = VecDeque<Callback>; struct RcuMonitor { is_monitoring: AtomicBool, full_mask_bits: u64,`

### 相关符号

`struct RcuMonitor` at `os/src/sync/rcu.rs:L17`、`fn new` at `os/src/sync/rcu.rs:L24`、`fn after_grace_period` at `os/src/sync/rcu.rs:L32`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn enable_local, fn disable_local, fn local_enabled, fn with_disabled, enum InterruptKind。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/hal/irq.rs` | L8-L12 | 关键词命中 |
| `os/src/hal/trap.rs` | L13-L17 | 关键词命中 |
| `os/src/hal/irq/external.rs` | L6-L10 | 关键词命中 |
| `os/src/hal/irq.rs` | L7-L11 | fn enable_local |
| `os/src/hal/irq.rs` | L13-L17 | fn disable_local |
| `os/src/hal/irq.rs` | L19-L23 | fn local_enabled |

### 关键代码片段

  - `os/src/hal/irq.rs:L8-L12`：关键词命中
    代码片段：`#[inline] pub fn enable_local() { arch::irq::enable_local(); }`
  - `os/src/hal/trap.rs:L13-L17`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug)] pub enum InterruptKind { Timer, External, Software,`
  - `os/src/hal/irq/external.rs:L6-L10`：关键词命中
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; use crate::hal::trap::TrapFrame; use crate::sync::{LocalIrqDisabled, Once, SpinLock};`
  - `os/src/hal/irq.rs:L7-L11`：fn enable_local
    代码片段：`/// 开启本地中断。 #[inline] pub fn enable_local() { arch::irq::enable_local(); }`

### 相关符号

`fn enable_local` at `os/src/hal/irq.rs:L7`、`fn disable_local` at `os/src/hal/irq.rs:L13`、`fn local_enabled` at `os/src/hal/irq.rs:L19`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct SmoltcpDevice, fn new, impl phy::Device::for::SmoltcpDevice, fn receive, fn transmit。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/drivers.rs` | L5-L7 | 关键词命中 |
| `os/src/net/device.rs` | L7-L11 | 关键词命中 |
| `os/src/hal/console.rs` | L18-L22 | 关键词命中 |
| `os/src/net/device.rs` | L12-L16 | struct SmoltcpDevice |
| `os/src/net/device.rs` | L18-L22 | fn new |
| `os/src/net/device.rs` | L23-L27 | impl phy::Device::for::SmoltcpDevice |

### 关键代码片段

  - `os/src/drivers.rs:L5-L7`：关键词命中
    代码片段：`pub mod block; pub mod platform; pub mod virtio;`
  - `os/src/net/device.rs:L7-L11`：关键词命中
    代码片段：`use smoltcp::phy::{self, DeviceCapabilities, Medium}; use smoltcp::time::Instant; use virtio_drivers::device::net::RxBuffer; use crate::drivers::virtio::net::VirtioNetDevice;`
  - `os/src/hal/console.rs:L18-L22`：关键词命中
    代码片段：`pub fn init_from_boot() { CONSOLE_BACKEND.call_once(|| { if let Some(u) = boot_info().uart { let mmio_base = arch::mm::phys_to_virt_io(u.base).as_usize(); // SAFETY: 'mmio_base'...`
  - `os/src/net/device.rs:L12-L16`：struct SmoltcpDevice
    代码片段：`/// 把 'VirtioNetDevice' 适配为 smoltcp 的以太网设备。 pub(super) struct SmoltcpDevice { device: Arc<VirtioNetDevice>, }`

### 相关符号

`struct SmoltcpDevice` at `os/src/net/device.rs:L12`、`fn new` at `os/src/net/device.rs:L18`、`impl phy::Device::for::SmoltcpDevice` at `os/src/net/device.rs:L23`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
