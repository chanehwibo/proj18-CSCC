# 2026 T2026100019911468 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026100019911468-282`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：203
- 代码/文本行数：41801
- 主要语言：c 29141 LOC, markdown 5505 LOC, text 3372 LOC, rust 1926 LOC, asm 1191 LOC

## 总览

2026 T2026100019911468 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 c: 29141 LOC, markdown: 5505 LOC, text: 3372 LOC, rust: 1926 LOC。仓库包含 203 个已扫描文件、约 41801 行可分析文本，当前抽取到 787 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 7 |
| 文件系统 | 已确认 | high | 5 |
| 同步机制 | 已确认 | high | 4 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, la-minimal/linker.ld, la-minimal/Makefile, xv6-k210/Makefile, xv6-k210/bootloader/SBI/rustsbi-k210/build.rs。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`SRC := xv6-k210 LA_SRC := la-minimal DOCKER_IMAGE ?= zhouzhouyi/os-contest:20260104`
  - `la-minimal/linker.ld:L1-L3`：构建入口
    代码片段：`OUTPUT_ARCH(loongarch) ENTRY(_start)`
  - `la-minimal/Makefile:L1-L3`：构建入口
    代码片段：`CROSS ?= loongarch64-linux-gnu- CC := $(CROSS)gcc OBJDUMP := $(CROSS)objdump`
  - `xv6-k210/Makefile:L1-L3`：构建入口
    代码片段：`#platform	:= k210 platform ?= qemu # mode := debug`
  - `xv6-k210/bootloader/SBI/rustsbi-k210/build.rs:L1-L3`：构建入口
    代码片段：`// Ref: https://github.com/luojia65/rustsbi/blob/master/platform/k210/build.rs use std::env; use std::fs;`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct busybox_cmd, fn reg_info, struct context, struct cpu, enum procstate。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `xv6-k210/kernel/proc.c` | L5-L9 | 关键词命中 |
| `xv6-k210/kernel/include/proc.h` | L32-L36 | 关键词命中 |
| `xv6-k210/kernel/vm.c` | L6-L10 | 关键词命中 |
| `xv6-k210/kernel/proc.c` | L69-L73 | struct busybox_cmd |
| `xv6-k210/kernel/proc.c` | L253-L257 | fn reg_info |
| `xv6-k210/kernel/include/proc.h` | L11-L15 | struct context |

### 关键代码片段

  - `xv6-k210/kernel/proc.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/proc.h" #include "include/intr.h" #include "include/kalloc.h"`
  - `xv6-k210/kernel/include/proc.h:L32-L36`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `xv6-k210/kernel/vm.c:L6-L10`：关键词命中
    代码片段：`#include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h" #include "include/string.h"`
  - `xv6-k210/kernel/proc.c:L69-L73`：struct busybox_cmd
    代码片段：`"strings.lua", }; struct busybox_cmd { char *name; char *shell;`

### 相关符号

`struct busybox_cmd` at `xv6-k210/kernel/proc.c:L69`、`fn reg_info` at `xv6-k210/kernel/proc.c:L253`、`struct context` at `xv6-k210/kernel/include/proc.h:L11`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：struct run, macro PGSIZE, macro PTE_V, macro PTE_R, macro PTE_W。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `xv6-k210/kernel/vm.c` | L5-L9 | 关键词命中 |
| `xv6-k210/kernel/kalloc.c` | L9-L13 | 关键词命中 |
| `xv6-k210/kernel/include/vm.h` | L10-L14 | 关键词命中 |
| `xv6-k210/kernel/kalloc.c` | L17-L21 | struct run |
| `xv6-k210/kernel/include/riscv.h` | L335-L339 | macro PGSIZE |
| `xv6-k210/kernel/include/riscv.h` | L341-L345 | macro PTE_V |

### 关键代码片段

  - `xv6-k210/kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`
  - `xv6-k210/kernel/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/kalloc.h" #include "include/string.h" #include "include/printf.h"`
  - `xv6-k210/kernel/include/vm.h:L10-L14`：关键词命中
    代码片段：`uint64          kvmpa(uint64); void            kvmmap(uint64, uint64, uint64, int); int             mappages(pagetable_t, uint64, uint64, uint64, int); pagetable_t     uvmcreate...`
  - `xv6-k210/kernel/kalloc.c:L17-L21`：struct run
    代码片段：`extern char kernel_end[]; // first address after kernel. struct run { struct run *next; };`

### 相关符号

`struct run` at `xv6-k210/kernel/kalloc.c:L17`、`macro PGSIZE` at `xv6-k210/kernel/include/riscv.h:L335`、`macro PTE_V` at `xv6-k210/kernel/include/riscv.h:L341`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn devintr, struct linux_timeval, struct linux_timespec, struct linux_iovec, struct linux_rlimit。（置信度：medium）
  - 静态识别到 1 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `xv6-k210/kernel/syscall.c` | L6-L10 | 关键词命中 |
| `xv6-k210/kernel/include/syscall.h` | L10-L14 | 关键词命中 |
| `xv6-k210/kernel/proc.c` | L895-L899 | 关键词命中 |
| `xv6-k210/kernel/trap.c` | L211-L215 | fn devintr |
| `xv6-k210/kernel/syscall.c` | L286-L290 | struct linux_timeval |
| `xv6-k210/kernel/syscall.c` | L291-L295 | struct linux_timespec |

### 关键代码片段

  - `xv6-k210/kernel/syscall.c:L6-L10`：关键词命中
    代码片段：`#include "include/spinlock.h" #include "include/proc.h" #include "include/syscall.h" #include "include/sysinfo.h" #include "include/kalloc.h"`
  - `xv6-k210/kernel/include/syscall.h:L10-L14`：关键词命中
    代码片段：`int argaddr(int n, uint64 *ip); int argstr(int n, char *buf, int max); void syscall(void); #endif`
  - `xv6-k210/kernel/proc.c:L895-L899`：关键词命中
    代码片段：`//     0x13, 0x05, 0x05, 0x00,     // mv a0, a0 //     0x93, 0x08, 0x60, 0x01,     // li a7, 22 //     0x73, 0x00, 0x00, 0x00,     // ecall //     0xef, 0xf0, 0x1f, 0xff,     //...`
  - `xv6-k210/kernel/trap.c:L211-L215`：fn devintr
    代码片段：`//          1 if other device, //          0 if not recognized. int devintr(void) { uint64 scause = r_scause();`

### 相关符号

`fn devintr` at `xv6-k210/kernel/trap.c:L211`、`struct linux_timeval` at `xv6-k210/kernel/syscall.c:L286`、`struct linux_timespec` at `xv6-k210/kernel/syscall.c:L291`、`macro __SYSCALL_H` at `xv6-k210/kernel/include/syscall.h:L1`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct file, struct devsw。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `xv6-k210/kernel/file.c` | L157-L161 | 关键词命中 |
| `xv6-k210/kernel/include/file.h` | L33-L37 | 关键词命中 |
| `xv6-k210/kernel/ext4.c` | L504-L508 | 关键词命中 |
| `xv6-k210/kernel/include/file.h` | L2-L6 | struct file |
| `xv6-k210/kernel/include/file.h` | L18-L22 | struct devsw |

### 关键代码片段

  - `xv6-k210/kernel/file.c:L157-L161`：关键词命中
    代码片段：`// addr is a user virtual address. int fileread(struct file *f, uint64 addr, int n) { int r = 0;`
  - `xv6-k210/kernel/include/file.h:L33-L37`：关键词命中
    代码片段：`struct file*    filedup(struct file*); void            fileinit(void); int             fileread(struct file*, uint64, int n); int             filestat(struct file*, uint64 addr)...`
  - `xv6-k210/kernel/ext4.c:L504-L508`：关键词命中
    代码片段：`ext4_root.ref = 1; ext4.active = 1; printf("[ext4] mounted readonly block=%d inode=%d desc=%d\n", ext4.block_size, ext4.inode_size, ext4.desc_size); return 0;`
  - `xv6-k210/kernel/include/file.h:L2-L6`：struct file
    代码片段：`#define __FILE_H struct file { enum { FD_NONE, FD_PIPE, FD_ENTRY, FD_DEVICE } type; int ref; // reference count`

### 相关符号

`struct file` at `xv6-k210/kernel/include/file.h:L2`、`struct devsw` at `xv6-k210/kernel/include/file.h:L18`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct spinlock。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `xv6-k210/kernel/bio.c` | L18-L22 | 关键词命中 |
| `xv6-k210/kernel/exec.c` | L4-L8 | 关键词命中 |
| `xv6-k210/kernel/ext4.c` | L2-L6 | 关键词命中 |
| `xv6-k210/kernel/include/spinlock.h` | L5-L9 | struct spinlock |

### 关键代码片段

  - `xv6-k210/kernel/bio.c:L18-L22`：关键词命中
    代码片段：`#include "include/types.h" #include "include/param.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/riscv.h"`
  - `xv6-k210/kernel/exec.c:L4-L8`：关键词命中
    代码片段：`#include "include/memlayout.h" #include "include/riscv.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/proc.h"`
  - `xv6-k210/kernel/ext4.c:L2-L6`：关键词命中
    代码片段：`#include "include/param.h" #include "include/riscv.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/buf.h"`
  - `xv6-k210/kernel/include/spinlock.h:L5-L9`：struct spinlock
    代码片段：`// Mutual exclusion lock. struct spinlock { uint locked;       // Is the lock held?`

### 相关符号

`struct spinlock` at `xv6-k210/kernel/include/spinlock.h:L5`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn plicinit, fn devintr, fn timerinit, fn timer_tick, struct trapframe。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `xv6-k210/kernel/plic.c` | L5-L9 | 关键词命中 |
| `xv6-k210/kernel/trap.c` | L6-L10 | 关键词命中 |
| `xv6-k210/kernel/timer.c` | L1-L3 | 关键词命中 |
| `xv6-k210/kernel/plic.c` | L26-L30 | fn plicinit |
| `xv6-k210/kernel/trap.c` | L211-L215 | fn devintr |
| `xv6-k210/kernel/timer.c` | L14-L18 | fn timerinit |

### 关键代码片段

  - `xv6-k210/kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
  - `xv6-k210/kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
  - `xv6-k210/kernel/timer.c:L1-L3`：关键词命中
    代码片段：`// Timer Interrupt handler`
  - `xv6-k210/kernel/plic.c:L26-L30`：fn plicinit
    代码片段：`} void plicinit(void) { plic_set_source_priority(UART_IRQ, 1); #if !defined(VISIONFIVE2)`

### 相关符号

`fn plicinit` at `xv6-k210/kernel/plic.c:L26`、`fn devintr` at `xv6-k210/kernel/trap.c:L211`、`fn timerinit` at `xv6-k210/kernel/timer.c:L14`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：fn consputc, struct virtio_blk_outhdr, macro VIRTIO_MMIO_MAGIC_VALUE, macro VIRTIO_MMIO_VERSION, macro VIRTIO_MMIO_DEVICE_ID。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `xv6-k210/kernel/uart.c` | L1-L4 | 关键词命中 |
| `xv6-k210/kernel/console.c` | L1-L4 | 关键词命中 |
| `xv6-k210/kernel/vf2_uart.c` | L1-L5 | 关键词命中 |
| `xv6-k210/kernel/console.c` | L35-L39 | fn consputc |
| `xv6-k210/kernel/virtio_disk.c` | L196-L200 | struct virtio_blk_outhdr |
| `xv6-k210/kernel/include/virtio.h` | L17-L21 | macro VIRTIO_MMIO_MAGIC_VALUE |

### 关键代码片段

  - `xv6-k210/kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `xv6-k210/kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `xv6-k210/kernel/vf2_uart.c:L1-L5`：关键词命中
    代码片段：`#include "include/types.h" #include "include/memlayout.h" #include "include/console.h" #ifdef VISIONFIVE2`
  - `xv6-k210/kernel/console.c:L35-L39`：fn consputc
    代码片段：`} void consputc(int c) { if(c == BACKSPACE){ // if the user typed backspace, overwrite with a space.`

### 相关符号

`fn consputc` at `xv6-k210/kernel/console.c:L35`、`struct virtio_blk_outhdr` at `xv6-k210/kernel/virtio_disk.c:L196`、`macro VIRTIO_MMIO_MAGIC_VALUE` at `xv6-k210/kernel/include/virtio.h:L17`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
