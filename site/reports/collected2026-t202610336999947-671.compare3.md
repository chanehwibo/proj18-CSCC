# 2026 T202610336999947 比较报告

- 对比历史仓库：2024 ChCore
- 生成时间：2026-07-01T21:00:08.087766+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 ChCore（来源：赛事历史作品）：画像相似度 score=9.86；同属 ucore-variant 风格; 语言构成相似度 0.97; OS 维度重合度 1.00; 代码规模接近度 0.92

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 ChCore 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `arceos/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `kernel/include/io/uart.h:L14-L18`：关键词命中
    代码片段：`#define IO_UART_H /* Use mini-uart or PL011 */ #define USE_mini_uart 0`
  - `kernel/arch/riscv64/drivers/uart/uart.c:L11-L15`：关键词命中
    代码片段：`*/ #include <io/uart.h> #include <arch/machine/sbi.h>`
- 与 2024 ChCore 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `arceos/modules/axfs/src/fs/fatfs.rs:L32-L36`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
  - `testsuits-for-oskernel/busybox/libbb/copy_file.c:L182-L186`：关键词命中
    代码片段：`} } /* remember (dev,inode) of each created dir. * NULL: name is not remembered */ add_to_ino_dev_hashtable(&dest_stat, NULL);`
  - `ltp/testcases/kernel/syscalls/sync_file_range/sync_file_range02.c:L104-L108`：关键词命中
    代码片段：`/* * Fat does not support sparse files, we have to pre-fill the file so * that the zero-filled start of the file has been written to disk * before the test starts.`
  - `ltp/testcases/kernel/syscalls/dup2/dup204.c:L7-L11`：关键词命中
    代码片段：`* [Description] * * Test whether the inode number are the same for both file descriptors. */`
- 与 2024 ChCore 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use axcpu::trap::{IRQ, register_trap_handler};`
  - `testsuits-for-oskernel/iperf/src/timer.c:L31-L35`：关键词命中
    代码片段：`#include <stdlib.h> #include "timer.h" #include "iperf_time.h"`
  - `kernel/irq/ipi.c:L12-L16`：关键词命中
    代码片段：`#include <arch/sync.h> #include <irq/ipi.h> #include <machine.h> #include <common/kprint.h>`
  - `kernel/irq/timer.c:L11-L15`：关键词命中
    代码片段：`*/ #include <irq/irq.h> #include <irq/timer.h> #include <sched/sched.h>`
- 与 2024 ChCore 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `arceos/modules/axmm/src/backend/alloc.rs:L1-L5`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PAGE_SIZE_4K, PageIter4K,...`
  - `arceos/modules/axmm/src/lib.rs:L78-L82`：关键词命中
    代码片段：`let mut aspace = AddrSpace::new_empty(base, size)?; if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use separate pag...`
  - `kernel/mm/kmalloc.c:L176-L180`：关键词命中
    代码片段：`} void kfree(void *ptr) { _kfree(ptr, true);`
  - `kernel/mm/vmspace.c:L64-L68`：关键词命中
    代码片段：`static void free_cow_private_page(struct cow_private_page *record) { kfree(record->page); kfree(record); }`
- 与 2024 ChCore 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/lmbench_src/src/lib_sched.c:L50-L54`：关键词命中
    代码片段：`if (!sched || strcasecmp(sched, "DEFAULT") == 0) { /* do nothing.  Allow scheduler to control placement */ return 0; } else if (strcasecmp(sched, "SINGLE") == 0) {`
  - `testsuits-for-oskernel/ltp-full-20240524/lib/tst_thread_state.c:L18-L22`：关键词命中
    代码片段：`unsigned int msecs = 0; snprintf(proc_path, sizeof(proc_path), "/proc/self/task/%i/stat", tid); for (;;) {`
  - `kernel/sched/sched.c:L18-L22`：关键词命中
    代码片段：`#include <common/util.h> /* Scheduler global data */ struct thread *current_threads[PLAT_CPU_NUM]; struct thread idle_threads[PLAT_CPU_NUM];`
  - `kernel/object/thread.c:L20-L24`：关键词命中
    代码片段：`#include <mm/mm.h> #include <mm/uaccess.h> #include <object/thread.h> #include <object/recycle.h> #ifdef CHCORE_SIGNAL`
- 与 2024 ChCore 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/busybox/miscutils/flash_lock_unlock.c:L30-L34`：关键词命中
    代码片段：`//usage:       "MTD_DEVICE OFFSET SECTORS" //usage:#define flash_lock_full_usage "\n\n" //usage:       "Lock part or all of an MTD device. If SECTORS is -1, then all sectors\n"...`
  - `testsuits-for-oskernel/ltp-full-20240524/include/tst_fuzzy_sync.h:L155-L159`：关键词命中
    代码片段：`float max_dev_ratio; /** Internal; Atomic counter used by fzsync_pair_wait() */ int a_cntr; /** Internal; Atomic counter used by fzsync_pair_wait() */`
  - `kernel/include/common/lock.h:L16-L20`：关键词命中
    代码片段：`#include <common/types.h> /* Simple/Compact ticket/rwlock impl */ struct lock {`
  - `kernel/arch/riscv64/sync/rwlock.c:L14-L18`：关键词命中
    代码片段：`#include <common/errno.h> #include <common/macro.h> #include <common/lock.h> #include <arch/sync.h>`
- 与 2024 ChCore 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/UnixBench/src/syscall.c:L1-L5`：关键词命中
    代码片段：`/******************************************************************************* *  The BYTE UNIX Benchmarks - Release 3 *          Module: syscall.c   SID: 3.3 5/15/91 19:30:21...`
  - `testsuits-for-oskernel/lmbench_src/src/lat_syscall.c:L138-L142`：关键词命中
    代码片段：`benchmp(NULL, do_getppid, NULL, 0, parallel, warmup, repetitions, &state); micro("Simple syscall", get_n()); } else if (!strcmp("write", av[optind])) { state.fd = open("/dev/nul...`
  - `unixbench/src/syscall.c:L1-L5`：关键词命中
    代码片段：`/******************************************************************************* *  The BYTE UNIX Benchmarks - Release 3 *          Module: syscall.c   SID: 3.3 5/15/91 19:30:21...`
  - `kernel/syscall/syscall.c:L36-L40`：关键词命中
    代码片段：`#include <irq/irq.h> #include <common/poweroff.h> #include <syscall/opentrustee.h> #ifdef CHCORE_OPENTRUSTEE #include <arch/trustzone/smc.h>`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 文件路径重合：与 2024 ChCore 在“中断与异常”维度出现同名文件源码路径 `testsuits-for-oskernel/iperf/src/timer.c` / `kernel/irq/timer.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/iperf/src/timer.c:L31-L35`：关键词命中
    代码片段：`#include <stdlib.h> #include "timer.h" #include "iperf_time.h"`
  - `kernel/irq/timer.c:L11-L15`：关键词命中
    代码片段：`*/ #include <irq/irq.h> #include <irq/timer.h> #include <sched/sched.h>`
- 文件路径重合：与 2024 ChCore 在“中断与异常”维度出现同名文件源码路径 `testsuits-for-oskernel/iperf/src/timer.h` / `kernel/include/irq/timer.h`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/iperf/src/timer.h:L36-L40`：关键词命中
    代码片段：`#include "iperf_time.h" /* TimerClientData is an opaque value that tags along with a timer.  The ** client can use it for whatever, and it gets passed to the callback when ** th...`
  - `kernel/include/irq/timer.h:L24-L28`：struct sleep_state
    代码片段：`/* Every thread has a sleep_state struct */ struct sleep_state { /* Link sleeping threads on each core */ struct list_head sleep_node;`
- 文件路径重合：与 2024 ChCore 在“系统调用”维度出现同名文件源码路径 `testsuits-for-oskernel/UnixBench/src/syscall.c` / `unixbench/src/syscall.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/UnixBench/src/syscall.c:L1-L5`：关键词命中
    代码片段：`/******************************************************************************* *  The BYTE UNIX Benchmarks - Release 3 *          Module: syscall.c   SID: 3.3 5/15/91 19:30:21...`
  - `unixbench/src/syscall.c:L1-L5`：关键词命中
    代码片段：`/******************************************************************************* *  The BYTE UNIX Benchmarks - Release 3 *          Module: syscall.c   SID: 3.3 5/15/91 19:30:21...`
- 文件路径重合：与 2024 ChCore 在“系统调用”维度出现同名文件源码路径 `testsuits-for-oskernel/UnixBench/src/syscall.c` / `kernel/syscall/syscall.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/UnixBench/src/syscall.c:L1-L5`：关键词命中
    代码片段：`/******************************************************************************* *  The BYTE UNIX Benchmarks - Release 3 *          Module: syscall.c   SID: 3.3 5/15/91 19:30:21...`
  - `kernel/syscall/syscall.c:L36-L40`：关键词命中
    代码片段：`#include <irq/irq.h> #include <common/poweroff.h> #include <syscall/opentrustee.h> #ifdef CHCORE_OPENTRUSTEE #include <arch/trustzone/smc.h>`

## 相似点

- 与 2024 ChCore 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 ChCore 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `arceos/modules/axdriver/src/virtio.rs:L19-L23`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `arceos/modules/axdriver/src/drivers.rs:L6-L10`：关键词命中
    代码片段：`use axdriver_base::DeviceType; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
- 与 2024 ChCore 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `arceos/modules/axfs/src/fs/fatfs.rs:L32-L36`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
  - `testsuits-for-oskernel/busybox/libbb/copy_file.c:L182-L186`：关键词命中
    代码片段：`} } /* remember (dev,inode) of each created dir. * NULL: name is not remembered */ add_to_ino_dev_hashtable(&dest_stat, NULL);`
- 与 2024 ChCore 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use axcpu::trap::{IRQ, register_trap_handler};`
  - `testsuits-for-oskernel/iperf/src/timer.c:L31-L35`：关键词命中
    代码片段：`#include <stdlib.h> #include "timer.h" #include "iperf_time.h"`
- 与 2024 ChCore 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `arceos/modules/axmm/src/backend/alloc.rs:L1-L5`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PAGE_SIZE_4K, PageIter4K,...`
  - `arceos/modules/axmm/src/lib.rs:L78-L82`：关键词命中
    代码片段：`let mut aspace = AddrSpace::new_empty(base, size)?; if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use separate pag...`
- 与 2024 ChCore 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/lmbench_src/src/lib_sched.c:L50-L54`：关键词命中
    代码片段：`if (!sched || strcasecmp(sched, "DEFAULT") == 0) { /* do nothing.  Allow scheduler to control placement */ return 0; } else if (strcasecmp(sched, "SINGLE") == 0) {`
  - `testsuits-for-oskernel/ltp-full-20240524/lib/tst_thread_state.c:L18-L22`：关键词命中
    代码片段：`unsigned int msecs = 0; snprintf(proc_path, sizeof(proc_path), "/proc/self/task/%i/stat", tid); for (;;) {`
- 与 2024 ChCore 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/busybox/miscutils/flash_lock_unlock.c:L30-L34`：关键词命中
    代码片段：`//usage:       "MTD_DEVICE OFFSET SECTORS" //usage:#define flash_lock_full_usage "\n\n" //usage:       "Lock part or all of an MTD device. If SECTORS is -1, then all sectors\n"...`
  - `testsuits-for-oskernel/ltp-full-20240524/include/tst_fuzzy_sync.h:L155-L159`：关键词命中
    代码片段：`float max_dev_ratio; /** Internal; Atomic counter used by fzsync_pair_wait() */ int a_cntr; /** Internal; Atomic counter used by fzsync_pair_wait() */`
- 与 2024 ChCore 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `testsuits-for-oskernel/UnixBench/src/syscall.c:L1-L5`：关键词命中
    代码片段：`/******************************************************************************* *  The BYTE UNIX Benchmarks - Release 3 *          Module: syscall.c   SID: 3.3 5/15/91 19:30:21...`
  - `testsuits-for-oskernel/lmbench_src/src/lat_syscall.c:L138-L142`：关键词命中
    代码片段：`benchmp(NULL, do_getppid, NULL, 0, parallel, warmup, repetitions, &state); micro("Simple syscall", get_n()); } else if (!strcmp("write", av[optind])) { state.fd = open("/dev/nul...`
- 与 2024 ChCore 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 ChCore 的语言构成不同：待测作品为 {'build': 25533, 'markdown': 4799, 'rust': 20321, 'c': 1237469, 'toml': 137, 'make': 1752, 'text': 24768, 'asm': 1355, 'cpp': 1434, 'yaml': 32, 'json': 54}，历史样本为 {'json': 35, 'text': 15034, 'build': 11544, 'make': 1866, 'markdown': 6258, 'c': 1165153, 'yaml': 32, 'asm': 6070, 'cpp': 1510}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：18
- 含证据关键结论数：18（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
