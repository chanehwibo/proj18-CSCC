# 2026 T2026104869911155 比较报告

- 对比历史仓库：2025 只编译了一次
- 生成时间：2026-07-02T05:01:28.876601+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 只编译了一次（来源：赛事历史作品）：画像相似度 score=9.26；同属 ucore-variant 风格; 语言构成相似度 0.83; OS 维度重合度 1.00; 代码规模接近度 0.60

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 只编译了一次 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/drivers/block.rs:L2-L6`：关键词命中
    代码片段：`#[cfg(target_arch = "riscv64")] mod virtio_mmio; pub const SECTOR_SIZE: usize = 512;`
  - `src/drivers/block/virtio_mmio.rs:L12-L16`：关键词命中
    代码片段：`}; const VIRTIO_MAGIC: u32 = 0x7472_6976; const VIRTIO_DEVICE_BLOCK: u32 = 2; const VIRTIO_VERSION_1_HIGH_BIT: u32 = 1;`
  - `os/src/console.rs:L74-L78`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/mod.rs:L1-L4`：关键词命中
    代码片段：`mod block; pub mod serial; pub use block::block_dev::BlockDevice;`
- 与 2025 只编译了一次 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/fs/ext4.rs:L251-L255`：关键词命中
    代码片段：`println!( "ext4: magic=0x{:04x} block_size={} inode_size={} blocks={} inodes={}", fs.superblock.magic, fs.superblock.block_size(), fs.superblock.inode_size,`
  - `src/fs/file.rs:L1319-L1323`：关键词命中
    代码片段：`)); } if let Some((inode, bytes)) = self.block_device_sysfs_bytes(&path) { return Ok(OpenFile::new( path,`
  - `os/src/fs/mod.rs:L2-L6`：关键词命中
    代码片段：`mod fat32; mod file; pub(crate) mod inode; mod pipe; mod stdio;`
  - `os/src/fs/file.rs:L1-L3`：关键词命中
    代码片段：`use crate::fs::inode::{FatType, OSInode}; use crate::mm::UserBuffer; use alloc::string::String;`
- 与 2025 只编译了一次 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/task/mod.rs:L9129-L9133`：关键词命中
    代码片段：`Ok(()) => 0, Err(error) => { println!("timer_create: copy timer id failed: {}", error.as_str()); -14 }`
  - `src/task/riscv64.rs:L193-L197`：关键词命中
    代码片段：`Err(error) => { println!( "trap: page fault handling failed scause={} sepc=0x{:x} stval=0x{:x}: {}", scause, frame.sepc,`
  - `os/src/timer.rs:L72-L76`：关键词命中
    代码片段：`let current_ms = get_time_ms(); TIMERS.exclusive_session(|timers| { while let Some(timer) = timers.peek() { if timer.expire_ms <= current_ms { wakeup_task(Arc::clone(&timer.task));`
  - `os/src/hal/arch/riscv/trap/mod.rs:L1-L3`：关键词命中
    代码片段：`//! 陷阱（Trap）处理模块。 //! //! 本模块负责处理所有 CPU 异常（Exceptions）和中断（Interrupts）。`
- 与 2025 只编译了一次 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/mm/user.rs:L53-L57`：关键词命中
    代码片段：`struct UserAddressSpaceInner { page_table: Sv39PageTable, pages: Vec<UserPage>, reservations: Vec<Reservation>,`
  - `src/mm/frame.rs:L6-L10`：关键词命中
    代码片段：`}; static FRAME_ALLOCATOR: FrameAllocator = FrameAllocator::new(); const MAX_FREE_FRAMES: usize = 32768; static mut FREE_FRAMES: [usize; MAX_FREE_FRAMES] = [0; MAX_FREE_FRAMES];`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`pub mod address; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/address.rs:L1-L3`：关键词命中
    代码片段：`//! 虚拟/物理地址与页号模块（SV39） //! //! # Overview`
- 与 2025 只编译了一次 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/task/mod.rs:L171-L175`：关键词命中
    代码片段：`FutexRequeue(FutexRequeueRequest), Sleep(SleepRequest), Yield, Exit(isize), ExitGroup(isize),`
  - `src/task/riscv64.rs:L13-L17`：关键词命中
    代码片段：`mm::user::LoadedElf, println, task::{ AcceptWaitRequest, DatagramReadWaitRequest, DatagramWriteWaitRequest, EpollWaitRequest, ExitStatus, FanotifyPermissionRequest, FutexRequeue...`
  - `os/src/task/mod.rs:L13-L17`：关键词命中
    代码片段：`//! - 所有对 PCB/TCB 内部的可变访问通过 'UPIntrFreeCell' 或 'UPIntrRefMut' 独占访问，避免数据竞争 //! - 用户资源（ustack、trap_cx、tid）在主线程退出前被正确释放，防止内存泄漏 //! - 'schedule' 必须提供有效 'TaskContext' 指针，否则会导致上下文切换错误...`
  - `os/src/task/task.rs:L44-L48`：关键词命中
    代码片段：`use crate::mm::{MapPermission, PhysPageNum, VirtAddr}; use crate::sync::{UPIntrFreeCell, UPIntrRefMut}; use crate::task::context::TaskContext; use crate::task::process::ProcessC...`
- 与 2025 只编译了一次 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/fs/file.rs:L6-L10`：关键词命中
    代码片段：`}; use core::cell::RefCell; use core::sync::atomic::{AtomicI32, Ordering}; use crate::{`
  - `src/mm/heap.rs:L2-L6`：关键词命中
    代码片段：`use core::alloc::{GlobalAlloc, Layout}; use core::ptr::null_mut; use core::sync::atomic::{AtomicBool, AtomicUsize, Ordering}; use crate::{mm::align_up, println};`
  - `os/src/sync/mod.rs:L7-L11`：关键词命中
    代码片段：`//! //! 模块内部按功能拆分为多个子模块： //! - 'mutex'：互斥锁抽象及其具体实现（自旋 / 阻塞） //! - 'semaphore'：计数型信号量 //! - 'condvar'：条件变量`
  - `os/src/sync/mutex.rs:L1-L3`：关键词命中
    代码片段：`//! # 互斥锁（Mutex）同步原语模块 //! //! ## Overview`
- 与 2025 只编译了一次 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/mm/user.rs:L779-L783`：关键词命中
    代码片段：`const TRAMPOLINE: [u8; 8] = [ 0x93, 0x08, 0xb0, 0x08, // li a7, 139 0x73, 0x00, 0x00, 0x00, // ecall ];`
  - `src/task/mod.rs:L1220-L1224`：关键词命中
    代码片段：`} pub fn handle_syscall(&mut self, number: usize, args: [usize; 6]) -> SyscallResult { if number == 221 { return self.sys_execve(args[0], args[1], args[2]);`
  - `os/src/syscall/mod.rs:L41-L45`：关键词命中
    代码片段：`pub use process::*; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> isize { match syscall_id { //SYSCALL_OPEN => sys_openat(args[0] as *const u8, args[1] as u32),`
  - `os/src/hal/arch/riscv/sbi.rs:L5-L9`：关键词命中
    代码片段：`//! //! # Design //! - 所有 SBI 调用通过 'ecall' 指令触发陷入 S 模式执行。 //! - 'sbi_call' 函数是通用封装，将函数号和参数传递给 SBI。 //! - 上层函数（如 'set_timer'、'console_putchar'）直接调用 'sbi_call'，简化内核接口。`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2025 只编译了一次 在“文件系统”维度发现 3 个同名定义：read, read_at, open_dir。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src/fs/ext4.rs:L981-L981`：fn read
    代码片段：`pub fn read(device: &mut impl BlockDevice) -> Result<Self, Error> {`
  - `os/src/fs/block_cache.rs:L193-L193`：fn read
    代码片段：`pub fn read<T, V>(&self, offset: usize, f: impl FnOnce(&T) -> V) -> V {`
  - `src/fs/ext4.rs:L1037-L1037`：fn read_at
    代码片段：`pub fn read_at(`
  - `os/src/fs/file.rs:L17-L17`：fn read_at
    代码片段：`fn read_at(&self, offset: usize, buf: &mut [u8]) -> Result<usize, isize>;`
- 与 2025 只编译了一次 在“内存管理”维度发现片段级代码相似度 0.67 （token=0.57, structure=0.86）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, init, pub。（置信度：medium）
  证据：
  - `src/mm/mod.rs:L25-L29`：fn init
    代码片段：`} pub fn init() { heap::init();`
  - `os/src/mm/mod.rs:L7-L11`：fn init
    代码片段：`/// 初始化内存管理子系统 /// 包括堆内存分配器、物理页帧分配器和内核虚拟地址空间的建立与激活 pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`
- 函数/符号名重合：与 2025 只编译了一次 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/console.rs:L23-L23`：fn write_str
    代码片段：`fn write_str(&mut self, text: &str) -> fmt::Result {`
  - `user/src/console.rs:L11-L11`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 函数/符号名重合：与 2025 只编译了一次 在“同步机制”维度发现 1 个同名定义：read。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/fs/ext4.rs:L981-L981`：fn read
    代码片段：`pub fn read(device: &mut impl BlockDevice) -> Result<Self, Error> {`
  - `os/src/fs/block_cache.rs:L193-L193`：fn read
    代码片段：`pub fn read<T, V>(&self, offset: usize, f: impl FnOnce(&T) -> V) -> V {`
- 结构体/类型重合：与 2025 只编译了一次 在“文件系统”维度发现 1 个同名定义：DirEntry。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/fs/ext4.rs:L961-L961`：struct DirEntry
    代码片段：`struct DirEntry<'a> {`
  - `os/src/fs/file.rs:L90-L90`：struct DirEntry
    代码片段：`pub struct DirEntry {`
- 文件路径重合：与 2025 只编译了一次 在“文件系统”维度出现同名文件源码路径 `src/fs/file.rs` / `os/src/fs/file.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/fs/file.rs:L1319-L1323`：关键词命中
    代码片段：`)); } if let Some((inode, bytes)) = self.block_device_sysfs_bytes(&path) { return Ok(OpenFile::new( path,`
  - `os/src/fs/file.rs:L1-L3`：关键词命中
    代码片段：`use crate::fs::inode::{FatType, OSInode}; use crate::mm::UserBuffer; use alloc::string::String;`

## 相似点

- 与 2025 只编译了一次 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 只编译了一次 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/drivers/block.rs:L2-L6`：关键词命中
    代码片段：`#[cfg(target_arch = "riscv64")] mod virtio_mmio; pub const SECTOR_SIZE: usize = 512;`
  - `src/drivers/block/virtio_mmio.rs:L12-L16`：关键词命中
    代码片段：`}; const VIRTIO_MAGIC: u32 = 0x7472_6976; const VIRTIO_DEVICE_BLOCK: u32 = 2; const VIRTIO_VERSION_1_HIGH_BIT: u32 = 1;`
- 与 2025 只编译了一次 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/fs/ext4.rs:L251-L255`：关键词命中
    代码片段：`println!( "ext4: magic=0x{:04x} block_size={} inode_size={} blocks={} inodes={}", fs.superblock.magic, fs.superblock.block_size(), fs.superblock.inode_size,`
  - `src/fs/file.rs:L1319-L1323`：关键词命中
    代码片段：`)); } if let Some((inode, bytes)) = self.block_device_sysfs_bytes(&path) { return Ok(OpenFile::new( path,`
- 与 2025 只编译了一次 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/task/mod.rs:L9129-L9133`：关键词命中
    代码片段：`Ok(()) => 0, Err(error) => { println!("timer_create: copy timer id failed: {}", error.as_str()); -14 }`
  - `src/task/riscv64.rs:L193-L197`：关键词命中
    代码片段：`Err(error) => { println!( "trap: page fault handling failed scause={} sepc=0x{:x} stval=0x{:x}: {}", scause, frame.sepc,`
- 与 2025 只编译了一次 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/mm/user.rs:L53-L57`：关键词命中
    代码片段：`struct UserAddressSpaceInner { page_table: Sv39PageTable, pages: Vec<UserPage>, reservations: Vec<Reservation>,`
  - `src/mm/frame.rs:L6-L10`：关键词命中
    代码片段：`}; static FRAME_ALLOCATOR: FrameAllocator = FrameAllocator::new(); const MAX_FREE_FRAMES: usize = 32768; static mut FREE_FRAMES: [usize; MAX_FREE_FRAMES] = [0; MAX_FREE_FRAMES];`
- 与 2025 只编译了一次 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/task/mod.rs:L171-L175`：关键词命中
    代码片段：`FutexRequeue(FutexRequeueRequest), Sleep(SleepRequest), Yield, Exit(isize), ExitGroup(isize),`
  - `src/task/riscv64.rs:L13-L17`：关键词命中
    代码片段：`mm::user::LoadedElf, println, task::{ AcceptWaitRequest, DatagramReadWaitRequest, DatagramWriteWaitRequest, EpollWaitRequest, ExitStatus, FanotifyPermissionRequest, FutexRequeue...`
- 与 2025 只编译了一次 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/fs/file.rs:L6-L10`：关键词命中
    代码片段：`}; use core::cell::RefCell; use core::sync::atomic::{AtomicI32, Ordering}; use crate::{`
  - `src/mm/heap.rs:L2-L6`：关键词命中
    代码片段：`use core::alloc::{GlobalAlloc, Layout}; use core::ptr::null_mut; use core::sync::atomic::{AtomicBool, AtomicUsize, Ordering}; use crate::{mm::align_up, println};`
- 与 2025 只编译了一次 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/mm/user.rs:L779-L783`：关键词命中
    代码片段：`const TRAMPOLINE: [u8; 8] = [ 0x93, 0x08, 0xb0, 0x08, // li a7, 139 0x73, 0x00, 0x00, 0x00, // ecall ];`
  - `src/task/mod.rs:L1220-L1224`：关键词命中
    代码片段：`} pub fn handle_syscall(&mut self, number: usize, args: [usize; 6]) -> SyscallResult { if number == 221 { return self.sys_execve(args[0], args[1], args[2]);`
- 与 2025 只编译了一次 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 只编译了一次 的语言构成不同：待测作品为 {'build': 48, 'markdown': 6994, 'asm': 192, 'rust': 30521, 'c': 1176}，历史样本为 {'json': 20, 'build': 367, 'markdown': 4892, 'toml': 117, 'rust': 12330, 'text': 6, 'c': 1833, 'asm': 366, 'make': 116}。（置信度：medium）

## 可能创新点

- 待测作品包含 359 个历史样本 2025 只编译了一次 中未见的独特函数/类型定义（如 `read_exact`, `discover_script_programs`, `append_memory_file`, `close_range`, `reserve_range`, `link_memory`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `src/fs/ext4.rs:L1182-L1182`：fn read_exact
    代码片段：`fn read_exact(device: &mut impl BlockDevice, offset: u64, output: &mut [u8]) -> Result<(), Error> {`
  - `src/fs/ext4.rs:L493-L493`：fn discover_script_programs
    代码片段：`pub fn discover_script_programs(device: &mut impl BlockDevice, fs: &Ext4) -> Vec<ScriptProgram> {`

## 附录：核验摘要

- 关键结论数：21
- 含证据关键结论数：21（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
