# 2026 T2026100069910965 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026100069910965-1418`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：74
- 代码/文本行数：11362
- 主要语言：c 9339 LOC, markdown 1373 LOC, asm 459 LOC, build 171 LOC, json 20 LOC

## 总览

2026 T2026100069910965 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 c: 9339 LOC, markdown: 1373 LOC, asm: 459 LOC, build: 171 LOC。仓库包含 74 个已扫描文件、约 11362 行可分析文本，当前抽取到 639 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（90/100）
- 已确认 OS 维度：6/7；高置信维度：6/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 70/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 未确认 | unconfirmed | 0 |
| 中断与异常 | 已确认 | high | 3 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, arch/loongarch/linker.ld, arch/riscv/linker.ld。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`SHELL := /bin/sh BUILD_DIR   := build`
  - `arch/loongarch/linker.ld:L1-L3`：构建入口
    代码片段：`OUTPUT_ARCH(loongarch) ENTRY(_start)`
  - `arch/riscv/linker.ld:L1-L3`：构建入口
    代码片段：`OUTPUT_ARCH(riscv) ENTRY(_start)`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct context, struct trapframe, enum procstate, struct vma, struct proc。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/proc.c` | L1-L3 | 关键词命中 |
| `include/kernel/proc.h` | L1-L3 | 关键词命中 |
| `kernel/cpu.c` | L1-L4 | 关键词命中 |
| `include/kernel/proc.h` | L8-L12 | struct context |
| `include/kernel/proc.h` | L15-L19 | struct trapframe |
| `include/kernel/proc.h` | L56-L60 | enum procstate |

### 关键代码片段

  - `kernel/proc.c:L1-L3`：关键词命中
    代码片段：`/* Process table, kernel threads, and the round-robin scheduler. * * Phase 1: cooperative + timer-preemptive scheduling of kernel threads. Each`
  - `include/kernel/proc.h:L1-L3`：关键词命中
    代码片段：`/* Process / thread model and scheduler interface. */ #ifndef _KERNEL_PROC_H #define _KERNEL_PROC_H`
  - `kernel/cpu.c:L1-L4`：关键词命中
    代码片段：`/* Per-CPU state. Single-hart for now (secondary harts are parked in boot.S). */ #include "kernel/proc.h" static struct cpu the_cpu;`
  - `include/kernel/proc.h:L8-L12`：struct context
    代码片段：`/* Callee-saved register set switched by swtch() (arch/<arch>/swtch.S). * Layout MUST match the assembly: ra @0, sp @8, s0..s11 @16.. */ struct context { u64 ra; u64 sp;`

### 相关符号

`struct context` at `include/kernel/proc.h:L8`、`struct trapframe` at `include/kernel/proc.h:L15`、`enum procstate` at `include/kernel/proc.h:L56`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：macro PGSIZE, macro PTE_V, macro PTE_R, macro PTE_W, macro PTE_X。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `include/kernel/mm.h` | L16-L20 | 关键词命中 |
| `include/kernel/vm.h` | L1-L4 | 关键词命中 |
| `kernel/elf.c` | L1-L3 | 关键词命中 |
| `include/kernel/param.h` | L3-L7 | macro PGSIZE |
| `include/kernel/riscv.h` | L27-L31 | macro PTE_V |
| `include/kernel/riscv.h` | L28-L32 | macro PTE_R |

### 关键代码片段

  - `include/kernel/mm.h:L16-L20`：关键词命中
    代码片段：`void *kmalloc(size_t n); void *kzalloc(size_t n); void  kfree(void *p); #endif /* _KERNEL_MM_H */`
  - `include/kernel/vm.h:L1-L4`：关键词命中
    代码片段：`/* Virtual-memory and user-trap interface (RISC-V implementation in arch/riscv). * Page permission bits use the RISC-V Sv39 PTE encoding. */ #ifndef _KERNEL_VM_H #define _KERNEL...`
  - `kernel/elf.c:L1-L3`：关键词命中
    代码片段：`/* Minimal ELF64 loader: map PT_LOAD segments into a user page table and report * the program-header location so exec can build the auxv. */ #include "kernel/types.h"`
  - `include/kernel/param.h:L3-L7`：macro PGSIZE
    代码片段：`#define _KERNEL_PARAM_H #define PGSIZE   4096UL #define PGSHIFT  12`

### 相关符号

`macro PGSIZE` at `include/kernel/param.h:L3`、`macro PTE_V` at `include/kernel/riscv.h:L27`、`macro PTE_R` at `include/kernel/riscv.h:L28`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：struct statfs64, macro SYS_getcwd, macro SYS_dup, macro SYS_dup3, macro SYS_fcntl。（置信度：medium）
  - 静态识别到 8 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/syscall.c` | L6-L10 | 关键词命中 |
| `include/kernel/syscall.h` | L1-L3 | 关键词命中 |
| `arch/riscv/sbi.c` | L31-L35 | 关键词命中 |
| `kernel/syscall.c` | L510-L514 | struct statfs64 |
| `include/kernel/syscall.h` | L5-L9 | macro SYS_getcwd |
| `include/kernel/syscall.h` | L6-L10 | macro SYS_dup |

### 关键代码片段

  - `kernel/syscall.c:L6-L10`：关键词命中
    代码片段：`#include "kernel/vm.h" #include "kernel/mm.h" #include "kernel/syscall.h" #include "kernel/errno.h" #include "kernel/printk.h"`
  - `include/kernel/syscall.h:L1-L3`：关键词命中
    代码片段：`/* Syscall numbers (Linux generic ABI, shared by RV64 and LA64) and dispatch. */ #ifndef _KERNEL_SYSCALL_H #define _KERNEL_SYSCALL_H`
  - `arch/riscv/sbi.c:L31-L35`：关键词命中
    代码片段：`register long a6 asm("a6") = fid; register long a7 asm("a7") = ext; asm volatile("ecall" : "+r"(a0), "+r"(a1) : "r"(a2), "r"(a6), "r"(a7)`
  - `kernel/syscall.c:L510-L514`：struct statfs64
    代码片段：`static long sys_statfs_common(struct proc *p, u64 ubuf) { struct statfs64 { i64 f_type; i64 f_bsize;`

### 相关符号

`struct statfs64` at `kernel/syscall.c:L510`、`macro SYS_getcwd` at `include/kernel/syscall.h:L5`、`macro SYS_dup` at `include/kernel/syscall.h:L6`、`macro _KERNEL_SYSCALL_H` at `include/kernel/syscall.h:L1`、`fn sys_getpid` at `kernel/sysproc.c:L143`、`fn sys_getppid` at `kernel/sysproc.c:L144`、`fn sys_gettid` at `kernel/sysproc.c:L145`、`fn sys_getuid` at `kernel/sysproc.c:L146`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct pipe, fn le16, fn le32, struct block_cache_entry, struct lookup_cache_entry。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `fs/ext4/ext4.c` | L1-L3 | 关键词命中 |
| `include/fs/ext4.h` | L7-L11 | 关键词命中 |
| `include/fs/file.h` | L47-L51 | 关键词命中 |
| `fs/pipe.c` | L7-L11 | struct pipe |
| `fs/ext4/ext4.c` | L14-L18 | fn le16 |
| `fs/ext4/ext4.c` | L15-L19 | fn le32 |

### 关键代码片段

  - `fs/ext4/ext4.c:L1-L3`：关键词命中
    代码片段：`/* Read-only ext4 driver: superblock, group descriptors, inodes, extent trees, * directory lookup and file reads. Sufficient to load test binaries/scripts * from the contest dis...`
  - `include/fs/ext4.h:L7-L11`：关键词命中
    代码片段：`#define EXT4_ROOT_INO 2 int  ext4_mount(void);   /* read + validate the superblock; 0 on success */ /* Resolve an absolute path to its inode number, or 0 if not found. */`
  - `include/fs/file.h:L47-L51`：关键词命中
    代码片段：`int   dev; u64   off; u32   ino;             /* FD_INODE: ext4 inode number */ u64   size;            /* FD_INODE: file size */ int   is_dir;`
  - `fs/pipe.c:L7-L11`：struct pipe
    代码片段：`#define PIPESIZE 512 struct pipe { char buf[PIPESIZE]; u64  nread;     /* bytes consumed */`

### 相关符号

`struct pipe` at `fs/pipe.c:L7`、`fn le16` at `fs/ext4/ext4.c:L14`、`fn le32` at `fs/ext4/ext4.c:L15`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：未确认该维度存在可追溯实现线索。（综合置信度：unconfirmed）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：当前未在核心源码路径中确认 同步机制 的实现证据。

### 证据表

| 证据 | 说明 |
| --- | --- |
| 未确认 | 当前未找到可引用的源码证据 |

### 复核建议

- 建议人工补查目录命名不典型的源码文件，或在后续版本中扩展该维度关键词。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `include/kernel/trap.h` | L1-L3 | 关键词命中 |
| `kernel/main.c` | L10-L14 | 关键词命中 |
| `kernel/proc.c` | L1-L5 | 关键词命中 |

### 关键代码片段

  - `include/kernel/trap.h:L1-L3`：关键词命中
    代码片段：`/* Trap / interrupt setup (arch-specific implementation). */ #ifndef _KERNEL_TRAP_H #define _KERNEL_TRAP_H`
  - `kernel/main.c:L10-L14`：关键词命中
    代码片段：`#include "kernel/mm.h" #include "kernel/proc.h" #include "kernel/trap.h" #include "drivers/blkdev.h" #include "fs/ext4.h"`
  - `kernel/proc.c:L1-L5`：关键词命中
    代码片段：`/* Process table, kernel threads, and the round-robin scheduler. * * Phase 1: cooperative + timer-preemptive scheduling of kernel threads. Each * thread owns a kernel stack from...`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：macro VIRTIO_MMIO_BASE, macro VIRTIO_MMIO_STRIDE, macro VIRTIO_MMIO_SLOTS, macro VIRTIO_MMIO_MAGIC_VALUE, macro VIRTIO_MMIO_VERSION。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `drivers/virtio_blk.c` | L1-L3 | 关键词命中 |
| `include/drivers/blkdev.h` | L1-L3 | 关键词命中 |
| `arch/riscv/sbi.c` | L1-L3 | 关键词命中 |
| `drivers/virtio_blk.c` | L15-L19 | macro VIRTIO_MMIO_BASE |
| `drivers/virtio_blk.c` | L16-L20 | macro VIRTIO_MMIO_STRIDE |
| `drivers/virtio_blk.c` | L17-L21 | macro VIRTIO_MMIO_SLOTS |

### 关键代码片段

  - `drivers/virtio_blk.c:L1-L3`：关键词命中
    代码片段：`/* virtio-blk over virtio-mmio, polled. Supports both the legacy (version 1) * and modern (version 2) transports — QEMU's virt machine defaults to legacy. *`
  - `include/drivers/blkdev.h:L1-L3`：关键词命中
    代码片段：`/* Block device interface (single virtio-blk disk for now). */ #ifndef _DRIVERS_BLKDEV_H #define _DRIVERS_BLKDEV_H`
  - `arch/riscv/sbi.c:L1-L3`：关键词命中
    代码片段：`/* RISC-V platform glue: NS16550 UART console + OpenSBI shutdown. */ #include "kernel/types.h" #include "kernel/kio.h"`
  - `drivers/virtio_blk.c:L15-L19`：macro VIRTIO_MMIO_BASE
    代码片段：`void *memset(void *dst, int c, size_t n); #define VIRTIO_MMIO_BASE   0x10001000UL #define VIRTIO_MMIO_STRIDE 0x1000UL #define VIRTIO_MMIO_SLOTS  8`

### 相关符号

`macro VIRTIO_MMIO_BASE` at `drivers/virtio_blk.c:L15`、`macro VIRTIO_MMIO_STRIDE` at `drivers/virtio_blk.c:L16`、`macro VIRTIO_MMIO_SLOTS` at `drivers/virtio_blk.c:L17`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：13
- 含证据关键结论数：13（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：1
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
