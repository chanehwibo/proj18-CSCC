# 2026 T2026104869911460 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026104869911460-387`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：5957
- 代码/文本行数：1876393
- 主要语言：rust 1805770 LOC, markdown 39671 LOC, build 19422 LOC, toml 10008 LOC, asm 578 LOC

## 总览

2026 T2026104869911460 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 1805770 LOC, markdown: 39671 LOC, build: 19422 LOC, toml: 10008 LOC。仓库包含 5957 个已扫描文件、约 1876393 行可分析文本，当前抽取到 122842 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, NoAxiom/Cargo.toml, NoAxiom/Makefile, NoAxiom/kernel/Cargo.toml, NoAxiom/kernel/Makefile。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`# AsyncBridge Makefile # General config`
  - `NoAxiom/Cargo.toml:L1-L3`：构建入口
    代码片段：`# workspace for kernel [workspace] members = ["kernel"]`
  - `NoAxiom/Makefile:L1-L3`：构建入口
    代码片段：`# Compatibility wrapper for graders that run make from the NoAxiom subdir. .PHONY: all build build-all clean default`
  - `NoAxiom/kernel/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] edition = "2021" name = "kernel"`
  - `NoAxiom/kernel/Makefile:L1-L3`：构建入口
    代码片段：`ifeq ($(MODE),release) CARGO_ARGS += --release endif`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct PCB, fn default, fn status, fn set_status, fn set_status_with_wait。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `NoAxiom/kernel/src/task/mod.rs` | L10-L14 | 关键词命中 |
| `NoAxiom/kernel/src/task/pcb.rs` | L6-L10 | 关键词命中 |
| `NoAxiom/kernel/src/task/tcb.rs` | L1-L4 | 关键词命中 |
| `NoAxiom/kernel/src/task/pcb.rs` | L16-L20 | struct PCB |
| `NoAxiom/kernel/src/task/pcb.rs` | L38-L42 | fn default |
| `NoAxiom/kernel/src/task/pcb.rs` | L56-L60 | fn status |

### 关键代码片段

  - `NoAxiom/kernel/src/task/mod.rs:L10-L14`：关键词命中
    代码片段：`pub mod signal; pub mod status; pub mod task; pub mod task_main; pub mod taskid;`
  - `NoAxiom/kernel/src/task/pcb.rs:L6-L10`：关键词命中
    代码片段：`use bitflags::Flags; use super::{exit::ExitCode, status::TaskStatus, Task}; use crate::{ include::process::{robust_list::RobustList, TaskFlags},`
  - `NoAxiom/kernel/src/task/tcb.rs:L1-L4`：关键词命中
    代码片段：`use alloc::sync::Arc; use core::{sync::atomic::AtomicBool, task::Waker}; use arch::TrapContext;`
  - `NoAxiom/kernel/src/task/pcb.rs:L16-L20`：struct PCB
    代码片段：`/// so there's no need to use any lock in this struct #[repr(align(64))] pub struct PCB { // task status pub status: TaskStatus,  // task status`

### 相关符号

`struct PCB` at `NoAxiom/kernel/src/task/pcb.rs:L16`、`fn default` at `NoAxiom/kernel/src/task/pcb.rs:L38`、`fn status` at `NoAxiom/kernel/src/task/pcb.rs:L56`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn frame_init, fn ekernel, struct ShmManager, struct ShmArea, struct ShmTracker。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `NoAxiom/kernel/src/mm/mod.rs` | L4-L8 | 关键词命中 |
| `NoAxiom/lib/config/src/mm.rs` | L12-L16 | 关键词命中 |
| `NoAxiom/kernel/src/include/mm.rs` | L27-L31 | 关键词命中 |
| `NoAxiom/kernel/src/mm/mod.rs` | L14-L18 | fn frame_init |
| `NoAxiom/kernel/src/mm/mod.rs` | L16-L20 | fn ekernel |
| `NoAxiom/kernel/src/mm/shm.rs` | L24-L28 | struct ShmManager |

### 关键代码片段

  - `NoAxiom/kernel/src/mm/mod.rs:L4-L8`：关键词命中
    代码片段：`pub mod memory_set; pub mod mmap_manager; pub mod page_table; pub mod permission; pub mod shm;`
  - `NoAxiom/lib/config/src/mm.rs:L12-L16`：关键词命中
    代码片段：`/// raw vpn & ppn width: 9 pub const PAGE_NUM_WIDTH: usize = PAGE_WIDTH - 3; /// page table entry per page: 512 pub const PTE_PER_PAGE: usize = 1 << PAGE_NUM_WIDTH;`
  - `NoAxiom/kernel/src/include/mm.rs:L27-L31`：关键词命中
    代码片段：`} if prots.contains(MmapProts::PROT_WRITE) { // RISC-V treats W without R as a reserved PTE encoding. Keep the // logical mmap prot unchanged; only the hardware PTE gains R. fla...`
  - `NoAxiom/kernel/src/mm/mod.rs:L14-L18`：fn frame_init
    代码片段：`use platform::memory::PHYS_MEMORY_END; pub fn frame_init() { extern "C" { fn ekernel(); // virt address`

### 相关符号

`fn frame_init` at `NoAxiom/kernel/src/mm/mod.rs:L14`、`fn ekernel` at `NoAxiom/kernel/src/mm/mod.rs:L16`、`struct ShmManager` at `NoAxiom/kernel/src/mm/shm.rs:L24`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：struct LinuxFileHandleHeader, struct FileHandleEntry, impl Syscall<'_>, fn fs_ioctl_allowed, fn check_sendfile_offset_ptr_writable。（置信度：medium）
  - 静态识别到 502 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `NoAxiom/kernel/src/syscall/fs.rs` | L12-L16 | 关键词命中 |
| `NoAxiom/kernel/src/syscall/io.rs` | L10-L14 | 关键词命中 |
| `NoAxiom/kernel/src/syscall/mm.rs` | L26-L30 | 关键词命中 |
| `NoAxiom/kernel/src/syscall/fs.rs` | L91-L95 | struct LinuxFileHandleHeader |
| `NoAxiom/kernel/src/syscall/fs.rs` | L97-L101 | struct FileHandleEntry |
| `NoAxiom/kernel/src/syscall/fs.rs` | L108-L112 | impl Syscall<'_> |

### 关键代码片段

  - `NoAxiom/kernel/src/syscall/fs.rs:L12-L16`：关键词命中
    代码片段：`use memory::address::PhysAddr; use super::{Syscall, SyscallResult}; use crate::{ constant::fs::{`
  - `NoAxiom/kernel/src/syscall/io.rs:L10-L14`：关键词命中
    代码片段：`use ksync::assert_no_lock; use super::{Syscall, SyscallResult}; use crate::{ constant::fs::AT_FDCWD,`
  - `NoAxiom/kernel/src/syscall/mm.rs:L26-L30`：关键词命中
    代码片段：`}, return_errno, syscall::Syscall, utils::align_ceil, };`
  - `NoAxiom/kernel/src/syscall/fs.rs:L91-L95`：struct LinuxFileHandleHeader
    代码片段：`#[repr(C)] #[derive(Clone, Copy)] struct LinuxFileHandleHeader { handle_bytes: u32, handle_type: i32,`

### 相关符号

`struct LinuxFileHandleHeader` at `NoAxiom/kernel/src/syscall/fs.rs:L91`、`struct FileHandleEntry` at `NoAxiom/kernel/src/syscall/fs.rs:L97`、`impl Syscall<'_>` at `NoAxiom/kernel/src/syscall/fs.rs:L108`、`impl Syscall<'_>` at `NoAxiom/kernel/src/syscall/fs.rs:L108`、`fn sys_umask` at `NoAxiom/kernel/src/syscall/fs.rs:L760`、`fn sys_getcwd` at `NoAxiom/kernel/src/syscall/fs.rs:L806`、`fn sys_pipe2` at `NoAxiom/kernel/src/syscall/fs.rs:L834`、`fn sys_dup` at `NoAxiom/kernel/src/syscall/fs.rs:L862`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：enum FatType, fn from_clusters, fn bits_per_fat_entry, fn min_clusters, fn max_clusters。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `NoAxiom/lib/fatfs/src/fs.rs` | L24-L28 | 关键词命中 |
| `NoAxiom/kernel/src/fs/mod.rs` | L24-L28 | 关键词命中 |
| `NoAxiom/kernel/src/fs/nsfd.rs` | L8-L12 | 关键词命中 |
| `NoAxiom/lib/fatfs/src/fs.rs` | L32-L36 | enum FatType |
| `NoAxiom/lib/fatfs/src/fs.rs` | L46-L50 | fn from_clusters |
| `NoAxiom/lib/fatfs/src/fs.rs` | L56-L60 | fn bits_per_fat_entry |

### 关键代码片段

  - `NoAxiom/lib/fatfs/src/fs.rs:L24-L28`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `NoAxiom/kernel/src/fs/mod.rs:L24-L28`：关键词命中
    代码片段：`pub mod socketpair; pub mod timerfd; pub mod vfs; use arch::{Arch, ArchInt};`
  - `NoAxiom/kernel/src/fs/nsfd.rs:L8-L12`：关键词命中
    代码片段：`use crate::{ config::fs::BLOCK_SIZE, fs::vfs::basic::{ dentry::{Dentry, EmptyDentry}, file::{File, FileMeta},`
  - `NoAxiom/lib/fatfs/src/fs.rs:L32-L36`：enum FatType
    代码片段：`/// 'FatType' values are based on the size of File Allocation Table entry. #[derive(Copy, Clone, Eq, PartialEq, Debug)] pub enum FatType { /// 12 bits per FAT entry Fat12,`

### 相关符号

`enum FatType` at `NoAxiom/lib/fatfs/src/fs.rs:L32`、`fn from_clusters` at `NoAxiom/lib/fatfs/src/fs.rs:L46`、`fn bits_per_fat_entry` at `NoAxiom/lib/fatfs/src/fs.rs:L56`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：enum RecordLockKind, fn from_flock_type, fn as_flock_type, struct RecordLock, fn conflicts。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `NoAxiom/kernel/src/fs/record_lock.rs` | L7-L11 | 关键词命中 |
| `NoAxiom/vendor/concurrent-queue/src/sync.rs` | L1-L5 | 关键词命中 |
| `NoAxiom/vendor/crossbeam-utils/src/sync/mod.rs` | L2-L6 | 关键词命中 |
| `NoAxiom/kernel/src/fs/record_lock.rs` | L17-L21 | enum RecordLockKind |
| `NoAxiom/kernel/src/fs/record_lock.rs` | L23-L27 | fn from_flock_type |
| `NoAxiom/kernel/src/fs/record_lock.rs` | L31-L35 | fn as_flock_type |

### 关键代码片段

  - `NoAxiom/kernel/src/fs/record_lock.rs:L7-L11`：关键词命中
    代码片段：`use include::errno::{Errno, SysResult}; use ksync::mutex::SpinLock; use crate::{`
  - `NoAxiom/vendor/concurrent-queue/src/sync.rs:L1-L5`：关键词命中
    代码片段：`//! Synchronization facade to choose between 'core' primitives and 'loom' primitives. #[cfg(all(feature = "portable-atomic", not(loom)))] mod sync_impl { pub(crate) use core::cell;`
  - `NoAxiom/vendor/crossbeam-utils/src/sync/mod.rs:L2-L6`：关键词命中
    代码片段：`//! //! * ['Parker'], a thread parking primitive. //! * ['ShardedLock'], a sharded reader-writer lock with fast concurrent reads. //! * ['WaitGroup'], for synchronizing the begi...`
  - `NoAxiom/kernel/src/fs/record_lock.rs:L17-L21`：enum RecordLockKind
    代码片段：`#[derive(Debug, Clone, Copy, PartialEq, Eq)] enum RecordLockKind { Read, Write,`

### 相关符号

`enum RecordLockKind` at `NoAxiom/kernel/src/fs/record_lock.rs:L17`、`fn from_flock_type` at `NoAxiom/kernel/src/fs/record_lock.rs:L23`、`fn as_flock_type` at `NoAxiom/kernel/src/fs/record_lock.rs:L31`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：trait TimerEvent, fn callback, struct Timer, impl Timer, fn new。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `NoAxiom/kernel/src/time/timer.rs` | L1-L4 | 关键词命中 |
| `NoAxiom/kernel/src/trap/ext_int.rs` | L1-L3 | 关键词命中 |
| `NoAxiom/kernel/src/trap/soft_int.rs` | L1-L3 | 关键词命中 |
| `NoAxiom/kernel/src/time/timer.rs` | L31-L35 | trait TimerEvent |
| `NoAxiom/kernel/src/time/timer.rs` | L37-L41 | fn callback |
| `NoAxiom/kernel/src/time/timer.rs` | L43-L47 | struct Timer |

### 关键代码片段

  - `NoAxiom/kernel/src/time/timer.rs:L1-L4`：关键词命中
    代码片段：`//! reference: Phoenix //! - time maneger: https://gitlab.eduxiji.net/educg-group-22026-2376550/T202418123993075-1053/-/blob/265a693df30be6e11d9b96a3466bd5a70cd0f6af/modules/tim...`
  - `NoAxiom/kernel/src/trap/ext_int.rs:L1-L3`：关键词命中
    代码片段：`//! external interrupt handler pub fn ext_int_handler() {`
  - `NoAxiom/kernel/src/trap/soft_int.rs:L1-L3`：关键词命中
    代码片段：`//! software interrupt handler use alloc::sync::Arc;`
  - `NoAxiom/kernel/src/time/timer.rs:L31-L35`：trait TimerEvent
    代码片段：`/// The TimerEvent trait requires a callback method to be implemented, /// which will be called when the timer expires. pub trait TimerEvent: Send + Sync { /// The callback meth...`

### 相关符号

`trait TimerEvent` at `NoAxiom/kernel/src/time/timer.rs:L31`、`fn callback` at `NoAxiom/kernel/src/time/timer.rs:L37`、`struct Timer` at `NoAxiom/kernel/src/time/timer.rs:L43`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct Dma, fn new, fn paddr, fn vaddr, fn raw_slice。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `NoAxiom/vendor/virtio-drivers/src/hal.rs` | L5-L9 | 关键词命中 |
| `NoAxiom/vendor/virtio-drivers/src/lib.rs` | L1-L3 | 关键词命中 |
| `NoAxiom/vendor/virtio-drivers/src/queue.rs` | L21-L25 | 关键词命中 |
| `NoAxiom/vendor/virtio-drivers/src/hal.rs` | L10-L14 | struct Dma |
| `NoAxiom/vendor/virtio-drivers/src/hal.rs` | L29-L33 | fn new |
| `NoAxiom/vendor/virtio-drivers/src/hal.rs` | L43-L47 | fn paddr |

### 关键代码片段

  - `NoAxiom/vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `NoAxiom/vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
  - `NoAxiom/vendor/virtio-drivers/src/queue.rs:L21-L25`：关键词命中
    代码片段：`use zerocopy::{FromBytes, FromZeros, Immutable, IntoBytes, KnownLayout}; /// The mechanism for bulk data transport on virtio devices. /// /// Each device can have zero or more v...`
  - `NoAxiom/vendor/virtio-drivers/src/hal.rs:L10-L14`：struct Dma
    代码片段：`/// A region of contiguous physical memory used for DMA. #[derive(Debug)] pub struct Dma<H: Hal> { paddr: usize, vaddr: NonNull<u8>,`

### 相关符号

`struct Dma` at `NoAxiom/vendor/virtio-drivers/src/hal.rs:L10`、`fn new` at `NoAxiom/vendor/virtio-drivers/src/hal.rs:L29`、`fn paddr` at `NoAxiom/vendor/virtio-drivers/src/hal.rs:L43`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
