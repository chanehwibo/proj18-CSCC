# 2026 T202610055999826 比较报告

- 对比历史仓库：2023 Runable
- 生成时间：2026-07-01T20:56:13.100918+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2023 Runable（来源：赛事历史作品）：画像相似度 score=9.30；同属 rcore-variant 风格; 语言构成相似度 0.92; OS 维度重合度 1.00; 代码规模接近度 0.45

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2023 Runable 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`pub mod serial; #[cfg(feature = "arch_riscv64")]`
  - `src/drivers/serial.rs:L7-L11`：关键词命中
    代码片段：`const UART0: usize = 0x1000_0000; pub struct Serial; impl Serial {`
  - `os/src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)) } }`
  - `os/src/drivers/block/mod.rs:L1-L3`：关键词命中
    代码片段：`mod virtio_blk; mod block_device;`
- 与 2023 Runable 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/fs/ext4.rs:L141-L145`：关键词命中
    代码片段：`if ret != EXT4_SUPERBLOCK_SIZE as isize { crate::println!("[ext4] read superblock failed: ret={}", ret); return Err(err(EIO)); }`
  - `src/task/task.rs:L258-L262`：关键词命中
    代码片段：`} /// Load and exec an ELF from the EXT4 disk via VFS page cache + streaming. pub fn exec_elf_from_disk(&mut self, path: &str, argv: &[&[u8]]) -> isize { if self.kind != TaskKin...`
  - `os/src/fs/mod.rs:L1-L4`：关键词命中
    代码片段：`#![allow(unused)] mod inode; mod pipe; mod stdio;`
  - `os/src/fatfs/fs.rs:L76-L80`：关键词命中
    代码片段：`let cluster = { let mut fat = self.fat_slice(); match table_alloc_cluster(&mut fat, prev_cluster, hint, self.total_clusters) { Ok(x) => x,`
- 与 2023 Runable 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/trap/mod.rs:L2-L6`：关键词命中
    代码片段：`pub mod handler; // 进行trap入口设置 pub fn init() { crate::arch::set_trap_entry();`
  - `src/trap/handler.rs:L14-L18`：关键词命中
    代码片段：`pub extern "C" fn rust_trap_handler(cx: &mut TrapContext) { crate::kprintln!( "[trap] handler begin, from={}, scause={:#x}, sepc={:#x}, stval={:#x}", if cx.from_user() { "U" } e...`
  - `os/src/timer.rs:L88-L92`：关键词命中
    代码片段：`let current_ms = get_time_ms(); let mut timers = TIMERS.exclusive_access(); while let Some(timer) = timers.peek() { if timer.expire_ms <= current_ms { add_task(Arc::clone(&timer...`
  - `os/src/trap/mod.rs:L9-L13`：关键词命中
    代码片段：`}; use self::page_fault::{handle_page_fault}; use crate::timer::{check_timer, set_next_trigger}; use core::arch::{asm, global_asm}; use riscv::register::{`
- 与 2023 Runable 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`pub mod address; pub mod frame_allocator; pub mod heap_allocator; pub mod memory_set;`
  - `src/mm/memory_set.rs:L3-L7`：关键词命中
    代码片段：`use crate::arch::ArchPageTable; use crate::mm::address::{PAGE_SIZE, PhysAddr, PhysPageNum, VPNRange, VirtAddr, VirtPageNum}; use crate::mm::frame_allocator::{frame_alloc, frame_...`
  - `os/src/mm/mod.rs:L1-L4`：关键词命中
    代码片段：`mod address; mod frame_allocator; mod heap_allocator; mod memory_set;`
  - `os/src/mm/mmap.rs:L1-L3`：关键词命中
    代码片段：`use super::{frame_alloc, FrameTracker, MapPermission, translated_byte_buffer, UserBuffer}; use super::{PTEFlags, PageTable}; use super::{VirtAddr, VirtPageNum};`
- 与 2023 Runable 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/task/mod.rs:L3-L7`：关键词命中
    代码片段：`pub mod manager; pub mod processor; pub mod scheduler; pub mod sleep_queue; pub mod task;`
  - `src/task/task.rs:L76-L80`：关键词命中
    代码片段：`pub fn new_user_app(pid: usize, app_name: &str, parent: Option<usize>) -> Self { let (memory_set, entry, user_stack_top, _tls) = loader::load_user_app(app_name).expect("[task] u...`
  - `os/src/task/id.rs:L123-L127`：关键词命中
    代码片段：`pub ustack_top: usize,              // user stack top pub ustack_bottom: usize,          // user stack bottom // manager all the stacks(thread0 may have many stack frame, but ot...`
  - `os/src/task/mod.rs:L7-L11`：关键词命中
    代码片段：`mod switch; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
- 与 2023 Runable 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/drivers/virtio_pci.rs:L1-L5`：关键词命中
    代码片段：`// LoongArch virtio-blk over modern PCI transport use core::sync::atomic::{compiler_fence, Ordering}; use crate::syscall::errno::{EIO, ENODEV, err};`
  - `src/drivers/virtio_mmio.rs:L1-L4`：关键词命中
    代码片段：`use crate::syscall::errno::{EINVAL, EIO, ENODEV, err}; use core::sync::atomic::{Ordering, compiler_fence}; const VIRTIO_MMIO_BASE: usize = 0x1000_1000;`
  - `os/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`mod condvar; mod mutex; mod semaphore;`
  - `os/src/sync/mutex.rs:L5-L9`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub trait Mutex: Sync + Send { fn lock(&self); fn unlock(&self);`
- 与 2023 Runable 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `src/syscall/fd.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use crate::syscall::errno::{EBADF, EEXIST, EFAULT, EINVAL, EIO, EISDIR, ENOENT, ENOMEM, err}; use crate::task::task::UserFile;`
  - `src/syscall/mod.rs:L80-L84`：关键词命中
    代码片段：`pub const SYS_FORK: usize = 4000; pub fn syscall(id: usize, args: [usize; 6]) -> isize { match id { SYS_WRITE => fd::sys_write(args[0], args[1], args[2]),`
  - `os/src/syscall/mod.rs:L53-L57`：关键词命中
    代码片段：`use sys_info::*; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> isize { match syscall_id { SYSCALL_CHDIR => sys_chdir(args[0] as *const u8),`
  - `user/src/syscall.rs:L34-L38`：关键词命中
    代码片段：`// 目前系统调用参数除了id外最多6个 fn syscall(id: usize, args: [usize; 6]) -> isize { let mut ret: isize; unsafe {`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2023 Runable 在“文件系统”维度发现 4 个同名定义：is_dir, is_file, create_file, sys_mmap。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src/fs/ext4.rs:L67-L67`：fn is_dir
    代码片段：`pub fn is_dir(&self) -> bool {`
  - `os/src/fatfs/dir_entry.rs:L96-L96`：fn is_dir
    代码片段：`pub fn is_dir(&self) -> bool {`
  - `src/fs/ext4.rs:L71-L71`：fn is_file
    代码片段：`pub fn is_file(&self) -> bool {`
  - `os/src/fatfs/file.rs:L210-L210`：fn is_file
    代码片段：`pub fn is_file(&self) -> bool {`
- 函数/符号名重合：与 2023 Runable 在“系统调用”维度发现 4 个同名定义：sys_write, sys_read, sys_close, sys_dup。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `src/syscall/fd.rs:L19-L19`：fn sys_write
    代码片段：`pub fn sys_write(fd: usize, buf: usize, len: usize) -> isize {`
  - `user/src/syscall.rs:L76-L76`：fn sys_write
    代码片段：`pub fn sys_write(fd: usize, buffer: &[u8]) -> isize {`
  - `src/syscall/fd.rs:L87-L87`：fn sys_read
    代码片段：`pub fn sys_read(fd: usize, buf: usize, len: usize) -> isize {`
  - `user/src/syscall.rs:L69-L69`：fn sys_read
    代码片段：`pub fn sys_read(fd: usize, buffer: &mut [u8]) -> isize {`
- 与 2023 Runable 在“系统调用”维度发现片段级代码相似度 0.71 （token=0.59, structure=0.93）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：args, const, fn, isize, match, pub, syscall, usize。（置信度：medium）
  证据：
  - `src/syscall/mod.rs:L80-L84`：关键词命中
    代码片段：`pub const SYS_FORK: usize = 4000; pub fn syscall(id: usize, args: [usize; 6]) -> isize { match id { SYS_WRITE => fd::sys_write(args[0], args[1], args[2]),`
  - `os/src/syscall/mod.rs:L53-L57`：关键词命中
    代码片段：`use sys_info::*; pub fn syscall(syscall_id: usize, args: [usize; 6]) -> isize { match syscall_id { SYSCALL_CHDIR => sys_chdir(args[0] as *const u8),`
- 与 2023 Runable 在“系统调用”维度发现片段级代码相似度 0.61 （token=0.54, structure=0.73）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：buf, fd, fn, isize, len, let, pub, sys_write。（置信度：medium）
  证据：
  - `src/syscall/fd.rs:L17-L21`：fn sys_write
    代码片段：`} pub fn sys_write(fd: usize, buf: usize, len: usize) -> isize { let mut copied = 0; let mut tmp = [0u8; 256];`
  - `os/src/syscall/fs.rs:L46-L50`：fn sys_write
    代码片段：`} pub fn sys_write(fd: usize, buf: *const u8, len: usize) -> isize { if fd != 1 {println!("write fd {}",fd);} let token = current_user_token();`
- 函数/符号名重合：与 2023 Runable 在“设备驱动”维度发现 1 个同名定义：write_str。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/drivers/serial.rs:L32-L32`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
  - `user/src/console.rs:L11-L11`：fn write_str
    代码片段：`fn write_str(&mut self, s: &str) -> fmt::Result {`
- 函数/符号名重合：与 2023 Runable 在“内存管理”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/mm/address.rs:L97-L97`：fn new
    代码片段：`pub fn new(start: VirtPageNum, end: VirtPageNum) -> Self {`
  - `user/src/bin/user_shell.rs:L30-L30`：fn new
    代码片段：`pub fn new(command: &str) -> Self {`
- 函数/符号名重合：与 2023 Runable 在“调度与任务管理”维度发现 1 个同名定义：sys_yield。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/syscall/task.rs:L7-L7`：fn sys_yield
    代码片段：`pub fn sys_yield() -> isize {`
  - `user/src/syscall.rs:L89-L89`：fn sys_yield
    代码片段：`pub fn sys_yield() -> isize {`
- 函数/符号名重合：与 2023 Runable 在“同步机制”维度发现 1 个同名定义：lock。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `src/mm/heap_allocator.rs:L29-L29`：fn lock
    代码片段：`pub fn lock(&self) -> &mut T {`
  - `user/src/bin/adder_atomic.rs:L27-L27`：fn lock
    代码片段：`fn lock() {`
- 文件路径重合：与 2023 Runable 在“内存管理”维度出现同名文件源码路径 `src/mm/memory_set.rs` / `os/src/mm/memory_set.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `src/mm/memory_set.rs:L3-L7`：关键词命中
    代码片段：`use crate::arch::ArchPageTable; use crate::mm::address::{PAGE_SIZE, PhysAddr, PhysPageNum, VPNRange, VirtAddr, VirtPageNum}; use crate::mm::frame_allocator::{frame_alloc, frame_...`
  - `os/src/mm/memory_set.rs:L1-L4`：关键词命中
    代码片段：`use super::mmap::{MmapFlags, MmapProts}; use super::{frame_alloc, FrameTracker, frame_add_ref, frame_get_ref}; use super::{PTEFlags, PageTable, PageTableEntry}; use super::{Phys...`

## 相似点

- 与 2023 Runable 同属 rcore-variant 风格。（置信度：medium）
- 与 2023 Runable 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/drivers/mod.rs:L1-L3`：关键词命中
    代码片段：`pub mod serial; #[cfg(feature = "arch_riscv64")]`
  - `src/drivers/serial.rs:L7-L11`：关键词命中
    代码片段：`const UART0: usize = 0x1000_0000; pub struct Serial; impl Serial {`
- 与 2023 Runable 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/fs/ext4.rs:L141-L145`：关键词命中
    代码片段：`if ret != EXT4_SUPERBLOCK_SIZE as isize { crate::println!("[ext4] read superblock failed: ret={}", ret); return Err(err(EIO)); }`
  - `src/task/task.rs:L258-L262`：关键词命中
    代码片段：`} /// Load and exec an ELF from the EXT4 disk via VFS page cache + streaming. pub fn exec_elf_from_disk(&mut self, path: &str, argv: &[&[u8]]) -> isize { if self.kind != TaskKin...`
- 与 2023 Runable 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/trap/mod.rs:L2-L6`：关键词命中
    代码片段：`pub mod handler; // 进行trap入口设置 pub fn init() { crate::arch::set_trap_entry();`
  - `src/trap/handler.rs:L14-L18`：关键词命中
    代码片段：`pub extern "C" fn rust_trap_handler(cx: &mut TrapContext) { crate::kprintln!( "[trap] handler begin, from={}, scause={:#x}, sepc={:#x}, stval={:#x}", if cx.from_user() { "U" } e...`
- 与 2023 Runable 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`pub mod address; pub mod frame_allocator; pub mod heap_allocator; pub mod memory_set;`
  - `src/mm/memory_set.rs:L3-L7`：关键词命中
    代码片段：`use crate::arch::ArchPageTable; use crate::mm::address::{PAGE_SIZE, PhysAddr, PhysPageNum, VPNRange, VirtAddr, VirtPageNum}; use crate::mm::frame_allocator::{frame_alloc, frame_...`
- 与 2023 Runable 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/task/mod.rs:L3-L7`：关键词命中
    代码片段：`pub mod manager; pub mod processor; pub mod scheduler; pub mod sleep_queue; pub mod task;`
  - `src/task/task.rs:L76-L80`：关键词命中
    代码片段：`pub fn new_user_app(pid: usize, app_name: &str, parent: Option<usize>) -> Self { let (memory_set, entry, user_stack_top, _tls) = loader::load_user_app(app_name).expect("[task] u...`
- 与 2023 Runable 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/drivers/virtio_pci.rs:L1-L5`：关键词命中
    代码片段：`// LoongArch virtio-blk over modern PCI transport use core::sync::atomic::{compiler_fence, Ordering}; use crate::syscall::errno::{EIO, ENODEV, err};`
  - `src/drivers/virtio_mmio.rs:L1-L4`：关键词命中
    代码片段：`use crate::syscall::errno::{EINVAL, EIO, ENODEV, err}; use core::sync::atomic::{Ordering, compiler_fence}; const VIRTIO_MMIO_BASE: usize = 0x1000_1000;`
- 与 2023 Runable 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `src/syscall/fd.rs:L2-L6`：关键词命中
    代码片段：`use alloc::vec::Vec; use crate::syscall::errno::{EBADF, EEXIST, EFAULT, EINVAL, EIO, EISDIR, ENOENT, ENOMEM, err}; use crate::task::task::UserFile;`
  - `src/syscall/mod.rs:L80-L84`：关键词命中
    代码片段：`pub const SYS_FORK: usize = 4000; pub fn syscall(id: usize, args: [usize; 6]) -> isize { match id { SYS_WRITE => fd::sys_write(args[0], args[1], args[2]),`
- 与 2023 Runable 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2023 Runable 的语言构成不同：待测作品为 {'json': 20, 'build': 219, 'text': 1, 'markdown': 236, 'toml': 23, 'rust': 11238, 'asm': 1054}，历史样本为 {'json': 20, 'build': 593, 'markdown': 1658, 'toml': 7, 'text': 283, 'rust': 40526, 'asm': 569, 'yaml': 79, 'c': 48}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：23
- 含证据关键结论数：23（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
