# 2026 T202610486999642 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t202610486999642-1278`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：2253
- 代码/文本行数：759351
- 主要语言：rust 695305 LOC, c 27967 LOC, markdown 18876 LOC, build 7331 LOC, toml 6865 LOC

## 总览

2026 T202610486999642 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 695305 LOC, c: 27967 LOC, markdown: 18876 LOC, build: 7331 LOC。仓库包含 2253 个已扫描文件、约 759351 行可分析文本，当前抽取到 47843 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Makefile, os/Cargo.toml, os/Makefile, os/build.rs, testsuits/ltp-full-20240524/runtest/Makefile。（置信度：high）
  证据：
  - `Makefile:L1-L3`：构建入口
    代码片段：`MODE ?= release PERF_COUNTERS ?= 0 CARGO_HOME ?= $(CURDIR)/vendor`
  - `os/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "os" version = "0.1.1"`
  - `os/Makefile:L1-L3`：构建入口
    代码片段：`ARCH ?= riscv64 MODE ?= release PERF_COUNTERS ?= 0`
  - `os/build.rs:L1-L3`：构建入口
    代码片段：`fn main() { println!("cargo::rerun-if-env-changed=TARGET");`
  - `testsuits/ltp-full-20240524/runtest/Makefile:L1-L3`：构建入口
    代码片段：`# #    runtest Makefile. #`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct FdFlags, struct FdTableEntry, fn from_file, fn from_file_with_dir_path, fn duplicate。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/task/id.rs` | L139-L143 | 关键词命中 |
| `os/src/task/mod.rs` | L13-L17 | 关键词命中 |
| `os/src/task/exec.rs` | L330-L334 | 关键词命中 |
| `os/src/task/fd.rs` | L12-L16 | struct FdFlags |
| `os/src/task/fd.rs` | L22-L26 | struct FdTableEntry |
| `os/src/task/fd.rs` | L32-L36 | fn from_file |

### 关键代码片段

  - `os/src/task/id.rs:L139-L143`：关键词命中
    代码片段：`} // Per-thread stack windows keep one unmapped guard page between neighboring // user stacks by spacing bases with PAGE_SIZE + USER_STACK_SIZE. fn ustack_bottom_from_tid(ustack...`
  - `os/src/task/mod.rs:L13-L17`：关键词命中
    代码片段：`mod signal; #[allow(clippy::module_inception)] mod task; use self::id::TaskUserRes;`
  - `os/src/task/exec.rs:L330-L334`：关键词命中
    代码片段：`/// The caller has already resolved the executable/interpreter and copied /// argv/envp from userspace. This function owns the process image switch: /// memory set replacement,...`
  - `os/src/task/fd.rs:L12-L16`：struct FdFlags
    代码片段：`/// 'O_NONBLOCK', which belong to the shared open file description. #[derive(Clone, Copy, Debug, Default, PartialEq, Eq)] pub struct FdFlags: u32 { const CLOEXEC = 1; }`

### 相关符号

`struct FdFlags` at `os/src/task/fd.rs:L12`、`struct FdTableEntry` at `os/src/task/fd.rs:L22`、`fn from_file` at `os/src/task/fd.rs:L32`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn init, enum ShmError, struct ShmCreateContext, struct ShmCaller, struct ShmSetAttrs。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/mm/mod.rs` | L2-L6 | 关键词命中 |
| `os/src/mm/shm.rs` | L1-L3 | 关键词命中 |
| `os/src/mm/area.rs` | L1-L3 | 关键词命中 |
| `os/src/mm/mod.rs` | L25-L29 | fn init |
| `os/src/mm/shm.rs` | L34-L38 | enum ShmError |
| `os/src/mm/shm.rs` | L45-L49 | struct ShmCreateContext |

### 关键代码片段

  - `os/src/mm/mod.rs:L2-L6`：关键词命中
    代码片段：`mod area; mod elf_loader; mod frame_allocator; mod heap_allocator; mod kernel_space;`
  - `os/src/mm/shm.rs:L1-L3`：关键词命中
    代码片段：`use super::{FrameTracker, MapPermission, PhysPageNum, frame_alloc}; use crate::config::PAGE_SIZE; use crate::sync::UPIntrFreeCell;`
  - `os/src/mm/area.rs:L1-L3`：关键词命中
    代码片段：`use super::page_table::PTEFlags; use super::{ FrameTracker, PageTable, PhysAddr, PhysPageNum, StepByOne, VPNRange, VirtAddr, VirtPageNum,`
  - `os/src/mm/mod.rs:L25-L29`：fn init
    代码片段：`pub use user_space::{MemoryProtectError, MmapFaultAccess, MmapFaultResult}; pub fn init() { heap_allocator::init_heap(); frame_allocator::init_frame_allocator();`

### 相关符号

`fn init` at `os/src/mm/mod.rs:L25`、`enum ShmError` at `os/src/mm/shm.rs:L34`、`struct ShmCreateContext` at `os/src/mm/shm.rs:L45`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：struct LinuxIocb, struct LinuxIoEvent, struct AioContext, struct AioManager, fn new。（置信度：medium）
  - 静态识别到 512 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/syscall/mod.rs` | L1-L3 | 关键词命中 |
| `os/src/syscall/time.rs` | L1-L5 | 关键词命中 |
| `os/src/syscall/uapi.rs` | L2-L6 | 关键词命中 |
| `os/src/syscall/aio.rs` | L13-L17 | struct LinuxIocb |
| `os/src/syscall/aio.rs` | L30-L34 | struct LinuxIoEvent |
| `os/src/syscall/aio.rs` | L38-L42 | struct AioContext |

### 关键代码片段

  - `os/src/syscall/mod.rs:L1-L3`：关键词命中
    代码片段：`// Linux generic syscall numbers used by both contest RISC-V and LoongArch // ABIs. Keep this table aligned with the userspace libc headers, not with // local implementation order.`
  - `os/src/syscall/time.rs:L1-L5`：关键词命中
    代码片段：`use crate::perf; use crate::sync::UPIntrFreeCell; use crate::syscall::SyscallContext; use crate::task::{ CAP_SYS_TIME, ProcessCpuTimesSnapshot, block_current_task_no_schedule,`
  - `os/src/syscall/uapi.rs:L2-L6`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug, Default)] pub struct LinuxTimeSpec { pub(in crate::syscall) tv_sec: isize, pub(in crate::syscall) tv_nsec: isize, }`
  - `os/src/syscall/aio.rs:L13-L17`：struct LinuxIocb
    代码片段：`#[repr(C)] #[derive(Clone, Copy, Debug, Default)] pub(super) struct LinuxIocb { aio_data: u64, aio_key: u32,`

### 相关符号

`struct LinuxIocb` at `os/src/syscall/aio.rs:L13`、`struct LinuxIoEvent` at `os/src/syscall/aio.rs:L30`、`struct AioContext` at `os/src/syscall/aio.rs:L38`、`fn sys_io_setup` at `os/src/syscall/aio.rs:L114`、`fn sys_io_destroy` at `os/src/syscall/aio.rs:L135`、`fn sys_io_cancel` at `os/src/syscall/aio.rs:L140`、`fn sys_io_getevents` at `os/src/syscall/aio.rs:L153`、`fn sys_io_pgetevents` at `os/src/syscall/aio.rs:L189`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct FatBlockDevice, struct FatMount, fn new, fn size, fn bounded_len。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/fs/fat.rs` | L1-L5 | 关键词命中 |
| `os/src/fs/mod.rs` | L7-L11 | 关键词命中 |
| `os/src/fs/ext4.rs` | L3-L7 | 关键词命中 |
| `os/src/fs/fat.rs` | L23-L27 | struct FatBlockDevice |
| `os/src/fs/fat.rs` | L30-L34 | struct FatMount |
| `os/src/fs/fat.rs` | L42-L46 | fn new |

### 关键代码片段

  - `os/src/fs/fat.rs:L1-L5`：关键词命中
    代码片段：`use super::dirent::{DT_DIR, DT_REG, RawDirEntry, write_dir_entries}; use super::mount::BlockPartition; use super::vfs::{FileSystemBackend, FileSystemStat, FsError, FsNodeKind, F...`
  - `os/src/fs/mod.rs:L7-L11`：关键词命中
    代码片段：`mod eventfd; mod ext4; mod fat; mod inode; mod memfd;`
  - `os/src/fs/ext4.rs:L3-L7`：关键词命中
    代码片段：`LINUX_DIRENT64_HEADER_SIZE, }; use super::vfs::{FileSystemBackend, FileSystemStat, FsError, FsNodeKind, FsResult}; use super::{FS_STATX_ATTR_FLAGS, FileStat, FileTimestamp}; use...`
  - `os/src/fs/fat.rs:L23-L27`：struct FatBlockDevice
    代码片段：`#[derive(Clone)] struct FatBlockDevice { dev: Arc<VirtIOBlock>, start_block: u64,`

### 相关符号

`struct FatBlockDevice` at `os/src/fs/fat.rs:L23`、`struct FatMount` at `os/src/fs/fat.rs:L30`、`fn new` at `os/src/fs/fat.rs:L42`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct UPSafeCell, fn new, fn exclusive_access, struct UPSafeCellRaw, fn with_mut。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/sync/mod.rs` | L1-L3 | 关键词命中 |
| `os/src/sync/condvar.rs` | L3-L7 | 关键词命中 |
| `os/src/sync/sleep_mutex.rs` | L20-L24 | 关键词命中 |
| `os/src/sync/up.rs` | L12-L16 | struct UPSafeCell |
| `os/src/sync/up.rs` | L22-L26 | fn new |
| `os/src/sync/up.rs` | L28-L32 | fn exclusive_access |

### 关键代码片段

  - `os/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`mod condvar; mod sleep_mutex; mod up;`
  - `os/src/sync/condvar.rs:L3-L7`：关键词命中
    代码片段：`use alloc::{collections::VecDeque, sync::Arc}; pub struct Condvar { pub inner: UPIntrFreeCell<CondvarInner>, }`
  - `os/src/sync/sleep_mutex.rs:L20-L24`：关键词命中
    代码片段：`pub struct SleepMutexGuard<'a, T> { mutex: &'a SleepMutex<T>, }`
  - `os/src/sync/up.rs:L12-L16`：struct UPSafeCell
    代码片段：`/// In order to get mutable reference of inner data, call /// 'exclusive_access'. pub struct UPSafeCell<T> { /// inner data inner: RefCell<T>,`

### 相关符号

`struct UPSafeCell` at `os/src/sync/up.rs:L12`、`fn new` at `os/src/sync/up.rs:L22`、`fn exclusive_access` at `os/src/sync/up.rs:L28`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：struct PLIC, enum IntrTargetPriority, impl PLIC, fn priority_ptr, fn hart_id_with_priority。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/drivers/plic.rs` | L1-L4 | 关键词命中 |
| `os/src/arch/riscv64/timer.rs` | L16-L20 | 关键词命中 |
| `os/src/arch/loongarch64/irq.rs` | L33-L37 | 关键词命中 |
| `os/src/drivers/plic.rs` | L1-L4 | struct PLIC |
| `os/src/drivers/plic.rs` | L5-L9 | enum IntrTargetPriority |
| `os/src/drivers/plic.rs` | L10-L14 | impl PLIC |

### 关键代码片段

  - `os/src/drivers/plic.rs:L1-L4`：关键词命中
    代码片段：`#[allow(clippy::upper_case_acronyms)] pub struct PLIC { base_addr: usize, }`
  - `os/src/arch/riscv64/timer.rs:L16-L20`：关键词命中
    代码片段：`pub const TICKS_PER_SEC: usize = 100; // CONTEXT: Keep Linux-visible clock ticks at 100 Hz, but drive scheduler // timer interrupts at 1 kHz so 1 ms clock_nanosleep workloads ca...`
  - `os/src/arch/loongarch64/irq.rs:L33-L37`：关键词命中
    代码片段：`// matching the current single-hart contest runtime. The contest QEMU // already exposes EIOINTC; probing IOCSR MISC_FUNC is not required for // this virtual platform and may tr...`
  - `os/src/drivers/plic.rs:L1-L4`：struct PLIC
    代码片段：`#[allow(clippy::upper_case_acronyms)] pub struct PLIC { base_addr: usize, }`

### 相关符号

`struct PLIC` at `os/src/drivers/plic.rs:L1`、`enum IntrTargetPriority` at `os/src/drivers/plic.rs:L5`、`impl PLIC` at `os/src/drivers/plic.rs:L10`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct Stdout, fn write_str, fn print, struct PLIC, enum IntrTargetPriority。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `os/src/console.rs` | L1-L4 | 关键词命中 |
| `os/src/drivers/mod.rs` | L5-L9 | 关键词命中 |
| `os/src/drivers/block.rs` | L2-L6 | 关键词命中 |
| `os/src/console.rs` | L3-L7 | struct Stdout |
| `os/src/console.rs` | L6-L10 | fn write_str |
| `os/src/console.rs` | L12-L16 | fn print |

### 关键代码片段

  - `os/src/console.rs:L1-L4`：关键词命中
    代码片段：`use crate::drivers::chardev::CharDevice; use crate::drivers::chardev::UART; use core::fmt::{self, Write};`
  - `os/src/drivers/mod.rs:L5-L9`：关键词命中
    代码片段：`#[cfg(target_arch = "riscv64")] pub mod plic; pub mod virtio; pub use input::*;`
  - `os/src/drivers/block.rs:L2-L6`：关键词命中
    代码片段：`use crate::board::{BlockDeviceConfig, BlockDeviceImpl}; use crate::drivers::block_cache; use crate::drivers::virtio::{VirtioHal, VirtioTransport, mmio_transport}; use crate::syn...`
  - `os/src/console.rs:L3-L7`：struct Stdout
    代码片段：`use core::fmt::{self, Write}; struct Stdout; impl Write for Stdout {`

### 相关符号

`struct Stdout` at `os/src/console.rs:L3`、`fn write_str` at `os/src/console.rs:L6`、`fn print` at `os/src/console.rs:L12`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
