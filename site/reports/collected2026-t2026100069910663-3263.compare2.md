# 2026 T2026100069910663 比较报告

- 对比历史仓库：2021 kCore
- 生成时间：2026-07-02T09:00:23.519644+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2021 kCore（来源：赛事历史作品）：画像相似度 score=9.56；同属 ucore-variant 风格; 语言构成相似度 0.98; OS 维度重合度 1.00; 代码规模接近度 0.61

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2021 kCore 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/console/mod.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::{self, Write}; pub struct Console; impl Write for Console {`
  - `kernel/src/drivers/mod.rs:L2-L6`：关键词命中
    代码片段：`pub mod bus; pub mod selftest; pub mod virtio; pub fn init() {`
  - `os/src/console.rs:L20-L24`：关键词命中
    代码片段：`macro_rules! print { ($fmt: literal $(, $($arg: tt)+)?) => { $crate::console::print(format_args!($fmt $(, $($arg)+)?)); } }`
  - `os/src/drivers/block/mod.rs:L2-L6`：关键词命中
    代码片段：`#[cfg(feature = "board_qemu")] type BlockDeviceImpl = virtio_blk::VirtIOBlock; #[cfg(feature = "board_k210")]`
- 与 2021 kCore 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L9-L11`：关键词命中
    代码片段：`pub mod socket; pub mod userdb; pub mod vfs;`
  - `kernel/src/fs/pipe.rs:L4-L8`：关键词命中
    代码片段：`use crate::{ error::{EBADF, EPIPE}, fs::vfs::{DeviceId, FileTimes, FileType, Metadata, NodeId}, sync::spin::SpinLock, };`
  - `os/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`mod pipe; mod stdio; mod inode; use crate::mem::UserBuffer;`
  - `os/src/fs/inode.rs:L4-L8`：关键词命中
    代码片段：`use kcore_fs::{ KcoreFileSystem, Inode, }; use crate::drivers::BLOCK_DEVICE;`
- 与 2021 kCore 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/arch/trap_frame.rs:L1-L3`：关键词命中
    代码片段：`//! Architecture-independent trap frame access interface. //! //! Each architecture implements this trait for its 'TrapFrame' type`
  - `kernel/src/arch/riscv64/trap.S:L1-L3`：关键词命中
    代码片段：`.section .text.trap, "ax" .align 2 .globl __riscv64_trap_entry`
  - `os/src/trap/mod.rs:L3-L7`：关键词命中
    代码片段：`use riscv::register::{ mtvec::TrapMode, scause::{self, Exception, Interrupt, Trap}, sie, stval, stvec, };`
  - `os/src/sbi.rs:L38-L42`：关键词命中
    代码片段：`} pub fn set_timer(timer: usize) { sbi_call(SBI_SET_TIMER, timer, 0, 0); }`
- 与 2021 kCore 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/mm/mod.rs:L4-L8`：关键词命中
    代码片段：`pub mod heap; pub mod memory_set; pub mod page_table; pub mod user;`
  - `kernel/src/mm/heap.rs:L13-L17`：关键词命中
    代码片段：`struct HeapSpace([u8; KERNEL_HEAP_SIZE]); // SAFETY: LockedHeapSpace provides exclusive access to HEAP_SPACE through // the heap allocator's atomic operations. The UnsafeCell al...`
  - `os/src/mem/page.rs:L13-L17`：关键词命中
    代码片段：`/// } /// ''' /// and The address of the multi-level page table /// will be stored in the satp register, you can /// get the page table like this`
  - `os/src/mem/page_table.rs:L10-L14`：关键词命中
    代码片段：`use core::fmt::{self, Debug, Formatter}; pub struct PageTable { pub root: PhyPageNum, pages: Vec<PhyPageWrapper>,`
- 与 2021 kCore 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L2-L6`：关键词命中
    代码片段：`pub mod id; pub mod process; pub mod scheduler; pub mod wait; pub mod wait_queue;`
  - `kernel/src/task/process.rs:L9-L13`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug, Eq, PartialEq)] pub enum TaskAction { Yield, Exit(i32), Block,`
  - `os/src/task/mod.rs:L4-L8`：关键词命中
    代码片段：`mod processor; mod switch; mod task; use crate::loader::get_app_data_by_name;`
  - `os/src/task/task.rs:L60-L64`：关键词命中
    代码片段：`let kernel_stack = KernelStack::new(&pid_handle); let kernel_stack_top = kernel_stack.get_top(); // push a task context which goes to trap_return to the top of kernel stack let...`
- 与 2021 kCore 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/sync/spin.rs:L3-L7`：关键词命中
    代码片段：`hint::spin_loop, ops::{Deref, DerefMut}, sync::atomic::{AtomicBool, Ordering}, };`
  - `kernel/src/fs/pipe.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec}; use core::sync::atomic::{AtomicUsize, Ordering}; use crate::{`
  - `os/src/fs/pipe.rs:L3-L7`：关键词命中
    代码片段：`use super::File; use alloc::sync::{Arc, Weak}; use spin::Mutex; use crate::mem::{ UserBuffer,`
  - `os/src/fs/inode.rs:L11-L15`：关键词命中
    代码片段：`use bitflags::*; use alloc::vec::Vec; use spin::Mutex; use super::File; use crate::mem::UserBuffer;`
- 与 2021 kCore 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/syscall/shm.rs:L223-L227`：关键词命中
    代码片段：`user::UserBuffer, }, syscall::process::{install_current_user, UserTask}, };`
  - `kernel/src/syscall/fs/fd.rs:L2-L6`：关键词命中
    代码片段：`fs::vfs, mm::user::UserBuffer, syscall::{err, process, SyscallRet, EBADF, EFAULT, EINVAL, EOPNOTSUPP, EPERM, EROFS}, };`
  - `os/src/syscall/mod.rs:L18-L22`：关键词命中
    代码片段：`pub mod mem; pub fn syscall(syscall_id: usize, args: [usize; 3]) -> isize { match syscall_id { SYSCALL_READ => sys_read(args[0], args[1] as *const u8, args[2]),`
  - `os/src/sbi.rs:L15-L19`：关键词命中
    代码片段：`let mut ret; unsafe { llvm_asm!("ecall" : "={x10}" (ret) : "{x10}" (arg0), "{x11}" (arg1), "{x12}" (arg2), "{x17}" (which)`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2021 kCore 在“设备驱动”维度发现片段级代码相似度 0.88 （token=0.81, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：core, fmt, for, impl, self, struct, use, write。（置信度：high）
  证据：
  - `kernel/src/console/mod.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::{self, Write}; pub struct Console; impl Write for Console {`
  - `os/src/console.rs:L2-L6`：struct Stdout
    代码片段：`use core::fmt::{self, Write}; struct Stdout; impl Write for Stdout {`
- 与 2021 kCore 在“系统调用”维度发现片段级代码相似度 0.65 （token=0.55, structure=0.83）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：buf, fn, isize, len, match, pub, usize。（置信度：medium）
  证据：
  - `kernel/src/syscall/io.rs:L95-L99`：fn sys_syslog
    代码片段：`} pub fn sys_syslog(action: usize, buf: usize, len: usize) -> SyscallRet { match action { SYSLOG_ACTION_SIZE_BUFFER => KERNEL_LOG.len() as isize,`
  - `os/src/syscall/fs.rs:L21-L25`：fn sys_read
    代码片段：`} pub fn sys_read(fd: usize, buf: *const u8, len: usize) -> isize { match fd { FD_STDIN => {`
- 文件路径重合：与 2021 kCore 在“同步机制”维度出现同名文件源码路径 `kernel/src/fs/pipe.rs` / `os/src/fs/pipe.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/fs/pipe.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec}; use core::sync::atomic::{AtomicUsize, Ordering}; use crate::{`
  - `os/src/fs/pipe.rs:L3-L7`：关键词命中
    代码片段：`use super::File; use alloc::sync::{Arc, Weak}; use spin::Mutex; use crate::mem::{ UserBuffer,`

## 相似点

- 与 2021 kCore 同属 ucore-variant 风格。（置信度：medium）
- 与 2021 kCore 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/console/mod.rs:L1-L5`：关键词命中
    代码片段：`use core::fmt::{self, Write}; pub struct Console; impl Write for Console {`
  - `kernel/src/drivers/mod.rs:L2-L6`：关键词命中
    代码片段：`pub mod bus; pub mod selftest; pub mod virtio; pub fn init() {`
- 与 2021 kCore 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/fs/mod.rs:L9-L11`：关键词命中
    代码片段：`pub mod socket; pub mod userdb; pub mod vfs;`
  - `kernel/src/fs/pipe.rs:L4-L8`：关键词命中
    代码片段：`use crate::{ error::{EBADF, EPIPE}, fs::vfs::{DeviceId, FileTimes, FileType, Metadata, NodeId}, sync::spin::SpinLock, };`
- 与 2021 kCore 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/arch/trap_frame.rs:L1-L3`：关键词命中
    代码片段：`//! Architecture-independent trap frame access interface. //! //! Each architecture implements this trait for its 'TrapFrame' type`
  - `kernel/src/arch/riscv64/trap.S:L1-L3`：关键词命中
    代码片段：`.section .text.trap, "ax" .align 2 .globl __riscv64_trap_entry`
- 与 2021 kCore 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/mm/mod.rs:L4-L8`：关键词命中
    代码片段：`pub mod heap; pub mod memory_set; pub mod page_table; pub mod user;`
  - `kernel/src/mm/heap.rs:L13-L17`：关键词命中
    代码片段：`struct HeapSpace([u8; KERNEL_HEAP_SIZE]); // SAFETY: LockedHeapSpace provides exclusive access to HEAP_SPACE through // the heap allocator's atomic operations. The UnsafeCell al...`
- 与 2021 kCore 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/task/mod.rs:L2-L6`：关键词命中
    代码片段：`pub mod id; pub mod process; pub mod scheduler; pub mod wait; pub mod wait_queue;`
  - `kernel/src/task/process.rs:L9-L13`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug, Eq, PartialEq)] pub enum TaskAction { Yield, Exit(i32), Block,`
- 与 2021 kCore 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/sync/spin.rs:L3-L7`：关键词命中
    代码片段：`hint::spin_loop, ops::{Deref, DerefMut}, sync::atomic::{AtomicBool, Ordering}, };`
  - `kernel/src/fs/pipe.rs:L1-L4`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec}; use core::sync::atomic::{AtomicUsize, Ordering}; use crate::{`
- 与 2021 kCore 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/syscall/shm.rs:L223-L227`：关键词命中
    代码片段：`user::UserBuffer, }, syscall::process::{install_current_user, UserTask}, };`
  - `kernel/src/syscall/fs/fd.rs:L2-L6`：关键词命中
    代码片段：`fs::vfs, mm::user::UserBuffer, syscall::{err, process, SyscallRet, EBADF, EFAULT, EINVAL, EOPNOTSUPP, EPERM, EROFS}, };`
- 与 2021 kCore 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2021 kCore 的语言构成不同：待测作品为 {'json': 48, 'markdown': 13387, 'build': 3156, 'toml': 20, 'yaml': 141, 'rust': 233257, 'text': 35, 'asm': 284}，历史样本为 {'json': 87, 'build': 3692, 'markdown': 16369, 'toml': 60, 'rust': 452565, 'yaml': 92, 'c': 48, 'asm': 572, 'text': 1871}。（置信度：medium）

## 可能创新点

- 待测作品包含 296 个历史样本 2021 kCore 中未见的独特函数/类型定义（如 `syscall5`, `ll_sc_increment_with_pcalau_between_bounded`, `ll_sc_increment_recomputed_address_bounded`, `expand_shorthand`, `ll_sc_increment_internal_retry_bounded`, `source`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `tools/user-smoke.rs:L4115-L4115`：fn syscall5
    代码片段：`fn syscall5(id: usize, arg0: usize, arg1: usize, arg2: usize, arg3: usize, arg4: usize) -> usize {`
  - `tools/user-smoke-loongarch64.rs:L1291-L1291`：fn ll_sc_increment_with_pcalau_between_bounded
    代码片段：`unsafe fn ll_sc_increment_with_pcalau_between_bounded(word: *mut u32, max_tries: usize) -> bool {`

## 附录：核验摘要

- 关键结论数：18
- 含证据关键结论数：18（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
