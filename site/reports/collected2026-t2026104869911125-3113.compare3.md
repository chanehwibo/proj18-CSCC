# 2026 T2026104869911125 比较报告

- 对比历史仓库：2025 RuOK
- 生成时间：2026-07-02T09:02:38.846894+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 RuOK（来源：赛事历史作品）：画像相似度 score=9.60；同属 ucore-variant 风格; 语言构成相似度 0.88; OS 维度重合度 1.00; 代码规模接近度 0.84

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 RuOK 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/devs/uart.cc:L1-L3`：关键词命中
    代码片段：`#include "uart.hh" #include "printer.hh" #include "console.hh"`
  - `kernel/devs/uart.hh:L191-L194`：关键词命中
    代码片段：`} }; extern UartManager k_uart; // 全局的uart管理器 }`
  - `kernel/include/fs/file/device.hh:L5-L9`：class StreamDevice
    代码片段：`namespace hsai { class StreamDevice; } // namespace hsai`
  - `kernel/include/fs/file/device.hh:L11-L15`：class dentry
    代码片段：`namespace fs { class dentry; class device_file : public file {`
- 与 2025 RuOK 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs/bio.cc:L21-L25`：关键词命中
    代码片段：`#include "platform.hh" #include "fs/vfs/fs.hh" #include "fs/buf.hh" #include "devs/device_manager.hh"`
  - `kernel/fs/buf.hh:L1-L4`：关键词命中
    代码片段：`#pragma once #include "fs/vfs/fs.hh" #include "sleeplock.hh" struct buf {`
  - `kernel/fs/file.cc:L8-L12`：关键词命中
    代码片段：`#include "fs/file/file.hh" #include "fs/dentry.hh" #include "fs/inode.hh"`
  - `kernel/fs/path.cc:L11-L15`：关键词命中
    代码片段：`#include "fs/file/file.hh" #include "fs/file/normal.hh" #include "fs/dentry.hh" #include "fs/fs_defs.hh" #include "fs/fat/fat32fs.hh"`
- 与 2025 RuOK 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap/riscv/plic.cc:L3-L7`：关键词命中
    代码片段：`#include "mem/memlayout.hh" #include "platform.hh" #include "plic.hh" #include "printer.hh" #include "cpu.hh"`
  - `kernel/trap/riscv/plic.hh:L7-L11`：关键词命中
    代码片段：`void inithart(); int claim(); void complete(int irq); };`
  - `kernel/xn6_start_kernel.cc:L123-L127`：关键词命中
    代码片段：`// while ( 1 ); // timer init tmm::k_tm.init( "timer manager" ); log_info( "tm init" );`
  - `kernel/include/mm/memlayout.hh:L21-L25`：关键词命中
    代码片段：`vm_user_start = 0x0, vm_user_end = vm_user_start + ( hsai::page_size << vm_page_cnt_shift ),		// 64 GiB vm_trap_frame = vm_user_end - hsai::page_size,								// 用户空间最后一个页面是 trap...`
- 与 2025 RuOK 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/vm_area.hh:L138-L142`：关键词命中
    代码片段：`uint64 page_index_for_va(uint64 va) const { return (PGROUNDDOWN(va) - PGROUNDDOWN(addr)) / PGSIZE; }`
  - `kernel/proc/vm_object.cc:L150-L154`：关键词命中
    代码片段：`{ memmove(page, src, copy_bytes); if (zero_fill_tail && copy_bytes < PGSIZE) { memset(reinterpret_cast<char *>(page) + copy_bytes, 0, PGSIZE - copy_bytes);`
  - `kernel/mm/page_table.cc:L7-L11`：关键词命中
    代码片段：`// #include "mm/page_table.hh" #include <hsai_global.hh>`
  - `kernel/include/mm/page.hh:L46-L50`：关键词命中
    代码片段：`// 		dir3_vpn_mask = pt_bits_mask << dir3_vpn_shift, // 		pte_width = 0x0UL // 	};`
- 与 2025 RuOK 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc/pipe.cc:L16-L20`：关键词命中
    代码片段：`#include "printer.hh" #include "signal.hh" #include "scheduler.hh" // #include "fs/vfs/fs_defs.hh" #include "sys/syscall_defs.hh"`
  - `kernel/proc/pipe.hh:L22-L26`：关键词命中
    代码片段：`class PageTable; } namespace proc { class Pcb;`
  - `kernel/pm/scheduler.cc:L1-L4`：关键词命中
    代码片段：`#include "hal/cpu.hh" #include "pm/scheduler.hh" #include "pm/process_manager.hh" #include "tm/timer_manager.hh"`
  - `kernel/include/asched.h:L12-L16`：关键词命中
    代码片段：`#define CLONE_VFORK	0x00004000	/* set if the parent wants the child to wake it up on mm_release */ #define CLONE_PARENT	0x00008000	/* set if we want to have the same parent as t...`
- 与 2025 RuOK 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/libs/semaphore.cc:L1-L4`：关键词命中
    代码片段：`#include "semaphore.hh" #include "proc_manager.hh" #include "scheduler.hh"`
  - `kernel/libs/semaphore.hh:L1-L5`：关键词命中
    代码片段：`#pragma once #include "types.hh" #include "spinlock.hh" #include "param.h" #include "platform.hh"`
  - `kernel/include/pm/sleep_lock.hh:L11-L15`：关键词命中
    代码片段：`#include "types.hh" #include <smp/spin_lock.hh> namespace pm`
  - `kernel/include/fs/fs.hh:L4-L8`：关键词命中
    代码片段：`#include "fs/file/file_defs.hh" #include <smp/spin_lock.hh> #include <EASTL/string.h>`
- 与 2025 RuOK 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/sys/sysproc.cc:L7-L11`：关键词命中
    代码片段：`#include "proc/proc_manager.hh" namespace syscall { uint64 SyscallHandler::sys_getppid()`
  - `kernel/sys/syscall_abi.hh:L4-L8`：关键词命中
    代码片段：`#include "fs/vfs/file/socket_defs.hh" namespace syscall::abi { // 这里集中保存“用户态可见的 Linux ABI 布局”和常量。`
  - `kernel/syscall/syscall_handler.cc:L7-L11`：关键词命中
    代码片段：`// #include "syscall/syscall_handler.hh" // include syscall number`
  - `kernel/syscall/syscall_interface.cc:L8-L12`：关键词命中
    代码片段：`#include <syscall_interface.hh> #include <syscall/syscall_handler.hh> namespace hsai`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 RuOK 在“同步机制”维度发现片段级代码相似度 0.84 （token=0.75, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：class, private, spinlock。（置信度：high）
  证据：
  - `kernel/devs/spinlock.hh:L10-L14`：class SpinLock
    代码片段：`class Cpu; class SpinLock { private:`
  - `hsai/include/smp/spin_lock.hh:L17-L21`：class SpinLock
    代码片段：`class VirtualCpu; class SpinLock { private:`
- 与 2025 RuOK 在“系统调用”维度发现片段级代码相似度 0.72 （token=0.67, structure=0.83）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：class, constexpr, max_arg_num, syscallhandler, uint。（置信度：medium）
  证据：
  - `kernel/sys/syscall_handler.hh:L13-L17`：class SyscallHandler
    代码片段：`constexpr uint max_arg_num = 256; class SyscallHandler { // 使用了这个类的成员函数指针`
  - `kernel/include/syscall/syscall_handler.hh:L29-L33`：class SyscallHandler
    代码片段：`constexpr uint max_arg_num = 32; class SyscallHandler { private:`
- 文件路径重合：与 2025 RuOK 在“内存管理”维度出现同名文件源码路径 `kernel/mem/loongarch/page.hh` / `kernel/include/mm/page.hh`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/mem/loongarch/page.hh:L51-L55`：关键词命中
    代码片段：`dir3_vpn_mask = pt_bits_mask << dir3_vpn_shift, pte_width = 0x0UL };`
  - `kernel/include/mm/page.hh:L46-L50`：关键词命中
    代码片段：`// 		dir3_vpn_mask = pt_bits_mask << dir3_vpn_shift, // 		pte_width = 0x0UL // 	};`
- 文件路径重合：与 2025 RuOK 在“系统调用”维度出现同名文件源码路径 `kernel/sys/syscall_defs.hh` / `kernel/include/syscall/syscall_defs.hh`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/sys/syscall_defs.hh:L1-L4`：关键词命中
    代码片段：`#pragma once namespace syscall {`
  - `kernel/include/syscall/syscall_defs.hh:L11-L15`：关键词命中
    代码片段：`#include "types.hh" namespace syscall {`
- 文件路径重合：与 2025 RuOK 在“系统调用”维度出现同名文件源码路径 `kernel/sys/syscall_handler.cc` / `kernel/syscall/syscall_handler.cc`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/sys/syscall_handler.cc:L3862-L3866`：macro BIND_SYSCALL
    代码片段：`SyscallHandler k_syscall_handler; #define BIND_SYSCALL(sysname)                                       \ _syscall_funcs[SYS_##sysname] = &SyscallHandler::sys_##sysname; \ _syscal...`
  - `kernel/syscall/syscall_handler.cc:L7-L11`：关键词命中
    代码片段：`// #include "syscall/syscall_handler.hh" // include syscall number`

## 相似点

- 与 2025 RuOK 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 RuOK 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/devs/uart.cc:L1-L3`：关键词命中
    代码片段：`#include "uart.hh" #include "printer.hh" #include "console.hh"`
  - `kernel/devs/uart.hh:L191-L194`：关键词命中
    代码片段：`} }; extern UartManager k_uart; // 全局的uart管理器 }`
- 与 2025 RuOK 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs/bio.cc:L21-L25`：关键词命中
    代码片段：`#include "platform.hh" #include "fs/vfs/fs.hh" #include "fs/buf.hh" #include "devs/device_manager.hh"`
  - `kernel/fs/buf.hh:L1-L4`：关键词命中
    代码片段：`#pragma once #include "fs/vfs/fs.hh" #include "sleeplock.hh" struct buf {`
- 与 2025 RuOK 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap/riscv/plic.cc:L3-L7`：关键词命中
    代码片段：`#include "mem/memlayout.hh" #include "platform.hh" #include "plic.hh" #include "printer.hh" #include "cpu.hh"`
  - `kernel/trap/riscv/plic.hh:L7-L11`：关键词命中
    代码片段：`void inithart(); int claim(); void complete(int irq); };`
- 与 2025 RuOK 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/vm_area.hh:L138-L142`：关键词命中
    代码片段：`uint64 page_index_for_va(uint64 va) const { return (PGROUNDDOWN(va) - PGROUNDDOWN(addr)) / PGSIZE; }`
  - `kernel/proc/vm_object.cc:L150-L154`：关键词命中
    代码片段：`{ memmove(page, src, copy_bytes); if (zero_fill_tail && copy_bytes < PGSIZE) { memset(reinterpret_cast<char *>(page) + copy_bytes, 0, PGSIZE - copy_bytes);`
- 与 2025 RuOK 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc/pipe.cc:L16-L20`：关键词命中
    代码片段：`#include "printer.hh" #include "signal.hh" #include "scheduler.hh" // #include "fs/vfs/fs_defs.hh" #include "sys/syscall_defs.hh"`
  - `kernel/proc/pipe.hh:L22-L26`：关键词命中
    代码片段：`class PageTable; } namespace proc { class Pcb;`
- 与 2025 RuOK 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/libs/semaphore.cc:L1-L4`：关键词命中
    代码片段：`#include "semaphore.hh" #include "proc_manager.hh" #include "scheduler.hh"`
  - `kernel/libs/semaphore.hh:L1-L5`：关键词命中
    代码片段：`#pragma once #include "types.hh" #include "spinlock.hh" #include "param.h" #include "platform.hh"`
- 与 2025 RuOK 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/sys/sysproc.cc:L7-L11`：关键词命中
    代码片段：`#include "proc/proc_manager.hh" namespace syscall { uint64 SyscallHandler::sys_getppid()`
  - `kernel/sys/syscall_abi.hh:L4-L8`：关键词命中
    代码片段：`#include "fs/vfs/file/socket_defs.hh" namespace syscall::abi { // 这里集中保存“用户态可见的 Linux ABI 布局”和常量。`
- 与 2025 RuOK 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 RuOK 的语言构成不同：待测作品为 {'markdown': 17111, 'build': 509, 'asm': 839, 'cpp': 140790, 'c': 115996, 'text': 22702}，历史样本为 {'json': 20, 'text': 122, 'build': 1789, 'markdown': 8397, 'make': 117, 'cpp': 110793, 'c': 122657, 'asm': 861, 'yaml': 157, 'toml': 14, 'rust': 1926}。（置信度：medium）

## 可能创新点

- 待测作品在“设备驱动”维度的实现深度显著高于历史样本 2025 RuOK（6 条证据 vs 3 条），可能存在更完整或更精细的实现。（置信度：low）
  证据：
  - `kernel/devs/uart.cc:L1-L3`：关键词命中
    代码片段：`#include "uart.hh" #include "printer.hh" #include "console.hh"`
  - `kernel/devs/uart.hh:L191-L194`：关键词命中
    代码片段：`} }; extern UartManager k_uart; // 全局的uart管理器 }`
- 待测作品包含 141 个历史样本 2025 RuOK 中未见的独特函数/类型定义（如 `ext4_bg_set_inode_table_first_block`, `ext4_balloc_verify_bitmap_csum`, `ext4_bmap_is_bit_clr`, `ext4_bmap_bit_clr`, `ext4_bg_set_free_blocks_count`, `ext4_bg_set_free_inodes_count`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/fs/lwext4/ext4_block_group.hh:L91-L91`：fn ext4_bg_set_inode_table_first_block
    代码片段：`static inline void ext4_bg_set_inode_table_first_block(struct ext4_bgroup *bg, struct ext4_sblock *s, uint64_t blk) {`
  - `kernel/fs/lwext4/ext4_balloc.cc:L113-L113`：fn ext4_balloc_verify_bitmap_csum
    代码片段：`static bool ext4_balloc_verify_bitmap_csum(struct ext4_sblock *sb, struct ext4_bgroup *bg, void *bitmap __unused) {`

## 附录：核验摘要

- 关键结论数：21
- 含证据关键结论数：21（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
