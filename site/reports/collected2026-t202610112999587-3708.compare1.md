# 2026 T202610112999587 比较报告

- 对比历史仓库：2025 模仿游戏
- 生成时间：2026-07-01T20:56:39.821816+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2025 模仿游戏（来源：赛事历史作品）：画像相似度 score=9.48；同属 ucore-variant 风格; 语言构成相似度 0.83; OS 维度重合度 1.00; 代码规模接近度 0.81

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2025 模仿游戏 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `general/src/dev/drivers/mod.rs:L26-L30`：关键词命中
    代码片段：`mod cfi_flash; mod virtio_block_common; mod virtio_blk;`
  - `general/src/dev/drivers/random.rs:L106-L110`：关键词命中
    代码片段：`spins += 1; if spins > RANDOM_LOCK_SPIN_YIELD_LIMIT { // 与 uart 行为一致：长时间争用时让出调度。 sched_yield_best_effort(); spins = 0;`
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2025 模仿游戏 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `libs/vfs/src/sync.rs:L1-L3`：关键词命中
    代码片段：`//! VFS 层内部使用的自旋锁原语。 //! //! 在 'no_std' 环境下，标准库的 'std::sync::Mutex' 不可用。这里提供一个基于`
  - `kernel/src/syscalls/fs.rs:L10-L14`：关键词命中
    代码片段：`use general::mm::{VmSpace, copy_cstr_from_user, copy_from_user, copy_to_user}; use general::syscall::SyscallContext; use general::vfs::{current_fdtable, current_vfs_context, nam...`
  - `vendor/fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `arceos/modules/axfs/src/fs/fatfs.rs:L34-L38`：关键词命中
    代码片段：`fatfs::format_volume(&mut disk, opts).expect("failed to format volume"); let inner = fatfs::FileSystem::new(disk, fatfs::FsOptions::new()) .expect("failed to initialize FAT file...`
- 与 2025 模仿游戏 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `general/src/trap.rs:L1-L3`：关键词命中
    代码片段：`/// Trap 类型：区分异常/中断/系统调用等 #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub enum TrapType {`
  - `general/src/dev/drivers/plic.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V PLIC (Platform-Level Interrupt Controller) 平台驱动。 //! //! 本模块匹配 DTB 中的 'sifive,plic-1.0.0' / 'riscv,plic0' 中断控制器节点，`
  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use core::sync::atomic::{AtomicU64, Ordering};`
  - `arceos/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
- 与 2025 模仿游戏 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `general/src/mm/ops.rs:L165-L169`：关键词命中
    代码片段：`/// fork 时把 src 的 ['range'] 区间已映射页拷到 dst。保底"全深拷"语义； /// 后续接 COW 时本字段签名不变，只是 arch 实现里多个引用计数路径。 /// /// # Safety`
  - `general/src/mm/fault.rs:L34-L38`：关键词命中
    代码片段：`/// 权限：执行不允许。 PermExec, /// 权限等级异常：硬件未提供读/写/取指细分，按 VMA 权限重新校正 PTE。 Privilege, }`
  - `core/src/mm.rs:L26-L30`：关键词命中
    代码片段：`pub fn copy_from_kernel(aspace: &mut AddrSpace) -> AxResult { if !cfg!(target_arch = "aarch64") && !cfg!(target_arch = "loongarch64") { // ARMv8 (aarch64) and LoongArch64 use se...`
  - `arceos/modules/axmm/src/backend/alloc.rs:L2-L6`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PhysAddr, VirtAddr};`
- 与 2025 模仿游戏 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/src/sched.rs:L27-L31`：关键词命中
    代码片段：`use sched::signal::{SigAction, SigActionFlags, SigHandler, SigInfo, SigProcMaskHow, SigSet}; use sched::sync::Spinlock; use sched::task::{TaskExtCloneHook, TaskExtExitHook, Task...`
  - `libs/sched/src/task.rs:L1-L5`：关键词命中
    代码片段：`//! 单个任务的核心抽象。 //! //! ['Task'] 是调度器管理的最小实体，对应 Linux 的 'task_struct'。本设计**不分配 //! PID**：任务身份即 'Arc<Task>'，父子关系通过 'Weak' / 'Arc' 直接互引。 //!`
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. use core::{`
  - `vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`
- 与 2025 模仿游戏 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `libs/vfs/src/sync.rs:L1-L5`：关键词命中
    代码片段：`//! VFS 层内部使用的自旋锁原语。 //! //! 在 'no_std' 环境下，标准库的 'std::sync::Mutex' 不可用。这里提供一个基于 //! 'AtomicBool' 的简单自旋锁（spinlock），足以保护 VFS 层的短临界区（如 inode //! 元数据的读写、dentry 缓存的更新）。`
  - `libs/sched/src/sync.rs:L15-L19`：关键词命中
    代码片段：`use core::marker::PhantomData; use core::ops::{Deref, DerefMut}; use core::sync::atomic::{AtomicBool, Ordering}; pub struct SpinlockGuard<'a, T> {`
  - `core/src/task.rs:L4-L8`：关键词命中
    代码片段：`alloc::Layout, cell::RefCell, sync::atomic::{AtomicUsize, Ordering}, time::Duration, };`
  - `arceos/modules/axfs/src/root.rs:L7-L11`：关键词命中
    代码片段：`use axfs_vfs::{VfsNodeAttr, VfsNodeOps, VfsNodePerm, VfsNodeRef, VfsNodeType, VfsOps, VfsResult}; use axns::{ResArc, def_resource}; use axsync::Mutex; use lazyinit::LazyInit; us...`
- 与 2025 模仿游戏 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `general/src/syscall.rs:L4-L8`：关键词命中
    代码片段：`//! //! 1. **arch 注入契约 ['SyscallFrameOps']**：从 trap frame 取号、读参、写返回、 //!    推进 PC 的 4 个回调。这是 arch 唯一对 syscall 子系统的耦合点。 //! 2. **表驱动分发**：['SYSCALL_TABLE'] 是 '[Option<SyscallFn>;...`
  - `kernel/src/syscalls/fs.rs:L1-L3`：关键词命中
    代码片段：`//! 文件系统相关 syscall。 use alloc::string::String;`
  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_api::*;`
  - `vendor/syscalls/src/syscall/arm.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %r5 // // %r7 is used for the syscall number. // // %r0 is reused for the syscall return value.`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2025 模仿游戏 在“同步机制”维度发现片段级代码相似度 0.68 （token=0.50, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：atomic, core, ordering, sync, use。（置信度：medium）
  证据：
  - `kernel/src/vdso.rs:L2-L6`：关键词命中
    代码片段：`use core::mem::size_of; use core::sync::atomic::{AtomicI64, AtomicU32, AtomicUsize, Ordering, fence}; use errno::Errno;`
  - `arceos/modules/axhal/src/irq.rs:L1-L5`：关键词命中
    代码片段：`//! Interrupt management. use core::sync::atomic::{AtomicU64, Ordering}; use handler_table::HandlerTable;`
- 与 2025 模仿游戏 在“同步机制”维度发现片段级代码相似度 0.59 （token=0.55, structure=0.67）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：fn, pub, self, t。（置信度：medium）
  证据：
  - `libs/vfs/src/sync.rs:L68-L72`：fn lock
    代码片段：`/// pause/yield 提示， /// 降低总线争用，提高超线程系统的整体吞吐量。 pub fn lock(&self) -> SpinlockGuard<'_, T> { // 使用 Acquire 语义获取锁：确保临界区内对受保护数据的读写操作， // 不会被编译器或 CPU 重排到锁获取之前。`
  - `vendor/once_cell_polyfill/src/sync/mod.rs:L7-L11`：fn get
    代码片段：`} pub fn get(&self) -> Option<&T> { self.0.get() }`
- 文件路径重合：与 2025 模仿游戏 在“文件系统”维度出现同名文件源码路径 `kernel/src/syscalls/fs.rs` / `vendor/fatfs/src/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `kernel/src/syscalls/fs.rs:L10-L14`：关键词命中
    代码片段：`use general::mm::{VmSpace, copy_cstr_from_user, copy_from_user, copy_to_user}; use general::syscall::SyscallContext; use general::vfs::{current_fdtable, current_vfs_context, nam...`
  - `vendor/fatfs/src/fs.rs:L20-L24`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
- 文件路径重合：与 2025 模仿游戏 在“中断与异常”维度出现同名文件源码路径 `general/src/trap.rs` / `arceos/modules/axhal/src/trap.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `general/src/trap.rs:L1-L3`：关键词命中
    代码片段：`/// Trap 类型：区分异常/中断/系统调用等 #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub enum TrapType {`
  - `arceos/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
- 文件路径重合：与 2025 模仿游戏 在“调度与任务管理”维度出现同名文件源码路径 `libs/sched/src/task.rs` / `core/src/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `libs/sched/src/task.rs:L1-L5`：关键词命中
    代码片段：`//! 单个任务的核心抽象。 //! //! ['Task'] 是调度器管理的最小实体，对应 Linux 的 'task_struct'。本设计**不分配 //! PID**：任务身份即 'Arc<Task>'，父子关系通过 'Weak' / 'Arc' 直接互引。 //!`
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. use core::{`
- 文件路径重合：与 2025 模仿游戏 在“调度与任务管理”维度出现同名文件源码路径 `libs/sched/src/task.rs` / `vendor/x86/src/task.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `libs/sched/src/task.rs:L1-L5`：关键词命中
    代码片段：`//! 单个任务的核心抽象。 //! //! ['Task'] 是调度器管理的最小实体，对应 Linux 的 'task_struct'。本设计**不分配 //! PID**：任务身份即 'Arc<Task>'，父子关系通过 'Weak' / 'Arc' 直接互引。 //!`
  - `vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`
- 文件路径重合：与 2025 模仿游戏 在“系统调用”维度出现同名文件源码路径 `general/src/syscall.rs` / `src/syscall.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `general/src/syscall.rs:L4-L8`：关键词命中
    代码片段：`//! //! 1. **arch 注入契约 ['SyscallFrameOps']**：从 trap frame 取号、读参、写返回、 //!    推进 PC 的 4 个回调。这是 arch 唯一对 syscall 子系统的耦合点。 //! 2. **表驱动分发**：['SYSCALL_TABLE'] 是 '[Option<SyscallFn>;...`
  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_api::*;`
- 文件路径重合：与 2025 模仿游戏 在“系统调用”维度出现同名文件源码路径 `general/src/trap.rs` / `arceos/modules/axhal/src/trap.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `general/src/trap.rs:L1-L5`：enum TrapType
    代码片段：`/// Trap 类型：区分异常/中断/系统调用等 #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub enum TrapType { Exception(Exception), Interrupt(Interrupt),`
  - `arceos/modules/axhal/src/trap.rs:L43-L47`：fn post_trap_callback
    代码片段：`#[unsafe(no_mangle)] pub(crate) fn post_trap_callback(tf: &mut TrapFrame, from_user: bool) { for cb in crate::trap::POST_TRAP.iter() { cb(tf, from_user);`

## 相似点

- 与 2025 模仿游戏 同属 ucore-variant 风格。（置信度：medium）
- 与 2025 模仿游戏 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `general/src/dev/drivers/mod.rs:L26-L30`：关键词命中
    代码片段：`mod cfi_flash; mod virtio_block_common; mod virtio_blk;`
  - `general/src/dev/drivers/random.rs:L106-L110`：关键词命中
    代码片段：`spins += 1; if spins > RANDOM_LOCK_SPIN_YIELD_LIMIT { // 与 uart 行为一致：长时间争用时让出调度。 sched_yield_best_effort(); spins = 0;`
- 与 2025 模仿游戏 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `libs/vfs/src/sync.rs:L1-L3`：关键词命中
    代码片段：`//! VFS 层内部使用的自旋锁原语。 //! //! 在 'no_std' 环境下，标准库的 'std::sync::Mutex' 不可用。这里提供一个基于`
  - `kernel/src/syscalls/fs.rs:L10-L14`：关键词命中
    代码片段：`use general::mm::{VmSpace, copy_cstr_from_user, copy_from_user, copy_to_user}; use general::syscall::SyscallContext; use general::vfs::{current_fdtable, current_vfs_context, nam...`
- 与 2025 模仿游戏 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `general/src/trap.rs:L1-L3`：关键词命中
    代码片段：`/// Trap 类型：区分异常/中断/系统调用等 #[derive(Debug, Clone, Copy, PartialEq, Eq)] pub enum TrapType {`
  - `general/src/dev/drivers/plic.rs:L1-L3`：关键词命中
    代码片段：`//! RISC-V PLIC (Platform-Level Interrupt Controller) 平台驱动。 //! //! 本模块匹配 DTB 中的 'sifive,plic-1.0.0' / 'riscv,plic0' 中断控制器节点，`
- 与 2025 模仿游戏 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `general/src/mm/ops.rs:L165-L169`：关键词命中
    代码片段：`/// fork 时把 src 的 ['range'] 区间已映射页拷到 dst。保底"全深拷"语义； /// 后续接 COW 时本字段签名不变，只是 arch 实现里多个引用计数路径。 /// /// # Safety`
  - `general/src/mm/fault.rs:L34-L38`：关键词命中
    代码片段：`/// 权限：执行不允许。 PermExec, /// 权限等级异常：硬件未提供读/写/取指细分，按 VMA 权限重新校正 PTE。 Privilege, }`
- 与 2025 模仿游戏 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/src/sched.rs:L27-L31`：关键词命中
    代码片段：`use sched::signal::{SigAction, SigActionFlags, SigHandler, SigInfo, SigProcMaskHow, SigSet}; use sched::sync::Spinlock; use sched::task::{TaskExtCloneHook, TaskExtExitHook, Task...`
  - `libs/sched/src/task.rs:L1-L5`：关键词命中
    代码片段：`//! 单个任务的核心抽象。 //! //! ['Task'] 是调度器管理的最小实体，对应 Linux 的 'task_struct'。本设计**不分配 //! PID**：任务身份即 'Arc<Task>'，父子关系通过 'Weak' / 'Arc' 直接互引。 //!`
- 与 2025 模仿游戏 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `libs/vfs/src/sync.rs:L1-L5`：关键词命中
    代码片段：`//! VFS 层内部使用的自旋锁原语。 //! //! 在 'no_std' 环境下，标准库的 'std::sync::Mutex' 不可用。这里提供一个基于 //! 'AtomicBool' 的简单自旋锁（spinlock），足以保护 VFS 层的短临界区（如 inode //! 元数据的读写、dentry 缓存的更新）。`
  - `libs/sched/src/sync.rs:L15-L19`：关键词命中
    代码片段：`use core::marker::PhantomData; use core::ops::{Deref, DerefMut}; use core::sync::atomic::{AtomicBool, Ordering}; pub struct SpinlockGuard<'a, T> {`
- 与 2025 模仿游戏 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `general/src/syscall.rs:L4-L8`：关键词命中
    代码片段：`//! //! 1. **arch 注入契约 ['SyscallFrameOps']**：从 trap frame 取号、读参、写返回、 //!    推进 PC 的 4 个回调。这是 arch 唯一对 syscall 子系统的耦合点。 //! 2. **表驱动分发**：['SYSCALL_TABLE'] 是 '[Option<SyscallFn>;...`
  - `kernel/src/syscalls/fs.rs:L1-L3`：关键词命中
    代码片段：`//! 文件系统相关 syscall。 use alloc::string::String;`
- 与 2025 模仿游戏 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2025 模仿游戏 的语言构成不同：待测作品为 {'json': 25933, 'build': 18115, 'markdown': 32392, 'toml': 11415, 'text': 29042, 'rust': 2286890, 'c': 1833, 'asm': 68}，历史样本为 {'json': 282075, 'build': 18424, 'markdown': 32818, 'toml': 943, 'rust': 1514694, 'text': 6083, 'c': 39849, 'yaml': 36, 'make': 1299, 'asm': 983}。（置信度：medium）

## 可能创新点

- 未确认。

## 附录：核验摘要

- 关键结论数：22
- 含证据关键结论数：22（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
