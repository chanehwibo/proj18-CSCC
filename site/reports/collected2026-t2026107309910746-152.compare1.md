# 2026 T2026107309910746 比较报告

- 对比历史仓库：2024 比赛时在干什么有没有空帮我修个bug
- 生成时间：2026-07-02T05:03:24.323158+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 比赛时在干什么有没有空帮我修个bug（来源：赛事历史作品）：画像相似度 score=9.72；同属 ucore-variant 风格; 语言构成相似度 0.90; OS 维度重合度 1.00; 代码规模接近度 0.92

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 比赛时在干什么有没有空帮我修个bug 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs.h:L7-L11`：关键词命中
    代码片段：`// Disk layout: // [ boot block | super block | log | inode blocks | //                                          free bit map | data blocks] //`
  - `kernel/fs.c:L174-L178`：关键词命中
    代码片段：`{ int reult, ino; struct ext4_inode inode; char *file_path = NULL; // TODO: implement`
  - `kernel/file.c:L127-L131`：关键词命中
    代码片段：`kst.size = f->file->fsize; kst.st_ino = ref.index; kst.st_mode = ref.inode->mode; kst.st_nlink = ref.inode->size_lo; kst.st_uid = 0;`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L32-L36`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
  - `kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/vm.c:L13-L17`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/kalloc.c:L37-L41`：关键词命中
    代码片段：`static uint64 super_start; static uint64 super_end; static uint refcnt[(PHYSTOP - SUPERPGNUM * SUPERPGSIZE) / PGSIZE]; static uint super_refcnt[SUPERPGNUM];`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`
  - `kernel/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/kalloc.h" #include "include/string.h" #include "include/printf.h"`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "riscv.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "fcntl.h"`
  - `kernel/proc.h:L21-L25`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `kernel/proc.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/proc.h" #include "include/intr.h" #include "include/kalloc.h"`
  - `kernel/include/proc.h:L59-L63`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/fs.c:L15-L19`：关键词命中
    代码片段：`#include "param.h" #include "stat.h" #include "spinlock.h" #include "proc.h" #include "sleeplock.h"`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "riscv.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "fs.h"`
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`#include "include/types.h" #include "include/riscv.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/buf.h"`
  - `kernel/bio.c:L18-L22`：关键词命中
    代码片段：`#include "include/types.h" #include "include/param.h" #include "include/spinlock.h" #include "include/sleeplock.h" #include "include/riscv.h"`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `kernel/trap.c:L59-L63`：关键词命中
    代码片段：`kexit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "fs.h"`
  - `kernel/syscall.c:L6-L10`：关键词命中
    代码片段：`#include "include/spinlock.h" #include "include/proc.h" #include "include/syscall.h" #include "include/sysinfo.h" #include "include/kalloc.h"`
  - `kernel/include/syscall.h:L10-L14`：关键词命中
    代码片段：`int argaddr(int n, uint64 *ip); int argstr(int n, char *buf, int max); void syscall(void); #endif`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 比赛时在干什么有没有空帮我修个bug 在“调度与任务管理”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：context, cpu, int, noff, proc, struct。（置信度：high）
  证据：
  - `kernel/proc.h:L21-L25`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
  - `kernel/include/proc.h:L59-L63`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“调度与任务管理”维度发现片段级代码相似度 1.00 （token=1.00, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：context, ra, sp, struct, uint64。（置信度：high）
  证据：
  - `kernel/proc.h:L1-L4`：struct context
    代码片段：`// Saved registers for kernel context switches. struct context { uint64 ra; uint64 sp;`
  - `kernel/include/proc.h:L38-L42`：struct context
    代码片段：`// Saved registers for kernel context switches. struct context { uint64 ra; uint64 sp;`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“内存管理”维度发现片段级代码相似度 0.63 （token=0.58, structure=0.71）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：next, run, struct。（置信度：medium）
  证据：
  - `kernel/kalloc.c:L15-L19`：struct run
    代码片段：`// defined by kernel.ld. struct run { struct run *next; };`
  - `kernel/kalloc.c:L17-L21`：struct run
    代码片段：`extern char kernel_end[]; // first address after kernel. struct run { struct run *next; };`
- 宏名重合：与 2024 比赛时在干什么有没有空帮我修个bug 在“文件系统”维度发现 4 个同名定义：NELEM, CONSOLE, CLONE_FS, CLONE_FILES。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/defs.h:L257-L257`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x)/sizeof((x)[0]))`
  - `kernel/include/defs.h:L242-L242`：macro NELEM
    代码片段：`#define NELEM(x) (sizeof(x)/sizeof((x)[0]))`
  - `kernel/file.h:L82-L82`：macro CONSOLE
    代码片段：`#define CONSOLE 1`
  - `kernel/include/file.h:L60-L60`：macro CONSOLE
    代码片段：`#define CONSOLE 1`
- 结构体/类型重合：与 2024 比赛时在干什么有没有空帮我修个bug 在“文件系统”维度发现 3 个同名定义：file, devsw, dirent。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：high）
  证据：
  - `kernel/file.h:L21-L21`：struct file
    代码片段：`struct file {`
  - `kernel/include/file.h:L4-L4`：struct file
    代码片段：`struct file {`
  - `kernel/file.h:L75-L75`：struct devsw
    代码片段：`struct devsw {`
  - `kernel/include/file.h:L51-L51`：struct devsw
    代码片段：`struct devsw {`
- 宏名重合：与 2024 比赛时在干什么有没有空帮我修个bug 在“设备驱动”维度发现 2 个同名定义：BACKSPACE, CONSOLE。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/console.c:L22-L22`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100`
  - `kernel/console.c:L24-L24`：macro BACKSPACE
    代码片段：`#define BACKSPACE 0x100`
  - `kernel/file.h:L82-L82`：macro CONSOLE
    代码片段：`#define CONSOLE 1`
  - `xv6-user/init.c:L7-L7`：macro CONSOLE
    代码片段：`#define CONSOLE 1`
- 宏名重合：与 2024 比赛时在干什么有没有空帮我修个bug 在“内存管理”维度发现 2 个同名定义：AT_PAGESZ, CLONE_VM。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/exec.h:L8-L8`：macro AT_PAGESZ
    代码片段：`#define AT_PAGESZ 6`
  - `kernel/include/elf.h:L53-L53`：macro AT_PAGESZ
    代码片段：`#define AT_PAGESZ 6  /* System page size */`
  - `kernel/flag.h:L7-L7`：macro CLONE_VM
    代码片段：`#define CLONE_VM             0x00000100`
  - `kernel/include/proc.h:L14-L14`：macro CLONE_VM
    代码片段：`#define CLONE_VM             0x00000100`
- 宏名重合：与 2024 比赛时在干什么有没有空帮我修个bug 在“调度与任务管理”维度发现 1 个同名定义：CLONE_THREAD。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/flag.h:L13-L13`：macro CLONE_THREAD
    代码片段：`#define CLONE_THREAD         0x00010000`
  - `kernel/include/proc.h:L22-L22`：macro CLONE_THREAD
    代码片段：`#define CLONE_THREAD         0x00010000`
- 文件路径重合：与 2024 比赛时在干什么有没有空帮我修个bug 在“设备驱动”维度出现同路径源码路径 `kernel/uart.c` / `kernel/uart.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
- 文件路径重合：与 2024 比赛时在干什么有没有空帮我修个bug 在“文件系统”维度出现同路径源码路径 `kernel/fs.c` / `kernel/fs.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs.c:L174-L178`：关键词命中
    代码片段：`{ int reult, ino; struct ext4_inode inode; char *file_path = NULL; // TODO: implement`
- 文件路径重合：与 2024 比赛时在干什么有没有空帮我修个bug 在“中断与异常”维度出现同路径源码路径 `kernel/plic.c` / `kernel/plic.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/plic.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/sbi.h" #include "include/plic.h" #include "include/proc.h" #include "include/printf.h"`
- 文件路径重合：与 2024 比赛时在干什么有没有空帮我修个bug 在“中断与异常”维度出现同路径源码路径 `kernel/trap.c` / `kernel/trap.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/trap.c:L32-L36`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
  - `kernel/trap.c:L6-L10`：关键词命中
    代码片段：`#include "include/proc.h" #include "include/sbi.h" #include "include/plic.h" #include "include/trap.h" #include "include/syscall.h"`
- 文件路径重合：与 2024 比赛时在干什么有没有空帮我修个bug 在“内存管理”维度出现同路径源码路径 `kernel/vm.c` / `kernel/vm.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/vm.c:L13-L17`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/vm.h" #include "include/kalloc.h" #include "include/proc.h" #include "include/printf.h"`
- 文件路径重合：与 2024 比赛时在干什么有没有空帮我修个bug 在“内存管理”维度出现同路径源码路径 `kernel/kalloc.c` / `kernel/kalloc.c`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：high）
  证据：
  - `kernel/kalloc.c:L37-L41`：关键词命中
    代码片段：`static uint64 super_start; static uint64 super_end; static uint refcnt[(PHYSTOP - SUPERPGNUM * SUPERPGSIZE) / PGSIZE]; static uint super_refcnt[SUPERPGNUM];`
  - `kernel/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "include/riscv.h" #include "include/spinlock.h" #include "include/kalloc.h" #include "include/string.h" #include "include/printf.h"`
- 结构体/类型重合：与 2024 比赛时在干什么有没有空帮我修个bug 在“内存管理”维度发现 1 个同名定义：run。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `kernel/kalloc.c:L17-L17`：struct run
    代码片段：`struct run {`
  - `kernel/kalloc.c:L19-L19`：struct run
    代码片段：`struct run {`

## 相似点

- 与 2024 比赛时在干什么有没有空帮我修个bug 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/virtio.h:L1-L4`：关键词命中
    代码片段：`// // virtio device definitions. // for both the mmio interface, and virtio descriptors. // only tested with qemu.`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs.c:L2-L6`：关键词命中
    代码片段：`//   + Blocks: allocator for raw disk blocks. //   + Log: crash recovery for multi-step updates. //   + Files: inode allocator, reading, writing, metadata. //   + Directories: i...`
  - `kernel/fs.h:L7-L11`：关键词命中
    代码片段：`// Disk layout: // [ boot block | super block | log | inode blocks | //                                          free bit map | data blocks] //`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/plic.c:L6-L10`：关键词命中
    代码片段：`// // the riscv Platform Level Interrupt Controller (PLIC). //`
  - `kernel/trap.c:L32-L36`：关键词命中
    代码片段：`// // handle an interrupt, exception, or system call from user space. // called from, and returns to, trampoline.S // return value is user satp for trampoline.S to switch to.`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/vm.c:L13-L17`：关键词命中
    代码片段：`/* * the kernel's page table. */ pagetable_t kernel_pagetable;`
  - `kernel/kalloc.c:L37-L41`：关键词命中
    代码片段：`static uint64 super_start; static uint64 super_end; static uint refcnt[(PHYSTOP - SUPERPGNUM * SUPERPGSIZE) / PGSIZE]; static uint super_refcnt[SUPERPGNUM];`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/proc.c:L4-L8`：关键词命中
    代码片段：`#include "riscv.h" #include "spinlock.h" #include "proc.h" #include "defs.h" #include "fcntl.h"`
  - `kernel/proc.h:L21-L25`：关键词命中
    代码片段：`// Per-CPU state. struct cpu { struct proc *proc;          // The process running on this cpu, or null. struct context context;     // swtch() here to enter scheduler(). int nof...`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/fs.c:L15-L19`：关键词命中
    代码片段：`#include "param.h" #include "stat.h" #include "spinlock.h" #include "proc.h" #include "sleeplock.h"`
  - `kernel/vm.c:L5-L9`：关键词命中
    代码片段：`#include "riscv.h" #include "defs.h" #include "spinlock.h" #include "proc.h" #include "fs.h"`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `kernel/trap.c:L59-L63`：关键词命中
    代码片段：`kexit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "spinlock.h" #include "proc.h" #include "syscall.h" #include "defs.h" #include "fs.h"`
- 与 2024 比赛时在干什么有没有空帮我修个bug 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 比赛时在干什么有没有空帮我修个bug 的语言构成不同：待测作品为 {'build': 405, 'make': 1, 'c': 50218, 'asm': 274, 'text': 6}，历史样本为 {'json': 35, 'build': 476, 'markdown': 920, 'c': 49662, 'asm': 1733, 'text': 76, 'toml': 21, 'rust': 2924}。（置信度：medium）

## 可能创新点

- 待测作品包含 66 个历史样本 2024 比赛时在干什么有没有空帮我修个bug 中未见的独特函数/类型定义（如 `ext4_bcache_clear_dirty`, `ext4_bcache_set_dirty`, `ext4_dir_search_result`, `ext4_direntry`, `ext4_xattr_list_entry`, `ext4_block_group_ref`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `kernel/lwext4/include/ext4_bcache.h:L183-L183`：fn ext4_bcache_clear_dirty
    代码片段：`static inline void ext4_bcache_clear_dirty(struct ext4_buf *buf) {`
  - `kernel/lwext4/include/ext4_bcache.h:L178-L178`：fn ext4_bcache_set_dirty
    代码片段：`static inline void ext4_bcache_set_dirty(struct ext4_buf *buf) {`

## 附录：核验摘要

- 关键结论数：30
- 含证据关键结论数：30（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
