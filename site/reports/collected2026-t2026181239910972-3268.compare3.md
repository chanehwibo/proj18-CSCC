# 2026 T2026181239910972 比较报告

- 对比历史仓库：2024 海底小纵队
- 生成时间：2026-07-02T05:03:47.126410+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2024 海底小纵队（来源：赛事历史作品）：画像相似度 score=8.94；同属 ucore-variant 风格; 语言构成相似度 0.72; OS 维度重合度 1.00; 代码规模接近度 0.51

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2024 海底小纵队 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-kernel/src/driver/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Here lies the driver framework, and all driver implementations. //! //! Most drivers implement the [Driver] trait, but some drivers are for system`
  - `anemone-kernel/src/device/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console subsystem. //! //! Here lies /dev/console.`
  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
- 与 2024 海底小纵队 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-kernel/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Virtual file system and filesystem drivers. // vfs infrastructure mod anonymous; mod cache_stats;`
  - `anemone-kernel/src/fs/file.rs:L7-L11`：关键词命中
    代码片段：`bitflags! { #[derive(Debug, Clone, Copy, PartialEq, Eq)] /// Open-time VFS behavior flags for one file object. /// /// This is not a Linux UAPI bitset or a mirror of Linux 'fmod...`
  - `vendor/fatfs/src/fs.rs:L21-L25`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `kernel/src/main.rs:L190-L194`：关键词命中
    代码片段：`.expect("can't create tmp dir"); // Initialize the Dentry node. // dentry::dentry_init(rootfs); // FileItem::fs_open("/bin", OpenFlags::O_DIRECTORY)`
- 与 2024 海底小纵队 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-kernel/src/time/timer/irq.rs:L3-L7`：关键词命中
    代码片段：`use super::{TimerEvent, expire_ticks_after, push_timer_event}; /// Schedule a timer event to run in interrupt context on the local CPU after /// the given duration. ///`
  - `anemone-kernel/src/time/timer/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Soft timer. //! //! Timer events are one-shot completions. Callers explicitly choose whether a`
  - `kernel/src/main.rs:L64-L68`：关键词命中
    代码片段：`#[polyhal::arch_interrupt] /// Handle kernel interrupt fn kernel_interrupt(cx_ref: &mut TrapFrame, trap_type: TrapType) { match trap_type {`
  - `kernel/src/tasks/mod.rs:L132-L136`：关键词命中
    代码片段：`#[derive(Debug, Clone, Copy, Default)] pub struct ProcessTimer { pub timer: ITimerVal, pub next: TimeVal, pub last: TimeVal,`
- 与 2024 海底小纵队 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-kernel/src/mm/stack.rs:L26-L30`：关键词命中
    代码片段：`let vpn_range = unsafe { mm::remap::alloc_virt_range(NPAGES + 1) } .expect("failed to allocate virtual range for boot stack guard page"); // The first page is the guard – we sim...`
  - `anemone-kernel/src/mm/layout.rs:L7-L11`：关键词命中
    代码片段：`/// Common kernel layout and key constants: /// /// ## Page Table Managed Spaces: /// ''' ///  -0x0   +-----------+---------------+ -0x0`
  - `kernel/src/syscall/mm.rs:L4-L8`：关键词命中
    代码片段：`use polyhal::PAGE_SIZE; use polyhal::USER_VADDR_END; use frame_allocator::ceil_div; use log::debug;`
  - `kernel/src/main.rs:L39-L43`：关键词命中
    代码片段：`use devices::{self, get_int_device}; use executor::current_task; use frame_allocator::{self, frame_alloc_persist, frame_unalloc}; use hal; use tasks::UserTask;`
- 与 2024 海底小纵队 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-kernel/src/task/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File System related structures and functions for a task. //! //! Reference:`
  - `anemone-kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! we expose all fields of Task, ThreadGroup, etc. to the whole task module. //! this is a bit too much, but i think that's inevitable for kernel code. too //! much encapsulati...`
  - `vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`
  - `kernel/src/tasks/elf.rs:L14-L18`：关键词命中
    代码片段：`use crate::tasks::memset::MemType; use super::task::UserTask; pub trait ElfExtra {`
- 与 2024 海底小纵队 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-kernel/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`//! TODO: Typed no-irq lock. //! //! TODO: Interrupt-safe lazy.`
  - `anemone-kernel/src/sync/mono.rs:L1-L4`：关键词命中
    代码片段：`#[cfg(debug_assertions)] use core::sync::atomic::{AtomicBool, Ordering}; use core::{cell::UnsafeCell, mem::MaybeUninit};`
  - `kernel/src/main.rs:L30-L34`：关键词命中
    代码片段：`mod user; use core::sync::atomic::{AtomicBool, Ordering}; use polyhal::addr::{PhysPage, VirtPage};`
  - `kernel/src/socket.rs:L5-L9`：关键词命中
    代码片段：`use fs::INodeInterface; use lose_net_stack::net_trait::SocketInterface; use sync::Mutex; use vfscore::{Metadata, PollEvent, VfsResult};`
- 与 2024 海底小纵队 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `anemone-abi/src/syscall/mod.rs:L12-L16`：关键词命中
    代码片段：`pub use native::*; /// The Linux kernel actually does not define a maximum syscall number, /// but it's obvious that syscall numbers won't exceed this value minus one, on /// an...`
  - `anemone-abi/src/syscall/riscv.rs:L14-L18`：关键词命中
    代码片段：`unsafe { core::arch::asm!( "ecall", in("a7") sysno, in("a0") arg0,`
  - `kernel/src/syscall/fd.rs:L14-L18`：关键词命中
    代码片段：`use crate::epoll::{EpollEvent, EpollFile}; use crate::syscall::consts::{from_vfs, FcntlCmd, IoVec, AT_CWD}; use crate::syscall::func::timespc_now; use crate::syscall::time::curr...`
  - `kernel/src/syscall/mm.rs:L7-L11`：关键词命中
    代码片段：`use log::debug; use crate::syscall::consts::from_vfs; use crate::syscall::consts::MSyncFlags; use crate::syscall::consts::MapFlags;`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 与 2024 海底小纵队 在“文件系统”维度发现片段级代码相似度 0.81 （token=0.71, structure=1.00）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：clone, copy, debug, derive, eq, partialeq。（置信度：high）
  证据：
  - `anemone-kernel/src/fs/file.rs:L7-L11`：关键词命中
    代码片段：`bitflags! { #[derive(Debug, Clone, Copy, PartialEq, Eq)] /// Open-time VFS behavior flags for one file object. /// /// This is not a Linux UAPI bitset or a mirror of Linux 'fmod...`
  - `vendor/fatfs/src/fs.rs:L29-L33`：enum FatType
    代码片段：`/// 'FatType' values are based on the size of File Allocation Table entry. #[derive(Copy, Clone, Eq, PartialEq, Debug)] pub enum FatType { /// 12 bits per FAT entry Fat12,`
- 与 2024 海底小纵队 在“中断与异常”维度发现片段级代码相似度 0.59 （token=0.53, structure=0.69）。这属于代码级相似线索，不等同于抄袭结论，需要人工结合完整文件和提交历史复核。 共同 token：f, fmt, fn, for, formatter, impl, mut, result。（置信度：medium）
  证据：
  - `anemone-kernel/src/time/timer/mod.rs:L22-L26`：fn fmt
    代码片段：`impl Debug for TimerLane { fn fmt(&self, f: &mut core::fmt::Formatter<'_>) -> core::fmt::Result { match self { Self::Irq(_) => f.write_str("Irq"),`
  - `vendor/x86/src/irq.rs:L18-L22`：fn fmt
    代码片段：`impl fmt::Display for InterruptDescription { fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result { write!( f,`
- 文件路径重合：与 2024 海底小纵队 在“文件系统”维度出现同名文件源码路径 `anemone-rs/src/fs.rs` / `vendor/fatfs/src/fs.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `anemone-rs/src/fs.rs:L9-L13`：struct OpenOptions
    代码片段：`#[derive(Debug)] pub struct OpenOptions { read: bool, write: bool,`
  - `vendor/fatfs/src/fs.rs:L21-L25`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
- 文件路径重合：与 2024 海底小纵队 在“中断与异常”维度出现同名文件源码路径 `anemone-kernel/src/time/timer/irq.rs` / `vendor/x86/src/irq.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `anemone-kernel/src/time/timer/irq.rs:L3-L7`：关键词命中
    代码片段：`use super::{TimerEvent, expire_ticks_after, push_timer_event}; /// Schedule a timer event to run in interrupt context on the local CPU after /// the given duration. ///`
  - `vendor/x86/src/irq.rs:L9-L13`：struct InterruptDescription
    代码片段：`/// x86 Exception description (see also Intel Vol. 3a Chapter 6). #[derive(Debug)] pub struct InterruptDescription { pub vector: u8, pub mnemonic: &'static str,`

## 相似点

- 与 2024 海底小纵队 同属 ucore-variant 风格。（置信度：medium）
- 与 2024 海底小纵队 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-kernel/src/driver/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Here lies the driver framework, and all driver implementations. //! //! Most drivers implement the [Driver] trait, but some drivers are for system`
  - `anemone-kernel/src/device/console.rs:L1-L3`：关键词命中
    代码片段：`//! Console subsystem. //! //! Here lies /dev/console.`
- 与 2024 海底小纵队 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-kernel/src/fs/mod.rs:L1-L5`：关键词命中
    代码片段：`//! Virtual file system and filesystem drivers. // vfs infrastructure mod anonymous; mod cache_stats;`
  - `anemone-kernel/src/fs/file.rs:L7-L11`：关键词命中
    代码片段：`bitflags! { #[derive(Debug, Clone, Copy, PartialEq, Eq)] /// Open-time VFS behavior flags for one file object. /// /// This is not a Linux UAPI bitset or a mirror of Linux 'fmod...`
- 与 2024 海底小纵队 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-kernel/src/time/timer/irq.rs:L3-L7`：关键词命中
    代码片段：`use super::{TimerEvent, expire_ticks_after, push_timer_event}; /// Schedule a timer event to run in interrupt context on the local CPU after /// the given duration. ///`
  - `anemone-kernel/src/time/timer/mod.rs:L1-L3`：关键词命中
    代码片段：`//! Soft timer. //! //! Timer events are one-shot completions. Callers explicitly choose whether a`
- 与 2024 海底小纵队 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-kernel/src/mm/stack.rs:L26-L30`：关键词命中
    代码片段：`let vpn_range = unsafe { mm::remap::alloc_virt_range(NPAGES + 1) } .expect("failed to allocate virtual range for boot stack guard page"); // The first page is the guard – we sim...`
  - `anemone-kernel/src/mm/layout.rs:L7-L11`：关键词命中
    代码片段：`/// Common kernel layout and key constants: /// /// ## Page Table Managed Spaces: /// ''' ///  -0x0   +-----------+---------------+ -0x0`
- 与 2024 海底小纵队 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-kernel/src/task/fs.rs:L1-L3`：关键词命中
    代码片段：`//! File System related structures and functions for a task. //! //! Reference:`
  - `anemone-kernel/src/task/mod.rs:L1-L3`：关键词命中
    代码片段：`//! we expose all fields of Task, ThreadGroup, etc. to the whole task module. //! this is a bit too much, but i think that's inevitable for kernel code. too //! much encapsulati...`
- 与 2024 海底小纵队 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-kernel/src/sync/mod.rs:L1-L3`：关键词命中
    代码片段：`//! TODO: Typed no-irq lock. //! //! TODO: Interrupt-safe lazy.`
  - `anemone-kernel/src/sync/mono.rs:L1-L4`：关键词命中
    代码片段：`#[cfg(debug_assertions)] use core::sync::atomic::{AtomicBool, Ordering}; use core::{cell::UnsafeCell, mem::MaybeUninit};`
- 与 2024 海底小纵队 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `anemone-abi/src/syscall/mod.rs:L12-L16`：关键词命中
    代码片段：`pub use native::*; /// The Linux kernel actually does not define a maximum syscall number, /// but it's obvious that syscall numbers won't exceed this value minus one, on /// an...`
  - `anemone-abi/src/syscall/riscv.rs:L14-L18`：关键词命中
    代码片段：`unsafe { core::arch::asm!( "ecall", in("a7") sysno, in("a0") arg0,`
- 与 2024 海底小纵队 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2024 海底小纵队 的语言构成不同：待测作品为 {'markdown': 31718, 'yaml': 40, 'text': 4147, 'json': 104, 'build': 1381, 'toml': 398, 'rust': 97500, 'c': 27779, 'make': 664}，历史样本为 {'json': 123, 'yaml': 68, 'build': 6380, 'markdown': 20083, 'toml': 259, 'asm': 359, 'text': 28, 'rust': 379391, 'c': 27758, 'make': 664}。（置信度：medium）

## 可能创新点

- 待测作品包含 362 个历史样本 2024 海底小纵队 中未见的独特函数/类型定义（如 `now_us_with_probe`, `sched_yield_with_probe`, `wait_for_flag`, `case_result`, `on_case_start`, `unique_shmat`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `anemone-apps/user-test/src/ltp/component/wait_probe.rs:L126-L126`：fn now_us_with_probe
    代码片段：`pub(in crate::ltp) fn now_us_with_probe(probe: &mut LtpWaitLoopProbe<'_>) -> Result<i64, Errno> {`
  - `anemone-apps/user-test/src/ltp/component/wait_probe.rs:L156-L156`：fn sched_yield_with_probe
    代码片段：`pub(in crate::ltp) fn sched_yield_with_probe(probe: &LtpWaitLoopProbe<'_>) -> Result<(), Errno> {`

## 附录：核验摘要

- 关键结论数：19
- 含证据关键结论数：19（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
