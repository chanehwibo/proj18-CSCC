# 2026 T2026100049911101 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026100049911101-938`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：142
- 代码/文本行数：14270
- 主要语言：rust 13304 LOC, build 614 LOC, markdown 128 LOC, toml 59 LOC, yaml 53 LOC

## 总览

2026 T2026100049911101 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 13304 LOC, build: 614 LOC, markdown: 128 LOC, toml: 59 LOC。仓库包含 142 个已扫描文件、约 14270 行可分析文本，当前抽取到 1009 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, Makefile, kernel/build.rs, kernel/Cargo.toml, filesystem/devfs/Cargo.toml。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[profile.release] # debug = true`
  - `Makefile:L1-L3`：构建入口
    代码片段：`SHELL := /bin/bash include scripts/config.mk export BOARD := qemu`
  - `kernel/build.rs:L1-L3`：构建入口
    代码片段：`use std::io::Result; use std::{env, fs, path::PathBuf};`
  - `kernel/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "kernel" version = "0.1.0"`
  - `filesystem/devfs/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "devfs" version = "0.1.0"`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：trait ElfExtra, fn get_ph_addr, fn dynsym, fn relocate, enum UserTaskControlFlow。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/tasks/mod.rs` | L7-L11 | 关键词命中 |
| `kernel/src/tasks/exec.rs` | L17-L21 | 关键词命中 |
| `kernel/src/tasks/task.rs` | L18-L22 | 关键词命中 |
| `kernel/src/tasks/elf.rs` | L8-L12 | trait ElfExtra |
| `kernel/src/tasks/elf.rs` | L9-L13 | fn get_ph_addr |
| `kernel/src/tasks/elf.rs` | L10-L14 | fn dynsym |

### 关键代码片段

  - `kernel/src/tasks/mod.rs:L7-L11`：关键词命中
    代码片段：`mod shm; mod stack; mod task; use self::initproc::initproc;`
  - `kernel/src/tasks/exec.rs:L17-L21`：关键词命中
    代码片段：`pub fn exec_with_process( task: Arc<UserTask>, curr_dir: PathBuf, path: String,`
  - `kernel/src/tasks/task.rs:L18-L22`：关键词命中
    代码片段：`use core::{cmp::max, mem::size_of}; use devices::PAGE_SIZE; use executor::{release_task, task::TaskType, task_id_alloc, AsyncTask, TaskId}; use fs::{file::File, pathbuf::PathBuf...`
  - `kernel/src/tasks/elf.rs:L8-L12`：trait ElfExtra
    代码片段：`}; pub trait ElfExtra { fn get_ph_addr(&self) -> Result<u64, Errno>; fn dynsym(&self) -> Result<&[DynEntry64], &'static str>;`

### 相关符号

`trait ElfExtra` at `kernel/src/tasks/elf.rs:L8`、`fn get_ph_addr` at `kernel/src/tasks/elf.rs:L9`、`fn dynsym` at `kernel/src/tasks/elf.rs:L10`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn sys_brk, fn sys_mmap, fn sys_munmap, fn sys_mprotect, fn sys_msync。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/syscall/mm.rs` | L11-L15 | 关键词命中 |
| `kernel/src/main.rs` | L107-L111 | 关键词命中 |
| `kernel/src/tasks/exec.rs` | L40-L44 | 关键词命中 |
| `kernel/src/syscall/mm.rs` | L15-L19 | fn sys_brk |
| `kernel/src/syscall/mm.rs` | L24-L28 | fn sys_mmap |
| `kernel/src/syscall/mm.rs` | L134-L138 | fn sys_munmap |

### 关键代码片段

  - `kernel/src/syscall/mm.rs:L11-L15`：关键词命中
    代码片段：`use syscalls::Errno; // The high 25bits in sv39 should be the same as bit 38. const MAP_AREA_START: usize = 0x2_0000_0000;`
  - `kernel/src/main.rs:L107-L111`：关键词命中
    代码片段：`"illegal instruction fault @ {:#x} paddr: {:?}", addr, task.page_table.translate(addr.into()), ); warn!("the fault occurs @ {:#x}", cx_ref[TrapFrameArgs::SEPC]);`
  - `kernel/src/tasks/exec.rs:L40-L44`：关键词命中
    代码片段：`user_task.pcb.lock().memset.clear(); user_task.page_table.restore(); user_task.page_table.change();`
  - `kernel/src/syscall/mm.rs:L15-L19`：fn sys_brk
    代码片段：`impl UserTaskContainer { pub fn sys_brk(&self, addr: usize) -> SysResult { let heap = self.task.pcb.lock().heap; debug!("sys_brk @ new: {:#x} old: {:#x}", addr, heap);`

### 相关符号

`fn sys_brk` at `kernel/src/syscall/mm.rs:L15`、`fn sys_mmap` at `kernel/src/syscall/mm.rs:L24`、`fn sys_munmap` at `kernel/src/syscall/mm.rs:L134`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn sys_dup, fn sys_dup3, fn sys_dup2, fn sys_read, fn sys_write。（置信度：medium）
  - 静态识别到 118 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/syscall/mm.rs` | L1-L4 | 关键词命中 |
| `kernel/src/syscall/mod.rs` | L22-L26 | 关键词命中 |
| `kernel/src/syscall/task.rs` | L1-L5 | 关键词命中 |
| `kernel/src/syscall/fd.rs` | L25-L29 | fn sys_dup |
| `kernel/src/syscall/fd.rs` | L31-L35 | fn sys_dup3 |
| `kernel/src/syscall/fd.rs` | L54-L58 | fn sys_dup2 |

### 关键代码片段

  - `kernel/src/syscall/mm.rs:L1-L4`：关键词命中
    代码片段：`use super::SysResult; use crate::syscall::types::mm::map_mprot_to_flags; use crate::tasks::{MemArea, MemType}; use crate::user::UserTaskContainer;`
  - `kernel/src/syscall/mod.rs:L22-L26`：关键词命中
    代码片段：`impl UserTaskContainer { pub async fn syscall(&self, call_id: usize, args: [usize; 6]) -> Result<usize, Errno> { let sysno = Sysno::new(call_id).ok_or(Errno::EINVAL)?; match sys...`
  - `kernel/src/syscall/task.rs:L1-L5`：关键词命中
    代码片段：`use super::SysResult; use crate::{ syscall::{time::WaitUntilsec, types::signal::SignalUserContext}, tasks::{exec::exec_with_process, futex_requeue, futex_wake, UserTask, WaitFut...`
  - `kernel/src/syscall/fd.rs:L25-L29`：fn sys_dup
    代码片段：`impl UserTaskContainer { pub fn sys_dup(&self, fd: usize) -> SysResult { debug!("sys_dup3 @ fd_src: {}", fd); let fd_dst = self.task.alloc_fd().ok_or(Errno::EMFILE)?;`

### 相关符号

`fn sys_dup` at `kernel/src/syscall/fd.rs:L25`、`fn sys_dup3` at `kernel/src/syscall/fd.rs:L31`、`fn sys_dup2` at `kernel/src/syscall/fd.rs:L54`、`fn sys_dup` at `kernel/src/syscall/fd.rs:L25`、`fn sys_dup3` at `kernel/src/syscall/fd.rs:L31`、`fn sys_dup2` at `kernel/src/syscall/fd.rs:L54`、`fn sys_read` at `kernel/src/syscall/fd.rs:L58`、`fn sys_write` at `kernel/src/syscall/fd.rs:L71`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：fn build_devfs, fn init, struct WaitBlockingRead, fn poll, struct WaitBlockingWrite。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/syscall/fd.rs` | L8-L12 | 关键词命中 |
| `filesystem/fs/src/lib.rs` | L9-L13 | 关键词命中 |
| `filesystem/fs/src/file.rs` | L1-L3 | 关键词命中 |
| `filesystem/fs/src/lib.rs` | L31-L35 | fn build_devfs |
| `filesystem/fs/src/lib.rs` | L37-L41 | fn init |
| `filesystem/fs/src/lib.rs` | L58-L62 | struct WaitBlockingRead |

### 关键代码片段

  - `kernel/src/syscall/fd.rs:L8-L12`：关键词命中
    代码片段：`use core::time::Duration; use executor::yield_now; use fs::dentry::umount; use fs::file::File; use fs::{pipe::create_pipe, SeekFrom};`
  - `filesystem/fs/src/lib.rs:L9-L13`：关键词命中
    代码片段：`extern crate bitflags; pub mod dentry; #[cfg(root_fs = "fat32")] mod fatfs_shim;`
  - `filesystem/fs/src/file.rs:L1-L3`：关键词命中
    代码片段：`use crate::{dentry::get_mounted, pathbuf::PathBuf, WaitBlockingRead, WaitBlockingWrite}; use alloc::{string::String, sync::Arc, vec::Vec}; use libc_types::{`
  - `filesystem/fs/src/lib.rs:L31-L35`：fn build_devfs
    代码片段：`pub use vfscore::{FileType, INodeInterface, SeekFrom}; pub fn build_devfs() -> Arc<DevFS> { let dev_dir = DevDir::new();`

### 相关符号

`fn build_devfs` at `filesystem/fs/src/lib.rs:L31`、`fn init` at `filesystem/fs/src/lib.rs:L37`、`struct WaitBlockingRead` at `filesystem/fs/src/lib.rs:L58`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct LazyInit, fn init_by, fn is_init, fn try_get, fn check_init。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/socket.rs` | L9-L13 | 关键词命中 |
| `crates/sync/src/lib.rs` | L2-L6 | 关键词命中 |
| `kernel/src/tasks/shm.rs` | L1-L5 | 关键词命中 |
| `crates/sync/src/lib.rs` | L11-L15 | struct LazyInit |
| `crates/sync/src/lib.rs` | L27-L31 | fn init_by |
| `crates/sync/src/lib.rs` | L33-L37 | fn is_init |

### 关键代码片段

  - `kernel/src/socket.rs:L9-L13`：关键词命中
    代码片段：`use lose_net_stack::net_trait::SocketInterface; use polyhal::debug_console::DebugConsole; use sync::Mutex; use syscalls::Errno; use vfscore::VfsResult;`
  - `crates/sync/src/lib.rs:L2-L6`：关键词命中
    代码片段：`pub use spin::{ lazy::Lazy, Mutex, MutexGuard, RwLock, RwLockReadGuard, RwLockUpgradableGuard, RwLockWriteGuard, };`
  - `kernel/src/tasks/shm.rs:L1-L5`：关键词命中
    代码片段：`use alloc::{collections::BTreeMap, sync::Arc, vec::Vec}; use runtime::frame::FrameTracker; use sync::Mutex; pub static SHARED_MEMORY: Mutex<BTreeMap<usize, Arc<SharedMemory>>> =...`
  - `crates/sync/src/lib.rs:L11-L15`：struct LazyInit
    代码片段：`use core::sync::atomic::{AtomicBool, Ordering}; pub struct LazyInit<T> { inited: AtomicBool, data: UnsafeCell<MaybeUninit<T>>,`

### 相关符号

`struct LazyInit` at `crates/sync/src/lib.rs:L11`、`fn init_by` at `crates/sync/src/lib.rs:L27`、`fn is_init` at `crates/sync/src/lib.rs:L33`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn register_irq, struct PLIC, impl Driver::for::PLIC, fn get_id, fn try_handle_interrupt。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/src/main.rs` | L38-L42 | 关键词命中 |
| `kernel/src/syscall/fd.rs` | L19-L23 | 关键词命中 |
| `kernel/src/tasks/task.rs` | L49-L53 | 关键词命中 |
| `crates/devices/src/device.rs` | L94-L98 | fn register_irq |
| `driver/general-plic/src/lib.rs` | L14-L18 | struct PLIC |
| `driver/general-plic/src/lib.rs` | L18-L22 | impl Driver::for::PLIC |

### 关键代码片段

  - `kernel/src/main.rs:L38-L42`：关键词命中
    代码片段：`use libc_types::fcntl::OpenFlags; use polyhal::common::PageAlloc; use polyhal::irq::IRQ; use polyhal::mem::{get_fdt, get_mem_areas}; use polyhal::timer::{current_time, set_next_...`
  - `kernel/src/syscall/fd.rs:L19-L23`：关键词命中
    代码片段：`use libc_types::types::{IoVec, Stat, StatFS, StatMode, TimeSpec}; use log::debug; use polyhal::timer::current_time; use polyhal::VirtAddr; use syscalls::Errno;`
  - `kernel/src/tasks/task.rs:L49-L53`：关键词命中
    代码片段：`pub futex_table: Arc<Mutex<FutexTable>>, pub shms: Vec<MapedSharedMemory>, pub timer: [ProcessTimer; 3], pub threads: Vec<Weak<UserTask>>, pub exit_code: Option<usize>,`
  - `crates/devices/src/device.rs:L94-L98`：fn register_irq
    代码片段：`pub trait IntDriver: Driver { fn register_irq(&self, irq: u32, driver: Arc<dyn Driver>); }`

### 相关符号

`fn register_irq` at `crates/devices/src/device.rs:L94`、`struct PLIC` at `driver/general-plic/src/lib.rs:L14`、`impl Driver::for::PLIC` at `driver/general-plic/src/lib.rs:L18`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：enum DeviceType, struct DeviceSet, fn add_device, fn default, trait Driver。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `driver/kvirtio/src/lib.rs` | L6-L10 | 关键词命中 |
| `driver/kramdisk/src/lib.rs` | L11-L15 | 关键词命中 |
| `driver/ns16550a/src/lib.rs` | L6-L10 | 关键词命中 |
| `crates/devices/src/device.rs` | L3-L7 | enum DeviceType |
| `crates/devices/src/device.rs` | L13-L17 | struct DeviceSet |
| `crates/devices/src/device.rs` | L32-L36 | fn add_device |

### 关键代码片段

  - `driver/kvirtio/src/lib.rs:L6-L10`：关键词命中
    代码片段：`extern crate log; pub mod virtio_blk; pub mod virtio_impl; pub mod virtio_input;`
  - `driver/kramdisk/src/lib.rs:L11-L15`：关键词命中
    代码片段：`use alloc::sync::Arc; use devices::{ device::{BlkDriver, DeviceType, Driver}, driver_define, };`
  - `driver/ns16550a/src/lib.rs:L6-L10`：关键词命中
    代码片段：`use alloc::{sync::Arc, vec::Vec}; use devices::{ device::{DeviceType, Driver, UartDriver}, driver_define, fdt::Node,`
  - `crates/devices/src/device.rs:L3-L7`：enum DeviceType
    代码片段：`use crate::{INT_DEVICE, MAIN_UART}; pub enum DeviceType { RTC(Arc<dyn RtcDriver>), BLOCK(Arc<dyn BlkDriver>),`

### 相关符号

`enum DeviceType` at `crates/devices/src/device.rs:L3`、`struct DeviceSet` at `crates/devices/src/device.rs:L13`、`fn add_device` at `crates/devices/src/device.rs:L32`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
