# 2026 T202610487999858 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t202610487999858-1903`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：920
- 代码/文本行数：321556
- 主要语言：c 282443 LOC, cpp 36728 LOC, markdown 954 LOC, asm 864 LOC, build 549 LOC

## 总览

2026 T202610487999858 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 c: 282443 LOC, cpp: 36728 LOC, markdown: 954 LOC, asm: 864 LOC。仓库包含 920 个已扫描文件、约 321556 行可分析文本，当前抽取到 11606 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：CMakeLists.txt, Makefile, kernel/CMakeLists.txt, kernel/boot/CMakeLists.txt, kernel/driver/CMakeLists.txt。（置信度：high）
  证据：
  - `CMakeLists.txt:L1-L3`：构建入口
    代码片段：`cmake_minimum_required(VERSION 3.21) #if(NOT RISCV)`
  - `Makefile:L1-L3`：构建入口
    代码片段：`MAKEFLAGS += --no-print-directory`
  - `kernel/CMakeLists.txt:L1-L3`：构建入口
    代码片段：`#entry.S 处于SOURCES的第一个位置`
  - `kernel/boot/CMakeLists.txt:L1-L3`：构建入口
    代码片段：`if(RISCV) set_source_files_properties(`
  - `kernel/driver/CMakeLists.txt:L1-L3`：构建入口
    代码片段：`if(RISCV) set(driver_sources ${CMAKE_CURRENT_SOURCE_DIR}/riscv/virtio_disk.c`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct commit, fn alloc_aux, fn loadaux, enum redir, struct pipe。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/proc/exec.c` | L4-L8 | 关键词命中 |
| `kernel/proc/pipe.c` | L4-L8 | 关键词命中 |
| `kernel/proc/proc.c` | L4-L8 | 关键词命中 |
| `kernel/proc/exec.c` | L15-L19 | struct commit |
| `kernel/proc/exec.c` | L178-L182 | fn alloc_aux |
| `kernel/proc/exec.c` | L188-L192 | fn loadaux |

### 关键代码片段

  - `kernel/proc/exec.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h" #include "lib/elf.h"`
  - `kernel/proc/pipe.c:L4-L8`：关键词命中
    代码片段：`#include "param.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "lock/sleeplock.h" #include "fs/fcntl.h"`
  - `kernel/proc/proc.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "fs/vfs/fs.h"`
  - `kernel/proc/exec.c:L15-L19`：struct commit
    代码片段：`struct commit { char *path; uint64 last_bss;`

### 相关符号

`struct commit` at `kernel/proc/exec.c:L15`、`fn alloc_aux` at `kernel/proc/exec.c:L178`、`fn loadaux` at `kernel/proc/exec.c:L188`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn pci_map, fn kernelmap, fn protectpages, fn pa2pgnm, fn kinit。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/mem/vm.c` | L8-L12 | 关键词命中 |
| `kernel/mem/kalloc.c` | L9-L13 | 关键词命中 |
| `kernel/mem/slab.c` | L3-L7 | 关键词命中 |
| `kernel/mem/vm.c` | L206-L210 | fn pci_map |
| `kernel/mem/vm.c` | L282-L286 | fn kernelmap |
| `kernel/mem/vm.c` | L316-L320 | fn protectpages |

### 关键代码片段

  - `kernel/mem/vm.c:L8-L12`：关键词命中
    代码片段：`#include "defs.h" #include "fs/vfs/fs.h" #include "mem/kalloc.h" #include "lib/string.h" #include "dev/virtio.h"`
  - `kernel/mem/kalloc.c:L9-L13`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "mem/kalloc.h" #include <mem/slab.h>`
  - `kernel/mem/slab.c:L3-L7`：关键词命中
    代码片段：`#include "platform.h" #include "mem/kalloc.h" struct slab_cache *slab_cache[5];`
  - `kernel/mem/vm.c:L206-L210`：fn pci_map
    代码片段：`} void pci_map(int bus, int dev, int func, void *pages) { #ifdef LOONGARCH`

### 相关符号

`fn pci_map` at `kernel/mem/vm.c:L206`、`fn kernelmap` at `kernel/mem/vm.c:L282`、`fn protectpages` at `kernel/mem/vm.c:L316`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn unused, fn pagefault_handler, fn pagefault_handler, fn kernelvec, fn handle_merr。（置信度：medium）
  - 静态识别到 55 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/sys/syscall.c` | L5-L9 | 关键词命中 |
| `kernel/trap/riscv/trap.c` | L188-L192 | 关键词命中 |
| `kernel/trap/loongarch/trap.c` | L202-L206 | 关键词命中 |
| `kernel/sys/syscall.c` | L91-L95 | fn unused |
| `kernel/trap/riscv/trap.c` | L107-L111 | fn pagefault_handler |
| `kernel/trap/loongarch/trap.c` | L102-L106 | fn pagefault_handler |

### 关键代码片段

  - `kernel/sys/syscall.c:L5-L9`：关键词命中
    代码片段：`#include "lock/spinlock.h" #include "proc/proc.h" #include "sys/syscall.h" #include "defs.h" #include "lib/string.h"`
  - `kernel/trap/riscv/trap.c:L188-L192`：关键词命中
    代码片段：`exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->epc += 4;`
  - `kernel/trap/loongarch/trap.c:L202-L206`：关键词命中
    代码片段：`exit(-1); // sepc points to the ecall instruction, // but we want to return to the next instruction. p->trapframe->era += 4;`
  - `kernel/sys/syscall.c:L91-L95`：fn unused
    代码片段：`} uint64 unused() { return 0; }`

### 相关符号

`fn unused` at `kernel/sys/syscall.c:L91`、`fn pagefault_handler` at `kernel/trap/riscv/trap.c:L107`、`fn pagefault_handler` at `kernel/trap/loongarch/trap.c:L102`、`fn sys_mmap` at `kernel/sys/sysmem.c:L228`、`fn sys_munmap` at `kernel/sys/sysmem.c:L315`、`fn sys_mremap` at `kernel/sys/sysmem.c:L367`、`fn sys_madvise` at `kernel/sys/sysmem.c:L402`、`fn sys_mprotect` at `kernel/sys/sysmem.c:L407`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：fn init_fs_table, fn fs_init, fn filesystem_init, fn filesystem2_init, fn fs_mount。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/fs/vfs/fs.c` | L1-L3 | 关键词命中 |
| `kernel/fs/vfs/ops.c` | L1-L3 | 关键词命中 |
| `kernel/fs/vfs/file.c` | L5-L9 | 关键词命中 |
| `kernel/fs/vfs/fs.c` | L26-L30 | fn init_fs_table |
| `kernel/fs/vfs/fs.c` | L34-L38 | fn fs_init |
| `kernel/fs/vfs/fs.c` | L45-L49 | fn filesystem_init |

### 关键代码片段

  - `kernel/fs/vfs/fs.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include <defs.h>`
  - `kernel/fs/vfs/ops.c:L1-L3`：关键词命中
    代码片段：`#include "fs/vfs/ops.h" #include <fs/fcntl.h>`
  - `kernel/fs/vfs/file.c:L5-L9`：关键词命中
    代码片段：`#include "lock/sleeplock.h" #include "fs/stat.h" #include "fs/vfs/fs.h" #include "fs/vfs/file.h"`
  - `kernel/fs/vfs/fs.c:L26-L30`：fn init_fs_table
    代码片段：`struct spinlock fs_table_lock; void init_fs_table(void) { initlock(&fs_table_lock, "fs_table_lock"); for (int i = 0; i < VFS_MAX_FS; i++) {`

### 相关符号

`fn init_fs_table` at `kernel/fs/vfs/fs.c:L26`、`fn fs_init` at `kernel/fs/vfs/fs.c:L34`、`fn filesystem_init` at `kernel/fs/vfs/fs.c:L45`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn sem_init, fn sem_p, fn sem_v, class _LOCK_T, struct spinlock。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/proc/semaphore.c` | L1-L4 | 关键词命中 |
| `kernel/mem/uart.c` | L7-L11 | 关键词命中 |
| `kernel/fs/vfs/fs.c` | L24-L28 | 关键词命中 |
| `kernel/proc/semaphore.c` | L3-L7 | fn sem_init |
| `kernel/proc/semaphore.c` | L18-L22 | fn sem_p |
| `kernel/proc/semaphore.c` | L39-L43 | fn sem_v |

### 关键代码片段

  - `kernel/proc/semaphore.c:L1-L4`：关键词命中
    代码片段：`#include "lock/semaphore.h" #include "defs.h"`
  - `kernel/mem/uart.c:L7-L11`：关键词命中
    代码片段：`#include "mem/memlayout.h" #include "platform.h" #include "lock/spinlock.h" #include "proc/proc.h" #include "defs.h"`
  - `kernel/fs/vfs/fs.c:L24-L28`：关键词命中
    代码片段：`filesystem_t root_fs; // 仅用来加载init程序 struct spinlock fs_table_lock; void init_fs_table(void) {`
  - `kernel/proc/semaphore.c:L3-L7`：fn sem_init
    代码片段：`#include "defs.h" void sem_init(sem *s, int value, char *name) { s->value = value; s->top = 0;`

### 相关符号

`fn sem_init` at `kernel/proc/semaphore.c:L3`、`fn sem_p` at `kernel/proc/semaphore.c:L18`、`fn sem_v` at `kernel/proc/semaphore.c:L39`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn pagefault_handler, fn pagefault_handler, fn kernelvec, fn handle_merr, fn uservec。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/sys/plic.c` | L4-L8 | 关键词命中 |
| `kernel/trap/riscv/trap.c` | L8-L12 | 关键词命中 |
| `kernel/trap/loongarch/apic.c` | L3-L7 | 关键词命中 |
| `kernel/trap/riscv/trap.c` | L107-L111 | fn pagefault_handler |
| `kernel/trap/loongarch/trap.c` | L102-L106 | fn pagefault_handler |
| `kernel/trap/riscv/kernelvec.S` | L8-L12 | fn kernelvec |

### 关键代码片段

  - `kernel/sys/plic.c:L4-L8`：关键词命中
    代码片段：`#include "platform.h" #include "defs.h" #include "proc/plic.h" //`
  - `kernel/trap/riscv/trap.c:L8-L12`：关键词命中
    代码片段：`#include "mem/mem.h" #include "sbi.h" #include "proc/plic.h" #include "dev/virtio.h" #include "lib/string.h"`
  - `kernel/trap/loongarch/apic.c:L3-L7`：关键词命中
    代码片段：`#include "mem/memlayout.h" #include "platform.h" #include "trap/apic.h" #include <dev/pci/pci.h>`
  - `kernel/trap/riscv/trap.c:L107-L111`：fn pagefault_handler
    代码片段：`} int pagefault_handler(uint64 va, uint64 cause) { /* *TODO: 支持lazy allocation和cow`

### 相关符号

`fn pagefault_handler` at `kernel/trap/riscv/trap.c:L107`、`fn pagefault_handler` at `kernel/trap/loongarch/trap.c:L102`、`fn kernelvec` at `kernel/trap/riscv/kernelvec.S:L8`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：fn consoleready, fn pci_scan_device, fn pci_get_device_by_bus, struct virtio_blk_outhdr, macro VIRTIO_NET_F_CSUM。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `kernel/mem/uart.c` | L1-L4 | 关键词命中 |
| `kernel/driver/bio.c` | L23-L27 | 关键词命中 |
| `kernel/lib/console.c` | L1-L4 | 关键词命中 |
| `kernel/lib/console.c` | L215-L219 | fn consoleready |
| `kernel/driver/loongarch/pci.c` | L101-L105 | fn pci_scan_device |
| `kernel/driver/loongarch/pci.c` | L227-L231 | fn pci_get_device_by_bus |

### 关键代码片段

  - `kernel/mem/uart.c:L1-L4`：关键词命中
    代码片段：`// // low-level driver routines for 16550a UART. //`
  - `kernel/driver/bio.c:L23-L27`：关键词命中
    代码片段：`#include "fs/vfs/fs.h" #include "fs/buf.h" #include "dev/virtio.h" struct {`
  - `kernel/lib/console.c:L1-L4`：关键词命中
    代码片段：`// // Console input and output, to the uart. // Reads are line at a time. // Implements special input characters:`
  - `kernel/lib/console.c:L215-L219`：fn consoleready
    代码片段：`} int consoleready(void) { acquire(&cons.lock); const int ready = cons.w - cons.r;`

### 相关符号

`fn consoleready` at `kernel/lib/console.c:L215`、`fn pci_scan_device` at `kernel/driver/loongarch/pci.c:L101`、`fn pci_get_device_by_bus` at `kernel/driver/loongarch/pci.c:L227`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
