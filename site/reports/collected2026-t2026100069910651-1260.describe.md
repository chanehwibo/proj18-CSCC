# 2026 T2026100069910651 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026100069910651-1260`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：5823
- 代码/文本行数：2317804
- 主要语言：rust 1883678 LOC, json 282238 LOC, c 66544 LOC, markdown 43543 LOC, build 21235 LOC

## 总览

2026 T2026100069910651 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 1883678 LOC, json: 282238 LOC, c: 66544 LOC, markdown: 43543 LOC。仓库包含 5823 个已扫描文件、约 2317804 行可分析文本，当前抽取到 136706 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：build.rs, Cargo.toml, Makefile, api/Cargo.toml, arceos/Cargo.toml。（置信度：high）
  证据：
  - `build.rs:L1-L3`：构建入口
    代码片段：`use std::fs::{File, read_dir}; use std::io::{Result, Write}; use std::path::PathBuf;`
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] resolver = "2" members = ["api", "core", "process", "syscall_trace", "modules/vfs"]`
  - `Makefile:L1-L3`：构建入口
    代码片段：`AX_ROOT ?= $(PWD)/arceos AX_TESTCASE ?= oscomp ARCH ?= x86_64`
  - `api/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "ourkernel2026-api" version.workspace = true`
  - `arceos/Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] resolver = "2"`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：struct TaskExt, fn current_thread, fn current_thread_data, fn current_process, fn current_process_data。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `core/src/task.rs` | L15-L19 | 关键词命中 |
| `src/fs/imp/proc.rs` | L631-L635 | 关键词命中 |
| `vendor/x86/src/task.rs` | L1-L3 | 关键词命中 |
| `core/src/task.rs` | L18-L22 | struct TaskExt |
| `core/src/task.rs` | L27-L31 | fn current_thread |
| `core/src/task.rs` | L31-L35 | fn current_thread_data |

### 关键代码片段

  - `core/src/task.rs:L15-L19`：关键词命中
    代码片段：`use spin::Once; use ourkernel2026_process::process::Process; use ourkernel2026_process::thread::Thread; /// Task extended data for the monolithic kernel.`
  - `src/fs/imp/proc.rs:L631-L635`：关键词命中
    代码片段：`Seccomp:        2 Seccomp_filters:        1 Speculation_Store_Bypass:       thread vulnerable SpeculationIndirectBranch:      conditional enabled Cpus_allowed:   ffff`
  - `vendor/x86/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! Helpers to program the task state segment. //! See Intel 3a, Chapter 7`
  - `core/src/task.rs:L18-L22`：struct TaskExt
    代码片段：`/// Task extended data for the monolithic kernel. pub struct TaskExt { /// The time statistics. pub time: RefCell<TimeStat>,`

### 相关符号

`struct TaskExt` at `core/src/task.rs:L18`、`fn current_thread` at `core/src/task.rs:L27`、`fn current_thread_data` at `core/src/task.rs:L31`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn handle_page_fault, fn new_user_aspace_empty, fn copy_from_kernel, fn map_trampoline, fn map_elf。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `core/src/mm.rs` | L24-L28 | 关键词命中 |
| `arceos/modules/axmm/src/backend/alloc.rs` | L2-L6 | 关键词命中 |
| `src/fs/dynamic/file.rs` | L2-L6 | 关键词命中 |
| `src/mm.rs` | L12-L16 | fn handle_page_fault |
| `core/src/mm.rs` | L12-L16 | fn new_user_aspace_empty |
| `core/src/mm.rs` | L21-L25 | fn copy_from_kernel |

### 关键代码片段

  - `core/src/mm.rs:L24-L28`：关键词命中
    代码片段：`#[cfg(not(any(target_arch = "aarch64", target_arch = "loongarch64")))] { // ARMv8 (aarch64) and LoongArch64 use separate page tables for user space // (aarch64: TTBR0_EL1, Loong...`
  - `arceos/modules/axmm/src/backend/alloc.rs:L2-L6`：关键词命中
    代码片段：`use axalloc::global_allocator; use axhal::mem::{phys_to_virt, virt_to_phys}; use axhal::paging::{MappingFlags, PageSize, PageTable}; use memory_addr::{PhysAddr, VirtAddr};`
  - `src/fs/dynamic/file.rs:L2-L6`：关键词命中
    代码片段：`use super::dynamic::{DynamicFs, DynamicNode}; use alloc::{borrow::Cow, sync::Arc, vec::Vec}; use axsync::RawMutex; use inherit_methods_macro::inherit_methods;`
  - `src/mm.rs:L12-L16`：fn handle_page_fault
    代码片段：`#[register_trap_handler(PAGE_FAULT)] fn handle_page_fault(vaddr: VirtAddr, access_flags: MappingFlags, is_user: bool) -> bool { if vaddr.as_usize() > PHYS_VIRT_OFFSET { error!(`

### 相关符号

`fn handle_page_fault` at `src/mm.rs:L12`、`fn new_user_aspace_empty` at `core/src/mm.rs:L12`、`fn copy_from_kernel` at `core/src/mm.rs:L21`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn handle_syscall, fn stub_unimplemented, fn stub_bypass, fn stub_kill, fn post_trap_callback。（置信度：medium）
  - 静态识别到 547 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/syscall.rs` | L2-L6 | 关键词命中 |
| `vendor/syscalls/src/syscall/arm.rs` | L7-L11 | 关键词命中 |
| `vendor/syscalls/src/syscall/x86.rs` | L7-L11 | 关键词命中 |
| `src/syscall.rs` | L27-L31 | fn handle_syscall |
| `src/syscall.rs` | L539-L543 | fn stub_unimplemented |
| `src/syscall.rs` | L547-L551 | fn stub_bypass |

### 关键代码片段

  - `src/syscall.rs:L2-L6`：关键词命中
    代码片段：`use axhal::{ arch::TrapFrame, trap::{SYSCALL, register_trap_handler}, }; use starry_core::task::{time_stat_from_kernel_to_user, time_stat_from_user_to_kernel};`
  - `vendor/syscalls/src/syscall/arm.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %r5 // // %r7 is used for the syscall number. // // %r0 is reused for the syscall return value.`
  - `vendor/syscalls/src/syscall/x86.rs:L7-L11`：关键词命中
    代码片段：`// arg6: %ebp // // eax is used for both the syscall number and the syscall return value. // // No other registers are clobbered. syscalls can also modify memory. With the`
  - `src/syscall.rs:L27-L31`：fn handle_syscall
    代码片段：`#[register_trap_handler(SYSCALL)] fn handle_syscall(tf: &mut TrapFrame, syscall_num: usize) -> isize { let sysno = Sysno::new(syscall_num as _); if sysno.is_none() {`

### 相关符号

`fn handle_syscall` at `src/syscall.rs:L27`、`fn stub_unimplemented` at `src/syscall.rs:L539`、`fn stub_bypass` at `src/syscall.rs:L547`、`fn handle_syscall` at `src/syscall.rs:L27`、`fn handle_syscall` at `arceos/modules/axhal/src/trap.rs:L51`、`fn syscall0` at `vendor/syscalls/src/syscall/arm.rs:L21`、`fn syscall1` at `vendor/syscalls/src/syscall/arm.rs:L39`、`fn syscall2` at `vendor/syscalls/src/syscall/arm.rs:L57`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：fn mount_at, fn mount_all, fn new_devfs, struct Null, fn read_at。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `src/fs/imp/tmp.rs` | L50-L54 | 关键词命中 |
| `src/fs/dynamic/file.rs` | L45-L49 | 关键词命中 |
| `vendor/fatfs/src/fs.rs` | L18-L22 | 关键词命中 |
| `src/fs/mount.rs` | L7-L11 | fn mount_at |
| `src/fs/mount.rs` | L20-L24 | fn mount_all |
| `src/fs/imp/dev.rs` | L16-L20 | fn new_devfs |

### 关键代码片段

  - `src/fs/imp/tmp.rs:L50-L54`：关键词命中
    代码片段：`pub struct MemoryFs { inodes: Mutex<Slab<Arc<Inode>>>, root: Mutex<Option<DirEntry<RawMutex>>>, }`
  - `src/fs/dynamic/file.rs:L45-L49`：关键词命中
    代码片段：`#[inherit_methods(from = "self.node")] impl NodeOps<RawMutex> for SimpleFile { fn inode(&self) -> u64; fn metadata(&self) -> VfsResult<Metadata>; fn update_metadata(&self, updat...`
  - `vendor/fatfs/src/fs.rs:L18-L22`：关键词命中
    代码片段：`use crate::time::{DefaultTimeProvider, TimeProvider}; // FAT implementation based on: //   http://wiki.osdev.org/FAT //   https://www.win.tue.nl/~aeb/linux/fs/fat/fat-1.html`
  - `src/fs/mount.rs:L7-L11`：fn mount_at
    代码片段：`use ourkernel2026_vfs::types::{NodePermission, NodeType}; fn mount_at(path: impl AsRef<Path>, mount_fs: Filesystem<RawMutex>) -> LinuxResult<()> { let path = path.as_ref(); let...`

### 相关符号

`fn mount_at` at `src/fs/mount.rs:L7`、`fn mount_all` at `src/fs/mount.rs:L20`、`fn new_devfs` at `src/fs/imp/dev.rs:L16`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：struct OnceLock, fn get, fn get_mut, fn set, fn get_or_init。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `core/src/task.rs` | L1-L3 | 关键词命中 |
| `src/fs/imp/tmp.rs` | L6-L10 | 关键词命中 |
| `src/fs/dynamic/dynamic.rs` | L2-L6 | 关键词命中 |
| `vendor/once_cell_polyfill/src/sync/mod.rs` | L1-L4 | struct OnceLock |
| `vendor/once_cell_polyfill/src/sync/mod.rs` | L7-L11 | fn get |
| `vendor/once_cell_polyfill/src/sync/mod.rs` | L11-L15 | fn get_mut |

### 关键代码片段

  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`use core::{alloc::Layout, sync::atomic::Ordering}; use crate::ctypes::TimeStat;`
  - `src/fs/imp/tmp.rs:L6-L10`：关键词命中
    代码片段：`use crate::fs::dynamic::dynamic::dummy_stat_fs; use axsync::{Mutex, RawMutex}; use slab::Slab; use ourkernel2026_vfs::fs::{Filesystem, FilesystemOps, StatFs};`
  - `src/fs/dynamic/dynamic.rs:L2-L6`：关键词命中
    代码片段：`use alloc::{borrow::ToOwned, collections::btree_map::BTreeMap, string::String, sync::Arc}; use axsync::{Mutex, RawMutex}; use inherit_methods_macro::inherit_methods; use slab::S...`
  - `vendor/once_cell_polyfill/src/sync/mod.rs:L1-L4`：struct OnceLock
    代码片段：`#[derive(Debug, Clone, Default, Eq, PartialEq)] pub struct OnceLock<T>(std::sync::OnceLock<T>); impl<T> OnceLock<T> {`

### 相关符号

`struct OnceLock` at `vendor/once_cell_polyfill/src/sync/mod.rs:L1`、`fn get` at `vendor/once_cell_polyfill/src/sync/mod.rs:L7`、`fn get_mut` at `vendor/once_cell_polyfill/src/sync/mod.rs:L11`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn dispatch_irq_common, fn register_handler_common, fn handler_irq, fn post_trap_callback, fn handle_syscall。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `arceos/modules/axhal/src/irq.rs` | L1-L3 | 关键词命中 |
| `arceos/modules/axhal/src/trap.rs` | L1-L3 | 关键词命中 |
| `arceos/modules/axhal/src/arch/riscv/trap.S` | L53-L57 | 关键词命中 |
| `arceos/modules/axhal/src/irq.rs` | L15-L19 | fn dispatch_irq_common |
| `arceos/modules/axhal/src/irq.rs` | L27-L31 | fn register_handler_common |
| `arceos/modules/axhal/src/irq.rs` | L37-L41 | fn handler_irq |

### 关键代码片段

  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use handler_table::HandlerTable;`
  - `arceos/modules/axhal/src/trap.rs:L1-L3`：关键词命中
    代码片段：`//! Trap handling. use linkme::distributed_slice as def_trap_handler;`
  - `arceos/modules/axhal/src/arch/riscv/trap.S:L53-L57`：关键词命中
    代码片段：`.global trap_vector_base trap_vector_base: // sscratch == 0: trap from S mode // sscratch != 0: trap from U mode csrrw   sp, sscratch, sp            // swap sscratch and sp`
  - `arceos/modules/axhal/src/irq.rs:L15-L19`：fn dispatch_irq_common
    代码片段：`/// Platform-independent IRQ dispatching. #[allow(dead_code)] pub(crate) fn dispatch_irq_common(irq_num: usize) { trace!("IRQ {}", irq_num); if !IRQ_HANDLER_TABLE.handle(irq_num) {`

### 相关符号

`fn dispatch_irq_common` at `arceos/modules/axhal/src/irq.rs:L15`、`fn register_handler_common` at `arceos/modules/axhal/src/irq.rs:L27`、`fn handler_irq` at `arceos/modules/axhal/src/irq.rs:L37`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：struct Dma, fn new, fn paddr, fn vaddr, fn raw_slice。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `vendor/virtio-drivers/src/hal.rs` | L5-L9 | 关键词命中 |
| `vendor/virtio-drivers/src/lib.rs` | L1-L3 | 关键词命中 |
| `vendor/virtio-drivers/src/queue.rs` | L18-L22 | 关键词命中 |
| `vendor/virtio-drivers/src/hal.rs` | L10-L14 | struct Dma |
| `vendor/virtio-drivers/src/hal.rs` | L29-L33 | fn new |
| `vendor/virtio-drivers/src/hal.rs` | L43-L47 | fn paddr |

### 关键代码片段

  - `vendor/virtio-drivers/src/hal.rs:L5-L9`：关键词命中
    代码片段：`use core::{marker::PhantomData, ptr::NonNull}; /// A physical address as used for virtio. pub type PhysAddr = usize;`
  - `vendor/virtio-drivers/src/lib.rs:L1-L3`：关键词命中
    代码片段：`//! VirtIO guest drivers. //! //! These drivers can be used by bare-metal code (such as a bootloader or OS kernel) running in a VM`
  - `vendor/virtio-drivers/src/queue.rs:L18-L22`：关键词命中
    代码片段：`use zerocopy::{AsBytes, FromBytes, FromZeroes}; /// The mechanism for bulk data transport on virtio devices. /// /// Each device can have zero or more virtqueues.`
  - `vendor/virtio-drivers/src/hal.rs:L10-L14`：struct Dma
    代码片段：`/// A region of contiguous physical memory used for DMA. #[derive(Debug)] pub struct Dma<H: Hal> { paddr: usize, vaddr: NonNull<u8>,`

### 相关符号

`struct Dma` at `vendor/virtio-drivers/src/hal.rs:L10`、`fn new` at `vendor/virtio-drivers/src/hal.rs:L29`、`fn paddr` at `vendor/virtio-drivers/src/hal.rs:L43`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
