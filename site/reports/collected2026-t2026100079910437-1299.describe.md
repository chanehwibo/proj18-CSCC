# 2026 T2026100079910437 项目描述报告

## 基本信息

- 仓库 ID：`collected2026-t2026100079910437-1299`
- 风格：ucore-variant
- 架构：未确认
- 样本来源等级：current_contest_case
- 文件数：630
- 代码/文本行数：73948
- 主要语言：rust 55862 LOC, c 10229 LOC, markdown 3476 LOC, build 2893 LOC, make 683 LOC

## 总览

2026 T2026100079910437 是一个 ucore-variant 风格的小型操作系统相关仓库，主要语言统计为 rust: 55862 LOC, c: 10229 LOC, markdown: 3476 LOC, build: 2893 LOC。仓库包含 630 个已扫描文件、约 73948 行可分析文本，当前抽取到 5875 个符号定义。

## 摘要评分

- 综合成熟度：A 级：机制完整、证据充分（100/100）
- 已确认 OS 维度：7/7；高置信维度：7/7
- 构建入口：已确认；证据健康度：100.0% 覆盖率；无效证据引用：0
- 评分口径：该分数由本地静态分析、源码证据和 self-check 派生，不代表比赛官方评分，也不调用 LLM。

| 评分项 | 得分 | 依据 |
| --- | --- | --- |
| OS 机制覆盖 | 80/80 | 调度、内存、系统调用、文件系统、同步、中断、驱动等维度的确认情况 |
| 构建入口 | 10/10 | 是否识别到 Makefile、Cargo.toml、CMakeLists.txt 等构建入口 |
| 证据健康度 | 10/10 | 关键结论证据覆盖率与无效证据引用数 |

| OS 维度 | 状态 | 置信度 | 证据数 |
| --- | --- | --- | --- |
| 调度与任务管理 | 已确认 | high | 6 |
| 内存管理 | 已确认 | high | 6 |
| 系统调用 | 已确认 | high | 11 |
| 文件系统 | 已确认 | high | 6 |
| 同步机制 | 已确认 | high | 6 |
| 中断与异常 | 已确认 | high | 6 |
| 设备驱动 | 已确认 | high | 6 |

## 构建系统

- 仓库包含构建入口：Cargo.toml, Makefile, api/Cargo.toml, arceos/Cargo.toml, arceos/Makefile。（置信度：high）
  证据：
  - `Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] resolver = "2" members = ["api", "core"]`
  - `Makefile:L1-L3`：构建入口
    代码片段：`# Build Options export ARCH := riscv64 export LOG := warn`
  - `api/Cargo.toml:L1-L3`：构建入口
    代码片段：`[package] name = "starry-api" version.workspace = true`
  - `arceos/Cargo.toml:L1-L3`：构建入口
    代码片段：`[workspace] resolver = "2"`
  - `arceos/Makefile:L1-L3`：构建入口
    代码片段：`# Available arguments: # * General options: #     - 'ARCH': Target architecture: x86_64, riscv64, aarch64, loongarch64`

## 调度与任务管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `scheduler` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含任务/线程管理与调度相关实现。（置信度：high）
  - 相关符号包括：fn new_user_task, fn new_vsched_user_task, struct RobustList, struct RobustListHead, fn handle_futex_death。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `api/src/task.rs` | L13-L17 | 关键词命中 |
| `core/src/task.rs` | L1-L3 | 关键词命中 |
| `core/src/task/stat.rs` | L5-L9 | 关键词命中 |
| `api/src/task.rs` | L29-L33 | fn new_user_task |
| `api/src/task.rs` | L108-L112 | fn new_vsched_user_task |
| `api/src/task.rs` | L157-L161 | struct RobustList |

### 关键代码片段

  - `api/src/task.rs:L13-L17`：关键词命中
    代码片段：`futex::FutexKey, shm::SHM_MANAGER, task::{ AsThread, get_process_data, get_task, send_signal_to_process, send_signal_to_thread, set_timer_state,`
  - `core/src/task.rs:L1-L3`：关键词命中
    代码片段：`//! User task management. mod stat;`
  - `core/src/task/stat.rs:L5-L9`：关键词命中
    代码片段：`use starry_signal::Signo; use crate::task::AsThread; /// Represents the '/proc/[pid]/stat' file.`
  - `api/src/task.rs:L29-L33`：fn new_user_task
    代码片段：`/// Create a new user task. pub fn new_user_task(name: &str, mut uctx: UserContext, set_child_tid: usize) -> TaskInner { TaskInner::new( move || {`

### 相关符号

`fn new_user_task` at `api/src/task.rs:L29`、`fn new_vsched_user_task` at `api/src/task.rs:L108`、`struct RobustList` at `api/src/task.rs:L157`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 内存管理

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `memory` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含页表、物理页、虚拟内存或堆分配等内存管理实现。（置信度：high）
  - 相关符号包括：fn check_region, fn check_null_terminated, struct UserPtr, fn from, fn default。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `api/src/mm.rs` | L66-L70 | 关键词命中 |
| `core/src/mm.rs` | L44-L48 | 关键词命中 |
| `api/src/syscall/mm/brk.rs` | L13-L17 | 关键词命中 |
| `api/src/mm.rs` | L19-L23 | fn check_region |
| `api/src/mm.rs` | L39-L43 | fn check_null_terminated |
| `api/src/mm.rs` | L93-L97 | struct UserPtr |

### 关键代码片段

  - `api/src/mm.rs:L66-L70`：关键词命中
    代码片段：`// TODO: this is inefficient, but we have to do this instead of // querying the page table since the page might has not been // allocated yet. let curr = current();`
  - `core/src/mm.rs:L44-L48`：关键词命中
    代码片段：`#[cfg(not(any(target_arch = "aarch64", target_arch = "loongarch64")))] { // ARMv8 (aarch64) and LoongArch64 use separate page tables for user space // (aarch64: TTBR0_EL1, Loong...`
  - `api/src/syscall/mm/brk.rs:L13-L17`：关键词命中
    代码片段：`let proc_data = &curr.as_thread().proc_data; let current_top = proc_data.get_heap_top() as usize; let heap_limit = USER_HEAP_BASE + USER_HEAP_SIZE_MAX; if addr == 0 {`
  - `api/src/mm.rs:L19-L23`：fn check_region
    代码片段：`use starry_vm::{vm_load_until_nul, vm_read_slice, vm_write_slice}; fn check_region(start: VirtAddr, layout: Layout, access_flags: MappingFlags) -> AxResult<()> { let align = lay...`

### 相关符号

`fn check_region` at `api/src/mm.rs:L19`、`fn check_null_terminated` at `api/src/mm.rs:L39`、`struct UserPtr` at `api/src/mm.rs:L93`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 系统调用

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `syscall` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含系统调用入口、编号或分发逻辑。（置信度：high）
  - 相关符号包括：fn handle_syscall, fn sys_getuid, fn sys_geteuid, fn sys_getgid, fn sys_getegid。（置信度：medium）
  - 静态识别到 249 个系统调用相关符号。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `api/src/syscall/mod.rs` | L20-L24 | 关键词命中 |
| `api/src/syscall/fs/ctl.rs` | L100-L104 | 关键词命中 |
| `api/src/syscall/net/io.rs` | L14-L18 | 关键词命中 |
| `api/src/syscall/mod.rs` | L20-L24 | fn handle_syscall |
| `api/src/syscall/sys.rs` | L12-L16 | fn sys_getuid |
| `api/src/syscall/sys.rs` | L16-L20 | fn sys_geteuid |

### 关键代码片段

  - `api/src/syscall/mod.rs:L20-L24`：关键词命中
    代码片段：`}; pub fn handle_syscall(uctx: &mut UserContext) { if uctx.sysno() == starry_core::vsched::context::VSCHED2_INTO_KERNEL_SYSNO { debug!("vsched2 into_kernel ecall intercepted, ro...`
  - `api/src/syscall/fs/ctl.rs:L100-L104`：关键词命中
    代码片段：`} // Directory buffer for getdents64 syscall struct DirBuffer { buf: Vec<u8>,`
  - `api/src/syscall/net/io.rs:L14-L18`：关键词命中
    代码片段：`mm::{UserConstPtr, UserPtr, VmBytes, VmBytesMut}, socket::SocketAddrExt, syscall::net::{CMsg, CMsgBuilder}, };`
  - `api/src/syscall/mod.rs:L20-L24`：fn handle_syscall
    代码片段：`}; pub fn handle_syscall(uctx: &mut UserContext) { if uctx.sysno() == starry_core::vsched::context::VSCHED2_INTO_KERNEL_SYSNO { debug!("vsched2 into_kernel ecall intercepted, ro...`

### 相关符号

`fn handle_syscall` at `api/src/syscall/mod.rs:L20`、`fn sys_getuid` at `api/src/syscall/sys.rs:L12`、`fn sys_geteuid` at `api/src/syscall/sys.rs:L16`、`fn handle_syscall` at `api/src/syscall/mod.rs:L20`、`fn sys_getuid` at `api/src/syscall/sys.rs:L12`、`fn sys_geteuid` at `api/src/syscall/sys.rs:L16`、`fn sys_getgid` at `api/src/syscall/sys.rs:L20`、`fn sys_getegid` at `api/src/syscall/sys.rs:L24`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 文件系统

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `filesystem` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含文件系统、VFS、inode、目录项或文件读写相关实现。（置信度：high）
  - 相关符号包括：struct DummyFd, fn path, fn poll, fn register, fn sys_dummy_fd。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `api/src/syscall/fs/mount.rs` | L4-L8 | 关键词命中 |
| `api/src/syscall/fs/fd_ops.rs` | L12-L16 | 关键词命中 |
| `arceos/modules/axfs/src/fs/mod.rs` | L1-L3 | 关键词命中 |
| `api/src/syscall/fs/io.rs` | L20-L24 | struct DummyFd |
| `api/src/syscall/fs/io.rs` | L22-L26 | fn path |
| `api/src/syscall/fs/io.rs` | L27-L31 | fn poll |

### 关键代码片段

  - `api/src/syscall/fs/mount.rs:L4-L8`：关键词命中
    代码片段：`use axfs::FS_CONTEXT; use crate::{mm::vm_load_string, vfs::MemoryFs}; pub fn sys_mount(`
  - `api/src/syscall/fs/fd_ops.rs:L12-L16`：关键词命中
    代码片段：`use bitflags::bitflags; use linux_raw_sys::general::*; use starry_core::{task::AsThread, vfs::Device}; use crate::{`
  - `arceos/modules/axfs/src/fs/mod.rs:L1-L3`：关键词命中
    代码片段：`#[cfg(feature = "fat")] mod fat;`
  - `api/src/syscall/fs/io.rs:L20-L24`：struct DummyFd
    代码片段：`}; struct DummyFd; impl FileLike for DummyFd { fn path(&self) -> Cow<'_, str> {`

### 相关符号

`struct DummyFd` at `api/src/syscall/fs/io.rs:L20`、`fn path` at `api/src/syscall/fs/io.rs:L22`、`fn poll` at `api/src/syscall/fs/io.rs:L27`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 同步机制

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `sync` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含锁、信号量或原子操作等同步机制。（置信度：high）
  - 相关符号包括：fn assert_unsigned, fn sys_futex, fn sys_get_robust_list, fn sys_set_robust_list, fn sys_membarrier。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `api/src/syscall/sync/futex.rs` | L1-L3 | 关键词命中 |
| `api/src/syscall/sync/membarrier.rs` | L1-L3 | 关键词命中 |
| `core/src/mm.rs` | L13-L17 | 关键词命中 |
| `api/src/syscall/sync/futex.rs` | L15-L19 | fn assert_unsigned |
| `api/src/syscall/sync/futex.rs` | L23-L27 | fn sys_futex |
| `api/src/syscall/sync/futex.rs` | L119-L123 | fn sys_get_robust_list |

### 关键代码片段

  - `api/src/syscall/sync/futex.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::Ordering; use axerrno::{AxError, AxResult, LinuxError};`
  - `api/src/syscall/sync/membarrier.rs:L1-L3`：关键词命中
    代码片段：`use core::sync::atomic::{Ordering, compiler_fence}; use axerrno::{AxError, AxResult};`
  - `core/src/mm.rs:L13-L17`：关键词命中
    代码片段：`}; use axmm::{AddrSpace, backend::Backend}; use axsync::Mutex; use axtask::current; use extern_trait::extern_trait;`
  - `api/src/syscall/sync/futex.rs:L15-L19`：fn assert_unsigned
    代码片段：`use crate::time::TimeValueLike; fn assert_unsigned(value: u32) -> AxResult<u32> { if (value as i32) < 0 { Err(AxError::InvalidInput)`

### 相关符号

`fn assert_unsigned` at `api/src/syscall/sync/futex.rs:L15`、`fn sys_futex` at `api/src/syscall/sync/futex.rs:L23`、`fn sys_get_robust_list` at `api/src/syscall/sync/futex.rs:L119`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 中断与异常

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `interrupt` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含 trap、中断、异常或定时器处理逻辑。（置信度：high）
  - 相关符号包括：fn register_irq_hook, fn irq_handler, fn on_timer_tick, fn handle_irq, fn handle_user_irq。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `arceos/modules/axhal/src/irq.rs` | L1-L3 | 关键词命中 |
| `arceos/modules/trampoline/src/trap_api.rs` | L2-L6 | 关键词命中 |
| `api/src/mm.rs` | L11-L15 | 关键词命中 |
| `arceos/modules/axhal/src/irq.rs` | L17-L21 | fn register_irq_hook |
| `arceos/modules/axhal/src/irq.rs` | L34-L38 | fn irq_handler |
| `arceos/modules/trampoline/src/trap_api.rs` | L7-L11 | fn on_timer_tick |

### 关键代码片段

  - `arceos/modules/axhal/src/irq.rs:L1-L3`：关键词命中
    代码片段：`//! Interrupt management. use core::sync::atomic::{AtomicUsize, Ordering};`
  - `arceos/modules/trampoline/src/trap_api.rs:L2-L6`：关键词命中
    代码片段：`use asynctask::{TrapFrame, TrapStatus}; #[cfg(feature = "irq")] #[doc(cfg(feature = "irq"))] /// Handles periodic timer ticks for the task manager.`
  - `api/src/mm.rs:L11-L15`：关键词命中
    代码片段：`use axhal::{ paging::MappingFlags, trap::{PAGE_FAULT, register_trap_handler}, }; use axio::prelude::*;`
  - `arceos/modules/axhal/src/irq.rs:L17-L21`：fn register_irq_hook
    代码片段：`/// /// TODO: design a better api! pub fn register_irq_hook(hook: fn(usize)) -> bool { IRQ_HOOK .compare_exchange(`

### 相关符号

`fn register_irq_hook` at `arceos/modules/axhal/src/irq.rs:L17`、`fn irq_handler` at `arceos/modules/axhal/src/irq.rs:L34`、`fn on_timer_tick` at `arceos/modules/trampoline/src/trap_api.rs:L7`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 设备驱动

- 结论：已确认该维度存在可追溯实现线索。（综合置信度：high）
- 分析口径：本维度主要关注 `driver` 相关的源码路径、符号定义和关键词证据；文档命中不直接作为实现证据。
- 设计判断：
  - 项目包含串口、块设备、控制台、中断控制器或 virtio 等设备驱动相关实现。（置信度：high）
  - 相关符号包括：trait VirtIoDevMeta, fn try_new, struct VirtIoNet, struct VirtIoBlk, struct VirtIoGpu。（置信度：medium）

### 证据表

| 文件 | 行号 | 说明 |
| --- | --- | --- |
| `arceos/modules/axdriver/src/virtio.rs` | L18-L22 | 关键词命中 |
| `arceos/modules/axdriver/src/drivers.rs` | L9-L13 | 关键词命中 |
| `arceos/modules/axdriver/src/dyn_drivers/mod.rs` | L16-L20 | 关键词命中 |
| `arceos/modules/axdriver/src/virtio.rs` | L19-L23 | trait VirtIoDevMeta |
| `arceos/modules/axdriver/src/virtio.rs` | L25-L29 | fn try_new |
| `arceos/modules/axdriver/src/virtio.rs` | L30-L34 | struct VirtIoNet |

### 关键代码片段

  - `arceos/modules/axdriver/src/virtio.rs:L18-L22`：关键词命中
    代码片段：`} /// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`
  - `arceos/modules/axdriver/src/drivers.rs:L9-L13`：关键词命中
    代码片段：`pub use super::dummy::*; use crate::AxDeviceEnum; #[cfg(feature = "virtio")] use crate::virtio::{self, VirtIoDevMeta};`
  - `arceos/modules/axdriver/src/dyn_drivers/mod.rs:L16-L20`：关键词命中
    代码片段：`#[cfg(feature = "block")] pub mod blk; /// Sets up the device driver subsystem.`
  - `arceos/modules/axdriver/src/virtio.rs:L19-L23`：trait VirtIoDevMeta
    代码片段：`/// A trait for VirtIO device meta information. pub trait VirtIoDevMeta { const DEVICE_TYPE: DeviceType;`

### 相关符号

`trait VirtIoDevMeta` at `arceos/modules/axdriver/src/virtio.rs:L19`、`fn try_new` at `arceos/modules/axdriver/src/virtio.rs:L25`、`struct VirtIoNet` at `arceos/modules/axdriver/src/virtio.rs:L30`

### 复核建议

- 建议人工检查上述代码片段所在文件，确认关键词命中是否确实对应完整机制，而不是仅出现名称或注释。

## 附录：核验摘要

- 关键结论数：16
- 含证据关键结论数：16（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
