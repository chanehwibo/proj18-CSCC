# 2026 T202610459999656 比较报告

- 对比历史仓库：2022 FTL OS
- 生成时间：2026-07-02T05:01:01.715270+00:00
- 参考库边界：只有带官方来源的 `verified_award` 样本才会被视为获奖案例；未核验比赛样本不作为特奖/一等奖背书。

## 历史样本选择

- 2022 FTL OS（来源：赛事历史作品）：画像相似度 score=9.86；同属 rcore-variant 风格; 语言构成相似度 0.97; OS 维度重合度 1.00; 代码规模接近度 0.92

## 功能重合与疑似重复证据

本节只说明功能维度和实现线索的重合，不直接判定代码抄袭。是否构成代码级重复，需要结合完整代码、提交历史和人工复核进一步确认。

- 与 2022 FTL OS 在“设备驱动”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/drivers.rs:L5-L7`：关键词命中
    代码片段：`pub mod block; pub mod platform; pub mod virtio;`
  - `os/src/net/device.rs:L7-L11`：关键词命中
    代码片段：`use smoltcp::phy::{self, DeviceCapabilities, Medium}; use smoltcp::time::Instant; use virtio_drivers::device::net::RxBuffer; use crate::drivers::virtio::net::VirtioNetDevice;`
  - `code/kernel/src/console.rs:L13-L17`：关键词命中
    代码片段：`pub fn init() { ftl_util::console::init(print); }`
  - `code/kernel/src/drivers/mod.rs:L4-L8`：关键词命中
    代码片段：`// mod blockdev; pub use block::device; pub use ftl_util::device::BlockDevice;`
- 与 2022 FTL OS 在“文件系统”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/fs/pty.rs:L9-L13`：关键词命中
    代码片段：`fs::{ file_handle::{FileLike, PollEvents}, utils::{AccessMode, InodeMode, InodeType, Metadata, StatusFlags, SuperBlock, NAME_MAX}, }, hal::VirtAddr,`
  - `os/src/fs/ext4.rs:L38-L42`：关键词命中
    代码片段：`fs::utils::{ CreateNode, CreateOwner, DirSink, FileAttributeFlags, FileAttributes, FileSystem, FsFlags, Inode, InodeMode, InodeType, Metadata, StatxAttributes, StatxMetadata, Su...`
  - `code/vfs/src/fssp/mod.rs:L14-L18`：关键词命中
    代码片段：`use crate::{ dentry::{DentryCache, DentryFsspNode}, inode::{FsInode, InodeFsspNode, VfsInode}, manager::{VfsClock, VfsSpawner},`
  - `code/kernel/src/fs/mod.rs:L14-L18`：关键词命中
    代码片段：`time::Instant, }; use vfs::{select::PL, DevAlloc, File, FsInode, VfsClock, VfsFile, VfsManager, VfsSpawner}; use crate::{`
- 与 2022 FTL OS 在“中断与异常”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/hal/irq.rs:L8-L12`：关键词命中
    代码片段：`#[inline] pub fn enable_local() { arch::irq::enable_local(); }`
  - `os/src/hal/trap.rs:L13-L17`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug)] pub enum InterruptKind { Timer, External, Software,`
  - `code/kernel/src/trap/mod.rs:L13-L17`：关键词命中
    代码片段：`mod kernel_interrupt; core::arch::global_asm!(include_str!("trap.S")); pub fn init() {`
  - `code/kernel/src/timer/mod.rs:L19-L23`：关键词命中
    代码片段：`#[derive(Clone, Copy)] pub struct ITimerval { it_interval: TimeVal, // Interval for periodic timer it_value: TimeVal,    // Time until next expiration }`
- 与 2022 FTL OS 在“内存管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/mm.rs:L11-L15`：关键词命中
    代码片段：`pub mod page_fault_handler; pub mod page_prop; pub mod page_table; pub mod reclaim; pub mod status;`
  - `os/src/mm/init.rs:L9-L13`：关键词命中
    代码片段：`meta::init as init_frame_metadata, }, mm::{kspace::init_kernel_page_table, page_table::boot_pt}, };`
  - `code/kernel/src/memory/mod.rs:L4-L8`：关键词命中
    代码片段：`pub mod auxv; pub mod map_segment; mod page_table; pub mod rcu; pub mod user_ptr;`
  - `code/kernel/src/memory/asid.rs:L7-L11`：关键词命中
    代码片段：`hart::csr, impl_usize_from, local, memory::{address::VirAddr, page_table::PageTable}, sync::mutex::SpinNoIrqLock, tools::container::{never_clone_linked_list::NeverCloneLinkedLis...`
- 与 2022 FTL OS 在“调度与任务管理”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/thread.rs:L12-L16`：关键词命中
    代码片段：`use crate::{ fs::fs_context::{FsCloneFlags, FsContext}, hal::{task::TaskContext, HartId, HartMask}, mm::heap::HeapAllocError, process::Process,`
  - `os/src/hal/task.rs:L14-L18`：关键词命中
    代码片段：`/// - 可以通过 ['TaskContext::set_stack_pointer'] 与 ['TaskContext::set_instruction_pointer'] ///   初始化一个“首次运行”的内核线程。 pub use arch::task::TaskContext; /// 在两个任务上下文之间切换。`
  - `code/kernel/src/fs/proc/mod.rs:L23-L27`：关键词命中
    代码片段：`impl FsType for ProcType { fn name(&self) -> String { "proc".to_string() } fn new_fs(&self, _dev: usize) -> Box<dyn Fs> {`
  - `code/kernel/src/process/thread.rs:L4-L8`：关键词命中
    代码片段：`pin::Pin, sync::atomic::{AtomicI32, AtomicUsize, Ordering}, task::{Context, Poll}, };`
- 与 2022 FTL OS 在“同步机制”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/sync.rs:L2-L6`：关键词命中
    代码片段：`pub mod guard; pub mod mutex; pub mod once; pub mod rcu;`
  - `os/src/sync/rcu.rs:L7-L11`：关键词命中
    代码片段：`mem::ManuallyDrop, ops::Deref, sync::atomic::{AtomicBool, Ordering}, };`
  - `code/kernel/src/sync/mod.rs:L4-L8`：关键词命中
    代码片段：`pub mod even_bus; pub mod mutex; pub type SleepMutex<T> = ftl_util::sync::sleep_mutex::SleepMutex<T, SpinNoIrq>;`
  - `code/ftl-util/src/sync/mod.rs:L2-L6`：关键词命中
    代码片段：`pub mod rw_sleep_mutex; pub mod rw_spin_mutex; pub mod semaphore; pub mod seq_mutex; pub mod sleep_mutex;`
- 与 2022 FTL OS 在“系统调用”维度存在功能重合：双方均有可追溯源码证据，属于需要重点复核的相似实现线索。（置信度：medium）
  证据：
  - `os/src/syscall.rs:L163-L167`：关键词命中
    代码片段：`} /// 验证 message-header 坏地址在 syscall 边界返回 'EFAULT'。 #[cfg(feature = "ktest")] pub(crate) fn ktest_socket_message_badaddr_contract() -> crate::Result<()> {`
  - `os/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! syscall：文件系统相关分组 extern crate alloc;`
  - `code/kernel/src/syscall/mod.rs:L112-L116`：关键词命中
    代码片段：`const SYSCALL_MEMBARRIER: usize = 283; pub struct Syscall<'a> { cx: &'a mut UKContext, thread: &'a Thread,`
  - `code/kernel/src/syscall/net.rs:L19-L23`：关键词命中
    代码片段：`}; use super::Syscall; const PRINT_SYSCALL_NET: bool = true && PRINT_SYSCALL || PRINT_SYSCALL_ALL;`

## 代码级相似线索检测

本节从文件路径、函数/符号名、结构体/宏名和 evidence 片段 token/结构相似度四个层面输出可复核线索，并优先保留高价值代表项。该结果比功能重合更接近代码级分析，但仍不是抄袭裁定。

- 函数/符号名重合：与 2022 FTL OS 在“文件系统”维度发现 1 个同名定义：new。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/drivers/virtio/blk/queue.rs:L136-L136`：fn new
    代码片段：`fn new(dma: Arc<DmaPages>, offset: usize, bytes: usize) -> Self {`
  - `code/vfs/src/hash_name.rs:L51-L51`：fn new
    代码片段：`pub fn new(parent: *const Dentry, name: &str) -> Self {`
- 函数/符号名重合：与 2022 FTL OS 在“内存管理”维度发现 1 个同名定义：alloc。该结果表示命名和局部结构存在可复核相似线索，不直接表示代码复制。（置信度：medium）
  证据：
  - `os/src/drivers/block/bio.rs:L401-L401`：fn alloc
    代码片段：`pub fn alloc(nblocks: usize, direction: BioDirection) -> Self {`
  - `code/vfs/src/manager/mod.rs:L39-L39`：fn alloc
    代码片段：`fn alloc(&self) -> usize;`
- 文件路径重合：与 2022 FTL OS 在“设备驱动”维度出现同名文件源码路径 `os/src/hal/console.rs` / `code/kernel/src/console.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/hal/console.rs:L18-L22`：关键词命中
    代码片段：`pub fn init_from_boot() { CONSOLE_BACKEND.call_once(|| { if let Some(u) = boot_info().uart { let mmio_base = arch::mm::phys_to_virt_io(u.base).as_usize(); // SAFETY: 'mmio_base'...`
  - `code/kernel/src/console.rs:L13-L17`：关键词命中
    代码片段：`pub fn init() { ftl_util::console::init(print); }`
- 文件路径重合：与 2022 FTL OS 在“调度与任务管理”维度出现同名文件源码路径 `os/src/thread.rs` / `code/kernel/src/process/thread.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/thread.rs:L12-L16`：关键词命中
    代码片段：`use crate::{ fs::fs_context::{FsCloneFlags, FsContext}, hal::{task::TaskContext, HartId, HartMask}, mm::heap::HeapAllocError, process::Process,`
  - `code/kernel/src/process/thread.rs:L4-L8`：关键词命中
    代码片段：`pin::Pin, sync::atomic::{AtomicI32, AtomicUsize, Ordering}, task::{Context, Poll}, };`
- 文件路径重合：与 2022 FTL OS 在“调度与任务管理”维度出现同名文件源码路径 `os/src/thread.rs` / `code/kernel/src/syscall/thread.rs`。这说明实现位置或文件命名存在可复核相似线索，不等同于代码重复裁定。（置信度：medium）
  证据：
  - `os/src/thread.rs:L12-L16`：关键词命中
    代码片段：`use crate::{ fs::fs_context::{FsCloneFlags, FsContext}, hal::{task::TaskContext, HartId, HartMask}, mm::heap::HeapAllocError, process::Process,`
  - `code/kernel/src/syscall/thread.rs:L3-L7`：关键词命中
    代码片段：`impl Syscall<'_> { pub fn sys_gettid(&mut self) -> SysRet { Ok(self.thread.tid().0) } pub fn sys_membarrier(&mut self) -> SysRet {`

## 相似点

- 与 2022 FTL OS 同属 rcore-variant 风格。（置信度：medium）
- 与 2022 FTL OS 在“设备驱动”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/drivers.rs:L5-L7`：关键词命中
    代码片段：`pub mod block; pub mod platform; pub mod virtio;`
  - `os/src/net/device.rs:L7-L11`：关键词命中
    代码片段：`use smoltcp::phy::{self, DeviceCapabilities, Medium}; use smoltcp::time::Instant; use virtio_drivers::device::net::RxBuffer; use crate::drivers::virtio::net::VirtioNetDevice;`
- 与 2022 FTL OS 在“文件系统”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/fs/pty.rs:L9-L13`：关键词命中
    代码片段：`fs::{ file_handle::{FileLike, PollEvents}, utils::{AccessMode, InodeMode, InodeType, Metadata, StatusFlags, SuperBlock, NAME_MAX}, }, hal::VirtAddr,`
  - `os/src/fs/ext4.rs:L38-L42`：关键词命中
    代码片段：`fs::utils::{ CreateNode, CreateOwner, DirSink, FileAttributeFlags, FileAttributes, FileSystem, FsFlags, Inode, InodeMode, InodeType, Metadata, StatxAttributes, StatxMetadata, Su...`
- 与 2022 FTL OS 在“中断与异常”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/hal/irq.rs:L8-L12`：关键词命中
    代码片段：`#[inline] pub fn enable_local() { arch::irq::enable_local(); }`
  - `os/src/hal/trap.rs:L13-L17`：关键词命中
    代码片段：`#[derive(Clone, Copy, Debug)] pub enum InterruptKind { Timer, External, Software,`
- 与 2022 FTL OS 在“内存管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/mm.rs:L11-L15`：关键词命中
    代码片段：`pub mod page_fault_handler; pub mod page_prop; pub mod page_table; pub mod reclaim; pub mod status;`
  - `os/src/mm/init.rs:L9-L13`：关键词命中
    代码片段：`meta::init as init_frame_metadata, }, mm::{kspace::init_kernel_page_table, page_table::boot_pt}, };`
- 与 2022 FTL OS 在“调度与任务管理”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/thread.rs:L12-L16`：关键词命中
    代码片段：`use crate::{ fs::fs_context::{FsCloneFlags, FsContext}, hal::{task::TaskContext, HartId, HartMask}, mm::heap::HeapAllocError, process::Process,`
  - `os/src/hal/task.rs:L14-L18`：关键词命中
    代码片段：`/// - 可以通过 ['TaskContext::set_stack_pointer'] 与 ['TaskContext::set_instruction_pointer'] ///   初始化一个“首次运行”的内核线程。 pub use arch::task::TaskContext; /// 在两个任务上下文之间切换。`
- 与 2022 FTL OS 在“同步机制”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/sync.rs:L2-L6`：关键词命中
    代码片段：`pub mod guard; pub mod mutex; pub mod once; pub mod rcu;`
  - `os/src/sync/rcu.rs:L7-L11`：关键词命中
    代码片段：`mem::ManuallyDrop, ops::Deref, sync::atomic::{AtomicBool, Ordering}, };`
- 与 2022 FTL OS 在“系统调用”维度均有可确认实现。（置信度：medium）
  证据：
  - `os/src/syscall.rs:L163-L167`：关键词命中
    代码片段：`} /// 验证 message-header 坏地址在 syscall 边界返回 'EFAULT'。 #[cfg(feature = "ktest")] pub(crate) fn ktest_socket_message_badaddr_contract() -> crate::Result<()> {`
  - `os/src/syscall/fs.rs:L1-L3`：关键词命中
    代码片段：`//! syscall：文件系统相关分组 extern crate alloc;`
- 与 2022 FTL OS 在 driver, filesystem, interrupt, memory, scheduler, sync, syscall 等维度均有可确认实现，可作为进一步人工复核重点。（置信度：medium）

## 差异点

- 与 2022 FTL OS 的语言构成不同：待测作品为 {'build': 6236, 'markdown': 11526, 'toml': 84, 'rust': 588336, 'asm': 532, 'text': 6096, 'json': 249, 'yaml': 60, 'make': 52, 'c': 119018}，历史样本为 {'json': 98, 'markdown': 14400, 'text': 1537, 'build': 6491, 'c': 149046, 'rust': 624277, 'asm': 1721, 'make': 29, 'toml': 235, 'yaml': 51}。（置信度：medium）

## 可能创新点

- 待测作品包含 339 个历史样本 2022 FTL OS 中未见的独特函数/类型定义（如 `stop_and_requeue_front`, `probe_mmio_network`, `shared_desc_from_output`, `submit`, `complete_used`, `probe_mmio_blocks`），可能体现独立设计或扩展实现。这是命名级线索，需结合源码语义确认是否构成创新。（置信度：low）
  证据：
  - `os/src/drivers/virtio/blk/core.rs:L127-L127`：fn stop_and_requeue_front
    代码片段：`fn stop_and_requeue_front(&mut self, request: DispatchRequest, resource_release_epoch: usize) {`
  - `os/src/drivers/virtio/net/probe.rs:L157-L157`：fn probe_mmio_network
    代码片段：`fn probe_mmio_network(boot: &crate::hal::BootInfo) -> Result<Option<(Nic, u32)>, ProbeError> {`

## 附录：核验摘要

- 关键结论数：20
- 含证据关键结论数：20（100.0%）
- 无效证据引用数：0
- 未确认关键结论数：0
- 统计口径：关键结论指需要源码证据支撑的设计判断；语言构成、风格标签和汇总性描述不计入证据率。
