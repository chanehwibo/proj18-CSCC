[project]
name = "kernelsage"
version = "0.1.0"
description = "Agent system for describing and comparing education-oriented OS kernels."
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["src"]

